---
name: plan-task
description: Спланировать ОБЫЧНУЮ задачу/баг/идею в один операционный план — вектор цели, критерии приёмки, шаги с чекбоксами, верификация наблюдением, риски — так, чтобы церемония не перевешивала работу. Сначала прогоняет тест тяжести и ТЯЖЁЛУЮ задачу передаёт /plan-epic (полная лестница research → мета-план → фазы). Используй, когда владелец говорит «спланируй задачу», «составь план по этому багу/идее», «как подойдёшь к этому», «plan this task», или когда агент берёт неспланированный пункт беклога; для эпиков — /plan-epic.
---

# /plan-task — один операционный план для обычной задачи

Неспланированная задача исполняется импровизацией, а импровизация не переживает теряющую контекст
сессию. ОБЫЧНОЙ задаче положен ровно ОДИН артефакт: короткий операционный план, который свежая
сессия может исполнить и по которому судится работа. Без лестницы и фаз — это территория
`/plan-epic`, и тащить церемонию эпика на маленькую задачу так же неправильно, как пропустить
планирование на большой.

## Шаг 0 — тест тяжести (канон: AGENT_GUIDE.md → «Дисциплина планирования»)

Задача ТЯЖЁЛАЯ, если выполняются **≥2** из:

- трогает ≥3 подсистемы или канон-документа;
- опирается на внешнюю правду или индустриальный стандарт;
- не помещается в одну сессию;
- меняет состав поставки или публичные контракты;
- требует решений владельца.

ТЯЖЁЛАЯ → стоп, переключайся на **`/plan-epic`** (скажи об этом одной строкой в чате). Иначе — дальше.

## Шаг 1 — сбор (минуты, не часы)

- Исходный документ (`ideas/NN`, `bugs/NN` или просьба владельца дословно).
- Релевантный срез карты (`PROJECT_ARCHITECTURE_INTERNAL_MAP.md` — blast radius).
- Греп `EXPERIENCE.md` по тегам задачи — процитируй релевантные уроки или скажи «нет таких».
- Задача опирается на внешнюю правду — сначала разведдок (чек-лист, шаг 8); планировать по памяти
  значит выдумывать.

## Шаг 2 — написать план

Структура (по возможности — один экран):

```
## План: <цель одной строкой>
**Вектор цели:** <какую боль решаем и где хотим оказаться — называй Achieve / Maintain / Avoid, где это проясняет>
**Критерии приёмки (готово, когда):** <наблюдаемые критерии — что будет ВИДНО работающим, не «код написан»; числовой критерий несёт fit criterion: Scale · Meter · Target — или по одному четырёхстрочному сценарию на критерий (REQUIREMENTS_FRAMEWORK.md → «Сценарная форма»):>
- Ситуация. <состояние мира с конкретными значениями — не действие>
- Действие. <ровно одно действие пользователя, системы или агента>
- Результат. <что ВИДНО снаружи: число, строка вывода, файл — никогда «работает корректно»>
- Проверка. <команда или запрос репозитория + ожидаемый выход; Scale · Meter · Target живут здесь>
**Шаги:**
- [ ] <шаг — достаточно мелкий, чтобы проверяться сам по себе>
- [ ] ...
**Верификация:** <КАКИМИ ТЕСТОВЫМИ АРТЕФАКТАМИ проверяется каждое утверждение И ГДЕ ОНИ ЖИВУТ —
свод/кейс/фикстура/страж и его путь в репозитории; артефакты производит ЭТОТ ЖЕ план, теми же
шагами, а не «потом» (TESTING_FRAMEWORK.md → «Работа производит и средство своей проверки»)>
**Риски:** <топ-1..3, у каждого — реакция на срабатывание (ранжирование Мерфи из PHILOSOPHY.md)>
```

Блок «Вектор цели + Критерии приёмки» ОТКРЫВАЕТ план и пишется по
`REQUIREMENTS_FRAMEWORK.md` (проверяемые формулировки, без стоп-слов, fit criteria). Вектор и
критерии — не истины в последней инстанции: они модифицируются, добавляются и удаляются по мере
работы; правка вектора — это правка, а не провал.

Размещение: план совсем маленькой задачи живёт **секцией в её документе идеи/бага**; покрупнее —
своим `plans/NN_<имя>.md`. В обоих случаях план коммитится до начала работы
(`node tools/commit.mjs "<msg>"`).

**Планируешь ФАЗУ эпика?** Тогда этот план — ребёнок того эпика, и он объявляет родителя дважды:
**именем файла** — `NN_epicMM_<фаза>_<имя>.md`, где `MM` — номер родительского эпика
(`plans/README.md` → «Именование»), — и **шагами**, каждый из которых цитирует строку-якорь
мета-плана, которую исполняет. Имя связывает семью; цитата доказывает, что шаг в скоупе. Шаг,
который не удаётся заякорить в родителе, — дрейф скоупа, пойманный до диффа.

## Шаг 3 — допуск, затем вперёд

- План задевает территорию владельца (бренд, UX, архитектура, канон)? Сначала подними развилку —
  один точечный вопрос в чате для двусмысленности уровня задачи, `/interview` для уровня видения.
- Иначе — исполняй сразу (fable-цикл, чек-лист шаг 6): план — стоячая авторизация на собственные
  обратимые шаги.

## Что навык ЗАПРЕЩАЕТ

- Планировать эпик плоским списком шагов (тест тяжести ловит дрейф скоупа на планировании, а не
  посреди исполнения).
- План без критериев приёмки и верификации — «шаги сделаны» ≠ «задача сделана»
  (`TESTING_FRAMEWORK.md`: сырому доверия нет).
- Пропустить план, потому что «задача понятна» — понятна ЭТОЙ сессии; план пишется для следующей.
