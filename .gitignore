# Dependencies
node_modules/
tools/node_modules/
package-lock.json
tools/package-lock.json

# OS
.DS_Store

# Logs / temp / scratch
*.log
*.stackdump
scratchpad/

# Интерактивный контур (K5): транзиенты — замки окон и временные рендеры;
# сами решения (decisions/*.json + archive/) — коммитимая машинная память.
interviews/decisions/*.lock
tools/.review-tmp/

# Note: README.pdf and FRAMEWORK.md are committed deliverables (generated, but tracked).
