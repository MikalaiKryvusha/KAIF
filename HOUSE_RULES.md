# KAIF — домашние правила истока (HOUSE_RULES)

> **Что это.** Местный закон ЭТОГО репозитория — ярус 4 таксономии документов (`AGENT_GUIDE.md` → «Таксономия документов»): справочные
> таблицы и соглашения истока, которые действуют здесь и никуда не едут (в поставку не входят). **В ядро перечитывания файл НЕ входит** —
> открывай по контекст-роутеру: нужен инструмент или страж (§1) · закрываешь чат или выпускаешь версию — прогони реестр пар (§2) ·
> показываешь что-то владельцу (§3) · опираешься на факт о машине (§4) · работаешь с портретом голоса владельца (§5).
>
> **Откуда.** Заведён 2026-09-18 10:51 +03:00 (сессия 66, эпик CB «Бюджет канона», `plans/95` критерий 4 — исток проходит собственный гейт бюджета):
> разделы перенесены ДОСЛОВНО из `AGENT_GUIDE.md` (был 1314 строк при бюджете 1200) и `STATUS.md` (300 при 200); правила остались в гиде,
> сюда уехали таблицы. Решение `[ИИ]` — адрес выноса назван самим каноном («летопись, `researches/`, файл домашних правил»).
> Правь ЗДЕСЬ; новый инструмент, новая пара, новая команда контура вписываются сюда в день рождения.

---

## 1. Инструменты истока

### 1а. Из `AGENT_GUIDE.md` → «Инструменты» (что делает каждая команда)

| Команда | Что делает |
|---------|--------------|
| `node tools/build-framework.mjs` | Регенерирует `KAIF.md` из `framework/`. |
| `node tools/build-diagrams.mjs` | Регенерирует схемы README в `assets/` (3 чертежа × светлая/тёмная × EN/RU) из одного источника. Гейт ширины текста валит сборку, если подпись не влезает в свой контейнер — наезд невозможен по построению. Схемы правь ТОЛЬКО здесь, не в SVG. |
| `node tools/readme-pdf.mjs` | Рендерит `README.md` → `README.pdf` (нужен `md-to-pdf`; `npm i` в `tools/`). |
| `node tools/sandbox-suite.mjs` (`npm run test:core`) | **Постоянный песочный полигон машинерии** (план 21): число сводов и проверок печатает сам полигон («all N suites green») — по матрице полевых профилей (свежая/анонимная/легаси установки, по-модульный update, расписки/adopt-current/diff, слепок, tool-модули provenance/canon-lint/requirements-lint, контур K5). ОБЯЗАТЕЛЕН после любой правки `framework/installer/*`, `framework/tools/*` или сборщика и перед релизом. |
| `node tools/doc-header-lint.mjs` | Линтер шапки-меты И блока требований документов знаний (шапка — `plans/53`; блок — `plans/55`): фикс-словарь `Создан`/`Родитель`/`Статус`/`Вовне` + диалекты bugs (`Fix accepted when` — норма N2) /interviews + ярус корневых + блок «Вектор цели/Критерии приёмки» в нормо-эпохных целевых (plans ≥ 26, ideas ≥ 21) + композиция: гоняет стоп-словарь payload-модуля `kaif-requirements-lint` по секциям требований plans/bugs/ideas. Плюс ось ИМЁН `plans/` (задача T2): у каждого ребёнка `NN_epicMM_*` родитель `MM_` существует и назван `MM_EPIC_*` — скоуп задаёт сам паттерн (старые планы его не используют и молчат). Плюс ось МЕТОК ВРЕМЕНИ (задача T8): метка момента несёт И дату, И время, И смещение зоны — примета узкая (глагол принятия/фиксации/закрытия вплотную к дате, только формы одного момента), порог — СОБСТВЕННАЯ дата метки, поэтому канон действует вперёд без базовой линии. Плюс ось ПРАВДИВОСТИ метки (`bugs/78`): момент, объявленный в документе, не может быть ПОЗЖЕ коммита, который эту строку внёс — примета без ложных срабатываний по построению (записать момент раньше, чем он наступил, физически нельзя). Источник истины — АВТОРСКОЕ время коммита: ребейз переписывает коммиттерское и краснил бы честные метки. Известный исторический долг — базовая линия `tools/doc-header-lint.stamp-baseline.json`, она печатается одной строкой и только УМЕНЬШАЕТСЯ; всё новое кричит поимённо. Плюс ось **PENDING без СУДЬБЫ** (`bugs/72`): у живого маркера `PENDING:`/`PENDING →` обязана быть судьба — *подтверждено / опровергнуто / спрошено* (годится адрес `interviews/…`) на той же или следующей строке. **«Вынесено в <фазу>» судьбой НЕ является** — это адрес, и приёмник о нём может не знать: шесть обязательств эпика M так и провисели, пока их не нашёл круг R1. Строка-отрицание («PENDING нет»), слово в обратных кавычках и код молчат по построению. Дефолт — живые документы, `--all` — включая DONE, `--selftest` — красный на сломанной фикстуре. Консультирует, не блокирует старт работы. |
| `node tools/refresh-stamp.mjs --trigger <hour\|heavy-task\|compaction\|ritual:имя> --docs <док>…` (`--show`) | Пишет свидетельство освежения `.kaif/refresh-marker.json`. **Момент берёт САМ инструмент** у системных часов — назвать его сессия не может по построению (`bugs/78`: два маркера подряд получили правдоподобное, но неверное время, и охота судьи «refresh-witness» на них не сработала бы, потому что формально они безупречны). Пустой список перечитанного отвергается: маркер обязан НАЗЫВАТЬ, что освежено, иначе он утверждает освежение, не свидетельствуя о нём. Цитата-приёмка в чате остаётся обязательной — маркер делает факт проверяемым, цитата доказывает, что чтение дошло до задачи. |
| `node tools/counters-guard.mjs` (`--selftest`) | Страж КЛАССА «протухший счётчик прозы» (bugs/49; прародитель bugs/09, урок EXP-0025): берёт ЖИВЫЕ числа из артефактов (встроенные файлы `framework/` · блоки `dist/KAIF-CORE-BUNDLE.md` · модули карты · своды `sandbox-suite.mjs`) и сверяет разом ВСЕ зеркала (число печатает сам прогон) — ось ПОСТАВКИ (строка AGENT_GUIDE · обе половины README: счётчики и число сводов · строка «актуальная сборка» в STATUS) и ось НАВЫКОВ (alt-тексты обеих половин · SVG-генератор · пропись в четырёх местах · Таблица 3 КАЖДОЙ половины · ключи `skill-triggers.json` каждого языкового пакета). **Ось навыков сверяет И СЧЁТ, И ИМЕНА** (bugs/55 F1): мощность списка — прокси, свойство — тождество множеств, и переименование навыка счёт не меняет; расхождение печатается поимённо, обе стороны разности. Прежняя команда реестра стерегла одно зеркало из пяти; ось навыков до плана 62 не стерёг никто (восемь рукописных «34»). `--selftest` доказывает красный мутацией на девяти зеркалах, включая ПЕРЕИМЕНОВАНИЕ навыка в витрине и в языковом пакете. |
| `node tools/lib/temp-root.mjs --selftest` | Модуль-помощник + страж КЛАССА «фиксированное имя каталога в общем OS-temp» (bugs/59). `tempRoot(name, explicit)` — ЕДИНСТВЕННЫЙ способ взять временный корень прогона: имя уникально по построению (`mkdtemp`), поэтому два одновременных прогона (две сессии · полигон рядом с отдельным сводом · `--selftest` рядом с обычным прогоном · CI рядом с локальным) больше не сносят каталог друг у друга и не дают ЛОЖНОГО КРАСНОГО в главном гейте. Зелёный прогон корень убирает, **красный — ОСТАВЛЯЕТ и печатает путь**; `KAIF_TMP_KEEP=1` оставляет всегда; явный путь аргументом принадлежит вызывающему. Страж обходит `tools/` и `framework/`, стоит ПРЕПОЛЁТОМ в `npm run test:core` и доказан красным синтетической мутацией. |
| `node tools/sandbox-mute-guard.mjs` (`--selftest`) | Страж КЛАССА «немая команда песочного свода» (bugs/61). Свод — это КОМАНДЫ (приводят дерево в состояние) и АССЕРТЫ (судят состояние); немая команда — та, чей результат не видит ни один `ok(...)`. Когда она падает, красным становится ЧУЖОЙ ассерт про симптом («история не выросла»), а причина — код возврата и вывод упавшей команды — выбрасывается: так родился красный, который не воспроизвёлся ни за 108 итераций изолята, ни за 12 полных прогонов свода. Два законных хода, оба видны в исходнике: судить результат внутри `ok(...)` (условием или уликой) ИЛИ обернуть установочный шаг в `must(run, …)` из `tools/lib/sandbox-run.mjs` — она называет команду, код и полный вывод и останавливает свод на упавшем шаге. Долг **НУЛЕВОЙ по построению** (все 30 мест переведены в день рождения стража), поэтому это ГЕЙТ, а не советчик: стоит преполётом в `npm run test:core` рядом со стражами bugs/59 и bugs/55 F5. `--selftest` доказывает красный на обеих формах класса и молчание на обоих законных ходах. |
| `node tools/stylometry-snapshot.mjs` (`--check` · `--version-check` · `--report <путь>`) | Генератор ПУБЛИЧНОГО слепка портрета голоса владельца `AUTHOR_STYLOMETRY.md` из приватного ядра `krinik-stylometry` (задача T12, решение №60). Правила переносит КОД побайтно, рамку пишет модель (EXP-0049: перенос дословного руками молча теряет «ё», подменяет глифы и съедает экранирование). **Безопасность по умолчанию:** наружу не едет НИ ОДНА цитата текста — только языковые токены, явно разрешённые спаны (`tools/stylometry-snapshot.config.json` → `allowSpans`) и публичные слова владельца ИЗ ЭТОГО репо, вытянутые по адресу `файл:строка`; остальное схлопнуто в «…», адреса обезличены до номера произведения. Приёмка сборки красная = личное могло протечь (ловит и многострочные цитаты). `--version-check` КОНСТАТИРУЕТ расхождение слепка и ядра и всегда возвращает 0 (решение №62); без приватного ядра честно молчит. |
| `node tools/owner-quotes-harvest.mjs` (`--list` · `--out` · `--min-words` · `--selftest`) | Сборщик ПУБЛИЧНЫХ дословных слов владельца из этого репозитория — доказательная база слепка и материал для витрины его голосом. Три механически опознаваемых класса: `GOAL.md` целиком (написан владельцем) · строки с маркером контура `<!-- owner-review:` (буквально им напечатанное) · «ёлочные» цитаты на строках со словом «дословно» (конвенция проекта). Третий класс — КАНДИДАТЫ: выбирает модель, переносит код. `--selftest` доказывает, что все три класса опознаются И что посторонние строки молчат. |
| `node tools/kaif-stats.mjs` (`--from <тег>` · `--all` · `--json`) | Летописная статистика релиза: во что обошлась версия. Считает из НАБЛЮДАЕМЫХ источников (git · файлы репозитория · транскрипты сессий), потому что витринное число читает посторонний, а проверить его он не может (класс «протухший счётчик» — `bugs/09`, `bugs/49`, EXP-0025). Вывод разделён заголовками по надёжности: **ИЗМЕРЕНО** (коммиты, строки, документы, токены, активное время) · **ОЦЕНКА ПО API-ПРАЙСУ** (деньги — не счёт владельца: работа шла по подписке) · **ОЦЕНКА С ВИДИМЫМИ ДОПУЩЕНИЯМИ** (человеко-часы; обе ставки названы в исходнике, подставь свои) · **ЧУЖАЯ ОЦЕНКА** (энергия — Anthropic Вт·ч на токен не публикует, вилка из работ по другим моделям). Плюс перевод каждой метрики в бытовую реальность с названными константами и источниками. Питает блок «Интересные факты» витрины (задача T11). |
| `node tools/voice-lint.mjs <файл.md>…` (`--selftest`) | Машинная минута чек-листа портрета голоса владельца (`AUTHOR_STYLOMETRY.md` §7, слой 7A): «написано голосом владельца» — заявление, а заявление без проверки есть ложный `[TESTED]`. ЖЁСТКИЕ пункты валят прогон (A2 стоп-паттерн «важно/стоит/следует отметить» с нулевой авторской базой · A3 текст не кончается вопросом · B9 «ибо» в документном жанре · B12 глиф тире не смешан · B5 один тип кавычек · G1 согласование «который» по числу с придаточным сказуемым — `bugs/66` №6); КОРИДОРЫ печатаются справкой (A12 ритм РАЗБОРА 11,0–17,5 слова и 7–19 % коротких · B10 «является» не ниже машинного уровня 9,4 на 10 000 — машина связку НЕДОБИРАЕТ · B8 счёт антитез «не X, а Y»). Пункты с объявленной в портрете границей жанра (A4 заголовки, A5 болд, A9/A10 капс на термине) в документе НЕ запускаются. **Чего инструмент не делает, и это сказано в его шапке:** не судит похожесть (класс «вкус» — вердикт владельца), не судит английскую половину витрины (правила выведены из русских корпусов), не закрывает слой 7E (у каждого факта назван источник) — тот остаётся ручным гейтом выдачи; и не судит ПАДЕЖ с ПРЕДЛОЖНЫМ УПРАВЛЕНИЕМ («замыкание цикл», «под наблюдение проектов») — это не форма, а морфологический разбор, которого у инструмента нет, поэтому вторую половину грамматики держат глаза владельца (`homeworks/05`), и в таблице волны `plans/67` это записано вместо прежнего кредита машине. `--selftest` доказывает каждый жёсткий пункт ОБОИМИ ответами: красный на подсунутом нарушении и молчание на чистой фикстуре. |
| `node tools/guarded-watchdog.mjs --arm\|--check\|--status\|--disarm` (`--selftest`) | ВНЕШНИЙ сторож защищённого цикла (`/guarded-loop`, дефер G4): пульс пишет агент по факту завершённого шага (`.kaif/heartbeat.log`), свежесть судит ЧУЖОЙ процесс из планировщика ОС. Пульс берётся ПОСЛЕДНЕЙ СТРОКОЙ, а не mtime — поэтому «пульс от таймера» (фрод, на который охотится судья) технически не проходит. Дебаунс из измеренной длительности самого долгого шага, потолок эскалации, саморазвод по сроку прогона. **Честная граница в шапке файла:** внешний процесс НЕ может воскресить зависший чат — лестница кончается сигналом владельцу, не рестартом. |
| `node tools/showcase-lint.mjs` (`--selftest`) | Страж ВИТРИНЫ — текста, который читает посторонний (фаза Q, `plans/67`). 13 классов волны владельца, каждый записан формой, а не обобщением: **META** (описание устройства собственного текста) · **EXCUSE** (оправдание числа припиской) · **BACKSTAGE** («в действительности», «на самом деле» — намёк на закулисье) · **DENIAL** (отрицание, подрывающее названное число) · **CALQUE** (английская фраза русскими словами) · **PASSIVE** (безличный залог вместо обращения к читателю; запускается ТОЛЬКО в разделах порядка действий) · **JARGON** (внутренний ярлык в названии строки таблицы) · **RANGE** (вилка шире ×3; периоды лет пропускаются) · **AUTHORSHIP** (витрина приписывает человеку работу агента — `bugs/77`: «написано руками» владелец отверг дважды, слово приехало из внутренней метрики, где «рукописное» значит «не сгенерировано сборкой») · **NBSP** (обычный пробел между числом и единицей — перенос отрывает единицу от величины; класс заведён по слову владельца со снимком, где «по 5 $» разорвано, и лечит два слепых пятна сразу: после «$» нет границы слова, а в JavaScript «\b» слеп к кириллице — EXP-0082) · **STRAY** (посторонний символ, приклеенный к открывающему блочному тегу — `bugs/80`: правка витрины в веб-редакторе принесла цифру в первую строку README, и она рендерилась над логотипом при ЗЕЛЁНЫХ гейтах). Плюс ось СИММЕТРИИ ПОЛОВИН: подразделы, картинки и строки каждой таблицы обязаны совпадать между половинами — этим ловится «логотип пропал из русского блока». Плюс ось **NOTES-ANCHORS** (2.5, сессия 52): каждая ссылка старших нот на README попадает в существующий якорь — слаг заголовка по правилу GitHub или явный `<a id>`; ноты 2.2–2.4 наследовали друг от друга три мёртвых якоря, и ни один страж их не судил, пока судья релиза 2.5 не открыл README. Не судит красоту: вердикт «нравится» выносит владелец. |
| `node tools/estimates-guard.mjs` (`--selftest`) | Страж КЛАССА «оценочная константа без названного источника» (Q4). У константы, участвующей в оценке мира (цена, ставка, расход, бытовой эквивалент), в комментарии назван ВНЕШНИЙ источник: ссылка · «источник:» · слово владельца. Границей секции служит ПУСТАЯ строка — иначе одна ссылка в шапке файла «покрывала» бы все константы ниже, и страж был бы зелёным по построению. Константы метода (порог простоя, длина рабочего дня) исключены ПОИМЁННО и с причиной. |
| `node tools/private-names-guard.mjs` (`--selftest`) | Страж КЛАССА «приватное имя проекта владельца в публичной поставке» (Q6). Ищет имена ЦЕЛЫМИ словами (немецкое «Geschmacksklasse» и «unlimited» молчат) по зонам `framework/`, `dist/`, README, релиз-ноты. Список — `.kaif/private-names.json`, файл В IGNORE: перечень приватных имён сам является приватными данными. **Без файла страж НЕ СУДИТ** и говорит это вслух — выдумать список имён владельца агент не вправе, а молчаливый зелёный был бы ложью о проверке. |
| `node tools/revert-guard.mjs` (`--selftest`) | Страж КЛАССА «рабочая копия откатилась к прошлой ревизии» (bugs/45, два наблюдения). Примета узкая: рабочая копия ПОБАЙТНО равна не-HEAD ревизии этого же файла — обычная правка такого не даёт. Хеш считает `git hash-object`, а не самодельный sha1: при `core.autocrlf=true` диск с CRLF никогда не совпал бы с LF в объектах. Стоит ПРЕПОЛЁТОМ в `tools/commit.mjs`; намеренный откат проходит `--allow-revert <файл>`. |
| `node tools/lang-packs-guard.mjs` (`--write-lock` · `--update-pins` · `--selftest`) | Страж ЗАМОРОЗКИ языковых пакетов (№56/№57; bugs/92 №2–№5): восемь пакетов запинены ПОБАЙТНО (EOL-норм — CRLF-чекаут не красит, класс bug 12) на состоянии 2.2; ростер пакетов — поимённо ИЗ ЛОКА, не с диска (92.5); живой ru — состав поимённо + пол объёма 40 % против EN-исходника (92.4); EN-описания навыков — пины против тихого протухания ru-алиасов (92.2). В реестре пар → гоняется в `/end-chat-soft`/`/release`. `--selftest` — шесть мутаций красные на фикстурах, дерево не тронуто. |
| `node tools/release-body-guard.mjs` (`--tag vX.Y` · `--selftest`) | Страж КЛАССА «опубликованное тело релиза разошлось с файлом нот» (bugs/98 №2): сверяет `gh release view --json body` с `reports/RELEASE_NOTES_<ver>.md` построчно (CRLF нормализован, пустой хвост страницы — законная разница носителя) плюс ось NBSP — страница v2.2 теряла все неразрывные пробелы файла при четырёх зелёных инструментах по исходнику. Гоняется шагом 6.9 `/release` ПОСЛЕ публикации; глаза не заменяет (EXP-0071: рендер судят глаза, страж судит текст). `--selftest` — красный без сети на обеих осях. |
| `node tools/commit.mjs "<msg>"` · `--msg-file <путь>` · `--only <путь>` · `--with-new` (`--selftest`) | Инкрементирует номер сборки, коммитит (`git commit -F`, текст не проходит через argv), пушит. Не-ASCII сообщение — ТОЛЬКО через `--msg-file` (страж отвергает argv-кириллицу; bugs/46). Преполётами гоняет `revert-guard` и **гейт неожиданного файла** (bugs/79): НОВЫЙ файл в дереве останавливает ПОДМЕТАЮЩИЙ коммит и называет себя — молчаливый `git add -A` увёл в origin два файла владельца под чужим сообщением. Намерение объявляется `--only` (по флагу НА ПУТЬ — девять путей одним флагом инструмент прежде глотал молча, теперь это отказ; при объявленном наборе оси нет, потому что нет подметания) или `--with-new` (взять новые разом); набор ПЕЧАТАЕТСЯ построчно всегда. Гейт стоит ДО бампа `version.json` — отказ не оставляет в дереве поднятый номер сборки. `--selftest` доказывает оба ответа: красный на чужом новом файле и молчание на названном. |

| `node tools/budget-gate.mjs` (`--selftest`) | ДВЕРЬ бюджета ядра перечитывания ДЛЯ ИСТОКА (2.7, эпик CB; тикет #71). Дверь поставки `node .kaif/kaif-core.mjs check --gate-budgets` у истока неисполнима по построению (здесь исходник, а не развёртывание), поэтому ритуал закрытия чата гоняет эту команду — ПОСЛЕ стрижки бонсая. Числа и адреса выноса берёт из ОДНОГО места — таблицы `DOC_BUDGETS` ядра поставки (разбором текста; копии чисел не держит); у истока каждая строка собственная, поэтому счёт — строки файла. Код 1 — строка `✖ <документ>: own lines N of budget M → <адрес>` на каждый документ выше бюджета, пропавший документ ядра — находка; код 2 — таблица не прочитана или в ней не девять строк (инструмент, не прочитавший свою истину, не зеленеет). Красный доказан на девяти документах ревизии `3d57c09` (четыре выше бюджета); лечение — вынос, никогда не поднятие числа. |
| `node tools/sandbox/probes/invisible-characters.mjs` (`--summary`) · `node tools/sandbox/probes/hooks-mutants.mjs` | ПРОБЫ (полигон их не исполняет, только читает преполётом). Первая — инвентарь невидимых и управляющих символов внутри тел отслеживаемых текстовых файлов (`bugs/122`: escape, набранный в параметре инструмента правки, ложится в файл настоящим символом) — красная по построению, пока класс открыт; набор кодовых точек собран из чисел. Вторая — четыре адресных мутанта свода `s14` на копиях `dist` (гонять В ОДИНОЧКУ: четыре прогона свода); мутация, которая не применилась, — отказ, а не зелёный. |

### 1б. Из `STATUS.md` → «Инструменты, которыми работать, а не открывать заново» (что каждая стережёт)

| Команда | Что стережёт |
|---|---|
| `node tools/build-framework.mjs` + `npm run test:core` | обязательны после ЛЮБОЙ правки машинерии (число сводов и преполётов печатает сам полигон — на срезе 2026-09-18 их 27 и 6); **генератор контура в своде или пробе — только `env: quietEnv()`** (`tools/lib/sandbox-run.mjs`; `bugs/116` — старое ядро под швом `KAIF_DIST` показывало владельцу страницу и звало голосом) |
| `node tools/counters-guard.mjs` | зеркала живых чисел (сколько их — печатает сам прогон; после стрижки CB — 46); ось навыков и документов — ПОИМЁННО |
| `node tools/questions-guard.mjs` | место вопросов · вариант ПОТЕРЯН · вопрос ОТПРАВЛЯЕТ в документ · ратчет долга · **G4 ждёт и ни разу не показан · G5 вопрос владельцу прозой вне `interviews/` · G6 живой вопрос без сценария · G7 вердикт без разведки · G8 словарь агента в вопросе владельцу** (2.6; G5/G6 — вперёд от даты ≥ 2026-09-05, G8 — ≥ 2026-09-06; коды эпиков — из таблиц живых мета-планов; исключение — `questions-guard:vocabulary-ok причина`) |
| `node tools/review.mjs --queue --list` · `--mark-shown <док> --transport чат` | очередь владельца БЕЗ браузера: возраст ожидания и показа, ни разу не показанные первыми, **код 2 = гейт** (I41/I42); факт показа для вопроса, заданного в чате (I40); карта — `interviews/decisions/shown.json` |
| `node tools/review.mjs …` = обёртка над `framework/tools/contour/review.mjs` (`--selftest` · `<док> --check` — проверка формы БЕЗ страницы и зова, код 3/0 (2.7 QL1) · `--mark-implemented <док> <Q> --where <адрес>` — четвёртый факт «внесено» (2.7 QL2) · `<док> --no-open` — поднимает и ЗОВЁТ, это не проверка · `--proofread` · `--mockup`) | **отгружаемый генератор контура** (2.6 IC3/IC5; в поле — `.kaif/tools/contour/`): предполёт формы вариантов §2 контракта, самопроверка «радиогрупп = вопросов», три лица; параметры истока — `.kaif/kaif.json` → `contour`; свод `s22`; исток ест свою поставку с IC5 |
| `node tools/doc-header-lint.mjs` | шапка-мета · метки времени и их ПРАВДИВОСТЬ · закрытие против тела · **PENDING без СУДЬБЫ** |
| `node tools/showcase-lint.mjs` · `estimates-guard` · `private-names-guard` | витрина: 13 классов и четыре оси — симметрия половин (включая подписи таблиц и блоки цитат) · врап тела релиза · числа нот ↔ README · **якоря README в ссылках нот** (2.5, сессия 52); источник у оценки; утечка имён (зоны — поставка, README, все ноты, слепок, реестры суда) · **ось №90 (2.6 HY): псевдоним донора `project A…F` без пометки `source-kept` в поставке — красный; реестры суда и слепок — вне оси** |
| `node tools/verify-contour.mjs --etalon-only` | дешёвая приёмка контура — в `/end-chat-soft`; полный прогон в браузере — в `/release` |
| `node tools/commit.mjs --msg-file <путь> --as "<имя модели>"` | коммит; **`--only` берёт ОДИН путь НА ФЛАГ**, посторонний аргумент отвергается, новый файл в дереве останавливает подметающий прогон; **гейт 1c** (W2-1): коммит с поверхностью поставки гонит `private-names-guard` |
| `node tools/refresh-stamp.mjs --trigger <…> --docs <…>` | маркер освежения: момент берёт САМ инструмент |
| `node tools/build-logo-title.mjs` (`--check`) | подпись версии на фирменном логотипе: старое лого + перенабранная надпись в стиле, снятом пробами (шрифт, медь, фаска, посадка). **Выпускаешь версию — прогони**, иначе логотип уедет с прошлым именем (так вышло с 2.3). `--check` перенабирает подпись 2.2 и сверяет с оригиналом; красный доказан подменой гарнитуры |
| `node tools/build-story-card.mjs` | Таблица 6 витрины → PNG 9:16 для соцсетей: строки читаются из живого README, кегль ищется замером, типографика лечит переносы |
| `node tools/build-team-logo.mjs` (`--check`, `--title`) | логотип 2.4/2.5: арт v2 владельца (побайтно нетронут) + подпись стилем 2.3 через `renderTitle`; геометрия — плита 2.3 × масштаб арта + ручки вкуса `TUNE_*` (меняются только словом владельца); `--check` — шесть осей вкл. «медь, не серебро» |
| `node tools/release-body-guard.mjs --tag vX.Y` | опубликованное тело релиза ↔ файл нот построчно + NBSP; шаг 6.9 `/release` |
| `node tools/kaif-stats.mjs` · `voice-lint` · `guarded-watchdog` | статистика релиза · слой 7A портрета · сторож защищённого цикла |
| `node framework/tools/kaif-guard-lint.mjs check <пути>` (`selftest`) | блок объявления стража `@guard` (THREAT · PROVED-AGAINST · GAP · ON-REAL-PATH) / `@forensic` (EXPLAINS · DURABLE-AT; close/exit/trip-only отвергнуты) / `@fork` (OPTIONS · COST · RECON · DECIDED) — гейт 5, вторая половина (2.5, CN3; issue #35); только явные маркеры, `SKIPPED=3` без них; в поставке — `.kaif/tools/`; свод s15 |
| `node framework/tools/kaif-scenario-lint.mjs check <пути>` (`selftest`) | форма сценария критерия (Situation · Action · Result · Check, правила 1–6 + порядок; 2.5, SF; issue #39); `SKIPPED=3` без сценария; свод s19 |
| `node framework/tools/kaif-testrun-lint.mjs check` (`selftest`; `check <дом>`) | **форма отчёта исполненного прогона** (2.7 TR, issue #59): каталог по дате `testcases/reports/<ГГГГ-ММ-ДД>_<работа>.md` · семь полей непустые · «Прогоны» с командой в код-спане и моментом · «Найдено» списком или явным «ноль» · вердикт назван; `SKIPPED=3` без каталога — ненаписанный отчёт линтер не видит, на заявление без отчёта охотится судья; шаблон — `framework/templates/_testrun-report-template.md` (в поле `.kaif/_testrun-report-template.md`); свод `s25` (и ось `check` ядра «`/resume` покрывает ядро» — предупреждение поимённо, доказано на развёрнутой копии) |
| `node tools/experience-lint.mjs` (`--selftest`) | поле механизации у новых записей опыта (issue #14, эпик X): mechanized/none-cheap/subject-lesson; ловушка по форме требует первых двух; долг — baseline, только убывает; в `/end-chat-soft` |
| `node framework/tools/kaif-attribution-lint.mjs check` (`selftest` · `--write-baseline`) | **авторство решения** (2.7 AW, issue #55): ссылка на волю владельца («решение/по слову/владелец велел», EN-формы) без дословной цитаты, адреса интервью или номера решения в окне ±2 строк — находка; подпись `[AI]`/`[ИИ]` на строке или секция «Решения без владельца» — решение агента; исключение — `<!-- attribution-ok: адрес -->`; **долг истока 45** (базовая линия `.kaif/attribution-lint.baseline.json`, проба 2026-09-12 01:14: 41 на приёмке + 4 приняты явно `--adopt-new` после ужесточения улик по суду AW), новых обязано быть 0 — в `/end-chat-soft`; повторный `--write-baseline` при новой находке — отказ (линия только убывает); в поставке — `.kaif/tools/`, свод `s24` |

**Тронул контур** (`framework/tools/contour/*`, обёртку `tools/review.mjs` или шим `tools/lib/review-core.mjs`) → обязателен полный
`node tools/verify-contour.mjs` в живом браузере; новый живой документ роняет эталон НАМЕРЕННО —
лечится `--write-etalon` с пересмотром чисел ГЛАЗАМИ.

---

## 2. Реестр пар «истина↔зеркало» этого проекта

Правило и его обоснование — `AGENT_GUIDE.md` → «Гигиена документов и текста» → «Реестр пар»; здесь — сама таблица.

Реестр пар ЭТОГО проекта (сверка — bash; расхождение = стоп до починки):

| Истина | Зеркало | Команда сверки |
|---|---|---|
| `framework/skills/fable-method,fable-judge` (вендорено, EN) | `.claude/skills/…` (побайтовые копии) | `diff framework/skills/fable-method/SKILL.md .claude/skills/fable-method/SKILL.md && diff framework/skills/fable-judge/SKILL.md .claude/skills/fable-judge/SKILL.md` |
| `framework/*` (источник) | корневые генерированные копии + `dist/*` | `node tools/build-framework.mjs && git diff --stat` (дифф генератов после пересборки = дрейф) |
| Вывод сборки (счётчики) | строка счётчиков в этом документе (раздел «Тестирование») + оба README | `node tools/counters-guard.mjs` (сверяет ЖИВЫЕ числа сборки со всеми зеркалами разом; bugs/49) |
| Состав навыков `framework/skills/` | строки Таблицы 3 README (EN+RU половины) | `node tools/counters-guard.mjs` (ось навыков: сверяются И СЧЁТ, И ИМЕНА по КАЖДОЙ половине — симметрическая разность печатается поимённо; счёт один ловит дубль, но не переименование — bugs/55 F1) |
| Счётчик ритуалов в SVG (генератор) | alt-тексты README + пропись в README/AGENT_GUIDE/KAIF_REFERENCE | `node tools/counters-guard.mjs` (ось навыков; зашитые «34» из этих команд убраны в план 62 — число теперь живое) |
| Канон меток времени (правило ниже) | метки в `plans/`/`bugs/`/`ideas/`/`researches/`/`interviews/` + корневые ключевые | `node tools/doc-header-lint.mjs` (ось T8, ДВЕ приметы: глагол принятия/фиксации/закрытия/снятия вплотную к дате И безглагольная веха в ЗНАЧЕНИИ поля «Статус:». Ось смотрит и в `_DONE_`-файлы БЕЗ `--all` — метка закрытия там и рождается; порог — собственная дата метки, поэтому история молчит. Изъятия: поле схемы (`When/context:`), разговор О ФОРМАТЕ, дата в обратных кавычках, явное `<!-- t8-ok: причина -->`. `bugs/67`) |
| Портрет голоса в приватном ядре `krinik-stylometry` (истина) | `AUTHOR_STYLOMETRY.md` — публичный слепок правил (зеркало, ГЕНЕРИРУЕТСЯ) | `node tools/stylometry-snapshot.mjs --version-check` — **единственная КОНСТАТИРУЮЩАЯ строка реестра** (решение №62): печатает расхождение версий и завершается кодом 0, потому что обновление слепка идёт только по явному слову владельца, в обе стороны. Пересборка слепка — `node tools/stylometry-snapshot.mjs`, сверка без записи — `--check` (нужен доступ к приватному ядру; без него сверка честно молчит) |
| Живые интервью в `interviews/` (счёт вопросов и вариантов) | замороженный эталон `tools/verify-contour.etalon.json` | `node tools/verify-contour.mjs --etalon-only` (без браузера, доли секунды; новый живой документ роняет прогон НАМЕРЕННО по норме G7 — лечится `--write-etalon` с пересмотром чисел глазами). Вызов стережёт `s12`: снимешь шаг из `/end-chat-soft` или `/release` — свод краснеет (`bugs/69`) |
| Состав навыков `framework/skills/` | ключи `skill-triggers.json` языковых пакетов | `node tools/counters-guard.mjs` (ось навыков, третье вхождение bugs/55: прежний однострочник реестра сверял ГОЛЫЙ СЧЁТ ключей — переименование навыка счёт не меняет, и все девять пакетов оставались с алиасами мёртвой команды; `check-framework` §5f эти файлы намеренно пропускает как машинерию алиасов) |
| Заморозка языковых пакетов №56 (истина — `tools/lang-packs.lock.json`: побайтные sha восьми пакетов состояния 2.2 · состав живого ru · пины EN-описаний навыков) | `framework/templates/languages/*` + `framework/skills/*/SKILL.md` (строки description) | `node tools/lang-packs-guard.mjs` — красное: замороженный файл изменён/потерян/подменён (92.3–92.5), живой перевод выпотрошен (92.4), EN-описание переписано при несверенных ru-алиасах (92.2). Пины описаний двигает `--update-pins` ПОСЛЕ сверки алиасов глазами; замороженные sha — только словом владельца (`--write-lock`) |
| Молитвы 2.4 (символ веры + молитва философии) в `framework/AGENT_GUIDE.md` (EN-шаблон — истина формы) | корневой `AGENT_GUIDE.md` (RU-зеркало смысла; тексты — приёмка владельца) | bash: `for f in AGENT_GUIDE.md framework/AGENT_GUIDE.md; do for m in KAIF:CREED:BEGIN KAIF:PRAYER:BEGIN; do [ "$(grep -c "^<\!-- $m -->" "$f")" = 1 ] \|\| { echo "RED: $m отсутствует в $f"; exit 1; }; done; done && echo GREEN` — доказан красным удалением маркера на копии; якорь `^` держит пробу слепой к этой строке реестра (первый прогон покраснел ровно об неё) |
| `version.json` (истина версии истока) | `.kaif/kaif.json` (маркер самообёртки) + строка «Версия KAIF» в `KAIF_FRAMEWORK.md` (семейство `bugs/101`: оба зеркала согласованно стояли на 2.0 при живой 2.2, и `/kaif-version` предложил бы мигрировать исток на самого себя) | bash: `v="$(node -p "const j=require('./version.json'); j.major+'.'+j.minor")" && m="$(node -p "require('./.kaif/kaif.json').version")" && f="$(grep -c "\| \*\*Версия KAIF\*\* \| $v" KAIF_FRAMEWORK.md)" && [ "$v" = "$m" ] && [ "$f" = "1" ] && echo GREEN \|\| echo RED` — доказан красным на живом дефекте 2026-08-14 до починки |
| Состав референсов навыка в поставке (`framework/skills/<навык>/references/`, истина) | переводная копия обвязки `.claude/skills/<навык>/references/` — зеркало по СОСТАВУ (тексты — перевод от смысла, побайтно не сверяются) | bash: `for d in framework/skills/*/references; do n=$(basename "$(dirname "$d")"); diff <(ls "$d") <(ls ".claude/skills/$n/references") >/dev/null \|\| { echo "RED: $n"; exit 1; }; done && echo GREEN` — доказан красным на фикстуре с референсом только в поставке (EXP-0103: RU-копия `/team-deployment` отстала от поставки на целый эпик, и ни одна строка реестра этого не видела) |

---

## 3. Команды интерактивного контура этого репозитория

**Контур ЭТОГО репозитория построен (фаза K5, `plans/48`) — команды ритуала:**

| Что | Команда |
|---|---|
| Страж места вопросов (нарушения + оба плеча разноса + долг ratchet) | `node tools/questions-guard.mjs` |
| Покажи неотвеченные вопросы СТРАНИЦЕЙ (очередь «Накопилось N») | `node tools/review.mjs --queue` |
| Очередь БЕЗ браузера: возраст, факт показа, ни разу не показанные первыми; код 2 = гейт (I41/I42) | `node tools/review.mjs --queue --list` |
| Вопрос задан точечно в чате — запиши факт показа (I40) | `node tools/review.mjs --mark-shown <док> --transport чат` |
| Показать любой md владельцу (показ — действие, не ссылка) | `node tools/review.mjs <документ.md>` |
| **Сообщить** владельцу (класс «сообщение», I37/I38: зовёт, но ответа не ждёт) | `node tools/review.mjs <документ.md> --notice` · только в очередь: `--enqueue <док> --notice` |
| Гейт отправки / реальный отправитель (issue от имени владельца) | `node tools/review-gate.mjs <док> <id>` · `node tools/send-outbound.mjs <док> <id> --apply` |
| Приёмочный QA-прогон контура в живом браузере | `node tools/verify-contour.mjs` (+ `--visible` для QA2) — стоит в `/release` |
| Дешёвая половина той же приёмки, без браузера (доли секунды) | `node tools/verify-contour.mjs --etalon-only` — стоит в `/end-chat-soft`; красное лечится `--write-etalon` с пересмотром чисел ГЛАЗАМИ (`bugs/69`) |

---

## 4. Досье окружения

Правило (факты о машине снимаются пробами, в каждом шелле отдельно; граница DRY с «Гигиеной») — `AGENT_GUIDE.md` → «Досье окружения».

> **Досье окружения.** Снято: **2026-08-07** · Перегенерация: `/refresh-context` → шаг досье
> (прогнать пробы колонки 3, переписать значения и эту дату) · **Протухание: факты старше четырёх
> недель — ГИПОТЕЗЫ, переснимай пробой, прежде чем на них опираться.**

| Факт | Значение | Проба |
|---|---|---|
| ОС | Windows 11 Pro 10.0.26200.0 | PS: `[Environment]::OSVersion.Version` |
| CPU / RAM | AMD Ryzen 7 5700G · 8 ядер / 16 потоков · 32 ГБ | PS: `Get-CimInstance Win32_Processor` |
| PowerShell | **5.1** Desktop; **pwsh 7 НЕ установлен** | PS: `$PSVersionTable` |
| git-bash | GNU bash 5.2.21 (MINGW64); **локаль ПУСТАЯ** (`$LANG` не задан) | bash: `echo "[$LANG]"` |
| Кодировки | консоль 65001 (UTF-8), **ACP 1251** → `Set-Content` без `-Encoding utf8` пишет ANSI; **ЧТЕНИЕ симметрично:** `Get-Content`/`-Tail` без `-Encoding UTF8` читает UTF-8 как ANSI → мнимый mojibake на здоровом файле (наступлено 2026-08-14 при знании пишущей половины). Файлы читай инструментом Read; шеллом — только с явным `-Encoding UTF8` | PS: `[Console]::OutputEncoding.CodePage` и `[System.Text.Encoding]::Default.CodePage` |
| node / npm | v24.15.0 / 10.8.2 | `node -v` · `npm -v` |
| git | 2.43.0.windows.1 · `core.autocrlf=true` · helper `manager` | `git --version` · `git config core.autocrlf` |
| gh | 2.95.0, аутентифицирован (аккаунт владельца) | `gh --version` · `gh auth status` |
| python | `C:\Python314\python` | bash: `command -v python` |
| `tar` | PS: `system32\tar.exe` (bsdtar) · bash: `/usr/bin/tar` (GNU) — **РАЗНЫЕ программы** | PS: `Get-Command tar` · bash: `type -p tar` |
| `curl` | PS 5.1: **Alias на `Invoke-WebRequest`** (другая программа; у алиаса `.Source` пуст — смотри `CommandType`) · bash: `/mingw64/bin/curl` | PS: `Get-Command curl` · bash: `type -p curl` |
| `find` | PS: `system32\find.exe` (Windows-фильтр строк!) · bash: `/usr/bin/find` (GNU) | PS: `Get-Command find` · bash: `type -p find` |
| `$TMPDIR` | git-bash: **ПУСТ** (задан только `$TMP`) → `"$TMPDIR/run.log"` схлопывается в `/run.log`, а MSYS2 разворачивает это в `C:\Program Files\Git\run.log` — файл уезжает в каталог установки Git, прогон при этом ЗЕЛЁНЫЙ. Так туда натекло 19 агентских черновиков за месяц (убраны 2026-08-09). Пиши в скретчпад сессии полным путём либо бери `os.tmpdir()`/`tools/lib/temp-root.mjs` | bash: `echo "[$TMPDIR]"` · `node -e "console.log(require('os').tmpdir())"` |
| Пакетные менеджеры | winget 1.29.280 · chocolatey установлен · scoop нет | PS: `winget --version` · `Get-Command choco` |
| Оплаченные грабли | EXP-0005 (CRLF в регэкспах) · EXP-0007 (BOM PS5.1) · EXP-0026 (экранирование в командах доков) · EXP-0027 (сигналы/keep-alive Windows) · EXP-0032 (`grep -i` не кейс-фолдит кириллицу — следствие пустой локали) · EXP-0034 (MSYS2 конвертирует argv) | `grep 'EXP-00' EXPERIENCE.md` |

---

## 5. Работа ВНЕ этого репозитория — ядро голоса владельца

`d:\work\krinik_voice`, приватный `MikalaiKryvusha/krinik-stylometry`, **версия ядра 1.1**
(2026-08-09, круг 5 — АП27–АП37 из волны обзора витрины). KAIF связан с ним ТОЛЬКО канон-именем
файла `AUTHOR_STYLOMETRY.md`; личное творчество владельца в открытую поставку не едет, а имена
его проектов снимаются анонимизацией прямо в генераторе слепка.

Портрет ПРИНЯТ владельцем (решение №63), и приёмка НЕ закрыла три названные слабости:
**(а)** позитивные правила голоса выведены из ОДНОГО корпуса свободной прозы — четыре корпуса
высшего и среднего веса не разобраны ни одним аналитиком; **(б)** документному регистру круг 5 дал
доказательную базу только с одной стороны — анти-портрет; чем документный текст владельца
ЯВЛЯЕТСЯ, портрет не знает; **(в)** корпус рабочей речи QA пуст — задание в
`corpus/qa_work_confluence_jira/_ЗАДАНИЕ.md`, исполнитель — отдельный агент рабочего проекта.
