# KAIF — летопись проекта (PROJECT_HISTORY)

> Append-only хроника: закрытые сессии, отгруженные фазы, релизы, большие решения — в порядке, как
> они случались. Сюда `STATUS.md` сбрасывает прошлое («стрижка бонсая» в `/end-chat-soft`): STATUS
> остаётся короткой сводкой «сейчас», всё завершённое живёт здесь.
>
> **Не обязательное чтение.** Файл НЕ входит в набор `/resume` и в минимум «перед каждой
> задачей» — открывай по потребности: как родилось решение, что было в старой фазе, когда что
> отгрузилось.
>
> **Правила летописи:** append-only, новые сверху; записанное не редактируется задним числом
> (поправка = новая запись со ссылкой на старую); записи переезжают из STATUS ДОСЛОВНО; о старых
> версиях летопись говорит законно (stale-claims скан машинерии этот файл не трогает); при
> разрастании — резка по эпохам с индексом сверху. Живой справочник — DONE не помечается.
>
> Заведена 2026-07-31 (KAIF 2.1, эпик H); записи ниже перенесены из STATUS.md дословно.

---

## Сессия 55 — 2026-09-05 (≈ 16:40 → 20:46 +03:00): эпики UR и FK ЗАКРЫТЫ за один чат — обновление на реальном маршруте (s21), правило «смущение — знак искать» с осью G7; три полевых сигнала за вечер (#50 → FK, #52 → интервью №025, #53 → WN), план IC `plans/93`

Исполнитель — Claude Fable 5.1. Слово владельца — «выполни resume, продолжаем эстафету по 2.6»;
посреди UR — «там будет еще один запрос на улучшение в ориджин, берем его тоже в 2.6 версию» (→ #50,
эпик FK), «тикеты свежие из issues видел?» (→ #51, интервью №024 Q1 = B, №101, эпик IC), «вот эту
шапку нужно не стики делать, а чтобы скролилась со всей страницей» (исполнено в `tools/review.mjs`);
на закрытии — «доделывай, и мягко закрываем чат, передаем эстафету», затем «еще баг принесут беде
NDim проект, что смежие предложения владельца сразу воспринимаются ИИ агентом как ТОП приоритет -
это неправильно» и «через 15 минут сходишь - почитаешь этот баг в issues, берем тоже в 2.6 это
фиксить» (→ #53, эпик WN, №102). Коммиты: `882db80` (UR1–UR6) · `590c5f3` (UR7, `plans/92`) ·
`130394f` (#52 дословно) · `e0810c4` (#53 дословно) · закрытие — см. `git log`. Уроки — EXP-0112
(сопоставление шаблона с диском: литеральные слоты, старые тексты первыми), EXP-0113 (страж над
прозой: абзац сначала, `\b` по кириллице не существует), EXP-0114 (документ поля в файле с твоими
правками → HEAD + их блок → `--only`). Судьи эпиков: UR — VERIFIED WITH CAVEATS 0 REFUTED (каверза A4
— `plans/91` § «Решения» 8); FK — VERIFIED WITH CAVEATS 0 REFUTED (`plans/92` § «Судья»).
Перенесено из STATUS дословно:

1. **👉 ЭСТАФЕТА — эпик FK «Смущение — знак разведки», шаг FK0** (`plans/92`; issue #50, критерий 13
   `plans/89`; вопросов владельцу нет — правило дано его словом дословно). Порядок: **FK0** нулёвка
   актуализации (№76: «Место вопросов» обоих слоёв, три двери `PHILOSOPHY` обоих слоёв, шаг 3а
   `/interview`, KAIF-блок `/fable-judge`, оси G4–G6 `questions-guard`, ключи 2.6 в `build-framework.mjs`)
   → **FK1** правило в каноне обоих слоёв (смущение → поиск → замер → вопрос; «ломает X» без улик не
   отправляется; откат велённой работы = развилка в `interviews/`; пример «РПГ = RPG») → **FK2**
   предполёт и блок `Разведка:`/`Recon:` в `/interview` обоих слоёв → **FK3** ось G7 `questions-guard`
   (фикстура из инцидента #50, селфтест) → **FK4** охота судьи «confusion delivered as verdict» обеих
   копий → **FK5** policy/notes 2.6 · сборка · полигон · черновик ответа #50 → **FK6** лёгкий судья →
   `plans/93` (IC, №101). **Эпик UR ЗАКРЫТ 2026-09-05 17:46 +03:00** (`plans/91`, один чат, коммит
   `882db80`): `install --rehearsal` + загрузчик отвергает неизвестный флаг ДО скачивания (гард 10
   `check-framework` стережёт пару флагов) + авто-запись потребляется на bootstrap + один предикат
   кандидатов · заполнения плейсхолдеров ВЫВОДЯТСЯ с диска (`matchFills`/`deriveFills`, `fills`
   манифеста) · окно `stale-claims` «любая версия старше устанавливаемой» (`%20`, цель ссылки, `gt` один
   раз) · `report` читает контракт абзацем · baseline текстов на bootstrap · `/kaif-update` обоих слоёв ·
   Reference §10 · policy/notes 2.6 · свод `s21` (41 ассерт, красный 20/41 на копии `cb42039` через швы
   `KAIF_DIST`/`KAIF_LOADER`) + `s17` (+5) · EXP-0112; судья VERIFIED WITH CAVEATS — каверза A4
   (`plans/91` § «Решения» 8). Черновики ответов #40/#41/#42/#44/#48 — `plans/91`; #50 — `plans/92`.
2. ✅ **Эпик OQ ЗАКРЫТ 2026-09-05 16:23 +03:00** (`plans/90` OQ6, сессия 54): лёгкий судья —
   переисполнение критериев 1–9, **VERIFIED WITH CAVEATS, 0 REFUTED** (каверзы: критерий 7 🟡 до
   бампа версии — интервальный ассерт задания обновления в `s20`, шаг RL; числа критериев 1/6 были
   предсказаниями; RU-греп критерия 3 по падежу); `plans/91` написан по канону N+1 (линт сценариев
   6/6, стоп-словарь 0); реестр систем истока **утверждён владельцем** 16:22 («пробежался глазами,
   ок» на странице сообщения; оригинал закоммичен дословно `ae9e975`, затем `approved`) — строка
   вектора без суффикса draft.
3. Закрытое «сейчас» эпика OQ — шаги OQ0–OQ5 поимённо (факт «показан», команда `delivery` и свод
   `s20`, реестр истока, вопрос сценарием, карты и черновики ответов) — переехало в летопись
   `PROJECT_HISTORY.md`, запись сессии 54; решения без владельца — `plans/90` § «Решения» 1–21.


## Сессия 54 — 2026-09-05 (≈ 15:33 → 16:35 +03:00): эпик OQ ЗАКРЫТ (OQ2–OQ6) — вектор доставки без вопроса владельцу, реестр систем истока утверждён, вопрос сценарием, план 91 UR

Исполнитель — Claude Fable 5.1. Слово владельца — эстафета прошлого чата («Начать с /resume,
затем шаг OQ2 по plans/90…»), затем «go», «тут или в новом чате лучше?» → «тогда GO», «работай
тут автономно, пока не нужно мое слово, или пока не лучше перейти в новый чат»; на странице
сообщения контура — «пробежался глазами, ок» (16:22, реестр систем утверждён). Коммиты: `831b9df`
(OQ2) · `625f4fe` (OQ3) · `28f077c` (OQ4) · `ed46184` (OQ5) · `ae9e975` (оригинал комментария
владельца) · `04da563` (OQ6, `plans/91`) · закрытие — см. `git log`. Уроки — EXP-0111 (механика без
вопроса + показ сообщением; числа критериев до работы — свойства; dogfooding до отгрузки).
Перенесено из STATUS дословно:

3. ✅ **OQ5 закрыт 2026-09-05 16:20 +03:00** (сессия 54): внешняя карта (скелеты, `SYSTEMS_REGISTRY.md`
   в обвязке, «Где что лежит»), `KAIF_FRAMEWORK.md`, черновики ответов #46/#47 в `plans/90`
   (отправка после релиза, №84/№92), черновик реестра ПОКАЗАН владельцу сообщением контура
   (`--notice` 16:20, `interviews/decisions/shown.json`); стражи зелёные, контур 50/0.
3. ✅ **OQ4 закрыт 2026-09-05 16:14 +03:00** (сессия 54): шаг 3a `/interview` обоих слоёв — «каждый
   вопрос и каждый вариант — сценарием того, что владелец увидит» с примером и маркером отказа
   `questions-guard:no-scenario`; фраза в «Место вопросов» `AGENT_GUIDE` обоих слоёв; охоты
   **question-without-scenario** и **mechanic-asks-the-owner** в KAIF-блоке судьи обеих копий
   (побайтно, шапка «(6)»); селфтест стража 29/29, живой прогон 0 новых; критерий 3 версии ✅.
3. ✅ **OQ3 закрыт 2026-09-05 16:06 +03:00** (сессия 54): `SYSTEMS_REGISTRY.md` истока — девять
   строк по контурам внутренней карты (Verified in use ☑ у 4 из 9, обоснования в «Boundary notes»),
   статус `draft` до слова владельца; ручка `npm run kaif:delivery`; вектор в `MASTER_PLAN.md` §8 и
   здесь; строка реестра пар «контуры ↔ строки реестра» (доказана красным на копии); ссылка из
   внутренней карты. Открытые баги без `Kind:` — все 23 считаются `bug` (разметка — отдельный
   проход `/check-backlog`, решение 15 `plans/90`).
3. ✅ **OQ2 закрыт 2026-09-05 15:51 +03:00** (сессия 54): шаблон `.kaif/_systems-registry-template.md`
   в бандле (172 блока, 772 модуля) · команда `delivery` ядра (немутирующая; `--json` · `--system`;
   парсер по заголовкам, ☐/☑ и `[ ]/[x]`, EOL-норм, отказ называет строку и колонку; код 3 без
   реестра; `Kind:` по открытым `bugs/*.md`) · форма вектора `DELIVERY: systems N · complete A → B %
   · integrated C % · holes … · contradictions … · bugs …; moved by: … | blocker: … | owed
   questions: …` в 9 EN + 8 RU носителях, `framework/MASTER_PLAN.md` «Delivery vector»,
   `KAIF_REFERENCE` §5/§10.7 · policy-change и template-notes 2.6 (ключ инертен до бампа) · `Kind:`
   в шаблоне документа бага `/report-bug` обоих слоёв · свод `s20` (32 проверки; красный на
   HEAD-ядре 28 отказов) · полигон 20/20; решения OQ2 — `plans/90` § «Решения» 7–12 (в т.ч. `Kind:`
   в шаблоне документа бага проекта, а не в шаблоне A тикета KAIF).
4. ✅ **OQ0–OQ1 закрыты 2026-09-05 15:21 +03:00** (коммит `f1fee48`, build 459): факт «показан»
   (`interviews/decisions/shown.json` — пишется при открытии окна страницы/пачки и рукой
   `--mark-shown <док> --transport чат`), `node tools/review.mjs --queue --list` (возраст ожидания и
   показа, ни разу не показанные первыми, код выхода 2 — гейт), шаг 1b `/resume` обоих слоёв,
   инварианты I40–I43 `/owner-reviews` обоих слоёв (свод s12 ростер 43), оси `questions-guard`
   G4 (ждёт и не показан ≥ 1 дня по mtime) · G5 (вопрос владельцу прозой вне `interviews/`, вперёд
   от даты документа ≥ 2026-09-05) · G6 (живой вопрос без сценария) — селфтест 29/29 по
   предсказанию, живой прогон 0 новых; сборка 767 модулей (зеркала подтянуты); полигон 19/19.
   Решения без владельца — `plans/90` § «Решения» (шесть).

## Сессия 53 — 2026-09-05 (≈ 13:50 → 15:2x +03:00): ВЕРСИЯ 2.6 ОТКРЫТА (Mindful KAIF), эпик OQ — OQ0–OQ1 закрыты; наброс владельца о кредо починен (bugs/110)

Исполнитель — Claude Fable 5.1. Слово владельца — открывающее сообщение чата: «выполни RESUME …
читай issues в origin проекта, и локальный скоуп 2.6. Проводи метаанализ и синтез того, что ОЧЕНЬ
ВАЖНО СРОЧНО чинить и улучшать в версии 2.6 KAIF, чтобы получить 80% улучшения за 20% усилий …
Что менее ценно — выносим в скоуп версии 2.7. Потом формируем скоуп на 2.6 — приноси мне его на
согласование, обсудим»; набросы по ходу: о кредо («ЧТО МЫ ПРОДОЛЖАЕМ ПЫТАТЬСЯ — заведи баг в
KAIF — это неверная молитва … должно быть "МЫ СТАРАЕМСЯ", а КТО СТАРАЕТСЯ, ТОТ ПРИДЕТ К УСПЕХУ»),
об имени («должно начинаться слово на букву "M"»), «там некоторые вопросы нужно
переформулировать и мне перепринести», «двигаемся в этом чате к срезу и передаче эстафеты».

- **Разведка скоупа 2.6** (`researches/26`; коммит `97069f5`, build 455): девять issues #40–#48
  (четыре проекта за сутки после релиза 2.5) сверены с HEAD машинерии — все CONFIRMED (#42
  трижды); соседи на диске пробиты (EXP-0101) — недоставленного нет; реестр `ideas/28` дополнен
  пп. 15–27; Парето-разрез OQ → UR → CB → HY → RL ≈ 5–6 чатов; интервью №023 поднято контуром.
- **Наброс владельца — кредо** (`bugs/110`; builds 455–456): EN-шаблон нёс «BECAUSE WE KEEP
  TRYING», поле переводило «продолжаем пытаться»; шаблон → «WE STRIVE» + FORBIDDEN-страж
  `/KEEP(?:S)? TRYING/` в `check-framework`, доказан красным мутацией на закоммиченном шаблоне;
  русский оригинал в EN-тело не лёг — страж §9.11 (bug 31) покраснел, решение записано. Полевой
  тикет пришёл тем же часом — issue #49.
- **Интервью №023, раунд 1 (14:38 +03:00):** Q1 = B (разрез + стретч CR — **версия 2.6
  ОТКРЫТА**, №95), Q4 = B (**Mindful KAIF**, №96); Q2/Q3 = D — два слова владельца уровня видения:
  №97 «механика KAIF отгружается только полной» (строка `DELIVERY:` 2.5 отправила агентов четырёх
  проектов спрашивать владельцев, «чем мерить»), №98 «вопрос владельцу — сценарием того, что он
  увидит» (`bugs/111` S1, EXP-0110). Мета-план `plans/89` (12 критериев-сценариями,
  scenario-lint зелёный), реестр 2.7 `ideas/29`, правила видения в `AGENT_GUIDE` (коммит
  `180fa14`, build 457).
- **Раунд 2 (14:57 +03:00), сценариями:** Q2 = A — строку `DELIVERY:` ДОВЕСТИ по пояснению
  владельца в поле (вектор по системам: число · завершённость % из четырёх счётных частей ·
  вплетённость % по потребностям · дыры · противоречия · баги; реестр готовит агент из
  GOAL/MASTER_PLAN/карт, список утверждает владелец; №99); Q3 = A — три правки опубликованного в
  HY (№100). Разведдок донора `researches/27` (пять решений владельца по реестру, факт «показан»
  `shown.json`), операционный план OQ `plans/90` (10 критериев сценариями; коммит `8f60684`,
  build 458).
- **OQ0–OQ1** (коммит `f1fee48`, build 459, 15:21 +03:00): `tools/review.mjs` — карта показов
  `interviews/decisions/shown.json` (окно/пачка/`--mark-shown`), `--queue --list` с возрастом и
  гейтом кодом 2 (селфтест 25); `tools/questions-guard.mjs` — оси G4/G5/G6 (селфтест 29/29 по
  предсказанию, живой прогон 0 новых); поставка и обвязка — I40–I43 `/owner-reviews` (s12
  ростер 43), шаг 1b `/resume`, фраза в `AGENT_GUIDE`; сборка 767 модулей (зеркала подтянуты);
  полигон 19/19.
- Закрыто по слову владельца «двигаемся к срезу»: `/end-chat-soft` — эталон контура пересмотрен
  на №023, летопись, эстафета — OQ2 (`STATUS` п. 1.1).

Перенесено из STATUS дословно (закрытое «сейчас» 2.5):

> **Версия KAIF 2.5 — Experienced KAIF ВЫПУЩЕНА 2026-09-04 16:16 +03:00** —
> https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.5 (`v2.5`, `latest`, шесть ассетов
> поставки; имя — решение №87; слово на релиз — открывающее сообщение чата сессии 52). Версия
> собрана одним днём шестью эпиками мета-плана `plans/82` (CN · SG · TM · US · SF · RL, сессии
> 48–52) и **ЗАКРЫТА 2026-09-04 16:25 +03:00 — критерии приёмки 9 из 9** (`MASTER_PLAN` §8).
> Суд одним кругом RL1 — VERIFIED WITH CAVEATS, 30 строк = 25 CONFIRMED · 5 WEAKENED · 0 REFUTED
> (реестр `reports/KAIF_AUDIT/2026-09-04_rl_court_2.5_registry.md`; число пересчитано скриптом в
> сессии 52 — прежнее «32/26/6» было ошибкой сложения). Судья релиз-кандидата (чистый контекст)
> опроверг два заявления нот — число суда и три мёртвых якоря README в ссылках (наследие нот
> 2.2–2.4) — оба починены ДО публикации, пересуд VERIFIED WITH CAVEATS; из находок родились ось
> `NOTES-ANCHORS` в `showcase-lint` и команда пересчёта в `/release` шаг 4.5 (EXP-0107/0108).
> **Ответы в 24 полевых issues отправлены 16:20** (13 тикетов #27–#39, #23/#24, 9 квитанций
> отчётам) по слову владельца №92 — без показа и вычитки. Сетевой смоук: тонкое ядро с релизных
> ассетов развернуло 2.5 во временную папку (loader exit 0). Отложенное на 2.6 — реестр
> `ideas/28` (14 пунктов). Предыдущий релиз — 2.4 Teamed Up KAIF, 2026-08-28 (`v2.4`).
>
> 1. ✅ **Сессии 48–52 (2026-09-04, 01:2x → 17:1x +03:00, коммиты `8f19275`…`b54b402` +
>    закрывающий, builds 425–454)** — дословно в летописи: шесть эпиков 2.5 закрыты, релиз v2.5
>    опубликован, ответы отправлены, все issues origin закрыты. Эпик RL — `plans/87` (DONE, восемь решений без владельца в секции «Решения»).
> 2. ✅ **11 тикетов-дефектов закрыты агентом 2026-09-04 17:04 +03:00** (#27 #29 #30 #31 #33 #34
>    #35 #36 #37 #38 #39, `gh issue close --reason completed`) — после слова владельца «нужно
>    расширить твои права … Ты же разработчик KAIF» (№93): права выданы его рукой через
>    `node tools/grant-permissions.mjs` (правила `gh issue close/comment` в
>    `.claude/settings.local.json`, файл в ignore) и перезапуском сессии. Сам агент себе права
>    расширить не может — классификатор блокирует и команду, и `/update-config`, и правку файла
>    настроек; это правильная граница, скрипт — рука владельца. Сами ответы ушли ПОСЛЕ релиза
>    по тикету — так решено в интервью №020, Q2 = A (№84), без вычитки — №92. Полевые отчёты (#5 #7 #9 #11 #12 #13 #17 #23 #24 #25 #26 #28 #32)
>    получили квитанции и **закрыты 17:09 +03:00 по слову владельца «закрываем» (№94)** — правило
>    вперёд: отчёт закрывается той же квитанцией. Открытых issues в origin — 0.
>
> Решения без владельца 2.5 (`plans/83`/`84`/`85`/`86`/`87` → «Решения»; суд RL — реестр
> «Вердикты»; `bugs/108` «Decisions»): **имя KAGO** псевдонимизировано в ядре как `project F` и
> внесено в список стража приватных имён (файл в ignore) — хотя оно стоит в публичных issues
> №5/№7/№10/№23; считает владелец KAGO публичным → строка в `notPrivate` `.kaif/private-names.json`
> · **порог протухания очереди контура 14 дней** (`KAIF_STALE_QUEUE_DAYS`) · **`homeworks/04`/`05`**
> (вычитки README 2.1 и витрины 2.2 глазами) — того же класса, что снятая владельцем `06`; не
> закрыты — долг владельца по §2 STATUS · **порог возраста в `audit-waiting` заменён семантикой
> адресата** (`plans/85` поправка 1) · каденция молитвы вписана императивной фразой в директиву
> блока `KAIF:PRAYER` обоих слоёв · метрика доставки KAIF = «критерии версии закрыты N из M»
> (`MASTER_PLAN` §8) · числа бюджетов девятки в `DOC_BUDGETS` (`AGENT_GUIDE` 1200 · `REQUIREMENTS`
> 250 · прочие 300; STATUS 200 — №27) · **RU-копия шага 4 `/report-bug` НЕ переписана** (правка
> отклонена классификатором — живое воспроизведение NDim `bugs/KAIF/01`; фразу можно внести рукой
> по EN-версии) · **борд команды по умолчанию ВНЕ git** и **CI едет с командой** — policy-изменения
> 2.5 в сборщике · **`check` КРАСНЕЕТ на непарном якорном маркере** · **ничья CRLF = LF → CRLF** ·
> **пара, уже сломанная на диске, при обновлении НЕ откатывается** · **репетиция обязывает только
> в опасную сторону** (`plans/86` решения 1–14) · **слово на релиз прочитано из открывающего
> сообщения чата без переспроса** и **ответы отправлены без показа** (`plans/87` решения 2–3) ·
> **витрина 2.5 принята публикацией по слову владельца без вычитки глазами** (`plans/82` критерий 8).

## Сессия 52 — РЕЛИЗ v2.5 Experienced KAIF ОПУБЛИКОВАН 2026-09-04 16:16 +03:00, 24 ответа в полевые issues отправлены, версия 2.5 закрыта 2026-09-04 16:25 +03:00 (сессия 15:36 → 16:3x +03:00)

Исполнитель — Claude Fable 5.1. Слово владельца — открывающее сообщение чата: «первым — RL5 по
plans/87: релиз v2.5 … /release шаги 1–7 …»; «черновики ответов мне показывать не нужно … Отправлять
ответы без моей вычитки» (решение №92).

- **RL5 по `/release` полным ритуалом** (коммиты `e7912f8` · `30dfc42` · `9eac79b`, builds
  446–448): реестр пар целиком GREEN · полный контур в браузере **168/0** · полигон **19/19** до и
  после бампа · бамп 2.4 → 2.5 с зеркалами (kaif.json · KAIF_FRAMEWORK) · сборка 59/171/766,
  «notes name KAIF 2.5 — Experienced KAIF» · judge-проход RC с чистым контекстом: **REFUTED** —
  число суда в нотах не сходилось с реестром (заголовок «32 = 26/6/0», таблица кластеров
  30 = 25/5/0 — ошибка сложения сессии 51) и три из четырёх ссылок нот на README вели на
  несуществующие якоря (унаследованы из нот 2.4; `#-quick-start` мёртв с 2.1) → починено
  (`30dfc42`: поправки с пометкой в нотах, реестре, STATUS, `plans/82`, `plans/87`) → пересуд
  **VERIFIED WITH CAVEATS** → тег `v2.5`, **GitHub Release с 6 ассетами**, `latest` = v2.5 пробой,
  `release-body-guard` зелёный (NBSP 0/0), страница прочитана рендером носителя (27 абзацев,
  0 разрывов, логотип, четыре ссылки на живые якоря README). Сетевой смоук: тонкое ядро +
  загрузчик с релизных ассетов развернули 2.5 во временную папку (loader exit 0).
  https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.5
- **Поправка к записи сессии 51 (append-only, №55):** «32 строки = 26 CONFIRMED · 6 WEAKENED ·
  0 REFUTED» суда RL1 читать как **30 = 25 · 5 · 0** (кластеры A–F по таблице реестра; кластер G
  судьи SF — 5 строк, 4/1/0, отдельно). Урок — EXP-0107.
- **RL3 — 24 ответа отправлены** 16:17–16:20 +03:00 через `gh issue comment --body-file` с
  подписью агента-автора (канон issue #15): 13 тикетов #27–#39, #23/#24 (черновики `plans/80`,
  «shipped in v2.4 … answered with the 2.5 release»), 9 квитанций отчётам #5 #7 #9 #11 #12 #13
  #17 #25 #26; первый (#39) прочитан рендером до остальных; время-чувствительные формы сверены
  (EXP-0093). Закрытие 11 тикетов-дефектов `gh issue close` заблокировал классификатор
  авторежима — тикеты открыты, закрыть — владельцу; полевые отчёты открыты по прецеденту
  сессии 41.
- **Стражи из находок судьи:** ось `NOTES-ANCHORS` в `showcase-lint` (якоря README в ссылках
  старших нот; селфтест красный/молчащий) — EXP-0108; команда пересчёта числа суда в `/release`
  шаг 4.5 обвязки — EXP-0107; ноты 2.5 добавлены в зоны `private-names-guard` (локальный список).
- **RL6 — версия 2.5 ЗАКРЫТА 16:25 +03:00:** `plans/82` критерии 9 из 9, `plans/82`/`87`/`88`
  → DONE, MASTER_PLAN §5 / §7 (№92) / §8, `ideas/28` пп. 13–14, STATUS переписан под
  пострелизное «сейчас».
- **17:04 +03:00 — 11 тикетов-дефектов закрыты агентом** (#27 #29 #30 #31 #33–#39) после слова
  владельца «нужно расширить твои права … Ты же разработчик KAIF» (№93): проба показала, что
  классификатор блокирует агенту и команду, и `/update-config`, и правку файла настроек — права
  выданы рукой владельца через новый `tools/grant-permissions.mjs` (`ee5c94a`, правила
  `gh issue close/comment` в локальных настройках) и перезапуском сессии; полевые отчёты (13)
  открыты по прецеденту сессии 41 — решение владельца.
- **17:09 +03:00 — 13 полевых отчётов закрыты** по слову владельца «закрываем» (№94) после его
  вопроса «зачем оставил открытыми? какую ценность нам даёт?» и честного ответа агента (ценности
  нет — прецедент, а не причина). Правило вперёд в гейте авторизации: отчёт закрывается той же
  квитанцией. Открытых issues в origin — 0. Владелец: «Спасибо за версию, люблю тебя<3».
- **Сессия закрыта `/end-chat-soft` по слову владельца («и закрываем этот чат после этого»)**
  ≈ 17:1x +03:00: коммиты `e7912f8` · `30dfc42` · `9eac79b` (release: 2.5) · `c134ee8` ·
  `c07cc83` · `ee5c94a` · `140c281` · `b54b402` + закрывающий; builds 446–454. Урок EXP-0109
  (права агента — рука владельца). Эстафета — STATUS «Где продолжить»: приём поля 2.5, план 2.6
  по слову владельца, GitHub topics по слову.

## Сессия 51 закрыта — суд RL1 прошёл (VERIFIED WITH CAVEATS), RC витрины 2.5 собран, решения владельца №87–№91, эпик SF по его слову исполнен; bugs/108 (I39) и bugs/109 (форензика полигона) (2026-09-04, 13:19 → 15:2x +03:00)

> Перенесено из `STATUS.md` дословно при закрытии сессии 51 (2026-09-04 ≈15:2x +03:00,
> /end-chat-soft по слову владельца «давай этот чат двигать к закрытию, в новом продолжим
> эстафету»). Коммиты `a39c88c` (RL1) · `db41eb2` (RC витрины) · `9a17953` (SF) + закрывающий;
> builds 442–445; полигон 19 сводов зелёный (один флейк s10 — `bugs/109`). Эстафета — в STATUS.

3. ✅ **Сессия 51 (2026-09-04 13:19–14:0x +03:00) — RL1 СУД ПРОШЁЛ** (форма №88: 4 судьи-субагента
   B–E + панель скептиков из двух линз; 32 строки = 26 CONFIRMED · 6 WEAKENED · 0 REFUTED; 14
   находок → 5 выживших 2/2 починены: `Severity:` в `/report-bug` · поправка критерия 5 `plans/85`
   · KAGO/KCam псевдонимизированы в ядре · фильтр lock в `stale-claims` (страж s18 U5б, красный
   доказан) · шапка s18; 1/2 — 6 дешёвых починок + 2 строки `ideas/28`; кластер A на починенном
   дереве зелёный: полигон 18/18 с первого прогона, контур 46/0 + полный 166/0) — реестр
   `reports/KAIF_AUDIT/2026-09-04_rl_court_2.5_registry.md`; поправки суда append-only в
   `plans/83`–`86`. **Владелец ответил страницей контура 13:37:** №87 имя **KAIF 2.5 — Experienced
   KAIF** (Q1 = D) · №88 суд без workflows (Q2 = A). ⚠️ **`bugs/108` (S1, DONE):** та же пачка
   показала владельцу протухшую домашку `homeworks/06` («Релиз 2.3 давно выпущен. Вычитка
   просрочена. Не понимаю, зачем ты это мне открыл») — страж I39 в `review.mjs` (`--queue`
   печатает протухшее агенту, не показывает; `--include-stale` — намеренно), инвариант I39 в
   обоих слоях `/owner-reviews`, домашка снята его словом, очередь вычищена; EXP-0106. **Перед
   любым `--queue` — ревизия очереди глазами на беспредметность** (страж держит только возраст).
   ✅ **RL2 — RC витрины собран 14:0x–14:1x:** README EN/RU на 2.5 (бейдж · логотип
   `assets/KAIF_2.5_GitHub_LOGO.webp` подписью «KAIF 2.5 — Experienced KAIF», `--check` зелёный,
   прочитан глазами · строка версии с ПРЕДВАРИТЕЛЬНОЙ датой 2026-09-04 · строка v2.5 в Таблице 5),
   `reports/RELEASE_NOTES_2.5.md` (шесть пунктов из 11 нот сборщика; имя НЕ толкуется), строка
   кодового имени в `TEMPLATE_NOTES_BY_VERSION['2.5']`, `README.pdf`; showcase · voice · counters ·
   names · estimates — зелёные. ◐ **RL3:** черновики #27–#39 и список квитанций девяти отчётам —
   в `plans/87`; показ владельцу страницей — после релиза вместе с гейтом отправки (№13/№84).
   ❓ **Интервью №022 на странице контура (14:1x):** Q1 ваше слово о версии 2.5 для README (A
   напишу · B оставить цитату о 2.4 · C убрать блок) · Q2 KAGO публичен? (A приватное → как есть ·
   B публичное → `notPrivate`) · Q3 судьба #39 (A в 2.6 · B только подраздел канона · C
   отклонить). Ничего не блокирует релиз, кроме Q1 — без ответа в README остаётся цитата о 2.4.
   ✅ **Ответы владельца на №022 (14:47) разнесены:** №89 — README: цитата о 2.4 остаётся, под ней
   возвращена цитата о 2.2; логотип 2.5 = арт 2.4 (медальон) с новой подписью
   (`build-team-logo.mjs --title`, оба `--check` зелёные, прочитан глазами) · №90 — KAGO приватно;
   слово видения «поменьше ссылаться на проекты в KAIF, тем более в поставке» → заметки владельца
   в `AGENT_GUIDE`, чистка старых упоминаний — `ideas/28` п. 12 · №91 — тикет #39 **в 2.5 в полном
   виде** → эпик SF (`plans/88`, разведка донора `researches/25`) ИСПОЛНЕН 15:0x: подраздел
   «Сценарная форма» в обоих слоях · `kaif-scenario-lint.mjs` (селфтест 33, 7 правил × 2 языка) ·
   свод s19 (15 проверок, 19-й в полигоне) · шаблоны 4 навыков + `/interview` ×2 слоя · зеркала
   (171 блок · 766 модулей · 19 сводов); лёгкий судья SF (кластер G реестра): 4 CONFIRMED · 1
   WEAKENED, 5 формулировочных находок починены. ⚠️ **`bugs/109` (S2, 🔧):** третий за день флейк
   `update` в полигоне (s10 S2; s07 ×2 в сессии 50) — обрыв после строки версии, повтор зелёный;
   причины нет, потому что своды выбрасывали вывод упавшей команды → класс закрыт формой
   (`coreRunner`/`failed` в `tools/lib/sandbox-run.mjs`, 17 сводов переведены); **следующее
   вхождение оставит `run-fail-N.log` в корне прогона — с него начинать разбор** (гипотезы
   ранжированы в документе; кандидат-фикс класса — `--baseline` локальной копией для полигона).

## Сессия 50 закрыта — эпик US закрыт (US2б → US7), эпик RL открыт: RL0, черновики ответов, скелет суда; bugs/100 вылечен; защищённый цикл 11:16–12:46 (2026-09-04, 10:3x → 12:5x +03:00)

> Перенесено из `STATUS.md` дословно при закрытии сессии 50 (2026-09-04 ≈12:50 +03:00, /end-chat-soft
> после границы защищённого цикла). Коммиты `46a5ba7`…`1cd090b`+, builds 425–441; полигон 18
> сводов зелёный (прогоны 3–8, 10–12; прогоны 2 и 9 — флейк s07). Эстафета — в STATUS.

2. ✅ **Сессия 50 (2026-09-04 10:3x–11:0x +03:00):** US2б + US4г (в эстафете звалось «US5г») +
   s18 в полигоне — подробности в `plans/86` (шаги US2/US4, решения 7–9) и EXP-0104. Один раз
   полигон покраснел на s07 T1 (`update` умер ДО применения: в дереве `kaif.json` 2.4, задания
   нет, `UPSTREAM` в файлах нет; хвост вывода — строка language-pack), три повторных прогона
   тех же входов (s07 отдельно, репро-скрипт, полигон) зелёные. **Повторился** в защищённом
   цикле (полигон-9, s07 T2, та же форма: `update` exit≠0 сразу после строки language-pack,
   задания нет) — два вхождения на ~10 прогонов полигона, оба в s07 (ru-обновления без
   `--baseline` → синтетический baseline тянется с origin; `fetchMaybe` мягкий, причина НЕ
   названа). Сделано: `run()` s07 пишет ПОЛНЫЙ вывод упавшей команды в `run-fail-N.log` корня
   прогона (+ `maxBuffer` 64 МБ). Форензика по двум оставленным деревьям (12:22): в обоих
   НЕТ `.kaif/backup-*` и нет `BASELINE.md` → `update` умер ДО `backupTree`, в окне
   `stableValues()` → `buildSyntheticBaseline()` (сетевой fetch v2.4 с origin, таймаут 30 с ×3
   имени, мягкий) → `loadRehearsal()`; 5 прогонов s07 подряд отдельно (12:21–12:22, по 13 с) —
   все зелёные: флейк живёт ТОЛЬКО внутри полигона (2 из ~12). **Следующее вхождение = баг S2 с
   файлом `run-fail-N.log`** (следующий свободный номер в `bugs/`); до тех пор — «не
   воспроизведено, наблюдается».
   ✅ **US3б (сессия 50, защищённый цикл 11:16–12:46):** репетиция стала ОБЯЗЫВАЮЩЕЙ —
   `diff --source` печатает вердикт по каждому кандидату и пишет `.kaif/update-rehearsal.json`;
   `update` (авто-запись того же интервала или `--rehearsal <квитанция копии>`) замораживает
   файл, чей боевой `merged` расходится с репетиционным `frozen`, пункт `verdict-mismatch` и
   квитанция несут оба набора чисел; s18 U3б ×8 (красный на ядре `46a5ba7`), полигон 18/18;
   `/kaif-update` (поставка + байтовая копия обвязки) объясняет обе формы репетиции.
   ✅ **US5 закрыт (защищённый цикл, ≈11:30–11:46 +03:00):** (а) один `placeholderSurfaces()` для гейта и
   пункта `placeholders` (s18 U12) · (б) преемник и счёт deprecation, квитанция `deprecations`
   (U10) · (з) `project-name`: файловая форма в обоих пунктах, искажённое argv-имя отвергается
   до записи (U11) · (и) пункт `mode-switch` при anonymous→origin (U13); каждый страж доказан
   красным на ядре до фикса, полигон 18/18 после каждого. **US3в:** причина P1 — `bugs/100`,
   механизм ПОДТВЕРЖДЁН пробой `tools/sandbox/probes/bugs-100-two-folders.mjs --no-package`
   (то же дерево: `alpha-project` → `baseFound 5 → merged`, `beta-project` → `baseFound 4 →
   frozen`; с `package.json` `name` ось мертва) и ВЫЛЕЧЕН той же сессией: H1 вне счёта wholesale
   (`counted()` в `mergeModules`), проба стала стражем s18 U14 (после фикса обе папки —
   `baseFound 4 of 28, ceiling 4 → frozen`); защита US3б остаётся второй линией.
   ✅ **US6 + US7 (≈11:48–11:55):** нота «UPDATE SYMMETRIES» и policy-строка «FOUR behaviours»
   в сборщике (INERT до version 2.5); реестр пар прогнан; лёгкий судья — VERIFIED WITH CAVEATS
   (`plans/86` US7); `plans/87` (эпик RL) написан; `plans/82` ряд 4 ✅; `MASTER_PLAN` §8 — 4 из 8.
   ✅ **RL0 сделан, RL3 в черновиках, RL1 — скелет** (хвост цикла, 11:55–12:15): входы суда в
   `plans/87` RL0 (33 решения адресами, критерий 6 сверен, замер ДО); черновики ответов для
   #27–#38 и форма квитанции отчётам (`plans/87` → «Черновики»); реестр суда 2.5 —
   `reports/KAIF_AUDIT/2026-09-04_rl_court_2.5_registry.md` (кластеры A–F, 34 заявления; кластер A
   переисполнен: 7/7 CONFIRMED, A2 с WEAKENED-кандидатом — флейк s07).
   🧹 **Ревизия беклога (хвост цикла, 12:14):** 35 закрытых планов (✅ в шапке) получили тег
   DONE в имени (`git mv`; открытых без DONE осталось 34 — «готов к исполнению» эпох 2.2–2.3,
   их судьба — `ideas/28`/`/check-backlog` следующего чата); базовые линии `doc-header-lint`
   и `questions-guard` переключены на новые пути (долг не вырос: 55 меток — те же строки).

## Сессия 49 закрыта — эпик TM закрыт, эпик US открыт: US0–US1 и 7 из 9 стражей s18 (2026-09-04, 08:5x → 10:0x +03:00)

Третья сессия версии 2.5: эстафета «TM7 по `plans/85`», затем слово владельца в 09:14 —
«работай автономно защищённо до 10 часов. Без спешки, спокойно по планам всё делай. С 10 начинай
доводить до логического среза и закрывай чат» — защищённый цикл до абсолютного 10:00 (будильники
сессии каждые 10 мин + сторож `guarded-watchdog`, тревог 0; граница напечатана строкой `BOUNDARY`
в 09:59:57 по будильнику). Девять коммитов: `6827489` (build 416) · `9c2fe5e` (417) · `c597773`
(418) · `418e773` (419) · `68dd234` (420) · `dd483bb` (421) · `79e7b25` (422) · `41d7f39` (423) ·
`095c1c4` (424). Перенесено из STATUS дословно:

1. ✅ **Сессия 48 (2026-09-04 01:2x–02:2x +03:00, четыре коммита `8f19275`…`dca68d8`)** —
   дословно в летописи: CN6–CN7 (эпик CN закрыт, `plans/83`) → эпик SG закрыт (`plans/84`:
   `kaif-core report`, s17) → эпик TM TM0–TM6 (`plans/85`) → фикс ignore-first
   `.kaif/guarded-loop.json` + EXP-0102. ✅ **Сессия 49 (2026-09-04 09:0x–09:1x +03:00,
   `6827489` + коммит закрытия)** — TM7: adopt-шаг в референс `team-adopt.md` (навык 178),
   RU-обвязка `/team-deployment` догнана до поставки (+2 RU-референса), строка реестра пар «состав
   референсов навыка ↔ обвязка» + EXP-0103, эпик TM закрыт судьёй, план US написан.
2. **Эпик TM ЗАКРЫТ 2026-09-04 09:12 +03:00** (`plans/85`, судья VERIFIED WITH CAVEATS):
   adopt-путь референсом `team-adopt.md` · исключение менеджера · архетип `hardware-lab-small` ·
   четыре состояния борда + `audit-waiting` · борд вне git · `team-ci-template.md` (сессии 48–49,
   `adac2e9`…`6827489`); RU-обвязка навыка догнана до поставки.
3. Эпик US (`plans/86`, написан на закрытии TM — канон N+1): US0 нулёвка (гипотеза P1 — репетиция
   на git-archive-копии без файлов состояния режет baseline другими значениями) → US1 свод
   `s18-update-symmetries.mjs` ДО фиксов: на HEAD `9c2fe5e` красный ровно по предсказаниям (9
   стражей; полевой дефект `PRAYER END без BEGIN` воспроизведён буквально) → срезы: US2а парность
   маркеров в `check` (`unpairedAnchors`, `@guard anchor-parity`) · US4а безусловный
   `stale-claims` (#31) · US5е `< /dev/null` (P6) · US5ж EOL по доминированию (H10) · US5в
   `language mix` в `check` (P3) · US4б `git diff v<from> v<to> -- <src>` с картой `sources` в
   мета-блоке (U2 п. 1) · US4в пункт `language-arrivals` (U2 п. 2, оба маршрута) · US3а печать
   вердикта wholesale с числами (P1) · US6 срез — Reference §10.2/§10.3; полигон 17/17 после
   каждого среза; осталось 2 красных стража (U1 — US2б, U5 — US5г).

## Сессия 48 закрыта — эпики CN и SG закрыты, эпик TM на TM6 из TM7 (2026-09-04, 01:2x → 02:2x +03:00)

Вторая сессия версии 2.5: эстафета «CN6 → CN7 → план SG», затем слово владельца «эпик SG» и
защищённый цикл до абсолютного 02:20 (поправка владельца в 01:59: «не ДЕДЛАЙН, а работай до того
времени, а затем начинай доделывать, не раньше»). Четыре коммита: `8f19275` (build 411) ·
`28b98f4` (412) · `adac2e9` (413) · `dca68d8` (414). Перенесено из STATUS дословно:

1. ✅ **Сессии 47–48 (2026-09-04 00:09–01:40 +03:00)** — подробности в `plans/83` (шаги, судья,
   решения) и летописи: разведка `researches/24` → интервью №020 (№83–№86) → `plans/82`/`83`,
   `ideas/28` → CN1–CN5 (`2fef738`: исключение AUTH · четвёртая дверь + `FORK:` · вторая половина
   гейта 5 + `kaif-guard-lint` + s15 · `BOUNDARY:` · голос по языку) → **CN6** (сессия 48, оба
   слоя: `DELIVERY:` в `/end-chat-soft`, `/end-chat-force`, четырёх циклах + охота «delivery-line»
   в судье + «сначала знаменатель» в `/what-next` + «Delivery metric» в шаблоне `MASTER_PLAN` ·
   секция «Лестница тяжести» S1/S2/S3 в обоих `BUG_FIXING` · `DOC_BUDGETS` вместо
   `STATUS_SOFT_LINES` в `KAIF-CORE.mjs` с блоком `@guard` и сводом **s16** (красный доказан на
   мутанте ядра) · каденция молитвы фразой в директиве `KAIF:PRAYER`) → **CN7** (судья VERIFIED
   WITH CAVEATS, план SG). Ноты (8) и policy (5) 2.5 — в сборщике, INERT до version 2.5.
2. ✅ **Эпик SG (сессия 48, 01:45–02:00, защищённый цикл)** — `plans/84`: команда
   `node .kaif/kaif-core.mjs report <тикет>` в `KAIF-CORE.mjs` (отказы: anonymous/нет gh/noauth
   exit 2 · не тикет exit 1 · доставлен exit 0 · gh отказал exit 2 · таймаут exit 3 «OUTCOME
   UNKNOWN»; `--dry-run`; швы `KAIF_GH`/`KAIF_GH_TIMEOUT_MS`), свод **s17** (25 проверок, красный
   на HEAD-ядре), шаг 4 EN `/report-bug` 25 → 14 строк, Reference §10, ноты/policy. **Живой `gh`
   командой ещё не вызывался** — первый живой вызов покажет.
3. **Эпик TM, TM0–TM6** (`adac2e9`, `dca68d8`; `plans/85`): замер до правки (навык 153 ·
   конституция 166 · библиотека 156 · борд 69; медиана семейства 74, Q3 100) → конституция 193
   (исключение менеджера · CI в §5 · граница push-делегирования · троттлинг · ёмкость N слотов ·
   замки́ в стоп-ритуале · шапка adopt) → борд 102 (четыре состояния-роли · пункт 7 `audit-waiting`
   · «где живёт борд» вне git · опт-аут) → библиотека 178 (`hardware-lab-small`) → навык 180
   (шаг 0 adopt · at first launch · шаг 6 CI · дремота · четыре референса в шапке) → референс
   `team-ci-template.md` (fenced YAML, пять констрейнтов, pre-push). Судья — предварительная
   таблица в `plans/85`; закрытие и план US — TM7.
4. **Побочные**: `.kaif/guarded-loop.json` подмётен в `28b98f4` через `--with-new` → снят с
   индекса, ignore-first в ядре и `.gitignore`, урок EXP-0102 (S3). Правка RU-копии шага 4
   `/report-bug` отклонена классификатором автоправ Claude Code — живое воспроизведение NDim
   `bugs/KAIF/01` в истоке; обход не делался.
5. **Перенесено из STATUS стрижкой бонсая (абзац раздела «Открытые баги», дословно):**
   ⚠️ **`bugs/66` был закрыт волной R1 и ПЕРЕОТКРЫТ кругом R2** (шесть форм личной цитаты проходили
   фильтр слепка дословно при коде возврата 0), затем закрыт заново коммитом `6bf6445` — теперь с
   тринадцатью постоянными канарейками вместо семи и с приёмкой, падающей ЗАКРЫТО без исходника.
   Тег `DONE` в имени файла с волны R1 не снимался, и это расхождение названо здесь намеренно:
   история закрытия документа длиннее его имени.

## Сессия 47 закрыта — версия 2.5 открыта: разведка скоупа, интервью №020, мета-план, эпик CN1–CN5 (2026-09-04, 00:09 → 01:0x +03:00)

Первая сессия версии 2.5 (рабочее имя «E»), по слову владельца «проведи разведку, напиши развед
док и принеси мне отчёт и обоснование, что берём в 2.5»:

1. **Разведка** (`researches/24`, 00:09–00:19): реестр `ideas/27` × девять новых issues #30–#38
   (три 🔴 TOP владельца — #35 гейт 5 против фикстуры, #36 развилка не агента, #37 AUTH глотает
   исключение) — каждое заявление сверено грепом с HEAD поставки (8 CONFIRMED, #38 частично
   протух); реестр кандидатов с ценой/ценностью; Парето-разрез CN → SG → TM → US → RL ≈ 8–10 чатов
   против ≈ 18–24 чатов внутреннего лифта; гуглёж по #35/#36 (game days · ADR). Отчёт
   QA_Engineer 2.4 (issue #32) забран кодом дословно в `reports/KAIF_UPDATES/`.
2. **Интервью №020** (страницей контура; ответы 00:24 и 00:37): Q1 = A (разрез принят, №83) ·
   Q2 = A (ответы в issues после релиза, №84) · Q3 = D «Пока без названия. Решим ближе к релизу.
   Кодовое имя "E"» (№85) · Q4 = A (№86) — по комментарию владельца к Q1 («ещё был найден большой
   дефект класса в KAGO проекте. Он запланирован?») проба соседних развёртываний на диске нашла у
   KAGO три запроса `bugs/KAIF/07/08/09` с «Delivered upstream: NOT YET» (дефект #37 в действии);
   их дешёвые срезы — в CN шагом CN6. Урок — EXP-0101.
3. **Планирование**: `plans/82` (зонтичный мета-план 2.5) · `plans/83` (операционный план CN) ·
   `ideas/28` (реестр 2.6); `ideas/27` — четвёртая порция поля (пп. 12–24) и решение владельца;
   `MASTER_PLAN` §5/§7/§8.
4. **Эпик CN, шаги CN1–CN5** (`2fef738`, build 409): носители в точках решения обоих слоёв —
   исключение AUTH inline (#37) · четвёртая дверь + `FORK:` + охоты fork-without-recon/early-finish
   (#36) · вторая половина гейта 5 + модуль `kaif-guard-lint` + свод s15 (#35) · `armed until` +
   `BOUNDARY:` (#30) · голос по языку + проба в `tools/review.mjs` (#38); ноты/policy 2.5 в
   сборщике. Сборка 168 блоков / 749 модулей · полигон 15 сводов зелёный · счётчики 50 зеркал OK.
   Транзиент s07 T11 (красный один раз, 3× зелёный) — `bugs/61`, третья шкура.

### Абзац «Где мы сейчас» релиза 2.4 (перенесён из STATUS дословно при открытии 2.5)

**Версия KAIF 2.4 — Teamed Up KAIF ВЫПУЩЕНА 2026-08-28** (тег `v2.4`, `latest`, 6 ассетов;
публикация по слову владельца «Да, публикуй»; release-body-guard зелёный) —
https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.4. Все пять эпиков `plans/78` закрыты
одним днём тремя чатами (PY · SC · FP · TD · RL); суд версии — VERIFIED WITH CAVEATS, 26 строк
**0 REFUTED** (`reports/KAIF_AUDIT/2026-08-28_rl_court_registry.md`); judge витрины VERIFIED —
шесть заявлений нот переисполнены командами. Имя — решение №82; логотип команды принят
владельцем («да, шикарно», `bugs/107` DONE). Скоуп 2.5 — `ideas/27`, НЕ открыт. Ход сессий —
летопись.

## Сессия 46 закрыта — пострелизная раскладка поля 2.4: скоуп 2.5 полон, эпики FT/MD/US/CI (2026-08-28, 15:1x → 17:0x +03:00)

Первая пострелизная сессия 2.4 — забор полевых отчётов и формирование скоупа 2.5 (перенесено из
STATUS дословно, стрижка бонсая):

1. ✅ **Полевые отчёты 2.4 ЗАБРАНЫ и разложены** (2026-08-28 15:20 +03:00, по слову владельца):
   четыре отчёта (NDim + KAGO × обновление + `/team-deployment`) скопированы в
   `reports/KAIF_UPDATES/`, пять тикетов `bugs/KAIF/` их проектов прочитаны; синтез —
   `researches/23`; из сигналов сформированы **три эпика скоупа 2.5 в `ideas/27`**: FT «Команда
   в поле» (adopt-путь, исключение менеджера, архетип hardware-lab, замки́ в стоп-ритуале) ·
   MD «Доставка сигналов машинерией» (`kaif-core report` — классификатор бьёт по предмету,
   воспроизведено на двух версиях) · US «Симметрии обновления» (quote-skip предикат,
   marker-aware i18n merge, преемник deprecation). Обе судьи поля — VERIFIED WITH CAVEATS;
   позитив: репетиция-предикт 3×, EOL-фикс подтверждён, борд-контракт 6/6.
2. ✅ **Третья порция забрана** (2026-08-28 16:29 +03:00, пинг владельца): отчёт Prompt Modding
   1.6→2.4 (issue #28 → `reports/KAIF_UPDATES/`, легаси-прыжок, macOS arm64, судья VERIFIED
   WITH CAVEATS) + тикет issue #27; эпик US в `ideas/27` расширен двумя HIGH — недетерминизм
   вердикта translated-wholesale (416 английских строк в ru-гайде) и якорные блоки без парных
   маркеров (класс двух шкур с KAGO R2); синтез дополнен (`researches/23` §2а/§5).
3. ✅ **Обещанный тикет пришёл — issue #29** (создан через минуту после проверки issues; найден
   повторной проверкой по слову «проверь все issues» — урок EXP-0100): `/team-deployment` не
   поставляет CI — оформлен **эпиком CI «Команда поставляется с CI»** (`ideas/27` п. 11:
   `team-ci.yml` в операции 3, дешёвые гейты, красный CI блокирует merge, деградация на
   не-GitHub). Полнота проверена: #24-коммент = известный +1 (эпик MD); KUMM #17 обработан
   циклом 2.3 (`plans/77`).

Коммиты сессии: `a614b63` (build 399, раскладка NDim+KAGO) · `7db868c` (400, ожидание третьего
отчёта) · `3a6c4c4` (401, Prompt Modding) · `9f9ec30` (402, эпик CI — скоуп полон). Слово
владельца о GitHub Workflows дословно — в `ideas/27` п. 11 и `researches/23` §5.

## Сессия 45 закрыта — чат 3 версии 2.4: РЕЛИЗ v2.4 Teamed Up KAIF ОПУБЛИКОВАН (2026-08-28, 11:1x → 13:58 +03:00)

Чат 3 закрыл эпик RL и выпустил версию:

- **Имя версии — решение №82** (11:19): точечный вопрос по эстафете с двумя кандидатами из
  документов → слово владельца: **KAIF 2.4 — Teamed Up KAIF**. Зафиксировано в журнале решений,
  STATUS, `bugs/107`, шапке `plans/78`.
- **Витрина текстом** (коммит `ee43461`, build 390): `reports/RELEASE_NOTES_2.4.md` (6 пунктов
  дельты по форме нот 2.3) + SEO-пропитка README по `researches/22` §4 обеих половин (первый
  экран «агентный харнесс (обвязка)» · §1.1 мост «Agent = Model + Harness» · §3.1 слои ·
  строка v2.4 Таблицы 5); ноты 2.4 добавлены в зоны `private-names-guard`.
- **Логотип — перерисован и ПРИНЯТ** («да, шикарно», 13:06; `bugs/107` → DONE, коммиты
  `16e1eb7`/`27b9cbf`): арт v2 владельца (ультрамикс 3762², по его слову) побайтно нетронут,
  подпись — механикой `build-logo-title.mjs` (Franklin Gothic Medium + медь + фаска). Три показа:
  «добавь отступов как в старом» → замеры плиты 2.3 → «посмотри, сколько воздуха было!» →
  сравнительный лист трёх панелей → выбор «Вариант B» → три поправки («текст выше» · «чуть
  меньше» · «медальон больше») → ручки вкуса TUNE_* 0.85/0.85/0.55. По дороге пойман и
  застережён дефект «медь → серебро» (PNG-писатель ImageMagick схлопывает чёрную плиту в
  grayscale; лечение PNG24, ось «медь, не серебро» в `--check`) — **EXP-0099**. Шесть осей
  `--check` доказаны красным на браке v1.
- **Цитата рамки README** — слова владельца о 2.4 («KAIF стал командным…»), оригинал дословно в
  `plans/78` (коммит `25e3904`), причёсанная по его указанию — на витрине обеих половин.
- **Релиз по `/release` полным ритуалом** (коммиты `ebee810`/`b93baf6`, builds 395–396): реестр
  пар целиком · полный контур в браузере **162/0** · TEMPLATE_NOTES 2.4 (5 пунктов + замороженное
  имя) · бамп с зеркалами (kaif.json, KAIF_FRAMEWORK) · сборка 59/167/749 + полигон 14/14 ·
  judge витрины **VERIFIED** (шесть заявлений нот переисполнены командами) · подтверждение
  владельца «Да, публикуй» → тег `v2.4`, **GitHub Release с 6 ассетами поставки**, `latest`
  подтверждён пробой, `release-body-guard` зелёный, страница открыта глазами.
  https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.4
- **Полевые прогоны 2.4 ЗАПУЩЕНЫ владельцем** (13:5x, «запустил»): по промптам этой сессии —
  NDim (обновление до 2.4 + актуализация живой команды по `/team-deployment`) и KAGO (обновление
  + развёртывание команды с нуля), оба с заказом двух отчётов и сигналами в `bugs/KAIF/`.
  Отчёты — входы скоупа 2.5; чтение — следующий чат.

## Сессия 44 закрыта — чат 2 версии 2.4: эпик TD одним чатом; RL: суд пройден, три входа владельца, логотип забракован (2026-08-28, 09:01 → 11:1x +03:00)

Чат 2 закрыл версия-образующий эпик и суд последнего:

- **TD «Team Deployment» ✅ 09:29 — ОДНИМ чатом при бюджете 3–4:** нулёвка — разведдок
  `researches/21` (дистилляция ЖИВОЙ команды NDim: манифест 297 строк, доска, инструменты;
  Phase 0 своей поставки — references/ навыков едут бандлом автоматически; Парето-разрез 11 фаз
  справочника) + операционный план `plans/81`; **37-й навык `team-deployment`** (payload EN +
  обвязка RU) + три шаблона в `references/` (конституция — 9 секций-инвариантов живого манифеста,
  статус-доска с контрактом инструмента, библиотека 5 ролей + 2 архетипов); зеркала (Таблица 3
  обеих половин, счётчики 37/59/167/749, ru-алиасы, пины, note 2.4). Полигон ЧЕСТНО покраснел
  шестью сводами на «two-headed document» (два H1 в библиотеке) — починено, 14/14; судья
  VERIFIED. Коммит `3d1faef` (build 381). Попутно EXP-0097 (Edit-инструмент нормализует NBSP —
  правка таких строк только скриптом-файлом).
- **RL: суд одним кругом ✅ 10:28 — VERIFIED WITH CAVEATS, 0 REFUTED** (реестр
  `reports/KAIF_AUDIT/2026-08-28_rl_court_registry.md`): БЕЗ workflows (слово владельца) —
  4 судьи-субагента с чистыми контекстами (B: PY+SC · C: FP · D: TD · E: сквозной+охоты) →
  26 строк = 18 CONFIRMED · 8 WEAKENED; 19 сырых находок → панель скептиков 2/2 пропустила 3,
  починены и пересужены тем же часом: S2 — сфабрикованная метка «оплачено полем» на
  гипотетическом примере справочника (лучшая находка: судья D исчерпывающим поиском по NDim
  доказал, что инцидента не было); S3 — `ideas/27` не пополнен отложенным разрезом TD при
  лгущем указателе; S4 — README обещал офлайн-ядру «то же развёртывание» при объявленном
  SUBSET. Каверзы S1/H10 — строки `ideas/27` п. 7 (2.5). Коммит `9f60060` (build 382).
- **Три входа владельца по ходу** (все дословно в `plans/78` § «Входы RL»): SEO/семантическое
  ядро — «kaif это harness … ии харнесс, обвязка, упряжь» → разведдок `researches/22` (термин,
  история Хашимото/LangChain 02.2026, узус RU, план пропитки §4; реализация — перед `/release`);
  именование мест команды `проект-team-роль` → инвариант в шаблонах TD обоих слоёв; фикс
  интерактивного контура → I35 переписан (нейродвижок Silero + ОДИН голос владельца —
  обязательный шаг развёртывания, стоковый — только объявляющий себя фолбэк с долгом; полевой
  закон «неверным голосом ты меня позвал» — дословно), P3 открыт обязательством «варианты —
  ВСЕГДА РАДИОКНОПКИ». Коммиты `721ef2d`/`5c242b5` (builds 383–384).
- **Логотип релиза — сделан и ЗАБРАКОВАН владельцем:** новый инструмент `build-team-logo.mjs`
  (подпись словными полосами из caption оригинала; srgba-грабля `-alpha off`; `--check`
  RMSE 0.0014; PNG 4000² + webp 2000² 739 КБ; коммит `86402b4`, build 385) — вердикт владельца:
  «Шрифт не тот, что был в 2.3, название версии не то, что я заказывал. …пока не лезь, просто
  запиши баг» → `bugs/107` (критерий приёмки: стиль 2.3 + точное имя от владельца + его слово
  после показа) + **EXP-0098** («по образцу инструмента X» в брендовом заказе = И СТИЛЬ X; текст
  релизной надписи — заказ владельца, не производная имени файла). Builds 387–388.
- Закрытие — `/end-chat-soft`: свип реестра пар целиком зелёный (counters 50 · lang-packs ·
  questions 0 · private-names · showcase · контур 42/0 · шапки 0 · experience 0 · слепок ==
  ядро `5265de5` · маркеры молитв · зеркала версии · fable-пара побайтно).

## Сессия 43 закрыта — 2.4 ОТКРЫТА владельцем; чат 1: планирование + эпики PY, SC, FP (2026-08-28, 00:35 → 01:48 +03:00)

Версия **KAIF 2.4 — Team KAIF** открыта словом владельца («делаем версию 2.4… Приступаем!»,
00:35 +03:00); рамка: ≤10 чатов · 20% усилий → 80% ценности · MAX без ultracode/workflows.
Чат 1 закрыл планирование и три эпика из пяти:

- **Планирование:** мета-план `plans/78` (порядок PY → SC → FP → TD → RL); Парето-разрез — всё
  тяжёлое унаследованное ушло в `ideas/27` (скоуп 2.5); запрос владельца в `ideas/26` причёсан
  (оригинал дословно — коммит `4c25877`); полевые отчёты 2.3 (NDim, KAGO) втянуты в
  `reports/KAIF_UPDATES/`.
- **PY «Молитвы» ✅ 01:10:** символ веры (по NDim) + молитва 16 принципов (по KAGO) — в ядре
  поставки (`framework/AGENT_GUIDE.md`, EN, `<AUTHOR>`) и обвязке (RU); произнесение — `/resume`
  обоих слоёв + блок в AGENT_GUIDE; тексты приняты владельцем («теперь суппер, снимай пометки
  AI, берём») после его поправки «молитва — аксиома, без обоснований» (урок EXP-0095); пара
  слоёв — строка реестра пар, проба доказана красным.
- **SC «Мягкие церемонии» ✅ 01:29** (`plans/79`): `/end-chat` раздвоен на `/end-chat-force`
  (срочно: эстафета + коммит + строка долга церемоний) и `/end-chat-soft` (заявка заранее →
  доделать до среза → полные церемонии); старый навык снесён, бандл 2.4 несёт deprecation +
  template-note + policy-note; семантика «названное время = СТАРТ мягкого закрытия» — в
  AGENT_GUIDE обоих слоёв + четыре цикла; замороженные пакеты №56 сверяются с локом, живые — с
  ростером (counters-guard научен); ростер 36 навыков / 58 embedded / 163 блока / 712 модулей.
- **FP «Поле 2.3» ✅ 01:48** (`plans/80`): 4 фикса по отчётам — `writeMatchingEol` сохраняет
  CRLF-конвенцию переписываемых файлов (тикет KAGO 06) · merge-строка называет оставленные модули
  сигнатурами · маркер `KAIF-VERSION-OK` глушит stale-claims навсегда (NDim R2) · шаг 4
  `/report-bug` переписан вокруг стоячей авторизации №15, классификатор-выживаемо (NDim R1);
  каждый фикс доказан красным на мутации до-фиксового ядра; черновики ответов #23/#24 — в
  `plans/80`, отправка после релиза (№80). Уроки: EXP-0095 (молитва-аксиома), EXP-0096
  (heredoc-эскейпы этого харнеса).

Ход чата вёлся при живом владельце; его входы записаны дословно в `plans/78` (дашборд статусов —
обязательная часть TD; «фича не такая страшная, как сам план-документ»). Закрытие — первым живым
прогоном свежего `/end-chat-soft` (заявка «после эпика закрывай» пришла заранее, работа доделана
до среза).

### Перенесено из STATUS дословно (стрижка бонсая, закрытое 2.3)

**Витрина ДОГНАЛА выпущенную версию 2026-08-22 20:2x +03:00** (сессия 42, коммит `7124748`):
релиз 2.3 вышел с логотипом «KAIF 2.2 — Yolden KAIF» и README целиком на 2.2 (бейдж, строка
версии, alt, таблица истории без строки v2.3). Собран `assets/KAIF_2.3_GitHub_LOGO.png/.webp`
(2900×2300, уроборос не тронут — 0 различий попиксельно) инструментом
`node tools/build-logo-title.mjs`; README и ноты обновлены, ноты получили логотип в обе половины;
тело релиза на GitHub перепубликовано, `release-body-guard` зелёный, страницы прочитаны глазами.
Вердикт владельца по картинке: «да, супер красиво». Цитата под логотипом оставлена его словами
про 2.2 — по смыслу она совпадает с именем 2.3 («the eye stands for KAIF being under the
observation of the projects that use it»); новая реплика — по его желанию.

⚠️ **ИМЯ `Subjected KAIF` НЕ означает «подсудная»** (решение №81, слово владельца 2026-08-22):
версия была В ЦЕНТРЕ ВНИМАНИЯ и выросла на отзывах полевых агентов. Прошлая сессия вывела смысл
из названия догадкой и построила на ней всю рамку витрины; исправлено. Урок — EXP-0094: имя
владельца — данное, его СМЫСЛ — пробел, и пробел закрывается словом владельца, а не этимологией.

**Мера 2.3 — БЕЗ числа-мишени** (решение №75, слово владельца: «Энтропия и хаос должны
уменьшаться, а не увеличиваться»): замер лесов до/после печатается, вектор — вниз, каждый страж
доказан красным, охват = заявленному.

## Сессия 42 закрыта — витрина догнала выпущенную 2.3: логотип, README, ноты; смысл имени исправлен владельцем (2026-08-22, 19:31 → 20:2x +03:00)

Исполнитель — Claude Opus 5 (1M context). Чат открылся `/resume`; работу задал владелец: «прошлый
агент странно выпустил версию 2.3 в GitHub, не убрав текст-подпись с новым логотипом глазом»,
затем — «корректно обносить релиз ноустсы и витрину README под выпущенную версию 2.3». Коммит
`7124748` (build 372).

**Что было не так.** Релиз 2.3 вышел 2026-08-21 с логотипом, на котором выжжено «KAIF 2.2 —
Yolden KAIF», а витрина README целиком осталась на 2.2: бейдж версии, строка версии, alt-текст,
логотип, и в таблице истории версий не было строки v2.3. В нотах 2.3 логотипа не было вовсе,
хотя в нотах 2.2 он открывал обе половины. Корневая причина класса названа: перерисовать подпись
было НЕЧЕМ — инструмента не существовало, поэтому картинку никто и не тронул.

**Логотип.** `assets/KAIF_2.3_GitHub_LOGO.png/.webp` (2900×2300, как у 2.2) собран новым
инструментом `tools/build-logo-title.mjs`: старое лого переиспользовано, область уробороса не
тронута (попиксельная сверка 2900×1960 — 0 различий), перенабрана только полоса подписи. Стиль
снят ПРОБАМИ, а не угадан: гарнитура опознана сверкой масок слова «KAIF» из девяти кандидатов
(Franklin Gothic Medium — ширина +0,2 % при равной высоте, RMSE наименьшая), геометрия — cap
149 px, базовая линия y=2170, центр x=1450; медь — по замеренному профилю стойки, фаска — свет
сверху по кромке каждой буквы. Попутно вскрыто расхождение двух ассетов 2.2: в PSD владельца
(`KAIF_2.2_GH_RELEASE_PAGE_LOGO.jpg`) подпись набрана Arial Bold — владелец помнил верно, но
целевой назвал ту картинку, что висит на GitHub («целимся на тот, что висит в GITHUB»). Раскладку
выбрал владелец (вариант A: кегль как у 2.2, строка длиннее). Вердикт владельца по вкусу:
«да, супер красиво, спасибо большое».

**Смысл имени — поправка владельца (решение №81).** Прошлая сессия вывела дух версии ИЗ НАЗВАНИЯ
и построила на догадке всю рамку витрины («The version that was put on trial», «фреймворк теперь
подсуден собственному суду», заголовок «Суд стал частью релиза»). Слово владельца дословно: «Её
не судили, а на версии 2.2 работали ИИ агенты и давали отзывы… Она не судимая»; «она наоборот,
прекрасная и взрощенная на обратной связи потреителей и пользователей»; «название её вышло из
того, что она была в центре внимания и получала отзывы и обратную связь, чтобы стать лучше».
Открывающие абзацы обеих половин нот и строки v2.3 обеих таблиц истории README переписаны на этот
смысл; факт переисполнения 47 заявлений сохранён как факт QA, но перестал называть суд сутью
версии. Урок — EXP-0094.

**Инструмент со своей проверкой.** У `build-logo-title.mjs` есть контрольный прогон `--check`:
перенабирает подпись 2.2 с нуля и сверяет с оригиналом по габариту и тону. Допуск ширины 3 %
назван по ИЗМЕРЕННОЙ причине (+2,2 % от естественного трекинга: принудительное сжатие съедает
пробелы вокруг длинного тире), а не подогнан под прогон; различающая сила доказана мутацией на
КОПИИ — подмена гарнитуры на Arial Bold даёт +9,4 % и красный с кодом возврата 1. Из PNG убрана
метка времени `png:tIME`: два прогона теперь дают идентичные байты.

**Побочно закрыта дыра:** в зонах `private-names-guard` стоял путь с номером версии
(`RELEASE_NOTES_2.2.md`), поэтому ноты 2.3 публиковались вообще без этой проверки — путь 2.3
добавлен (файл в ignore), прогон зелёный.

**Публикация и приёмка глазами:** тело релиза v2.3 обновлено из файла, `release-body-guard`
построчно зелёный (NBSP 2/2); картинка отдаётся с raw.githubusercontent байт в байт (sha совпал);
страница релиза и README открыты и прочитаны — логотип наверху, бейдж 2.3, строка версии и строка
v2.3 в таблице на месте.

## Сессия 41 закрыта — W5 исполнена, фаза W и ВЕРСИЯ 2.3 ЗАКРЫТЫ словом владельца (2026-08-21, 19:1x → 19:4x +03:00)

Продолжение записи ниже (та же сессия). После отправки 13 ответов сессия взяла W5 по слову
владельца («Да, давай это всё сделаем, это важно»), исполнила её целиком и закрыла фазу W его
словом «закрываем» (19:37 +03:00); тем же словом чат закрыт («и чат тоже закрываем» — /end-chat).
Коммиты: `4de2a91` (W5) + закрывающие этой записи. Исполнитель — Claude Fable 5.

**Дословный перенос из STATUS (ход W5, каким он был к закрытию):**

**Фаза W закрыта 2026-08-21 19:37 +03:00 словом владельца «закрываем»** (гейт W5, `plans/75`);
тем же словом DONE — `ideas/24` (скоуп 2.3). Ход W5 (сессия 41): 13 ответов в issues отправлены
и тикеты закрыты (19:10; семь полевых отчётов #5 #7 #9 #11 #12 #13 #17 открыты — не дефекты) ·
`/check-backlog` — DONE: `bugs/92`/`98`/`99`, `ideas/08`/`19`/`22` · задел 2.4 — `ideas/26` §7
(каверза ADDR-охвата: 4 носителя + остаток 98.3) · вехи 2.2/2.3 в MASTER_PLAN §5, §8 освежён ·
урок EXP-0093 · замер лесов «после-после»: 31 файл / 15 164 строки (`plans/75` W5.4).

## Сессия 41 — 13 ответов в полевые issues отправлены, тикеты закрыты (2026-08-21, 19:06 → 19:10 +03:00)

Исполнитель — Claude Fable 5. Первое дело нового чата по эстафете сессии 40: 13 ответов
отправлены в issues origin и тикеты закрыты 2026-08-21 19:10 +03:00 — первым #22 с проверкой
рендера глазами (API-HTML носителя: один абзац, линейка, курсивная подпись, автолинки #14/#15
целы), затем #21 · #18 · #14 и девятка #19 · #15 · #16 · #8 · #10 · #3 · #4 · #6 · #20. Тела —
дословно черновики `plans/76`/`plans/77` (переносы строк развёрнуты под рендер чужого носителя —
урок EXP-0071; «for the upcoming 2.3» заменено фактом вышедшего релиза v2.3 — сам смысл
решения №80 «ответы ссылаются на вышедшую версию»), каждый через `gh issue comment --body-file`,
подпись агента-автора по канону issue #15. Семь полевых отчётов (#5 #7 #9 #11 #12 #13 #17)
оставлены открытыми — не дефекты. Слово владельца: №80 + чат 2026-08-21 («а я думал issues
закроются по релизу, мы с тобой так договаривались» · «можем это сделать в новом чате»).

**Дословный перенос из STATUS (п. 1 «Где продолжить», каким он был к исполнению):**

Релиз 2.3 опубликован (W4 закрыт; ход сессии 40 — летопись). Владелец ЯВНО подтвердил отправку
и закрытие тикетов по релизу (решение №80 + слово в чате 2026-08-21: «а я думал issues закроются
по релизу, мы с тобой так договаривались»; перенос в новый чат — его же слово «можем это сделать
в новом чате»). Инструкция незнакомцу:

1. Черновики ответов — ДОСЛОВНО из планов: `plans/76` § «Черновики ответов в issues» (4 шт.:
   **#22 · #21 · #18 · #14**) и `plans/77` § черновиков (9 шт.: **#19 · #15 · #16 · #8 · #10 ·
   #3 · #4 · #6 · #20**).
2. Авторство АГЕНТСКОЕ с подписью агента-автора (канон issue #15: транспорт ≠ авторство) —
   страница одобрения владельцу НЕ нужна; тело КАЖДОГО ответа — через ФАЙЛ, не argv:
   `gh issue comment <N> --body-file <файл>` → затем `gh issue close <N>`.
3. Прежде одного массового прогона — отправь ОДИН (#22), открой страницу issue глазами
   (правило чужого носителя), затем остальные двенадцать.
4. После отправки: галки и даты в `plans/76`/`plans/77` (вехи в Статус:), строка здесь → в
   летопись; семь полевых ОТЧЁТОВ (#5 #7 #9 #11 #12 #13 #17) — НЕ дефекты, не закрывать.
5. Затем W5: `/check-backlog` · кандидаты 2.4 из каверз ADDR-охвата — в `ideas/26` · закрытие
   фазы W в `plans/75`.

## Сессия 40 — РЕЛИЗ KAIF 2.3 — Subjected KAIF: пул, волны вычитки, три бага контура, публикация (2026-08-21, ≈10:10 → 19:0x +03:00)

Коммиты: `7564e4a` → `aa5ce52` + закрывающий этой записи (builds 353–367+). Исполнитель —
Claude Fable 5. Слова владельца в сессии: «ты на max усилиях сейчас, я выключил воркфлоус» ·
«подними ДЗ интерактивным контуром. Интересные факты по 2.3 писать не будем, этта версия
короткая» · «контур твой сломан» / «я просто ок нажал, а требует необязательный комментарий»
(трижды) · ответы Q1/Q2 страницей (18:31) · «делай релиз, моё ДА» · «а я думал issues закроются
по релизу, мы с тобой так договаривались» · «можем это сделать в новом чате, а то этот распух».

**Автономная половина (до слова владельца):** `bugs/104` закрыт (пометка ВНУТРИ жирного — все
три счётчика одной грамматики; 151/0 браузером) · машинная половина ставок витрины
(`SHOWCASE_RATES` в `estimates-guard`, bugs/73 «в») · постоянное покрытие лоадера и twin-run в
s01 (долг эпика L) · сюита называет упавший свод поимённо (рецидив-кандидат bugs/61) · три
адреса карты O5 пересняты живой пробой — у Muse Code родилась вендорская дока · EXP-0090.

**Дословный перенос из STATUS (блок «СЕЙЧАС», каким он был к закрытию):**

Сессия 39 (2026-08-21, летопись) прошла W0 → W1 → W2 → W3: суд оркестрацией дал **VERIFIED
WITH CAVEATS** (58 строк = 49/9/0; реестр и улики —
`reports/KAIF_AUDIT/2026-08-21_w1_court_registry.md` + `…_result.json`), все 5 находок
починены и пересужены точечно (гейт W2 ✅ 09:51), витрина 2.3 написана и машинно чиста
(`reports/RELEASE_NOTES_2.3.md` + Таблица 6 обеих половин README + PDF; showcase/voice/
estimates/private-names зелёные). **Волна вычитки ДЗ-06 отработана** (три слова владельца записаны в ДЗ дословно; контур по ходу
дал два дефекта — `bugs/105` «нельзя нажать просто ок» и `bugs/106` «протухший notice-класс
выбросил ответы», оба закрыты той же сессией): **Q1 ИСПОЛНЕН 2026-08-21 18:32 +03:00** — «по
Ёлден каиф оставлям, по 2.3 не пишем»: блок «Интересные факты» снят из витрины 2.3 ЦЕЛИКОМ
(раздел 8.5 README с Таблицей 6, обе половины + факты нот; страница 2.2 не тронута), story-card
2.3 НЕ собирается, вопрос «заплачено» закрыт, реестр ставок `estimates-guard` пуст, ось
«ноты↔README» молчит по мёртвой паре (воскреснет с возвратом блока).

**W4 ЗАКРЫТ релизом 2026-08-21 18:50 +03:00** (`/release` пройден целиком: волны вычитки
исполнены · bugs/104–106 закрыты по ходу · контрольный блок из пяти команд · judge VERIFIED ·
бамп тройкой · публикация · body-guard ✅ · `latest` = v2.3 · story-card НЕ делается по слову
Q1).

**Пул, закрытый сессией (дословно из STATUS):**

- [x] ✅ Постоянное покрытие ОТГРУЖЕНО 2026-08-21 10:48 +03:00 (долг закрытия эпика L, линза 3
      №3): s01 несёт S7 — sha256-гейт лоадера герметично (--source локальный; контраст + tamper
      ядра и бандла, 7 ассертов; красный доказан мутацией копии лоадера с выключенным сравнением)
      и S8 — twin-run в ТОТ ЖЕ путь, побайтная идентичность всех файлов без исключений (число
      печатает прогон; twin в РАЗНЫХ каталогах краснел бы на честной параметризации именем/путём
      проекта — проверено пробой; красный компаратора доказан порчей файла между слепками).
- [x] ✅ Три адреса источников карты O5 ПЕРЕСНЯТЫ живой пробой 2026-08-21 (`researches/19` §1.2):
      Antigravity (antigravity.google/docs/hooks — injectSteps жив) · Grok Build (docs.x.ai/build/
      features/hooks — паритет `.claude/settings.json` подтверждён вендором) · **Muse Code —
      СОСТОЯНИЕ ИЗМЕНИЛОСЬ: вендорская дока ПОЯВИЛАСЬ** (dev.meta.ai/docs/muse-code/extending.md,
      12 событий вкл. PreCompact/PostCompact; впрыска контекста в контракте нет → образец
      по-прежнему не пишется, причина обновлена в README модуля hooks).
- [x] ✅ Машинная половина ставки витрины ОТГРУЖЕНА 2026-08-21 10:40 +03:00 (`bugs/73` пункт «в»):
      живой реестр рукописных ставок — `SHOWCASE_RATES` в самом `estimates-guard` (страж в своём
      `WATCHED` — источник у ставки требует та же механика; ось «реестр ↔ витрина» краснеет на
      протухании; селфтест — оба ответа обеих осей). «Какое из чисел прозы — ставка» остаётся
      глазам владельца (вычитка), это сказано в шапке стража.
- ✅ ДЗ-06 ЗАКРЫТА словом «делай релиз, моё ДА» (2026-08-21, чат) — релиз 2.3 опубликован.

Уроки сессии: EXP-0090 (живой реестр живёт в инструменте-стороже) · EXP-0091 (на развилке
«состояние против работы человека» побеждает работа) · EXP-0092 (правки строк с невидимыми
символами — патч-скриптом по якорям).

## Сессия 39 — фаза W до границы слова владельца: суд W1, починки W2, витрина W3 (2026-08-21, ≈08:50 → 10:1x +03:00)

Коммиты: `4a3dfe7` → `6bee54b` + закрывающий этой записи (builds 337–352+). Исполнитель —
Claude Fable 5. Слово владельца в сессии: workflows на суд выданы (№79, интервью №019 Q1 = A,
раунд 2), «будь экономным»; ответы в 13 issues — после релиза (№80, Q2 = C); «когда доделаешь
фазу — закрытие чата по канону с передачей эстафеты».

**W0:** гейт входа X ✅ + U′ ✅ подтверждён судьями эпиков; план `plans/75` сверен с HEAD;
критерии релиза 2.3 пересобраны под состав №77 секцией W0 (минус P/V — 2.4; минус вектор-вниз
лесов — мандат T в 2.4, в 2.3 честная печать; плюс канон командами X-1…X-5; полевые сигналы —
13 issues фактом).

**W1:** реестр суда — 47 заявлений в 8 кластерах + 4 охоты
(`reports/KAIF_AUDIT/2026-08-21_w1_court_registry.md`). Суд оркестрацией по №79: 50 агентов —
6 ревизоров по кластерам (мутации адресности в изолированных worktree: сплайс → 5 ❌ ровно на
классе, переход anonymous→origin → 2+2 ❌ полумутациями, счётчик диска → 1 ❌ именно S16,
crash-журнал → 10 ❌ все в S9-crash, живое EN-описание → страж назвал навык поимённо) + 4
охоты + панель скептиков (экономный профиль: 2 линзы, выживание 2/2). Вердикт **VERIFIED WITH
CAVEATS**: 58 строк = 49 CONFIRMED · 9 WEAKENED · 0 REFUTED; из 20 сырых находок выжило 7 → 5
после дедупа; 4 каверзы поимённо (G1-команда замера — закрыта дословной фиксацией в реестре;
FC-1 browser-прогон — на W4; ADDR-охваты — кандидаты 2.4; refresh-witness — недоказуем задним
числом). Сырой результат — `…_w1_court_result.json`, приватные имена заменены алиасами.

**W2 (все 5 починены + пересуд):** W2-1 blocker — приватное имя в комментариях crash-журнала
CORE (утечка U′2 пережила закрытие эпика: судья U′ private-names-guard не гонял) → алиас +
**гейт 1c в `commit.mjs`** (коммит с поверхностью поставки гонит private-names-guard; красный
доказан остановкой реального коммита с непересобранным dist) · W2-2 substantial — fail-open
слепка: без `.kaif/private-names.json` пересборка молча писала реальные имена с exit 0 → fail-
closed exit 2 + канарейка K14 · W2-4 — канарейка K15 (расширение белого списка #20 доказывает
себя мутацией) · W2-3 — шаг 4 `/release` без зашитого счётчика команд · W2-5 — рецепт C14
якорными грепами. Точечный пересуд (workflow, 3 верификатора): W2-1…4 подтверждены с мутациями
в worktree; W2-5 ОПРОВЕРГНУТ дословным исполнением (экранированный пайп клетки — литерал для
grep) и починен код-блоком; дословный прогон 9 и 4. Полигон после фиксов — all 14 suites green.

**W3 (агентская половина):** `reports/RELEASE_NOTES_2.3.md` — шесть пунктов дельты + факты окна
из `kaif-stats` (68 коммитов · 601 млн токенов · ≈$948 по API-прайсу) · README Таблица 6 обеих
половин заменена метриками 2.3 (замер лесов +742/+2 — строкой витрины, критерий №75) · PDF
перегенерирован · showcase-lint: ось NOTES-VS-README скоуплена на СТАРШИЕ ноты (историческая
2.2 осиротела бы против нового README; красный оси доказан мутантной копией 81,999) ·
`build-story-card` получил версию из `version.json` + страж разъезда (красный живьём: «Таблица
6 несёт 2.3, version.json — 2.2»; сгенерированная до бампа карточка перезаписала карточку 2.2 —
восстановлена из git, порядок «бамп → карточка» — шагом W4) · ДЗ-06 вычитки + вопросы А
(строка «Фактически заплачено») и Б (слово на релиз) — очередью контура.

**Попутно поймано судом над самим собой:** `bugs/104` — парсер контура молча выбросил варианты
формы `**A (пометка)**` из ОБЕИХ развилок №019 (третий повтор класса bugs/51; ось G11 —
односторонний инвариант), раунд 1 владелец отвечал по неполному набору, переспрос с полной
развилки СМЕНИЛ ответ Q1 B→A — потерянный вариант оказался выбором владельца; оба раунда
дословно в истории git. Минимум фикса — на W4 вместе с обязательным browser-прогоном. Уроки:
EXP-0087 (страж полноты не делит регэксп с парсером) · EXP-0088 (комментарий владельца —
условие запуска, оплачено остановленным workflow) · EXP-0089 (генерат с версией в идентичности —
только после бампа; механизировано стражем).

---

## Сессия 38 — эпик U′ закрыт целиком: U′2–U′5 (2026-08-21, 02:10 → 03:33 +03:00)

Перенесено из STATUS дословно (абзац «ЭПИК U′ ЗАКРЫТ ЦЕЛИКОМ»): U′ закрыт 2026-08-21 03:29
+03:00 (`plans/77`: шесть фаз, лёгкий судья по критериям 1–6 зелёный, таблица исходов — 9/9
issues + все взятые R2-вхождения, черновики ответов во все девять issues). Сессия 38 закрыла
кодом U′2–U′5: семь фиксов машинерии (#16 сплайс `package.json` · #8 переход anonymous→origin ·
#10 URL/формы/libuv-`dieSoft` EXP-0085 · 92.1 `--lang` · 99.1–99.3 · crash-журнал+`resume`
§12.4) · канон поставки (#4 owner-voice · #6 язык по аудитории · #3 сфера в списке) · исток
(#20 «одно пространство суждения» EXP-0086 — раздел 0 с шестью запретами доезжает до слепка;
98.1 реальные команды шага 4; 98.2 страж `release-body-guard`) · заморозка №56 (README обе
половины + Reference §7.4 + строка установки; страж `lang-packs-guard` — 8 пакетов побайтно,
живой ru, 35 пинов EN-описаний; 92.2–92.5 закрыты). Коммиты `7bb1753` → `bd3af3e`, build 336.

**Эпик X «Канон командами» (закрыт сессией 37, перенос из STATUS дословно):** ЗАКРЫТ 2026-08-21
00:48 +03:00 (`plans/76`; судья VERIFIED, полигон 14/14): правило формы обязательства в каноне
обоих слоёв · TESTING_FRAMEWORK перестроен (активности, «фича/кейс», гейты продакшена 6–7,
шаблон `.kaif/_testcases-template.md` в поставке) · чек-лист письма в REQUIREMENTS ·
механизация уроков + страж `experience-lint`. Черновики ответов в issues #22/#21/#18/#14 —
секцией `plans/76`.

---

## Сессия 37 — Subjected KAIF: сокращение скоупа 2.3, эпик X целиком, U′0–U′1 (2026-08-21, ≈00:00 → 01:4x +03:00)

Коммиты: `052eb04`, `9ebe559`, `f5660d4`, `a3679d9`, `e79a9e1`, `dc6990d`, `49c32c3`, `3fe1bf2`,
`6c79254`, `d4bbeaf` (+ закрывающий этой записи). Исполнитель — Claude Fable 5.

**Скоуп 2.3 сокращён владельцем (решения №77/№78).** Критерий его слова: «за минимальные усилия
(20%) даст 80% полезного улучшения KAIF… максимум до 3...5 эпиков… за не более чем 5...10 сессий»;
полный текст указаний дословно — `ideas/24` § «Сокращение скоупа». Действующий состав: **S(✅) →
эпик X «Канон командами» → эпик U′ «Полевые точечные фиксы» → фаза W**; эпики T/V/P и остаток U —
в новый реестр скоупа 2.4 (`ideas/26`), готовые планы наследуются с нулёвками. Кодовое имя
подтверждено: **KAIF 2.3 — Subjected KAIF** (№78; описка «2.2» в первой фразе разобрана точечным
вопросом). Тем же вечером — дополнение владельца к постановке X (дословно в `ideas/24`): модели
плохо понимают запреты и точные инструкции — строгость и точность переносятся в код и хуки,
творчество остаётся структурированной прозой пошаговыми планами. Главный новый вход, которого не
было при планировании 2026-08-14: семь полевых issues #14–#22, поданных агентами живых проектов.

**Эпик X «Канон командами» закрыт целиком за одну сессию** (`plans/76`; судья VERIFIED, полигон
14/14): **X1** — правило формы обязательства (команда · шаг с условием выхода · чекбокс; проза —
обоснование под носителем; запрет — только положительным руководством или стражем) в
PHILOSOPHY+AGENT_GUIDE обоих слоёв; **X2** — `TESTING_FRAMEWORK.md` перестроен: цепочка тестовых
активностей (#21), правило «`[TESTED]` на фиче — только с написанным набором кейсов», гейты
продакшена 6–7 (#18: войти как пользователь · одна идентичность сборки), конверсия формы (#22:
126→191 строка / 0→30 нумерованных шагов), шаблон `.kaif/_testcases-template.md` в поставке;
**X3** — чек-лист письма в `REQUIREMENTS_FRAMEWORK.md` (0→6 чекбоксов, команда линтера
носителем); **X4** — механизация уроков (#14): вопрос при ПЕРВОЙ записи, поле из трёх исходов,
`#action:`-ось recall, страж `tools/experience-lint.mjs` с baseline 83 (доказан красным обоими
ответами). Черновики ответов в issues #22/#21/#18/#14 — секцией `plans/76` (отправка — слово
владельца). Урок EXP-0084: класс EXP-0082 (`\b` слеп к кириллице) повторился в новом страже и был
пойман его же селфтестом до живого прогона.

**Эпик U′ начат** (`plans/77`): U′0-нулёвка дала твин-находку (артефактная ветка контура истока
`if(st)` теряла замечание без статуса — вторая половина класса issue #19); **U′1 закрыта** —
фикс + блок 8в в `verify-contour` (обе половины живым кликом; красный доказан мутацией: 147/2 →
149/0), P7 контракта `/owner-reviews` расширен ТРЕМЯ состояниями снимка («варианты отвергнуты,
вот направление» = СТОП, не согласие), #15 «транспорт ≠ авторство» канонизирован в `/report-bug`
(сигнал о дефекте KAIF — в origin автономно, с подписью агента-автора; send-гейт — для чужих
репозиториев).

**Перенесено из STATUS дословно (закрытое):**

> **Фаза S (триаж R2) закрыта 2026-08-14 07:18 +03:00:** 104/104 вердикта (ultracode, 40 агентов,
> 0 ошибок) — **CONFIRMED 76 (0 blocker) · REFUTED 17 · STALE 11; адресация: эпик T 59 · эпик U
> 17**; таблица — `reports/KAIF_AUDIT/2026-08-14_r2_triage_SUMMARY.md`, вердикты — секциями
> «Триаж 2.3» в семействах; `bugs/101`/`102` — DONE. Исток больше не врёт о своей версии (маркер и
> `KAIF_FRAMEWORK.md` → 2.2; пара в реестре, страж доказан красным).

## Сессия 36 — планирование 2.3 целиком + фаза S (2026-08-14, ≈00:50 → 07:2x +03:00)

Коммиты: `b1eda81`, `7aadd9b`, `db06268`, `13f2d07`, `fea9a7e`, `a3178b1`, `998e507`, `48a5f01`
(+ закрывающий этой записи). Исполнитель — Claude Fable 5. Полный конспект хода — `reports/28`.

**Синтез и метапланирование 2.3** (слово владельца: «провести синтез и эпическое
метапланирование… с назрезкой подробных дорожных карт и пошаговых детальных операционных
планов»). Забраны семь полевых отчётов эпохи 2.2 (NDIM, Unliminium, KLAS, QA_Engineer, KAGO ×3)
из репозиториев проектов и issues origin → `reports/KAIF_UPDATES/`. Наброс владельца о боли
обвязки — в `ideas/24` п. 6 дословно. Режим разведки выбран владельцем: «Max Fable +
дайджестеры» (ultracode отложен на исполнение). Написан метасинтез `researches/20`: 104
R2-вхождения по слоям — **81 % в обвязке** (леса ≈13,7 тыс. строк > payload ≈12 тыс.), паттерны
P1–P6, полевой синтез (21 пункт), индустриальная разведка «один движок + правила-данные».
Зонтичный мета-план `plans/70`: 8 критериев приёмки, нарезка **S → T → U → V → P → W**.
Интервью №018 отвечено страницей контура (Q1=A · Q2=A · Q3=A · Q4 — числовой цели не будет:
«Энтропия и хаос должны уменьшаться, а не увеличиваться») → решения №72–№75; judge артефактов
планирования — VERIFIED WITH CAVEATS.

**Фаза S исполнена целиком** (`plans/71` ЗАКРЫТ): ultracode-триаж 40 агентов / 0 ошибок —
**104/104 вердикта: CONFIRMED 76 (59 substantial · 17 hygiene · 0 blocker) · REFUTED 17 ·
STALE 11; адресация T 59 · U 17**; сводная таблица `reports/KAIF_AUDIT/2026-08-14_r2_triage_SUMMARY.md`,
секции «Триаж 2.3» во всех 20 семействах; `bugs/101` и `bugs/102` закрыты DONE. Гигиена истока:
маркер `.kaif/kaif.json` и `KAIF_FRAMEWORK.md` сведены с 2.0 на 2.2 Yolden KAIF, пара
«version.json ↔ носители версии истока» в реестре пар, страж доказан красным на живом дефекте до
починки. Урок `EXP-0083` (~27 % отсева неверифицированных находок). Досье окружения дополнено
читающей половиной правила кодировок (наступ: голый `Get-Content` = мнимый mojibake — замечание
владельца).

**Полные планы всех фаз по решению №76** (слово владельца: «запланировать полноценно все эпики.
И операционные планы по ним. Чтобы в следующий раз, через неделю, мы начали с того, что все
планы уже есть, и мы тупо по ним работаем»): эпик T `plans/72` · эпик U `plans/73` · эпик V
`plans/74` (поверх постановки `plans/64`) · эпик P `plans/32` актуализирован · фаза W
`plans/75`; у каждого — нулёвка «актуализация входов».

**Перенесено из STATUS дословно (закрытые блоки эпохи 2.2):**

> **Витрина принята владельцем 2026-08-09 вечером.** Он вычитал README и страницу релиза вживую и
> выдал две волны — 38 + 48 правок; все записаны ДОСЛОВНО в `plans/69`, все исполнены, тело релиза
> перезалито последним. Слово владельца на закрытии: «всё готово… Мы выпустили релиз».

> ⚠️ **РЕШЕНИЕ ВЛАДЕЛЬЦА №70: остаток находок R2 НЕ чинится в 2.2.** Дословно: «Ты чинишь блокеры.
> Все остально, что будет найдено, описываем баг документами и оставляем это на версию 2.3, которую
> будет чинить сильный Fable». Форма документа сужена им же: **предположительный баг и только он** —
> без причины, без плана починки, без способа проверки. *(Исполнено фазой S 2.3: триаж 104/104.)*

> **📦 Задел на 2.3 (решения владельца 2026-08-08, НЕ в 2.2)** — сводка из STATUS: заморозка
> восьми языковых пакетов (№56/№57) · форма 2.3 — обычный релиз со скоупом из идеи владельца,
> сокращение поверхности пунктом внутри (№58) · эпик P — кандидат (№35/№47) · первый пункт
> владельца: поручить Fable ревью `/code-revision`. *(Раскрыто в `ideas/24` и планах 2.3;
> из STATUS снято как исполненное планированием.)*

## Сессия 35 — витрина принята владельцем, карточка сторис (2026-08-09, 19:46 → 21:14 +03:00)

Коммиты: `a971bd3`, `1374a1b`, `54db1ba`, `be419b6`, `95d2a05`, `02130ba`, `85db78f`, `5315133`,
`3cf2e99`, `dd90342`, `5d0abc2`, `e6e2b72`, `18dfbd5`, `4054243`, `a096315`, `61ea7ac`, `80fb0d7`,
`7f0af86`.

**Вторая волна витрины — сорок восемь правок владельца (записи 39–86 в `plans/69`).** Владелец
вычитывал README и страницу релиза вживую; каждое слово записано в план ДОСЛОВНО до починки, каждая
правка уехала в три места сразу (обе половины README + релиз-ноты) с перезаливкой тела релиза.

**Сверка записи с первоисточником.** На вопрос владельца «а они не зафиксированы в документах???»
ответ снят пробой: транскрипт прошлой сессии разобран скриптом — реплик владельца 83, из них 57
отправлены ПОСРЕДИ хода агента (записи `attachment.type = "queued_command"`, невидимые обычной
выборке). В документе стояло 38 строк; семь замечаний восстановлены дословно (строки 44–50). Шесть
из семи были ПОЧИНЕНЫ, но не ЗАПИСАНЫ, а два сегодняшних (39, 40) показали цену потерянной записи:
за ней уходит и правка.

### Что изменилось на витрине

- Метрики переименованы честно: «за время работы над версией» вместо «в репозиторий», рядом —
  объём САМОГО ядра поставки (104 000 слов). Проза меряется словами, код — строками.
- Безымянные «романы» заменены названными книгами: «Анна Каренина» (349 736 слов), «Хоббит»
  (95 000), семикнижие «Гарри Поттера» (1 084 625), «Война и мир» (587 287) — объёмы сняты из сети,
  таблица источников в `plans/69`.
- Коммиты сравнены с отраслью по живой статистике: 1–3 коммита на разработчика за день, медиана
  размера коммита 16 строк, типичный 50–100 (Pluralsight Flow · Count.co · arXiv 1408.4974 ·
  Allstacks · GitClear); наш замер — 204 коммита, ≈ 250 строк на коммит, коммит каждые 12 минут.
- Сняты строки, которые владелец назвал бесполезными: сессии работы, размер коммита, отношение
  прайса к оплате, построчная стоимость работы моделей. Денежные строки и лимит подписки уехали
  в низ таблицы.
- Страница релиза получила развёрнутую установку: четыре просьбы, у каждой сказано, что она меняет.
- Снято обещание «во время установки у вас ничего не спрашивают» — витрина не отвечает за чужой
  харнесс (слово владельца).

### Что закрыто механически

- **Класс `NBSP` в `showcase-lint`** (13-й): обычный пробел между числом и единицей. Заведён после
  того, как правило дважды за вечер отработало вхолостую — после `$` нет границы слова, а `\b` в
  JavaScript слеп к кириллице. Обе ветки доказаны красным дословными мутациями.
- **Ось охвата селфтеста витрины**: прогон печатал «каждый класс доказан», а проверял только классы
  с мутацией. Теперь охват ВЫЧИСЛЯЕТСЯ и краснеет на непокрытом.
- **Урок EXP-0082**: счётчик «поправлено N» считает сработавшую ветку, а молчащая в него не
  попадает — массовая правка обязана кончаться приёмкой по остаткам.

### Карточка Таблицы 6 для социальных сетей

`tools/build-story-card.mjs` из заготовки `[NOT-TESTED]` доведён до рабочего инструмента:
вся таблица в ОДНОМ кадре 9:16 (PNG 2160×3840), вид тёмной таблицы GitHub, три колонки, строки
читаются из живого README по шапке таблицы. Кегль ищется замером — самый крупный, при котором
таблица влезает; кадр делится только если ячейка стала бы мельче 16 px. Три дефекта рендера найдены
пробами и починены до показа: ширины не действовали без `colgroup` (при `table-layout:fixed` их
задаёт первая строка); константа неразрывного пробела в самом рендере оказалась ОБЫЧНЫМ пробелом и
стирала неразрывные из README; заработавшие неразрывные склеили значение в токен, наезжавший на
соседнюю колонку.

### Что эта волна говорит о процессе

Два класса, названные владельцем и подтверждённые пробами: **агент подменяет факт владельца своим
предпочтением** и **агент чинит, не записывая**. Оба оплачены дважды. Метрика здоровья прежняя:
следующая волна владельца по витрине меньше этой.

---

## Сессия 34 — круг R2, блокеры и публикация релиза 2.2 (2026-08-09, 14:33 → 19:39 +03:00)

> Перенесено из `STATUS.md` ДОСЛОВНО при закрытии следующей сессии («стрижка бонсая»).

⚔️ **КРУГ R1 ИСПОЛНЕН 2026-08-09 09:29 +03:00.** Многоагентная оркестрация: 66 агентов, 56 находок
судимы, **46 CONFIRMED / 10 REFUTED**, двенадцать классов `bugs/65`–`bugs/76`. Сквозной диагноз:
заявление и его проверка расходятся, когда меняются окрестности — прокси вместо свойства ·
фикс-список охвата · закрытие пересказом вместо переисполнения.
**Полный вердикт, таблица классов и назначенные оси R2 — `plans/59` → «КРУГ R1 — вердикт».**

✅ **ФАЗА ПОЧИНОК R1 ЗАКРЫТА 2026-08-09 14:13 +03:00 — одиннадцать классов волны из двенадцати**
(сессии 31–33). Двенадцатый — `bugs/74` — ждёт ПОСЛЕ R2 по слову владельца (окончательный замер
витрины назначен им туда), и это НЕ долг фазы, а её граница. Плюс шесть багов, заведённых и
закрытых по ходу починок (`bugs/78`–`bugs/83`).

📌 **Правило, по которому фаза велась** (слово владельца 2026-08-09 ≈12:15 +03:00, дословно):
«нужно починить в фазе R1 всё, что фаза R1 нашла. Не полагаемся на фазу R2. Не надеемся на неё,
что ей что-то передадим и она пофиксит. Она будет независимой от R1. R2 — полный аудит после R1 с
фиксами, проделанными в R1. Надеемся, что R2 не выявит баги, поэтому всё чиним в R1».

🧭 **Форма починки, оплаченная волной** (уроки EXP-0079…EXP-0081): **охват — вычисление, а не
список; свойство мерится ТАМ, ГДЕ ЖИВЁТ; изъятие требует названной причины.** Где вычислить нельзя
по природе задачи — это говорится вслух и называется исполнитель-человек, вместо кредита машине,
которого она не отрабатывала.

⚔️ **КРУГ R2 ИСПОЛНЕН 2026-08-09 17:32 +03:00.** Две половины, обе обязательные по решению №64:
**полное ревью 2.2 ЗАНОВО** по семи осям, которых R1 не смотрел (39 находок судимы → **24
CONFIRMED / 15 REFUTED**), и **переисполнение заявлений фиксов R1** (18 баг-доков волны → **10
REFUTED · 8 с оговорками**, 115 дыр охвата). Всего 114 агентов, 3219 прогонов инструментов.
Сведение сырых находок обоих кругов: **161 → 112 различных дефектов** (blocker 5 · major 92 ·
minor 15).

🧭 **Сквозной диагноз R2 — тот же механизм, что нашёл R1, но воспроизведённый В САМИХ ПОЧИНКАХ
R1.** Стражи, построенные волной R1, настоящие: почти каждый краснеет на подсунутом дефекте и
молчит на чистой копии. Провалилось другое — **охват снова задан фикс-списком дня рождения**.
Правило «охват — вычисление, а не список» было записано, оплачено тремя уроками и не исполнено
теми же руками, что его писали.

✅ **ВСЕ БЛОКЕРЫ ЗАКРЫТЫ 2026-08-09** (коммиты `300104b`, `6bf6445`, `2cb1c33`, `f65107f`):
утечка личного текста в публичный слепок · двойной номер решения №66 · разнос по первому
адресату · отсутствие одобрения на странице контура · пустой ключ документа · полевой отчёт не
требовался на штатном маршруте обновления. У каждого фикса страж, доказанный красным на
сломанной версии; мутации — на копиях, рабочее дерево не тронуто.

📮 **ISSUE #2 ЗАКРЫТ 2026-08-09 15:47 +03:00** (момент снят у GitHub: `closedAt` 12:47:49Z) —
первая живая отправка через гейт за всю историю проекта
(`issuecomment-5231596643`, состояние `CLOSED` проверено после отправки). Сверка §12 дала 11/11
ещё 2026-08-07; не хватало ответа и закрытия.

**Релиз 2.2 ОПУБЛИКОВАН** 2026-08-09: тег переставлен на выпускаемый коммит (force-push по
слову владельца «A»), шесть артефактов приложены, метка pre-release снята, `latest` = `v2.2`.
Проверено состоянием: `gh release view v2.2` → `prerelease: false`, `assets: 6`.

**Первая волна витрины — 38 правок владельца**, записанных дословно в `plans/69`; там же найден
дефект `kaif-stats`: флаг `--since` брал базой не последний коммит ДО границы, а результат
`git log --since --reverse`, и окно молча теряло 57 коммитов первого дня. Починено: граница
берётся `git rev-list -1 --before`.

🗑️ **Снято по слову владельца:** инвентарный блок Таблицы 6 (12 строк обеих половин) вместе с его
стражем `showcase-stats-guard` и ВСЕМИ ссылками на него. Следствие названо вслух: **на витрине не
осталось ни одного числа под механической защитой** — всё оставшееся живёт в git-истории и
транскриптах сессий.

---

## Сессия 33 — закрытие фазы починок R1 (2026-08-09, 12:48 → 14:26 +03:00)

Коммиты: `51babed`, `ebacf74`, `0b9d719`, `6c652e7`, `f55e5ad`, `45aeec5`, `0a1ba09`, `4809202`,
`000d2cc`.

**Закрыто пять классов волны и три бага, заведённых по ходу.** Из волны R1: `bugs/65` (четыре
ассерта-прокси), `bugs/71` (правило без исполнителя), `bugs/72` (PENDING без судьбы), `bugs/73`
(строки волны Q кредитуют несуществующих стражей), `bugs/79` (гейт неожиданного файла). Заведены
и закрыты тем же заходом: `bugs/81`, `bugs/82`, `bugs/83`.

**Итог фазы: одиннадцать классов волны R1 из двенадцати.** Двенадцатый — `bugs/74` — вынесен ЗА
фазу словом владельца: окончательный замер витрины назначен им после R2.

### Что дала форма починки

`bugs/65` — свойство измеряется ТАМ, ГДЕ ЖИВЁТ, а не там, где его удобно достать в точке проверки.
Счётчик установки читает диск, а не план (`applyLanguage` стоит ВЫШЕ фильтра анонимности и знать о
нём не может — анонимный прогон печатал 35 при 31 навыке на диске); двухполовинность ВЫЧИСЛЯЕТСЯ
из якоря `<a id="russian">`, поэтому следующий двухполовинный файл реестра получит посегментную
проверку без правки списка; порог тишины контура, измеряемый минутами, получил ОДНОСТОРОННЕЕ окно
наблюдения — переменная окружения принимается, только если она строже канона, поэтому поле не может
стать терпеливее слова владельца. Каждая мутация уложена в фикстуру (`s09` G10d/G10e · гард 5e
`check-framework --selftest` · `verify-contour` QA3).

`bugs/71` — у правила либо исполнитель, либо честное «механизации нет». Обещание «показ стережётся
механически» СНЯТО из обоих слоёв: проверяемый текст есть ответ агента в чат, его нет на диске, и
команда для него невозможна по построению — исполнителем назван сам агент в точке отправки. Норма
доставки полевого отчёта наверх переписана на реальность в обоих слоях (отчёт остаётся локальным,
отправка — действие от имени владельца). Двадцать файлов машинерии из сорока четырёх не несли ни
`[TESTED]`, ни `[NOT-TESTED]` — проставлены ПО НАБЛЮДЕНИЮ, один честным `[NOT-TESTED]`
(`readme-pdf.mjs`: `md-to-pdf` в дереве не разрешается); гейт маркеров встал четвёртым преполётом
`npm run test:core` при нулевом долге.

`bugs/72` — у обязательства появилась СУДЬБА, а не адрес. Шесть обязательств эпика M висели под
формулировкой «вынесены в Фазу R»: адрес выглядит судьбой, но приёмник о них не знал — слова
`PENDING` в плане Фазы R не было вовсе. Три подняты интервью №017 и решены владельцем, два закрыты
починкой, одно объявлено долгом с названным носителем. Класс механизирован осью «PENDING без
СУДЬБЫ» в `doc-header-lint`. Попутно карта разведки O5 приведена к поставке (строка Antigravity
обещала стража STATUS, которого свод `s14` требует НЕ везти), Grok Build перестал брать запрещённый
планом третий исход, адаптеры ресинкнуты на шипующийся модуль `.kaif/hooks/`.

`bugs/73` — шесть строк волны Q получили либо фикстуру, либо честного исполнителя-человека. Ось
HALVES научилась считать подписи таблиц и БЛОКИ цитат; форм-лист EXCUSE знает приписку со ссылкой
на внутренний документ; ставка «1 800 знаков» получила названный внешний источник в рабочем
документе. Полный проход всех 22 строк НЕ делался — сужение названо вслух, с причиной и с
исполнителем (круг R2).

`bugs/79` — у коммита появился гейт неожиданного файла (новый файл в дереве останавливает
ПОДМЕТАЮЩИЙ прогон и называет себя), отказ на постороннем аргументе и `--selftest` на оба ответа;
гейт стоит ДО бампа `version.json`.

### Три бага, которые нашла РАБОТА КОНТУРА, а не чтение

`bugs/81` — клетка буквы с пометкой в скобках (`| **A** (рекомендация агента) |`) не опознавалась
разбором, и вариант ИСЧЕЗАЛ со страницы владельца. Форма прижилась именно у рекомендации агента,
поэтому выпадал систематически рекомендованный вариант. **Дефект достиг владельца:** в
опубликованном `interview_016` автор написал четыре варианта, страница показала три. Молчали ТРИ
проверки: ось «вопрос НЕОТВЕЧАЕМ» мерила НОЛЬ (а ноль — частный случай потери), независимая примета
G11 делила с парсером то же слепое пятно, эталон морозил числа, снятые с дефектного разбора.

`bugs/82` — ключ ратчета считался по всему тексту строки, поэтому маркер `<!-- closed-ok: … -->`,
дописанный соседней осью, воскрешал давний долг «новым нарушением». Страж наказывал ровно за то,
что канон требует делать, — называть причину изъятия на строке.

`bugs/83` — вопрос отправлял владельца в документ. Его слова со страницы контура дословно: «покажи
мне этот порядок в контексте задаваемого вопроса! не отправляй меня капаться в MD документах!
Тысячу раз об этом просил! Открытый вопрос должен быть достаточен для моего понимания решаемого
вопроса!» Канон требовал этого для АРТЕФАКТОВ («показ — действие, а не ссылка»), но вопрос
артефактом не является. Норма дописана в канон обоих слоёв и механизирована осью
`questions-guard`. **Цена дефекта видна на том же вопросе: переписанный, он принёс ДРУГОЙ ответ** —
владелец, увидев порядок целиком, переставил его, а за ссылкой подтвердил бы предложение агента
молча.

### Решения владельца №66–68 (интервью №017)

- **№66** — отчёты об установке лежат вместе с отчётами об обновлении (`reports/KAIF_UPDATES/`).
- **№67** — **ПРОСТОТА ПОДНЯТА С ДЕВЯТОГО МЕСТА НА ТРЕТЬЕ** в векторе целей KAIF, остальные цели
  сдвинуты вниз. Слово владельца: «пункт 9 поднять до 3-го, остальные передвинуть ниже. принимаем».
  Рекомендация агента отвергнута. Простота встала выше правила «решения владельца у владельца».
- **№68** — оговорки истока в README директорий остаются как есть.

### Уборка и хозяйство

Промо-постер 2.2 закоммичен в `assets/`, но в поставку не едет по построению (`framework/` →
`KAIF.md` + `dist/`; `assets/` в набор встраиваемых блоков не входит) — записано строкой во внешней
карте. Из `C:\Program Files\Git\` убраны 19 агентских черновиков, натёкших туда с 11 июля: в
git-bash `$TMPDIR` ПУСТА, поэтому `"$TMPDIR/run.log"` схлопывается в `/run.log`, а MSYS2
разворачивает это в каталог установки Git — при зелёном прогоне. Факт и проба записаны в досье
окружения.

Уроки сессии: **EXP-0080** (прокси и свойство расходятся при первом изменении окрестностей; молча
проглоченный аргумент CLI — самый дорогой вид прогона; гейт, краснеющий там, где риска нет, —
будущий отключённый гейт) и **EXP-0081** (вопрос владельцу самодостаточен; плохо заданный вопрос не
раздражает, а ФАЛЬСИФИЦИРУЕТ решение).

---

## Сессия 32 — девять классов волны R1 (2026-08-09, 10:55 → 12:44 +03:00) — подробности

> Перенесено из STATUS дословно при стрижке бонсая 2026-08-09 14:26 +03:00.

**Что дал `bugs/66` сверх самих фиксов** — общая форма починки на весь класс: **охват перестал
быть списком и стал вычислением**, а изъятие требует названной причины. Где вычислить нельзя по
природе задачи, это сказано вслух и назван исполнитель-человек (грамматика: падеж и управление —
глаза владельца, а не `voice-lint`). Новые охваты в первый же прогон нашли ТРИ настоящих дефекта
на живых артефактах: три утечки личного текста в опубликованном слепке · «for every dollar
**actually** paid» на витрине README · одиннадцать несверяемых зеркал раскладки и документов.

⚠️ **Новые оси нашли ШЕСТЬ настоящих дефектов на живых артефактах, и три из них были СВЕЖИМИ
работами самого агента** — метка, поставленная вперёд через час после написания оси правдивости;
`bugs/69`, закрытый «целиком» без одного пункта плана; кириллица в английском шаблоне. Ни одну не
нашло чтение. **Это и есть довод EXP-0079: правило, держащееся на намерении, не держится — даже у
сессии, которая в этот самый час его и пишет.**

- ✅ `bugs/66` — **ЗАКРЫТ ЦЕЛИКОМ 2026-08-09 11:51 +03:00** (все семь охватов). ⚠️ Один хвост
  остаётся НЕ у агента: при починке выяснилось, что утечка была НЕ латентной — в опубликованном
  слепке нашлись **три настоящие утечки** личного текста владельца. Слепок пересобран и чист,
  `--selftest` доказывает семь форм, но **судьба git-истории — решение владельца** (удаление файла
  историю не отзывает; переписывание требует force-push, то есть строки `AUTH:`).
- 🟡 `bugs/69` — **блокер ЗАКРЫТ 2026-08-09 10:14 +03:00.** Эталон пересмотрен глазами и
  переморожен (прогон: 125 зелёных, 0 красных); у приёмки появилась дешёвая половина
  `--etalon-only` (0,12 с, без браузера) — она стоит в `/end-chat`, полный прогон в `/release`,
  а снятие любого вызова краснит свод `s12`. Открытым остался `g5-live-numbers` (пять зашитых
  обращений к живым документам).
- 🔴 **`bugs/78` — метка проверяется ФОРМОЙ, а не ПРАВДОЙ** (нашёл ВЛАДЕЛЕЦ 2026-08-09 10:56 +03:00:
  «на часах 10 частв, не понимаю, о каких 12 ты говоришь»). Агент выдумывал время меток вместо
  пробы; расхождение доросло почти до двух часов и попало в семь баг-доков, вердикт круга и маркер
  освежения. Все одиннадцать меток восстановлены из моментов коммитов.

**Исполнимые правила прошлых чатов** (перенесено из STATUS; сами уроки живут в `EXPERIENCE.md`):
**(а)** карточка находки несёт СЕГОДНЯШНИЙ измеренный замер — сними его ДО кода (EXP-0056…0058);
**(б)** мутационная проверка отвечает на ДВА вопроса — краснеет на дефекте И молчит на «не
трогать» (EXP-0059); **(в)** документ, по которому поднята страница контура, НЕ трогать до её
закрытия, мутация делается на КОПИИ (`bugs/62`); **(г)** инвентарь зеркал грепом по ЦИФРЕ
пропускает вхождения в другой формулировке — грепи по ПОНЯТИЮ (EXP-0070); **(д)** сравнительное
числительное в отчёте стареет ВНУТРИ своей же задачи (EXP-0067); **(е)** правило языка стережётся
ФОРМОЙ нарушения, а не обобщением этой формы (EXP-0074); **(ж)** страж, который не проверяют
селфтестом, ломается тем же дрейфом, который сам стережёт (EXP-0075).

---

## Сессия 31 — круг R1 и первая треть починок (2026-08-09, 08:18 → 10:55 +03:00)

**Круг R1 исполнен многоагентной оркестрацией**, включённой владельцем («ultracode + workflows
включены!»): девять независимых ревизоров по кластерам ЗАЯВЛЕНИЙ (эпики L·K·M·N·O, задачи T1–T6 и
T7–T12, фаза Q, сквозная охота на ложные заявления), скептик с дефолтом REFUTED на каждую находку
(три линзы), критик полноты в конце. **66 агентов · 56 находок судимы · 46 CONFIRMED · 10 REFUTED ·
0 UNCERTAIN.** Находки легли двенадцатью классами — `bugs/65`–`bugs/76`, четыре блокера релиза.
Сквозной диагноз круга: заявление и его проверка расходятся, когда меняются окрестности —
прокси вместо свойства · фикс-список охвата · закрытие пересказом вместо переисполнения.
Оси круга R2 назначены критиком полноты и записаны в `plans/59`. Урок — EXP-0076.

**Починки R1, первая треть** (коммиты `1450cb9`, `73f158b`, `dc0f5ce`, `2efbabf`, `320cf2a`,
`4ddfe84`, `4aa16b3`):

- `bugs/66` №1 — фильтр публичного слепка стилометрии переписан формой: сканер вложенности +
  страхующее выражение по всем парам кавычек + проход по абзацу для многострочных; приёмка
  получила третью, НЕЗАВИСИМУЮ от глифов ось (n-граммы против доказательных блоков источника).
  **Она и нашла три настоящие утечки в опубликованном слепке** — то есть находка R1 была мягче
  правды. `--selftest` на семи формах; пересборка против побайтного эталона: 114 слов убрано,
  новых токенов нет.
- `bugs/69` блокер — эталон контура пересмотрен ГЛАЗАМИ и переморожен (119/3 → 125/0); блок 9
  вынесен в дешёвый режим `--etalon-only` (0,12 с, без браузера) и вставлен в `/end-chat`, полный
  прогон — в `/release`; сам факт вызова стережёт `s12` (17 → 20 проверок).
- `bugs/65` №3 — эвал-ловушка дедупа перестала быть тавтологичной: контракт читается из
  РАЗВЁРНУТОГО навыка, формат отпечатка выводится из его шаблона. Вырез 4503 символов
  KAIF-ветвления `/report-bug` из бандла теперь красит свод (прежде проходил зелёным).
- `bugs/74` — построен гейт публикации `showcase-stats-guard`: живые числа из `kaif-stats --json`,
  каждая половина README отдельно, честная граница «что НЕ судится» печатается вслух.
- `bugs/70` ✅ — «Ждут ответа: 0» больше не может быть ложным зелёным: закрытость читается
  РАЗМЕТКОЙ (строка статуса), распознавание отрицательное, ось bugs/62 скоупится собственным
  свойством вопроса. Попутно найдено: селфтест ядра печатал красный и выходил кодом 0.
- `bugs/67` ✅ — ось меток получила вторую примету (безглагольная веха в поле «Статус:»), снят
  фильтр `_DONE_`, шесть изъятий по реальным ложным тревогам; три исторические метки восстановлены
  из git.
- `bugs/68` ✅ — у стража счётчиков появилась ОСЬ РАСКЛАДКИ: 38 → 48 зеркал, красный доказан на 11.
  Девять протухших счётчиков сведены; страж витрины научился называть свой состав (`--classes`).

**Дефект, найденный ВЛАДЕЛЬЦЕМ в конце сессии:** агент выдумывал время меток вместо того, чтобы
снимать его пробой, — расхождение доросло до почти двух часов и попало в семь баг-доков, вердикт
круга и маркер освежения. Все одиннадцать меток восстановлены из моментов коммитов; заведён
`bugs/78` (страж проверяет ФОРМУ метки и не умеет проверить её ПРАВДИВОСТЬ), урок — EXP-0078.

Уроки сессии: EXP-0076 (суди заявления, а не работу), EXP-0077 (мутация — на копии; `\b` не
работает с кириллицей), EXP-0078 (время — факт о мире, снимается пробой).

---

## Перенесено из STATUS 2026-08-09 (стрижка бонсая, сессия 31)

✅ **ФАЗА Q ИСПОЛНЕНА 2026-08-09 02:40 +03:00** (`plans/67`, решение №66) — витрина, инструментарий,
канон и ядро стилометрии пересмотрены; у каждого из **22 дефектов** волны стоит страж или правило.
**Фазы R разблокированы.** Что именно сделано — летопись, сессия 30.

🚀 **Тело страницы релиза задеплоено 2026-08-09 08:05 +03:00** и проверено ГЛАЗАМИ по шагу 6.9
(`gh release view v2.2 --web`): абзацы целы, логотип на месте, обрывов нет.

---

## Записи (новые сверху)

### Сессия 2026-08-09 (тридцатая, ЗАКРЫТА) — **фаза Q исполнена: витрина, инструментарий, канон и ядро стилометрии пересмотрены**

Чат начался с `/resume` в 01:14 +03:00. Владелец накануне нашёл на витрине 2.2 волну дефектов
глазами за один проход и своим словом отодвинул фазы R: «Раз я нахожу такие косяки, значит есть
дыры в этом всём. R фазы отодвигаются. Перед ними появляется эта новая ваза» (решение №66).
Корневой диагноз — тоже его: «такое ощущение, что ты пишешь английский, но русскими словами».

Посреди работы владелец добавил ещё два класса дефектов и снял ограничение по автономности:
«работай автономно. Критерий выхода - готовность по фазе, переработка всего, что задано на фазу,
витриты и стилометрического ядра».

**Что сделано:**

- **Витрина переписана, а не подлатана.** Русская половина README написана заново от смысла:
  порядок установки обращается к читателю («Положите», «Скажите», «Одобрите», «Заполните») вместо
  безличного залога; убрана определительная связка «X является Y» (14 вхождений) и канцелярское
  «настоящий документ/репозиторий»; сняты все приписки-оправдания — сертификация на 12B, ссылка на
  домашку как доказательство, «эти деньги НЕ платились», методики счёта в клетках таблицы метрик,
  команда замера в шапке «Интересных фактов» (границы окна оставлены — EXP-0066 требует именно их).
- **Двадцать два дефекта волны, у каждого страж или правило** — таблица `plans/67` заполнена
  целиком; два новых класса владелец назвал прямо по ходу фазы. Рядом в плане — вторая таблица
  «найдено СТРАЖАМИ сверх волны» из девяти позиций.
- **Четыре новых стража, каждый доказан красным И молчанием:** `showcase-lint` (девять классов
  витрины дословными формами из волны + три оси: симметрия половин, врап в теле релиза, равенство
  чисел нот числам README) · `estimates-guard` (у оценочной константы назван внешний источник;
  границей секции служит пустая строка, иначе одна ссылка в шапке файла «покрывала» бы всё) ·
  `private-names-guard` (приватные имена не едут в поставку; без списка НЕ СУДИТ и говорит это
  вслух) · `revert-guard` (рабочая копия побайтно равна не-HEAD ревизии — примета узкая, ложных
  срабатываний нет по построению; хеш считает `git hash-object`, потому что при `core.autocrlf=true`
  самодельный sha1 был бы зелёным всегда).
- **Раздел «Витрина — текст, который читает посторонний человек»** (десять правил) записан в ОБА
  слоя `AGENT_GUIDE`; **шаг 6.9 «гейт публикации»** — в `/release` обоих слоёв.
- **Ядро `krinik-stylometry` поднято до 1.1**: АП27–АП37 (одиннадцать записей — калька, антитеза,
  оправдание числа, описание собственного текста, намёк на второй уровень, отрицание при числе,
  внутренний ярлык, склеенные величины, вилка шире источника, проверка публикации, безличный залог
  в инструкции). Слепок `AUTHOR_STYLOMETRY.md` пересобран генератором. Реестр корпусов получил
  строку `owner_showcase_reviews` — первый разобранный источник ДОКУМЕНТНОГО регистра.
- **Утечка приватных имён закрыта дважды:** 110 вхождений пяти имён в поставке заменены
  псевдонимами «project A»…«project E» (псевдоним сохраняет ЧИСЛО независимых свидетельств), и
  ещё пять нашлись в ПУБЛИЧНОМ слепке портрета — они закрыты В ГЕНЕРАТОРЕ, а не руками в генерате.
- **`bugs/64` заведён и закрыт:** прогон селфтестов ВСЕХ тринадцати стражей (пункт автономного
  беклога) нашёл один сломанный — `counters-guard` не мог доказать собственное МОЛЧАНИЕ с момента,
  как оси бейджей добавили ему два живых источника в корне, а список файлов песочницы не тронули.
- **`bugs/45` механизирован:** предкоммитный дифф поймал ВТОРОЙ откат рабочей копии `plans/26`
  (тот же файл, снова пропала самая свежая правка), что выполнило условие механизации, записанное
  самим документом бага.
- **`voice-lint` B10 понижен из порога в справку** с названной границей: оба числа коридора
  замерены на свободной прозе, корпуса документного регистра нет, а владелец на витрине забраковал
  ровно определительную связку «X является Y». Пункт, требующий того, что владелец отвергает,
  порогом быть не может.
- **Релиз-ноты сверены с README:** ноты несли ДРУГОЙ замер того же окна («≈ 18 825 романов»,
  «2 149 834 символа» против «17 919» и «2 149 895»). Один замер, два зеркала, молчаливый дрейф —
  теперь его стережёт ось NOTES-VS-README.

**Уроки:** EXP-0072 (ритуал внимания становится механизмом через УЗКУЮ формальную примету; хеш
обязан считать тот, кто его сравнивает) · EXP-0073 (страж утечки ловит только названные зоны, а
забытых всегда больше; псевдоним лучше вычёркивания) · EXP-0074 (правило языка стережётся ФОРМОЙ
нарушения, а не обобщением) · EXP-0075 (страж, который не проверяют, ломается тем же дрейфом двух
списков, который сам стережёт).

**Решения агента без владельца** — шесть, все выписаны в `plans/67`. Главное из них: дословные
слова владельца из волны НЕ поехали в публичный слепок портрета, хотя решение №60 их пропустило бы
(часть — крепкая брань, и выносить её на канон-артефакт без слова владельца агент не стал; в
слепке на их месте «…» с сохранённым адресом).

**Чем закрылась сессия:** дерево зелёное — `npm run test:core` 14 сводов, 13 селфтестов стражей,
`counters-guard` 38 зеркал, `showcase-lint`/`private-names-guard`/`estimates-guard`/`voice-lint`/
`doc-header-lint` чисты, `questions-guard` «Ждут ответа: 0», `README.pdf` пересобран. Коммиты
`73ecffc`, `10e2f1e`, `e15dacf`, `30b0a28`, `9974321`, `9293ca2`; приватное ядро — `ce018c9`,
`3795c1e`.

### Сессия 2026-08-08 → 2026-08-09 (двадцать девятая, ЗАКРЫТА) — **задача T11: витрина 2.2 опубликована в дату 08.08.2026**; решение №65; дефер G4 закрыт

Чат начался с `/resume` в 23:19 +03:00 — за **сорок минут до полуночи** — и с просьбы владельца:
«нам нужно будет задеплоить README и релиз ноутсы, чтобы они успели попасть в GitHub в красивую дату
08.08.2026 — день рождения KAIF 2.2… Можно даже оформить будет релиз без артефактов». Следом,
посреди работы: «сделаем несколько версий README и релиз ноутсов — черновые в 08.08.2026, а затем уже
неспеша их переделываем КАК ПОЛОЖЕНО».

**Что сделано:**

- **Страница релиза v2.2 опубликована 23:28 +03:00** —
  https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.2, заголовок `KAIF 2.2 — Yolden KAIF`,
  версионное лого сверху в КАЖДОЙ половине, `version.json` поднят до 2.2, released 2026-08-08.
  Метка **pre-release** поставлена осознанно и доказана пробой: канал `release` загрузчика тянет
  ассеты из ПОСЛЕДНЕГО релиза, поэтому релиз без артефактов в роли «latest» сломал бы установку
  парку — при метке `latest` остаётся у v2.1 с шестью ассетами.
- **Витрина T11, шаги 1–6** (`plans/65`): команда установки без `--mode`, анонимность понижена из
  полноценного §2.3 в `<sub>`-сноску §2.2 обеих половин, лого версии с авторской трактовкой
  символики, README актуализирован под 2.2 (модуль `refresh-hooks` в §3.4, новый §5.6 «Свежий
  контекст», хук-конфиги в §7.2, строка v2.2 в Таблице 5), релиз-ноты компактной дельтой
  (1 830 слов против 2 120 у нот 2.1), блок «Интересные факты» с Таблицей 6.
  Определение «KAIF — это…» не тронуто: 0 строк диффа за всю сессию.
- **Три протухших счётчика на публичной витрине** — §8.2 обеих половин говорила «34 шаблона
  навыков» при 35, «шесть README директорий» при семи, «девять адаптеров» при десяти. План 62
  объявил ось навыков покрытой, но эти зеркала в неё не попали. Класс закрыт ФОРМОЙ:
  `counters-guard` получил живые оси `adapters`/`langs`, **26 → 34 зеркала**; красный достался
  бесплатно на живых дефектах, адресность потом доказана мутантами 7→6 и 10→9.
- **`tools/voice-lint.mjs`** — слой 7A чек-листа портрета голоса одной командой (жёсткие
  A2/A3/B5/B9/B12, коридоры A12/B8/B10, границы жанра портрета соблюдены), вшит в `/release` шаг 2.
  Три из пяти первых срабатываний оказались дефектами САМОГО линтера (числовой диапазон принят за
  тире, код-спан через врап, буллет за тире) и были починены в страже, а не в тексте.
- **`tools/guarded-watchdog.mjs`** — внешний сторож защищённого цикла: **дефер G4 закрыт первым
  полевым прогоном**. Пульс судится последней СТРОКОЙ `.kaif/heartbeat.log`, а не mtime, поэтому
  «пульс от таймера» технически не проходит; задача планировщика Windows наблюдена живьём
  (`LastTaskResult=0`), за прогон ноль тревог, шесть строк пульса на шесть коммитов.
- **Решение №65 (интервью №016, ответ владельца за минуту через контур):** витрина изъята из-под
  пометок провенанса `[AI]`. Вопрос нашёл СОБСТВЕННЫЙ judge-проход: `/release` называет README
  артефактом владельца, а write-gate требует в артефактах владельца видимых пометок — вместе это
  означало публикацию README со служебной разметкой, и правило нарушалось молча два релиза подряд.
  Изъятие дописано в оба слоя поимённо по файлам; очередь на приёмку осталась обязательной —
  вычитка владельцем, `homeworks/05`.
- **`bugs/63`** (research-only): бамп до 2.2 дал НЕвоспроизводимый красный в своде s03/S10, а разбор
  нашёл рядом воспроизводимый механизм — обновление ищет baseline-артефакт по
  `releases/download/v2.2` и без него деградирует в legacy-путь у всего парка. Изолят на 100
  прогонов не гонялся осознанно (урок `bugs/61`: при названном механизме форма дороже призрака).
- **Judge-проход опроверг ДВА собственных заявления агента:** «страж покраснел на живых дефектах»
  держалось только для одной оси (охота «адресность мутации»), и «ноты вдвое короче» — замер дал
  14 %, потому что заявление было верным при письме и устарело внутри задачи.
- **Уроки:** EXP-0067 (сравнительное числительное стареет внутри своей же задачи) · EXP-0068 (релиз
  без артефактов меняет путь обновления всему парку) · EXP-0069 (первый прогон текстового стража —
  калибровка стража, не список находок) · EXP-0070 (инвентарь зеркал грепом по ЦИФРЕ пропускает
  вхождения в другой формулировке).
- **Правки владельца по витрине:** кавычки вокруг кодового имени убраны везде («мы их никогда не
  ставили для названия» — правило в канон-таблице идентичности), грамматика авторской трактовки
  символики причёсана в трёх местах (причастный оборот после определяемого слова, точка вместо
  «умной запятой», парное тире вместо рассогласованного «все и…и»), лого добавлено в русский блок нот.

**Дерево на закрытии:** `npm run test:core` — 478 ✅ / all 14 suites green / exit 0 ·
`counters-guard` 34 зеркала · `build-framework` 57/161/689 · `questions-guard` «Ждут ответа: 0» ·
`doc-header-lint` 0 находок · `requirements-lint` 0 находок · вендоренная пара fable совпадает
побайтно · README.pdf пересобран.

### Сессия 2026-08-08 (двадцать восьмая, ЗАКРЫТА) — **задача T12: у KAIF появилась своя `AUTHOR_STYLOMETRY.md`; ядро стилометрии принято владельцем, версия 1.0**; решение №63

Чат начался с вопроса владельца: «а где моя авторская стилометрия? не вижу файла?» — и закрыл дыру
dogfooding целиком за одну сессию. Исток фреймворка канонизировал имя файла портрета голоса, вёз
скелет поставкой — и сам файла не имел.

**Что сделано:**

- **`AUTHOR_STYLOMETRY.md` в корне** — публичный слепок ПРАВИЛ портрета из приватного
  `krinik-stylometry` (решение №60, Q1 = B). Не письмо модели, а **генератор**
  `tools/stylometry-snapshot.mjs`: правила и цитаты переносит код побайтно, рамку пишет модель
  (EXP-0049). Безопасность по умолчанию — наружу не едет НИ ОДНА цитата текста, кроме явно
  разрешённого; адреса обезличены до номера произведения; §6 (пары ДО/ПОСЛЕ) снята целиком и
  объявляет это вслух. Порог по длине спана был построен первым и **выброшен после замера**:
  четырёхсловные «ёлочки» в источнике оказались целыми авторскими строками.
- **`tools/owner-quotes-harvest.mjs`** — сборщик публичных дословных слов владельца (три
  механических класса, `--selftest` доказывает и опознание, и молчание на постороннем).
- **Сверка версий КОНСТАТИРУЕТ и ничего не требует** (решение №62): `--version-check`, код
  возврата 0 всегда; строка реестра пар помечена как единственная констатирующая. Якорь свежести
  сузился до самого ФАЙЛА портрета через двадцать минут после рождения инструмента — правка README
  ядра сдвинула HEAD и дала бы ложное расхождение (EXP-0063).
- **Слепой тест №015 проведён владельцем** (21:46): авторство 6/8, по языку 4/6. Разбор шестью
  агентами (4 аналитика + 2 состязательных скептика, статистика пересчитана независимо дважды)
  признал **замер недействительным**: отрывок, принятый владельцем за свой, оказался пересказом
  его же дословных слов из `ideas/19:28`; носитель подсказывал ответ (его отрывки — одна строка
  markdown, агентские — четыре-пять, а рендерер контура даёт каждой строке отдельный `<p>`);
  регистры не совпали. Уроки — **EXP-0064** (три канала утечки слепого теста) и **EXP-0065**
  (критерий «различие незначимо» самообслуживающий: гейт проходился наугад с вероятностью 0.637).
- **Решение №63 (22:05 +03:00), дословно:** «я целом я скажу, что похоже. Если твои разведчики
  найдут ЯВНЫЕ проблемы или запросы на улучшение по ядру - давай их подтюним и больше слепой тест
  не проводим. Считаем ядро принятым после этого цикла полировки».
- **Цикл полировки — круг 4 в приватном ядре** (коммит `12aacf4`): страж A9 **чинён** (паттерн
  гасился дефисом и зеленел ровно на своём же случае); «капс на термине = машина» понижен из
  «самого надёжного теста в обоих жанрах» до жанровой приметы — контрпример написан рукой автора
  (`GOAL.md:5` «КАК ИИ-АГЕНТ»); С4, С5, АП5, АП10, АП12 сужены по жанру; B11 получил исключение
  для перечислительного периода; добавлены **АП22–АП26** с замерами; **источник ядра спасён** из
  скретчпада чужой сессии в OS-temp в `data/` репозитория; объявлена **версия 1.0**, статус
  `accepted`. Слепок пересобран: **140 правил**.
- **Побочно:** `/release` о портрете голоса не знал вовсе — абзац добавлен в шаг 2 обоих слоёв;
  приватный README ядра нёс устаревший запрет вывоза, отменённый решением №60, — поправлен.
- **Блокер задачи T11 снят.**

**Перенос из STATUS дословно (закрытая работа чатов 25–27):**

> 1. ✅ **ЧАТЫ 25–27 ЗАКРЫТЫ; ВЕСЬ УРОЖАЙ ПРОБНОГО АУДИТА T3 ОТРАБОТАН** (шесть дефектов:
>    `bugs/59`, `55`, `60`, `56`, `57`, `58`), плюс закрыты найденные по дороге `bugs/61` и
>    `bugs/62`. Уроки EXP-0051…EXP-0061.

### Сессия 2026-08-08 (двадцать седьмая, ЗАКРЫТА) — **урожай аудита T3 добит ПОЛНОСТЬЮ** (`bugs/57`, `58`), закрыты `bugs/61`/`62`, заведена задача T12, решения владельца №59–62

Чат по разбиению владельца — «в следующем чате будем чинить пять дефектов». Задача выполнена и
переросла себя: кроме двух переданных дефектов закрыты два найденных по дороге, оба — классы, а не
экземпляры, и оба нашёл не гейт (один — мутационный прогон, второй — сам ВЛАДЕЛЕЦ на живой
странице).

**Перенос из STATUS дословно:**

> 2. ✅ **ПЯТЁРКА ДОБИТА 2026-08-08 19:55 +03:00 — `bugs/57` и `bugs/58` ЗАКРЫТЫ.** Весь урожай
>    пробного аудита T3 (шесть дефектов) отработан.
>    - **`bugs/57`** — ветка bootstrap разделена по ИНТЕРВАЛУ, а не по наличию чек-поинтов: задача
>      прошлого интервала уходит в `KAIF_UPDATE_TASK.superseded.md` вместе с записанными пунктами,
>      поставка нового интервала доезжает до задачи. Свод `s04`/**S14e** (12 проверок).
>    - **`bugs/58`** — у записи зеркала одна форма `writeMirror` (mkdir + именованный отказ вместо
>      сырого ENOENT); собственный навык проекта больше не роняет `sync`, чек-поинты и оба финальных
>      гейта. Свод `s03`/**S12b** (12 проверок).
>    - 🟡 попутно заведён и **отработан `bugs/61`** (по слову владельца в этом же чате): транзиент
>      не воспроизведён за **120 прогонов**, поэтому починена его БЕЗЫМЯННОСТЬ — немых команд
>      полигона **30 → 0** (`must` + страж преполётом при нулевом долге). Остаток назван; урок —
>      **EXP-0060**.
>    > 💡 **Приём подтверждён третий раз подряд:** замер ДО кода снова опроверг карточку — дословный
>    > `grep -r` из её критерия приёмки зеленел БЫ на сломанном коде (бандл-источник несёт примету
>    > по построению). Урок вынесен в **EXP-0058**; второй урок сессии — **EXP-0059** (красный свода
>    > это половина доказательства, вторая половина — зелёное «не трогать» под той же мутацией).

**Что ещё случилось в этой сессии (сверх перенесённого):**

- **`bugs/62` — «ты опять сломал радиокнопки!»** Владелец на живой странице контура: варианты
  интервью №014 были записаны формой, которой парсер не знает (третьей), и вопрос отрендерился без
  радиокнопок. **Контур был исправен — сломан был документ.** Зелёным при этом было ВСЁ: страница
  поднялась, страж насчитал вопросы, гейты чистые. Симптом владелец поймал ВТОРОЙ раз (`bugs/51` —
  другая причина, тот же симптом), поэтому лечение — ось **«вопрос ОТВЕЧАЕМ»** в `questions-guard`:
  живой вопрос обязан нести ≥2 РАЗОБРАННЫХ парсером варианта или объявить себя свободным. Селфтест
  стража 9 → 13 мутаций; красный доказан на той самой редакции (`git show cb3b00b`).
- **Задача T12 заведена и разблокирована в тот же вечер** (`plans/66`): у KAIF появляется своя
  `AUTHOR_STYLOMETRY.md` — рабочий слепок из приватного `krinik-stylometry`. Развилка «что едет в
  ПУБЛИЧНЫЙ репозиторий» поднята контуром и закрыта владельцем: решения **№60** (Q1=B — слепок
  ПРАВИЛ без личных цитат, граница `STATUS` в силе), **№61** (Q2=B — сначала слепой тест, потом
  публикация; против рекомендации агента), **№62** (Q3=B — KAIF констатирует расхождение версий и
  ничего не требует). Плюс решение **№59** — сам факт внесения T12 в скоуп 2.2.
- **Q3 переформулирован по слову владельца** «не понял варианты ответа»: первая редакция стояла в
  терминах агента (реестр пар vs ресинк), тогда как модель владельца — единое ВЕРСИОНИРОВАННОЕ ядро
  `krinik-stylometry`, которое проекты тянут и дорабатывают ПО ЕГО ЯВНОМУ ТРЕБОВАНИЮ. Исходная
  редакция и его ответ сохранены в git (`c19b01a`).

**Три собственные ошибки сессии, названные честно** (все — нарушения канона, который агент читал на
старте; каждая закрыта механизмом или правилом, а не обещанием быть внимательнее):

1. **Кириллица в argv** (`python -c`), дважды — владелец увидел `������������` и спросил: «ТЫ НЕ
   ЗНАЕШЬ СВОЕГО ОКРУЖЕНИЯ?». Файлы не пострадали (проверено), испорчен был служебный вывод. Дыра
   была в ФОРМУЛИРОВКЕ правила: оно читалось как «про документы проекта». Канон уточнён в обоих
   слоях — правило связывает **АРГУМЕНТ**, а не документ; урок **EXP-0061**.
2. **Вопросы вынесены в чат** таблицей Q1/Q2/Q3 вместо контура, и владельцу была названа команда
   вместо показа («Показ — это действие, а не ссылка»). Реплика владельца: «нет у нас инструмента,
   как можно было бы мне вопросы задать?». Канон предсказал этот провал дословно: «правило нарушают
   даже агенты, которые его ЗНАЮТ — чат дешевле в моменте».
3. **Живой документ перезаписывался под открытой страницей контура**, пока владелец на ней отвечал.
   Ответы уцелели (документ и квитанция `decisions/*.decision.json` согласны), но это была удача.
   Правило записано в `bugs/62` и STATUS. Отдельно: коммит `c19b01a` переобещал состав («до любых
   правок агента»), потому что `commit.mjs` стейджит всё дерево, а `git diff --stat` перед коммитом
   был пропущен — поправка сделана новой записью, история не переписана (решение №55).

**Итог сессии:** шесть дефектов урожая T3 закрыты полностью; два новых класса закрыты стражами при
нулевом долге; четыре решения владельца разнесены; остаток 2.2 — T11, T12 и Фаза R.

### Сессия 2026-08-08 (двадцать шестая, ЗАКРЫТА) — **ЧЕТЫРЕ ДЕФЕКТА ЗАКРЫТЫ** (`bugs/59`, `55`, `60`, `56`), остаток пятёрки — `57`/`58`; заведена задача T11 и скоуп 2.3

Чат по разбиению владельца — «в следующем чате будем чинить пять дефектов». Урожай пробного аудита
T3 чинился ОТДЕЛЬНЫМ проходом от находок, как требует канон ревизии. Закрыто четыре из пяти плюс
один найденный по дороге; два оставлены следующей сессии осознанно (контекст этого чата исчерпан —
`bugs/57`/`58` живут в `KAIF-CORE.mjs` на две тысячи строк, и заход в него на исходе контекста
напрашивался бы на сжатие посреди отладки).

Перенос из STATUS дословно:

- ✅ **`bugs/59` ЗАКРЫТ 2026-08-08 17:30 +03:00** — класс закрыт ФОРМОЙ: единый
  `tools/lib/temp-root.mjs` (`mkdtemp`), **20 вхождений** вместо заявленных 13 (+4 в
  `verify-contour`, +3 фикстуры `--selftest`). Критерий сошёлся (`A=1` → `A=0`/`B=0`); страж
  класса стоит ПРЕПОЛЁТОМ в `npm run test:core` и доказан красным мутацией; контур —
  119 зелёных / 0 красных.
- ✅ **`bugs/55` ЗАКРЫТ 2026-08-08 17:55 +03:00** — закрыты ВСЕ ТРИ вхождения класса: F1 (ось
  навыков сверяет ИМЕНА по каждой половине, а не мощность) · F5 (тавтологичный ассерт `s03`
  заменён наблюдаемым утверждением; страж на класс — преполётом в `npm run test:core`,
  доказан красным) · третье (ключи 9 языковых пакетов — однострочник реестра сверял голый счёт).
  Замер снят ПЕРВЫМ шагом, как требовала карточка: множества тождественны, вопрос алиасов снялся
  сам (`/go` не стоит первой ячейкой строки) — итерация не потеряна, урок EXP-0056.
- ✅ **`bugs/60` ЗАКРЫТ 2026-08-08 17:50 +03:00** — блокировал критерий F1(3), починен тем же
  заходом: цель мутации берётся из текста тем же паттерном, каким её ищет сверка.
- ✅ **`bugs/56` ЗАКРЫТ 2026-08-08 18:35 +03:00** — обратное плечо I20 сверяет СОВМЕСТНУЮ
  примету «интервью №NNN, QN» в окне (замер живых пар: разрыв 1–2 символа, не 172), а не два
  независимых токена по всему файлу; граница `(?![0-9])` убила подкласс «Q1 внутри Q12»; мягкий
  перенос снимается перед проверкой (одна живая пара разорвана врапом — жёсткое «в одной
  строке» покрасило бы дерево). Селфтест 6 → 9 мутаций, красный доказан на СТАРОМ выражении;
  контур 119/0. Урок EXP-0057.

**Что эта сессия добавила сверх самих фиксов.** Два новых стража стоят ПРЕПОЛЁТОМ в главном гейте
(`tools/sandbox-suite.mjs`): уникальность временных корней по построению и запрет ассертов,
дизъюнкция которых заканчивается константной истиной, — оба доказаны красным мутацией. Появился
общий модуль `tools/lib/temp-root.mjs`; инвариант вписан в
`PROJECT_ARCHITECTURE_INTERNAL_MAP.md` → «Инварианты модели». Ось навыков `counters-guard` выросла
17 → 26 зеркал и сверяет ИМЕНА (витрина обеих половин + ключи девяти языковых пакетов), рукописный
однострочник из реестра пар убран.

**Дефект найден по дороге и заведён отдельно — `bugs/60`.** `counters-guard --selftest` был красным
на ЧИСТОМ дереве: цель мутации собиралась конкатенацией с зашитым русским окончанием («бандл 161
блоков»), а проза согласует «161 блок». Доказано отдельным `git worktree` на `HEAD 01317a9`, что к
правкам `bugs/59` он отношения не имеет; починен вместе с `bugs/55`, чей критерий он блокировал.

**Вопросы владельца по ходу и что за ними нашлось.** Владелец заметил в таблице `plans/26` §1
жёлтые круги и открытые чекбоксы. Проверка показала дрейф «истина ↔ зеркало»: дочерние планы
закрыты, а строки родителя не обновлялись — T3 (`plans/33`, 16:50), T4 (`plans/34`, 12:55), T10
(`plans/61`, 2026-08-07 23:30). Актуализировано по уликам из самих планов; отмечены четыре
чекбокса T1, каждый проверен предметно (каталог навыка · строки обеих половин Таблицы 3 · ключи
девяти пакетов), а не по слову документа. Семь чекбоксов §9 НЕ тронуты — Фаза R честно открыта.
Заодно выяснилось, что пункты T7 в файле давно `[x]`, а пустыми их рисовал просмотрщик.

**Заведены документы по слову владельца.** `ideas/24_kaif_2.3_scope.md` — краткий реестр скоупа 2.3
(форма задана владельцем: «кратко! один пункт что сделать нужно в пяти предложениях, и ссылка на
документ плана»), с двумя постановками: `plans/63` — поручение Fable скептически проревьюировать и
переписать под себя навык `/code-revision` (писал Opus, работать по нему Fable); `plans/64` —
ревизия и оптимизация ВСЕХ модулей KAIF, «навести порядок в ящике с инструментами» (тест тяжести
5 из 5 — эпик, первая ступень research). И `plans/65` — задача **T11 в 2.2**: витрина голосом
владельца, чистка команды установки от `--mode`, версионное лого 2.2.

**Страж поймал агента.** `questions-guard` покраснел на вопросе владельцу, который агент оставил в
хвосте `plans/63`, — то самое правило места вопросов, которое, по канону, нарушают даже те, кто его
знает. Переформулировано в ДЕЙСТВИЕ (шаг 0: завести интервью контуром).

**Уроки:** EXP-0055 (починка формой имеет свой провал — вызов, ЗАВИСЕВШИЙ от старой формы как от
фичи; поймано в `verify-contour`, где `makeFixtureRoot()` вторым вызовом намеренно пересоздавала ту
же фикстуру) · EXP-0056 (предупреждение «сначала замерь» в критерии приёмки снимает развилку
целиком: вопрос алиасов оказался несуществующим) · EXP-0057 (эскиз фикса в находке — гипотеза, а не
спецификация: оба числа карточки `bugs/56` оказались неверны; и фикстура, доказывающая, что сканер
НЕ находит X, не имеет права содержать законный X).

Коммиты сессии: `85fb9cc` (build 194, `bugs/59`) · `504a33f` (build 195, `bugs/55`+`60`, plans/26,
скоуп 2.3) · `f523a5c` (build 196, `bugs/56`, задача T11).

### Сессия 2026-08-08 (двадцать пятая, ЗАКРЫТА) — **T3 ЗАКРЫТА: ВСЕ ДЕВЯТЬ ЗАДАЧ 2.2 ГОТОВЫ**, гейт Фазы R зелёный 🟢

- **`bugs/54` закрыт 2026-08-08 16:05 +03:00** — первым заходом, отдельно от T3. Скан протухших
  утверждений предлагал владельцу править ЕГО СОБСТВЕННЫЙ портрет голоса: `AUTHOR_STYLOMETRY.md`
  отсутствовал в `SKIP_FILES`. Правило записано ОДИН раз — портрет дописан в существующий список
  owner-документов, второго механизма исключения не заведено; комментарий над списком называет
  ПРИЧИНУ (портрет пишет агент, принимает владелец, и он ЛЕГАЛЬНО несёт токены версий) и обход,
  который дефект маскировал (`canonArtifacts` работает, но требует ручного действия владельца и по
  умолчанию не выполнен). **Примета стража сужена до ФОРМЫ хита** `· AUTHOR_STYLOMETRY.md:`, а не
  до имени файла: имя приезжает в то же задание нотой миграции 2.2, и голая примета зеленела бы по
  чужой строке — второй раз за два дня тот же способ подмены стража. Фикстура ставит портрет в ТУ
  ЖЕ позицию, что и соседний `PROJECT_NOTES.md`, поэтому свод доказывает «у соседа нашёл, здесь
  промолчал», а не «скан молчит вообще». Мутация **адресная**: ровно один красный — новый ассерт, и
  только он; четыре соседних ассерта S14c зелёные. **Twin-поиск дал ПЯТЬ ЛОЖНЫХ близнецов** —
  `OWNER_SEEDED` содержит пятерых вне `SKIP_FILES`, и расширять список было НЕЛЬЗЯ: `STATUS`,
  `MASTER_PLAN` и обе карты ведёт агент, их версионные утверждения протухают по-настоящему, а
  улика против расширения лежит в самом коде (точечное исключение `if (p === 'STATUS.md' && …)`
  стало бы мёртвым). Урок — **EXP-0051**.

- **T3 закрыта 2026-08-08 16:50 +03:00** (`plans/33`, все 5 шагов и все 7 критериев; рой включён
  владельцем). **Дыра была не в процедуре, а в АРТЕФАКТЕ:** навык подробно задавал, КАК ревизовать,
  и заканчивался строкой-запиской про выход, тогда как `reports/README.md` уже объявлял `KAIF_AUDIT/`
  каталогом отчётов «по документу на класс/семейство… чтобы более слабые модели могли потом
  исполнить фиксы». Контракт был объявлен и не исполнен ни в одном слое — семейство 5 собственной
  таксономии проекта («норма живёт только как проза»), пойманное на самом фреймворке.
  Навык **РАСШИРЕН, не пересоздан**: 74 → 100 строк, нумерация шагов 0–5 оставлена ИСХОДНОЙ
  (разведка сложена в Шаг 0, а не добавлена шестым), восемь защищённых формулировок проверены
  грепом побайтно. Детали ушли в `references/audit-report-template.md` — **канал
  `framework/skills/<n>/references/` уже существовал и уже считался гейтом**
  (`build-framework.mjs:303`, `check-framework.mjs:320-324`), регистрировать ничего не
  потребовалось; риск «раздуть навык» снят по построению. Бюджет тела ИЗМЕРЕН до письма: медиана
  35 навыков — 70 строк, коридор 60–96, хвост с 111; первая редакция вышла на 108 и была ужата.
  Контрпример в семействе — `report-bug` (166 строк, из них ~110 шаблоны A/B инлайном).
  **Второго канона не заведено ни в одном месте:** baseline-дедупликация переиспользует
  СУЩЕСТВУЮЩИЙ `kaif-fp` контура обратной связи, «машинно-проверяемый критерий приёмки» назван
  каноническим *fit criterion*, словарь вердиктов остался судейским. Разведка роем (4 читателя,
  0 ошибок, ~432k токенов) дала 8 полевых форм отчётов, 12 семейств оплаченных классов, 25 точек
  сшивки и 10 прецедентов компактности — конструкцию определила она, а не вкус.

- **Пробный аудит (шаг 5) — первый живой прогон нового `/code-revision`.** 13 агентов, 0 ошибок,
  ~1,18 млн токенов, три зоны (стражи · машинерия · полигон), на КАЖДУЮ находку отдельный скептик.
  **9 находок → 4 опровергнуто → 5 выжило**; отсев 44 % против полевых 43 % (9 из 21) обоих
  аудитов, из которых вырос навык. Цитаты сверены скриптом ДО суждения — **5/5 побайтно**. Одна
  находка умерла из-за неверного адреса при верной мысли (`:617` вместо `:637`): контракт цитаты
  отсеивает и настоящие находки — осознанная цена. Отчёт лёг по шаблону:
  `reports/KAIF_AUDIT/2026-08-08_*` — сводка (вердикт первым · скоуп и методология · карта покрытия
  с ЧЕСТНЫМ «не смотрели» · инвентарь · пределы · «что сменить следующему прогону») плюс 4
  документа семейств с карточками из 8 полей. Находки заведены `bugs/55`–`bugs/58` лёгкими
  баг-доками со ссылкой на карточки (DRY: криминалистика в одном месте). Новых семейств прогон не
  открыл — все пять находок оказались НОВЫМИ ЛИЦАМИ оплаченных классов, то есть таксономия
  работает как прицел.

- **Тест исполнимости проверял карточку, а проверил ШАБЛОН.** Субагент получил ТОЛЬКО карточку, без
  доступа к кодовой базе: `canAct: true` — назвал файл, план правки, три команды доказательства и
  список «не трону». И тут же нашёл в шаблоне четыре дыры, три починены тем же заходом: поле «фикс
  принят, когда» теперь требует СЕГОДНЯШНЕЕ ИЗМЕРЕННОЕ состояние (иначе неизвестно, не красный ли
  критерий уже сегодня по второй причине); поле цитаты требует назвать файлы ФИКСА, если он ложится
  не туда, где дефект; и главное — **поля 6 и 7 читаются ДРУГ ПРОТИВ ДРУГА**: карточка, чей критерий
  приёмки требует менять то, что её же «не трогать» замораживает, дефектна, а исполнитель это
  противоречие разрешить не может и пойдёт по догадке (он ровно так и поступил). «Работа производит
  средство своей проверки» замкнулось в тот же шаг.

- **`bugs/59` + EXP-0052 — побочная находка ВЕРИФИКАЦИИ, а не аудита.** `npm run test:core` дал
  «1 of 14 suites FAILED», чистый перепрогон — зелёный, дерево не менялось. Красный не замолчан:
  греп показал, что все 13 сводов работают в каталогах с ФИКСИРОВАННЫМИ именами в общем OS-temp, и
  прямая проба подтвердила — два одновременных прогона ОДНОГО свода дают `A_EXIT=1` при `B_EXIT=0`,
  упавший выдаёт сырой стектрейс `cpSync`/`unlink` (`EPIPE … used by another process`). Транзиент
  оказался детерминированным классом; чинится ФОРМОЙ (`mkdtemp`), TWINS перечисляет все 13
  вхождений. Отдельно оплачен урок: верификационный прогон нельзя пускать через `| tail` — хвост
  показывает ИТОГ, а нужна ПРИЧИНА, и имя упавшего свода отрезается ровно там.

- **Судья — VERIFIED WITH CAVEATS, 0 фродов.** Каждое несущее заявление перепроверено заново:
  «расширен, не пересоздан» доказано грепом восьми защищённых строк побайтно (все «удалённые»
  строки диффа оказались переформатированием); 74 → 100 подтверждено через `git show`; «триггеры не
  менялись» — 0 изменённых `skill-triggers.json`; числа аудита пересчитаны из сырого результата;
  цитаты перепроверены скриптом; тестовых файлов в T3-коммитах не тронуто ни одного. **Обе оговорки
  — против собственной работы агента:** маркер освежения протух на 77 минут (часовой триггер
  сработал и был пропущен — снято освежением с цитатой-приёмкой), и коммит `0639007` не несёт
  строки recall'а `EXPERIENCE` (история append-only, поправка — новая запись).

- **Состояние 2.2 после сессии:** все девять задач T1–T10 закрыты, **гейт Фазы R стал ЗЕЛЁНЫМ**.
  В беклог легли пять новых открытых дефектов (`bugs/55`–`59`) — осознанное следствие ревизии:
  канон прямо запрещает чинить находки тем же проходом, что и находить. Сборка — 161 блок бандла,
  689 модулей; полигон 14 сводов зелёный.

### Сессия 2026-08-08 (двадцать четвёртая, ЗАКРЫТА) — **T5, T8, T6 и T4 ЗАКРЫТЫ** 🟢

- **ЯДРО ГОЛОСА ВЛАДЕЛЬЦА ЗАВЕДЕНО ВНЕ ЭТОГО РЕПОЗИТОРИЯ** — прямое следствие T4 и решение
  владельца в чате. Приватный репозиторий **`MikalaiKryvusha/krinik-stylometry`** (локально
  `d:\work\krinik_voice`): семь корпусов текстов владельца, `METHODOLOGY.md` и портрет
  `AUTHOR_STYLOMETRY.md`. **Портрет ОДИН НА ВЛАДЕЛЬЦА, а не по копии на проект** — проекты
  ССЫЛАЮТСЯ, копий не держат; причина в поле: два развёртывания дали два файла с разными именами.
  Новый корпус — проза владельца со стихи.ру (**67 текстов / 44 976 слов**, выкачано скриптом),
  первый корпус СВОБОДНОЙ речи: всё прежнее было жанром правил либо юношескими книгами.
  Три круга разбора (7 аналитиков по осям → сверщик → критик полноты → три починки → состязательная
  проверка) дали 152 правила и 574 цитаты с адресами. **Статус портрета — ЧЕРНОВЫЕ НАБЛЮДЕНИЯ,
  6 порогов из 7:** регистр СЦЕНА не добран, и добрать его агентом НЕЛЬЗЯ — машинного повествования
  в корпусах нет (доказано разметкой 54 и 51 блока `[ИИ]`), «ПОСЛЕ» может написать только владелец.
  ⚠️ **Публичный KAIF об этом репозитории ничего не знает и знать не должен** — личное творчество
  владельца в открытую поставку не едет; связь только через канон-имя файла.
  Главная находка для самого KAIF: старший портрет описывал ДВА объекта под одним именем — ГОЛОС
  (колон 4–6 слов, инвариант: проза 4,59 · периоды 5,16 · свод правил 5,51) и ЖАНР (безличность,
  правовой словарь). Одна метка «врождённого» разоблачена: правовая модальность — **0 вхождений на
  42 037 слов** свободной прозы. Уроки — **EXP-0049** (дословность собирается КОДОМ, не
  перепечаткой: три круга подряд ловили молчаливую потерю «ё» и глифов) и **EXP-0050** (упавший
  верификатор — это «проверки не было»; перезапущенный нашёл три новых дефекта).

- **T4 закрыта 2026-08-08 12:55 +03:00** (`plans/34`). Портрет голоса получил канон-имя
  **`AUTHOR_STYLOMETRY.md`** (решение №39) на семи поверхностях: скелет · навык обоих слоёв ·
  `AGENT_GUIDE` обоих слоёв (роутер, чек-лист, разведартефакты) · `KAIF_REFERENCE` · обе половины
  README. **Корень класса оказался не в отсутствии имени, а в СЛИЯНИИ скелета и портрета:** навык
  говорил «fill it» про сам шаблон в `.kaif/`, отдельного файла не возникало — отсюда два полевых
  развёртывания с двумя разными именами (`nikolai_stylometry.md` и `OWNER_VOICE.md`, один владелец).
  Теперь сказано «скопируй бланк и заполняй копию». Имя дано как ДЕФОЛТ развёртывания, не как приказ:
  граница эпика C 2.1 («KAIF не решает, где лежит портрет») в силе, переименование живого
  владельческого файла едет ТОЛЬКО заданием `TEMPLATE_NOTES['2.2']`. Скелет получил два модуля —
  вынесенный ПОБАЙТНО реестр корпусов (у обоих полевых портретов он уехал в чужой документ и осиротел)
  и append-only журнал истории на четыре колонки; модульность нормирована СУЩЕСТВУЮЩИМ каноном
  сигнатурных якорей (решение №16), второй системы адресации не заведено. Карта модулей 680 → **682**.
  Стражи: `check-framework` +7 пар и негативный греп безымянной формулы, `s01` +5 ассертов, `s04` +1.
  **Мутация вскрыла ложный зелёный:** ассерт ноты 2.2 проходил по строке `AUTHOR_STYLOMETRY.md`,
  которая приезжает в задание ещё и диффами модулей `AGENT_GUIDE` — примета сужена до фразы,
  уникальной для самой ноты, и красный доказан заново.

- **T6 закрыта 2026-08-08 10:05 +03:00** (`plans/26` §5). «Сырому доверия нет» связывает
  ПРОИЗВОДИТЕЛЯ, а не только проверяющего: сделать что-то — значит сделать и то, чем это
  проверяется, причём В ТОМ ЖЕ шаге («проверка, отложенная на следующий шаг, — проверка, которой
  не будет»). Записано одной секцией и двумя полями шаблонов — риск постановки был «раздуть канон
  прозой». **Второго канона рядом не заведено:** секция названа применением ПРИНЦИПА 3 к
  производству вместо инспекции и явно НЕ повторяет правило стража, отсылая к `BUG_FIXING`; в
  самом `BUG_FIXING` — одна встречная строка («страж — то, что дефект оставляет после себя;
  test suite — то, что работа приносит с собой»). Точки решения: поле **Верификация** в
  `/plan-task` требует назвать артефакты и их ПУТИ, `/plan-epic` планирует фазу тестирования ПО
  написанным артефактам. Карта модулей 679 → **680**; расхождение четырёх зеркал прозы поймал
  `counters-guard`.

- **T5 закрыта 2026-08-08 08:40 +03:00** (`plans/26` §4, решение №53 «малой кровью»). Дефолт был
  origin и ДО задачи — закрыл её не факт кода, а **страж**: свод `s01` получил пять ассертов
  (маркер установки без единого флага = `tracking origin` + адрес origin · `--mode anonymous` →
  anonymous без поля origin · `update` на анонимном отказывается с названной причиной). Стережём
  НАБЛЮДАЕМЫЙ маркер, а не строку помощи: помощь — проза, маркер — поведение. Красный доказан
  мутацией дефолта на `'anonymous'` — оба ассерта S1 красные, пара S2 зелёная. README обеих
  половин §2.3 и `KAIF_REFERENCE` §11.3/§12.1 называют дефолт; `policyChanges` НЕ тегируется —
  поведение не изменилось, тег объявил бы полю несуществующую смену режима.
- **T8 закрыта 2026-08-08 09:35 +03:00** (`plans/26` §7, решение №49 — КАНОН соглашений KAIF).
  Метка момента несёт И дату, И время, И смещение зоны (часы владельца): проза —
  `ГГГГ-ММ-ДД ЧЧ:ММ ±ЧЧ:ММ`, квитанции машинерии — тот же момент локальным ISO 8601
  (`localStamp()` вместо трёх `toISOString().slice(0,10)` в расписке, `history` и `verifiedAt`).
  **Скоуп задаёт САМА МЕТКА** (прецедент оси имён T2): примета — глагол принятия/фиксации/
  закрытия вплотную к дате, порог — СОБСТВЕННАЯ дата метки; поэтому канон действует вперёд по
  построению, и базовой линии, которую надо вести, не заводится, а 46 исторических меток молчат.
  Красный доказан дважды: селфтест `doc-header-lint` 14 → **16** (фикстура держит все состояния,
  включая три МОЛЧАНИЯ — история, существительное «Закрытие», сводка «закрыты») и живой корень
  0 → мутация настоящей метки → 1 → откат → 0. Конвенция закрытия `DONE (date)` → `(date + time)`
  доехала до точек решения обоих слоёв **и девяти языковых пакетов** — ресинка потребовал страж
  протухания (`bugs/44`) на первой же сборке. Заголовки `EXPERIENCE` в охват не вошли осознанно:
  дата в ключе записи — идентификатор, а не момент решения (граница названа в самом каноне).

### Сессия 2026-08-08 (двадцать третья) — **T2 ЗАКРЫТА + КОНТУР ПЕРЕДЕЛАН ПОД ВЛАДЕЛЬЦА** (bugs/51–53) 🟢

- **T2 (`plans/26` §3, все 3 шага) закрыта 06:11 +03:00.** Конвенция, полгода жившая ДЕ-ФАКТО,
  записана в канон обоих слоёв и **взята под стража**: эпик = `NN_EPIC_<имя>.md` (пофазовая
  архитектура, никакой операционки), ребёнок = `NN_epicMM_<фаза>_<имя>.md` (`MM` — номер родителя),
  самостоятельный план = `NN_<имя>.md`; явная строка «конвенция действует вперёд — старые планы не
  переименовываются». Поверхности: README `plans/` обоих слоёв · `/plan-epic` и `/plan-task` обоих
  слоёв · **9 языковых пакетов**. Последнее вскрыл страж протухания пакетов (`bugs/44`) на первой
  же сборке: локализованный `plans/README.md` едет в девяти пакетах, и без стража конвенция
  доехала бы до поля по-английски в девяти локализациях — молча. **Правило-как-проза стало
  стражем:** ось ИМЁН вшита в `tools/doc-header-lint.mjs` (у каждого `NN_epicMM_*` родитель `MM_`
  существует И назван `MM_EPIC_*`); скоуп задаёт САМ ПАТТЕРН — старые планы его не используют и
  молчат, порог по номеру был бы константой без работы. Красный доказан мутацией: селфтест
  12 → **14** находок, свод **s13** проверяет три состояния на живом корне, включая обратное
  (переименовал родителя — страж замолчал). Живой прогон по `plans/`: **25 детей, 6 эпиков,
  0 нарушений**. Заодно синхронизирована таблица §1 зонтика — строки T1/T7/T9 стояли «открыта»
  после закрытия, и отмечены чекбоксы закрытых секций (пинок навигирует по последнему отмеченному
  боксу — неотмеченный бокс в закрытой секции это ловушка, а не украшение).
  Гейты: «all 14 suites green» · «counters OK — 17 зеркал» · «doc-header-lint 97/0» ·
  «requirements-lint 141/0».
- **Критерий 9 задачи T1 СНЯТ ПОЛНОСТЬЮ (06:00 +03:00) — чистый прогон пинка.** Владелец открыл
  НОВЫЙ чат и запустил сессию **голым `/kaif-go`**, не `/resume`: точка продолжения названа одной
  строкой, реплики «продолжать ли» не было. **Находка прогона:** холодный контекст закрыла не
  оговорка навыка «пустой контекст → `/resume`», а **ветка триггера Шага 2** — хук впрыснул
  «last refresh 350 min ago», и агент освежил девятку ядра до работы. То есть в системе С ХУКАМИ
  пинок в новом чате самодостаточен по построению, а оговорка про `/resume` страхует развёртывания
  БЕЗ хуков; текст навыка этого пока не называет (кандидат в правку формулировки, не дефект —
  обе ветки дали верное поведение).
- **ТРИ ДЕФЕКТА КОНТУРА, все найдены ВЛАДЕЛЬЦЕМ на живой странице (06:40–07:40).** Общий корень
  один: приёмка проверяла ПОВЕДЕНИЕ и ни разу — то, что ЧИТАЕТ человек.
  **`bugs/51`** — варианты, записанные таблицей, не становились радиокнопками; страж против этого
  класса (G11) существовал и зеленел, потому что его примета знала ту же единственную форму, что и
  парсер: сверка давала `0 == 0`. Вторая итерация того же бага: кнопки появились, но потеряли
  БУКВЫ — рекомендация «вариант C» не ложилась ни на одну.
  **`bugs/52`** — очередь рендерилась одной простынёй, поэтому запись по ОДНОМУ документу
  завершала контур и уносила окно с ещё не отвеченными; вдобавок черновики чистились по ключу всей
  пачки. **Форму задал владелец:** входная страница — только карточки, документ открывается своим
  окном; контур завершает закрытие ВХОДНОЙ страницы.
  **`bugs/53` (тяжелейший)** — ответ владельца записывался в документ и НЕ РАСПОЗНАВАЛСЯ разбором:
  метка времени канона T8 несёт вложенные скобки `(+03:00)`, а регэксп метки использовал `[^)]*`.
  Работа человека существовала на диске и не существовала для системы; дефект ВЫКЛЮЧАЛ целого
  стража (`questions-guard` молчал). Найден не гейтом, а живым круговым прогоном «записал →
  прочитал обратно», который я писал для `bugs/52`.
  Плюс полевые правки интерфейса по словам владельца: убраны пояснения агента о собственном
  устройстве и коды инвариантов (`(D)`, `(P7)`, `(I11)`) из подписей полей; контраст чипов
  **замерен** (белым было 2.15–2.54 при норме WCAG 4.5 → стало 7.4–11.3); рекомендация помечена
  чипом на САМОМ варианте. Приёмка контура **95/3 → 119 зелёных, 0 красных**; новые блоки QA8
  (форма очереди на живом сервере) и QA9 (контраст считается, интерфейс не говорит кодами).
- **Интервью №012 и №013 отвечены и разнесены** (решения **№54–№58**): трейлер коммитов несёт
  ФАКТИЧЕСКОЕ имя модели — зашитой константы в `tools/commit.mjs` больше нет, имя передаёт сессия
  (`--as` / `KAIF_AGENT_MODEL`), без имени трейлер не пишется; ретроспектива не трогается;
  восемь языковых пакетов ЗАМОРАЖИВАЮТСЯ в 2.3 с явным объявлением (ведём только `ru`/`en`);
  2.3 — обычный релиз, сокращение поверхности пунктом внутри него.


### Сессия 2026-08-08 (двадцать вторая) — **ЗАДАЧА T1 ЗАКРЫТА**: у владельца появился пинок 🟢

- **T1 (`plans/62`, все 8 шагов) закрыта 00:40 +03:00.** Навык **`kaif-go`** (короткий алиас
  `/go`) — имя по решению №36, подтверждено владельцем в чате 00:18 +03:00 на живой развилке
  «`kaif-go` vs `go`» (эпик P отложен в 2.3, но имя из №36 в силе — переименовывать не придётся).
  Пинок возобновляет УЖЕ НАЧАТУЮ работу без реплики «продолжать ли»: точка продолжения берётся
  из парковочной заметки → активного плана → STATUS; канон перечитывается ТОЛЬКО по триггеру
  (пинок звучит много раз за сессию и не должен жечь контекст, который бережёт). Отдельная
  **секция границы** в обоих слоях закрывает топ-1 риск постановки: пинок НИКОГДА не бланковое
  «да» на развилки видения, write-gate, `AUTH:` и разрушительные действия. Поставка полная —
  9 языковых пакетов, строка в Таблице 3 обеих половин README, `KAIF_REFERENCE`, `CLAUDE.md`;
  развёрнуто и наблюдено в чистой песочнице («35 skills trigger-aliased», файл на диске).
  **Главная находка задачи — вскрытая ось счётчика:** 35-й навык обнажил восемь рукописных «34»
  (alt-тексты обеих половин · SVG-генератор · пропись в 4 местах · по строке на навык в Таблице 3
  КАЖДОЙ половины), которых не стерёг НИКТО, плюс строку «актуальная сборка» в STATUS, уже
  стоявшую протухшей (671 модуль при 672). `counters-guard` вырос **5 → 17 зеркал**, `--selftest`
  доказывает красный мутацией на семи. Две команды реестра пар с зашитыми 34/68 удалены.
  Ожидание свода G10 переведено с зашитого «34» на живое число из бандла (правка теста —
  с обоснованием в коммите; проверяемый разрыв в 2 фантомных ключа сохранён).
  **Побочно заведён и закрыт `bugs/50`:** идемпотентность трейлера `Co-Authored-By` проверялась
  по ОДНОМУ литералу, поэтому коммит `89a9058` ушёл с двумя со-авторами; починено классом,
  доказано контрастной парой (1 против 2). Гейты: «all 14 suites green» · «counters OK — 17
  зеркал» · «doc-header-lint 96/0» · «requirements-lint 140/0».
  **Критерий 9 снят частично (01:05 +03:00):** владелец пнул живьём, навык загрузился и повёл
  ответ. Первая попытка дала «нет такой команды» — оказалось, клиенты читают список команд ОДИН
  РАЗ на старте, и перезапуск VS Code её проявил. Дефекта в навыке не было, но класс полевой:
  KAIF разворачивает 35 навыков в чужой проект, и «нет такой команды» — ПЕРВОЕ, что человек там
  увидит. Лечение уехало в поставку — строка в финальном выводе установщика («Restart your agent
  client so the N skills appear in its command list…», наблюдена на живой установке) + `EXP-0045`.
  Чистого прогона «голый пинок → молчаливое продолжение» пока нет — **он состоялся в двадцать
  третьей сессии, критерий 9 снят полностью** (см. запись выше).


### Сессия 2026-08-07 (двадцать первая) — **ЗАДАЧА T10 ЗАКРЫТА**: у контура появился класс «сообщение» 🟢

- **T10 (`plans/61`, все 6 шагов + верификация) закрыта 23:30 +03:00.** Контур научился не только
  спрашивать, но и **сообщать**: вызов `node tools/review.mjs <док> --notice` (плюс
  `--enqueue <док> --notice` — только в очередь). Развилки спрашивать было нечего — все три
  ответы владельца лежали в интервью №009, и реализация стартовала с них. Что получилось:
  страница-сообщение несёт тело документа, необязательный комментарий и ЯВНУЮ пометку
  **«ОК, прочитано»**, и НИ ОДНОЙ радиокнопки (проверено: `type="radio"` — 0 штук на живой
  странице); пометка — **штатный исход, код 0**, а не «закрыто без ответа»; без пометки сообщение
  честно объявляется НЕ доставленным («повторю в следующей пачке» — долг агента, I38);
  непрочитанные копятся отдельной группой **строго ПОД вопросами** (доказано индексом в HTML:
  4654 → 5875, а не впечатлением). Всё наблюдено на живом сервере: POST → `readAt` в очереди,
  `kind: "notice"` в записи решения, `EXIT_CODE=0`. **Побочная находка, ставшая правкой ядра:**
  штатная пометка без комментария переписывала документ владельца впустую и срезала бы ему BOM —
  теперь `recordDecision` не трогает файл, когда писать нечего (страж — селфтест ядра).
  Контракт расширен в ОБОИХ слоях: **I37/I38** (номера новые, происхождение названо честно —
  «не после инцидента, а по просьбе владельца»), ростер s12 36 → **38 позиций**. Три стража
  доказаны красным мутацией. Гейты: «all 14 suites green» · «bundle 159 blocks» · «counters OK —
  5 зеркал» · «doc-header-lint 96/0» · «Ждут ответа: 0».

### Сессия 2026-08-07 (двадцатая) — **ЭПИК O ЗАКРЫТ, ПЕРЕОТКРЫТ И ЗАКРЫТ СНОВА**: впрыск наблюдён + фаза O5 (хуки под 10 систем) 🟢

- **Фаза O5 (`plans/60`, все 6 шагов) — внесена владельцем и закрыта тем же днём.** Разведка
  **живыми источниками по 10 системам** (`researches/19` §1.3–§1.4): контракты Cursor, Codex,
  Copilot, Windsurf, Cline, Grok Build, Google Antigravity сняты в вендорских доках 2026-08-07.
  Оба поручения владельца закрыты вердиктами: **Grok Build** — гипотеза паритета ПОДТВЕРЖДЕНА
  (дока прямо говорит, что `.claude/settings.json` читается наряду со своими файлами), образца не
  требует; **у Meta система есть** — Muse Code, бета 2026-08-05, хуки заявлены, но контракт не
  опубликован → образец не пишется, строка «ждём доку». **Главный технический вывод: «есть хуки»
  — непортируемое утверждение.** Переносимость решает не наличие событий, а наличие поля впрыска
  АГЕНТУ: у Cursor его нет на промпте, у Copilot — не на всех событиях, у Windsurf и Cline нет
  вовсе (там честное «не поддержано»), у Codex — те же имена полей, что у Claude Code. Поэтому
  скрипты получили флаг формы **`--emit`** (claude/cursor/copilot/antigravity), а не
  автоопределение: хук обязан молча выходить при неясности, значит неверная догадка отказала бы
  НЕВИДИМО. Четыре образца конфигов, каждый честен про то, чего НЕ несёт. Свод s14 +21 ассерт,
  включая обратную проверку «образец не обещает хук, который система не тянет»; **красный доказан
  мутацией**. Адаптеры ресинкнуты по хукам (6 секций с датой сверки) + **новый
  `google-antigravity.md`**. Улов сверх задачи — две перемены ландшафта: **Gemini CLI закрыт
  2026-06-18** (переезд на Antigravity CLI), доки Windsurf переехали под Devin. Гейты: «all 14
  suites green» · «bundle 159 blocks» · «counters OK — 5 зеркал».

- **Фаза O4 (`plans/58`, шаг 2 — последний) закрыта 21:47 +03:00.** Владелец выполнил `/clear` —
  и сессия открылась уже с приказом в контексте; это ровно то событие, которого ждал эпик.
  Цитата дословно: «KAIF context refresh (SessionStart:clear). The context was just cleared: what
  this session now remembers of the canon is a retelling, not the canon. BEFORE task work: …».
  Приказ опознан ПО ИСТОЧНИКУ, а не по впечатлению: текст собран веткой `source === 'clear'`
  (`framework/hooks/session-start-refresh.mjs:34-41`) — «just cleared» и `trigger:
  "ritual:/clear"`, а не `compaction`, то есть матчер `compact|clear` не просто сработал, но и
  **различил событие**. Тем же первым промптом пришёл третий независимый впрыск таймера («last
  refresh 66 min ago») — два хука на одном промпте не помешали друг другу. **Итог эпика: все три
  хука поставки наблюдены живьём, каждый своим событием, ни один не выведен из соседнего** —
  собственная охота «Refresh witness» соблюдена. Каверза судьи (критерий 6 — «наполовину») снята
  ровно тем наблюдением, которое было в ней названо; **вердикт эпика поднят до VERIFIED 7/7** с
  явной оговоркой, что повторного судейского прохода это не заменяет (полный адверсарный суд
  всего 2.2 — Фаза R, решение №48). Закрытия: `plans/58` · `plans/31` (+ решения эпика без
  владельца) · `plans/26` §1 · `MASTER_PLAN.md` §8. Порядок остатка 2.2 сократился до **Фазы R**.

### Сессия 2026-08-07 (девятнадцатая) — **ЭПИК O, ФАЗА O4**: хуки подключены живьём, страж наблюдён, судья эпика 🟡

- **Фаза O4 (`plans/58`, шаги 1/3/4/5 из 5):** хук-модуль **подключён в этом репозитории** —
  носитель `.claude/settings.json` (проектный, коммитится; слово владельца дословно: «это
  механизм проекта! Поэтому он подключается в settings.json для Claude, и в аналогичные хуки
  для другиех вгентских систем»). Скрипты вызываются **прямо из `framework/hooks/`**, копия в
  `.kaif/hooks/` не заводится — прецедент истока проверен исполнением, а не формулировкой
  (`tools/doc-header-lint.mjs:27`: «собственного .kaif/tools/ исток не держит»); образец
  поставки не тронут (`.kaif/hooks/` ×4 в фрагменте). **Страж STATUS НАБЛЮДЁН ЖИВЬЁМ**: при
  состаренном `STATUS.md` и грязном дереве в контекст пришёл дословный блок «KAIF STATUS guard
  (fires once per session)… — then finish», повтор той же сессии молчит (cooldown). Побочный
  результат важнее критерия: **живой Claude Code читает `.claude/settings.json` БЕЗ перезапуска
  сессии** — риск 1 плана не реализовался, проводка доказана end-to-end. Досье окружения
  переснято пробами — **13/13 строк, дрейф 0** (воспроизвёлся и улов O1: `curl` в PS 5.1 —
  `Alias` с пустым `.Source`); правок не потребовалось. Строка «Контур свежего контекста» во
  внутренней карте переписана с «долг с адресом» на **четыре закрытых плеча**. Карта контракта
  `researches/19` §1.1 дополнена формой `args` (третий живой fetch: `args` присутствует →
  exec-форма) — карта была неполна, рабочий фрагмент верен. **Судья эпика O (лёгкий, №48):
  VERIFIED WITH CAVEATS** — 6 критериев из 7 переисполнены независимыми командами полностью,
  критерий 6 — наполовину; одна находка проверена и отклонена (у Stop-стража нет `OUTPUT_CAP`:
  он печатает `decision/reason`, к 10k не приближается по построению). Гейты: «all 14 suites
  green» · «doc-header-lint: scanned 91, findings 0» · «requirements lint OK — 136 file(s), 0
  findings» · «counters OK — 5 зеркал сверены» · «Ждут ответа: 0 · Новых нарушений: 0».
  Открытым оставалось ровно одно — впрыск после `/clear`: живое наблюдение, рождаемое человеком;
  вывести его из сработавшего Stop-хука = фрод класса ложного `[TESTED]` (охота «Refresh
  witness», вшитая этим же эпиком). **Закрыто сессией двадцатой** (запись выше). Поручение
  владельца «аналогичные хуки для других агентских систем» → **`ideas/22`** (вопрос скоупа: 2.2
  фазой O5 или 2.3 — за владельцем).

### Сессия 2026-08-07 (восемнадцатая) — **ЭПИК O, ФАЗА O3 ЗАКРЫТА**: hook-модуль в поставке + досье окружения 🟢

- **Фаза O3 (`plans/57`, все 5 шагов):** опциональный **модуль refresh-hooks**
  `framework/hooks/` → `.kaif/hooks/` — три скрипта по ЖИВОМУ контракту Claude Code
  (`session-start-refresh` matcher `compact|clear` · `prompt-refresh-timer` предикат «возраст
  маркера > 60 мин», молчит при свежем · `stop-status-guard` cooldown раз/сессию, единственный
  блокирующий) + `settings-fragment.json` + README опт-ина; впрыск — ПРИКАЗ перечитать под капом
  10k, не тела документов; чужие `settings.json` машинерия не трогает. **Контракт уточнён живым
  fetch:** `additionalContext` ВЛОЖЕН в `hookSpecificOutput` с `hookEventName` — карта
  `researches/19` §1.1 исправлена (проектная карта O1 описывала верхнеуровневое поле).
  Регистрация полным кругом (сборка · валидатор · Reference §14 · AGENT_GUIDE обоих слоёв ·
  обе карты проекта) + свод **s14** — красный доказан против HEAD-бандла («ПРОВАЛОВ: 19»),
  зелёный после пересборки. **Досье окружения:** секция в payload AGENT_GUIDE (таблица «факт →
  значение → проба», дата снятия · команда перегенерации · staleness 4 недели · 6 осей ·
  DRY-граница с гигиеной текста), ЖИВОЕ досье истока в RU-гиде (пробы пересняты наблюдением;
  дельта против O1 — **chocolatey установлен**), процедура — шагом 3 в `/refresh-context` обоих
  слоёв (Оккам: отдельный навык не заводится, скрипт-сборщик тоже — пробы шелло-зависимы по
  своей природе). AGENTS.md-форма признана действующей по построению (указатель на AGENT_GUIDE
  пишется всегда; досье — его секция), новый файл не заводится. **Попутная находка —
  `bugs/49`** (born DONE): счётчики обоих README протухли (662 модуля при 666, 13 сводов при 14),
  потому что страж реестра стерёг 1 зеркало из 5 → новый `tools/counters-guard.mjs` сверяет все
  пять с живыми числами, красный доказан мутацией (`--selftest`). Цитаты гейтов: «56 embedded
  files (14 docs + 7 readmes + 34 skills + 1 tools) · **bundle 155 blocks** · **671 modules**» ·
  «sandbox suite: **all 14 suites green**» · «doc-header-lint: scanned 90, findings 0» ·
  «requirements lint OK — 135 file(s), 6 word classes, 0 findings» · «Ждут ответа: 0 · Новых
  нарушений: 0» · «counters OK — 5 зеркал сверены». **Два судьи параллельно: VERIFIED WITH
  CAVEATS + «блокеров нет»** — 10 находок, все закрыты тем же заходом, из них тяжёлые: ложный
  `[TESTED]` в файле поставки (текст маркера лгал о поведении при битом маркере); гард 5d был
  СЛЕП к новой payload-поверхности (доказано мутацией → расширен на hooks/spheres/adapters,
  `tools/*.mjs` исключён явно — там кириллица и есть payload); модуль отсутствовал в release
  notes 2.2 (добавлены две записи, включая «DECISION FOR THE OWNER: активация ваша»); секция
  досье была `static` вместо `adaptive` (иначе `diff` вечно рапортовал бы чужую правку). Одна
  находка опровергнута переисполнением («висячая §9.10» = §9 пункт 10, класс EXP-0032).
  Уроки — EXP-0036 (инвариант проверяется прецедентом, карта старше дня — гипотеза) и
  EXP-0037 (страж пишется под класс зеркал).
  План O4 по канону N+1 → **`plans/58`** (живое подключение хуков + наблюдение впрыска + судья
  эпика; брать свежей сессией).

### Сессия 2026-08-07 (семнадцатая) — **ЭПИК O, ФАЗА O2 ЗАКРЫТА**: канон и ритуалы освежения 🟢

- **Фаза O2 (`plans/56`, все 5 шагов, закрыта 19:45 +03:00):** канон-секция **«Context refresh /
  Освежение контекста»** в AGENT_GUIDE обоих слоёв — 4 триггера (час · тяжёлая задача ·
  сжатие/пауза · точки ритуалов) + двухчастное свидетельство (маркер `.kaif/refresh-marker.json`
  + цитата-приёмка в чате) дословно по проекту `researches/19` §2; ядро перечитывания — ссылкой
  на таксономию N4 (второго списка девятки нет — греп по секции 0); указатели из чек-листов
  обоих гидов. Маркер канонизирован: ignore-first машинерии (`ensureIgnoreFirst`, аналог
  heartbeat; [TESTED] обновлён) + `.gitignore` репо (`git check-ignore` подтвердил). Охота
  судьи: канон-строка «фрод класса ложного [TESTED]» + пункт **«Refresh witness (KAIF 2.2)»**
  помеченным патчем в вендоренный `fable-judge` (шапка патча (4) → «2.1–2.2»; обе копии
  побайтово равны — реестр пар). Вплетение в **7 навыков ритуалов на слой** (грепом:
  refresh-context · resume · pause · autoloop · dayloop · nightloop · guarded-loop; DRY —
  ссылкой на секцию). **Первый живой маркер отштампован этой же сессией** (trigger
  `ritual:/resume`, цитата-приёмка в чате — догфудинг формата до судьи). Сборка: «56 embedded
  files (14 docs + 7 readmes + 34 skills + 1 tools) · bundle 150 blocks · **666 modules**»
  (цитата; 665→666 — новая канон-секция; счётчики RU-гида и команда реестра пар перепинены тем
  же заходом); полигон «sandbox suite: all 13 suites green»; «doc-header-lint: scanned 89,
  findings 0» · «requirements lint OK — 133 file(s), 6 word classes, 0 findings» (счёт с новым
  `plans/57`); страж вопросов «Ждут ответа: 0 · Новых нарушений: 0». Судья фазы (лёгкий, №48): вердикт — в
  закрытии `plans/56`. План O3 по канону N+1 → **`plans/57`** (hook-модуль + досье окружения;
  брать свежей сессией).

### Сессия 2026-08-07 (шестнадцатая) — **ЭПИК O ОТКРЫТ, ФАЗА O1 ЗАКРЫТА**: карта хук-механизмов + проекты контуров + смок досье 🟢

- **Фаза O1 (`plans/39`, все 5 шагов, закрыта 2026-08-07):** артефакт — **`researches/19`**:
  (1) карта механизмов — контракт Claude Code hooks снят живым fetch 2026-08-07 (события/
  матчеры/кап 10k/exit-коды; судья переисполнил fetch — точность «до нюанса», включая timeout
  30 с UserPromptSubmit); дельта пейзажа: хуки появились у Cursor/Codex/Copilot/Windsurf/Cline
  ПОСЛЕ сверок адаптеров, у приоритетной Zoo Code хуков НЕТ (честный фолбэк — markdown-ритуал);
  (2) проект markdown-контура: 4 триггера + двухчастное свидетельство (маркер
  `.kaif/refresh-marker.json` + цитата-приёмка; DriftBench recall ≠ adherence закрыт);
  (3) проект hook-контура: 3 хука минимума с предикатами/cooldown, поставка `.kaif/hooks/`,
  чужие settings не трогаем молча; (4) досье окружения: процедура 6 осей + смок НА ЭТОЙ машине
  (живьём пойманы tar/find-раздвоение по шеллам, ACP-1251, пустая локаль git-bash — черновик
  досье истока таблицей в §4). **Судья лёгкий (№48): VERIFIED WITH CAVEATS** — смок
  переисполнен 14/15 (находка: `curl` в PS 5.1 — alias на `Invoke-WebRequest`, проба `.Source`
  слепа к алиасам; вторая — grok-build выпал из карты) — обе закрыты правкой артефакта тем же
  заходом. Закрытия: `plans/39` (чекбоксы+решения), `plans/31` (O1 ✅). План O2 по канону N+1 →
  **`plans/56`** (канон и ритуалы освежения; брать свежей сессией). Линтер: «doc-header-lint:
  scanned 88, findings 0» · «requirements lint OK — 132 file(s), 6 word classes, 0 findings».

### Сессия 2026-08-07 (пятнадцатая) — **ЭПИК N ЗАКРЫТ ЦЕЛИКОМ**: N5 — линтер требований целевых документов + судья эпика 🟢

- **Фаза N5 (`plans/55`, все 5 шагов, закрыта 18:55 +03:00) — последняя фаза эпика:** линтер
  целевых документов — расширение `tools/doc-header-lint.mjs` (блок «Вектор цели/Критерии
  приёмки» в нормо-эпохных plans ≥ 26 / ideas ≥ 21 — норма N2 не ретроактивна, пороги из
  обмера · bugs-диалект `Fix accepted when` · композиция: после структурных проверок линтер
  гоняет стоп-словарь payload-модуля по секциям требований), «selftest: ok — 12 предсказанных
  находок» (красный ДО применения). Первый живой прогон словаря: **13 находок → 0 тремя
  родами** (EXP-0035): фикс класса многострочных «…»-цитат в payload-модуле (**bugs/48** born
  DONE; красный s13-ассерт доказан на дофиксовом модуле) · `(оправдано:)`-аннотации
  имён-терминов · переформулировка в живом plans/34; закрытые планы не переформулированы.
  Ретрофиты нормы: ideas/21 (вектор) + bugs/02/45 (`Fix accepted when`). Финал: «doc-header-lint:
  scanned 86, findings 0 — all headers green» + «requirements lint OK — 130 file(s), 6 word
  classes, 0 findings»; полигон «sandbox suite: all 13 suites green» (s13 — обе половины);
  реестр пар зелёный; страж вопросов «Ждут ответа: 0 · Новых нарушений: 0». **Судья эпика N
  (лёгкий, №48): VERIFIED — 7/7 критериев переисполнены независимыми командами** («plans>=26
  live: 30/30 · ideas>=21: 1/1» · «contour rows: 9 · rows without fate: 0» · шаблоны 8/8),
  4 охоты пусты; единственная находка — ложная тревога его же кейс-грепа (класс EXP-0032),
  опровергнута переисполнением. Таблица улик — `plans/30`; закрытия — `plans/30`/`plans/26`/
  MASTER_PLAN §8. Следующий — **эпик O**.

### Сессия 2026-08-07 (четырнадцатая) — **ЭПИК N, ФАЗА N4 ЗАКРЫТА**: таксономия документов + термин «контур» 🟢

- **Фаза N4 (`plans/54`, все 4 шага, закрыта 18:16 +03:00):** канон-секция из трёх подсекций в
  шаблоне `framework/AGENT_GUIDE.md` (носитель — сосед контекст-роутера, KISS): **таксономия
  5 ярусов** владельца (идея 20 п. 18) с явным разведением «ядро перечитывания 9» ≠ «поставка
  14» (чек-листы обоих гидов сведены к канонической девятке — расхождение 6 vs 9 найдено и
  закрыто; ярус — вход эпика O под именем «re-read core») · **норма шапки-меты**
  (Created/Parent/Status/Outbound, метки на рабочем языке проекта) · **термин «контур»**
  (анатомия владельца 4 части, п. 16). RU-зеркало в корневом гиде; регистрация Reference
  §1/§5; норма в 4 EN README-шаблонах + 4 корневых RU + **36/36 локализаций** (9 языков × 4)
  с перепином `_lang-pack-source-shas.json` тем же коммитом (гард 5f молчит). **Инвентарь
  контуров** в `PROJECT_ARCHITECTURE_INTERNAL_MAP.md`: «contour rows: 9 · rows without
  fate: 0» (цитата; 7 чёткие · 2 размыты с адресами — линтер N5, механизм освежения эпик O).
  Сборка: «56 embedded files (14 docs + 7 readmes + 34 skills + 1 tools) · bundle 150 blocks ·
  665 modules» (цитата; 662→665 — три новые канон-секции); полигон «all 13 suites green»;
  «doc-header-lint: scanned 86, findings 0». Судья лёгкий (№48): **VERIFIED** — 7/7
  переисполнено (ядро 9 сверено с идеей 20 побуквенно в пяти точках), 4 охоты пусты (потеря
  прозы 0 в 44 файлах). План N5 по канону N+1 → **`plans/55`** (брать свежей сессией — линтер
  с красным доказательством + судья эпика).

### Сессия 2026-08-07 (тринадцатая) — **ЭПИК N, ФАЗА N3 ЗАКРЫТА**: шапка-мета документов 🟢

- **Фаза N3 (`plans/53`, все 5 шагов, закрыта 17:35 +03:00):** фикс-схема шапки выведена
  ОБМЕРОМ де-факто конвенции (82/86 H1, 79/86 blockquote; частоты меток) — носитель
  blockquote (НЕ YAML), поля H1-«что это» · `Создан` (ISO) · `Родитель` · `Статус` (с
  вехами) · `Вовне` + опц. `Наследники`; диалекты своих шапок сохранены (bugs/ — шаблон
  `/report-bug`; interviews/ — `Topic:`/`Status:`, парсит questions-guard); корневые —
  самоописание первым блоком; исключения явные (первоисточники владельца · reports/ ·
  генераты). НОВЫЙ обвязочный страж **`tools/doc-header-lint.mjs`** (селфтест «8
  предсказанных находок», строка в таблице инструментов AGENT_GUIDE). Массовая правка:
  **65 файлов / 153 находки → 0** за 9 пачек со сканами; попутно вычищены древний CJK в
  `researches/02` и врап-класс в `plans/44`; финал «doc-header-lint: scanned 84, findings
  0 — all headers green»; страж вопросов «Новых нарушений: 0». Payload-решение: норма едет
  фазой N4 вместе с таксономией (один ресинк локализаций вместо двух). Сборка: «56 embedded
  files (14 docs + 7 readmes + 34 skills + 1 tools) · bundle 150 blocks · 662 modules»
  (цитата, генераты не дрейфнули); полигон «all 13 suites green» (цитата); реестр пар 6/6.
  Судья лёгкий (№48): **VERIFIED** — 10/10 переисполнено (независимый счёт схемы 61/61),
  охоты пусты, фактчек актуализированных статусов — выдумки нет. План N4 по канону N+1 →
  **`plans/54`** (брать свежей сессией — канон-письмо payload + ресинк локализаций).
  **Инцидент после пуша:** сообщение коммита `dbe2db1` исковеркано git-bash/MSYS2-конвертацией
  argv (`:`→`;`, `/`→`\\`, вставлен путь git) — файлы целы; класс закрыт по построению —
  `bugs/46`: commit.mjs получил `--msg-file` (`git commit -F`) + страж не-ASCII argv (красный
  доказан), симптом 5 в канон-блоке «текст-через-CLI», урок EXP-0034; переписывание
  опубликованного сообщения — только по слову владельца (предложено в чате).

### Сессия 2026-08-07 (двенадцатая) — **ЭПИК N, ФАЗА N2 ЗАКРЫТА**: вектор цели + критерии приёмки — в шаблонах и навыках 🟢

- **Фаза N2 (`plans/52`, все 5 шагов, закрыта 16:55 +03:00):** блок «Вектор цели + Критерии
  приёмки» ПЕРВЫМ в шаблонах целевых документов, EN+RU: `/plan-task` («Готово, когда» →
  «Acceptance criteria (done when)» + абзац REQUIREMENTS_FRAMEWORK и оговорка модифицируемости),
  `/plan-epic` (мета-план открывается вектором цели эпика; операционные планы наследуют блок),
  `/report-bug` (`Fix accepted when (observable)` в шапке шаблона бага), `/propose-idea` («Goal
  vector — боль + как проверим» перед Essence). README plans/bugs/ideas: payload + корневые
  обвязки + **27/27 локализаций** (9 пакетов × 3) с перепином `_lang-pack-source-shas.json` ТЕМ
  ЖЕ коммитом — страж 5f молчит. Канон-строка AGENT_GUIDE ×2 (EN шаг 8 / RU шаг 7). Кандидаты
  N1: `/release` и сфера `programming` — ссылка НЕ нужна (обоснования в закрытии `plans/52`).
  **Dogfooding:** `plans/26–52` — вектор цели **27/27**, критерии приёмки **27/27**. Сборка:
  «56 embedded files (14 docs + 7 readmes + 34 skills + 1 tools) · bundle 150 blocks · 662
  modules» (цитата); полигон «all 13 suites green». Судья лёгкий (№48): **VERIFIED** (10/10
  заявлений; его единственная находка опровергнута — git-bash `grep -i` не кейс-фолдит
  кириллицу, класс EXP-0026). План N3 по канону N+1 → **`plans/53`** (шапка-мета; брать свежей
  сессией — массовая правка контекстоёмка).


### Сессия 2026-08-07 (одиннадцатая) — **ЭПИК N, ФАЗА N1 ЗАКРЫТА**: REQUIREMENTS_FRAMEWORK.md — 14-й ключевой документ 🟢

- **Фаза N1 (`plans/38`, все 5 шагов, закрыта 16:30 +03:00):** `framework/REQUIREMENTS_FRAMEWORK.md`
  (EN) + корневая RU-копия — 10 критериев владельца (❌/✅, якоря 29148) · NASA Appendix C ·
  стоп-словарь 6 классов · EARS с правилом зеркалирования · fit criterion (Scale/Meter/Target) ·
  Achieve/Maintain/Avoid · RFC 2119/8174 · границы (не DoR-шлюз · не BDUF · граница с TESTING
  двусторонне). **Тройка канонов** (уточнение владельца в чате): REQUIREMENTS формирует →
  TESTING сверяет → BUG_FIXING замыкает; вплетена в 12+ точек обоих слоёв; его же замечаниями
  вычищены мета-этикетки и провенанс-пометки из канон-текстов (урок EXP-0031). Доставка полным
  кругом — сборка: **«56 embedded files (14 docs + 7 readmes + 34 skills + 1 tools) · bundle
  150 blocks · 662 modules»** (KAIF_REFERENCE · resume/help-kaif ×2 · CONTEXT_POINTER ·
  KAIF_FRAMEWORK + 9 пакетов с перепином · README обе половины вкл. §5 цитатой · TEMPLATE_NOTES
  2.2). **Страж:** `kaif-requirements-lint.mjs` (`.kaif/tools/`, RU+EN словарь, check/selftest,
  exit 0/1/3, скоуп секций требований) + свод **s13** + 2 ассерта состава в s01 — красный
  доказан живой фикстурой и HEAD-бандлом (0 блоков до поставки); полигон **«all 13 suites
  green»**. Судья лёгкий (№48): VERIFIED. План N2 по канону N+1 → **`plans/52`**. Попутно:
  наброс владельца → задача **T9** в `plans/26` §8 (вернуть дословное определение «KAIF —
  это…» в README 2.2; канон-строки извлечены из git `1dd1747`, потеряны свапом README 2.1);
  Фаза R перенумерована §8→§9, гейт «T1–T7» → «T1–T9».

### Сессия 2026-08-07 (десятая) — **ЭПИК M ЗАКРЫТ ЦЕЛИКОМ**: M3 (протоколы) + M4 (принципы) + M5 (dogfooding) 🟢

- **Фазы M4+M5 (`plans/51`, один дочерний план, закрыты 15:25 +03:00):** M4 — принцип «The
  principle set is battle-tested, not sacred — propose and prune» в payload PHILOSOPHY (пара к
  «Best practices») + распоряжение «Proposing principles — a standing order» в payload
  AGENT_GUIDE (канал — шаблон B `/report-bug`, судьба предложений — решение владельца KAIF,
  blameless-рамка); RU-зеркала с цитатами владельца идеи 20 п. 14 ДОСЛОВНО; сборка «55 … 148
  blocks … **655 modules**». M5 — эвал-ловушка дедупа в s04/S13 (симулированный fingerprint:
  свой класс НАЙДЕН, чужой — нет; негативный контроль), вход сигналов парка (issues origin +
  `reports/KAIF_UPDATES/`) — строкой в обвязочный `/check-backlog`. Инцидент: рабочая копия
  `plans/26` оказалась ОТКАЧЕНА к до-L5-версии (без T8, K «—») при чистом HEAD — поймано
  ритуалом `git diff --stat` ДО коммита, восстановлено из HEAD, задокументировано в `bugs/45`
  (гипотеза — протухший буфер редактора; инструменты в plans/ не пишут — проверено грепом). **Судья эпика лёгкий (№48): VERIFIED**; таблица
  «критерий приёмки 1–8 → улика» — в закрытии `plans/51`; PENDING приёмки владельца (место
  install-отчётов · ранжирование вектора целей) — в `plans/29`, снимает Фаза R/вычитка.

- **Фаза M3 (`plans/50`, все 5 шагов, закрыта 15:09 +03:00):** протоколы петли стоят В ТОЧКАХ
  РЕШЕНИЯ полевого агента. Машинерия: задания update/install несут ОБЯЗАТЕЛЬНЫЙ пункт
  `field-report` (скелеты шаблонов C/D — секции с якорями, ≤ 30 строк; имя файла пинено на
  версию поставки; текст пунктов НЕ упоминает origin — анонимность по построению); чекпоинт
  `field-report` ИСПОЛНЯЕТ проверку файла в `reports/KAIF_UPDATES/` с версионным пином; сшивка
  №46 — без дубля judge-чекпоинта, отчёт цитирует вердикт. Навыки: `/report-bug` — ветка
  «дефект САМОГО фреймворка» (`bugs/KAIF/`, шаблоны A/B вендорены с якорей `researches/18` §8,
  fingerprint `kaif-fp:`, аттестация дедупа, гейт качества отправителя, режимы 8.1/8.2) +
  RU-обвязка с оговоркой истока; `/kaif-update` — шаг «полевой отчёт ОБЯЗАТЕЛЕН»;
  `/kaif-version` — подсказка о канале. Контур «дефект в САМОМ KAIF» (5 шагов, п. 1.11 NDim
  дословно) — в payload AGENT_GUIDE + сшивка `/experience` + RU-зеркало; регистрация в
  KAIF_REFERENCE §7.2/§10.3. Красным-вперёд 8 ассертов (s02/s03/s04; красные на HEAD-dist →
  зелёные), полигон **«all 12 suites green»**, сборка **«55 embedded files … bundle 148
  blocks … 654 modules»**. Судья лёгкий (№48): **VERIFIED**. План M4+M5 по канону N+1 →
  `plans/51` (одним дочерним планом — решение агента в закрытии `plans/50`). Попутно
  закоммичены дословно арт-файлы владельца `assets/KAIF_2.2.psd` +
  `KAIF_2.2_GH_RELEASE_PAGE_LOGO.jpg` (шаг 15 чек-листа).

### Сессия 2026-08-07 (девятая) — **фазы M1 и M2 эпика M закрыты**: петля формализована, reports/ в поставке 🟢

- **Фаза M2 (`plans/49`, все 5 шагов):** канон-директория **`reports/` в комплекте поставки** —
  шаблон `framework/readmes/reports.md` (жанр · `KAIF_UPDATES/` обязательные update/install-отчёты
  СТРОГО EN · `KAIF_AUDIT/` аудиты T3) + заметка `bugs/KAIF` в bugs-README; круг регистрации
  ПОЛНЫЙ (10 поверхностей payload вкл. SKIP_DIRS stale-claims; пакеты ресинканы 9×2 + пины 5f тем
  же коммитом); сборка **«55 embedded files (13 docs + 7 readmes + 34 skills + 1 tools) · bundle
  148 blocks · 651 modules»**; красным-вперёд 3 полигонных стража (установка создаёт / update
  доносит), полигон **«all 12 suites green»**. **Миграция №41 исполнена:** 5 lifecycle-отчётов →
  `reports/KAIF_UPDATES/`, 11 полевых → `reports/` (git mv, история жива); 110 ссылок-указателей
  выправлено, решенческие строки нетронуты; реестр цитат `researches/16` переповерен «189;
  промахов: 0» (заодно закрыт до-M2 дрейф цитаты I7). Судья лёгкий (№48): **VERIFIED**. План M3
  по канону N+1 → `plans/50`. Попутно добит протухший счётчик «тридцать один навык» (bugs/09).

- **Фаза M1 (`plans/37`, все 5 шагов):** рабочий документ **`researches/18` «Петля KAIF в
  терминах управления»** — сверка ПФУ по ПЕРВОИСТОЧНИКУ (PDF ДОТУ ред. 2011, дословные цитаты
  по печатным страницам, **скрипт-сверка 57/57**; находка: в Таблице 1 этапов ВОСЕМЬ — веб-конспект
  терял этап 7 «совершенствование концепции», суть эпика M; поправка внесена в `researches/15`
  §11.1) · вектор целей KAIF (9 инвариантов иерархией) · вектор состояния (8 источников) · вектор
  ошибки + допустимая ошибка (нормальное/допустимое/аварийное ↔ error budget) · схема
  предиктор-корректор (8 этапов ↔ механизмы KAIF, включая `/kaif-remove` как этап 8) ·
  классификатор 6 типов сигналов + режимы 8.1/8.2 · fingerprint `kaif-fp:` с дедупом «+1
  наблюдение» и progressive collection · **4 EN-шаблона**: issue **A) bug report / B) improvement
  request (разведение — слово владельца, чат ≈13:50)**, C) update report (вердикт судьи
  обязателен, №46), D) install report. Ретро-прогон шаблона C на отчёте NDim 2.1 — приложение B
  (потерь 0, обратная сверка по трём остальным). **Судья фазы лёгкий (№48): VERIFIED** — механика
  31/31 + цитаты 57/57. План M2 по канону N+1 → `plans/49`. EXP-0030 (CJK-подмены в генерации,
  скан механизирован).

### Сессия 2026-08-07 (восьмая) — **фаза K5 закрыта, ЭПИК K ЗАКРЫТ ЦЕЛИКОМ**; живой пилот контура с владельцем 🟢

- **Фаза K5 (`plans/48`, все 6 шагов):** этот репозиторий получил живой интерактивный контур,
  построенный по СОБСТВЕННОМУ вендоренному контракту (дефер D5 из 2.1 закрыт). Инструменты:
  `tools/questions-guard.mjs` (G1/G2/G3/I20/I21; долг ratchet — сейчас **11**, убыл с 12) ·
  `tools/lib/review-core.mjs` (C3/C4/C6/C7, I6/I22–I24) · `tools/review.mjs` (P1–P9, I5–I33,
  очередь/пачка, `--queue`/`--no-serve`) · `tools/review-gate.mjs` + `tools/send-outbound.mjs`
  (fail-closed; живая отправка — Фаза R, issue #2) · `tools/verify-contour.mjs` (CDP без
  зависимостей). Приёмка: **QA 78/78 headless · 3/3 видимое окно (T2) · эвал-ловушки 3/3 ·
  полигон 12/12** (новый свод s12 — ростер канона 97 позиций × оба слоя + red-proof мутацией).
  Команды контура — в ритуалах (AGENT_GUIDE «Место вопросов», `/end-chat`, `/release`).
- **ЖИВОЙ пилот с владельцем — интервью 008** (4 развилки решений K5): два полных цикла записи
  через страницу (Q1: C — тихих часов в этом репо НЕТ; Q2: A; Q3: A; Q4: B — секция «Ожидает
  владельца» удалена из STATUS, просьба → `homeworks/04`), побудка завершением процесса
  сработала оба раза; разнос выполнен, интервью закрыто. ~10 полевых правок владельца
  (сводка в шапке · язык интерфейса = язык владельца · полное тело вопроса в карточке ·
  большие радио · серые незвёрнутые закрытые · утечка провенанс-маркера) и 2 бага
  записи/парсера — закрыты тем же днём; инцидент «перезапуск сервера под живым окном» →
  EXP-0028. **Голос — вынесенный вердикт владельца: Silero v5 ru `eugene`** («рот» машины —
  `F:\KLAS\tools\voice-say.mjs`), фолбэк SAPI Irina Desktop; вердикт не переспрашивался.
- **Разведка трёх доноров контура** (NDim · KLAS · Unliminium) по прямому вопросу владельца —
  `researches/17` + EXP-0029 (разведка доноров — шаг ДО пилота); канон-обогащения по слову
  владельца (язык страницы · тело вопроса в карточке · поле «Происхождение» в `/interview` ·
  полевые числа в QA4) — отдельным канон-коммитом. **Судья эпика: VERIFIED WITH CAVEATS,
  0 блокеров; сверка §12 issue #2 — 11/11** (закрытие тикета — Фаза R). Идея 21
  (информативный зов) — наброс владельца, ждёт скоупа эпика M/O.

### Сессия 2026-08-07 (седьмая) — **фазы K3 и K4 эпика K закрыты**: контракт сборки + стражи и приёмка 🟢

- **Фаза K4 (`plans/47`, все 6 шагов):** в `/owner-reviews` (оба слоя попозиционно) — секция
  **«Guard norms (G1–G13)»** (узкие приметы и три мутации G1 поимённо · ratchet G2 — «A guard
  that is red from birth is not a gate.» дословно · сшивки G3→`/interview`, G9→грабля 5 (по
  находке судьи G9 сведён к чистой сшивке — одна копия, без дрейфа) · страж содержания G4 с
  `<!-- new -->` и «не блокирует» · правила построения G5–G13) + блок **«Red proof, guard by
  guard»** — семь стражей гейта, каждый со своей красной мутацией (7/7) + **«The acceptance
  checklist (QA1–QA7)»** (QA7 — headless с пятью true бага 54). Сборка чистая (**650
  модулей**), полигон **11/11**; механика — «all G1-G13 + QA1-QA7 (and C1-C13 + T1-T11 +
  DEF1-DEF8) present in dist section»; **судья: VERIFIED WITH CAVEATS, 0 блокеров** (13/13 G
  · 7/7 QA · red-proof 7/7 · зеркало попозиционно · гигиена чиста). План K5 по канону N+1 →
  `plans/48` (dogfooding: контур этому репозиторию + вход из L + судья эпика).

- **Фаза K3 (`plans/46`, все 6 шагов):** в `/owner-reviews` (payload EN + RU-зеркало
  попозиционно) — секция **«The executable build contract (C1–C13)»** (состав поставки ·
  6 шагов сшивкой с Build order, страж первым · нормализация/хеш «четыре лица — один хеш» ·
  5 правил разбора · сшивки C5→P1–P9, C6→I2, C7→I4 · сигнал · накопление + I8 + `--no-serve`
  + жёсткий срок дочерних вызовов · QA-таблица 11 блоков · «чего НЕ делать» 7 пунктов ·
  цена/сроки) + каталог **«Platform traps (T1–T11)»** (пометки платформ; сшивка на канон
  гигиены, не дубль) + таблица **«Canonical defaults (DEF1–DEF8)»** (маркер «конверт
  владельца») + M8: рендер печатает `RENDER IS NOT YET A SHOW` + готовую команду открытия.
  Сборка чистая (**648 модулей** — +3 заголовка секций, зеркала обновлены), полигон **11/11**.
- **Гейт фазы пройден:** эвал-субагент по ОДНОЙ dist-секции (прочие файлы запрещены) назвал
  все 6 шагов (страж первым), 3 места записи, `--no-serve`, DEF4, T2 и M8-строку — с
  дословными цитатами, «достаточно для сборки без донора»; механический скрипт — «all C1-C13
  + T1-T11 + DEF1-DEF8 present in dist section». **Судья (лёгкий, №48): VERIFIED WITH
  CAVEATS, 0 блокеров** (13/13 C
  · 11/11 T · 8/8 DEF, 15/15 чисел, бренд-токенов 0); действенная находка одна — «P1–P8» в
  строке C5 реестра при слое P1–P9 — закрыта тем же заходом. Попутно: команда README-сверки
  реестра пар была неисполнима дословно (ложный красный 0 вместо 68) — заменена
  бэктик-свободной, урок **EXP-0026**. План K4 по канону N+1 → `plans/47` (стражи G1–G13 +
  приёмка QA1–QA7).

### Сессия 2026-08-07 (шестая) — **фазы K1 и K2 эпика K закрыты**: реестр контура + канон-вшивание 🟢

- **Фаза K2 (`plans/45`) — норматив вшит в канон обоих слоёв:** `/owner-reviews` — единое
  пространство инвариантов **I1–I36** (I7 правлен: очередь — файл состояния, документы не
  переносятся; I8 вендорен EN-дословно; антагонисты I8↔I9↔I14 одним блоком с запрещённым
  прочтением) + именованный класс «обращение с работой человека» (M1, приёмка перечнем 9
  случаев) + элементы страницы P1–P9 + порядок разведки донора (M4); `/interview` — адресат
  ответа в шаблоне (I18), закрытие = разнос с капом формы (I19/I21), детектор протухшего
  статуса (G3), «вопрос — утверждение о каноне» с тремя подпроверками (M7); `AGENT_GUIDE` —
  «показ — действие, а не ссылка» (I15–I17); `PHILOSOPHY` — граница «Оккам не применяется к
  воспринимаемому» (M3). Сборка чистая (**645 модулей** — зеркала счётчиков обновлены), полигон
  **11/11**; судья канона — **VERIFIED WITH CAVEATS, 0 REFUTED** (второго канона нет, зеркала
  честные; 2 находки M4/M7 закрыты тем же заходом). План K3 по канону N+1 → `plans/46`
  (исполнимый контракт сборки, №34).

- **`researches/16` — сводный нормативный реестр интерактивного контура** (план `plans/35`,
  все 9 шагов): 92 позиции в слоях I/M/P/G/T/C/QA/O + константы DEF1–DEF8; **189 дословных
  цитат, 0 промахов** (скрипт по EXP-0015 — приложение А реестра); четыре первоисточника
  прочитаны лично целиком, состояние доноров зафиксировано (ndim `4aa1756`, unliminium
  `272a813`). Снапшот issue #2 (тело + 2 комментария) закоммичен дословно ДО анализа →
  `reports/26` (закрытие тикета — гейт эпика, Фаза R). **Коллизия «двух I8»
  снята** (NDim-I8 «сохранение будит ждущего» → I8; скоуп-И8 греп показа → I17); антагонисты
  I8↔I9↔I14 записаны рядом с named-запрещённым прочтением. **Константы №38 зафиксированы
  сверкой с живым кодом NDim**: писки 880/160 · 660/160 · 990/260 Гц/мс; автозакрытие 2000 мс
  (клиент) / 2500 мс (сервер); пульс 15 с дефолт, конверт 10–60 с; таймаут 0; обратный пульс —
  из issue #2 (в коде донора его нет). **Судья фазы (лёгкий, №48): VERIFIED WITH CAVEATS,
  0 REFUTED**, 7 находок внесено — главная: коллизия буквы I7 «pending folder» с полем NDim
  §12, разрешена (очередь — файл состояния; текст I7 правит K2). План K2 написан по канону
  N+1 → `plans/45`.

### Сессия 2026-08-07 (пятая) — фаза L5 закрыта, **ЭПИК L ЗАКРЫТ ЦЕЛИКОМ**; скоуп 2.2 облегчён (№47/№48) 🟢

- **Фаза L5 исполнена целиком** по `plans/44` (builds 105–107 + закрывающий) —
  `bugs/40/41/42/44 → DONE`, грань Q14 из `bugs/34_DONE` добита. **bugs/40:** пункт
  `placeholders` обоих заданий строится СКАНОМ диска («слот → реальные пути», кап 6 с
  честным «+N more»; чужие сферы вне списка); новые пункты `sphere-sync` (локальная сфера +
  изменившийся шаблон) и `local-inventories` (дельта скиллов; машиночитаемый инвентарь =
  deploy-manifest → paths — закрыл и NDim N6); предупреждение про строки владельца в
  чек-листах на обоих merge-пунктах. **bugs/41:** канонное имя — маркер `projectName` +
  команда `project-name` (лечит values-карту и снапшот; исполняющий чекпоинт-гейт); канон
  побеждает заморозку bug 26 → «klas»-заголовки лечатся обновлением. **bugs/42:** `route:
  bootstrap` при живом манифесте; `legacy-bootstrap` — только синтетика/adopt. **bugs/44**
  (№44): печать «INCOMPLETE BY DESIGN» границы пакета; Taste-абзац досинхронизирован во все
  9 пакетов; страж протухания 5f + пин-реестр `_lang-pack-source-shas.json` (красный
  доказан). **Q14 (№45):** `parseMarks` — заголовочная `[AI]`-марка = секция до заголовка
  того же уровня, без закрытия; s05 +6 стражей. **KLAS D13:** backup-дерево
  `.kaif/backup-<from>-<to>/` до первой записи, ignore-first. Хвосты скелета: 4 закрыто, 4
  деферировано с якорями «Вход из эпика L» в планах K/N/O/P.
- **Верификация:** свод `s11-l5-remaining` — **44 стража** (печатает сам свод — EXP-0025),
  20 красных ДО фиксов по предсказанию; полигон **11/11**; реестр пар 6/6. **Судья ЭПИКА —
  панель 4 адверсарных линз: 4× VERIFIED WITH CAVEATS, 0 REFUTED** (полнота триажа 46/46 ·
  воспроизведение исполнением на ДО-фиксном дереве · регрессия 13/13 + полевой стенд
  2.1→текущая на обоих маршрутах с byte-exact детерминизмом · канон/честность, 9 переводов
  прочитаны). Находки закрыты: учёт KCam §3 восстановлен; Q15-строка в plans/29 выправлена;
  счёт «34→44» механизирован печатью свода; ложный «self-healed»-лог убит каноническим
  сравнением; route-нота в `/kaif-update` (интервал исполняет СТАРОЕ ядро — на этот проход
  новые гарантии даёт bootstrap-маршрут). Принятый долг: постоянное покрытие sha256-гейта
  лоадера + twin-run (автономный пул). Вердикты — `plans/44` шаг 9.
- **Скоуп 2.2 правлен владельцем на лету (решения №47–№49):** **№47** — эпик P (kaif-*)
  NOT TO BE DONE в 2.2, кандидат 2.3 (порядок: L → K → M → N → O → Фаза R; №35/№36 в силе);
  **№48** — судейство эпиков лёгкое, полный суд всего 2.2 один раз в Фазе R; **№49** —
  КАНОН временных меток: и дата, И локальное время принятия/фиксации → новая задача **T8**
  (зонтик `plans/26` §7).

### Сессия 2026-08-07 (четвёртая) — фаза L4 закрыта: шум и честность аудита; интервью 007 отвечено целиком 🟢

- **Ответы владельца Q13–Q15** закоммичены дословно (build 99) → **решения №44–46** в
  MASTER_PLAN §7: Q13 «А» — языковые пакеты честно неполные + страж протухания (грань
  bugs/44, фаза L5); Q14 «А» — заголовочная `[AI]`-марка узаконена (дефер-грань bugs/34
  разблокирована → L5); Q15 «А» — судейство обновления обязательным чекпоинтом (эпик M).
  Открытых вопросов владельцу НЕТ. `/end-chat` вписан в шаг 8 плана L4 по AUTH владельца.
- **Фаза L4 эпика L исполнена целиком** по `plans/43` — `bugs/35/36/37/38 → DONE`.
  **bugs/35:** stale-claims — клейм = токен версии СМЕЖНО (≤16 симв.) с маркером KAIF;
  пропуски blockquote-цитат, датированных журналов/летописи, зеркал 4 систем, GOAL.md +
  canonArtifacts, атрибуций «(KAIF 1.6)»; кап по ФАЙЛАМ (20) с честным «shown N of M»; пункт
  задания — ПОСЛЕ review-news (миграция летописи до скана). **Точность на размеченной
  фикстуре 4 отчётов: 2 % ДО → 100 % ПОСЛЕ** (строко-гранулярный замер скриптом; критерий №6
  ≥ 50 %); трейд полноты (датированные/цитатные/скобочные/дальние клеймы пропускаются по
  построению — скан советчик) зафиксирован письменно. **bugs/36:** moduleAudit на
  `i18n: translated` — честный `localized` вместо 218/290/216 ложных «MODULE ABSENT»,
  реальная потеря в EN-файле видима; сферы плейсхолдеров — только объявленная (чужая с
  `<BUILD_COMMAND>` не блокирует, объявленная стережётся). **bugs/37:** warning-guard длины
  STATUS (>200 строк, `STATUS_SOFT_LINES`) существует в `check`; инвентарь обещаний нот 2.1
  пройден — мёртвые стали кодом. **bugs/38:** линт бандла 5e в `check-framework` (пары
  «документ ↔ ведущий навык» с двусторонней ведущей парой, FORBIDDEN-скан всего framework/,
  order-guard check-backlog) — **красный на ДО-фиксном HEAD: 23 находки**; все 8 рассинхронов
  KLAS D10 закрыты контентно в шаблонах И русской обвязке.
- **Верификация:** свод `s10-l4-audit-noise` (27 стражей, 12 красных ДО фиксов по
  предсказанию) вписан в полигон — **10/10 сводов**; реестр пар 6/6. **Судья фазы — панель
  4 адверсарных линз (все исполнением): 4× VERIFIED WITH CAVEATS, 0 REFUTED** — судьи
  независимо воспроизвели «2 %» на ДО-фиксном ядре из git-истории и 23 находки линта,
  реверс-тесты на сломанных temp-ядрах, совместимость с бандлом 2.1 на 4 стендах; все
  находки (строко-гранулярная метрика · страж продукт-версии · письменная фиксация трейда ·
  двусторонний линт · охват FORBIDDEN · рассинхроны корневого AGENT_GUIDE) закрыты до
  коммита (вердикт — в `plans/43`). Урок → EXP-0024 (шумный сканер чинится размеченной
  фикстурой и числом до/после). Оп-план L5 — `plans/44` (канон N+1).

### Сессия 2026-08-07 (третья, продолжение) — фаза L3 закрыта: CLI-безопасность и зелёная ложь 🟢

- **Фаза L3 эпика L исполнена целиком** по `plans/42` — `bugs/33/34/39 → DONE` (грань Q14 в
  bugs/34 — ⏸ явный дефер до ответа владельца). Машинерия: **голый/флаговый запуск = справка**
  (`help` — команда; старый install-дефолт похоронен — THE полевая потеря KCam Г1);
  единый спек COMMANDS ведёт диспетчер+справку+**вайтлист argv** (неизвестный флаг/аргумент =
  отказ, не молчаливое исполнение — `sync --dry-run`, `diff <файл>`, опечатки флагов);
  незавершённое задание БЕЗ чекпоинтов не регенерируется при same-version re-run (fail-closed);
  **SKIPPED/код 3** у ненастроенных стражей (canon-lint, provenance — check/report согласованы;
  пересмотр компромисса bug 30.2 по полевой цене); `"canonArtifacts": []` сеется в маркер;
  **`aiMarks`** — локализованные марки `[ИИ]` видимы провенансу (Unlim Г8); транзиенты
  исключены из сканов; счётчик алиасов считает ЗАПИСЬ (KLAS D3); **чекпоинты исполняют
  сканеры** (placeholders — ресинк+отказ; stale-claims — видимость; recheck — сперва sync,
  закрывает окно дрейфа зеркал KCam Г11); **`checkpoint judge --verdict-file`** (кириллица/
  BOM/многострочник; полный вердикт — в квитанцию `judgeVerdict`, решение №42); policy-записи
  2.2 предупреждают обновляющихся; KAIF_REFERENCE §12.1/12.3 дополнены.
- **Верификация:** свод `s09-l3-cli-safety` — 25 стражей красные ДО фиксов по предсказанию
  (до-фиксный голый запуск УНИЧТОЖИЛ задание в фикстуре — инцидент воспроизвёлся буквально);
  полигон **9/9**; регрессии, пойманные полигоном, закрыты в тот же заход (leak-токен
  «Krinik*» в цитате деплоящегося tool-модуля; порядок «ресинк зеркал ДО скана» у
  placeholders-гейта — класс bug 27). **Судья фазы — воркфлоу-панель 4 линз (~347k, всё
  исполнением): VERIFIED WITH CAVEATS** — красное доказательство перепроверено реверсом
  (ровно 25), полевая совместимость цела; находки (major: асимметрия защиты АДАПТАЦИОННОГО
  задания — «CHECKPOINT LOST» воспроизведён; значение `--channel`; + 4 minor) закрыты до
  коммита, стражи G15–G18 + s06-дострой (свод — `plans/42`).
  Оп-план L4 — `plans/43` (канон N+1). Инцидент сессии: шелл-правка plans/42 через PS5.1
  `Get-Content|-replace|Set-Content` перекодировала файл в моджибейк — восстановлен из git,
  правки переделаны файловыми инструментами (класс — AGENT_GUIDE «Гигиена», симптом 1).

### Сессия 2026-08-07 (третья) — фаза L2 закрыта: класс перевода и настоящие диффы задания 🟢

- **Фаза L2 эпика L исполнена целиком** по `plans/41` — `bugs/31/32/43 → DONE`. Машинерия
  (`framework/installer/KAIF-CORE.mjs`, +203 строки): вставка EN-модулей в переведённые файлы
  запрещена по построению (вставляется только по-настоящему новое против ШАБЛОНА прошлого
  релиза); порог сети `translatedWholesale` стал долей (0.15, N≤2 → 0; лицо KLAS «2 из 21»
  ловится, EXP-0019 сохранён); суждение о переводе — по ТЕЛУ (`bodyLocalized` — машинные
  алиасы во frontmatter больше не ослепляют); авто-простановка `i18n: translated` (KLAS D2);
  фильтр нулевой дельты («шаблон не менялся → пункта нет»); настоящие диффы «шаблон старой
  версии → шаблон новой» через синтетический baseline с ТЕКСТАМИ (`cmdUpdate` скачивает
  артефакт прошлого релиза; офлайн — честная деградация); frontmatter обновляется по модулю
  машинного алиас-хвоста (`stripAliasTail` — bugs/43). Кириллица вычищена из тел EN-шаблонов
  `pause`/`kaif-remove`; класс закрыт гардом сборки (check-framework 5d, красный на HEAD
  доказан); в `check` — детектор двухголовых SKILL.md (>1 H1).
- **Верификация:** новый свод `s08-l2-faces` — три полевых лица NDim/KLAS/KCam, 12 стражей
  красные ДО фиксов (красный прогон в шапке свода) → зелёные; полигон 8/8 сводов; регрессия
  s02/S6 (ориентация конфликтного диффа — канон 2.0) поймана полигоном и откачена в тот же
  заход. **Судья фазы: VERIFIED WITH CAVEATS** — 2 major (эшелонированная маскировка слоёв →
  изолирующие стражи F3d/F6/F7, каждый доказан красным на одиночном реверсе; двухголовый
  корневой док невидим → детектор >1 H1 расширен на все не-owner .md) + 3 minor — все закрыты
  правками до коммита (свод в `plans/41`). Уроки → EXP-0021 (машинные хвосты слепят
  сравнения), EXP-0022 (чужой свод стережёт семантику). Оп-план L3 — `plans/42` (канон N+1).

### Сессия 2026-08-07 (продолжение) — исполнение 2.2 СТАРТОВАЛО: фаза L1 (триаж) закрыта 🟢

- **Фаза L1 эпика L исполнена целиком** по `plans/36`: 4 полевых отчёта об обновлении 2.1
  прочитаны лично; реестр триажа — **46 запросов** (пересчёт: дайджест считал 45 — в KLAS §4
  двойная «9.») + ~60 наблюдений-граблей, у КАЖДОГО записана судьба; классы L-1…L-11 + 3
  уникальных дефекта → баг-доки **`bugs/31–44`** (по канону: свод класса, проявления,
  полевые репро, причины, фаза фикса).
- Цитатная дисциплина EXP-0015: **130/130 цитат сверено скриптом** (2 прогона; самопроверка
  до скрипта выправила ~21 искажение — сжатия, перевод-вместо-оригинала, „…"-кавычки).
- Развилки триажа → **интервью 007 Q13–Q15** (языковые пакеты · заголовочная форма
  `[AI]`-марки · судейство обновления обязательным) — ждут владельца, L2–L4 не блокируют.
- Чек-лист регрессии «не сломать» — секцией в `plans/28`; **судья фазы: ПОДТВЕРЖДЁН С
  ОГОВОРКАМИ** (2 major: потерянное наблюдение `canonArtifacts`, протухшие якоря фаз —
  класс EXP-0020; + 9 minor) — все находки закрыты правками до коммита. Оп-план L2 —
  `plans/41` (написан на закрытии L1, канон N+1).

### Сессия 2026-08-07 — KAIF 2.2 СПЛАНИРОВАН ЦЕЛИКОМ: 21 пункт скоупа → 6 эпиков + 7 задач 🟢

Владелец принёс дополненный скоуп 2.2 (`ideas/20` — блок 1.0–1.12 интерактивного контура с
4 полевыми инцидентами и 15 инвариантами + пп. 2–22) и попросил глубокое планирование
(эпики → фазы → операционные планы; разработка — в новом чате). Сделано (build 92+):

- **Разведка:** workflow 7 полевых читателей (~510k) — отчёт NDim о контуре (исполнимый
  контракт), баги Unliminium 54 / ndim 112, отчёт 25 (разнос ответов, 55 % без ссылок),
  GitHub issue #2 (19 требований A–D + обратный пульс), 4 отчёта об обновлении 2.1 (~45
  запросов, 11 классов ≥2 проектов), gap-анализ навыков + workflow 4 веб-разведчиков
  (ДОТУ/ПФУ · 29148/EARS · Claude Code hooks/context rot · агентный аудит) — всё с URL.
- **Метасинтез `researches/15`:** инвентарь «пункт → источник → судьба» (все пункты покрыты),
  сквозные инварианты 2.2 (11), индустриальная разведка §11, верификация цитат 36/36 скриптом
  (поймала 2 искажения слов владельца — исправлены).
- **Дерево планов:** зонтик `plans/26` (формула, критерии приёмки релиза, порядок, гейт
  интервью, операционка задач T1/T2/T5/T6/T7 инлайн) + 6 EPIC-мета-планов по новой конвенции
  п. 9 (`27_EPIC_K` интерактивный контур · `28_EPIC_L` полевые фиксы обновления · `29_EPIC_M`
  контур обратной связи · `30_EPIC_N` канон-архитектура/REQUIREMENTS · `31_EPIC_O`
  контекст+окружение · `32_EPIC_P` переименование kaif-*) + планы задач `33` (аудит-ревизия
  /code-revision) и `34` (стилометрия) + дочерние оп-планы ближайших фаз ВСЕХ шести эпиков
  `35–40` (K реестр · L триаж · M формализация петли · N REQUIREMENTS · O разведка механизмов ·
  P rename-карта) — дисциплина п. 13 (вектор цели + критерии приёмки первыми) применена ко
  всем досрочно.
- **Интервью 007 — ОТВЕЧЕНО ЦЕЛИКОМ в тот же день** (12/12; дословный коммит build 93;
  решения №32–43 в MASTER_PLAN §7): кодовое имя **«KAIF 2.2 — Yolden KAIF»** · контур —
  «Интерактивный контур» · контракт вендорится (без докачки origin) · kaif-* кроме fable-*,
  старые имена алиасами · kaif-go/gо + естественные триггеры · порядок L→K→M→N→O→P, один
  релиз · константы утверждены (пульсы допустимо раз в минуту) · AUTHOR_STYLOMETRY.md ·
  REQUIREMENTS 14-й ключевой · миграция ai_agents_reports→reports/ · UPDATE_TASK в ignore +
  квитанция с вердиктом · канон N+1 подтверждён. Все гейты эпиков СНЯТЫ.
- **Судейский проход пакета** (15 агентов, ~1,4 млн токенов: 5 линз → дедуп → 10 скептиков с
  дефолтом «не дефект»): 5 подтверждённых находок (протухшая нумерация Q-вопросов — блокер —
  и операционка M/N/O/P в телах эпиков вопреки букве п. 9) — все закрыты правками до пуша;
  5 опровергнуто; механика до судьи: цитаты 36/36 скриптом (поймано 2 искажения слов
  владельца), греп EXP-0018 (2 сломанных врапа). Первоисточники владельца закоммичены дословно
  ДО анализа (build 92).

### Сессия 2026-07-31 — KAIF 2.1 «Strong KAIF»: ВСЕ 9 эпиков закрыты за день; релиз ждёт слова владельца 🟢
Владелец принёс идею 19 (11 пунктов) + 9 полевых отчётов и сказал «Бери все эпики. Нужно доделать
версию 2.1». Сделано за день (builds 69–89; каждый шаг: полигон 7/7 + адверсарный судья до пуша):
- **B ✅** полевые фиксы обновления (bugs/20–30 DONE; свод s07); **A ✅** лестница планирования +
  `/plan-task`/`/plan-epic`; **F/E ✅** канон-гигиена + границы суждения (идентичность, вкус);
  **H ✅** STATUS→`PROJECT_HISTORY.md` (13-й документ; этот STATUS сжат по канону);
  **G ✅** `/guarded-loop` (сторож+heartbeat; полевой прогон — дефер).
- **I ✅ (батч)** — протезы ремесла: Craft recipes в сфере programming (скелет охранника ·
  платформенные образцы · правило перемера · двери стенда), третий KAIF-патч fable-method
  (craft-слоты: TWINS-MECH · таблица переноса · AFTER-WORK · BOTH-WAYS · дифф-обход), охоты
  судьи (identity-without-an-author · timer-fed heartbeat · адресность мутаций), `/code-revision`
  EN+RU, EXPERIENCE: Repro обязателен + Триггер + механизация повторов, реестр пар
  «истина↔зеркало» — методология в канон + реестр обвязки посеян (6 пар, все зелёные).
- **C ✅ (`plans/24`)** — голос владельца: шаблон портрета `_owner-voice-template.md` (пороги,
  анти-портрет, пары до/после, эвристики; едет в `.kaif/` — доказано песочницей), `/owner-voice`
  EN+RU (portrait/rewrite, провенанс-лестница, инварианты), секции сфер + фрод «Voice without a
  corpus», вплетение (роутер · чек-лист · разведартефакты «НЕ четвёртый тип»).
- **D ✅ (`plans/25`)** — контур согласований: канон-правило места вопросов (всё от владельца —
  строго в интервью; страж; критерий внедрённости), `/owner-reviews` EN+RU (I1–I7 + контракт имён
  из отчёта 13 — судья оттрассировал дословно), /interview: рендер-шаг + эквивалентность
  HTML=md=чат + by/at + накопление I7 (autoloop согласован).
- **Консолидация 34 навыков:** алиасы ×9 пакетов, README 31→34 (+3 строки таблиц обеих половин),
  KAIF_REFERENCE §6 (группы Owner contour / Code quality), CLAUDE.md, help-kaif ×2, счётчики
  обвязки 54/147/643, SVG-схемы перегенерированы, PDF.
- **J ✅** — README-руководство по рецепту KPOT: разведдок `researches/14` (мануальный профиль
  портрета; полный портрет в публичный репозиторий НЕ копируется — проход публичности), интервью
  006 отвечено (Q1 «А» — полный жанр руководства, Q2 «А» — скелет; решения №30–31), **свап
  выполнен**: README.md — двуязычное руководство (черновик удалён; каждое утверждение перевыведено
  — поймано протухшее ×23, честное ×34), стиле-чек грепами §7А чист, якоря 10/10, PDF.
- **Фаза R (заготовки):** `TEMPLATE_NOTES['2.1']` — 8 нот машинной методички слияния (включая
  миграцию STATUS→HISTORY — хвост H закрыт); кодовое имя утверждено дословно.
- **Батч-судья I+C+D:** 4 адверсарных ревьюера (~430k токенов): зеркала/счётчики CLEAN ·
  машинерия CLEAN (мутационный тест двусторонности формулы бандла; поставка скелета доказана
  реальной установкой) · источники: 2 дефекта · канон: 3 находки — все 5 исправлены до коммита.

Статусы эпиков 2.1 на закрытии (таблица из STATUS дословно):

| Эпик 2.1 | Статус |
|---|---|
| B · A · F · E · H · G | ✅ 2026-07-31 |
| I. Протезы ремесла (/code-revision, craft-слоты, реестр пар) | ✅ 2026-07-31 |
| C. Голос владельца (/owner-voice, `plans/24`) | ✅ 2026-07-31 |
| D. Контур согласований (/owner-reviews, `plans/25`) | ✅ 2026-07-31 |
| J. README-руководство (образец KPOT; интервью 006 А/А) | ✅ 2026-07-31 (свап выполнен; просмотр глазами на github.com — за владельцем после пуша) |

### Сессия 2026-07-31 — KAIF 2.1 взят в работу: метасинтез скоупа → 8 эпиков → планы 🟢
Владелец принёс идею 19 (скоуп 2.1, 11 пунктов) + 5 полевых отчётов ИИ-агентов
(`ideas/ai_agents_reports/`: 13 — интерактивные согласования · 17 — субъективные оценки ·
20 — бланковое добро / ноты vs README / текст-через-файлы · 21–22 — голос владельца) и отчёты
обновлений двух проектов до 2.0. Указание: «нарезай эпики → фазы → операционные планы — и делаем».
- **Первоисточники закоммичены дословно** (шаг 15; builds 69–70); отчёт ndim 1.6→2.0 найден в
  соседнем проекте и скопирован побайтово (`ai_agents_reports/23`, sha сверен); второй проект —
  KPOT 1.5→1.6, его отчёт уже был у нас (`updade_kaif_field_reports/03`, в метасинтезе researches/07).
- **`researches/13` — метасинтез скоупа** (метод EXP-0015: 5 документов лично, отчёты 13/17/20/21 —
  субагентами со схемой цитат, **101/101 цитата проверена механически**; индустриальная разведка
  4 направлений с URL: стилометрия · HITL-согласования · watchdog · статус-доки/ноты). Открытый
  вопрос отчёта 20 закрыт наблюдением: дефектные формулировки живы в `release/SKILL.md:85-86`.
- **Нарезка: 8 эпиков A–H** (B полевые фиксы обновления K1–K6 · A дисциплина планирования ·
  F канон-гигиена · E границы суждения · H STATUS→PROJECT_HISTORY · G защищённый цикл · C голос
  владельца · D интерактивный контур). **`plans/22`** — зонтичный план (операционка лёгких эпиков
  внутри), **`plans/23`–`25`** — операционные/мета-планы тяжёлых (B детально; C/D — по дисциплине
  лестницы: фаза 1 детально, дальше по закрытию). **`interviews/interview_005`** — 8 развилок
  (кодовое имя · порядок · состав «двух проектов» · загрузчик C · имена навыков · дефолты G ·
  порог H · граница идентичности E). Работа не заблокирована интервью (решение №20): эпик B может
  стартовать сразу.
- **Эпик B, Фазы B0–B2 ОТГРУЖЕНЫ в тот же день** (builds 73–76): баги классами (bugs/20–24) с
  адресами, сверенными с кодом · свод полигона **s07-translated** (22 стража; **12 красных ДО
  фиксов** — улики в багах) · фиксы K1–K5 (сетка translated-wholesale; по-файловый i18n-флаг +
  dry-run диффы; синтетическая база в diff для v1-манифестов; условная запись package.json;
  stale-claims не трогает журналы прошлого) · канонизация «Predict the pass» (песочная копия +
  diff --source) в /kaif-update и KAIF_REFERENCE §10.8. **Судья перед пушем: VERIFIED WITH
  CAVEATS** — F1 (вырождение порога на 1-модульных базах) закрыт ужесточением + стражем T6,
  F2 → **bugs/25 (OPEN)**: невлитая dry-run-дельта исчезает из следующего задания. Полигон
  **7/7 сводов**; сборка 12/47/139/**570**; bugs/20–23 ✅ DONE. Урок — **EXP-0019** (пороговые
  эвристики вырождаются на маленьких N; судья до пуша окупился двумя находками).
  **Остаток эпика B:** B3 (аудит K6 — bugs/24; решение bugs/25) · B4 (честный размер работы ·
  сводка размеров · хранение бандла пред. версии) · B5 (сводная верификация + судья эпика).
- **ЭПИК B ДОБИТ ЦЕЛИКОМ (build 80):** B3 — аудит K6 по всей поставке (bugs/24 DONE: unpack
  blockRe · runFinalGates · checkpoint; красное доказательство — HEAD-unpack на CRLF exit 1) +
  bugs/25 DONE (fail-closed гейт незавершённого задания + superseded-сохранение на bootstrap);
  B4 — честный размер работы · сводка size jumps · source в расписке · **модульный аудит в
  update-verify** (предложение A) · **bugs/29 DONE: анонимизированное ядро вместо удаления**
  (анонимам доступны check/sync/diff/adopt-current) · грани 30.4–30.6; B5 — полигон 7/7, третий
  судейский проход (VERIFIED WITH CAVEATS → F1/F2/F4/F5 закрыты до пуша, F6-каверзы записаны в
  баги). Весь беклог эпика bugs/20–30 ✅ DONE.
- **Интервью 005 отвечено владельцем в тот же день** → решения №21–28 в MASTER_PLAN §7 (кодовое имя
  **«Strong»**; порядок (а); имена навыков утверждены; дефолты G; порог H; граница идентичности) +
  **третий отчёт 2.0** добавлен владельцем (`ai_agents_reports/KAIF_2.0_UPDATE_REPORT.md`,
  анонимная установка: «механически, минуты вместо часов», 7 новых дефектов) → bugs/26–30.
  **Фаза B3a ОТГРУЖЕНА в тот же вечер** (builds 77–79): Д1 — снимок `values` в манифесте (сигнатуры
  adaptive-модулей не дрейфуют по построению; KAIF_REFERENCE §12.2) + H1-предохранители в merge;
  Д2 — resyncCopies ДО гейтов; Д3 — email-слот в PLACEHOLDERS + live-пункт placeholders в задании;
  Д4 — stale-claims скипает шаблон-тождественные файлы; Д6/Д7 — canon-lint «not configured» exit 0,
  формулировка записки устойчива к де-экспансии. Стражи s07 T7/T8/T8b + s06 красными до фиксов
  (7 новых красных), **судья до пуша: VERIFIED WITH CAVEATS → обе находки (шумный пункт
  placeholders; схема §12.2) закрыты**; bugs/26–28 ✅ DONE, 30 🟡 partial. Полигон 7/7.

### Сессия 2026-07-28 (продолжение) — витрина README: история в одну главу вех, упор на текущее состояние ✅
Владелец (после чтения README на GitHub): «старое скомпоновать в одну главу-список вех; основной
упор — в главе Excellent KAIF: не как мы сюда пришли, а что мы из себя представляем сейчас, что
умеем и какой опыт нас таким сделал». Сделано в обеих половинах (EN+RU) одним шагом:
- **Четыре хронологические главы** («New in 1.5» · «Tested KAIF» · «Fable-цикл» · «Homeostatic
  KAIF» · старый «Excellent KAIF») **слиты во флагманскую главу «Excellent KAIF — what it is
  today / какой он сегодня»** сразу после «How it works»: портрет в настоящем времени (обновление
  машинерией · сырому доверия нет + fable-цикл и судья · гвардрейлы для слабых моделей · один
  авторитетный справочник) + подраздел **«Опыт, который сделал его таким»** (8 полевых отчётов,
  обжёгшие проекты, сертификация на 12B, полигон, dogfooding). Все 6 рукописных якорей
  (#excellent/#tested/#guardrails × en/ru) сохранены — бейджи и внешние ссылки живы; в навигацию
  первого экрана добавлена ссылка на флагманскую главу (6 ссылок — в пределах 4–6 плана 19).
- **Новая компактная глава «Milestones / Вехи»** после «Четырёх идей»: 8 строк v1.0→v2.0, даты и
  кодовые имена сверены с `gh release list` (пойманы и поправлены: в v1.0 ядро называлось
  `FRAMEWORK.md`, а не `KAIF.md`; «полные ноты (с 1.1)» — у v1.0 нет GitHub-релиза, только тег).
- **«Обновление развёрнутого проекта» избавлено от «С 1.5»** и приведено к 2.0-семантике
  по-модульного мёржа (по `KAIF_REFERENCE` §10; старый текст «правленный файл остаётся байт в байт»
  противоречил и флагманской главе, и записке) + упомянута расписка.
- **Judge до пуша — workflow из 5 адверсарных ревьюеров** (синхрон EN/RU · факты · якоря/ссылки ·
  аудит потерь · стиль владельца). Блокер пойман и убит: перенос строки `+ ` в нулевой колонке рвал
  флагманский EN-абзац в маркированный список (CommonMark; доказано рендером marked). Возвращены
  потерянные при слиянии живые механизмы 1.6 (пятигейтный деплой-чеклист, «Решения без владельца»,
  Repro/Not for, data-shaped literals, объяснение слова *homeostatic* у цели бейджа); смягчён
  overreach «каждая дисциплина — из обжёгшихся проектов». Попутно закрыты 4 давние
  рассинхронизации EN/RU вне зоны правки (примеры алиасов, 2 строки таблицы документов,
  «а не потерянным чатом»). check-framework зелёный (47/139/569); PDF перегенерирован.

### Сессия 2026-07-28 — судейский проход по мегасессии 2.0: VERIFIED WITH CAVEATS, все находки закрыты ✅
Владелец: «проверить, как сделано, судейская оценка, найти и починить баги, отполировать — отчёт, потом
релиз». Итог: **вердикт /fable-judge по диапазону e114e63..49e4b0a — VERIFIED WITH CAVEATS**, все
подтверждённые находки починены (коммиты `15e9e0a` + текущий), 2.0 готов к Фазе 7.
- **Воспроизведено лично:** сборка 12 docs / 47 FULL / 139 бандл / 569 модулей (байт-в-байт, дерево
  чистое после перегона), полигон 6/6, все правки тестов диапазона обоснованы в коммитах, ослаблений
  ассертов нет, фродов и внешних эффектов сверх push нет.
- **6 параллельных адверсарных ревью** (счётчики · закрытие bugs/10–19 · код tool-модулей · навыки и
  9 языковых пакетов · записка vs код · git-диапазон) → ~40 находок; ключевые верифицированы
  механически ДО фикса (EXP-0015), одна опровергнута (пин сплиттера исполняется сборкой — build:336).
- **Главное пойманное:** (1) `kaif-provenance check` был красен в любом РЕАЛЬНОМ деплое — доки поставки
  цитируют пометки, песочница s05 была стерильна (блокер); + парсер по типам тегов вместо позиций,
  реестр приёмки писал «[AI]»/пустоту; (2) `kaif-canon-lint` ложно ЗЕЛЕНЕЛ на CRLF ($-якоря) и ложно
  краснел (CRLF/BOM); (3) класс bugs/09 НЕ был добит: «22 in all» уезжало пользователям в ядре,
  «26 ритуалов» вжжено в 4 SVG, «= 37» жило в критерии приёмки AGENT_GUIDE; (4) /help-kaif учил старой
  семантике /pause и 19 навыкам из 28.
- **Починено и застраховано:** инструменты переписаны (позиции, код-спаны/фенсы = цитаты, содержимое в
  реестре, CRLF/BOM, внятные ошибки) + **13 новых стражей s05/s06 — все доказаны красными ДО фикса**
  (s05 20 / s06 9 проверок); счётчики убиты формой (`{{SKILL_COUNT}}` в ядре и `SKILLS` в схемах —
  вычисляются сборкой); записка KAIF_REFERENCE выправлена по коду (диск-sha побайтный · kaif-remove не
  origin-tied · FULL = last-resort слепок · ~150 строк); help-kaif обоих слоёв знает все 28 навыков и
  пару /pause—/end-chat; обе карты проекта переписаны под 2.0; KAIF_FRAMEWORK и маркер `.kaif/kaif.json`
  1.1→1.6 (схема §12.1); кавычки алиасов zh-Hans/ja/hi по конвенциям пакетов; kaif-remove обвязки
  ресинк; STATUS вычищен от зомби-состояний. **bugs/09 ✅ DONE** (класс добит + убит формой), bugs/04
  переименован по конвенции, прочерки-стабы EN-секций решений в bugs/10–18 сняты, в bugs/16 — честная
  оговорка об отложенном полевом прогоне форка. Урок — **EXP-0017** (стерильная песочница для сканера =
  ложно-зелёный по построению; «класс закрыт» проверяется грепом класса).
- **Релизная готовность Фазы 7 проверена:** /release-канон на месте (judge-шаг, TEMPLATE_NOTES,
  формат заголовка, двуязычные ноты), `gh` авторизован, issue #1 OPEN (закроется релизом), релиз-лист
  канонический, version.json честный (1.6, кодовое имя 1.6, build растёт).

### Мегасессия 2026-07-27…28 — KAIF 2.0 ПОСТРОЕН ЦЕЛИКОМ (автономный цикл до релизной развилки) 🎉
Владелец: «делай всё в автономном цикле до полного завершения либо до развилки». Цикл дошёл до
развилки: **осталась только Фаза 7 — релиз (по явному «да»); владелец подтвердил готовность,
запуск — из нового чата** (этот чат закрыт через /end-chat). Итоги цикла (builds 52–60):
- **Эпик №1 (план 21) целиком, фазы 5.1–5.6:** карта модулей в сборке (сигнатурные якоря, классы
  вычисляются, пин ядро==библиотека исполнением); манифест v2 (templateShas ≠ diskShas, EOL-норм);
  по-модульный update (mergeModules от порядка диска; локализация живёт циклы; диффы только где
  апстрим менял); расписка last-update + history + adopt-current + diff + исполняющие чекпоинты;
  анонимный bootstrap механический (манифест переживает самоочистку), синтетический слепок для
  легаси (--baseline); notes по версиям интервалом; скан протухших утверждений; deprecations.
  Гейт плана 18 — вердикт-таблица П1–П10 в plans/21 §6б, пропусков нет.
- **Фаза 2b:** /pause → мягкая парковка (обкатана вживую ДО написания навыка), новый /end-chat
  (этот чат закрыт именно им); алиасы в 9 пакетах.
- **Фаза 5:** kaif-provenance (grep-ступень) · /derive-styleguide · kaif-canon-lint (selftest
  «страж умеет краснеть») · режимы строгости. Всё — опциональными модулями/навыками обоих слоёв.
- **Фаза 6:** пояснительная записка `KAIF_REFERENCE.md` (§1–16, 12-й ключевой документ, схемы
  маркера/манифеста/расписки); /help-kaif цитирует её параграфы; policyChanges; /kaif-fork починен.
- **Весь полевой беклог bugs/10–19 — DONE** (заведён из метасинтеза researches/07 в начале цикла).
- **Постоянный полигон** `npm run test:core`: 6 сводов, ~120 проверок по матрице 8 полевых
  профилей. Два адверсарных ревью субагентами поймали блокер (v1-adopt-current) и дедлок
  (judge-вердикт) ДО пуша. Сборка на закрытии: 12 docs / 47 FULL / 139 бандл / 569 модулей.

### Сессия 2026-07-27 (продолжение) — развилки отвечены, Фаза 1 плана 20 отгружена ✅
Владелец ответил на интервью 004 (решения №15–20 в MASTER_PLAN §7; ответы закоммичены дословно):
единый релиз 2.0 без промежуточного 1.7 · сигнатурные якоря карты модулей · локализация через
`i18n`-поле + эвристику письменности · записка одним полным EN-документом · kaif-provenance
опциональным инструментом · **автономность полная** (эскалация = интервью + пауза).
**Фаза 1 (полевые фиксы) реализована и верифицирована** (коммит `3fe770c`, build 45):
- Ядро: ANON из маркера (**фикс GH issue #1**), ignore-first для транзиентов, легаси наследует
  agents и не стирает чекпоинты, честные контекст-строки, «unchanged» вместо ложного «replaced»,
  скан плейсхолдеров по всем зеркалам/сферам, сверка содержимого зеркал, предупреждение о сфере
  без библиотеки, команда `sync`, resync не возвращает origin-tied анониму.
- Сборка/валидатор: `codename` в version.json, TEMPLATE_NOTES честно про 1.6 с вычисляемым
  кодовым именем, карта `assets` в манифесте, нейтральные шапки CORE/LOADER, **три стража
  доказаны красными на сломанных версиях** (кодовое имя · `<COMMIT_COMMAND>`-аргумент · бренд).
- Верификация: **19/19 песочных проверок** (S1 свежая · S2 анонимная — репро issue #1 снят ·
  S3 легаси 1.4 · S4 update) — скрипт `sandboxes.mjs` сессионный.
- Баги: 18, 19 — ✅ DONE; 10, 11, 14 — 🟡 partial (хвосты явно адресованы в Фазы 3–4).
- Попутно пойман класс EXP-0007 на себе: PS5.1 `Out-File` пишет BOM → новый код стража сделан
  BOM-терпимым.

### Сессия 2026-07-27 — KAIF 2.0 «Excellent KAIF» взят в работу: ресёрчи всех 6 эпиков + беклог дефектов поля 🟢
Владелец поставил **идею 18** (`ideas/18_kaif_2.0.md`): масштабная версия 2.0 из шести эпиков;
приоритет — эпик №1 (модульность, понятная машинерии; обновление машинерией, не умом агента).
- **Первоисточники владельца закоммичены дословно** (чек-лист, шаг 15): идея 18, идея 17
  (улучшения навыков), **восемь** полевых отчётов об update 1.6 в `ideas/updade_kaif_field_reports/`
  (было пять — владелец добавил три и переложил), эталоны академического стиля BSUIR (2 PDF).
  Планы 17/18 сшиты с 2.0, ссылки на отчёты починены.
- **`researches/07` — метасинтез восьми отчётов** (план 18, шаги 1–2 ✅): матрица 8 профилей ×
  (tracking, язык, маршрут, скачок); **10 системных пересечений** П1–П10 (топ: нет машинного
  диффа версий — 7/8; templateNotes 1.6 описывают 1.5 — 6/8; обновление воюет с локализацией —
  6/8); контракт «сработало отлично — не ломать» (святой инвариант держится во всех восьми);
  расхождения отчётов; выводы для эпика №1 (три слоя: карта модулей → провенанс → гейты
  содержимого; готовые опоры в текущем коде названы с точностью до строк). Метод: отчёты 04/05/07
  прочитаны лично; все 8 извлечены субагентами по схеме с дословными цитатами; **все 265 цитат
  проверены механически** (0 несовпадений); карта машинерии снята с кода.
- **Баги 10–19 заведены** — 10 классов дефектов поля, каждый с Repro (релиз утверждает чужую
  версию · гейты проверяют не то свойство (= GH issue #1) · снапшот без происхождения ·
  анонимный путь второсортен, гейт ловит фамилию владельца · легаси-путь слеп · update против
  локализации · /kaif-fork даёт неработоспособный origin · самозаверяемые чекпоинты ·
  инсталлятор нарушает ignore-first · пакет мелких контрактных дефектов).
- **Ресёрчи эпиков 2–6** (`researches/08`–`12`): /resume велит читать 5 канон-доков из 11, правила
  drive-by набросов в каноне нет (08); полевое ТЗ на инструменты plans/17 уже есть — «конвенция
  [AI] без инструмента протухнет первой» (09); GH-трекер: ровно один issue, актуален, фикс = грань
  bugs/11 (10); таблица навыков README — 14 строк на 26 навыков, 7 строк сгруппированы (11);
  стиль пояснительной записки снят с BSUIR-PDF программно (pdf-parse), терминология записки =
  имена модулей карты эпика №1 (12).
- **`plans/20` — зонтичный план 2.0** (7 фаз: фиксы поля → дешёвые эпики → проектирование и
  реализация модульности → инструменты гвардрейлов → записка → релиз по матрице 8 профилей) +
  **`interviews/interview_004`** — 6 развилок владельцу (промежуточный релиз 1.7 · якоря карты
  модулей · политика локализации · язык/место записки · место kaif-provenance · автономность).
  ⚠️ Примечание: план 20 и интервью 004 уехали в один коммит с ресёрчами 08–12 (git add -A) —
  сообщение коммита их не называет.
- Урок сессии — **EXP-0015** (извлечение из массива документов субагентами + механическая
  проверка цитат).

### Сессия 2026-07-26 (продолжение) — схемы отрисованы и вплетены в README 🟢
Интервью 003 отвечено владельцем: **SVG-файлы, ноль mermaid · «Было/Стало» первой · мем остаётся
первым · один файл, починить переключатель языков, без под катов**. Уточнение по стилю пришло по
ходу: **меньше «попсовых» картинок — технические чертежи, серьёзный деловой язык**; наезд текста на
графику недопустим. Метафора «айсберг» снята.
- **Спайк (Фаза 1) — наблюдением:** локальный `.svg` и `<picture>` в нашем конвейере **рендерятся,
  причём вектором** (в PDF ноль растровых объектов), тёмная ветка выбирается корректно, а фенс
  mermaid **печатается кодоблоком**. Решение Q1 подтверждено фактом, а не рассуждением.
- **`tools/build-diagrams.mjs`** — единственный источник схем: 3 чертежа × светлая/тёмная × EN/RU =
  **12 файлов в `assets/`**. Чертежи: «Знание по сессиям» (честный провал на 3-й сессии нарисован),
  «Одна сессия целиком», «Из чего состоит развёрнутый KAIF» (4 слоя). SVG руками не правятся.
- **Гейт ширины текста в сборке** (`fit()`): подпись шире контейнера валит генерацию — наезд текста
  на графику невозможен по построению. Введён после того, как первый вариант дал ровно такой дефект.
- README: схемы в **обеих половинах**; ASCII-блок на 97 символов (рвался на мобильном) удалён;
  **переключатель языков добавлен в RU-половину**; `assets/` вписана в карты (README EN+RU,
  `AGENT_GUIDE`, `PROJECT_STRUCTURE_EXTERNAL_MAP`). `.gitattributes`: `*.svg text eol=lf`.
- Проверено: все 6 локальных `<img>` загружаются; `README.pdf` перегенерирован (1 018 → 1 174 КБ).
- **Правка по замечанию владельца:** на графике знание прирастало и между сессиями — «так не бывает,
  накопление, как заряд конденсатора, идёт только во время сессий». Полка между сессиями формально
  была, но шириной 28 px из 200 и читалась как непрерывный подъём. Интервал разведён до 34 % шага,
  полосы «сессия идёт» затенены и вынесены в легенду, серая кривая явно лежит в нуле между сессиями.
  Урок — **EXP-0014: схема есть утверждение, судить её надо как утверждение**; формальная
  корректность геометрии не спасает, если различие не видно глазом.
- **Осталось:** шапка первого экрана (слот `<h3><em>`, ряд навигации, бейджи 9 → 5–6) и Фаза 5 —
  просмотр на самом github.com в обеих темах и на телефоне + проверка 8 якорей + judge-проход.

### Сессия 2026-07-26 — лицо KAIF: разведка и план наглядного README ✅
Владелец поставил задачу вне очереди 1.7: **сделать ценность KAIF понятной с первого взгляда на
GitHub** — сейчас README это 650 строк почти сплошного текста. Бриф владельца дословно: «объяснить
любому за пять минут на пальцах, и это будет не примитивное объяснение, а именно глубокое, но
понятное»; мем-тон бренда — разрядка, а не тезис. Зафиксировано в заметках владельца `AGENT_GUIDE.md`.
- **`researches/06_readme_visual_face.md`** — разведдок (внешняя правда снята наблюдением, затем
  перепроверена адверсарно: 13 утверждений опровергнуто, 15 уточнено ДО записи). Главное:
  инлайновый `<svg>` GitHub удаляет целиком · `style=`/`class=` вырезаются · шрифты в SVG не грузятся
  (CSP) · тёмная тема только парой файлов через `<picture>` · **mermaid печатается в `README.pdf`
  сырым кодом** (наш `md-to-pdf` не исполняет JS), а PDF назван в `GOAL.md:32` → рекомендация
  «SVG-файлы, ноль mermaid». Разобрано 25 живых README (12-factor-agents, spec-kit, ruff, aider,
  vhs, biome, cline…): первая картинка у сильных — **доказательство или движение**, не декорация.
- **Черновые схемы отрисованы и показаны владельцу** (три листа: «Было / Стало» · «Петля
  непрерывности» · «Просто и глубоко»), чтобы решение принималось глазами, а не по описанию.
- **`interviews/interview_003_readme_visual_face.md`** — 6 развилок владельцу (носитель графики ·
  порядок схем · судьба мема и баннера · один файл против `README.md`+`README.ru.md` · объём текста ·
  демо-каст). **Гейт: до ответов `README.md` не трогаем.**
- **`plans/19_readme_visual_face.md`** — 6 фаз с трассировкой находок разведки в шаги. Фаза 1 —
  **обязательный спайк**: рендерится ли локальный SVG в нашем PDF-конвейере (НЕ ПРОВЕРЕНО).
- **`bugs/09_stale_facts_in_wrapper_docs.md`** — попутная находка: четыре документа лгут, что
  `README.pdf` в gitignore (он коммитится, 11 коммитов), три документа несут устаревшие счётчики
  (21/18 навыков и «= 37 блоков» против реальных 26 и 44), переключателя языков нет в русской
  половине README. Опасно тем, что `AGENT_GUIDE.md:201` — критерий приёмки сборки.
- Полезная нагрузка `framework/` **не тронута**: корневой README развёртываемым проектам не
  поставляется, пересборка ядра для этой работы не нужна.

### 🎉 v1.6 (2026-07-24) — «Homeostatic KAIF» ✅ ОПУБЛИКОВАН
**Релиз: https://github.com/MikalaiKryvusha/KAIF/releases/tag/v1.6** (заголовок по канону, ноты EN+RU
в стиле v1.5, 5 ассетов: тонкий `KAIF.md` · `KAIF-CORE.mjs` · `KAIF-CORE-BUNDLE.md` ·
`kaif-manifest.json` · `KAIF-FULL.md`). README EN+RU: бейдж Guardrails + секция «Homeostatic KAIF»
в обоих языках, PDF перегенерирован. Judge перед публикацией — VERIFIED. **Пост-релизный смок:
загрузчик БЕЗ флагов с дефолтного релизного канала → «KAIF 1.6 deployed mechanically», гвардрейлы
грепом на месте, маркер 1.6 — продакшен-путь подтверждён.** План 16 — DONE целиком (задуман,
утверждён, реализован и отгружен в один день). Дальше: полевая обкатка конвенций → эпик 1.7
(`plans/17`, инструменты гвардрейлов).

### Сессия 2026-07-24 (продолжение) — эпик 1.6 «Homeostatic KAIF» РЕАЛИЗОВАН ✅ (остался только релиз)
- **Фазы 1–4 плана 16 отгружены в оба слоя** четырьмя шаговыми коммитами (правило одного шага —
  которое сами же и вводили): `7868355` канон-правила (разведка до кода, цитируй план, git-гигиена,
  write-gate, три двери, класс-не-экземпляр, стражи+проверка стражей, верификация находок, гейты
  наблюдения, канонический порядок, источник у фактов UI) · `ee331a8` форматы знаний («Решения без
  владельца» при закрытии, EXPERIENCE Repro/Not-for + цитирование уроков, канон-карта, инвентарь
  паритета, процессный док по просьбе) · `d482b81` судья перед КАЖДЫМ push/деплоем + guardrail hunts
  (KAIF-патч 3 в вендореном fable-judge) + правило одного шага в циклах + деплой-чеклист 5 гейтов в
  `/release` + обоснование тест-правок в коммите · `6bce77e` конвенция провенанса `[AI]`/`[AI-ed]`
  (пометка = очередь на приёмку; фрод «Unmarked AI text»).
- **Верификация (Фаза 5, без релиза): всё наблюдением.** Сборка 44 блока / 134 в бандле; песочница
  свежей установки — exit 0, `check` 52 файла + 108 артефактов; update-песочница 1.5→1.6-test штатной
  командой `update` — владельческое байт-в-байт, правленый файл kept (ровно 1 diverged), нетронутое
  заменено контентом 1.6, версия в маркере; греп-проверка правил: 30/30 в развёрнутой песочнице (EN),
  21/21 в RU-обвязке. Идеи 15/16 — DONE со статусами и секциями решений. Урок — EXP-0013 (тестируй
  update штатной командой на чистой установке; аномалия песочницы → сначала «что я сам сделал?»).
- **Осталось: релиз v1.6 «Homeostatic KAIF»** — README EN+RU, релиз-ноты, публикация по `/release`
  (нужно явное «да» владельца).

### Сессия 2026-07-24 — эпик 1.6 задуман: план по рекомендациям Fable 5 ✅ (план готов, ждёт развилок)
- Владелец передал два отчёта Fable 5 из боевых проектов (эпик «книга правил» + сессия NDim и корпус
  41 бага): как фреймворком выжимать надёжность из слабых моделей (класс Opus). Первоисточники
  закоммичены **дословно до любых правок** (само правило из отчёта), затем переномерованы из-за
  коллизии с закрытыми идеями: `ideas/15_kaif_weak_model_guardrails.md` +
  `ideas/16_kaif_for_weak_models_report.md` (контент не тронут).
- **`plans/16_kaif_1.6_weak_model_guardrails.md`** — план эпика 1.6: 4 фазы по возрастанию цены
  (канон-правила → шаблоны/форматы → судья+циклы+деплой → инструменты `kaif-provenance` и
  `/derive-styleguide`) + релиз; трассировка всех 26 рекомендаций по таблицам. Формула эпика:
  «слабой модели нельзя доверить суждение, но можно доверить процедуру».
- **Развилки отвечены владельцем в тот же день** (журнал решений MASTER_PLAN §7, №12–14):
  кодовое имя **`KAIF 1.6 — Homeostatic KAIF`**; объём — фазы 1–3 + конвенция провенанса `[AI]`,
  инструменты выделены в **эпик 1.7** (`plans/17_kaif_1.7_guardrail_tools.md`, не в работе до
  обкатки 1.6); судья перед **каждым** push; процессный документ на волну багов — без автоматики,
  по явной просьбе. Ответы владельца закоммичены дословно до вшивания в план.

### Сессия 2026-07-16 (продолжение-3) — эпик 1.5: Фаза 3 (языки: механизм + ru) 🟡→✅ частично
- **Языковой механизм работает насквозь**: пакеты `framework/templates/languages/<lang>/` едут в бандле
  (61 блок); ядро подменяет 8 owner-доков выбранного языка (GOAL, KAIF_FRAMEWORK, 6 README — набор Q6)
  и дописывает **триггер-алиасы** навыкам из `skill-triggers.json` (навыки EN, матчинг на языке
  владельца — Q6-вариант «а»); `<OWNER_LANGUAGE>`-нота «две аудитории — два языка» в AGENT_GUIDE.
- **Пакет ru отгружен** (основа — переводы из нашей же обвязки, дегенерализованы; ревью — за владельцем).
- Песочницы: `--lang ru` → русские owner-доки + 26 алиасов (вкл. `.roo/commands/`), agent-доки EN;
  `--lang de` → EN-фолбэк + пункт перевода в задании адаптации.
- Остальные 8 пакетов — в автономном пуле (контент, механизм готов).

### 🎉 v1.5 (2026-07-17) — «Tested KAIF» ✅ ОПУБЛИКОВАН
**Релиз: https://github.com/MikalaiKryvusha/KAIF/releases/tag/v1.5** (заголовок по канону, ноты EN+RU,
5 ассетов: тонкий `KAIF.md` · `KAIF-CORE.mjs` · `KAIF-CORE-BUNDLE.md` · `kaif-manifest.json` ·
`KAIF-FULL.md`). Пост-релизный смок: загрузчик БЕЗ флагов с дефолтного релизного канала → sha256 ok →
«KAIF 1.5 deployed mechanically» — продакшен-путь подтверждён.
- Корневой `KAIF.md` — тонкий (174 строки); полное ядро — `dist/KAIF-FULL.md` (44 блока); Slim retired;
  README EN+RU переписан + PDF; judge-проход перед публикацией выполнен (1 расхождение исправлено).
- Отгружены идеи **12** (тонкая установка + fable), **13** (Tested: TESTING_FRAMEWORK + маркеры),
  **14** (механический update). Планы 13/14/15 — DONE. Отчёт владельцу — `researches/05`.
- Экономия: чтение ×23, когнитивное письмо ×66. Полевая сертификация: gemma-4-12b насквозь (ДЗ-02),
  update реального 1.4-проекта байт-в-байт бережно (ДЗ-03); 12 полевых находок механизированы.

### Сессия 2026-07-17 (продолжение) — ДЗ-03 ЗАЧТЁН + замер экономии: **Фаза 4 закрыта целиком** ✅
- **ДЗ-03 (update реального 1.4-проекта ndim)** — полный путь: бутстрап → «running as an UPDATE» →
  механика (существующее цело, 131 файл добавлен) → UPDATE-задание → агент проекта смёржил новости
  1.5 в свои разошедшиеся файлы → update-verify → самоочистка. **Святой инвариант держится**: контент
  владельца байт в байт. Пять полевых находок закрыты, главная — детект update по СУЩЕСТВОВАНИЮ
  kaif.json (равные версии ломали старый детект), маркер наследует все поля включая кастомные
  (`1686b8e`); download-and-execute требует разового разрешения у осторожных харнессов (в релиз-ноты).
- **Замер экономии**: чтение ×23 меньше (227 729 → 9 913 байт), когнитивное письмо ×66 меньше
  (≈286 372 → ≈4 350 байт) — цель «на порядок» перекрыта.
- **Фаза 4 плана 13 закрыта. Эпику остался только релиз (Фаза 5)** — по явному «да» владельца:
  bump 1.5, swap корневого KAIF.md на тонкий, retire Slim, README EN+RU, judge-проход, публикация
  с 5 ассетами.

### Сессия 2026-07-17 — полевая сертификация тонкой установки на слабой ЛЛМ ✅ (ДЗ-02 ЗАЧТЁН, bugs/01 DONE)
- **5 прогонов gemma-4-12b** (Zoo Code 3.68 + эпизод Cline 4.0.8, llama-swap): прогон №5 — весь путь
  **без содержательной помощи** (бутстрап `KAIF-BOOT:` → sha256 → 52 файла + 108 артефактов → 7
  чекпоинтов командой → `verify-final` → самоочистка). Микро-сбои модели самовосстановились.
- **7 полевых находок → механизированы:** LF-канон артефактов (`dist/** -text`, bugs/04 с честной
  историей двух диагнозов) · write-back+re-sync копий (bugs/05) · **команда `checkpoint`** (слабые
  модели ломаются на механике редактирования — заменили командами) · **команда `sphere`** + снапшот
  маркера + самолечение kaif.json в verify-final · правила для слабых моделей в шапке задания ·
  петля таймаутов Cline+llama-swap (настройки харнесса) · баг Zoo `apply_diff` (номера строк в
  REPLACE) — **зарепорчен от имени владельца: Zoo-Code-Org/Zoo-Code#910**.
- Мониторинг вёлся тремя каналами: файловая система (Monitor), task-хранилище Zoo/Cline
  (`globalStorage`), llama-swap `/logs` (127.0.0.1:8080).
- `homeworks/02` → DONE (вердикт-таблица внутри) · `bugs/01` → DONE (класс «слабая ЛЛМ интерпретирует
  вместо копирования» устранён архитектурно). Остался полевой тест ДЗ-03 (update 1.4-проекта) и замер
  экономии токенов — потом Фаза 5 (релиз).

### Сессия 2026-07-16 (продолжение-4) — идеи 13/14 влились в 1.5: Tested KAIF + update-механика ✅
- **Фаза 3 закрыта целиком**: все 10 языков (en + 9 пакетов: ru, es, pt, fr, de, zh-Hans, ja, hi, ar) —
  по 8 owner-доков + таблица триггер-алиасов на 26 навыков в каждом; бандл 133 блока. Наблюдением
  пойманы и закрыты: невалидный JSON de-таблицы и регистр zh-Hans (регистронезависимый матч в ядре).
- **Идея 13 → план 14 → DONE**: новый (11-й) ключевой документ **`TESTING_FRAMEWORK.md`** (7 принципов
  ISTQB, универсально по сферам) + конвенция маркеров **`[NOT-TESTED]`/`[TESTED: дата · как]`** на всё
  генерируемое (ложный TESTED — фрод для judge; строка в сфере programming); вшито в чек-листы/роутеры/
  BUG_FIXING/CLAUDE.md обоих слоёв. Ядро — **44 блока**, бандл — **134**. Кодовое имя релиза сменено:
  **`KAIF 1.5 — Tested KAIF`**.
- **Идея 14 → план 15 → DONE**: уважительное обновление механизировано — sha256-слепки в
  `deploy-manifest`, команда **`update`** (заменяет нетронутое, хранит разошедшееся и контент владельца,
  наследует язык/системы/sphere из kaif.json, меняет само ядро, `KAIF_UPDATE_TASK.md` + `update-verify`),
  `templateNotes` в бандле, **legacy-детект** (1.4-проект + тонкий бутстрап = update, не переустановка),
  `kaif:update`-хендл, навык `/kaif-update` переписан. Песочницы: update/no-op/legacy — зелёные;
  правленые владельцем файлы переживают update всегда.
- Планы 14/15 — DONE в день создания; спутники эпика отмечены в шапке плана 13.

### Сессия 2026-07-16 (продолжение-2) — эпик 1.5: Фаза 2 (машинерия установки) ✅
- **Thin-KAIF-конвейер построен и проверен песочницами** (judge: VERIFIED WITH CAVEATS):
  тонкий `dist/KAIF.md` (**166 строк** против 3495 у полного; 3 бутстрап-шага с чекпоинтами
  `KAIF-BOOT:`) → `KAIF-LOADER.mjs` (sha256-верификация, каналы release/main/`--source`) →
  `KAIF-CORE.mjs` (распаковка 52-блочного бандла, autofill плейсхолдеров из реалий проекта, навыки
  сразу для 5 систем вкл. новый адаптер **Grok Build**, wiring, языковой хук, `KAIF_ADAPTATION_TASK.md`
  с чекпоинтами `KAIF-ADAPT:`, `verify-final` с самоочисткой; ядро остаётся в `.kaif/kaif-core.mjs`
  как бэкенд `kaif:*`). **Anonymous by design**: скип 4 origin-tied навыков, механический стрип
  авторской заметки (маркеры в шаблоне `KAIF_FRAMEWORK.md`), греп-гейт утечек, расширенная самоочистка.
- Сборка эмитит `dist/`: тонкий KAIF.md · KAIF-CORE.mjs · KAIF-CORE-BUNDLE.md · kaif-manifest.json ·
  оффлайн-`KAIF-FULL.md`; `check-framework` валидирует dist. Корневой `KAIF.md` не тронут до Фазы 5.
- Песочницы вскрыли и закрыли 3 бага: BOM-клоббер пользовательского package.json · мёртвый `check`
  после самоочистки · утечки анонимности. Урок — EXP-0007.
- Осталось по эпику: Фаза 3 (языки ×10 + триггер-алиасы), Фаза 4 (поле, слабая ЛЛМ), Фаза 5 (релиз:
  swap корня на тонкий, retire Slim, README, ассеты).

### Сессия 2026-07-16 (продолжение) — эпик 1.5: интервью закрыто + Фаза 1 (fable-method) ✅
- **`interview_002` отвечено владельцем** (Q4 уточнён в чате) — решения №6–11 в `MASTER_PLAN.md` §7:
  гибрид-вендоринг fable · сферы модернизируем идеями fable (адаптеры целиком не тянем) · judge
  обязателен в ритуалах · канон → «самодостаточен origin» + оффлайн-`KAIF-FULL.md` в релизах · Node
  никогда молча · 10 языков (en,ru,zh-Hans,es,hi,ar,pt,fr,de,ja) + триггер-алиасы · Slim retire.
- **Фаза 1 выполнена:** 4 навыка fable вендорены дословно (`framework/skills/fable-*` + references;
  EN, шапки-атрибуции с пином v1.4.0 и списком патчей: domains→сферы, GSD→циклы, suite→апстрим);
  сферы (`_template`, `_index`, 4 библиотеки) получили дисциплинарные секции (binding evidence set,
  порядок авторитетов, верификация наблюдением, таблица фродов для judge, «готово на примере»);
  ДНК-сшивка в оба слоя: чек-листы AGENT_GUIDE (пункт «исполняй по fable-циклу» + подсекция у роутера),
  BUG_FIXING (intent gate + twin check), циклы (`/dayloop`/`/nightloop`/`/autoloop` — исполнение пункта
  по fable + ОБЯЗАТЕЛЬНЫЙ judge-проход перед «готово»), `/release` (Step 4.5 judge), git-политика =
  стоячая авторизация коммитов, `CLAUDE.md` (fable-семейство в списке ритуалов), `.claude/skills/fable-*`
  (EN, dogfooding новой языковой политики «agent-only — EN»).
- **Сборка зелёная: 43 блока** (10 доков + 6 README + 26 навыков + 1 распаковщик). ⚠️ Интерим: полный
  `KAIF.md` вырос (3495 строк) — в релиз не попадёт, в 1.5 уходит тонкий (решение Q2).
- **Bug 03 найден и закрыт** (`bugs/03_DONE_build_breaks_on_crlf_checkout.md`): сборщик падал на
  Windows-CRLF-чекауте (срезка авторского комментария предполагала LF) — оба регэкспа → `\r?\n`.
  Уроки: EXP-0005 (CRLF), EXP-0006 (паттерн вендоринга).
- **Push разблокирован** владельцем (снята галочка «Block command line pushes...») — origin синхронен.

### Сессия 2026-07-16 — эпик 1.5 «Thin KAIF»: Фаза 0 (проработка) ✅
- **Окружение:** проект переехал на локальную Windows-машину владельца (`d:\work\ai_sandbox\KAIF`,
  свежий клон origin). Разработка деанонимизирована: локальный git-конфиг — `Mikalai Kryvusha
  <kotkrinik@yandex.ru>` (прежние коммиты шли под noreply-адресом GitHub); связь с origin сохранена,
  `gh` авторизован. ⚠️ Для линковки коммитов с профилем GitHub адрес должен быть подтверждён в
  настройках аккаунта (и снята галочка «Block command line pushes that expose my email»).
- **Идея 12 (эпик KAIF 1.5 — Thin KAIF) принята владельцем в работу.** Выполнена глубокая проработка,
  начиная с части 2 (fable-method), как требует идея:
  - `researches/03_fable_method.md` — fable-method (github.com/Sahir619/fable-method, v1.4.0) изучен по
    первоисточнику: 4 навыка think/act/prove/grow, принудительные артефакты на точках решения
    (INTENT/AUTH/TWINS/PENDING), доказательная база 15 eval-раундов. Главный урок: «слабые модели следуют
    правилам на точках решения, а не правилам в списках» — прямое лекарство от нашего Парето-пробела
    (researches/02, bugs/01/02). Рекомендация: гибрид «vendor 4 навыка дословно + сшивка точек вызова в ДНК».
  - `researches/04_thin_kaif_installation.md` — карта текущей установки (машинерия закрывает только
    Stage 1/1.5 + Zoo/Roo; 13 точек когнитивной нагрузки, неустранимы 3: понимание проекта/цели/архитектуры)
    + целевая архитектура: тонкий `KAIF.md` (~300 строк, 3 бутстрап-шага с чекпоинтами) → `KAIF-LOADER.mjs`
    → `KAIF-CORE.mjs` из origin (языковые шаблоны ×10, навыки сразу для 5 систем, autofill, wiring,
    anonymous by design, `--verify-final`). Grok Build проверен веб-разведкой: стандарт SKILL.md,
    `.grok/skills/` — механизируем.
  - `plans/13_kaif_1.5_thin_kaif.md` — план эпика: Фазы 0 (✅) – 5 (релиз `KAIF 1.5 — Thin KAIF`).
  - `interviews/interview_002_thin_kaif_and_fable_method.md` — 7 развилок владельцу.

### v1.4 (2026-07-08) — «Savvied KAIF» ✅
Отгружены три одобренные идеи (09+10+11) в обоих слоях; `KAIF.md` — **39 блоков** (10 доков + 6 README +
22 навыка + 1 распаковщик). Релиз опубликован: https://github.com/MikalaiKryvusha/KAIF/releases/tag/v1.4
(приложены `KAIF.md` + `KAIF-SLIM.md`).
- **Идея 09 → «ОПЫТ» (`EXPERIENCE.md`)** — растущий греп-дружелюбный журнал уроков; **теги инлайн на
  каждой записи, без общего облака** (уточнение владельца). Навык `/experience` (capture+recall); встройка
  recall/capture в чек-лист `AGENT_GUIDE`, `PHILOSOPHY` («Учись один раз»), `BUG_FIXING`, циклы, `resume`,
  `refresh-context`, `CLAUDE.md`, карты; усиление blast-radius. Корневой `EXPERIENCE.md` засеян (dogfooding).
- **Идея 10 → опциональные хуки-энфорсеры** — секция «Hooks (optional enforcement)» в адаптерах
  zoo-code/claude-code + §14 ядра; 4 несущих правила, энфорс опционален (бьёт в корень bugs 01/02).
- **Идея 11 → ленивая загрузка контекста** — контекст-роутер «тип задачи → минимальный срез» в `AGENT_GUIDE`.
- Все идеи/планы 09/10/11 и 10/11/12 помечены `DONE`.

### Сессия 2026-07-08 — разведка конкурентов (идея 08 выполнена)
- **Идея 08 одобрена и выполнена** (этапы 1–3). Артефакт: `researches/02_competitor_landscape.md` —
  ландшафт (EGC/BMAD/Spec Kit/Claude Flow/SuperClaude/Trellis/Ralph/SAM + слои памяти agentmemory/
  memsearch/OpenViking), честное сравнение с KAIF (что у них лучше / где KAIF выигрывает) и Парето 80/20.
- **Вывод Парето:** отставание KAIF — в НАДЁЖНОСТИ ИСПОЛНЕНИЯ (правила-как-проза не исполняются) и
  ПАМЯТИ ОБ ОПЫТЕ, а не в «мало фич». Витальные 20% оформлены отдельными идеями на одобрение:
  **09** (Decision Log), **10** (опциональные хуки-энфорсеры), **11** (ленивая загрузка контекста).
  Осознанно НЕ копируем: рои агентов/персоны, векторные БД, обязательный TDD, дашборды (анти-Парето).
- Идея 09 (взвешивание предложения Gemini) — Decision Log подтверждён рынком; уверенность ↑.

### Сессия 2026-07-07 — фикс bug 02 + идея 08 (обслуживание, без смены версии)
- **Bug 02 (`bugs/02_loops_self_stop_on_context_overflow.md`) — 🔧 фикс отгружен.** Циклы
  `/dayloop` и `/nightloop` сами обрывали ход на «переполнении контекста» вместо работы до упора.
  Причина — формулировка: `dayloop`, шаг 7 называл «контекст заполнен» триггером сердцебиения, а гарда
  `autoloop` («контекст/лимиты — забота харнесса, не стоп-условие») в этих навыках не было. Правка в
  **обоих слоях** (`.claude/skills/{dayloop,nightloop}` + `framework/skills/{dayloop,nightloop}`):
  убран триггер, добавлен гард, `ScheduleWakeup` переопределён как механический запасной механизм.
  `KAIF.md` пересобран (37 блоков), артефакт `KAIF.md` в релизе **v1.3** обновлён (`--clobber`, описание
  и README не тронуты). Ждёт полевой проверки в реальном `/dayloop`.
- **Идея 08 (`ideas/08_competitor_research_and_pareto_vision.md`) — ❓ ожидает одобрения владельца.**
  Лёгкая разведка конкурентов (EGC + аналоги) БЕЗ навыка глубокого исследования → видение по Парето 80/20.

### v1.3 (2026-07-06) — «Slim KAIF» ✅
Реализована идея 07 (`ideas/07_DONE_kaif_light.md`) — второй, лёгкий способ принять KAIF:
- **`KAIF-SLIM.md` — один файл, который *и есть* фреймворк на месте** (не распаковывается в ~40 файлов).
  Уронил в корень мини-проекта, рядом свой `GOAL.md` — и работаешь по KAIF без дорогого деплоя. Сохраняет
  суть (вынесенная память `STATUS.md`, накопление знаний по требованию, ограниченная автономность,
  KISS+Оккам, ритуалы-как-поведение), отбрасывает тяжёлую машинерию (карты, устанавливаемые навыки, сферы,
  адаптеры, анонимка, жизненный цикл). Английский, как и `KAIF.md`.
- **Источник** `framework/KAIF-SLIM.md`; `tools/build-framework.mjs` эмитит корневой `KAIF-SLIM.md` со
  штампом версии (правило «корень генерируется из `framework/`» соблюдено). Самопроверка `KAIF.md` не
  затронута — Slim не входит в 37 блоков ядра.
- **README EN+RU** — хирургические правки: бейдж Slim, вводная заметка, секция «Slim KAIF» с таблицей
  Slim vs Full и якорями `#slim-en`/`#slim-ru`, строка в структуре репозитория; PDF перегенерирован.
- Версия → **1.3**; `KAIF.md` пересобран (2900 строк, 37 блоков).

### v1.2 (2026-07-03) — «Anonymous KAIF» ✅
Реализована идея 06 (`ideas/06_DONE_extended_skills.md`) и фикс бага 01 по плану 09
(`plans/09_kaif_1.2_anonymous_kaif.md`):
- **Механическая распаковка (фикс bug 01 — главный шаг):** в ядро встроен скрипт-распаковщик
  `framework/kaif-unpack.mjs` (блок `FILE:` в `KAIF.md`) — извлекает все блоки дословно по каноническим
  путям; `--check` — валидация манифеста; `--agent zoo-code` — механическая трансляция навыков в
  `.roo/commands/` + `AGENTS.md` + `.roo/rules/kaif.md`; `--anonymous` — пропуск origin-привязанных
  навыков. §8 ядра: этап 1 — механический, этап 1.5 «валидация → доделка до 100%» обязателен, «FILE-метка
  — закон», `KAIF_DEPLOYMENT_PLAN.md` — строгий контракт. Протестировано в чистой песочнице (37/37,
  21/21 команда, анонимный 34/34). Полевая проверка — `homeworks/01` (за владельцем).
- **Анонимная установка:** третий параметр команды-инициатора (режим установки standard|anonymous);
  правила вычистки автора и отвязки origin (§8), `tracking: "anonymous"` в `.kaif/kaif.json` (§12).
- **Новые навыки:** `/fix-vision` (зафиксировать визионерские сообщения владельца из чата в документы) и
  `/what-next` («что дальше?» — шаги наибольшей ценности); EN-шаблоны + RU-обвязка. Итого **21 навык /
  37 блоков**.
- **Интервью:** лимит «1–5, не больше» снят → обычно 1–5, при необходимости до 10 (шаблон + обвязка).
- **Zoo Code — приоритет №1** (указание владельца; ZOO как «зоопарк», форк-цепочка Cline → Roo Code →
  Zoo Code, совместим с Roo): новый адаптер `zoo-code.md` (верифицирован по docs.zoocode.dev), roo-code
  помечен архивным; адаптеры codex/cursor/copilot/cline/windsurf актуализированы по официальным докам
  (2026-07-03); `researches/01_agent_skill_formats.md` — конвергенция экосистемы на `AGENTS.md` +
  стандарте Agent Skills; Ollama молча обрезает контекст (форензика bug 01).
- **Правки владельца на GitHub** (README-описания, ветка e5e6072) подтянуты и сохранены дословно.
- README EN+RU: три параметра инициатора, механическая распаковка, анонимный режим, таблица навыков 21,
  бейдж 1.2; PDF перегенерирован. Трейлер коммитов → `Claude Fable 5`.

### v1.1 (2026-07-01) — «Структурированный KAIF» ✅
Идея 05 по плану 06: версионирование `x.y`; ядро `FRAMEWORK.md` → `KAIF.md`; `PHILOSOPHY.md` +10
принципов; новая структура ключевых документов (`GOAL.md`, `MASTER_PLAN.md`, две карты,
`KAIF_FRAMEWORK.md`); директории `ideas/ researches/ homeworks/` + README во всех шести; этапная
распаковка; навыки `/revision`, `/help-kaif`, interview A/B/C/D, kaif-remove-гейт. Причёсывание репо
(планы 07/08): релиз опубликован, беклог финализирован, README с картинкой и таблицами.

### v1.0 (2026-06-30) — дистилляция + KAIF ✅
Ребрендинг → KAIF; `framework/` → `KAIF.md`; жизненный цикл (`.kaif`, `kaif-*`, `tools/kaif.mjs`);
мультисферность; мультиагентность; валидатор; двуязычный README; dogfooding-обвязка.


---

### Дополнение переноса (2026-07-31, находки судьи эпика H — потерянные при миграции строки, восстановлены дословно)

Из раздела «Где мы сейчас» старого STATUS (шапка v2.0):

> **🎉 KAIF v2.0 «Excellent KAIF» ОПУБЛИКОВАН: https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.0**
> (2026-07-28, 6 ассетов вкл. новый `kaif-module-map.json`; ноты EN+RU с секцией «Upgrading from 1.x»).
> Пост-релизный смок с дефолтного канала — зелёный насквозь («KAIF 2.0 deployed mechanically», маркер 2.0,
> check 57 файлов + 116 артефактов, записка и 28 навыков на месте). GH issue #1 закрыт с релизом.

Из пула автономного беклога:

> Ревизия 2026-07-28 (судейский проход): пункты «8 языковых пакетов» (сделано в 1.5 — 9 пакетов лежат в
> `framework/templates/languages/`), «перевести kaif-* на русский» (отменено языковой политикой 1.6:
> agent-facing документы — EN, вендоренные копии с staleness-шапками) и «полная логика миграции»
> (механизирована эпиком №1 2.0: по-модульный update, adopt-current, синтетический слепок, /kaif-fork
> переписан) сняты из пула как закрытые/отменённые.

Из «Ожидает владельца»: «ДЗ-02 и ДЗ-03 зачтены, ДЗ-01 устарел в пользу ДЗ-02».

## Исторические снимки состояния (перенесены из «Где мы сейчас»)

<details><summary>Снимок 1.4 «Savvied KAIF» (для истории)</summary>

| Область | Статус |
|------|--------|
| `EXPERIENCE.md` + навык `/experience` (оба слоя), теги инлайн | ✅ собрано, самопроверка зелёная |
| Контекст-роутер + опциональные хуки-энфорсеры | ✅ готово |
| README EN+RU (структура/таблицы/навыки 22/бейдж 1.4) + PDF + версия 1.4 | ✅ готово |
| Релиз **v1.4** в GitHub (двуязычное описание, KAIF.md + KAIF-SLIM.md) | ✅ опубликован (2026-07-08) |

</details>

<details><summary>Снимок 1.3 «Slim KAIF» (для истории)</summary>

| Область | Статус |
|------|--------|
| `KAIF-SLIM.md` (источник + генерация в билде + штамп версии) | ✅ собрано |
| README EN+RU: бейдж/секция/таблица Slim vs Full + якоря | ✅ готово |
| Релиз **v1.3** в GitHub (KAIF.md + KAIF-SLIM.md) | ✅ опубликован (2026-07-06) |

</details>

<details><summary>Снимок 1.2 «Anonymous KAIF» (для истории)</summary>

| Область | Статус |
|------|--------|
| Механическая распаковка (kaif-unpack.mjs + манифест + валидация) | ✅ собрано, песочница зелёная |
| Трансляция навыков в Zoo Code (`--agent zoo-code`) | ✅ готово (21/21) |
| Анонимная установка (§1/§8/§12 + `--anonymous`) | ✅ формализована |
| Навыки `/fix-vision`, `/what-next` (EN+RU) | ✅ готово |
| Интервью до 10 вопросов | ✅ готово |
| Адаптеры верифицированы по официальным докам | ✅ zoo-code/codex/cursor/copilot/cline/windsurf |
| README EN+RU + PDF, правки владельца сохранены | ✅ готово |
| Релиз **v1.2** в GitHub | ✅ опубликован (2026-07-03) |
| Полевая проверка деплоя на слабой ЛЛМ в Zoo Code | 🟡 за владельцем (`homeworks/01`) |

</details>

---

## Унаследованный раздел «Где продолжить» (перенесён при введении летописи, 2026-07-31)

> Пункты ниже — историческая навигация сессий до эпика H; актуальное «где продолжить» всегда в
> STATUS.md.


00. **🔴 ГЛАВНОЕ: KAIF 2.1 (`plans/22` — зонтик).** Скоуп нарезан (идея 19 → researches/13 →
   8 эпиков A–H); интервью 005 отвечено (решения №21–28: кодовое имя «Strong», порядок (а),
   имена навыков утверждены). **✅ ЭПИК B ЗАКРЫТ ЦЕЛИКОМ** (`plans/23`, builds 73–80): все три
   полевых отчёта обновлений (ndim K1–K6, третий отчёт Д1–Д7 + предложение A) отработаны,
   bugs/20–30 все DONE, полигон 7/7 (свод s07 — 30+ стражей, все доказаны красными), три
   судейских прохода. Обновление 2.0+фиксы **готово к раскатке на парк владельца**.
   **✅ ЭПИК A ЗАКРЫТ** (2026-07-31, build 82): канон «Дисциплина планирования — лестница задач»
   (тест тяжести ≥2 из 5 · research перед эпиком · операционный план фазы N+1 на закрытии фазы N)
   в обоих слоях + новые навыки **`/plan-task`** и **`/plan-epic`** (EN-шаблоны + RU-обвязка +
   алиасы в 9 пакетах) + вплетение (чек-листы, роутер, propose-idea, все 3 цикла, README 28→30,
   записка §6 с группой Planning); сборка 49/30/141/586, полигон 7/7, судья — 4 находки закрыты
   до пуша. **✅ ЭПИКИ F и E ЗАКРЫТЫ** (build 84): /release Шаг 0 — жёсткий стоп идентичности
   (3 легальных исхода вкл. безымянный релиз по №28 · артефакт источника имени · «не
   переименовывай сам») и Шаг 6 — дельта-принцип нот (README-абзацы больше не приглашаются;
   механическая проверка вставляемости; разные источники вдохновения); канон-блоки обоих слоёв:
   «Гигиена документов и текста» (границы документов + ТЕКСТ ЧЕРЕЗ ФАЙЛЫ, 4 симптома, спутники) ·
   «Класс "вкус"» (макет+домашка, артефакт-не-ссылка, поля домашки) · «Разрешение на действие ≠
   авторство идентичности»; TESTING_FRAMEWORK — «наблюдатель — человек»; охота судьи «Identity
   without an author» (4-й KAIF-патч); строка-симптом в BUG_FIXING; EXP-0004 частично superseded.
   Судья F/E: VERIFIED WITH CAVEATS — 7 находок (вкл. «Шаг 6 запрещал то, что Шаг 0 узаконил» и
   перевёрнутый квалификатор №28) закрыты до пуша; сборка 49/30/141/588, полигон 7/7.
   **Попутно эпик I добавлен в зонтик** (отчёт KLAS «как делать слабые модели умнее», build 83).
   **Следующий шаг: эпик H (STATUS → PROJECT_HISTORY, 13-й ключевой документ)** — план 22 §6.
   Затем: G → I → C (`plans/24`) → D (`plans/25`) → Фаза R (релиз по явному «да»; заголовок —
   **«KAIF 2.1 — Strong KAIF»**, слово владельца дословно, №21).

0. **KAIF 2.0 «Excellent KAIF» (`plans/20`) — ЗАКРЫТ ЦЕЛИКОМ.** Развилки отвечены (№15–20), **Фазы 1–2
   отгружены ✅** (фиксы поля + README 26 строк с новой шапкой + /resume на полный канон + drive-by
   правило + шапки lifecycle-навыков; GH issue #1 прокомментирован фикс-коммитом). Дальше:
   **🎉 ЭПИК №1 (план 21) РЕАЛИЗОВАН ЦЕЛИКОМ — все фазы 5.1–5.6 ✅, гейт П1–П10 плана 18 закрыт
   без пропусков** (вердикт-таблица в plans/21 §6б; два хвоста вынесены с адресом: тегирование
   смен политики и схема маркера → Фаза 6 записки; bugs/11 и 15 остаются 🟡, bugs/12/13/14/17 —
   DONE). Сверх 5.4: **5.5** — анонимный bootstrap-update стал механическим (манифест переживает
   самоочистку; S13), синтетический слепок для легаси (`--baseline`; S14), anonLeakScan по путям
   манифеста + кластеры токенов владельца (полевой блокер 44 ложных ликов воспроизведён и убит),
   unwire npm-ключей по значению; **5.6** — **постоянный полигон `npm run test:core`**
   (tools/sandbox-suite.mjs + 4 свода, ~97 проверок по матрице 8 полевых профилей), notes по
   версиям с печатью интервалом, скан протухших утверждений (П9), механизм deprecations (T10).
   **Фаза 2b ✅** (2026-07-28): /pause — мягкая парковка (семантика обкатана вживую), /end-chat —
   полное закругление с эстафетой; оба слоя, алиасы в 9 пакетах, README 27 строк, счётчики README
   26→27 (остальной хвост класса bugs/09 — «=37» в AGENT_GUIDE, «26 ритуалов» в схемах, карты —
   добит судейским проходом 2026-07-28), сборка 45/135/523 зелёная, полигон 4/4.
   **Фаза 5 ✅** (2026-07-28): все 4 гвардрейл-инструмента отгружены — kaif-provenance
   (grep-ступень, опциональный модуль в `.kaif/tools/`, s05 11/11), /derive-styleguide (оба слоя +
   9 пакетов алиасов), kaif-canon-lint (исполняемый шаблон со selftest «страж умеет краснеть»,
   s06 5/5), режимы строгости в AGENT_GUIDE обоих слоёв. Сборка 46/138/530; README 28 строк;
   полигон 6/6. Token-F1 `--mark` — объявленная 2-я ступень (после полевой обкатки).
   **Фаза 6 ✅** (2026-07-28): пояснительная записка **`KAIF_REFERENCE.md`** — двенадцатый
   ключевой документ поставки (§1–16: терминология, схемы маркера/манифеста/расписки, всё
   устройство; dest `.kaif/`); /help-kaif читает её и цитирует параграфы; policyChanges-механизм;
   /kaif-fork переписан с обязательным релизом. **ВЕСЬ полевой беклог bugs/10–19 закрыт (DONE).**
   Сборка 12 docs / 47 FULL / 139 бандл / 569 модулей; полигон 6/6.
   **Фаза 7 ✅ (2026-07-28): KAIF 2.0 «Excellent KAIF» ОПУБЛИКОВАН** — судейский проход
   (6 адверсарных ревью, VERIFIED WITH CAVEATS → все находки закрыты) → README-бейдж 2.0 + секция
   «Excellent KAIF» EN+RU → ноты EN+RU по канону с «Upgrading from 1.x» → TEMPLATE_NOTES['2.0']
   (машинная методичка слияния) → bump → публикация с 6 ассетами (+kaif-module-map.json) →
   пост-релизный смок с дефолтного канала зелёный → issue #1 закрыт. ПЛАН 20 ЗАКРЫТ ЦЕЛИКОМ.
   Хвосты вне гейта: README глазами на github.com; token-F1 ступень kaif-provenance (после
   полевой обкатки); идея fork-bundle.
0.0. **Хвост «лица KAIF» (`plans/19`) добит 2026-07-28** при реструктуризации README: шапка
   первого экрана готова (слот `<h3><em>`, ряд навигации — 6 ссылок вкл. флагманскую главу,
   6 бейджей одного стиля), якоря и ссылки проверены поимённо (0 мёртвых), judge-проход выполнен
   (5 адверсарных ревьюеров). Из Фазы 5 остаётся только просмотр ГЛАЗАМИ на самом github.com
   (обе темы, телефон) после пуша — за владельцем.

0.1. **v1.6 «Homeostatic KAIF» отгружен** — пост-релизная жизнь: полевая обкатка конвенций 1.6 на
   реальных проектах (ndim — кандидат на релизный `/kaif-update` 1.5→1.6); находки станут ТЗ для
   эпика 1.7 (`plans/17`, инструменты гвардрейлов: `kaif-provenance`, `/derive-styleguide`, линтер
   канона, режимы строгости).
1. **Эпик 1.5 отгружен** — пост-релизная жизнь: полевые отклики по тонкой установке (issue-трекер),
   ответ Zoo на #910. **ДЗ-03 закрыт целиком** (2026-07-17): смок ndim зелёный — 149/149 юнитов,
   сборка, типы, kaif-чек 28/28 (`ndim/test-results/kaif15-smoke-report.md`). Сразу за ним владелец
   запустил **первый полевой релизный `/kaif-update`** на ndim (1.4→1.5 с release-канала): апдейт
   завершён (39 replaced / 0 added / 13 kept; маркер унаследован полностью, версия 1.5; чекпоинты
   честные; update-verify exit 0 + самоочистка). **Поле поймало два дефекта — и оба закрыты в тот же
   день прямо в 1.5 (ассеты перезалиты в релиз, версия не менялась — решение владельца):**
   (1) bugs/06_DONE: машинерия молча заменила 18 RU-адаптированных навыков + PHILOSOPHY —
   «усыновлённые» прошлым апдейтом kept-файлы совпали со снапшотом и сочтены «нетронутыми»; агент
   ndim восстановил из git (ndim EXP-0047); фикс — происхождение `kept` в deploy-manifest,
   усыновлённое никогда не заменяется молча. (2) bugs/07_DONE: update-verify не давал гарантий
   verify-final — теперь оба пути идут одним `runFinalGates` (плейсхолдеры, самолечение маркера,
   re-sync копий, самоочистка). Заодно bugs/08_DONE (`check` судил по CLI-дефолту систем, а не по
   маркеру — нашёл наш перезаливочный смок) и дополнение в bugs/04 (кэш release-канала после
   `--clobber`). Верификация: 27 песочных проверок (сценарий ndim воспроизведён и закрыт) + релизный
   смок с дефолтного канала зелёные. README (EN+RU) и релиз-ноты описывают новую механику как
   штатную («классификация по происхождению», «финал с гарантиями свежей установки»). Наш урок —
   EXP-0012 (у снапшота должно быть происхождение; гарантии — свойство дерева, не пути).
2. `node tools/build-framework.mjs` — собрать + самопроверка (корневой `KAIF.md` — тонкий;
   `dist/KAIF-FULL.md` — **47 блоков**; бандл — **139**; карта — **569 модулей**; Slim больше не
   эмитится) и `npm run test:core` — полигон 6 сводов целиком.
3. `bugs/02` (циклы) — DONE-тег после полевой проверки реального `/dayloop`.
4. Хвосты из пула выше (приоритет: RU-перевод kaif-* → миграция → адаптеры/сферы) — но эпик 1.5 их
   частично поглощает (адаптеры/сферы решаются в Фазах 1–2 плана 13).

