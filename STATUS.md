# KAIF — текущий статус

> Читай перед каждой задачей. Обновляй при каждом значимом изменении состояния. Это основная передача
> эстафеты между сессиями, теряющими контекст — пиши так, чтобы свежая сессия стала продуктивной по
> одному этому файлу.
> 🧠 Главный принцип мышления — `PHILOSOPHY.md`. Рабочий канон — `AGENT_GUIDE.md` (учти фрактальную
> двухслойную структуру: полезная нагрузка `framework/`+`KAIF.md` vs. обвязка для dogfooding в корне).
>
> ⚠️ **STATUS — СВОДКА «сейчас», не летопись** (канон 2.1, эпик H). Каждая строка проходит два
> теста: «уберу — следующий агент ошибётся?» и «читается целиком за один присест» (мягкий ориентир
> ~200 строк). Закрытое ПЕРЕЕЗЖАЕТ дословно в `PROJECT_HISTORY.md` («стрижка бонсая» в
> `/end-chat`), а не копится здесь. Летопись прежних сессий/релизов — там же.

---

## Где мы сейчас

**KAIF 2.1 «Strong KAIF» ОПУБЛИКОВАН** (2026-07-31,
https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.1).

**KAIF 2.2 «Yolden KAIF» ИСПОЛНЕН ПОЛНОСТЬЮ, кроме Фазы R.** Спланирован целиком 2026-08-07
(`researches/15` → зонтик `plans/26`; интервью 007 — решения №32–46). Закрыты **все пять эпиков**
(L полевые фиксы · K интерактивный контур · M контур обратной связи · N канон-архитектура и
`REQUIREMENTS_FRAMEWORK` · O контекст и окружение) и **все девять задач** (T1, T2, T4–T10 и
T3 — 2026-08-08). Эпик P (переименование в `kaif-*`) — ⛔ NOT TO BE DONE в 2.2 (решение №47),
кандидат в 2.3. Подробности каждого закрытия — в `PROJECT_HISTORY.md`, там же все закрытые сессии.

✅ **Гейт Фазы R ЗЕЛЁНЫЙ с 2026-08-08 16:50 +03:00** — её скелет требует «все задачи T1–T9
закрыты», условие выполнено закрытием T3.

🎂 **Витрина 2.2 стоит** — https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.2, дата рождения
08.08.2026, `version.json` — **2.2 Yolden KAIF**. Два правила этой страницы живут дальше:
⚠️ **Кодовое имя версии пишется БЕЗ КАВЫЧЕК** — слово владельца 2026-08-09 00:20 +03:00: «кавычки
убрать, мы их никогда не ставили для названия». Правило в канон-таблице идентичности
`AGENT_GUIDE.md`, проверка перед сдачей витрины — греп там же. Историю не переписываем.
⚠️ **Метка pre-release снимается только после доезда артефактов:** канал `release` загрузчика тянет
ассеты из ПОСЛЕДНЕГО релиза, поэтому релиз без артефактов в роли «latest» сломал бы установку
полевым проектам. Пока метка стоит, «latest» остаётся у v2.1 с настоящими ассетами (`bugs/63`).

⚔️ **КРУГ R1 ИСПОЛНЕН 2026-08-09 09:29 +03:00.** Многоагентная оркестрация: 66 агентов, 56 находок
судимы, **46 CONFIRMED / 10 REFUTED**, двенадцать классов `bugs/65`–`bugs/76`, четыре блокера.
Сквозной диагноз: заявление и его проверка расходятся, когда меняются окрестности — прокси вместо
свойства · фикс-список охвата · закрытие пересказом вместо переисполнения.
**Полный вердикт, таблица классов и назначенные оси R2 — `plans/59` → «КРУГ R1 — вердикт».**

✅ **ФАЗА ПОЧИНОК R1 ЗАКРЫТА 2026-08-09 14:13 +03:00 — ОДИННАДЦАТЬ КЛАССОВ ВОЛНЫ ИЗ ДВЕНАДЦАТИ**
(сессии 31–33, 2026-08-09 09:29→14:13 +03:00). Двенадцатый — `bugs/74` — ждёт ПОСЛЕ R2 по слову
владельца (окончательный замер витрины назначен им туда), и это НЕ долг фазы, а её граница.
Из волны R1: `bugs/65` `66` `67` `68` `69` `70` `71` `72` `73` `75` `76` — закрыты целиком.
Заведены и закрыты по ходу: `bugs/78` (правдивость метки) · `bugs/79` (гейт неожиданного файла) ·
`bugs/80` (мусорный символ) · **`bugs/81`** (помеченный вариант выпадал со страницы владельца) ·
**`bugs/82`** (ратчет воскрешал пояснённый долг) · **`bugs/83`** (вопрос отправлял владельца в
документ). Закрыта механическая половина `bugs/74` (гейт публикации).

📌 **Правило, по которому фаза велась** (слово владельца
2026-08-09 ≈12:15 +03:00, дословно): «нужно починить в фазе R1 всё, что фаза R1 нашла. Не
полагаемся на фазу R2. Не надеемся на неё, что ей что-то передадим и она пофиксит. Она будет
независимой от R1. R2 — полный аудит после R1 с фиксами, проделанными в R1. Надеемся, что R2 не
выявит баги, поэтому всё чиним в R1».

🧭 **Форма починки, годная для остатка** (выведена из шести закрытых классов, урок EXP-0079):
**охват перестаёт быть списком и становится вычислением, а изъятие требует названной причины.**
Где вычислить нельзя по природе задачи — это говорится вслух и называется исполнитель-человек,
вместо кредита машине, которого она не отрабатывала.

⚠️ **Новые оси нашли ШЕСТЬ настоящих дефектов на живых артефактах, и три из них были СВЕЖИМИ
работами самого агента** — метка, поставленная вперёд через час после написания оси правдивости;
`bugs/69`, закрытый «целиком» без одного пункта плана; кириллица в английском шаблоне. Ни одну не
нашло чтение. **Это и есть довод EXP-0079: правило, держащееся на намерении, не держится — даже у
сессии, которая в этот самый час его и пишет.**

**Что дал `bugs/66` сверх самих фиксов** — общая форма починки на весь класс: **охват перестал
быть списком и стал вычислением**, а изъятие требует названной причины. Где вычислить нельзя по
природе задачи, это сказано вслух и назван исполнитель-человек (грамматика: падеж и управление —
глаза владельца, а не `voice-lint`). Новые охваты в первый же прогон нашли ТРИ настоящих дефекта
на живых артефактах: три утечки личного текста в опубликованном слепке · «for every dollar
**actually** paid» на витрине README · одиннадцать несверяемых зеркал раскладки и документов.

✅ **`bugs/79` ЗАКРЫТ 2026-08-09 13:13 +03:00** — у коммита есть гейт неожиданного файла (новый
файл в дереве останавливает ПОДМЕТАЮЩИЙ прогон и называет себя), отказ на постороннем аргументе и
`--selftest` на оба ответа; гейт стоит ДО бампа `version.json`. Второе наблюдение класса поймала
уже отгруженная печать набора — и поймала на авторе фикса: девять путей одним флагом `--only`
уехали как три файла, а баг-док `bugs/65` попал в историю ПЕРЕИМЕНОВАННЫМ в `_DONE_` при теле
«PARTIAL». Урок — EXP-0080.

⚠️ **Круг R1 ОШИБСЯ в мягкую сторону по одной находке, и это важно для доверия к его отчёту.**
По слепку стилометрии он записал «дыра латентна, реализованной утечки нет». Опровергнуто при
починке: независимая ось приёмки нашла **три настоящие утечки личного текста владельца в
ОПУБЛИКОВАННОМ** `AUTHOR_STYLOMETRY.md`. Слепок пересобран и чист; **судьба git-истории —
решение владельца** (переписывание требует force-push, то есть строки `AUTH:`).

**Порядок остатка 2.2: ~~R1~~ → починки R1 (здесь) → R2 → починки R2 → релиз** (закрытие issue #2
через `send-outbound`, доезд артефактов и окончательный замер «Интересных фактов» — в `/release`).
⚠️ **R2 идёт ОТДЕЛЬНЫМ ЧАТОМ** — слово владельца 2026-08-09 ≈10:20 +03:00: «р2 будет в отдельном
чате. текущий скоуп - р1».

⏳ **Открыто на владельце** (два пункта, оба намеренные): **(а)** вычитка витрины глазами —
`homeworks/05`, дополнена разделом «что изменилось после Q»; **(б)** судьба приватных имён в
РАБОЧИХ документах и в истории git — поставка, витрина и слепок портрета уже чисты.

> **Критерий владельца на весь остаток 2.2 (дословно, решение №52):** «мы не торопимся. Мы
> делаем хорошо и полно, качественно, и тестируем, и полируем, чтобы 2.2 получилась
> полноценной и крепкой версией». Развилки скоупа решаются по ПОЛНОТЕ, не по скорости.

Актуальная сборка — 14 ключевых документов / 7 README /
**35 навыков** / 57 блоков FULL / **161 бандла** / **691 модуль** (цифры печатает сборка — не
переписывай руками; с плана 62 их стережёт `node tools/counters-guard.mjs`); полигон `npm run test:core` — **14 сводов, зелёный** (s14 — модуль
refresh-hooks); контур: `node tools/review.mjs --queue` (очередь страницей) · `node
tools/questions-guard.mjs`; линтер документов знаний: `node tools/doc-header-lint.mjs`
(шапка-мета + блок вектора + словарь требований, консультативный); страж счётчиков прозы:
`node tools/counters-guard.mjs` (число зеркал печатает сам прогон; ось навыков — ПОИМЁННО,
`--selftest`); **линтер голоса владельца: `node tools/voice-lint.mjs <файлы.md>`** (слой 7A
чек-листа портрета, вшит в `/release` шаг 2); **сторож защищённого цикла:
`node tools/guarded-watchdog.mjs --arm|--check|--status|--disarm`** (пульс по завершённой работе,
задача планировщика ОС; дефер G4 закрыт первым полевым прогоном 2026-08-09).

---

## 🎯 Где продолжить — порядок по ЧАТАМ (решение владельца 2026-08-08 17:00 +03:00)

> 🚩 **ПЕРВОЕ ДЕЙСТВИЕ СВЕЖЕЙ СЕССИИ — освежить канон.** Маркер `.kaif/refresh-marker.json`
> намеренно оставлен протухшим на закрытии чата (перечитывать девятку, чтобы тут же выбросить
> контекст, — трата; штамповать без чтения — фрод класса ложного `[TESTED]`). Хук напомнит сам;
> `/resume` закрывает это полным проходом канона.

Порядок остатка 2.2: **R1 → починки R1 → R2 → починки R2 → релиз** (решение №5). Задачи T1–T12 и
фаза Q закрыты; подробности каждого закрытия и все закрытые сессии — в `PROJECT_HISTORY.md`,
уроки — EXP-0051…EXP-0075.

> 🧰 **Инструменты фазы Q, которыми R1 обязан пользоваться, а не открывать заново:**
> `node tools/showcase-lint.mjs` (витрина) · `node tools/estimates-guard.mjs` (источник у оценки) ·
> `node tools/private-names-guard.mjs` (утечка имён) · `node tools/revert-guard.mjs` (откат рабочей
> копии, стоит преполётом в `commit.mjs`). Правила, которые они стерегут, — `AGENT_GUIDE.md` →
> «Витрина — текст, который читает посторонний человек».

1. 🧠 **Исполнимые правила прошлых чатов — не история, а то, чем работать в R1:**
   **(а)** карточка находки несёт СЕГОДНЯШНИЙ измеренный замер — **сними его ДО кода**;
   приём трижды подряд опроверг числа самой карточки (EXP-0056…EXP-0058).
   **(б)** мутационная проверка отвечает на ДВА вопроса — краснеет ли страж на дефекте И молчит
   ли он на «не трогать» (EXP-0059); адресаты мутанта называются ДО прогона.
   **(в)** документ, по которому поднята страница контура, **НЕ трогать до её закрытия** —
   владелец в это время отвечает; мутация для доказательства делается на КОПИИ (`bugs/62`).
   **(г)** инвентарь зеркал, собранный грепом по ЦИФРЕ, пропускает вхождения в другой
   формулировке — грепи по ПОНЯТИЮ (EXP-0070); три таких счётчика жили на витрине.
   **(д)** сравнительное числительное в отчёте («вдвое короче») стареет ВНУТРИ своей же
   задачи — либо пиши рядом команду замера, либо не пиши числа вовсе (EXP-0067).
   **(е)** правило языка стережётся ФОРМОЙ нарушения, а не обобщением этой формы (EXP-0074);
   **(ж)** страж, который не проверяют селфтестом, ломается тем же дрейфом, который сам стережёт
   (EXP-0075).

2. ⏳ **ДОЛГИ НА ВЛАДЕЛЬЦЕ:** вычитка витрины 2.2 глазами (`homeworks/05` — по решению №65 именно
   она, а не пометки `[AI]`, является очередью на приёмку README и релиз-нот) · **окончательный
   замер «Интересных фактов» после R2** — его же слово · старший долг того же класса
   `homeworks/04` (README 2.1 глазами).

4. ⏳ **ФАЗА R — ДВА КРУГА, R1 и R2** (`plans/59`; решение владельца №64, 2026-08-08 23:10 +03:00).
   - ✅ **R1 ИСПОЛНЕН** 2026-08-09 09:29 +03:00 — 46 подтверждённых находок, `bugs/65`–`bugs/76`.
   - ✅ **ПОЧИНКИ R1 ЗАКРЫТЫ** 2026-08-09 14:13 +03:00 — одиннадцать классов из двенадцати;
     `bugs/74` вынесен ЗА фазу словом владельца (замер витрины — после R2). Метод соблюдён на
     каждом: класс, а не экземпляр; у каждого фикса страж, доказанный красным НА КОПИИ; фикстура
     легла в `--selftest`, а не в сессию.
   - 👉 **СЛЕДУЮЩЕЕ — R2, ОТДЕЛЬНЫМ ЧАТОМ** (см. ниже). В этом чате остатка волны нет.

     ⚠️ **Коммит теперь ТРЕБУЕТ объявленного набора, пока в дереве лежат чужие новые файлы**
     (`bugs/79`): четыре постера владельца в `assets/` краснят подметающий прогон. Верный ход —
     `node tools/commit.mjs --only <путь>` **по флагу НА КАЖДЫЙ путь** (девять путей одним флагом
     инструмент прежде глотал молча; теперь это отказ). Судьба самих постеров — слово владельца.

     🧰 **Инструменты сессии 33:** `node tools/commit.mjs --selftest` (гейт неожиданного файла и
     отказ на постороннем аргументе) · `node tools/check-framework.mjs --selftest` (к гарду 5d
     добавлен 5e: двухполовинный документ судится ПОРОЗНЬ, признак половинности вычисляется из
     якоря `<a id="russian">`) · `KAIF_CONTOUR_SILENCE_MS`/`KAIF_CONTOUR_TICK_MS` — одностороннее
     окно наблюдения за вахтой тишины контура (принимается ТОЛЬКО значение строже канона) ·
     `questions-guard` — три новые оси: «вариант ПОТЕРЯН» (разница написанного и увиденного, не
     ноль), «вопрос ОТПРАВЛЯЕТ в документ» (`bugs/83`) и ключ ратчета без комментариев ·
     `doc-header-lint` — ось «PENDING без СУДЬБЫ» («вынесено в фазу» судьбой НЕ является) ·
     `showcase-lint` — ось HALVES считает подписи таблиц и БЛОКИ цитат, форм-лист EXCUSE знает
     приписку со ссылкой на внутренний документ.

     🧰 **Инструменты, появившиеся в сессии 32 — ими работать, а не открывать заново:**
     `node tools/refresh-stamp.mjs --trigger <…> --docs <…>` (маркер освежения пишет СКРИПТ, момент
     берёт сам) · `node tools/doc-header-lint.mjs` (две новые оси: правдивость метки против момента
     её коммита + «закрытие против тела»; известный исторический долг — базовая линия
     `tools/doc-header-lint.stamp-baseline.json`, она только УМЕНЬШАЕТСЯ) ·
     `node tools/commit.mjs --only <путь>…` (коммитит ровно названное; без `--only` набор
     ПЕЧАТАЕТСЯ вслух — `bugs/79`).

     ⚠️ **`bugs/77` (авторство на витрине) — НЕ починка агента:** формулировки принимает владелец,
     очередь — `homeworks/05`. Готовь варианты и показывай СТРАНИЦЕЙ контура, не решай сам.
   - **R2 — В ОТДЕЛЬНОМ ЧАТЕ** (слово владельца 2026-08-09 ≈10:20 +03:00: «р2 будет в отдельном
     чате. текущий скоуп - р1»). Полное ревью 2.2 **ЗАНОВО** плюс **переисполнение заявлений
     фиксов R1**. Оси уже НАЗНАЧЕНЫ критиком полноты — `plans/59` → «Оси круга R2»; брать их, а не
     повторять R1 (принцип 5, парадокс пестицида).
   - **Починки R2** — финишная полировка, затем `/release` и закрытие issue #2 через
     `send-outbound`. Третий круг по умолчанию НЕ заводится — назначает только владелец.
   > 💡 **Слово владельца про инструмент** (2026-08-07 23:05 +03:00): Фаза R — одно из двух мест,
   > где многоагентная оркестрация окупается; владелец просил СКАЗАТЬ ему явно в этот момент, а
   > включает её он сам. В двадцать пятой сессии рой отработал на T3 и себя оправдал.

5. ✅ **ОЧЕРЕДЬ ВЛАДЕЛЬЦА ПУСТА** — `node tools/questions-guard.mjs` печатает «Ждут ответа: 0».
   **Новые развилки поднимать КОНТУРОМ** — `node tools/review.mjs --queue`, отслеживаемой фоновой
   задачей, — а не репликой в чате: слово владельца «по вопросам ко мне — интерактивный контур».
   ⚠️ **Варианты ответа пиши СТРОКАМИ ТАБЛИЦЫ** `| **A** | что означает | цена и риск |` **или
   списком** `- **A)** …` — третьей формы парсер не знает, и вопрос отрендерится без радиокнопок
   (`bugs/62`); с этой сессии ось стережёт `questions-guard`.
   ⚠️ **Трейлер коммитов (решение №54):** зашитого имени модели нет — коммить
   `node tools/commit.mjs --msg-file <путь> --as "<имя своей модели>"`; без `--as` инструмент
   предупредит и уйдёт БЕЗ трейлера (это норма, а не поломка).

6. **Проверка после любой правки машинерии:** `node tools/build-framework.mjs` + `npm run
   test:core` (**14 сводов**); реестр пар — команды в `AGENT_GUIDE.md` (счётчики —
   `node tools/counters-guard.mjs`); страж места вопросов `node tools/questions-guard.mjs` — в
   `/end-chat` и `/release`. Числа в коммиты — только из вывода инструментов (EXP-0025).
   **Тронул контур** (`tools/review.mjs`, `tools/lib/review-core.mjs`) → обязателен
   `node tools/verify-contour.mjs` (живой браузер; счёт проверок печатает сам прогон — числом
   здесь не дублируется, EXP-0025) и пересмотр эталона глазами при новых живых документах:
   `node tools/verify-contour.mjs --write-etalon`. Дешёвая половина без браузера —
   `--etalon-only`, она стоит в `/end-chat`, полный прогон — в `/release` (`bugs/69`).
   ⚠️ **Логи верификации — в ФАЙЛ, не через `| tail`** (EXP-0052): хвост показывает итог, а нужна
   причина; имя упавшего свода отрезается ровно там.

---

## 📦 Задел на 2.3 (решения владельца 2026-08-08, НЕ в 2.2)

> 📄 **Скоуп 2.3 живёт документом — `ideas/24_kaif_2.3_scope.md`** (заведён 2026-08-08 17:53 +03:00
> по слову владельца). Пункты копятся ТАМ в день рождения; здесь — только сводка. Первый пункт,
> внесённый владельцем: **написать Fable поручение скептически проревьюировать `/code-revision` и
> переписать его под себя** — навык писал Opus, а работать по нему Fable.

- **Заморозка восьми языковых пакетов** (решения №56/№57): ведём только `ru` и `en`; восемь
  остальных замораживаются с ЯВНЫМ объявлением — на какой версии, в каком состоянии, почему
  («тяжело в поддержке, пока не окупается»), оживление по запросу сообщества. Страж протухания
  требует ресинк только для ведомых языков. Постановка — `interviews/interview_013` Q1.
- **Форма 2.3** (решение №58): обычный релиз со скоупом из новой идеи владельца; сокращение
  поверхности (пакеты · ревизия 35 навыков «каждый ли зарабатывает своё место» · удвоение
  payload/обвязка) — пунктом ВНУТРИ него. **Проработка идей начинается сейчас**, разведка — через
  многоагентную оркестрацию; включает её владелец по просьбе агента.
- **Эпик P** (переименование навыков в `kaif-*`, решения №35/№47) — кандидат сюда же. ⚠️ Он первым
  нагрузит пару, дырявую по `bugs/55`: до эпика P эту пару надо взять под стража по ИМЕНАМ.

---

## 🤖 Пул автономного беклога (остаточные хвосты)

- [ ] Постоянное покрытие в полигоне: tamper-тест sha256-гейта ЛОАДЕРА + twin-run byte-exact
      ассерт в s01 (принятый долг закрытия эпика L — находка №3 линзы 3; сейчас доказано
      только сессионными стендами).
- [x] ~~Прогнать `--selftest` всех стражей~~ — сделано 2026-08-09 (13 из 13; сломанный один,
      `bugs/64`). Остаток пункта живёт ниже, в задании `/code-revision`.
- [ ] Хвосты контура по потребности (интервью №008, Q3 + `researches/17` §4): G4-страж
      содержания вопроса · P6-медиавставки (iframe srcdoc макетов + «открыть отдельным
      экраном» — рецепт NDim готов) · карточка исходящего артефакта на странице (к Фазе R) ·
      «пометка для агента» цитатой `>` построчно · list-команда · детектор «поле-подсказка ≠
      статус».
- [ ] Адаптеры под остальные системы (Devin, OpenCode, Aider, Junie — сведения в `researches/01`).
- [ ] Библиотеки терминов по остальным сферам из `_template`.
- [ ] Идея fork-bundle (хвост bugs/16) и token-F1 ступень kaif-provenance — после полевой обкатки.
- [ ] Контр-стражи, честно отложенные в bugs/26/28.
- [ ] Долг фазы O5: машинерия `deployAgentSystems` не знает про Antigravity — адаптер
      документационный, автодеплой навыков туда не заведён (путь навыков не верифицирован;
      вписывать угаданный запрещено).
- [ ] **Следующий прогон `/code-revision`** (что менять — в `reports/KAIF_AUDIT/…_SUMMARY.md`):
      мутировать зеркала, которые селфтест НИКОГДА не красит · научить селфтесты различать «нашёл
      расхождение» и «упал с исключением» (`bugs/64`, класс на всех стражей сразу) · взять ось
      «односторонние инварианты» по контуру (зона не смотрена ни разу) · переподнять находку про
      `s10` с верным адресом `:637` · замерить effective-FP первого отчёта.

> Секция «❓ Ожидает владельца» удалена по слову владельца (интервью №008, Q4, вариант B):
> просьбы владельцу живут ТОЛЬКО в `homeworks/`/`interviews/` (текущая — `homeworks/04`,
> README 2.1 глазами); очередь показывает `node tools/review.mjs --queue`, нарушения ловит
> `node tools/questions-guard.mjs`.

---

## 🌍 Работа ВНЕ этого репозитория — ядро голоса владельца

`d:\work\krinik_voice`, приватный `MikalaiKryvusha/krinik-stylometry`, **версия ядра 1.1**
(2026-08-09, круг 5 — АП27–АП37 из волны обзора витрины). KAIF связан с ним ТОЛЬКО канон-именем
файла `AUTHOR_STYLOMETRY.md`; личное творчество владельца в открытую поставку не едет, а имена
его проектов снимаются анонимизацией прямо в генераторе слепка.

Портрет ПРИНЯТ владельцем (решение №63), и приёмка НЕ закрыла три названные слабости:
**(а)** позитивные правила голоса выведены из ОДНОГО корпуса свободной прозы — четыре корпуса
высшего и среднего веса не разобраны ни одним аналитиком; **(б)** документному регистру круг 5 дал
доказательную базу только с одной стороны — анти-портрет; чем документный текст владельца
ЯВЛЯЕТСЯ, портрет не знает; **(в)** корпус рабочей речи QA пуст — задание в
`corpus/qa_work_confluence_jira/_ЗАДАНИЕ.md`, исполнитель — отдельный агент рабочего проекта.

---

## Открытые баги

Открыто **шесть** (проба: `ls bugs/*.md | grep -v README | grep -vc DONE`): `02`, `45`, `61`, `63`,
`74`, `77`. Закрыты в сессии 31: `bugs/67`, `bugs/68`, `bugs/70`; в сессии 32: `bugs/66`,
`bugs/69`, `bugs/75`, `bugs/76`, `bugs/78`, `bugs/80`; в сессии 33: `bugs/65`, `bugs/71`,
`bugs/72`, `bugs/73`, `bugs/79`, плюс заведённые и закрытые тем же заходом `bugs/81`, `bugs/82`,
`bugs/83`.

⚠️ **Из шести открытых на агенте не висит ни один:** `74` и `77` — витрина, назначены владельцем
ПОСЛЕ R2; `02` ждёт полевого прогона; `45`, `61`, `63` — необъяснённые транзиенты, каждый уже под
стражем на своё последствие.

⚠️ **Три новых бага сессии 33 нашлись НЕ чтением, а работой контура с живым владельцем** —
и один из них достиг его: в опубликованном интервью №016 страница показала три варианта из
четырёх, потому что клетка с пометкой «(рекомендация агента)» выпадала из разбора. Это довод в
пользу того, чтобы поднимать вопросы контуром, а не репликой в чате: чат такой дефект не показал бы
никогда.

⚠️ **Витрина знает другие числа — «заведено 63, закрыто 58» — и это НАМЕРЕННО.** Волна R1 добавила
классы уже после того, как числа были записаны, а окончательный замер владелец назначил ПОСЛЕ R2.
Расхождение не молчит: `node tools/showcase-stats-guard.mjs` печатает все двенадцать строк и стоит
в `/release`, но НЕ в `/end-chat` — числа версии законно отстают, пока версия делается (`bugs/74`).

**Волна R1 — `bugs/65`–`bugs/76`, заведены 2026-08-09 09:29 +03:00** (полная таблица классов с
числом находок и тяжестью — `plans/59` → «КРУГ R1 — вердикт»). Чинятся фазой «починки R1» по
`BUG_FIXING_FRAMEWORK.md`: класс, а не экземпляр; страж на каждый фикс, доказанный красным.
**Четыре блокера релиза берутся первыми:**

- ✅ `bugs/66` — **ЗАКРЫТ ЦЕЛИКОМ 2026-08-09 11:51 +03:00** (все семь охватов). ⚠️ Один хвост
  остаётся НЕ у агента: при починке выяснилось, что утечка была НЕ латентной — в опубликованном
  слепке нашлись **три настоящие утечки** личного текста владельца. Слепок пересобран и чист,
  `--selftest` доказывает семь форм, но **судьба git-истории — решение владельца** (удаление файла
  историю не отзывает; переписывание требует force-push, то есть строки `AUTH:`).
- 🟡 `bugs/69` — **блокер ЗАКРЫТ 2026-08-09 10:14 +03:00.** Эталон пересмотрен глазами и
  переморожен (прогон: 125 зелёных, 0 красных); у приёмки появилась дешёвая половина
  `--etalon-only` (0,12 с, без браузера) — она стоит в `/end-chat`, полный прогон в `/release`,
  а снятие любого вызова краснит свод `s12`. Открытым остался `g5-live-numbers` (пять зашитых
  обращений к живым документам).
- ✅ `bugs/65` — **ЗАКРЫТ ЦЕЛИКОМ 2026-08-09 13:06 +03:00** (все четыре ассерта-прокси). Форма на
  весь класс: свойство мерится ТАМ, ГДЕ ЖИВЁТ — счётчик читает диск, а не план; двухполовинность
  ВЫЧИСЛЯЕТСЯ из якоря; величина масштаба «минуты» получила одностороннее окно наблюдения. Каждая
  мутация уложена в фикстуру (`s09` G10d/G10e · гард 5e · `verify-contour` QA3).
- ✅ `bugs/72` — **ЗАКРЫТ 2026-08-09 13:55 +03:00.** У каждого из шести обязательств есть СУДЬБА:
  три решены владельцем (интервью №017 — решения №66/№67/№68), два закрыты починкой, одно
  объявлено долгом с названным носителем. Класс механизирован осью «PENDING без СУДЬБЫ» в
  `doc-header-lint`: «вынесено в <фазу>» судьбой не является.
- ✅ `bugs/73` — **ЗАКРЫТ 2026-08-09 14:01 +03:00.** Шесть строк волны Q получили либо настоящего
  стража с фикстурой, либо честного исполнителя-человека; кредита без доказательства не осталось.
- 🔴 `bugs/74` — Таблица 6 витрины против живого репозитория: 10 строк из 12. Окончательный замер
  владелец назначил ПОСЛЕ R2 — нужен гейт публикации, а не переписывание чисел сейчас.

Остаток волны — один класс: `71` (правило объявлено механизированным без исполнителя).

⚠️ **STATUS перерос мягкий ориентир (~200 строк при 340) — стрижка бонсая назрела** и принадлежит
`/end-chat`: закрытое переезжает в `PROJECT_HISTORY.md` ДОСЛОВНО, а не удаляется. Сессия 32
закрыла девять классов, и их подробности — первые кандидаты на переезд.

🔴 **`bugs/78` — метка проверяется ФОРМОЙ, а не ПРАВДОЙ** (нашёл ВЛАДЕЛЕЦ 2026-08-09 10:56 +03:00:
«на часах 10 частв, не понимаю, о каких 12 ты говоришь»). Агент выдумывал время меток вместо
пробы; расхождение доросло почти до двух часов и попало в семь баг-доков, вердикт круга и маркер
освежения. Все одиннадцать меток восстановлены из моментов коммитов. **Урок EXP-0078 обязателен к
прочтению следующей сессией: время снимается пробой `date` в тот же ход, что и запись.**

🔴 **`bugs/77` — витрина неверно изображает авторство** (набросил владелец 2026-08-09 09:55 +03:00,
его слова в документе дословно). «Слов прозы **написано руками** | 316 764» — неправда: руками он
писал ровно три вещи — сообщения в чат агенту, ответы в комментариях интервью и часть ИЗНАЧАЛЬНОГО
документа скоупа версии (`ideas/20`), «весь остальной труд — ии агент». Плюс «написано самими
моделями» непонятно: за числами стоит различие «все токены (прочитано и перечитано) против
`output_tokens` (породили)», отношение ×210. Формулировки принимает владелец — очередь
`homeworks/05`.

Прежние четыре:

- `bugs/61` — 🟡 **PARTIAL: класс закрыт, призрак остался.** Красный ассерта S9 не воспроизвёлся за
  120 прогонов; класс «немая команда свода» закрыт формой (`must(run, …)` + `sandbox-mute-guard`
  преполётом при нулевом долге). Сам транзиент не объяснён — при рецидиве красный назовёт себя
  сам: три гипотезы и их приметы выписаны в документе. Урок — EXP-0060.
- `bugs/63` — 🔬 research-only: бамп версии без артефактов уводит обновление на легаси-путь.
  Лечится меткой pre-release (см. выше); красный не воспроизвёлся.
- `bugs/02` — 🔧 фикс отгружен (2026-07-07); ждёт полевой проверки в реальном длинном `/dayloop`.
- `bugs/45` — 🔧 механизирован 2026-08-09 стражем `revert-guard` после ВТОРОГО наблюдения отката
  рабочей копии; сам транзиент по-прежнему не объяснён, страж ловит его последствие.
