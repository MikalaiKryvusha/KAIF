# KAIF — текущий статус

> Читай перед каждой задачей. Обновляй при каждом значимом изменении состояния. Это основная передача
> эстафеты между сессиями, теряющими контекст — пиши так, чтобы свежая сессия стала продуктивной по
> одному этому файлу.
> 🧠 Главный принцип мышления — `PHILOSOPHY.md`. Рабочий канон — `AGENT_GUIDE.md` (учти фрактальную
> двухслойную структуру: полезная нагрузка `framework/`+`KAIF.md` vs. обвязка для dogfooding в корне).
>
> ⚠️ **STATUS — СВОДКА «сейчас», не летопись** (канон 2.1, эпик H). Каждая строка проходит два
> теста: «уберу — следующий агент ошибётся?» и «читается целиком за один присест» (мягкий ориентир
> ~200 строк). Закрытое ПЕРЕЕЗЖАЕТ дословно в `PROJECT_HISTORY.md` («стрижка бонсая» в
> `/end-chat-soft`), а не копится здесь. Летопись прежних сессий/релизов — там же.

---

## Где мы сейчас

**Версия KAIF 2.7 — Audited KAIF ВЫПУЩЕНА 2026-09-18 17:24 +03:00** (https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.7;
сессия 69, Claude Opus 5). Открыта 2026-09-12 00:25 +03:00 (интервью №031 Q1 = B, №109); мета-план `plans/100` — **25 из 25** критериев
(RL ✅ 17:26); суд версии одним кругом — шесть судей, 138 = 116 · 17 · 5, пять блокеров починены до выхода (последний, D-F2, — сессией 69);
18 тикетов origin #54–#71 отвечены и закрыты 17:26. Имя — **Audited KAIF** (слово владельца ≈ 2026-09-12 23:27 +03:00, №117). Предыдущий
релиз — **2.6 Mindful KAIF, 2026-09-06 12:46 +03:00**. Следующая версия — **2.8**, открывается словом владельца; реестр — `ideas/30`. Правила
видения в силе — №97 (механика отгружается только полной) · №98 (вопрос владельцу — сценарием) · №104 (проверка на реальном
мире) · №106 (голос беседы — язык заказчика) · №110 (вектор `DELIVERY:` снят) · №116 («тест» — функциональный прогон по
реальному продукту путём пользователя, гигиена тестом не является); 2.7 добавила правило «Авторство решения»
(`AGENT_GUIDE.md`: слово владельца цитируется, слово агента подписывается). Летопись эпиков версии — `plans/100` и
`PROJECT_HISTORY.md` (записи сессий 58, 60–69). Живая эстафета — п. 0 ниже.

Стоячие правила (молитва перед работой · мягкое и срочное закрытие · названное время = СТАРТ закрытия · кодовое имя без кавычек · правила
витрины) — в `AGENT_GUIDE.md`; справочные таблицы истока — в `HOUSE_RULES.md`; вектор целей — порядок владельца в `researches/18` §2 (№67).

Актуальная сборка — 14 ключевых документов / 7 README / **37 навыков** / 59 блоков FULL /
**182 бандла** / **798 модулей** (цифры печатает сборка — не переписывай руками; стережёт
`node tools/counters-guard.mjs`); полигон `npm run test:core` — **27 сводов, зелёный** (17:05–17:08 сессии 69, на бампе 2.7, в одиночку), шесть преполётов
(пятый — генератор контура из песочницы только в тихом окружении, `bugs/116`; шестой, 2.7 LP, — `dist` не старше исходников).

---

## 🎯 Где продолжить

> 🚩 **ПЕРВОЕ ДЕЙСТВИЕ СВЕЖЕЙ СЕССИИ — освежить канон.** Маркер `.kaif/refresh-marker.json`
> намеренно оставлен протухшим на закрытии чата (перечитывать девятку, чтобы тут же выбросить
> контекст, — трата; штамповать без чтения — фрод класса ложного `[TESTED]`). Хук напомнит сам;
> `/resume` закрывает это полным проходом канона.

### 1. 👉 СЕЙЧАС — разведка скоупа 2.8 ЗАВЕРШЕНА; разрез ждёт владельца — интервью №034 (страница поднята 2026-09-24 ≈ 18:55 +03:00)

0. **🚩 ЭСТАФЕТА — сессия 70 (Claude Opus 5.5, 2026-09-24 ≈ 18:20 → идёт; открыта словом владельца, [OWNER] дословно: «выполни resume» · «Почле чего смотрим, в каком
   состоянии проект, и что в issues GH накопилось, планируем скоуп на следующую версию KAIF»; посреди хода ≈ 18:41 — «еще будет импрув про имя агента, который зовет владельца
   голосом. ищи в оригин, сейчас пришлют его тебе» → #98 пойман фоновой пробой трекера 18:44).**
   **Сделано:** 27 тикетов origin #72–#98 (23 дефекта/улучшения + 4 полевых отчёта обновления до 2.7: NDim #76 · KUMM #79 · KAGO #83 · QA_Engineer #93) прочитаны целиком шестью
   субагентами с письменным брифом тишины и сверены с HEAD `6af0dad` пробами → **`researches/32_kaif_2.8_scope_recon.md`** (§2 — таблицы заявлений, §3 реестр кандидатов, §4 разрез,
   §5 реестр 2.9, §6 риски); соседи на диске — недоставленного нет. Разрез `[ИИ]`: **CK «Лёгкий канон и законный выход из ворот бюджета» → OW «Разговор с владельцем во время
   работы агента» → CH «Честные строки: отчёт в KAIF, язык и часы владельца» → UP «Обновление ничего не теряет молча» → SC «Сканеры видят проект глазами git» → RL**, ≈ 7 чатов.
   Вопросы — **`interviews/interview_034_kaif_2.8_scope.md`** (Q1 разрез · Q2 шесть переделок «на когда-то» — переформулирован от слова №77 по археологии); дверь `--check` 2/2,
   страж вопросов 0 новых нарушений; №031 Q1 отмечен внесённым (`32ed37e`). **Следующий шаг — по ответу:** мета-план 2.8 (`plans/NNN_EPIC_kaif_2.8.md`) + операционный план ТОЛЬКО
   первого эпика + реестр 2.9 `ideas/31`; `MASTER_PLAN.md` §5/§7. Ответы в 27 тикетов — после релиза 2.8 (№84/№92/№93).
   **Попутно наблюдено:** дверь археологии печатает `grep -rniE` без `LC_ALL=C.UTF-8` — на истоке 662 против 696 строк (#74 вживую; поиск этой сессии шёл с UTF-8).
0а. **Прежняя эстафета — сессия 69 (Claude Opus 5, 2026-09-18 16:49 → ≈ 17:50 +03:00; открыта словом владельца, [OWNER] дословно: «выполни resume» · «Продолжаем выпускать 2.7 версию.»;
   посреди хода — «сколько осталось работы до релиза?» (релиз уже вышел) и ≈ 17:30 «ну и как доделаешь - закрывай чат. Не торопись, не тороплю. Качественно доделывай» → `/end-chat-soft`).**
   **Сделано:** последний блокер суда D-F2 починен — страница контура сама видит, окно она или вкладка (`display-mode: standalone`), и обещает «агент заберёт» только в окне
   контура; во вкладке — кольцо спасения с текстом ответа. Свод `s22 D` открывает окно владельца окном `--app` и проверяет вкладку на чужом профиле (красный на сборке до
   починки — ровно этот ассерт, в выводе — сам дефект); QA7 `verify-contour` — в окне; помощник `headlessPage` — режим `app`; замер — `tools/sandbox/probes/display-mode-headless.mjs`.
   Коммит выпуска `32ed37e` (номер версии 2.7 · README 2.7 · отчёт прогона `testcases/reports/2026-09-18_release-2.7.md`): полигон 27 из 27 в одиночку, `verify-contour` 198/0,
   стражи зелёные, число суда пересчитано скриптом (138 = 116 · 17 · 5). Лёгкий судья релиз-кандидата — VERIFIED WITH CAVEATS 17:21; стоячая ложь (`plans/116`, п. 0 статуса,
   строка реестра суда об исходах) снята до тега. 13 из 18 черновиков ответов в тикеты были порваны жёсткими переносами — склеены (слова сохранены); #66 — без кода эпика и
   с исключением вкладки. Эстафеты сессий 68 и 67 — в летописи (стрижка 2026-09-18 17:22 +03:00).
   **Выпуск:** тег `v2.7` на `b41b3ac` · релиз опубликован 17:24 (шесть ассетов, sha256 равны `dist` тега; `release-body-guard` зелёный; первый экран
   страницы прочитан по безоконному снимку — витрину владельцу не показывали, №123) · 18 тикетов #54–#71 отвечены и закрыты 17:26 (ответы прочитаны
   обратно — равны файлам; открытых — 0) · `plans/100` 25 из 25, `plans/116` закрыт. **Следующему чату:** версия 2.8 открывается словом владельца
   (реестр `ideas/30`: п. 22 — `bugs/123`, п. 23 — находки суда, п. 24 — оговорки судьи релиз-кандидата); приём поля 2.7 (пп. 1–2 ниже).
   **Действует (оплачено сессиями 63–67):** первое слово `resume` — приказ · субагенту — письменный БРИФ с правилами тишины (без окон и звука, без `KAIF_DIST` старых генераторов) · видимое окно
   у владельца — только объявленное в чате · перед прогоном, который может позвать человека, — `grep -n '#contour\|#human-facing' EXPERIENCE.md` · escape вида «обратная косая + u + четыре
   цифры» в параметрах инструментов правки НЕ набирать — он ложится настоящим символом (`bugs/122`; сборка теперь отказывает) · скрипты правок — файлом (EXP-0121) · метка времени — пробой `date` ·
   новый гейт — по всему кругу: правка → сборка → полигон → коммит → полигон (EXP-0139) · красный полигона с кодом `3221226505` повторяется ОДИН раз (`bugs/109`) · поставочный
   `framework/AGENT_GUIDE.md` — ровно на бюджете 1200 строк, и свод `s16` теперь краснеет, если шаблон вырастет · на вопрос «сколько осталось» отвечай ЧАСАМИ, а не долей пунктов (EXP-0149) ·
   слитые `worktree-agent-*` и чужие detached-worktree — `git worktree prune` при случае.
1. **Действует на истоке.** Каждый исполненный прогон — отчёт `testcases/reports/<дата>_<работа>.md`, `[TESTED]` о прогоне называет отчёт
   (`kaif-testrun-lint check` — в `/end-chat-soft`). Проверка документа владельцу — `node tools/review.mjs <док> --check` (без страницы и зова;
   `--no-open` ЗОВЁТ); живой вопрос документа с датой ≥ 2026-09-18 — только с аттестацией поиска прошлого ответа (AQ); внёс решение →
   `--mark-implemented <док> <Q> --where <коммит>`; живую страницу закрывает только `<док> --close` (LP). Очередь владельца пуста (№032, №033
   отвечены и внесены). **Черновики ответов в issues** — в планах эпиков (#54/#56/#58/#60 `plans/102` · #55 `101` · #57 `106` · #59 `104` ·
   #61 `105` · #62/#63 `107` · #64 `108` · #65 `109` · #66 `111`) и в передачах (#67 `plans/handoff_SF_DONE.md` · #68 `handoff_FR_DONE.md` · #69 `handoff_EL_DONE.md` ·
   #70 `plans/handoff_AQ_DONE.md` · #71 — в передаче CB); отправлены и закрыты 2026-09-18 17:26 +03:00, после релиза, как решено (интервью №020, Q2 = A → №84; №92/№93; сессия 69; тексты — `reports/release_2.7/NN.md`).
2. **Приём поля 2.7 идёт:** открытых issues origin — **27** (#72–#98, проба 2026-09-24 18:44 +03:00, сессия 70; прежняя строка «0» была верна на
   2026-09-18 17:26); четыре полевых отчёта пришли issues (#76 #79 #83 #93), в `reports/KAIF_UPDATES/` их нет; разбор — `researches/32` §2д. Приём
   ожиданий истока: **`bugs/117` поле подтвердило четырежды** (NDim · KUMM · KAGO · QA_Engineer, цитаты — `researches/32` §2д) → закрыть DONE ВМЕСТЕ с
   правкой `ON-REAL-PATH: NOT YET` его стража (`framework/installer/KAIF-CORE.mjs:3192`) на первом эпике 2.8, трогающем ядро (пересборка + полигон) ·
   `bugs/114` — строка и один заголовок наблюдены у QA_Engineer на маршруте 2.4 → 2.7, не 2.6 → 2.7 критерия (засчитывать ли — решить при закрытии) ·
   `bugs/110`, `bugs/121`, `bugs/113` — нового свидетельства нет.
3. **`bugs/109` 🔧** (флейк `update` в полигоне): ПЯТОЕ вхождение — сессия 67, 15:08, свод `s02`, ПЕРВОЕ В ОДИНОЧКУ (`status 3221226505`, нативный крах
   `node`; гипотеза «параллельная нагрузка» ослаблена, общее — окно сетевого вызова `update`); красный с этим кодом повторяется ОДИН раз; следующий ход — герметичный по сети полигон.
4. ⚠️ README.pdf перегенерирован 2026-09-18 17:08 +03:00 из README 2.7; первый экран PDF глазами агента не проверен (на машине нет `pdftoppm`).
### 2. ⏳ ДОЛГИ НА ВЛАДЕЛЬЦЕ (агент их не закрывает)

- **`bugs/77` (авторство на витрине)** — формулировки принимает владелец. Готовь варианты и
  показывай СТРАНИЦЕЙ контура (`node tools/review.mjs <док>`), не решай сам.
- **Вычитка витрины 2.2 глазами** — `homeworks/05`. Две волны правок он уже прошёл вживую, но
  домашка как артефакт приёмки остаётся открытой.
- Старший долг того же класса — `homeworks/04` (README 2.1 глазами).
- **Судьба приватных имён в РАБОЧИХ документах и в истории git** — поставка, витрина и слепок
  портрета чисты (`private-names-guard` зелёный); переписывание истории требует force-push, то
  есть строки `AUTH:`.
- **`$62,50` живёт только в релиз-нотах**, в README этого числа нет — владельцу задан вопрос,
  дублировать ли его в README; ответа нет.
- **Вычитка витрин 2.5 И 2.6 глазами** (README EN/RU, ноты, страницы релизов) — не проводилась: критерий 8
  `plans/82` закрыт публикацией по вашему слову как приёмкой «как есть» (судья закрытия, каверза b);
  того же класса, что `homeworks/04`/`05`; витрина 2.6 опубликована по вашему «да» той же приёмкой «как есть» (логотип вы отсматривали страницей 2026-09-06, замечаний не записано — страница закрылась дефектом `bugs/113`).

### 3. 🧰 Инструменты — `HOUSE_RULES.md` §1

Таблица команд и того, что каждая стережёт, вынесена в `HOUSE_RULES.md` (стрижка 2026-09-18 10:51 +03:00). Обязательное после ЛЮБОЙ правки машинерии:
`node tools/build-framework.mjs` + `npm run test:core` (в одиночку, без параллельных браузеров); генератор контура в своде или пробе —
только `env: quietEnv()`. **Тронул контур** (`framework/tools/contour/*`, обёртку `tools/review.mjs`, шим `tools/lib/review-core.mjs`) →
полный `node tools/verify-contour.mjs`; новый живой документ роняет эталон НАМЕРЕННО — лечится `--write-etalon` с пересмотром чисел ГЛАЗАМИ.

### 4. ⚠️ Правила, нарушение которых уже стоило дорого

- **Время снимается ПРОБОЙ `date` в тот же ход, что и запись** — не «сессия помнит» (EXP-0078).
- **Вопрос владельцу САМОДОСТАТОЧЕН:** предмет решения приводится ВНУТРИ вопроса, а не адресуется
  ссылкой (EXP-0081, `bugs/83`). Плохо заданный вопрос не раздражает — он фальсифицирует решение.
- **Варианты ответа — СТРОКАМИ ТАБЛИЦЫ** `| **A** | что означает | цена и риск |` **или списком**
  `- **A)** …`; пометка в скобках у буквы законна. Третьей формы парсер не знает.
- **Новые развилки поднимать КОНТУРОМ** (`node tools/review.mjs`, отслеживаемой фоновой задачей),
  а не репликой в чате: слово владельца «по вопросам ко мне — интерактивный контур».
- **Мутация для доказательства делается на КОПИИ**; `git checkout` для незакоммиченного файла —
  не откат, а УДАЛЕНИЕ (EXP-0077).
- **Числа в коммиты и документы — только из вывода инструментов** (EXP-0025); **сводное число
  суда/реестра — только скриптом по строкам** (EXP-0107).
- **Логи верификации — в ФАЙЛ, не через `| tail`** (EXP-0052): хвост показывает итог, а нужна
  причина.
- **Ссылка на раздел чужого документа снимается с ЖИВОГО документа**, не наследуется из прошлого
  письма (EXP-0108; ось `NOTES-ANCHORS`).
- **Перед любым `--queue` — ревизия очереди глазами на беспредметность** (EXP-0106, `bugs/108`).
- **Вопрос владельцу — сценарием того, что он УВИДИТ** (Ситуация · Действие · Результат ·
  Проверка), на языке заказчика; «вектор/флаг/команда» без строки «Результат. Вы видите …» —
  плохо заданный вопрос (№98, EXP-0110, `bugs/111`). **Механика с шагом «спросите владельца
  проекта» — неполна и не отгружается** (№97).

---

## 🤖 Пул автономного беклога (остаточные хвосты)

- [ ] Хвосты контура по потребности (интервью №008 Q3 + `researches/17` §4): G4-страж содержания
      вопроса · P6-медиавставки · карточка исходящего артефакта на странице · «пометка для агента»
      цитатой `>` построчно · list-команда · детектор «поле-подсказка ≠ статус». Свежие кандидаты
      того же контура (2026-08-21, из bugs/105/106): buildDoc очереди по протухшему kind ·
      atHuman-метка квитанции печатает UTC-момент как локальный (сдвиг +3 ч — проверить пробой).
- [ ] Адаптеры под остальные системы (Devin, OpenCode, Aider, Junie — сведения в `researches/01`);
      по Muse Code вендорская дока ПОЯВИЛАСЬ 2026-08-21 (`researches/19` §1.3 — впрыска контекста
      в контракте нет, образец hooks не пишется).
- [ ] Библиотеки терминов по остальным сферам из `_template`.
- [ ] Идея fork-bundle (хвост bugs/16) и token-F1 ступень kaif-provenance — после полевой обкатки.
- [ ] Контр-стражи, честно отложенные в bugs/26/28.
- [ ] Долг фазы O5: машинерия `deployAgentSystems` не знает про Antigravity — автодеплой навыков
      туда не заведён (путь навыков не верифицирован; вписывать угаданный запрещено).
- [ ] **Следующий прогон `/code-revision`** — шесть пунктов «что должен сменить СЛЕДУЮЩИЙ прогон» в конце
      `reports/KAIF_AUDIT/2026-09-18_hooks_SUMMARY.md` (первым — проверка гипотезы о норме отсева 8 % против полевых 43–44 %).

> Просьбы владельцу живут ТОЛЬКО в `homeworks/`/`interviews/` (интервью №008 Q4): очередь
> показывает `node tools/review.mjs --queue`, нарушения ловит `node tools/questions-guard.mjs`.

---

## 🌍 Работа ВНЕ этого репозитория — `HOUSE_RULES.md` §5

Ядро голоса владельца (`d:\work\krinik_voice`, приватный репозиторий, версия ядра 1.1) и три слабости принятого портрета — там.

---

## Открытые баги

Открыто **тридцать три** (проба 2026-09-18 16:18 +03:00: `ls bugs/*.md | grep -v README | grep -vc DONE` → 33; `bugs/122` закрыт DONE, заведён `bugs/123`): восемнадцать строк ниже плюс
**пятнадцать R2-семейств с подтверждёнными триажом вхождениями** (фаза S, 2026-08-14: CONFIRMED 76 · REFUTED 17 · STALE 11; сводная
таблица — `reports/KAIF_AUDIT/2026-08-14_r2_triage_SUMMARY.md`) — T: 84–91, 95, 96, 100, 103; остаток U: 93, 94, 97 (реестр 2.8, `ideas/30`).

| Баг | Состояние | На ком |
|---|---|---|
| `bugs/77` | Витрина неверно изображает авторство: «Слов прозы написано руками 316 764» — неправда, руками владелец писал сообщения в чат, ответы в интервью и часть `ideas/20` | **владелец** — формулировки его, очередь `homeworks/05` |
| `bugs/02` | 🔧 фикс отгружен 2026-07-07 | ждёт полевой проверки в реальном длинном `/dayloop` |
| `bugs/45` | 🔧 механизирован стражем `revert-guard` после ВТОРОГО наблюдения отката рабочей копии | транзиент не объяснён; страж ловит последствие |
| `bugs/61` | 🟡 класс «немая команда свода» закрыт формой; рецидив-кандидат 2026-08-21 показал вторую шкуру — БЕЗЫМЯННЫЙ catch сюиты (починен: сюита называет упавший свод поимённо) | призрак; следующий рецидив назовёт себя сам |
| `bugs/63` | 🔬 research-only: бамп версии без артефактов уводит обновление на легаси-путь | лечится меткой pre-release; красный не воспроизвёлся |
| `bugs/109` | 🔧 флейк `update` в полигоне: пять вхождений (s07 ×2 · s10 · s02 ×2); четвёртое (сессия 66) и пятое (сессия 67, 15:08, полигон шёл ОДИН) — с форензикой `run-fail-N.log`: нативный крах `node`, код 3221226505, сразу после строки версии — в окне, где `update` идёт в сеть за артефактом прежнего релиза | красный с этим кодом повторяется ОДИН раз, оба исхода докладываются; следующий ход — герметичный по сети полигон (`--baseline` в сводах по образцу `s21`), он же опыт над гипотезой |
| `bugs/110` | 🔧 символ веры в EN-шаблоне говорил «KEEP TRYING»; фикс «STRIVE» + FORBIDDEN-страж отгружены 2026-09-05, ВЫПУЩЕНЫ в 2.6 (полевой тикет #49 — ответ и закрытие тем же днём) | ждёт проверки следующим обновлением ru-развёртывания (#49 → DONE после отчёта поля) |
| `bugs/111` | 🔧 S1: вопрос владельцу принесён техническим пояснением, не сценарием (интервью №023 Q2/Q3, «не понимаю проблему… как заказчик») | механизация ОТГРУЖЕНА в 2.6 (эпик OQ: `/interview` 3a обоих слоёв · ось G6 стража · охота судьи); DONE — после полевого наблюдения: вопрос через обновлённый навык, ответ выбором |
| `bugs/113` | 🔧 S1: лица «вычитка»/«макет» отгружаемого контура не давали нажать «Готово» без замечаний (владелец — на странице логотипа 2.6, 12:47: «даже у тебя самого интерактивный контур работает неправильно»); фикс в `main` 12:51: пустые поля = «замечаний нет», код 0; селфтест 47/47, s22 ✅ | DONE — после живого «Готово» без замечаний владельцем; ассеты v2.6 перевыложены с фиксом 13:34 словом владельца — выпуск 2.6 закончен |
| `bugs/114` | 🔧 S2: пара карты переименований с опечаткой в НОВОЙ половине молча возвращала дубль раздела при exit 0, гард 5f молчал (ключ по старой половине); карта была инертна до бампа версии — окно для обновления с `main`. Починено сессией 62 до коммита: строка лога «rename declaration broken», гард 5g (ОТКАЗ сборки, красный на реальном пути), потолок снят, `s27` случай E | DONE — после первого полевого обновления 2.6 → 2.7 со строкой «renamed: … baton … → … handover …» и одним заголовком в дереве |
| `bugs/115` | 🔧 S2: ложное «grep → пусто» в отчёте прогона HO — команда критерия стала неисполнимой самим фиксом (правило цитирует слово), исход записан по памяти; README EN тоже нёс слово. Починено: команда критерия переписана с обоснованием, строки отчёта — по дословному выводу, README EN | DONE — следующий лёгкий судья (CB) без каверзы класса «исход записан без переисполнения» |
| `bugs/116` | 🔧 S1: песочница пять раз показала владельцу тестовую страницу контура с голосовым зовом (утро 09:34 и ночь 22:57–00:09) — свод гонял генератор, отстающий от свода; класс был описан уроком EXP-0126 и не механизирован. Починено: `quietEnv()` + преполёт `@guard sandbox-quiet-child`, проба тишины безвредна и в собственном отказе | DONE — после красного `s22` против старого ядра без окна и звука у владельца (его слово или неделя без жалобы класса); корень поставки (незнакомый флаг → отказ до страницы, полигон против несвежего `dist`) — `ideas/30` п. 15 |
| `bugs/118` | 🟡 S2: стражи модуля хуков судят КОПИЮ истины (3 вхождения). №1 починено сессией 67: образец конфига, который владелец мержит руками, стоит под осью «какой скрипт под каким событием» — мутант, где приказ `/resume` уехал на `Stop`, теперь краснит свод | №2 (форма вывода судится подстрокой) и №3 (таблица README без стража) — реестр 2.8, `ideas/30` п. 25 (суд версии их не поднял) |
| `bugs/119` | 🟡 S2: хуки берут состояние из непроверенного входа. №3 починено в хуках сессией 67: событие с BOM (так его подаёт PowerShell 5.1) читается как событие без него | №1 (`cwd` ниже корня) · №2 (`at` в будущем) · девять мест «внешний JSON без снятия BOM» вне хуков — реестр 2.8, `ideas/30` п. 25 |
| `bugs/120` | 🔴 S2: порядок хуков утверждает ненаблюдавшееся (2 вхождения) | реестр 2.8, `ideas/30` п. 25 (суд версии не поднял) |
| `bugs/121` | 🔧 S2: документ подключения модуля обещал иное, чем исполнимо. Все три вхождения починены сессией 67: проба — по блоку на оболочку, и свод ИСПОЛНЯЕТ каждую строку в Git Bash и Windows PowerShell; наблюдаемое вместо универсалии о cooldown; ассерт «битый маркер + старый mtime» | DONE — после пробы из README в окне PowerShell владельца развёрнутого проекта |
| `bugs/123` | 🔴 S1: сообщения владельца, пришедшие посреди хода агента, сессия 67 не признала командами и работала после его «стоп»; в STATUS легла ложная «оговорка о канале» (снята сессией 68) | заведён 2026-09-18 16:18 +03:00 словом владельца; разведка класса и фикс — реестр 2.8 (`ideas/30` п. 22) |
| `bugs/117` | 🔧 S2: ось «недоставленный сигнал» судила только слова NOT YET — на реальных тикетах поля 15 строк доставки без машинной улики молчали (переведённое поле — форма тикетов перед #65). Починено до коммита: разрешающий список, `deliveryState()` на `report` и `check`, неоднозначное состояние, улика — только issue | DONE — после первого полевого `check` на обновлённом до 2.7 развёртывании с `tracking: origin` |

⚠️ **На агенте вне R2:** остатки `bugs/118`–`bugs/120` (реестр 2.8, `ideas/30` п. 25) · `bugs/123` — разведка класса в 2.8; `bugs/77` — владелец; 02/45/61/63/109 — ждут поля или рецидива (история `bugs/66` — летопись, сессия 48).
