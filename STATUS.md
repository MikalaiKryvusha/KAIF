# KAIF — текущий статус

> Читай перед каждой задачей. Обновляй при каждом значимом изменении состояния. Это основная передача
> эстафеты между сессиями, теряющими контекст — пиши так, чтобы свежая сессия стала продуктивной по
> одному этому файлу.
> 🧠 Главный принцип мышления — `PHILOSOPHY.md`. Рабочий канон — `AGENT_GUIDE.md` (учти фрактальную
> двухслойную структуру: полезная нагрузка `framework/`+`KAIF.md` vs. обвязка для dogfooding в корне).
>
> ⚠️ **STATUS — СВОДКА «сейчас», не летопись** (канон 2.1, эпик H). Каждая строка проходит два
> теста: «уберу — следующий агент ошибётся?» и «читается целиком за один присест» (мягкий ориентир
> ~200 строк). Закрытое ПЕРЕЕЗЖАЕТ дословно в `PROJECT_HISTORY.md` («стрижка бонсая» в
> `/end-chat`), а не копится здесь. Летопись прежних сессий/релизов — там же.

---

## Что сделано (короткий хвост — старшие записи в PROJECT_HISTORY.md)

### Сессия 2026-08-08 (двадцать четвёртая, в работе) — **T5 и T8 ЗАКРЫТЫ** 🟢

- **T5 закрыта 2026-08-08 08:40 +03:00** (`plans/26` §4, решение №53 «малой кровью»). Дефолт был
  origin и ДО задачи — закрыл её не факт кода, а **страж**: свод `s01` получил пять ассертов
  (маркер установки без единого флага = `tracking origin` + адрес origin · `--mode anonymous` →
  anonymous без поля origin · `update` на анонимном отказывается с названной причиной). Стережём
  НАБЛЮДАЕМЫЙ маркер, а не строку помощи: помощь — проза, маркер — поведение. Красный доказан
  мутацией дефолта на `'anonymous'` — оба ассерта S1 красные, пара S2 зелёная. README обеих
  половин §2.3 и `KAIF_REFERENCE` §11.3/§12.1 называют дефолт; `policyChanges` НЕ тегируется —
  поведение не изменилось, тег объявил бы полю несуществующую смену режима.
- **T8 закрыта 2026-08-08 09:35 +03:00** (`plans/26` §7, решение №49 — КАНОН соглашений KAIF).
  Метка момента несёт И дату, И время, И смещение зоны (часы владельца): проза —
  `ГГГГ-ММ-ДД ЧЧ:ММ ±ЧЧ:ММ`, квитанции машинерии — тот же момент локальным ISO 8601
  (`localStamp()` вместо трёх `toISOString().slice(0,10)` в расписке, `history` и `verifiedAt`).
  **Скоуп задаёт САМА МЕТКА** (прецедент оси имён T2): примета — глагол принятия/фиксации/
  закрытия вплотную к дате, порог — СОБСТВЕННАЯ дата метки; поэтому канон действует вперёд по
  построению, и базовой линии, которую надо вести, не заводится, а 46 исторических меток молчат.
  Красный доказан дважды: селфтест `doc-header-lint` 14 → **16** (фикстура держит все состояния,
  включая три МОЛЧАНИЯ — история, существительное «Закрытие», сводка «закрыты») и живой корень
  0 → мутация настоящей метки → 1 → откат → 0. Конвенция закрытия `DONE (date)` → `(date + time)`
  доехала до точек решения обоих слоёв **и девяти языковых пакетов** — ресинка потребовал страж
  протухания (`bugs/44`) на первой же сборке. Заголовки `EXPERIENCE` в охват не вошли осознанно:
  дата в ключе записи — идентификатор, а не момент решения (граница названа в самом каноне).

### Сессия 2026-08-08 (двадцать третья) — **T2 ЗАКРЫТА + КОНТУР ПЕРЕДЕЛАН ПОД ВЛАДЕЛЬЦА** (bugs/51–53) 🟢

- **T2 (`plans/26` §3, все 3 шага) закрыта 06:11 +03:00.** Конвенция, полгода жившая ДЕ-ФАКТО,
  записана в канон обоих слоёв и **взята под стража**: эпик = `NN_EPIC_<имя>.md` (пофазовая
  архитектура, никакой операционки), ребёнок = `NN_epicMM_<фаза>_<имя>.md` (`MM` — номер родителя),
  самостоятельный план = `NN_<имя>.md`; явная строка «конвенция действует вперёд — старые планы не
  переименовываются». Поверхности: README `plans/` обоих слоёв · `/plan-epic` и `/plan-task` обоих
  слоёв · **9 языковых пакетов**. Последнее вскрыл страж протухания пакетов (`bugs/44`) на первой
  же сборке: локализованный `plans/README.md` едет в девяти пакетах, и без стража конвенция
  доехала бы до поля по-английски в девяти локализациях — молча. **Правило-как-проза стало
  стражем:** ось ИМЁН вшита в `tools/doc-header-lint.mjs` (у каждого `NN_epicMM_*` родитель `MM_`
  существует И назван `MM_EPIC_*`); скоуп задаёт САМ ПАТТЕРН — старые планы его не используют и
  молчат, порог по номеру был бы константой без работы. Красный доказан мутацией: селфтест
  12 → **14** находок, свод **s13** проверяет три состояния на живом корне, включая обратное
  (переименовал родителя — страж замолчал). Живой прогон по `plans/`: **25 детей, 6 эпиков,
  0 нарушений**. Заодно синхронизирована таблица §1 зонтика — строки T1/T7/T9 стояли «открыта»
  после закрытия, и отмечены чекбоксы закрытых секций (пинок навигирует по последнему отмеченному
  боксу — неотмеченный бокс в закрытой секции это ловушка, а не украшение).
  Гейты: «all 14 suites green» · «counters OK — 17 зеркал» · «doc-header-lint 97/0» ·
  «requirements-lint 141/0».
- **Критерий 9 задачи T1 СНЯТ ПОЛНОСТЬЮ (06:00 +03:00) — чистый прогон пинка.** Владелец открыл
  НОВЫЙ чат и запустил сессию **голым `/kaif-go`**, не `/resume`: точка продолжения названа одной
  строкой, реплики «продолжать ли» не было. **Находка прогона:** холодный контекст закрыла не
  оговорка навыка «пустой контекст → `/resume`», а **ветка триггера Шага 2** — хук впрыснул
  «last refresh 350 min ago», и агент освежил девятку ядра до работы. То есть в системе С ХУКАМИ
  пинок в новом чате самодостаточен по построению, а оговорка про `/resume` страхует развёртывания
  БЕЗ хуков; текст навыка этого пока не называет (кандидат в правку формулировки, не дефект —
  обе ветки дали верное поведение).
- **ТРИ ДЕФЕКТА КОНТУРА, все найдены ВЛАДЕЛЬЦЕМ на живой странице (06:40–07:40).** Общий корень
  один: приёмка проверяла ПОВЕДЕНИЕ и ни разу — то, что ЧИТАЕТ человек.
  **`bugs/51`** — варианты, записанные таблицей, не становились радиокнопками; страж против этого
  класса (G11) существовал и зеленел, потому что его примета знала ту же единственную форму, что и
  парсер: сверка давала `0 == 0`. Вторая итерация того же бага: кнопки появились, но потеряли
  БУКВЫ — рекомендация «вариант C» не ложилась ни на одну.
  **`bugs/52`** — очередь рендерилась одной простынёй, поэтому запись по ОДНОМУ документу
  завершала контур и уносила окно с ещё не отвеченными; вдобавок черновики чистились по ключу всей
  пачки. **Форму задал владелец:** входная страница — только карточки, документ открывается своим
  окном; контур завершает закрытие ВХОДНОЙ страницы.
  **`bugs/53` (тяжелейший)** — ответ владельца записывался в документ и НЕ РАСПОЗНАВАЛСЯ разбором:
  метка времени канона T8 несёт вложенные скобки `(+03:00)`, а регэксп метки использовал `[^)]*`.
  Работа человека существовала на диске и не существовала для системы; дефект ВЫКЛЮЧАЛ целого
  стража (`questions-guard` молчал). Найден не гейтом, а живым круговым прогоном «записал →
  прочитал обратно», который я писал для `bugs/52`.
  Плюс полевые правки интерфейса по словам владельца: убраны пояснения агента о собственном
  устройстве и коды инвариантов (`(D)`, `(P7)`, `(I11)`) из подписей полей; контраст чипов
  **замерен** (белым было 2.15–2.54 при норме WCAG 4.5 → стало 7.4–11.3); рекомендация помечена
  чипом на САМОМ варианте. Приёмка контура **95/3 → 119 зелёных, 0 красных**; новые блоки QA8
  (форма очереди на живом сервере) и QA9 (контраст считается, интерфейс не говорит кодами).
- **Интервью №012 и №013 отвечены и разнесены** (решения **№54–№58**): трейлер коммитов несёт
  ФАКТИЧЕСКОЕ имя модели — зашитой константы в `tools/commit.mjs` больше нет, имя передаёт сессия
  (`--as` / `KAIF_AGENT_MODEL`), без имени трейлер не пишется; ретроспектива не трогается;
  восемь языковых пакетов ЗАМОРАЖИВАЮТСЯ в 2.3 с явным объявлением (ведём только `ru`/`en`);
  2.3 — обычный релиз, сокращение поверхности пунктом внутри него.

> Сессии «пятая»–«двадцать вторая» перенесены дословно в `PROJECT_HISTORY.md` (стрижка бонсая:
> пятая–одиннадцатая — при закрытии пятнадцатой, двенадцатая–девятнадцатая — при закрытии
> двадцатой, двадцатая–двадцать вторая — при закрытии двадцать третьей). Там же — эпики L, K, M
> целиком, весь эпик N (N1–N5), фазы O1–O5, L1–L4, задачи T1/T7/T9/T10, планирование 2.2 и всё
> старше. **Задача T1 (навык-пинок `kaif-go`) закрыта в двадцать второй сессии** — её запись там.

---

## Где мы сейчас

**KAIF 2.1 «Strong KAIF» ОПУБЛИКОВАН** (2026-07-31,
https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.1). **KAIF 2.2 «Yolden KAIF» в
исполнении**: спланирован целиком 2026-08-07 (`researches/15` → зонтик `plans/26`; интервью
007 — решения №32–46; скоуп облегчён №47/№48). **ЭПИКИ L И K ЗАКРЫТЫ ЦЕЛИКОМ 2026-08-07**:
L — все 5 фаз, беклог `bugs/31–44` DONE, панель 4× VERIFIED WITH CAVEATS; K — все 5 фаз
(реестр `researches/16` · канон I1–I36/M/P/G/T/C/QA/DEF · исполнимый контракт · стражи и
приёмка · dogfooding-контур этого репо, принятый ЖИВЫМ пилотом владельца — интервью 008),
судья VERIFIED WITH CAVEATS 0 блокеров, §12 issue #2 — 11/11 (тикет закрывается в Фазе R
через `send-outbound`). **ЭПИК M ЗАКРЫТ ЦЕЛИКОМ 2026-08-07** (все 5 фаз: формы петли —
`researches/18`; `reports/` в поставке, миграция №41; протоколы в навыках и машинерии с
исполняющим чекпоинтом отчётов; принцип п. 14; судья №48 VERIFIED — `plans/29`/`plans/51`).
Эпик P — ⛔ NOT TO BE DONE в 2.2 (№47).
**ЭПИК N ЗАКРЫТ ЦЕЛИКОМ 2026-08-07** (N1 — REQUIREMENTS_FRAMEWORK, 14-й ключевой документ +
линтер-страж; N2 — вектор цели в шаблонах; N3 — шапка-мета документов +
`tools/doc-header-lint.mjs`; N4 — таксономия 5 ярусов + шапка-мета в payload + термин «контур»
с инвентарём; N5 — линтер требований целевых документов с композицией стоп-словаря + судья
эпика VERIFIED 7/7 — `plans/30`). **ЭПИК O ЗАКРЫТ ЦЕЛИКОМ 2026-08-07 21:47 +03:00** (O1 — карта
хук-механизмов + проекты `researches/19`; O2 — канон освежения обоих слоёв + маркер-свидетельство
+ охота судьи + 7 навыков ритуалов — `plans/56`; O3 — модуль `.kaif/hooks/` в поставке со сводом
s14 + досье окружения + страж счётчиков `bugs/49` — `plans/57`; O4 — хуки ПОДКЛЮЧЕНЫ в
`.claude/settings.json` и ВСЕ ТРИ наблюдены живьём каждый своим событием — страж STATUS (блок +
изолированный cooldown) · таймер обеими половинами · впрыск `SessionStart:clear`; досье
переснято 13/13, судья эпика **VERIFIED 7/7** — `plans/58`; **O5 ✅ ЗАКРЫТА тем же днём** —
хук-конфиги под остальные агентские системы: 10 систем сняты живыми источниками, 4 образца,
адаптеры ресинкнуты + новый `google-antigravity.md`, `plans/60`). Эпик успел быть закрытым в
21:47 и переоткрытым решением владельца №50 через 18 минут — обе вехи в `plans/31`.
**ЗАДАЧА T10 ЗАКРЫТА 2026-08-07 23:30 +03:00** (`plans/61`; идея 21 помечена DONE) — класс
«сообщение» в контуре: `--notice`, штатный исход «прочитано», отдельная группа под вопросами,
инварианты I37/I38 в обоих слоях, ростер s12 38 позиций.
**Действующий порядок остатка 2.2: T3–T6, T8 → Фаза R** (T1, T2, T7, T9, T10 закрыты). ⚠️ **Гейт Фазы R красный:** её
скелет требует «все задачи T1–T9 закрыты», а инвентарь пробами (`plans/59` шаг 0) показал восемь
открытых задач и одну наполовину; решением №52 владелец оставил в 2.2 **все девять**.
Актуальная сборка — 14 ключевых документов / 7 README /
**35 навыков** / 57 блоков FULL / **160 бандла** / **679 модулей** (цифры печатает сборка — не
переписывай руками; с плана 62 их стережёт `node tools/counters-guard.mjs`); полигон `npm run test:core` — **14 сводов, зелёный** (s14 — модуль
refresh-hooks); контур: `node tools/review.mjs --queue` (очередь страницей) · `node
tools/questions-guard.mjs`; линтер документов знаний: `node tools/doc-header-lint.mjs`
(шапка-мета + блок вектора + словарь требований, консультативный); страж счётчиков прозы:
`node tools/counters-guard.mjs` (пять зеркал, `--selftest`).

---

## 📦 Задел на 2.3 (решения владельца 2026-08-08, НЕ в 2.2)

- **Заморозка восьми языковых пакетов** (решения №56/№57): ведём только `ru` и `en`; восемь
  остальных замораживаются с ЯВНЫМ объявлением — на какой версии, в каком состоянии, почему
  («тяжело в поддержке, пока не окупается»), оживление по запросу сообщества. Страж протухания
  требует ресинк только для ведомых языков. Постановка — `interviews/interview_013` Q1.
- **Форма 2.3** (решение №58): обычный релиз со скоупом из новой идеи владельца; сокращение
  поверхности (пакеты · ревизия 35 навыков «каждый ли зарабатывает своё место» · удвоение
  payload/обвязка) — пунктом ВНУТРИ него. **Проработка идей начинается сейчас**, разведка — через
  многоагентную оркестрацию; включает её владелец по просьбе агента.

---

## 🤖 Пул автономного беклога (остаточные хвосты)

- [ ] Постоянное покрытие в полигоне: tamper-тест sha256-гейта ЛОАДЕРА + twin-run byte-exact
      ассерт в s01 (принятый долг закрытия эпика L — находка №3 линзы 3; сейчас доказано
      только сессионными стендами).
- [ ] Первый полевой прогон `/guarded-loop` (дефер G4).
- [ ] Хвосты контура по потребности (интервью №008, Q3 + `researches/17` §4): G4-страж
      содержания вопроса · P6-медиавставки (iframe srcdoc макетов + «открыть отдельным
      экраном» — рецепт NDim готов) · карточка исходящего артефакта на странице (к Фазе R) ·
      «пометка для агента» цитатой `>` построчно · list-команда · детектор «поле-подсказка ≠
      статус».
- [ ] Адаптеры под остальные системы (Devin, OpenCode, Aider, Junie — сведения в `researches/01`).
- [ ] Библиотеки терминов по остальным сферам из `_template`.
- [ ] Идея fork-bundle (хвост bugs/16) и token-F1 ступень kaif-provenance — после полевой обкатки.
- [ ] Контр-стражи, честно отложенные в bugs/26/28.

> Секция «❓ Ожидает владельца» удалена по слову владельца (интервью №008, Q4, вариант B):
> просьбы владельцу живут ТОЛЬКО в `homeworks/`/`interviews/` (текущая — `homeworks/04`,
> README 2.1 глазами); очередь показывает `node tools/review.mjs --queue`, нарушения ловит
> `node tools/questions-guard.mjs`.

---

## Где продолжить в следующей сессии

> 🚩 **ПЕРВОЕ ДЕЙСТВИЕ СВЕЖЕЙ СЕССИИ — освежить канон.** Маркер `.kaif/refresh-marker.json`
> намеренно оставлен протухшим на закрытии чата (перечитывать девятку, чтобы тут же выбросить
> контекст, — трата; штамповать без чтения — фрод класса ложного `[TESTED]`). Хук напомнит сам;
> `/resume` закрывает это полным проходом канона. Прецедент — `plans/58` → «Наблюдения».

1. **ЗАДАЧИ T3–T6, T8 — БРАТЬ ПЕРВЫМИ** (решение №52, 2026-08-07 22:16 +03:00; перенесённых нет;
   **T9, T7, T1 и T2 закрыты 2026-08-08**). Осталось ПЯТЬ задач.
   Открыты: T3 ревизия
   `/code-revision` → `reports/KAIF_AUDIT` · T4 портрет-файл `AUTHOR_STYLOMETRY.md` (нужен
   владелец) · T5 малой кровью — свод «дефолтная установка = origin» + строки в доках (решение
   №53) · T6 канон test suite · T8 канон меток дата+время (строки и стража нет).
   Постановки — `plans/26` §4–§7, `plans/33`, `plans/34`.
   ✅ **T1 ЗАКРЫТА** (00:40 +03:00): навык `kaif-go` (алиас `/go`) в обоих слоях + 9 языковых
   пакетов; ось навыков взята под стража (`counters-guard` 5 → 17 зеркал); `bugs/50` заведён и
   закрыт тем же заходом (`plans/62`). **Критерий 9 снят полностью 06:00 +03:00** — голый пинок в
   новом чате повёл сессию; хвоста на владельца больше нет.
   ✅ **T2 ЗАКРЫТА** (06:11 +03:00): конвенция `NN_EPIC_*` / `NN_epicMM_*` в README обоих слоёв +
   9 языковых пакетов + `/plan-epic` и `/plan-task` обоих слоёв; ось ИМЁН `plans/` взята под
   стража в `doc-header-lint` (селфтест 12 → 14, свод s13 — три состояния); живой прогон
   25 детей / 6 эпиков / 0 нарушений (`plans/26` §3).
   ✅ **T9 ЗАКРЫТА** (00:10 +03:00): дословное определение владельца «KAIF — это…» вернулось в обе
   половины README побайтно (извлечено из `git show 1dd1747:README.md`, не перепечатано), на
   исходное место — EN снова строка 28; PDF пересобран (`plans/26` §8).
   ✅ **T7 ЗАКРЫТА** (00:25 +03:00): принцип «Код прежде когниции — детерминизм первым, модель
   вторым» в обоих слоях `PHILOSOPHY` (за Парето, пара 80/20 читается вместе); `AGENT_GUIDE`
   ссылается на него вместо второго канона; T7.3 (Оккам не для перцептивного) был выполнен ранее.
   Карта модулей выросла 671 → **672** (`plans/26` §6).
   > **Критерий владельца на весь остаток 2.2 (дословно, решение №52):** «мы не торопимся. Мы
   > делаем хорошо и полно, качественно, и тестируем, и полируем, чтобы 2.2 получилась
   > полноценной и крепкой версией». Развилки скоупа этой версии решаются по ПОЛНОТЕ, не по
   > скорости — предлагать облегчение как рекомендацию больше не надо.

2. **ФАЗА R — последняя** (`plans/59`): суд всего 2.2 → issue #2 через `send-outbound` →
   `/release`. Гейт снимается работой по пункту 1, а не решением.
   **Полный порядок остатка 2.2: T3–T6, T8 → Фаза R** (O5 и T10 закрыты 2026-08-07; T9, T7, T1,
   T2 — 2026-08-08).
   > 💡 **Совет по инструменту сессии** (слово владельца, 2026-08-07 23:05 +03:00): до T3 и Фазы R
   > хватает обычного прогона на max. **T3** (`/code-revision` — зонирование, параллельные ревизоры,
   > скептик на каждую находку) и **Фаза R** (полный адверсарный суд всего релиза) — те два места,
   > где многоагентная оркестрация окупается; владелец просил СКАЗАТЬ ему явно в этот момент, а
   > включает её он сам.
3. **Порядок по ЧАТАМ — решение владельца 2026-08-08 07:55 +03:00** («сначала добить 2.2»,
   разведку 2.3 отложить):
   - **этот, следующий чат: T5 → T8 → T6** — три канон-задачи одного жанра (постановки —
     `plans/26` §4, §7, §5). T5 закрывается «малой кровью» по решению №53: правка кода не нужна,
     нужен свод полигона «дефолтная установка = origin» + строки в README/`KAIF_REFERENCE`;
   - **отдельный чат: T3** (`plans/33`) — там владелец включает многоагентную оркестрацию сам;
   - **когда будет время владельца: T4** (`plans/34`) — портрет голоса снимается с ЕГО текстов;
   - **отдельный чат: Фаза R** (`plans/59`) — суд, issue #2, релиз.
4. **Ничего не ждёт владельца — очередь пуста** (`node tools/questions-guard.mjs` → «Ждут ответа:
   0»). Интервью №012 и №013 отвечены 2026-08-08 и разнесены (решения №54–№58). **Новые развилки
   поднимать контуром** (`node tools/review.mjs --queue`), а не репликой в чате — слово владельца:
   «по вопросам ко мне — интерактивный контур». Ближайшая известная развилка — T4.
   ⚠️ **Трейлер коммитов сменил контракт (решение №54):** зашитого имени модели больше нет —
   коммить `node tools/commit.mjs --msg-file <путь> --as "<имя своей модели>"`; без `--as`
   инструмент предупредит и уйдёт БЕЗ трейлера (это норма, а не поломка).
5. Открытым остался только `bugs/02` (ждёт полевого `/dayloop`) и автономный пул выше
   (первым — tamper-тест лоадера в s01). Плюс названный долг фазы O5: машинерия
   `deployAgentSystems` не знает про Antigravity — адаптер документационный, автодеплой навыков
   туда не заведён (путь навыков не верифицирован; вписывать угаданный запрещено).
6. Проверка после любой правки машинерии: `node tools/build-framework.mjs` + `npm run
   test:core` (**14 сводов**; s14 — модуль refresh-hooks); реестр пар — команды в
   `AGENT_GUIDE.md` (счётчики — `node tools/counters-guard.mjs`); страж места вопросов
   `node tools/questions-guard.mjs` — в `/end-chat` и `/release`. Числа в коммиты — только из
   вывода инструментов (EXP-0025). **Тронул контур** (`tools/review.mjs`, `tools/lib/review-core.mjs`)
   → обязателен `node tools/verify-contour.mjs` (**119 проверок**, живой браузер; блоки QA8/QA9
   стерегут форму очереди и читаемость интерфейса) и пересмотр эталона глазами при новых живых
   документах: `node tools/verify-contour.mjs --write-etalon`.

---

## Открытые баги

- `bugs/02_loops_self_stop_on_context_overflow.md` — 🔧 фикс отгружен (2026-07-07); ждёт полевой
  проверки в реальном длинном `/dayloop` (мегацикл 2026-07-28 прошёл без самоостановок — косвенная
  улика в плюс, но это был не /dayloop).
> `bugs/49` (счётчики README протухли — страж стерёг 1 зеркало из 5) заведён и закрыт
> 2026-08-07 тем же заходом: класс закрыт стражем `tools/counters-guard.mjs`.

- `bugs/45_working_copy_reverted_under_live_session.md` — 🔬 research-only: рабочая копия
  `plans/26` откатилась к старой версии посреди сессии (2026-08-07); состояние восстановлено из
  HEAD, класс пойман предкоммитным диффом; причина — гипотеза (протухший буфер редактора),
  механизация — при рецидиве.

> Весь полевой беклог эпика L 2.2 (`bugs/31–44`) закрыт 2026-08-07 пятью фазами; весь беклог
> 2.0–2.1 (bugs/01–30, кроме 02) закрыт ранее — хроника закрытий в летописи.
> `bugs/50–53` заведены и закрыты 2026-08-08 (трейлер коммитов · три дефекта контура, все три
> найдены владельцем на живой странице).
