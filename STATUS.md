# KAIF — текущий статус

> Читай перед каждой задачей. Обновляй при каждом значимом изменении состояния. Это основная передача
> эстафеты между сессиями, теряющими контекст — пиши так, чтобы свежая сессия стала продуктивной по
> одному этому файлу.
> 🧠 Главный принцип мышления — `PHILOSOPHY.md`. Рабочий канон — `AGENT_GUIDE.md` (учти фрактальную
> двухслойную структуру: полезная нагрузка `framework/`+`KAIF.md` vs. обвязка для dogfooding в корне).
>
> ⚠️ **STATUS — СВОДКА «сейчас», не летопись** (канон 2.1, эпик H). Каждая строка проходит два
> теста: «уберу — следующий агент ошибётся?» и «читается целиком за один присест» (мягкий ориентир
> ~200 строк). Закрытое ПЕРЕЕЗЖАЕТ дословно в `PROJECT_HISTORY.md` («стрижка бонсая» в
> `/end-chat-soft`), а не копится здесь. Летопись прежних сессий/релизов — там же.

---

## Где мы сейчас

**Версия KAIF 2.5 (рабочее кодовое имя «E») ОТКРЫТА 2026-09-04 00:24 +03:00** — интервью
№020: Q1 = A разрез принят · Q2 = A ответы в issues после релиза · Q3 = D «Пока без названия.
Решим ближе к релизу. Кодовое имя "E"» · Q4 = A срезы KAGO 07/08/09 в CN (решения №83–№86).
Мета-план — `plans/82`: **CN «Канон точек решения» → SG «Канал сигналов машинерией» → TM
«Команда в поле + CI» → US «Симметрии обновления» → RL**, ≈ 8–10 чатов; разведка —
`researches/24`; отложенное (T · V · P · X-полный · селфтесты · U3/U4 · TD-фазы) — `ideas/28`.
**Эпик CN ЗАКРЫТ 2026-09-04 01:40 +03:00** (`plans/83`, судья VERIFIED WITH CAVEATS): пять
носителей в точках решения обоих слоёв + `kaif-guard-lint` + свод s15 (`2fef738`) + CN6 —
`DELIVERY:` · лестница тяжести S1/S2/S3 · бюджеты `DOC_BUDGETS` в `check` со сводом s16 ·
каденция молитвы (сессия 48). **Эпик SG ЗАКРЫТ 2026-09-04 02:00 +03:00** (`plans/84`): команда
`kaif-core report` в ядре + свод s17 + шаг 4 `/report-bug` «запусти» (EN-поставка; RU-копия —
отказ классификатора, см. п. 3). **Эпик TM ЗАКРЫТ 2026-09-04 09:12 +03:00** (`plans/85`,
судья VERIFIED WITH CAVEATS; подробности — летопись, сессия 49). **Эпик US ЗАКРЫТ 2026-09-04 11:52 +03:00**
(`plans/86`, сессии 49–50, `c597773`…; лёгкий судья VERIFIED WITH CAVEATS — US7): US0–US7 —
неделимость якорной пары в
`mergeModules` (двухфазный откат + пункт `(anchored block KAIF:NAME)` + перенос точки вставки за
закрывающий модуль), парность в `check`, безусловный `stale-claims` со сканом скриптов проекта,
печать вердикта wholesale, `git diff` с картой `sources`, `language-arrivals`, EOL по
доминированию; свод s18 (U1–U9, включая новый U1б) весь зелёный и вписан в полигон
восемнадцатым; US3б — обязывающая репетиция (`diff --source` пишет вердикты, `update`
замораживает при расхождении, пункт `verdict-mismatch`); US5 закрыт целиком (quote-skip
предикат · преемник и счёт deprecation · `project-name` до акта · `mode-switch` при
anonymous→origin · EOL · `language mix` · `< /dev/null`); US3в — причина P1 ПОДТВЕРЖДЕНА
пробой «две папки» (`bugs/100`: H1 синтетического baseline из имени папки при v1-манифесте без
`values`, дерево без `package.json` `name` → `baseFound` ±1 на потолке; лечение — RL/2.6).
US6 — ноты/policy 2.5 в сборщике (INERT), реестр пар зелёный; US7 — судья, `plans/87` (эпик RL)
написан, `plans/82` ряд 4 ✅. **Следующий — эпик RL «Суд и релиз» (`plans/87`).** **Метрика доставки версии: критерии приёмки 2.5
закрыты 4 из 8.** Предыдущий релиз — 2.4 Teamed Up KAIF, 2026-08-28 (`v2.4`, `latest`; абзац релиза —
в летописи).

⚠️ **Молитвы канонизированы (2.4, PY):** перед работой произносится символ веры + молитва
(`AGENT_GUIDE.md`, блоки `KAIF:CREED`/`KAIF:PRAYER`; шаг в `/resume`). **Церемонии закрытия
раздвоены (SC):** `/end-chat-soft` (заявка заранее → доделать до среза → полные церемонии) и
`/end-chat-force` (срочно, с явной строкой долга); старого `/end-chat` больше нет. **Названное
время окончания = СТАРТ мягкого закрытия**, не дедлайн (`AGENT_GUIDE` → «Работа до названного
времени»).

**Кодовое имя версии пишется БЕЗ КАВЫЧЕК** (канон-таблица идентичности `AGENT_GUIDE.md`); правила
витрины, оплаченные волнами 2.2, — `plans/69` (три правила) и `AGENT_GUIDE.md` → «Витрина»
(десятка); вектор целей — порядок владельца в `researches/18` §2 (№67).

Актуальная сборка — 14 ключевых документов / 7 README / **37 навыков** / 59 блоков FULL /
**170 бандла** / **764 модуля** (цифры печатает сборка — не переписывай руками; стережёт
`node tools/counters-guard.mjs`); полигон `npm run test:core` — **18 сводов, зелёный**, четыре
преполёта.

---

## 🎯 Где продолжить

> 🚩 **ПЕРВОЕ ДЕЙСТВИЕ СВЕЖЕЙ СЕССИИ — освежить канон.** Маркер `.kaif/refresh-marker.json`
> намеренно оставлен протухшим на закрытии чата (перечитывать девятку, чтобы тут же выбросить
> контекст, — трата; штамповать без чтения — фрод класса ложного `[TESTED]`). Хук напомнит сам;
> `/resume` закрывает это полным проходом канона.

### 1. 👉 СЕЙЧАС — эпик RL по `plans/87`: RL0 нулёвка → RL1 суд одним кругом → RL2 витрина → RL3 черновики ответов → RL4 имя (владелец) → RL5 релиз по слову владельца → RL6

1. ✅ **Сессии 48–49 (2026-09-04 01:2x–10:0x +03:00, коммиты `8f19275`…`095c1c4`)** — дословно в
   летописи: эпики CN, SG, TM закрыты; эпик US открыт (US0–US1, 7 из 9 стражей s18 зелёные).
2. ✅ **Сессия 50 (2026-09-04 10:3x–11:0x +03:00):** US2б + US4г (в эстафете звалось «US5г») +
   s18 в полигоне — подробности в `plans/86` (шаги US2/US4, решения 7–9) и EXP-0104. Один раз
   полигон покраснел на s07 T1 (`update` умер ДО применения: в дереве `kaif.json` 2.4, задания
   нет, `UPSTREAM` в файлах нет; хвост вывода — строка language-pack), три повторных прогона
   тех же входов (s07 отдельно, репро-скрипт, полигон) зелёные. **Повторился** в защищённом
   цикле (полигон-9, s07 T2, та же форма: `update` exit≠0 сразу после строки language-pack,
   задания нет) — два вхождения на ~10 прогонов полигона, оба в s07 (ru-обновления без
   `--baseline` → синтетический baseline тянется с origin; `fetchMaybe` мягкий, причина НЕ
   названа). Сделано: `run()` s07 пишет ПОЛНЫЙ вывод упавшей команды в `run-fail-N.log` корня
   прогона (+ `maxBuffer` 64 МБ). **Следующее вхождение = баг S2 с этим файлом** (следующий свободный номер в `bugs/`);
   до тех пор — «не воспроизведено, наблюдается».
   ✅ **US3б (сессия 50, защищённый цикл 11:16–12:46):** репетиция стала ОБЯЗЫВАЮЩЕЙ —
   `diff --source` печатает вердикт по каждому кандидату и пишет `.kaif/update-rehearsal.json`;
   `update` (авто-запись того же интервала или `--rehearsal <квитанция копии>`) замораживает
   файл, чей боевой `merged` расходится с репетиционным `frozen`, пункт `verdict-mismatch` и
   квитанция несут оба набора чисел; s18 U3б ×8 (красный на ядре `46a5ba7`), полигон 18/18;
   `/kaif-update` (поставка + байтовая копия обвязки) объясняет обе формы репетиции.
   ✅ **US5 закрыт (защищённый цикл, ≈11:30–11:46 +03:00):** (а) один `placeholderSurfaces()` для гейта и
   пункта `placeholders` (s18 U12) · (б) преемник и счёт deprecation, квитанция `deprecations`
   (U10) · (з) `project-name`: файловая форма в обоих пунктах, искажённое argv-имя отвергается
   до записи (U11) · (и) пункт `mode-switch` при anonymous→origin (U13); каждый страж доказан
   красным на ядре до фикса, полигон 18/18 после каждого. **US3в:** причина P1 — `bugs/100`,
   механизм ПОДТВЕРЖДЁН пробой `tools/sandbox/probes/bugs-100-two-folders.mjs --no-package`
   (то же дерево: `alpha-project` → `baseFound 5 → merged`, `beta-project` → `baseFound 4 →
   frozen`; с `package.json` `name` ось мертва); кандидат лечения (а) — H1 вне счёта wholesale +
   свод «две папки» в s18; лечение — эпик RL/2.6, защита US3б уже стоит.
   ✅ **US6 + US7 (≈11:48–11:55):** нота «UPDATE SYMMETRIES» и policy-строка «FOUR behaviours»
   в сборщике (INERT до version 2.5); реестр пар прогнан; лёгкий судья — VERIFIED WITH CAVEATS
   (`plans/86` US7); `plans/87` (эпик RL) написан; `plans/82` ряд 4 ✅; `MASTER_PLAN` §8 — 4 из 8.
   👉 **ПЕРВЫМ в новом чате — RL0 по `plans/87`:** перечитать критерии 5–8 `plans/82`, свести
   «Решения без владельца» четырёх эпиков (`plans/83`–`86`) в один список для суда, сверить
   `researches/24` §3 «2.6» ↔ `ideas/28`; затем RL1 — суд одним кругом (форма №74, реестр в
   `reports/KAIF_AUDIT/`), RL2 витрина + релиз-ноты 2.5 из нот сборщика, RL3 черновики ответов во
   все открытые issues (показать страницей контура), RL4 — вопрос владельцу об имени (№85), RL5 —
   релиз ТОЛЬКО по слову владельца.
   (ноты (10) и policy (7) 2.5 в сборщике INERT: строка эпика US ОБЯЗАНА назвать «`check`
   краснеет на непарном маркере» и «`stale-claims` всегда в задании, сканирует и скрипты»;
   Reference; реестр пар; `doc-header-lint`) → US7 (судья, план RL, метрика 4 из 8).
3. ⚠️ **Для глаза владельца — решения без него** (`plans/83`/`84`/`85` → «Решения»): каденция молитвы
   вписана императивной фразой в директиву блока `KAIF:PRAYER` обоих слоёв (16 пунктов и граница
   не тронуты; RU-блок — текст его приёмки) · метрика доставки KAIF = «критерии 2.5 закрыты N
   из 8» (`MASTER_PLAN` §8) · числа бюджетов девятки в `DOC_BUDGETS` (`AGENT_GUIDE` 1200 ·
   `REQUIREMENTS` 250 · прочие 300; STATUS 200 — его №27) · **RU-копия шага 4 `/report-bug`
   (`.claude/skills/report-bug`) НЕ переписана**: правка отклонена классификатором автоправ Claude
   Code — живое воспроизведение NDim `bugs/KAIF/01` в истоке; обход не делался; фразу можно внести
   рукой по EN-версии шага 4 · **борд команды по умолчанию ВНЕ git** и **CI едет с командой** —
   policy-изменения 2.5 в сборщике (`plans/85`) · **adopt-путь вынесен референсом
   `team-adopt.md`** (навык 178 при потолке 179) и **RU-копия `/team-deployment` синхронизирована
   с поставкой** на закрытии TM без пункта плана, плюс строка реестра пар «состав референсов
   навыка ↔ обвязка» (`plans/85` решения 6–8, EXP-0103) · **`check` КРАСНЕЕТ на непарном
   якорном маркере** (вес двухголового документа: полевое дерево с END без BEGIN станет красным
   после обновления 2.5 — это сигнал, а не поломка) · **ничья CRLF = LF → CRLF** · **новое поле
   `sources` в мета-блоке бандла** · строки `language mix`/`language-arrivals` — честность, не
   отказ · **пара, уже сломанная на диске, при обновлении НЕ откатывается** (пункт задания +
   красный `check` до ручного восстановления) · **скан `stale-claims` по скриптам — белый список
   расширений + `package.json`, lock-файлы вне скана** · **репетиция обязывает только в опасную
   сторону** (frozen→merged замораживает; merged→frozen лишь называется) · **non-ASCII в argv
   `project-name` остаётся предупреждением, отказ — только искажённому значению** · **пункт
   `placeholders` = поверхности гейта** · **`mode-switch` ловит лишь механически обусловленную
   анонимную формулировку** · **US3в закрыт документом `bugs/100`, лечение — RL** (`plans/86`
   решения 1–14).
4. **Ответы в полевые issues — ПОСЛЕ релиза 2.5 по тикету** (№84 = интервью №020 Q2 = A):
   черновик пишется на закрытии эпика, который тикет закрывает; полевые отчёты получают одну
   благодарность-квитанцию со ссылкой на релиз. Открыто 10 тикетов + 13 отчётов; готовые
   черновики: #23/#24 (`plans/80`) и #27–#38 (все двенадцать; `plans/87` → «Черновики
   ответов», написаны на закрытии US; какие ещё открыты — сверить `gh issue list` в RL3);
   осталось квитанции отчётам и страница контура — RL3 (перед отправкой — сверка
   время-чувствительных форм, EXP-0093). Отправка `send-outbound` — внешнее действие, слово владельца в моменте (№13).
5. **GitHub topics + description** по SEO-плану (`researches/22` §4 п. 5: `ai-harness`,
   `agent-harness`, `ai-agents`, `harness-engineering`) — внешнее действие, по слову владельца.
6. ⚠️ README.pdf перегенерирован 2026-09-04 (сменились два числа — своды и блоки); первый экран
   глазами агента не проверен (на машине нет PDF-рендерера — Ghostscript); прежний PDF открывался
   владельцу, жалоб не поступало.

### 2. ⏳ ДОЛГИ НА ВЛАДЕЛЬЦЕ (агент их не закрывает)

- **`bugs/77` (авторство на витрине)** — формулировки принимает владелец. Готовь варианты и
  показывай СТРАНИЦЕЙ контура (`node tools/review.mjs <док>`), не решай сам.
- **Вычитка витрины 2.2 глазами** — `homeworks/05`. Две волны правок он уже прошёл вживую, но
  домашка как артефакт приёмки остаётся открытой.
- Старший долг того же класса — `homeworks/04` (README 2.1 глазами).
- **Судьба приватных имён в РАБОЧИХ документах и в истории git** — поставка, витрина и слепок
  портрета чисты (`private-names-guard` зелёный); переписывание истории требует force-push, то
  есть строки `AUTH:`.
- **`$62,50` живёт только в релиз-нотах**, в README этого числа нет — владельцу задан вопрос,
  дублировать ли его в README; ответа нет.


### 3. 🧰 Инструменты, которыми работать, а не открывать заново

| Команда | Что стережёт |
|---|---|
| `node tools/build-framework.mjs` + `npm run test:core` | обязательны после ЛЮБОЙ правки машинерии (18 сводов, 4 преполёта) |
| `node tools/counters-guard.mjs` | 50 зеркал живых чисел; ось навыков и документов — ПОИМЁННО |
| `node tools/questions-guard.mjs` | место вопросов · вариант ПОТЕРЯН · вопрос ОТПРАВЛЯЕТ в документ · ратчет долга |
| `node tools/doc-header-lint.mjs` | шапка-мета · метки времени и их ПРАВДИВОСТЬ · закрытие против тела · **PENDING без СУДЬБЫ** |
| `node tools/showcase-lint.mjs` · `estimates-guard` · `private-names-guard` | витрина: 13 классов, симметрия половин (включая подписи таблиц и блоки цитат), источник у оценки, утечка имён,  |
| `node tools/verify-contour.mjs --etalon-only` | дешёвая приёмка контура — в `/end-chat-soft`; полный прогон в браузере — в `/release` |
| `node tools/commit.mjs --msg-file <путь> --as "<имя модели>"` | коммит; **`--only` берёт ОДИН путь НА ФЛАГ**, посторонний аргумент отвергается, новый файл в дереве останавливает подметающий прогон; **гейт 1c** (W2-1): коммит с поверхностью поставки гонит `private-names-guard` |
| `node tools/refresh-stamp.mjs --trigger <…> --docs <…>` | маркер освежения: момент берёт САМ инструмент |
| `node tools/build-logo-title.mjs` (`--check`) | подпись версии на фирменном логотипе: старое лого + перенабранная надпись в стиле, снятом пробами (шрифт, медь, фаска, посадка). **Выпускаешь версию — прогони**, иначе логотип уедет с прошлым именем (так вышло с 2.3). `--check` перенабирает подпись 2.2 и сверяет с оригиналом; красный доказан подменой гарнитуры |
| `node tools/build-story-card.mjs` | Таблица 6 витрины → PNG 9:16 для соцсетей: строки читаются из живого README, кегль ищется замером, типографика лечит переносы |
| `node tools/build-team-logo.mjs` (`--check`) | логотип 2.4: арт v2 владельца (побайтно нетронут) + подпись стилем 2.3 через `renderTitle`; геометрия — плита 2.3 × масштаб арта + ручки вкуса `TUNE_*` (меняются только словом владельца); `--check` — шесть осей вкл. «медь, не серебро» |
| `node tools/kaif-stats.mjs` · `voice-lint` · `guarded-watchdog` | статистика релиза · слой 7A портрета · сторож защищённого цикла |
| `node framework/tools/kaif-guard-lint.mjs check <пути>` (`selftest`) | блок объявления стража `@guard` (THREAT · PROVED-AGAINST · GAP · ON-REAL-PATH) / `@forensic` (EXPLAINS · DURABLE-AT; close/exit/trip-only отвергнуты) / `@fork` (OPTIONS · COST · RECON · DECIDED) — гейт 5, вторая половина (2.5, CN3; issue #35); только явные маркеры, `SKIPPED=3` без них; в поставке — `.kaif/tools/`; свод s15 |
| `node tools/experience-lint.mjs` (`--selftest`) | поле механизации у новых записей опыта (issue #14, эпик X): mechanized/none-cheap/subject-lesson; ловушка по форме требует первых двух; долг — baseline, только убывает; в `/end-chat-soft` |

**Тронул контур** (`tools/review.mjs`, `tools/lib/review-core.mjs`) → обязателен полный
`node tools/verify-contour.mjs` в живом браузере; новый живой документ роняет эталон НАМЕРЕННО —
лечится `--write-etalon` с пересмотром чисел ГЛАЗАМИ.

### 4. ⚠️ Правила, нарушение которых уже стоило дорого

- **Время снимается ПРОБОЙ `date` в тот же ход, что и запись** — не «сессия помнит» (EXP-0078).
- **Вопрос владельцу САМОДОСТАТОЧЕН:** предмет решения приводится ВНУТРИ вопроса, а не адресуется
  ссылкой (EXP-0081, `bugs/83`). Плохо заданный вопрос не раздражает — он фальсифицирует решение.
- **Варианты ответа — СТРОКАМИ ТАБЛИЦЫ** `| **A** | что означает | цена и риск |` **или списком**
  `- **A)** …`; пометка в скобках у буквы законна. Третьей формы парсер не знает.
- **Новые развилки поднимать КОНТУРОМ** (`node tools/review.mjs`, отслеживаемой фоновой задачей),
  а не репликой в чате: слово владельца «по вопросам ко мне — интерактивный контур».
- **Мутация для доказательства делается на КОПИИ**; `git checkout` для незакоммиченного файла —
  не откат, а УДАЛЕНИЕ (EXP-0077).
- **Числа в коммиты и документы — только из вывода инструментов** (EXP-0025).
- **Логи верификации — в ФАЙЛ, не через `| tail`** (EXP-0052): хвост показывает итог, а нужна
  причина.

---

## 🤖 Пул автономного беклога (остаточные хвосты)

- [ ] Хвосты контура по потребности (интервью №008 Q3 + `researches/17` §4): G4-страж содержания
      вопроса · P6-медиавставки · карточка исходящего артефакта на странице · «пометка для агента»
      цитатой `>` построчно · list-команда · детектор «поле-подсказка ≠ статус». Свежие кандидаты
      того же контура (2026-08-21, из bugs/105/106): buildDoc очереди по протухшему kind ·
      atHuman-метка квитанции печатает UTC-момент как локальный (сдвиг +3 ч — проверить пробой).
- [ ] Адаптеры под остальные системы (Devin, OpenCode, Aider, Junie — сведения в `researches/01`);
      по Muse Code вендорская дока ПОЯВИЛАСЬ 2026-08-21 (`researches/19` §1.3 — впрыска контекста
      в контракте нет, образец hooks не пишется).
- [ ] Библиотеки терминов по остальным сферам из `_template`.
- [ ] Идея fork-bundle (хвост bugs/16) и token-F1 ступень kaif-provenance — после полевой обкатки.
- [ ] Контр-стражи, честно отложенные в bugs/26/28.
- [ ] Долг фазы O5: машинерия `deployAgentSystems` не знает про Antigravity — автодеплой навыков
      туда не заведён (путь навыков не верифицирован; вписывать угаданный запрещено).
- [ ] **Следующий прогон `/code-revision`** (что менять — в `reports/KAIF_AUDIT/…_SUMMARY.md`):
      мутировать зеркала, которые селфтест НИКОГДА не красит · научить селфтесты различать «нашёл
      расхождение» и «упал с исключением» (`bugs/64`) · взять ось «односторонние инварианты» по
      контуру · переподнять находку про `s10` с верным адресом `:637` · замерить effective-FP.

> Просьбы владельцу живут ТОЛЬКО в `homeworks/`/`interviews/` (интервью №008 Q4): очередь
> показывает `node tools/review.mjs --queue`, нарушения ловит `node tools/questions-guard.mjs`.

---

## 🌍 Работа ВНЕ этого репозитория — ядро голоса владельца

`d:\work\krinik_voice`, приватный `MikalaiKryvusha/krinik-stylometry`, **версия ядра 1.1**
(2026-08-09, круг 5 — АП27–АП37 из волны обзора витрины). KAIF связан с ним ТОЛЬКО канон-именем
файла `AUTHOR_STYLOMETRY.md`; личное творчество владельца в открытую поставку не едет, а имена
его проектов снимаются анонимизацией прямо в генераторе слепка.

Портрет ПРИНЯТ владельцем (решение №63), и приёмка НЕ закрыла три названные слабости:
**(а)** позитивные правила голоса выведены из ОДНОГО корпуса свободной прозы — четыре корпуса
высшего и среднего веса не разобраны ни одним аналитиком; **(б)** документному регистру круг 5 дал
доказательную базу только с одной стороны — анти-портрет; чем документный текст владельца
ЯВЛЯЕТСЯ, портрет не знает; **(в)** корпус рабочей речи QA пуст — задание в
`corpus/qa_work_confluence_jira/_ЗАДАНИЕ.md`, исполнитель — отдельный агент рабочего проекта.

---

## Открытые баги

Открыто **двадцать** (проба 2026-08-28 13:06: `ls bugs/*.md | grep -v README | grep -vc
DONE`): пять ниже плюс **пятнадцать R2-семейств с ПОДТВЕРЖДЁННЫМИ триажом вхождениями** (фаза S, 2026-08-14:
CONFIRMED 76 · REFUTED 17 · STALE 11; вердикты — секции «Триаж 2.3» в каждом документе, сводная
таблица — `reports/KAIF_AUDIT/2026-08-14_r2_triage_SUMMARY.md`). Оставшиеся семейства — скоуп
2.5: T — 84–91, 95, 96, 100, 103; остаток U — 93, 94, 97 (`ideas/27`); `bugs/101`/`102` — DONE
ранее, `92`/`98`/`99` — DONE этой ревизией.

| Баг | Состояние | На ком |
|---|---|---|
| `bugs/77` | Витрина неверно изображает авторство: «Слов прозы написано руками 316 764» — неправда, руками владелец писал сообщения в чат, ответы в интервью и часть `ideas/20` | **владелец** — формулировки его, очередь `homeworks/05` |
| `bugs/02` | 🔧 фикс отгружен 2026-07-07 | ждёт полевой проверки в реальном длинном `/dayloop` |
| `bugs/45` | 🔧 механизирован стражем `revert-guard` после ВТОРОГО наблюдения отката рабочей копии | транзиент не объяснён; страж ловит последствие |
| `bugs/61` | 🟡 класс «немая команда свода» закрыт формой; рецидив-кандидат 2026-08-21 показал вторую шкуру — БЕЗЫМЯННЫЙ catch сюиты (починен: сюита называет упавший свод поимённо) | призрак; следующий рецидив назовёт себя сам |
| `bugs/63` | 🔬 research-only: бамп версии без артефактов уводит обновление на легаси-путь | лечится меткой pre-release; красный не воспроизвёлся |

⚠️ **Из пяти открытых вне R2 на агенте не висит ни один в работе**: `bugs/77` — владелец;
02/45/61/63 — ждут поля/рецидива. (`bugs/107` закрыт 2026-08-28 13:06 +03:00 — логотип 2.4
принят владельцем.)
R2-остаток целиком на эпиках **2.5** (`ideas/27`): T-семейства (84–91, 95, 96, 100, 103) и
остаток U (93, 94, 97); исходы 2.3 (92 целиком, 98 с остатком-классом в `ideas/27` §7, 99
целиком) закрыты DONE ревизией W5. История повторного закрытия `bugs/66` (R1 → R2 → `6bf6445`,
тринадцать канареек) — в летописи, запись сессии 48.
