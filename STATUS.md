# KAIF — текущий статус

> Читай перед каждой задачей. Обновляй при каждом значимом изменении состояния. Это основная передача
> эстафеты между сессиями, теряющими контекст — пиши так, чтобы свежая сессия стала продуктивной по
> одному этому файлу.
> 🧠 Главный принцип мышления — `PHILOSOPHY.md`. Рабочий канон — `AGENT_GUIDE.md` (учти фрактальную
> двухслойную структуру: полезная нагрузка `framework/`+`KAIF.md` vs. обвязка для dogfooding в корне).
>
> ⚠️ **STATUS — СВОДКА «сейчас», не летопись** (канон 2.1, эпик H). Каждая строка проходит два
> теста: «уберу — следующий агент ошибётся?» и «читается целиком за один присест» (мягкий ориентир
> ~200 строк). Закрытое ПЕРЕЕЗЖАЕТ дословно в `PROJECT_HISTORY.md` («стрижка бонсая» в
> `/end-chat`), а не копится здесь. Летопись прежних сессий/релизов — там же.

---

## Что сделано (короткий хвост — старшие записи в PROJECT_HISTORY.md)

### Сессия 2026-08-07 (третья, продолжение) — фаза L3 закрыта: CLI-безопасность и зелёная ложь 🟢

- **Фаза L3 эпика L исполнена целиком** по `plans/42` — `bugs/33/34/39 → DONE` (грань Q14 в
  bugs/34 — ⏸ явный дефер до ответа владельца). Машинерия: **голый/флаговый запуск = справка**
  (`help` — команда; старый install-дефолт похоронен — THE полевая потеря KCam Г1);
  единый спек COMMANDS ведёт диспетчер+справку+**вайтлист argv** (неизвестный флаг/аргумент =
  отказ, не молчаливое исполнение — `sync --dry-run`, `diff <файл>`, опечатки флагов);
  незавершённое задание БЕЗ чекпоинтов не регенерируется при same-version re-run (fail-closed);
  **SKIPPED/код 3** у ненастроенных стражей (canon-lint, provenance — check/report согласованы;
  пересмотр компромисса bug 30.2 по полевой цене); `"canonArtifacts": []` сеется в маркер;
  **`aiMarks`** — локализованные марки `[ИИ]` видимы провенансу (Unlim Г8); транзиенты
  исключены из сканов; счётчик алиасов считает ЗАПИСЬ (KLAS D3); **чекпоинты исполняют
  сканеры** (placeholders — ресинк+отказ; stale-claims — видимость; recheck — сперва sync,
  закрывает окно дрейфа зеркал KCam Г11); **`checkpoint judge --verdict-file`** (кириллица/
  BOM/многострочник; полный вердикт — в квитанцию `judgeVerdict`, решение №42); policy-записи
  2.2 предупреждают обновляющихся; KAIF_REFERENCE §12.1/12.3 дополнены.
- **Верификация:** свод `s09-l3-cli-safety` — 25 стражей красные ДО фиксов по предсказанию
  (до-фиксный голый запуск УНИЧТОЖИЛ задание в фикстуре — инцидент воспроизвёлся буквально);
  полигон **9/9**; регрессии, пойманные полигоном, закрыты в тот же заход (leak-токен
  «Krinik*» в цитате деплоящегося tool-модуля; порядок «ресинк зеркал ДО скана» у
  placeholders-гейта — класс bug 27). **Судья фазы — воркфлоу-панель 4 линз (~347k, всё
  исполнением): VERIFIED WITH CAVEATS** — красное доказательство перепроверено реверсом
  (ровно 25), полевая совместимость цела; находки (major: асимметрия защиты АДАПТАЦИОННОГО
  задания — «CHECKPOINT LOST» воспроизведён; значение `--channel`; + 4 minor) закрыты до
  коммита, стражи G15–G18 + s06-дострой (свод — `plans/42`).
  Оп-план L4 — `plans/43` (канон N+1). Инцидент сессии: шелл-правка plans/42 через PS5.1
  `Get-Content|-replace|Set-Content` перекодировала файл в моджибейк — восстановлен из git,
  правки переделаны файловыми инструментами (класс — AGENT_GUIDE «Гигиена», симптом 1).

### Сессия 2026-08-07 (третья) — фаза L2 закрыта: класс перевода и настоящие диффы задания 🟢

- **Фаза L2 эпика L исполнена целиком** по `plans/41` — `bugs/31/32/43 → DONE`. Машинерия
  (`framework/installer/KAIF-CORE.mjs`, +203 строки): вставка EN-модулей в переведённые файлы
  запрещена по построению (вставляется только по-настоящему новое против ШАБЛОНА прошлого
  релиза); порог сети `translatedWholesale` стал долей (0.15, N≤2 → 0; лицо KLAS «2 из 21»
  ловится, EXP-0019 сохранён); суждение о переводе — по ТЕЛУ (`bodyLocalized` — машинные
  алиасы во frontmatter больше не ослепляют); авто-простановка `i18n: translated` (KLAS D2);
  фильтр нулевой дельты («шаблон не менялся → пункта нет»); настоящие диффы «шаблон старой
  версии → шаблон новой» через синтетический baseline с ТЕКСТАМИ (`cmdUpdate` скачивает
  артефакт прошлого релиза; офлайн — честная деградация); frontmatter обновляется по модулю
  машинного алиас-хвоста (`stripAliasTail` — bugs/43). Кириллица вычищена из тел EN-шаблонов
  `pause`/`kaif-remove`; класс закрыт гардом сборки (check-framework 5d, красный на HEAD
  доказан); в `check` — детектор двухголовых SKILL.md (>1 H1).
- **Верификация:** новый свод `s08-l2-faces` — три полевых лица NDim/KLAS/KCam, 12 стражей
  красные ДО фиксов (красный прогон в шапке свода) → зелёные; полигон 8/8 сводов; регрессия
  s02/S6 (ориентация конфликтного диффа — канон 2.0) поймана полигоном и откачена в тот же
  заход. **Судья фазы: VERIFIED WITH CAVEATS** — 2 major (эшелонированная маскировка слоёв →
  изолирующие стражи F3d/F6/F7, каждый доказан красным на одиночном реверсе; двухголовый
  корневой док невидим → детектор >1 H1 расширен на все не-owner .md) + 3 minor — все закрыты
  правками до коммита (свод в `plans/41`). Уроки → EXP-0021 (машинные хвосты слепят
  сравнения), EXP-0022 (чужой свод стережёт семантику). Оп-план L3 — `plans/42` (канон N+1).

> Сессии 2026-08-07 «планирование 2.2 целиком» и «фаза L1 (триаж)» + сессия 2026-07-31
> (закрытие всех 9 эпиков 2.1) перенесены дословно в `PROJECT_HISTORY.md` (стрижка бонсая
> 2026-08-07, вторая волна).

---

## Где мы сейчас

**KAIF 2.1 «Strong KAIF» ОПУБЛИКОВАН** (2026-07-31,
https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.1; пост-релизный смок зелёный; план 22
закрыт целиком) — **полевая обкатка состоялась**: 4 обновления парка владельца до 2.1 (NDim,
Unliminium, KLAS, KrinikCam) с полевыми отчётами. **KAIF 2.2 «Yolden KAIF» СПЛАНИРОВАН ЦЕЛИКОМ**
(2026-08-07): `researches/15` (метасинтез) → `plans/26` (зонтик) → 6 эпиков K–P
(`plans/27–32`) + 7 задач (T1–T7; свои планы — `plans/33–34`) + дочерние операционные планы
ближайших фаз всех эпиков (`plans/35–40`); интервью 007 отвечено целиком (решения №32–43 в
MASTER_PLAN §7), все гейты сняты. **Исполнение ИДЁТ: фазы L1 (триаж), L2 (класс перевода +
диффы задания) и L3 (CLI-безопасность + зелёная ложь) закрыты 2026-08-07** — реестр в
`plans/36`, беклог эпика L — `bugs/35–38, 40–42, 44` (8 открытых; 31/32/33/34/39/43 — DONE),
следующий шаг — фаза L4 (`plans/43`). Актуальная сборка — 13 ключевых документов /
**34 навыка** / 54 блока FULL / 147 бандла / 643 модуля (цифры печатает сборка — не
переписывай руками); полигон `npm run test:core` — 9 сводов, зелёный.

---

## 🤖 Пул автономного беклога (остаточные хвосты)

- [ ] Первый полевой прогон `/guarded-loop` (дефер G4) + минимальный контур /owner-reviews для
      этого репозитория (дефер D5) — следующая автономная сессия.
- [ ] Адаптеры под остальные системы (Devin, OpenCode, Aider, Junie — сведения в `researches/01`).
- [ ] Библиотеки терминов по остальным сферам из `_template`.
- [ ] Идея fork-bundle (хвост bugs/16) и token-F1 ступень kaif-provenance — после полевой обкатки.
- [ ] Контр-стражи, честно отложенные в bugs/26/28.

## ❓ Ожидает владельца

- **Интервью 007 Q13–Q15** (дописаны триажем L1, 2026-08-07): Q13 — языковые пакеты
  (докомплектовать / честно пометить неполными); Q14 — легальность заголовочной формы
  `[AI]`-марки; Q15 — судейство обновления обязательным шагом. Блокируют только грани фаз
  L5/L3 и эпика M — **фаза L2 идёт без них**.
- Просьба из 2.1 остаётся: **посмотреть README 2.1 глазами на github.com** (обе темы, телефон
  — хвост плана 19).

---

## Где продолжить в следующей сессии

1. **Исполнение KAIF 2.2 по зонтику `plans/26`** (утверждённый порядок L → K → M → N → O → P,
   решение №37): L1 ✅, L2 ✅, L3 ✅ закрыты — следующий шаг **фаза L4 (`plans/43` — шум и
   честность аудита: stale-claims ≥50 % · аудит переведённых · warning-guard STATUS · линт
   бандла; bugs/35–38)** — В НОВОМ чате (решение владельца 2026-08-07: свежий контекст);
   параллельно доступна фаза K1 (`plans/35`). Дети остальных эпиков — `plans/37–40`.
2. Автономный пул прежний: полевой прогон `/guarded-loop` (дефер G4); минимальный контур
   согласований для этого репо (дефер D5) — теперь это фаза K5 эпика K.
3. Проверка после любой правки машинерии: `node tools/build-framework.mjs` + `npm run test:core`;
   реестр пар — команды в `AGENT_GUIDE.md` («Гигиена документов и текста»).

---

## Открытые баги

- **`bugs/35–38, 40–42, 44` — полевой беклог эпика L 2.2** (заведён триажем L1, 2026-08-07;
  31/32/43 закрыты фазой L2, 33/34/39 — фазой L3): открыты 35 stale-claims · 36 переведённые
  красны по построению · 37 обещания ≠ код · 38 рассинхроны бандла · 40 placeholders ·
  41 имя проекта · 42 метка маршрута · 44 языковые пакеты (грань Q13). Фиксы — фазы L4–L5
  по `plans/28` (L4 — `plans/43`). Дефер-грань Q14 — в `bugs/34_DONE`.
- `bugs/02_loops_self_stop_on_context_overflow.md` — 🔧 фикс отгружен (2026-07-07); ждёт полевой
  проверки в реальном длинном `/dayloop` (мегацикл 2026-07-28 прошёл без самоостановок — косвенная
  улика в плюс, но это был не /dayloop).

> Весь полевой беклог 2.0–2.1 (bugs/01–30, кроме 02) закрыт — хроника закрытий в летописи.
