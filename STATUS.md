# KAIF — текущий статус

> Читай перед каждой задачей. Обновляй при каждом значимом изменении состояния. Это основная передача
> эстафеты между сессиями, теряющими контекст — пиши так, чтобы свежая сессия стала продуктивной по
> одному этому файлу.
> 🧠 Главный принцип мышления — `PHILOSOPHY.md`. Рабочий канон — `AGENT_GUIDE.md` (учти фрактальную
> двухслойную структуру: полезная нагрузка `framework/`+`KAIF.md` vs. обвязка для dogfooding в корне).
>
> ⚠️ **STATUS — СВОДКА «сейчас», не летопись** (канон 2.1, эпик H). Каждая строка проходит два
> теста: «уберу — следующий агент ошибётся?» и «читается целиком за один присест» (мягкий ориентир
> ~200 строк). Закрытое ПЕРЕЕЗЖАЕТ дословно в `PROJECT_HISTORY.md` («стрижка бонсая» в
> `/end-chat`), а не копится здесь. Летопись прежних сессий/релизов — там же.

---

## Где мы сейчас

**KAIF 2.1 «Strong KAIF» ОПУБЛИКОВАН** (2026-07-31,
https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.1).

**KAIF 2.2 «Yolden KAIF» ИСПОЛНЕН ПОЛНОСТЬЮ, кроме Фазы R.** Спланирован целиком 2026-08-07
(`researches/15` → зонтик `plans/26`; интервью 007 — решения №32–46). Закрыты **все пять эпиков**
(L полевые фиксы · K интерактивный контур · M контур обратной связи · N канон-архитектура и
`REQUIREMENTS_FRAMEWORK` · O контекст и окружение) и **все девять задач** (T1, T2, T4–T10 и
T3 — 2026-08-08). Эпик P (переименование в `kaif-*`) — ⛔ NOT TO BE DONE в 2.2 (решение №47),
кандидат в 2.3. Подробности каждого закрытия — в `PROJECT_HISTORY.md`, там же все закрытые сессии.

✅ **Гейт Фазы R ЗЕЛЁНЫЙ с 2026-08-08 16:50 +03:00** — её скелет требует «все задачи T1–T9
закрыты», условие выполнено закрытием T3.

🎂 **ЧЕРНОВАЯ ВИТРИНА 2.2 ОПУБЛИКОВАНА В ДАТУ 08.08.2026** (слово владельца: «задеплоить README и
релиз ноутсы, чтобы они успели попасть в GitHub в красивую дату 08.08.2026 — день рождения
KAIF 2.2», и следом «сделаем несколько версий README и релиз ноутсов — черновые в 08.08.2026, а
затем уже неспеша их переделываем КАК ПОЛОЖЕНО»).
Страница — https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.2, создана 23:28 +03:00,
`version.json` поднят до **2.2 Yolden KAIF**, released 2026-08-08.
⚠️ **Кодовое имя версии пишется БЕЗ КАВЫЧЕК** — слово владельца 2026-08-09 00:20 +03:00: «кавычки
убрать, мы их никогда не ставили для названия». Правило в канон-таблице идентичности
`AGENT_GUIDE.md`, проверка перед сдачей витрины — греп там же. Историю не переписываем.
⚠️ **Страница помечена pre-release ОСОЗНАННО и снимать метку до доезда артефактов нельзя:** канал
`release` загрузчика тянет ассеты из ПОСЛЕДНЕГО релиза, поэтому релиз без артефактов в роли
«latest» сломал бы установку полевым проектам. Пока метка стоит, «latest» остаётся у v2.1 с
настоящими ассетами. Побочный эффект бампа уже наблюдён и заведён — `bugs/63`.

**Остаток релиза — Фаза R** (`plans/59`): R1 → починки R1 → R2 → починки R2 → закрытие issue #2
через `send-outbound` → `/release` с доездом артефактов и окончательным замером «Интересных фактов».

> **Критерий владельца на весь остаток 2.2 (дословно, решение №52):** «мы не торопимся. Мы
> делаем хорошо и полно, качественно, и тестируем, и полируем, чтобы 2.2 получилась
> полноценной и крепкой версией». Развилки скоупа решаются по ПОЛНОТЕ, не по скорости.

Актуальная сборка — 14 ключевых документов / 7 README /
**35 навыков** / 57 блоков FULL / **161 бандла** / **689 модулей** (цифры печатает сборка — не
переписывай руками; с плана 62 их стережёт `node tools/counters-guard.mjs`); полигон `npm run test:core` — **14 сводов, зелёный** (s14 — модуль
refresh-hooks); контур: `node tools/review.mjs --queue` (очередь страницей) · `node
tools/questions-guard.mjs`; линтер документов знаний: `node tools/doc-header-lint.mjs`
(шапка-мета + блок вектора + словарь требований, консультативный); страж счётчиков прозы:
`node tools/counters-guard.mjs` (число зеркал печатает сам прогон; ось навыков — ПОИМЁННО,
`--selftest`); **линтер голоса владельца: `node tools/voice-lint.mjs <файлы.md>`** (слой 7A
чек-листа портрета, вшит в `/release` шаг 2); **сторож защищённого цикла:
`node tools/guarded-watchdog.mjs --arm|--check|--status|--disarm`** (пульс по завершённой работе,
задача планировщика ОС; дефер G4 закрыт первым полевым прогоном 2026-08-09).

---

## 🎯 Где продолжить — порядок по ЧАТАМ (решение владельца 2026-08-08 17:00 +03:00)

> 🚩 **ПЕРВОЕ ДЕЙСТВИЕ СВЕЖЕЙ СЕССИИ — освежить канон.** Маркер `.kaif/refresh-marker.json`
> намеренно оставлен протухшим на закрытии чата (перечитывать девятку, чтобы тут же выбросить
> контекст, — трата; штамповать без чтения — фрод класса ложного `[TESTED]`). Хук напомнит сам;
> `/resume` закрывает это полным проходом канона.

Владелец разложил остаток на три чата (дословно): **«в этом чате - стриги бонсай, сокращай STATUS,
пиши в опыт и летопись. В следующем чате будем чинить пять дефектов - с этого продолжим по resume.
И только в третьем чате - будем делать R и фикры по итогу ревизии»**.

1. ✅ **ЧАТЫ 25–28 ЗАКРЫТЫ** — урожай аудита T3 отработан полностью, задача **T12 закрыта**
   (`AUTHOR_STYLOMETRY.md` в корне, ядро принято, версия 1.0). Подробности — в
   `PROJECT_HISTORY.md`; уроки — EXP-0051…EXP-0065.
   > **Осталось от них следующей сессии — исполнимые правила, не история:**
   > **(а)** карточка находки несёт «сегодняшнее измеренное состояние» — **СНИМИ ЗАМЕР ДО КОДА**;
   > приём трижды подряд опроверг числа самой карточки (EXP-0056…EXP-0058).
   > **(б)** мутационная проверка отвечает на ДВА вопроса: краснеет ли страж на дефекте И молчит
   > ли он на «не трогать» — второй ответ бесплатен и почти никем не задаётся (EXP-0059).
   > **(в)** документ, по которому поднята страница контура, **НЕ трогать до её закрытия** —
   > владелец в это время отвечает; мутация для доказательства делается на КОПИИ (`bugs/62`).
   > **(г)** слепой/парный тест, где ЧЕЛОВЕК судит авторство, меряет три канала разом — язык,
   > содержание и НОСИТЕЛЬ, — и побеждает самый лёгкий: грепни свои синтагмы по текстам владельца
   > и смотри материал ОТРЕНДЕРЕННЫМ, а не в markdown (EXP-0064).

2. ✅ **ЗАДАЧА T11 ЗАКРЫТА ПО ШАГАМ 1–6** (`plans/65`), 2026-08-08 23:35 → 2026-08-09 00:05 +03:00.
   Витрина 2.2 стоит: команда установки без `--mode`, анонимность понижена в `<sub>`-сноску §2.2
   обеих половин, версионное лого с авторской трактовкой символики, README актуализирован,
   релиз-ноты компактной дельтой, блок «Интересные факты» с Таблицей 6.
   **Что знать следующей сессии — четыре строки, остальное в `plans/65`:**
   - **Три протухших счётчика найдены в §8.2 ОБЕИХ половин** («34 шаблона навыков» при 35 · «шесть
     README директорий» при семи · «девять адаптеров» при десяти). Класс закрыт формой:
     `counters-guard` получил живые оси `adapters`/`langs`, 26 → **34 зеркала**; страж покраснел на
     ЖИВЫХ дефектах ДО правки текста. **Урок класса:** инвентарь зеркал, собранный грепом по ЦИФРЕ,
     пропускает вхождения в другой формулировке — греп по ПОНЯТИЮ («навык», «skill»).
   - **Голос проверяется машиной:** `node tools/voice-lint.mjs README.md <ноты>` — слой 7A портрета
     одной командой, вшит в `/release` шаг 2. Оба артефакта: ноль жёстких нарушений; ритм нот
     выведен в коридор РАЗБОРА (18,8 → 12,3 слова). ⚠️ Зелёный линтер НЕ означает «голос
     владельца»: похожесть — класс «вкус», вердикт за владельцем, вычитка ОСТАЁТСЯ должной.
   - **Числа «Интересных фактов» — цитата ОДНОГО прогона** `node tools/kaif-stats.mjs --since
     "2026-08-07T00:00:00+03:00"` с меткой момента замера в самом тексте. Окончательный замер —
     в `/release` после R2 (слово владельца). Дата старта работ — 00:00 07.08.2026, его слово; из
     тега `v2.1` она НЕ выводится (EXP-0066).
   - **Три границы в тексте снимать нельзя:** деньги — не счёт владельца (подписка против
     API-прайса) · человеко-часы — оценка с видимыми ставками · энергия — ЧУЖАЯ оценка по другим
     моделям, и порядок там **киловатт**-часы, а не гигаватты.
   ⏳ **Остаток T11 — шаг 7:** вычитка владельцем (класс «вкус») и переделка «КАК ПОЛОЖЕНО» после R2.

3. ✅ **ЗАДАЧА T12 ЗАКРЫТА ЦЕЛИКОМ 2026-08-08 22:45 +03:00** (`plans/66`).
   **`AUTHOR_STYLOMETRY.md` лежит в корне** — публичный слепок ПРАВИЛ портрета из приватного
   `krinik-stylometry` (решения №60–№63). Дыра dogfooding закрыта: исток канонизировал имя файла,
   вёз скелет поставкой — и сам файла не имел. **Ядро принято владельцем, версия 1.0.**
   **Что знать следующей сессии — четыре строки, остальное в `plans/66`:**
   - **Слепок ГЕНЕРИРУЕТСЯ, руками не правится:** `node tools/stylometry-snapshot.mjs`
     (источник — `d:\work\krinik_voice`; без него пересборка невозможна, и это норма).
     Прогон печатает: **140 правил** · 28 публичных доказательств · 536 адресов в приватное ядро.
   - **Личные цитаты наружу НЕ едут** — безопасность по умолчанию: публикуется только явно
     разрешённое, остальное схлопнуто в «…», адреса обезличены до номера (`044_*.md:17`).
     Пары ДО/ПОСЛЕ (§6) не приехали вовсе — калибровки руки на примерах у агента НЕТ.
   - **Портрет ПРИНЯТ (решение №63), но слабости названы, а не спрятаны:** §6 не добран, правила
     из одного корпуса из семи, корпус рабочей речи ПУСТ — а витрина пишется именно в этом
     регистре. `rewrite` разрешён внутри провенанс-пометок; вердикт «мой голос» — всегда владельца.
   - **Сверка версий КОНСТАТИРУЕТ, не требует** (решение №62):
     `node tools/stylometry-snapshot.mjs --version-check` — строка расхождения, код возврата 0
     всегда; строка реестра пар в `AGENT_GUIDE.md`, команда вшита в `/release`.
   ⚠️ **Главный урок, который дороже самой задачи** (EXP-0064): слепой тест дал 6/8 и выглядел
   почти пройденным, а разбор шестью агентами признал **замер недействительным** — мерились
   содержание и вёрстка, не голос. Тест авторства меряет три канала разом (язык · содержание ·
   НОСИТЕЛЬ), и побеждает самый лёгкий. Второго круга не будет по слову владельца.

4. ⏳ **ФАЗА R — ТЕПЕРЬ ДВА КРУГА, R1 и R2** (`plans/59`; решение владельца №64, 2026-08-08
   23:10 +03:00). Порядок остатка 2.2: **T11 → R1 → починки R1 → R2 → починки R2 → релиз.**
   - **R1** — полный адверсарный суд всего 2.2. **Нормальный исход — НАХОДКИ:** «R1 ничего не
     нашёл» читается как слабость ревью, а не сила версии. Каждая выжившая находка — баг-док.
   - **Починки R1** — отдельная фаза: класс, а не экземпляр; страж на каждый фикс.
   - **R2** — полное ревью 2.2 **ЗАНОВО** плюс **переисполнение заявлений фиксов R1**. Оси брать
     ДРУГИЕ, чем в R1 (принцип 5, парадокс пестицида — иначе это ксерокопия первого круга).
   - **Починки R2** — финишная полировка, затем `/release` и закрытие issue #2 через
     `send-outbound`. Третий круг по умолчанию НЕ заводится — назначает только владелец.
   > 💡 **Слово владельца про инструмент** (2026-08-07 23:05 +03:00): Фаза R — одно из двух мест,
   > где многоагентная оркестрация окупается; владелец просил СКАЗАТЬ ему явно в этот момент, а
   > включает её он сам. В двадцать пятой сессии рой отработал на T3 и себя оправдал.

5. ✅ **ОЧЕРЕДЬ ВЛАДЕЛЬЦА ПУСТА** — `node tools/questions-guard.mjs` печатает «Ждут ответа: 0».
   Интервью №014, №015 и №016 закрыты; последнее (№016 — витрина против пометок провенанса) владелец
   ответил ЗА МИНУТУ через контур, и это решение №65.
   ⏳ Владельцу остаётся не вопрос, а ДЕЙСТВИЕ: **вычитать витрину 2.2 глазами** — по решению №65
   именно вычитка, а не пометки `[AI]`, является очередью на приёмку README и релиз-нот.
   **Новые развилки поднимать КОНТУРОМ** — `node
   tools/review.mjs --queue`, отслеживаемой фоновой задачей, — а не репликой в чате: слово
   владельца «по вопросам ко мне — интерактивный контур», и его же реплика этой сессии — «нет у
   нас инструмента, как можно было бы мне вопросы задать?».
   ⚠️ **Варианты ответа пиши СТРОКАМИ ТАБЛИЦЫ** `| **A** | что означает | цена и риск |` **или
   списком** `- **A)** …` — третьей формы парсер не знает, и вопрос отрендерится без радиокнопок
   (`bugs/62`); с этой сессии ось стережёт `questions-guard`.
   ⚠️ **Трейлер коммитов (решение №54):** зашитого имени модели нет — коммить
   `node tools/commit.mjs --msg-file <путь> --as "<имя своей модели>"`; без `--as` инструмент
   предупредит и уйдёт БЕЗ трейлера (это норма, а не поломка).

6. **Проверка после любой правки машинерии:** `node tools/build-framework.mjs` + `npm run
   test:core` (**14 сводов**); реестр пар — команды в `AGENT_GUIDE.md` (счётчики —
   `node tools/counters-guard.mjs`); страж места вопросов `node tools/questions-guard.mjs` — в
   `/end-chat` и `/release`. Числа в коммиты — только из вывода инструментов (EXP-0025).
   **Тронул контур** (`tools/review.mjs`, `tools/lib/review-core.mjs`) → обязателен
   `node tools/verify-contour.mjs` (**119 проверок**, живой браузер) и пересмотр эталона глазами
   при новых живых документах: `node tools/verify-contour.mjs --write-etalon`.
   ⚠️ **Логи верификации — в ФАЙЛ, не через `| tail`** (EXP-0052): хвост показывает итог, а нужна
   причина; имя упавшего свода отрезается ровно там.

---

## 📦 Задел на 2.3 (решения владельца 2026-08-08, НЕ в 2.2)

> 📄 **Скоуп 2.3 живёт документом — `ideas/24_kaif_2.3_scope.md`** (заведён 2026-08-08 17:53 +03:00
> по слову владельца). Пункты копятся ТАМ в день рождения; здесь — только сводка. Первый пункт,
> внесённый владельцем: **написать Fable поручение скептически проревьюировать `/code-revision` и
> переписать его под себя** — навык писал Opus, а работать по нему Fable.

- **Заморозка восьми языковых пакетов** (решения №56/№57): ведём только `ru` и `en`; восемь
  остальных замораживаются с ЯВНЫМ объявлением — на какой версии, в каком состоянии, почему
  («тяжело в поддержке, пока не окупается»), оживление по запросу сообщества. Страж протухания
  требует ресинк только для ведомых языков. Постановка — `interviews/interview_013` Q1.
- **Форма 2.3** (решение №58): обычный релиз со скоупом из новой идеи владельца; сокращение
  поверхности (пакеты · ревизия 35 навыков «каждый ли зарабатывает своё место» · удвоение
  payload/обвязка) — пунктом ВНУТРИ него. **Проработка идей начинается сейчас**, разведка — через
  многоагентную оркестрацию; включает её владелец по просьбе агента.
- **Эпик P** (переименование навыков в `kaif-*`, решения №35/№47) — кандидат сюда же. ⚠️ Он первым
  нагрузит пару, дырявую по `bugs/55`: до эпика P эту пару надо взять под стража по ИМЕНАМ.

---

## 🤖 Пул автономного беклога (остаточные хвосты)

- [ ] Постоянное покрытие в полигоне: tamper-тест sha256-гейта ЛОАДЕРА + twin-run byte-exact
      ассерт в s01 (принятый долг закрытия эпика L — находка №3 линзы 3; сейчас доказано
      только сессионными стендами).
- [ ] Первый полевой прогон `/guarded-loop` (дефер G4).
- [ ] Хвосты контура по потребности (интервью №008, Q3 + `researches/17` §4): G4-страж
      содержания вопроса · P6-медиавставки (iframe srcdoc макетов + «открыть отдельным
      экраном» — рецепт NDim готов) · карточка исходящего артефакта на странице (к Фазе R) ·
      «пометка для агента» цитатой `>` построчно · list-команда · детектор «поле-подсказка ≠
      статус».
- [ ] Адаптеры под остальные системы (Devin, OpenCode, Aider, Junie — сведения в `researches/01`).
- [ ] Библиотеки терминов по остальным сферам из `_template`.
- [ ] Идея fork-bundle (хвост bugs/16) и token-F1 ступень kaif-provenance — после полевой обкатки.
- [ ] Контр-стражи, честно отложенные в bugs/26/28.
- [ ] Долг фазы O5: машинерия `deployAgentSystems` не знает про Antigravity — адаптер
      документационный, автодеплой навыков туда не заведён (путь навыков не верифицирован;
      вписывать угаданный запрещено).
- [ ] **Следующий прогон `/code-revision`** (что менять — в `reports/KAIF_AUDIT/…_SUMMARY.md`):
      прогнать `--selftest` всех стражей и мутировать зеркала, которые селфтест НИКОГДА не красит ·
      взять ось «односторонние инварианты» по контуру (зона не смотрена ни разу) · переподнять
      находку про `s10` с верным адресом `:637` · замерить effective-FP первого отчёта.

> Секция «❓ Ожидает владельца» удалена по слову владельца (интервью №008, Q4, вариант B):
> просьбы владельцу живут ТОЛЬКО в `homeworks/`/`interviews/` (текущая — `homeworks/04`,
> README 2.1 глазами); очередь показывает `node tools/review.mjs --queue`, нарушения ловит
> `node tools/questions-guard.mjs`.

---

## 🌍 Работа ВНЕ этого репозитория — ядро голоса владельца

`d:\work\krinik_voice`, приватный `MikalaiKryvusha/krinik-stylometry`. KAIF связан с ним ТОЛЬКО
канон-именем файла `AUTHOR_STYLOMETRY.md`; личное творчество владельца в открытую поставку не
едет. Портрет — ЧЕРНОВЫЕ НАБЛЮДЕНИЯ, 6 порогов из 7. Открытые пункты, если владелец вернётся:
**(а)** разобрать четыре НЕпрочитанных корпуса (правила выведены пока из одного из семи);
**(б)** его собственная работа — перепеть 2–3 машинных абзаца и вычитать три «ПОСЛЕ» регистра
СЦЕНА (агентом не закрывается: машинного повествования в корпусах нет); **(в)** корпус рабочей
речи QA — задание в `corpus/qa_work_confluence_jira/_ЗАДАНИЕ.md`, исполнитель — отдельный агент
рабочего проекта.

---

## Открытые баги

**Урожай пробного аудита T3 закрыт ПОЛНОСТЬЮ** — все шесть (`bugs/59`, `55`, `60`, `56`, `57`,
`58`) 2026-08-08; подробности закрытий — в самих документах и в `PROJECT_HISTORY.md`.

**Новое (сессия 27):**

- `bugs/62` — ✅ **DONE.** Живой вопрос отрендерился без радиокнопок — владельцу нечего нажать, и не
  краснел никто. Контур был исправен, сломан был ДОКУМЕНТ: варианты записаны третьей формой, а
  парсер знает две. Закрыто осью **«вопрос ОТВЕЧАЕМ»** в `questions-guard` (≥2 разобранных
  варианта у живого вопроса, либо объявленный `<!-- questions-guard:open причина -->`); селфтест
  9 → **13 мутаций**, красный доказан на той самой редакции.
- `bugs/61` — 🟡 **PARTIAL: класс закрыт, призрак остался.** Ассерт S9 покраснел один раз и не
  воспроизвёлся за **120 прогонов** (изолят последовательно и в четыре процесса, полные прогоны
  свода волнами). Чинить нечего — но воспроизводится ДРУГОЕ: красный не назвал причину, потому что
  ассерт судил побочный эффект команды, а саму команду не судил никто. Замер: **30 немых команд из
  229** в 7 сводах. Закрыто ФОРМОЙ: помощник `must(run, …)` (`tools/lib/sandbox-run.mjs`) для
  установочных шагов + страж `node tools/sandbox-mute-guard.mjs` (`--selftest`) ПРЕПОЛЁТОМ в
  `npm run test:core` при **нулевом долге**. Зелёный вывод 14 сводов побайтно прежний.
  **Остаток:** сам транзиент не объяснён; при рецидиве красный назовёт себя сам — три гипотезы и
  их приметы выписаны в документе. Урок — **EXP-0060**.

**Старые открытые:**

- `bugs/02_loops_self_stop_on_context_overflow.md` — 🔧 фикс отгружен (2026-07-07); ждёт полевой
  проверки в реальном длинном `/dayloop`.
- `bugs/45_working_copy_reverted_under_live_session.md` — 🔬 research-only: рабочая копия
  откатилась посреди сессии (2026-08-07); класс пойман предкоммитным диффом; причина — гипотеза,
  механизация при рецидиве.

> Весь беклог 2.0–2.2 закрыт ранее (`bugs/01–54`, кроме 02 и 45) — хроника закрытий в летописи.
