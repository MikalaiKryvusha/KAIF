# KAIF — текущий статус

> Читай перед каждой задачей. Обновляй при каждом значимом изменении состояния. Это основная передача
> эстафеты между сессиями, теряющими контекст — пиши так, чтобы свежая сессия стала продуктивной по
> одному этому файлу.
> 🧠 Главный принцип мышления — `PHILOSOPHY.md`. Рабочий канон — `AGENT_GUIDE.md` (учти фрактальную
> двухслойную структуру: полезная нагрузка `framework/`+`KAIF.md` vs. обвязка для dogfooding в корне).
>
> ⚠️ **STATUS — СВОДКА «сейчас», не летопись** (канон 2.1, эпик H). Каждая строка проходит два
> теста: «уберу — следующий агент ошибётся?» и «читается целиком за один присест» (мягкий ориентир
> ~200 строк). Закрытое ПЕРЕЕЗЖАЕТ дословно в `PROJECT_HISTORY.md` («стрижка бонсая» в
> `/end-chat-soft`), а не копится здесь. Летопись прежних сессий/релизов — там же.

---

## Где мы сейчас

**Версия KAIF 2.7 ОТКРЫТА 2026-09-12 00:25 +03:00** (интервью №031 Q1 = B, №109; разведка по тикетам поля `researches/30` — семь
тикетов #54–#60 четырёх проектов за три дня после релиза 2.6; мета-план `plans/100`: **AW → QL → TR → VC → HO → CB → CR → RL**,
≈ 7,5–9,5 чатов, знаменатель метрики — 14 критериев (13-й — DR, слово владельца ≈ 2026-09-12 09:00 +03:00; 14-й — VC по тикету #61, слово владельца ≈ 2026-09-12 10:48 +03:00, №111); имя — нейтральное `KAIF 2.7` до слова владельца (№28/№85); реестр 2.8 —
`ideas/30`). **Эпик AW ✅ 2026-09-12 01:16 +03:00** (`plans/101`, сессия 58: подпись авторства решения в каноне обоих слоёв,
провенанс черновиков владельцу, страж `kaif-attribution-lint` + свод `s24`, охота судьи; судья VERIFIED WITH CAVEATS — 7
каверз починены). **Эпик DR ✅ 2026-09-12 09:22 +03:00** (`plans/103`, сессия 59, №110 — см. ниже; судья VERIFIED WITH CAVEATS — 3 каверзы починены до коммита; закрыто 3 из 13). **Эпик QL ✅ 2026-09-12 10:02 +03:00** (`plans/102`, сессия 59 — четыре коммита: дверь `--check` (#56) · четвёртый факт «внесено» (#54) · вид для чтения и три исхода (#54/#58) · плавающая кнопка (#60); судья — `plans/102` § «Решения» 9; **закрыто 7 из 13**). **Эпик TR ✅ ≈ 2026-09-12 11:45 +03:00** (`plans/104`, сессия 60 — один чат: раздел канона · шаблон отчёта прогона · `kaif-testrun-lint` · ось `check` «`/resume` покрывает ядро» · охота судьи · свод `s25` · первый живой отчёт истока; судья VERIFIED WITH CAVEATS — 7 каверз починены до коммита; **закрыто 9 из 14** — знаменатель вырос до 14 тикетом #61 → эпик VC, №111). **Эпик VC ✅ ≈ 2026-09-12 15:22 +03:00** (`plans/105`,
сессия 61 — контракт письма тремя шагами по слову владельца №112: пишет ПО портрету с ним в рабочем кеше (`kaif-voice-lint load` +
свидетель) → независимая проверка (`check` + §7Б чистым экземпляром) → правит → только тогда написан → владельцу; судья VERIFIED
WITH CAVEATS — 14 каверз починены до коммита; **закрыто 10 из 14**; формальные ✅ в планах поставлены сессией 62 своим
переисполнением, п. 0 ниже). **Следующий эпик — HO** (`plans/106`, план готов). Предыдущий релиз — **2.6 Mindful KAIF, 2026-09-06 12:46 +03:00** (летопись — записи сессий 57 и 58). Вектор доставки
`DELIVERY:` СНЯТ словом владельца ≈ 2026-09-12 09:00 +03:00 (№110, эпик DR `plans/103`): команда ядра, реестр систем, свод `s20`, строка в
церемониях и охота судьи убраны из обоих слоёв. Правила видения 2.6 в силе — №97 (механика
отгружается только полной) · №98 (вопрос владельцу — сценарием) · №104 (проверка на реальном мире) · №106 (голос беседы — язык
заказчика); 2.7 добавила №109 (скоуп) и правило «Авторство решения» (`AGENT_GUIDE.md`: слово владельца цитируется, слово агента
подписывается).

⚠️ **Молитвы канонизированы (2.4, PY):** перед работой произносится символ веры + молитва
(`AGENT_GUIDE.md`, блоки `KAIF:CREED`/`KAIF:PRAYER`; шаг в `/resume`). **Церемонии закрытия
раздвоены (SC):** `/end-chat-soft` (заявка заранее → доделать до среза → полные церемонии) и
`/end-chat-force` (срочно, с явной строкой долга); старого `/end-chat` больше нет. **Названное
время окончания = СТАРТ мягкого закрытия**, не дедлайн (`AGENT_GUIDE` → «Работа до названного
времени»).

**Кодовое имя версии пишется БЕЗ КАВЫЧЕК** (канон-таблица идентичности `AGENT_GUIDE.md`); правила
витрины, оплаченные волнами 2.2, — `plans/69` (три правила) и `AGENT_GUIDE.md` → «Витрина»
(десятка); вектор целей — порядок владельца в `researches/18` §2 (№67).

Актуальная сборка — 14 ключевых документов / 7 README / **37 навыков** / 59 блоков FULL /
**180 бандла** / **790 модулей** (цифры печатает сборка — не переписывай руками; стережёт
`node tools/counters-guard.mjs`); полигон `npm run test:core` — **25 сводов, зелёный**, четыре
преполёта.

---

## 🎯 Где продолжить

> 🚩 **ПЕРВОЕ ДЕЙСТВИЕ СВЕЖЕЙ СЕССИИ — освежить канон.** Маркер `.kaif/refresh-marker.json`
> намеренно оставлен протухшим на закрытии чата (перечитывать девятку, чтобы тут же выбросить
> контекст, — трата; штамповать без чтения — фрод класса ложного `[TESTED]`). Хук напомнит сам;
> `/resume` закрывает это полным проходом канона.

### 1. 👉 СЕЙЧАС — версия 2.7 ОТКРЫТА 2026-09-12 00:25 +03:00 (№109); AW ✅ 01:16, DR ✅ 09:22, QL ✅ 10:02, TR ✅ ≈ 11:45, VC ✅ ≈ 2026-09-12 15:22 +03:00 (сессия 61, `/end-chat-force`) — следующий эпик HO, план готов `plans/106`

0. **✅ ДОЛГ ФОРС-ЗАКРЫТИЯ ПОГАШЕН ≈ 2026-09-12 15:42 +03:00 (сессия 62).** Чат 61 был закрыт `/end-chat-force` (слово
   владельца «Закрывай этот чат, в новый перейдем. Этот раздулся» · «стоп резкий, только напиши кратко в статус, с чего
   начать новому чату») и оставил формальности долгом. Закрыто: `plans/100` — критерий 14 ✅ и строка 9 таблицы ✅, метрика
   «закрыто 10 из 14», ближайший операционный план → `plans/106`; `plans/105` — «Статус» ✅, VC7/VC8 ✅, метрика «1 из 1»;
   `MASTER_PLAN` §5 и §8 — веха VC ✅, следующий шаг HO; отчёт прогона — **строка 18 своим наблюдением** (переисполнение
   на закоммиченном дереве `93ec9ff`: сборка 180/790 без дрейфа · селфтест 48 · `s26` 47 · полигон 25 сводов 15:38 · все
   стражи и пары реестра), `kaif-testrun-lint check` → «OK — 2 report(s)»; стрижка бонсая — запись сессии 61 в
   `PROJECT_HISTORY.md`; README.pdf перегенерирован. **Следующее — эпик HO по `plans/106`** (нулёвка HO0: инвентарь
   `baton` 9 файлов / 19 строк, `FORK:` носителя карты переименований); дальше CB (`plans/95`) → **CR — ПОЛНОЕ
   переписание навыка `/code-revision` самим Fable под себя и агентов его класса ума (слово владельца ≈ 14:26, №113;
   `plans/63` перепланировать нулёвкой CR0)** → RL.
1. **✅ СДЕЛАНО (сессия 61) — эпик VC «Текст владельца — по его портрету, независимая проверка, потом владелец»**
   (`plans/100` строка 9, критерий 14; тикет #61 — взят словом владельца ≈ 10:48 +03:00, №111; план — `plans/105`):
   VC0–VC6 ✅ 14:20 (tool-модуль `kaif-voice-lint` — селфтест 48 кейсов EN/RU на финале · скелет портрета §8 таблицей
   `паттерн · класс · подсказка · исключение` · `/owner-voice` контракт письма + режимы `load`/`check` обоих слоёв ·
   четыре точки решения `AGENT_GUIDE` обоих слоёв · пункт установки · охота судьи · свод `s26` «all 47 checks green»,
   полигон 25 сводов, красный на ядре 2.6 — 7 из 47). ⚠️ **Постановка ПЕРЕДЕЛАНА словом владельца ≈ 14:25 +03:00 (№112):** агент **пишет ПО
   портрету** (правила, словник, анти-портрет, пары открыты до первого слова) → **независимая проверка по тому же
   портрету** (команда + проход §7Б чистым экземпляром) → **правит → только тогда написан → владельцу на согласование**;
   моя первая редакция «проверка командой до показа» была половиной — переделка шагом VC9 ✅ (канон обоих слоёв, навык,
   судья «owner text past the portrait», скелет §7, записи 2.7, планы); второе слово ≈ 14:35, дословно: «Это блять важно -
   писать ПО СТИЛОМЕТРИИ, С НЕЙ В СВОЕМ РАБОЧЕМ КЕШЕ» → VC10 ✅: команда `kaif-voice-lint load` печатает портрет в рабочий
   контекст и оставляет свидетеля `.kaif/voice-marker.json` (в ignore, история загрузок), `check` отказывает тексту без
   свидетеля, написанному раньше первой загрузки или позже часа после последней (правило часа), и свидетелю чужого портрета.
   VC7 ✅ (отчёт прогона · черновик #61 в `plans/105` · финальная сборка 180/790 и полигон 25) · VC8 ✅ — судья VERIFIED WITH
   CAVEATS, каверзы закрыты; формальные ✅ в планах поставлены сессией 62 (п. 0). План HO `plans/106` написан (N+1); дальше HO → CB → CR → RL.
   **Канон TR действует на истоке с сессии 60:** каждый
   исполненный прогон — отчёт в `testcases/reports/<дата>_<работа>.md` (первый — `2026-09-12_polygon-2.7-TR.md`),
   `node framework/tools/kaif-testrun-lint.mjs check` стоит в `/end-chat-soft`, заявление `[TESTED]` о прогоне называет
   отчёт. **Контур истока после QL:** проверка документа — `node tools/review.mjs <док> --check` (без страницы и зова),
   внёс решение → `--mark-implemented <док> <Q> --where <коммит>`; `--no-open` ЗОВЁТ. Имя версии — без имени до релиза
   (Q2 №031 без ответа — статус-кво №28/№85; один вопрос на витрине в RL). Очередь владельца пуста. Черновики ответов в
   #54/#56/#58/#60 — `plans/102`, в #55 — `plans/101`, в #59 — `plans/104`; отправка после релиза (№84/№92/№93).
   `bugs/112` — DONE интервью №031.
2. **Приём поля 2.6 продолжается** (отчёты обновлений в `reports/KAIF_UPDATES/` — пока ни одного; тикеты origin — новый сигнал строкой в
   реестр открытой версии с адресом; ответ по тикету — ПОСЛЕ релиза по нему (интервью №020, Q2 = A → №84), без вычитки и с закрытием тикета той же квитанцией (стоячее слово №92/№93/№94); разведка обязана пробить соседей на диске —
   EXP-0101). Открытых issues origin — 8 (#54–#61), все разложены в `researches/30` §3 (#61 — §2б, эпик VC).
   **Один баг 2.6 ждёт наблюдения на реальном пути:** `bugs/113` (контур — DONE после живого «Готово» без замечаний
   владельцем на странице вычитки/макета; фикс — в ассетах v2.6 с 13:34); `bugs/112` — DONE 2026-09-12 00:25 +03:00 (интервью №031). `bugs/110` (символ веры STRIVE) — выпущен в 2.6, #49
   отвечен и закрыт; DONE после отчёта поля о следующем обновлении ru-развёртывания.
3. **`bugs/109` 🔧** (флейк `update` в полигоне ×3 за день без причины): форензика
   `run-fail-N.log` стоит во всех сводах; в сессиях 52–55 полигон прошёл зелёным двенадцать раз без
   флейка (в 55-й — 21/21 ×6) — **следующее вхождение разбирать с этого файла** (баг S2 с полным выводом).
4. ⚠️ README.pdf перегенерирован 2026-09-12 15:44 +03:00 (сессия 62, погашение долга форс-закрытия — в README менялись
   только счётчики 180/790 и число сводов 25; предыдущий — 2026-09-12 11:49); первый экран глазами агента не проверен
   (на машине нет PDF-рендерера — Ghostscript); прежний PDF открывался владельцу, жалоб не было.
5. ⚠️ **Для глаза владельца — решения без него.** Сессии 53–60 — в летописи (`PROJECT_HISTORY.md`, записи сессий 57, 60 и 61).
   **Сессия 61 (VC):** `plans/105` § «Решения» 1–11 — форма носителя паттернов: таблица в §8 самого портрета, а не
   отдельный JSON (`FORK:` с разведкой Vale и полевого портрета) · синтаксис ячейки = синтаксис поля ripgrep, несобираемая
   строка предупреждает, а не валит прогон · §8 ищется по ключевому слову обоих языков ИЛИ по номеру — портрет на третьем
   языке не теряется · незаполненная копия скелета не может быть зелёной (`SKIPPED=3` на одних плейсхолдерах) · коды
   выхода (1 — попадания, 3 — не судилось, 2 — usage) · моя первая редакция постановки эпика была ПОЛОВИНОЙ, владелец
   поправил её №112 · «с ней в рабочем кеше» механизировано свидетелем загрузки, и граница названа вслух: свидетель не
   доказывает, что агент прочитал напечатанное · «написано позже часа после последней загрузки» — находка, не
   предупреждение (иначе отказывало бы один раз на дерево) · план HO и урок EXP-0128 написаны параллельно работе судьи,
   его судом не покрыты — покрываются нулёвкой HO0 · 14 каверз судьи закрыты до коммита без второго суда, по прецеденту TR.
   **Сессия 62:** `plans/100` строка 9 и критерий 14 ✅ поставлены по СОБСТВЕННОМУ переисполнению на закоммиченном дереве
   (прогон 18 отчёта), а не по памяти закрывшейся сессии.
### 2. ⏳ ДОЛГИ НА ВЛАДЕЛЬЦЕ (агент их не закрывает)

- **`bugs/77` (авторство на витрине)** — формулировки принимает владелец. Готовь варианты и
  показывай СТРАНИЦЕЙ контура (`node tools/review.mjs <док>`), не решай сам.
- **Вычитка витрины 2.2 глазами** — `homeworks/05`. Две волны правок он уже прошёл вживую, но
  домашка как артефакт приёмки остаётся открытой.
- Старший долг того же класса — `homeworks/04` (README 2.1 глазами).
- **Судьба приватных имён в РАБОЧИХ документах и в истории git** — поставка, витрина и слепок
  портрета чисты (`private-names-guard` зелёный); переписывание истории требует force-push, то
  есть строки `AUTH:`.
- **`$62,50` живёт только в релиз-нотах**, в README этого числа нет — владельцу задан вопрос,
  дублировать ли его в README; ответа нет.
- **Вычитка витрин 2.5 И 2.6 глазами** (README EN/RU, ноты, страницы релизов) — не проводилась: критерий 8
  `plans/82` закрыт публикацией по вашему слову как приёмкой «как есть» (судья закрытия, каверза b);
  того же класса, что `homeworks/04`/`05`; витрина 2.6 опубликована по вашему «да» той же приёмкой «как есть» (логотип вы отсматривали страницей 2026-09-06, замечаний не записано — страница закрылась дефектом `bugs/113`).

### 3. 🧰 Инструменты, которыми работать, а не открывать заново

| Команда | Что стережёт |
|---|---|
| `node tools/build-framework.mjs` + `npm run test:core` | обязательны после ЛЮБОЙ правки машинерии (25 сводов, 4 преполёта) |
| `node tools/counters-guard.mjs` | 50 зеркал живых чисел; ось навыков и документов — ПОИМЁННО |
| `node tools/questions-guard.mjs` | место вопросов · вариант ПОТЕРЯН · вопрос ОТПРАВЛЯЕТ в документ · ратчет долга · **G4 ждёт и ни разу не показан · G5 вопрос владельцу прозой вне `interviews/` · G6 живой вопрос без сценария · G7 вердикт без разведки · G8 словарь агента в вопросе владельцу** (2.6; G5/G6 — вперёд от даты ≥ 2026-09-05, G8 — ≥ 2026-09-06; коды эпиков — из таблиц живых мета-планов; исключение — `questions-guard:vocabulary-ok причина`) |
| `node tools/review.mjs --queue --list` · `--mark-shown <док> --transport чат` | очередь владельца БЕЗ браузера: возраст ожидания и показа, ни разу не показанные первыми, **код 2 = гейт** (I41/I42); факт показа для вопроса, заданного в чате (I40); карта — `interviews/decisions/shown.json` |
| `node tools/review.mjs …` = обёртка над `framework/tools/contour/review.mjs` (`--selftest` · `<док> --check` — проверка формы БЕЗ страницы и зова, код 3/0 (2.7 QL1) · `--mark-implemented <док> <Q> --where <адрес>` — четвёртый факт «внесено» (2.7 QL2) · `<док> --no-open` — поднимает и ЗОВЁТ, это не проверка · `--proofread` · `--mockup`) | **отгружаемый генератор контура** (2.6 IC3/IC5; в поле — `.kaif/tools/contour/`): предполёт формы вариантов §2 контракта, самопроверка «радиогрупп = вопросов», три лица; параметры истока — `.kaif/kaif.json` → `contour`; свод `s22`; исток ест свою поставку с IC5 |
| `node tools/doc-header-lint.mjs` | шапка-мета · метки времени и их ПРАВДИВОСТЬ · закрытие против тела · **PENDING без СУДЬБЫ** |
| `node tools/showcase-lint.mjs` · `estimates-guard` · `private-names-guard` | витрина: 13 классов и четыре оси — симметрия половин (включая подписи таблиц и блоки цитат) · врап тела релиза · числа нот ↔ README · **якоря README в ссылках нот** (2.5, сессия 52); источник у оценки; утечка имён (зоны — поставка, README, все ноты, слепок, реестры суда) · **ось №90 (2.6 HY): псевдоним донора `project A…F` без пометки `source-kept` в поставке — красный; реестры суда и слепок — вне оси** |
| `node tools/verify-contour.mjs --etalon-only` | дешёвая приёмка контура — в `/end-chat-soft`; полный прогон в браузере — в `/release` |
| `node tools/commit.mjs --msg-file <путь> --as "<имя модели>"` | коммит; **`--only` берёт ОДИН путь НА ФЛАГ**, посторонний аргумент отвергается, новый файл в дереве останавливает подметающий прогон; **гейт 1c** (W2-1): коммит с поверхностью поставки гонит `private-names-guard` |
| `node tools/refresh-stamp.mjs --trigger <…> --docs <…>` | маркер освежения: момент берёт САМ инструмент |
| `node tools/build-logo-title.mjs` (`--check`) | подпись версии на фирменном логотипе: старое лого + перенабранная надпись в стиле, снятом пробами (шрифт, медь, фаска, посадка). **Выпускаешь версию — прогони**, иначе логотип уедет с прошлым именем (так вышло с 2.3). `--check` перенабирает подпись 2.2 и сверяет с оригиналом; красный доказан подменой гарнитуры |
| `node tools/build-story-card.mjs` | Таблица 6 витрины → PNG 9:16 для соцсетей: строки читаются из живого README, кегль ищется замером, типографика лечит переносы |
| `node tools/build-team-logo.mjs` (`--check`, `--title`) | логотип 2.4/2.5: арт v2 владельца (побайтно нетронут) + подпись стилем 2.3 через `renderTitle`; геометрия — плита 2.3 × масштаб арта + ручки вкуса `TUNE_*` (меняются только словом владельца); `--check` — шесть осей вкл. «медь, не серебро» |
| `node tools/release-body-guard.mjs --tag vX.Y` | опубликованное тело релиза ↔ файл нот построчно + NBSP; шаг 6.9 `/release` |
| `node tools/kaif-stats.mjs` · `voice-lint` · `guarded-watchdog` | статистика релиза · слой 7A портрета · сторож защищённого цикла |
| `node framework/tools/kaif-guard-lint.mjs check <пути>` (`selftest`) | блок объявления стража `@guard` (THREAT · PROVED-AGAINST · GAP · ON-REAL-PATH) / `@forensic` (EXPLAINS · DURABLE-AT; close/exit/trip-only отвергнуты) / `@fork` (OPTIONS · COST · RECON · DECIDED) — гейт 5, вторая половина (2.5, CN3; issue #35); только явные маркеры, `SKIPPED=3` без них; в поставке — `.kaif/tools/`; свод s15 |
| `node framework/tools/kaif-scenario-lint.mjs check <пути>` (`selftest`) | форма сценария критерия (Situation · Action · Result · Check, правила 1–6 + порядок; 2.5, SF; issue #39); `SKIPPED=3` без сценария; свод s19 |
| `node framework/tools/kaif-testrun-lint.mjs check` (`selftest`; `check <дом>`) | **форма отчёта исполненного прогона** (2.7 TR, issue #59): каталог по дате `testcases/reports/<ГГГГ-ММ-ДД>_<работа>.md` · семь полей непустые · «Прогоны» с командой в код-спане и моментом · «Найдено» списком или явным «ноль» · вердикт назван; `SKIPPED=3` без каталога — ненаписанный отчёт линтер не видит, на заявление без отчёта охотится судья; шаблон — `framework/templates/_testrun-report-template.md` (в поле `.kaif/_testrun-report-template.md`); свод `s25` (и ось `check` ядра «`/resume` покрывает ядро» — предупреждение поимённо, доказано на развёрнутой копии) |
| `node tools/experience-lint.mjs` (`--selftest`) | поле механизации у новых записей опыта (issue #14, эпик X): mechanized/none-cheap/subject-lesson; ловушка по форме требует первых двух; долг — baseline, только убывает; в `/end-chat-soft` |
| `node framework/tools/kaif-attribution-lint.mjs check` (`selftest` · `--write-baseline`) | **авторство решения** (2.7 AW, issue #55): ссылка на волю владельца («решение/по слову/владелец велел», EN-формы) без дословной цитаты, адреса интервью или номера решения в окне ±2 строк — находка; подпись `[AI]`/`[ИИ]` на строке или секция «Решения без владельца» — решение агента; исключение — `<!-- attribution-ok: адрес -->`; **долг истока 45** (базовая линия `.kaif/attribution-lint.baseline.json`, проба 2026-09-12 01:14: 41 на приёмке + 4 приняты явно `--adopt-new` после ужесточения улик по суду AW), новых обязано быть 0 — в `/end-chat-soft`; повторный `--write-baseline` при новой находке — отказ (линия только убывает); в поставке — `.kaif/tools/`, свод `s24` |

**Тронул контур** (`framework/tools/contour/*`, обёртку `tools/review.mjs` или шим `tools/lib/review-core.mjs`) → обязателен полный
`node tools/verify-contour.mjs` в живом браузере; новый живой документ роняет эталон НАМЕРЕННО —
лечится `--write-etalon` с пересмотром чисел ГЛАЗАМИ.

### 4. ⚠️ Правила, нарушение которых уже стоило дорого

- **Время снимается ПРОБОЙ `date` в тот же ход, что и запись** — не «сессия помнит» (EXP-0078).
- **Вопрос владельцу САМОДОСТАТОЧЕН:** предмет решения приводится ВНУТРИ вопроса, а не адресуется
  ссылкой (EXP-0081, `bugs/83`). Плохо заданный вопрос не раздражает — он фальсифицирует решение.
- **Варианты ответа — СТРОКАМИ ТАБЛИЦЫ** `| **A** | что означает | цена и риск |` **или списком**
  `- **A)** …`; пометка в скобках у буквы законна. Третьей формы парсер не знает.
- **Новые развилки поднимать КОНТУРОМ** (`node tools/review.mjs`, отслеживаемой фоновой задачей),
  а не репликой в чате: слово владельца «по вопросам ко мне — интерактивный контур».
- **Мутация для доказательства делается на КОПИИ**; `git checkout` для незакоммиченного файла —
  не откат, а УДАЛЕНИЕ (EXP-0077).
- **Числа в коммиты и документы — только из вывода инструментов** (EXP-0025); **сводное число
  суда/реестра — только скриптом по строкам** (EXP-0107).
- **Логи верификации — в ФАЙЛ, не через `| tail`** (EXP-0052): хвост показывает итог, а нужна
  причина.
- **Ссылка на раздел чужого документа снимается с ЖИВОГО документа**, не наследуется из прошлого
  письма (EXP-0108; ось `NOTES-ANCHORS`).
- **Перед любым `--queue` — ревизия очереди глазами на беспредметность** (EXP-0106, `bugs/108`).
- **Вопрос владельцу — сценарием того, что он УВИДИТ** (Ситуация · Действие · Результат ·
  Проверка), на языке заказчика; «вектор/флаг/команда» без строки «Результат. Вы видите …» —
  плохо заданный вопрос (№98, EXP-0110, `bugs/111`). **Механика с шагом «спросите владельца
  проекта» — неполна и не отгружается** (№97).

---

## 🤖 Пул автономного беклога (остаточные хвосты)

- [ ] Хвосты контура по потребности (интервью №008 Q3 + `researches/17` §4): G4-страж содержания
      вопроса · P6-медиавставки · карточка исходящего артефакта на странице · «пометка для агента»
      цитатой `>` построчно · list-команда · детектор «поле-подсказка ≠ статус». Свежие кандидаты
      того же контура (2026-08-21, из bugs/105/106): buildDoc очереди по протухшему kind ·
      atHuman-метка квитанции печатает UTC-момент как локальный (сдвиг +3 ч — проверить пробой).
- [ ] Адаптеры под остальные системы (Devin, OpenCode, Aider, Junie — сведения в `researches/01`);
      по Muse Code вендорская дока ПОЯВИЛАСЬ 2026-08-21 (`researches/19` §1.3 — впрыска контекста
      в контракте нет, образец hooks не пишется).
- [ ] Библиотеки терминов по остальным сферам из `_template`.
- [ ] Идея fork-bundle (хвост bugs/16) и token-F1 ступень kaif-provenance — после полевой обкатки.
- [ ] Контр-стражи, честно отложенные в bugs/26/28.
- [ ] Долг фазы O5: машинерия `deployAgentSystems` не знает про Antigravity — автодеплой навыков
      туда не заведён (путь навыков не верифицирован; вписывать угаданный запрещено).
- [ ] **Следующий прогон `/code-revision`** (что менять — в `reports/KAIF_AUDIT/…_SUMMARY.md`):
      мутировать зеркала, которые селфтест НИКОГДА не красит · научить селфтесты различать «нашёл
      расхождение» и «упал с исключением» (`bugs/64`) · взять ось «односторонние инварианты» по
      контуру · переподнять находку про `s10` с верным адресом `:637` · замерить effective-FP.

> Просьбы владельцу живут ТОЛЬКО в `homeworks/`/`interviews/` (интервью №008 Q4): очередь
> показывает `node tools/review.mjs --queue`, нарушения ловит `node tools/questions-guard.mjs`.

---

## 🌍 Работа ВНЕ этого репозитория — ядро голоса владельца

`d:\work\krinik_voice`, приватный `MikalaiKryvusha/krinik-stylometry`, **версия ядра 1.1**
(2026-08-09, круг 5 — АП27–АП37 из волны обзора витрины). KAIF связан с ним ТОЛЬКО канон-именем
файла `AUTHOR_STYLOMETRY.md`; личное творчество владельца в открытую поставку не едет, а имена
его проектов снимаются анонимизацией прямо в генераторе слепка.

Портрет ПРИНЯТ владельцем (решение №63), и приёмка НЕ закрыла три названные слабости:
**(а)** позитивные правила голоса выведены из ОДНОГО корпуса свободной прозы — четыре корпуса
высшего и среднего веса не разобраны ни одним аналитиком; **(б)** документному регистру круг 5 дал
доказательную базу только с одной стороны — анти-портрет; чем документный текст владельца
ЯВЛЯЕТСЯ, портрет не знает; **(в)** корпус рабочей речи QA пуст — задание в
`corpus/qa_work_confluence_jira/_ЗАДАНИЕ.md`, исполнитель — отдельный агент рабочего проекта.

---

## Открытые баги

Открыто **двадцать четыре** (проба 2026-09-12 00:32 +03:00: `ls bugs/*.md | grep -v README | grep -vc
DONE` → 24; `bugs/112` закрыт сессией 58 — интервью №031 прошло ось G8 и отвечено выбором): девять ниже плюс
**пятнадцать R2-семейств с ПОДТВЕРЖДЁННЫМИ триажом вхождениями** (фаза S, 2026-08-14:
CONFIRMED 76 · REFUTED 17 · STALE 11; вердикты — секции «Триаж 2.3» в каждом документе, сводная
таблица — `reports/KAIF_AUDIT/2026-08-14_r2_triage_SUMMARY.md`). Оставшиеся семейства — скоуп
2.6 (`ideas/28`): T — 84–91, 95, 96, 100, 103; остаток U — 93, 94, 97; `bugs/101`/`102` — DONE
ранее, `92`/`98`/`99` — DONE ревизией W5.

| Баг | Состояние | На ком |
|---|---|---|
| `bugs/77` | Витрина неверно изображает авторство: «Слов прозы написано руками 316 764» — неправда, руками владелец писал сообщения в чат, ответы в интервью и часть `ideas/20` | **владелец** — формулировки его, очередь `homeworks/05` |
| `bugs/02` | 🔧 фикс отгружен 2026-07-07 | ждёт полевой проверки в реальном длинном `/dayloop` |
| `bugs/45` | 🔧 механизирован стражем `revert-guard` после ВТОРОГО наблюдения отката рабочей копии | транзиент не объяснён; страж ловит последствие |
| `bugs/61` | 🟡 класс «немая команда свода» закрыт формой; рецидив-кандидат 2026-08-21 показал вторую шкуру — БЕЗЫМЯННЫЙ catch сюиты (починен: сюита называет упавший свод поимённо) | призрак; следующий рецидив назовёт себя сам |
| `bugs/63` | 🔬 research-only: бамп версии без артефактов уводит обновление на легаси-путь | лечится меткой pre-release; красный не воспроизвёлся |
| `bugs/109` | 🔧 флейк `update` в полигоне ×3 за день (s07 ×2, s10 ×1) без форензики → `run-fail-N.log` во всех сводах | следующее вхождение — с этого файла; в сессии 52 полигон 19/19 ×3 без флейка |
| `bugs/110` | 🔧 символ веры в EN-шаблоне говорил «KEEP TRYING»; фикс «STRIVE» + FORBIDDEN-страж отгружены 2026-09-05, ВЫПУЩЕНЫ в 2.6 (полевой тикет #49 — ответ и закрытие тем же днём) | ждёт проверки следующим обновлением ru-развёртывания (#49 → DONE после отчёта поля) |
| `bugs/111` | 🔧 S1: вопрос владельцу принесён техническим пояснением, не сценарием (интервью №023 Q2/Q3, «не понимаю проблему… как заказчик») | механизация ОТГРУЖЕНА в 2.6 (эпик OQ: `/interview` 3a обоих слоёв · ось G6 стража · охота судьи); DONE — после полевого наблюдения: вопрос через обновлённый навык, ответ выбором |
| `bugs/113` | 🔧 S1: лица «вычитка»/«макет» отгружаемого контура не давали нажать «Готово» без замечаний (владелец — на странице логотипа 2.6, 12:47: «даже у тебя самого интерактивный контур работает неправильно»); фикс в `main` 12:51: пустые поля = «замечаний нет», код 0; селфтест 47/47, s22 ✅ | DONE — после живого «Готово» без замечаний владельцем; ассеты v2.6 перевыложены с фиксом 13:34 словом владельца — выпуск 2.6 закончен |

⚠️ **Из открытых вне R2 на агенте не висит ни один в работе**: `bugs/77` — владелец;
02/45/61/63/109 — ждут поля/рецидива. R2-остаток целиком на 2.6 (`ideas/28`): T-семейства
(84–91, 95, 96, 100, 103) и остаток U (93, 94, 97). История повторного закрытия `bugs/66`
(R1 → R2 → `6bf6445`, тринадцать канареек) — в летописи, запись сессии 48.
