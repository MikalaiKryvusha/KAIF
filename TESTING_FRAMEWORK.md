# TESTING_FRAMEWORK — как агент тестирует то, что создаёт

Сырому сгенерированному контенту — коду, документу, анализу, чему угодно — **доверять нельзя**. Он может
*выглядеть* логичным и рабочим и при этом быть сломанным или не отвечать реальным требованиям владельца
(идее, плану, видению). Ранний дефект, тихо доехавший до продакшена, — самый дорогой: такие баги
уничтожают проекты изнутри. Тестирование — особая, самостоятельная, неотъемлемая часть ЛЮБОЙ работы, а не
формальность после неё. Этот документ — канон тестирования для агента; он применим к **любому артефакту в
любой сфере** — функции, датасету, юридической формулировке, конструкции моста, мысли (что значит
«проверить» в конкретной сфере, определяет библиотека сферы: её секции *Verification by observation* и
*Minimum evidence set*).

## Семь принципов тестирования (канон)

1. **Тестирование показывает наличие дефектов, а не доказывает их отсутствие.** Зелёный прогон не
   доказывает, что багов нет — баги ЕСТЬ ВСЕГДА; тестирование снижает риск, но не до нуля.
2. **Исчерпывающее тестирование невозможно.** Все комбинации входов и условий не проверить —
   приоритизируй по риску и ценности, а не изображай полноту.
3. **Раннее тестирование экономит бюджет.** Проверяй уже на этапе требований/плана; чем позже найден
   дефект, тем он дороже (небоскрёб по waterfall на непроверенном фундаменте).
4. **Скопление дефектов.** Большинство ошибок живёт в нескольких узких модулях — где нашёл одну, ищи
   рядом (twin check fable-method — этот принцип, механизированный).
5. **Парадокс пестицида.** Одинаковые тесты перестают находить новые баги — меняй тесты, углы, данные.
6. **Зависимость от контекста.** Методы тестирования подбираются под проект и сферу — платёжную систему,
   научную статью и лендинг не тестируют одинаково.
7. **Заблуждение об отсутствии ошибок.** Безошибочный продукт, не решающий задачу пользователя,
   бесполезен — всегда тестируй против требований ВЛАДЕЛЬЦА (`GOAL.md`, идея, план), а не только против
   внутренней согласованности кода.

## Тестовые активности — цепочка, из-за которой «проверено» что-то значит

Контракт доверия ниже говорит, насколько ВЕРИТЬ результату; эта секция — как делается сама
тестовая РАБОТА. Причина её существования оплачена полем (issue #21 истока): без обязанности
спроектировать набор наблюдений агент прогнал ОДИН happy path, доложил «фича работает», а
владелец за минуту накидал пять непокрытых кейсов. Тестирование фичи — цепочка активностей, а не
одно наблюдение; проходи её по порядку, у каждого шага — условие выхода:

1. **Разбери тестовый базис.** Назови источник истины ожидаемого поведения — требование, слово
   владельца, спека, канон-карта (их форму задаёт `REQUIREMENTS_FRAMEWORK.md`). *Выход:* у
   каждого проверяемого утверждения процитировано, откуда ожидание; отсутствующее или
   непроверяемое ожидание возвращается дефектом требований (принцип 3 — здесь дешевле всего).
   Изучение требований ради тестового базиса — ЭТО тестовая активность, а не чужая глава.
2. **Спроектируй набор наблюдений именованными техниками.** Выводи кейсы стандартными
   инструментами: эквивалентное разбиение · граничные значения · таблицы решений · переходы
   состояний · pairwise · проход по сценарию использования · предугадывание ошибок. *Выход:*
   письменный список кейсов с НАЗВАННЫМИ измерениями — какие разбиения, границы и состояния
   покрыты, а какие осознанно нет (принцип 2: приоритизируй по риску и СКАЖИ, что выпущено).
3. **Запиши документацию до исполнения.** Тестовая документация живёт в файлах, а не в голове
   сессии: план (что и зачем) · сюита/чек-лист (упорядоченный набор) · кейсы (шаги · ожидаемое ·
   статус). Скопируй шаблон поставки в дом тестовой документации проекта (дефолт `testcases/`,
   создаётся при первом использовании; сфера или проект вправе назвать другой):
   `cp .kaif/_testcases-template.md testcases/TC_<фича>_<слаг>.md` — класс артефактов без дома и
   формы не пишется.
4. **Исполняй с учётом.** Каждый кейс кончается статусом — `pass` · `fail` · `blocked` ·
   `skipped` — с названным наблюдением (что запускалось, что увидено). *Выход:* нет кейса без
   статуса; покрытие — это список кейсов, никогда не впечатление.
5. **Прогони контрольный кейс до вердикта «работает».** Выключи управляющий флаг / убери
   управляющий параметр и наблюдай, как фича НЕ работает: проверка фичи, которая не может упасть,
   ничего не доказывает (гейт 5 ниже, применённый на уровне фичи).
6. **Оформляй дефекты в заданной форме.** Шаги воспроизведения · ожидаемое против фактического ·
   severity/priority · окружение · улики — и передавай в `BUG_FIXING_FRAMEWORK.md` (по документу
   на дефект; навык `/report-bug`).

## Маркеры тест-статуса — контракт доверия

Всё нетривиальное, что агент генерирует, несёт явный греп-дружелюбный тест-статус в своём
комментарии/примечании. Строки маркеров — канонические английские (как тег `DONE`), независимо от языка
проекта:

- **`[NOT-TESTED]`** — свежесгенерированное, сырое. **Доверять нельзя.** LLM «думала», что это верно, —
  это не улика.
- **`[TESTED: <дата> · <как проверено / что наблюдалось>]`** — проверено наблюдением, улика названа
  (прогон, рендер, пересчёт, сверка с источником).

**Правила:**

1. **Создал сырое** (нетривиальный блок/метод/модуль/секцию) → впиши `[NOT-TESTED]` в его комментарий
   при рождении. Комментирование и так обязательно (`AGENT_GUIDE.md`) — маркер входит в исходный
   комментарий.
2. **Встретил `[NOT-TESTED]`** (своё или унаследованное) → не строй на нём вслепую: запланируй проверку,
   проверь **наблюдением** (fable-method, шаг 5: запустилось, отрисовалось, посчиталось — не «вывел из
   чтения»), затем переведи маркер в `[TESTED: …]` с уликой.
3. **Встретил `[TESTED: …]`** → доверять можно, перепроверять не нужно — но держи толику сомнения
   (принцип 1: баги есть всегда). Улики противоречат маркеру → маркер неверен: расследуй.
4. **Тест нашёл дефект** → заведи его (`/report-bug`, метод — `BUG_FIXING_FRAMEWORK.md`), почини,
   перепроверь и только затем ставь `[TESTED]`.
5. **Ложный `[TESTED]`** — маркер без реально проведённой проверки — это фрод; `/fable-judge` охотится
   на него как на любое ложное заявление о завершении. Никогда не переключай маркер без наблюдения.
6. **Носитель по типу артефакта:** код — комментарий блока/метода; документ — примечание секции; иная
   сфера — ближайший комментируемый носитель по конвенции сферы.
7. **Маркер на ФИЧЕ требует спроектированного набора.** `[TESTED]` на фиче легален только рядом с
   письменным набором кейсов и его покрытыми измерениями (цепочка активностей выше); одно
   наблюдение переключает маркер одного КЕЙСА, никогда не фичи. «Один раз сработало на happy
   path» — факт уровня кейса: маркер, удовлетворённый одним наблюдением, свидетельствует, что
   что-то наблюдалось, — и молча заявляет, что фича протестирована; это два разных утверждения
   (issue #21 истока).

Маркеры — персистентная память о верификации: шаг 5 fable-method проверяет *в моменте*; маркер сохраняет
этот факт **между сессиями** — для будущих агентов и потомков. Кто ещё будет знать, что фундамент прошёл
испытание нагрузкой?

## Работа производит и средство своей проверки

«Сырому доверия нет» связывает ПРОИЗВОДИТЕЛЯ, а не только проверяющего: сделать что-то — значит
сделать и то, чем это проверяется: test suite, чек-лист, тест-кейсы, фикстуру, стража. Они
планируются ВМЕСТЕ с работой и ложатся В ТОТ ЖЕ шаг, а не «потом»: проверка, отложенная на
следующий шаг, — проверка, которой не будет, а проверка, живущая только в скретчпаде сессии,
умирает вместе с сессией (EXP-0016). Это принцип 3 (раннее тестирование), применённый к
ПРОИЗВОДСТВУ, а не к инспекции, — и ровно поэтому ниже существует секция харнесса: харнесс делает
проверку повторяемой после того, как она появилась.

Контракт в форме шагов — проходи его на каждой нетривиальной работе:

1. **Назови проверку при планировании работы.** Тот же шаг задачи, что строит X, называет, чем X
   проверяется, — свод · чек-лист · тест-кейсы · фикстура · страж.
2. **Положи обе в один шаг.** Проверка входит в репозиторий вместе с работой — никогда «потом» и
   никогда только в скретчпаде сессии.
3. **Докажи проверку на сломанной версии**, прежде чем верить её зелёному (гейт 5 ниже;
   `BUG_FIXING_FRAMEWORK.md` → Стражи). Закрытый дефект дополнительно рождается со стражем на весь
   его КЛАСС — это правило живёт в `BUG_FIXING_FRAMEWORK.md` («фикс без стража — фикс в кредит»)
   и здесь не повторяется.

Гейт тривиальности действует: тривиальная правка, проверенная своей одной очевидной проверкой, не
требует церемоний сверх обычного комментария и маркера. Нелегально другое — закончить нетривиальную
работу, не оставив ничего, чем её можно перепроверить.

## Зелёные тесты ≠ работает — гейты наблюдения

Зелёный прогон — одно наблюдение, а не вердикт (принцип 1): целые классы дефектов невидимы для всех
тестов и очевидны за минуту взгляда. Перед «готово» на всём, что запускается, рендерится или
отгружается, пройди применимые гейты:

1. **Живой смоук с глазами на логе.** Запусти настоящий процесс (не только тесты) и прочитай его первый
   рабочий цикл в логе — старт, ключевую операцию, отсутствие тихого потока ошибок.
2. **Самодостаточность отгружаемого артефакта.** Образ/бандл/пакет обязан стартовать в изоляции (свежий
   контейнер/каталог) — сборка, работающая только внутри твоего рабочего дерева, не отгружена.
3. **Инварианты домена, до/после.** До работы выпиши числа, которые не должны измениться (счётчики,
   суммы, размеры); после — сверь. Сверка двух чисел — та проверка, которую любая сессия выполняет
   безошибочно, а сигнал у неё один из высочайших.
4. **Считаемые прокси качества.** Где качество визуально или субъективно — найди, что можно посчитать
   (анимации на экран, проверка прозрачности панелей, рост бандла): ноль на счётчике — стоп-дефект.
   Прокси не заменяет глаз владельца — он ловит нули *до* владельца.
5. **Проверка, которая ни разу не падала, ничего не доказывает.** Каждый новый страж сначала проверяется
   на сломанной версии (см. `BUG_FIXING_FRAMEWORK.md` → Стражи); эталоны рефакторингов — побайтные:
   пустой diff — доказательство, «числа вроде те же» — нет.
6. **После деплоя гейт — сам продакшен, войдённый КАК ПОЛЬЗОВАТЕЛЬ.** Войди той дверью, которую
   продукт предлагает, пройди настоящие экраны, прочитай консоль — только после этого «задеплоено»
   становится фактом. Смоук, гуляющий только по публичным страницам, доказывает, что жив лендинг,
   а не продукт: если у продукта есть аутентифицированное состояние, неаутентифицированный смоук —
   НЕ улика о продукте. (Оплачено полем: три деплоя за ночь отдавали приложение, которое не
   стартовало вовсе, при зелёных локальных приборах — issue #18 истока.)
7. **Целостность артефакта до отгрузки.** «Собралось» и «это одна сборка» — разные утверждения:
   отгружаемый бандл несёт ровно ОДНУ идентичность сборки, и это проверяется механически до
   загрузки. Нечищенный между сборками выходной каталог отгружает смесь двух сборок — каждый файл
   по отдельности валиден, сломан НАБОР, — а смеси падают так, как не видит ни один тест.

Два правила размещения, оплаченные тем же инцидентом: гейты 6–7 живут В ПУТИ ДЕПЛОЯ, а не в
прозе — одна дверь деплоя сама гоняет их и падает на любом красном шаге (где у агентской системы
есть хуки — сырой команде деплоя ставится запрет; правило, живущее только в документе, отгружающая
сессия пропускает под давлением). И пост-деплойный смоук обязан УМЕТЬ УПАСТЬ на мёртвом продукте:
докажи, что было что мерить, прежде чем красить зелёным, — смоук, зеленеющий тем сильнее, чем
пустее продукт, хуже отсутствующего.

## Класс «вкус» — когда наблюдателем обязан быть человек

Субъективно-перцептивный критерий приёмки (прилагательное восприятия: красиво, естественно,
приятно, «нравится») тоже проверяется наблюдением — но НАБЛЮДАТЕЛЬ здесь по необходимости человек,
а не агент. Роль агента — ПОДГОТОВИТЬ наблюдение: смакетировать на материале проекта и отдать
артефакт восприятия (`AGENT_GUIDE.md` → «Класс "вкус"»; документ домашки с его двумя постоянными
полями). Собственное «по-моему, хорошо» агента проверкой не является и маркер не переключает;
переключает записанный вердикт владельца.

## Как это стыкуется с остальным KAIF

- **`REQUIREMENTS_FRAMEWORK.md`** — формирует ТРЕБУЕМОЕ до того, как что-то сделано; этот фреймворк
  проверяет СДЕЛАННОЕ против него. Принцип 3 (раннее тестирование) исполняется на этапе требований
  тем каноном; баги — то, что рождается на их стыке (`BUG_FIXING_FRAMEWORK.md`). Граница не
  закрывает дверь анализу требований: вывод тестового базиса ИЗ требований — шаг 1 цепочки
  активностей здесь.
- **fable-method** — шаг 5 (проверка наблюдением) — это КАК выполняется одна проверка; этот документ —
  ЧТО обязано нести статус и как распространяется доверие. Гейт тривиальности действует: тривиальная
  правка, проверенная своей одной очевидной проверкой, не требует церемоний сверх обычного комментария.
- **`/fable-judge`** — трактует маркеры как заявления: невоспроизводимый `[TESTED]` = REFUTED.
- **`BUG_FIXING_FRAMEWORK.md`** — куда уходят находки тестирования (по документу на дефект; 3 попытки →
  исследование).
- **Сферы** (`.kaif/spheres/`) — определяют улики сферы, смысл «проверено наблюдением» и таблицу фродов;
  принцип 6 живёт там.
- **Харнесс** — вкладывайся в инструментовку, делающую проверку наблюдаемой и детерминированной
  (`AGENT_GUIDE.md` → тестовый стенд); прикидка на глаз — не тестирование.

*Основа: семь принципов и цепочка активностей (базис → техники проектирования → документация →
исполнение → оформление дефектов) — канон ISTQB (istqb.org; рус.: testbase.ru), дистиллированный
для ИИ-агента и всех сфер. Секция активностей, правило «фича/кейс» и гейты 6–7 оплачены полем:
issues #21 и #18 истока.*
