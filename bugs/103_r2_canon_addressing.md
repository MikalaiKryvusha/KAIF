# Bug 103 — носитель принятого решения владельца объявляет его «предложением агента» раньше, чем себя опровергает

**Status:** 🔬 ПРЕДПОЛОЖИТЕЛЬНЫЙ — не верифицирован, причина не установлена · скоуп **2.3**
**Version/build:** build 279 · **When/context:** круги ревью R1/R2 фазы R, 2026-08-09
**Fix accepted when (observable):** НЕ ОПРЕДЕЛЁН — критерий приёмки задаёт исполнитель после
установления причины (решение №70: разбор и лечение за Fable, не за автором документа)

Семейство `канон-адресация` — одно вхождение. Оно о месте, куда `MASTER_PLAN.md` адресует читателя
за ПРИНЯТЫМ порядком вектора целей: сам порядок в таблице стоит по решению владельца, но абзац над
таблицей всё ещё называет ранжирование предложением агента, и читается он первым. Причина не
устанавливалась.

---

## 1. `researches/18` §2: вводный абзац называет порядок «предложением агента», опровержение — пятнадцатью строками ниже

**Адрес:** `researches/18_kaif_feedback_loop_in_control_terms.md:112`
Те же адреса: `researches/18_kaif_feedback_loop_in_control_terms.md:127`, `MASTER_PLAN.md:167`

**Заявлено.** `MASTER_PLAN.md:167` (реестр решений, строка решения №67) отправляет за принятым
порядком именно сюда и заявляет статус этого порядка дословно:

> **ПРОСТОТА ПОДНЯТА С ДЕВЯТОГО МЕСТА НА ТРЕТЬЕ**, остальные цели сдвинуты вниз. Слово владельца
> дословно: «пункт 9 поднять до 3-го, остальные передвинуть ниже. принимаем». Рекомендация агента
> (вариант A, «порядок как есть») ОТВЕРГНУТА. […] Принятый порядок живёт в `researches/18` §2;
> вектор модифицируем и впредь — решением владельца.

**Наблюдалось.** В `researches/18` §2 таблица приоритетов перестроена согласно решению (строка 119
— приоритет 3 «Простота: церемония не перевешивает работу»; строка 125 — приоритет 9
«Самодостаточность поставки»), а текст вокруг неё говорит о статусе порядка две разные вещи, и
неверная стоит первой:

- строка 112–113 (НАД таблицей, вводный абзац §2): «Ранжирование — предложение агента (см. секцию
  решений).»
- строка 127–131 (ПОД таблицей): «⚠️ **Порядок ПРИНЯТ ВЛАДЕЛЬЦЕМ с одной правкой** (2026-08-09
  13:49 +03:00, интервью №017 Q2 = вариант B, решение №67). […] Прежний порядок (простота
  последней) был предложением агента и отвергнут.»

**Улика.**

```
$ grep -n "предложение агента" researches/18_kaif_feedback_loop_in_control_terms.md
112:`MASTER_PLAN.md` §2, каноны `AGENT_GUIDE.md`. Ранжирование — предложение агента (см. секцию

$ grep -n "Принятый порядок живёт" MASTER_PLAN.md
167:| 67 | Порядок вектора целей KAIF (2026-08-09; принято и зафиксировано 13:49

$ awk 'NR>=110 && NR<=113' researches/18_kaif_feedback_loop_in_control_terms.md
Построен по правилу ДОТУ: первым — то, от чего отказываемся ПОСЛЕДНИМ. Каждая цель несёт
проверяемый инвариант и существующий наблюдатель (гейт). Источники: `GOAL.md`, принципы
`MASTER_PLAN.md` §2, каноны `AGENT_GUIDE.md`. Ранжирование — предложение агента (см. секцию
решений).

$ awk 'NR>=127 && NR<=131' researches/18_kaif_feedback_loop_in_control_terms.md
⚠️ **Порядок ПРИНЯТ ВЛАДЕЛЬЦЕМ с одной правкой** (2026-08-09 13:49 +03:00, интервью №017 Q2 =
вариант B, решение №67). Его слово дословно: «пункт 9 поднять до 3-го, остальные передвинуть ниже.
принимаем». То есть **простота поднялась с девятого места на третье** — выше даже правила «решения
владельца у владельца»: церемония, съедающая работу, вредит проекту раньше, чем неверно
адресованное решение. Прежний порядок (простота последней) был предложением агента и отвергнут.
```

**Кого касается.** Свежую сессию, пришедшую по адресу из `MASTER_PLAN.md:167` за принятым
порядком: первым она читает «Ранжирование — предложение агента» и может принять вектор за
незакреплённое наследие предшественника, а значит — переставить его сама, отменив решение
владельца №67. Отсылка «(см. секцию решений)» ведёт при этом в секцию, где решение №67 уже
зафиксировано.

**Замечено.** Круги ревью R1/R2 фазы R, 2026-08-09; сводка находок кругов ключом
`stale-claim-above-owner-decision`. Круг разделением в сводке не зафиксирован; решение №67
датировано 13:49 +03:00 того же дня, то есть раньше этого момента вхождение существовать не могло.

**Состояние на 2026-08-09.** Все три адреса на месте, наблюдаемое воспроизводится дословно (см.
улику) после шести блокерных починок дня (коммиты 300104b, 6bf6445, 2cb1c33, f65107f, 2079536):
строка 112 по-прежнему называет ранжирование предложением агента, строка 127 по-прежнему объявляет
его принятым владельцем.

## Триаж 2.3 (фаза S, 2026-08-14)

> Вердикты двухступенчатого триажа (механика на HEAD → скептик, дефолт REFUTED; сводная таблица — `reports/KAIF_AUDIT/2026-08-14_r2_triage_SUMMARY.md`).

| № | Вердикт | Тяжесть | Эпик | Улика триажа |
|---|---|---|---|---|
| 1 | CONFIRMED | substantial | T | researches/18_kaif_feedback_loop_in_control_terms.md:112 на HEAD в настоящем времени называет ранжирование «предложение агента», хотя таблица §2 (стр. 119: приоритет 3 — Простота; стр. 125: приоритет 9 — Самодостаточность) уже несёт порядок, принятый владельцем (стр. 127: «Порядок ПРИНЯТ ВЛАДЕЛЬЦЕМ… решение №67»; стр. 131: предложение агента отвергнуто), а MASTER_PLAN.md:167 адресует за принятым порядком именно сюда («Принятый порядок живёт в `researches/18` §2»). Прочтение строки 112 как провенанс-пометки не спасает: слов «изначально/прежний» нет, утверждение о текущем порядке фактически ложно. Не blocker: решение №67 продублировано в MASTER_PLAN:167 и в блоке ⚠️ под таблицей, потеря решения владельца маловероятна, но носитель канона первым абзацем понижает владельческое решение до агентского. |
