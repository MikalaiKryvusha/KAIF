# Bug 30 — пакет мелких дефектов третьего 2.0-отчёта (stale-claims на тексте шаблона · canon-lint selftest exit · артефакт де-экспансии · мелочи)

**Status:** 🟡 OPEN (pack; по образцу bugs/19)
**Version/build:** 2.0 · **When/context:** 2026-07-31, отчёт `KAIF_2.0_UPDATE_REPORT.md`
(дефекты 4, 6, 7 + §6 «мелочи»).

## Грани

1. **(дефект 4, 🟢)** `stale-claims` ловит собственный АКТУАЛЬНЫЙ текст апстрима: 10 из 28 хитов —
   строки пяти копий fable-judge, дословно равные шаблону 2.0 («added in KAIF 1.6» — проза о
   происхождении патча). Фикс в одну строку: пропускать пути, где
   `fileShaNorm(p) === templateShas[p]` — файл, тождественный текущему шаблону, не может нести
   устаревшее утверждение ПРОЕКТА. (Дополнение к закрытому bugs/23 — вторая грань того же шума;
   зеркала навыков каталогов `.claude/` и систем тоже покрываются этим правилом.)
2. **(дефект 6, 🟢)** `kaif-canon-lint selftest` красный (exit 1) на свежем развёртывании без
   rules-файла. Развести «не сконфигурирован» (exit 0 с подсказкой) и «гвардия не срабатывает»
   (exit 1); образец поведения — сосед kaif-provenance.
3. **(дефект 7, косметика)** `anonymize()`: «reference of the Krinik AI Framework (KAIF)» →
   «of the KAIF» — грамматический артефакт (единственный на весь набор). Фикс формулировкой
   шаблона KAIF_REFERENCE, устойчивой к схлопыванию.
4. **(§6.1)** thin `KAIF.md` удаляется self-clean'ом без предупреждения в самом файле — добавить
   строку «этот файл будет удалён по завершении» в `_thin-intro`.
5. **(§6.3)** `check` печатает 44 предупреждения о дрейфе зеркал на среднем апдейте — свести в одну
   строку «N mirrors lag the canon — normal until update-verify».
6. **(§6.4)** Семантика `kept` манифеста неточна: файл с непустым модульным merge попадает в
   `adopted` только при непустом divergedList — `AGENT_GUIDE.md` с расхождением в `kept` не попал
   (практического вреда на v2 нет — защита от `templateShas`; выправить семантику или
   задокументировать).

## Repro

Каждая грань — греп/прогон из отчёта; своды точечные при фиксе (стражи красными до).

## Fix plan

План 23, Фазы B3 (грани 1–3) и B4 (4–6). Грань 1 — вместе с классом bugs/23 (страж в s07/T4:
файл-копия шаблона со строкой «KAIF 1.6» не попадает в stale-claims).

## Decisions made without the owner

*Заполнится при закрытии.*

## Links

`KAIF_2.0_UPDATE_REPORT.md` §3.4/3.6/3.7/§6 · bugs/23_DONE (первая грань шума stale-claims) ·
bugs/19_DONE (прецедент pack-бага) · `plans/23`.
