# Bug 31 — класс L-1: перевод не защищён от ВСТАВКИ — английские модули в переведённых файлах («двухголовые» навыки)

**Status:** ✅ DONE (фикс L2, 2026-08-07; верификация — полигон s08, 8/8 сводов зелёные)
**Version/build:** машинерия 2.1 · **When/context:** 2026-08-07, триаж 4 полевых отчётов об
обновлении до 2.1 (`ideas/ai_agents_reports/2.1_update_reports/`); класс L-1 — `researches/15` §3.

## Симптом (класс, не экземпляр)

Механика обновления обещает переведённому развёртыванию неприкосновенность («mechanical
replacement disabled»), но защита закрывает только ЗАМЕНУ файла — ВСТАВКА модулей остаётся
разрешённой. Итог: английские блоки шаблона вписываются в русские документы — вплоть до
«двухголовых» навыков с двумя противоречащими процедурами (в KrinikCam — у деструктивного
`/kaif-remove`). Отягчение: у части проектов защита выключена целиком, потому что маркер
до-2.0 развёртываний не несёт `i18n: translated`, а обновление флаг не проставляет.

## Проявления по проектам (3 проекта; свод класса)

| Проект | Проявление | Где в отчёте |
|---|---|---|
| NDim | лог обещает «mechanical replacement disabled» и тут же «merged 4 module(s) into .claude/skills/pause/SKILL.md»; английский блок над русским H1 при НУЛЕВОМ изменении шаблонов («template 2.0 == 2.1 : True») | грабля 1 |
| KLAS | `PHILOSOPHY.md`: 21 EN-модуль рядом с 21 русским (20 671 → 31 038 байт); причины A и B воспроизведены скриптом-предикатом | D1 |
| KLAS | маркер вообще без ключа `i18n` — «защита по флагу выключена целиком» | D2 |
| KrinikCam | двухголовые `/pause` (сверху английские шаги, снизу русские «Шаг 5. Коммит и пуш в GitHub») и `/kaif-remove` (50 строк EN-процедуры удаления ПЕРЕД русской); `check` и `update-verify` зелёные на двухголовом файле | Г2 |

## Repro (полевые, из отчётов; локальный песочный свод — в фазе фикса)

1. Развернуть KAIF с переведёнными заголовками (или `--lang ru` + полный перевод навыка,
   оставив EN только `name:` во frontmatter), обновиться → в файле два H1, две процедуры.
2. Предикат сети отдельным скриптом (KLAS): `PHILOSOPHY.md` — совпало 2 сигнатуры из 21
   (`KISS`, `DRY` — термины не переводят) → `baseFound (2) <= ceiling (1)` ложь → файл идёт в
   по-сигнатурное слияние.
3. Детектор двухголовости (KrinikCam): `grep -c '^# ' SKILL.md` > 1.

## Root cause (4 независимые причины, все подтверждены полем)

1. **Вставка не блокируется:** `i18n: translated` отключает замену, но модуль с «отсутствующей»
   (переведённой) сигнатурой вставляется рядом — даже при байт-идентичных шаблонах старой и
   новой версии (грань bugs/32: дифф не от предыдущего релиза).
2. **Абсолютный порог сети:** `wholesaleCeiling = 1` предполагает, что перевод меняет ВСЕ
   заголовки; реальный перевод оставляет термины (KISS, DRY) → 2 выживших сигнатуры ломают
   распознавание. Порог обязан быть долей (2 из 21 = 9,5 % — очевидный перевод); вырождение
   порогов на малых N — уже оплаченный класс EXP-0019.
3. **Кириллица в телах EN-шаблонов ослепляет сеть:** условие «во входящем шаблоне НЕТ письма
   владельца» ложно для `pause` и `kaif-remove` — авторские русские примеры в телах. Ровно эти 2 шаблона
   из 34 дали дубль в трёх проектах независимо (NDim грабля 1, KLAS D1 причина B, KCam Г2 —
   те же два файла). Инвариант
   §9.11 researches/15: кириллица не живёт в телах EN-шаблонов — примеры в языковые пакеты.
4. **Флаг некому ставить:** проекты, переведённые до 2.0, не имеют `i18n` в маркере; установщик
   не спрашивает, обновление не выставляет (KLAS D2). Запросы K2: авто-простановка при
   `language != en` + срабатывании сети хотя бы на одном файле.

## Fix plan

Фаза **L2** родителя `plans/28_EPIC_kaif_2.2_L_update_field_fixes.md` (якорь: «### L2. Класс
перевода: детектор и вставка»). Состав фикса — по четырём причинам выше; гейт —
критерий приёмки эпика №2 (ноль вставок EN-модулей в translated-файлы; детектор `>1 H1` в
`check`); каждый страж доказан красным до фикса. Полигон-профиль «переведённое развёртывание»
воспроизводит все три полевых лица (NDim/KLAS/KCam).

## ✅ STATUS: DONE (2026-08-07, фаза L2 — `plans/41`)

Все четыре причины закрыты в `framework/installer/KAIF-CORE.mjs` (свод s08, стражи красные до
фикса → зелёные после; `[TESTED: 2026-08-07 · npm run test:core — 8/8 сводов]`):

1. **Вставка блокируется по построению:** модуль, отсутствующий на диске, вставляется ТОЛЬКО
   если он по-настоящему нов (отсутствует и в шаблоне предыдущего релиза). Был в старом шаблоне
   и апстрим его не менял → отсутствие — дело владельца (перевод/удаление), не вставляем;
   апстрим менял → дифф в задание, не воскрешение (гейт в цикле вставки `mergeModules`).
2. **Порог сети — доля:** `WHOLESALE_SURVIVOR_SHARE = 0.15` — потолок выживших сигнатур
   `max(1, floor(N·0.15))` при N>2; N≤2 требует нуля (EXP-0019 сохранён). Лицо KLAS
   (2 из 21 = 9,5 %) распознаётся: s08/F3a — 3462→3462 байт, ноль вставок.
3. **Кириллица из тел EN-шаблонов убрана** (`pause` «продолжаем», `kaif-remove»
   «выжги полностью»/«частично» → EN-формулировки); класс закрыт ФОРМОЙ — гард 5d в
   `tools/check-framework.mjs` (кириллица в теле EN-шаблона валит сборку; frontmatter и
   owner-seeded доки — легальные исключения; доказан красным на HEAD-версиях обоих файлов).
   Плюс сам тест «файл переведён» стал ТЕЛЕСНЫМ (`bodyLocalized`): преамбулу с машинными
   алиасами не судим — ослепление file-level проверки на всех 34 навыках снято.
4. **Авто-простановка флага:** `language != en` + сеть сработала ≥1 раз → маркер получает
   `"i18n": "translated"` с сообщением в логе (оба пути: core-update и легаси-bootstrap);
   s08/F3b.

Смежный гард: `check` краснеет на «двухголовом» файле (>1 H1, fence-aware) — по находке судьи
фазы расширен с навыков на ВСЕ не-owner .md поставки (двухголовая PHILOSOPHY — полевое
повреждение KLAS — иначе оставалась невидимой при нулевой дельте шаблона); s08/F4b + F4c.

**TWINS:** searched вставка-без-гейта — единственный цикл вставки в `mergeModules` (класс
закрыт в нём целиком, оба пути dryRun/real); searched кириллица в телах EN-шаблонов — грепом
по всем `framework/*` — ровно 2 файла (pause, kaif-remove), оба вычищены, страж 5d стережёт
весь класс навсегда.

## Decisions made without the owner

Триаж: грань «диффы от предыдущего релиза» вынесена в bugs/32 (другой механизм — построение
задания, не защита перевода); взаимная ссылка. Закрытие: (1) доля 0.15 выбрана агентом как
константа между полевым лицом KLAS (9,5 % — обязана ловиться) и владельческим кейсом T6/EXP-0019
(целая база + добавка — обязана НЕ ловиться); имени-числа от владельца нет — это порог механики,
не продуктовая константа; (2) русские примеры-триггеры во frontmatter `description` (26 навыков)
СОХРАНЕНЫ — это триггер-контракт, телесные проверки преамбулу не судят, инвариант §9.11 говорит
только о телах; (3) формулировки замен в телах pause/kaif-remove — «in any language the owner
speaks» — выбраны агентом (язык примеров убран, смысл правила сохранён).

## Links

Реестр триажа: `plans/36` (строки N1, K1, K2, C2 + наблюдения NDim гр.1, KLAS D1/D2, KCam Г2) ·
`researches/15` §3 (L-1) · bugs/32 (смежный класс задания) · bugs/20_DONE (прародитель — сеть
translatedWholesale 2.1) · bugs/26_DONE (дрейф сигнатур) · EXP-0019 (вырождение порогов).
