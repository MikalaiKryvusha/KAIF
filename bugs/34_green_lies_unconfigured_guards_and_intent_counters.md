# Bug 34 — класс L-4: «зелёная ложь» — стражи зелёные без конфигурации, счётчики считают намерение, чекпоинты-галочки, слепой провенанс

**Status:** 🔴 OPEN (заведён триажем L1 эпика L 2.2)
**Version/build:** машинерия 2.1 + tool-модули · **When/context:** 2026-08-07, триаж 4 полевых
отчётов об обновлении до 2.1; класс L-4 — `researches/15` §3. Самый населённый класс триажа
(8 запросов + 9 наблюдений из 3 проектов) — и ровно тот фрод-класс, на который охотится
`/fable-judge` («зелёное без содержания хуже красного», инвариант §9.5).

## Симптом (класс, не экземпляр)

Проверки поставки отвечают «✅» там, где им нечего было проверять, нечем было проверять или они
не проверяли вовсе. Ненастроенный страж выходит нулём; счётчик сводки рапортует о работе,
которой не было; чекпоинт ставит галочку без прогона существующего сканера; провенанс-гейт
молчит зелёным на локализованном каноне («худший класс: ложное зелено, а не отказ»).

## Проявления по проектам (3 проекта; свод класса)

| Проект | Проявление | Где в отчёте |
|---|---|---|
| Unliminium | `kaif-canon-lint.mjs selftest` без конфига → exit 0 («доказательство… отсутствует, но выглядит как успех») | Г9 |
| KLAS | оба охранника 2.1 зелёные на ненастроенной установке; вставленные сегодня в гейты, дают «вечнозелёный гейт»; нарушены «(а) охранник обязан уметь вернуть ненулевой код; (б) пустой вход обязан быть КРАСНЫМ» (KLAS EXP-0042) | D4 |
| KrinikCam | canon-lint selftest без правил → 0; поведение осознанное (bug 30.2), но следствие — вечнозелёный гейт у честного исполнителя рекомендации | Г8 |
| KLAS | «34 skills trigger-aliased» при 23 на диске: счётчик считает намерение, не запись; 11 скиллов остались без машиночитаемых алиасов | D3 |
| KLAS | чекпоинты исполняют проверку в 1 случае из 7 (`recheck`); у `stale-claims`/`placeholders` сканеры ЕСТЬ — перезапуск в момент галочки стоит ноль | D5 |
| Unliminium | чекпоинт `placeholders` отмечен закрытым при `update-verify`, падающем на том же дереве именно на плейсхолдере (нашёл судья) | §2.12-3 |
| Unliminium | `kaif-provenance check` красный на транзиенте самой машинерии (`KAIF_UPDATE_TASK.md` цитирует маркеры в описании новинки) | Г7 |
| Unliminium | `kaif-provenance` слеп к локализованным маркерам `[ИИ]`: «✅ no AI text awaits acceptance» при 91 блоке, ждущем приёмки автора | Г8 |
| KrinikCam | `provenance check` и `report` расходятся при незаявленном каноне: report — exit 0 (`nothing to report`), check — сканирует и падает с exit 1 | Г7 |
| KrinikCam | `check` exit 0 при дрейфе зеркал: в окне до sync пять агентских систем несут противоречащие навыки — «не пушь» в `.claude` против «собери APK, закоммить и запушь» в `.cline`; судья: зеркала со старым тяжёлым `/pause` и EN `/end-chat` с незаполненными плейсхолдерами | Г11 + суд.1 |
| Unliminium | `/derive-styleguide` ссылается на ключ `canonArtifacts`, которого развёртывание не создаёт — «инструкция была мёртвой» (найдено судьёй фазы L1 — триаж сперва потерял) | §3.2 |
| KLAS | «велит объявить ключ, которого в маркере нет даже пустым» — предложение сеять `"canonArtifacts": []` при развёртывании | D4 (хвост) |

## Repro (полевые)

Однострочные вызовы из таблицы; каждый воспроизводится на свежем/ненастроенном/локализованном
развёртывании (Unliminium Г8 — «Воспроизведение: локализовать маркеры (`[ИИ]`), объявить
canonArtifacts, запустить report»).

## Root cause

Единый корень: успех и «нечего проверять» неразличимы по коду выхода и значку; счётчики
инкрементируются на намерении (до записи на диск); чекпоинт — запись в файл, а не исполнение
проверки; провенанс-модуль знает только английскую пару маркеров и не читает конвенцию из
маркера развёртывания.

## Fix plan

Фаза **L3** родителя `plans/28_EPIC_kaif_2.2_L_update_field_fixes.md` (якорь: «### L3.
CLI-безопасность и зелёная ложь»): ненастроенный страж → `SKIPPED` с отдельным кодом
(3); счётчики считают запись; чекпоинты с существующим сканером ИСПОЛНЯЮТ его; провенанс читает
локализованные маркеры из маркера развёртывания и исключает транзиенты; `check`/`report`
согласованы; ключ `canonArtifacts` сеется пустым при развёртывании (мёртвая инструкция
/derive-styleguide оживает); грань «check при дрейфе зеркал» — решение фазы (красный или
`recheck` зовёт `sync`). Гейт — критерий приёмки эпика №5; каждый страж доказан красным до
фикса.

## Decisions made without the owner

*Заполняется при закрытии.* Триаж: легальность ЗАГОЛОВОЧНОЙ формы `[AI]`-марки (KrinikCam
ставит марку в заголовках секций — гейт требует парный инлайн) — НЕ решена здесь, вынесена
развилкой Q14 интервью 007.

## Links

Реестр триажа: `plans/36` (строки U3, U4, K3, K4, K5, C6, C7, C9 + наблюдения Unlim Г7/Г8/Г9,
§2.12-3, KLAS D3/D4/D5, KCam Г7/Г8/Г11, суд.1) · `researches/15` §3 (L-4), §9.5 ·
bugs/30_DONE грань 2 (прецедент: exit-семантика canon-lint — сознательный компромисс, поле
показало его цену) · bugs/17_DONE (прародитель: самоаттестация чекпоинтов) · интервью 007 Q14.
