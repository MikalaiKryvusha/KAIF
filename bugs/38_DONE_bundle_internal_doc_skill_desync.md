# Bug 38 — класс L-8: рассинхроны ВНУТРИ бандла 2.1 — пары «документ ↔ ведущий его навык» разъехались у автора фреймворка

**Status:** ✅ DONE (фикс L4, 2026-08-07; линт бандла 5e красный на ДО-фиксном HEAD — 23 находки; сборка зелёная)
**Version/build:** поставка 2.1 (framework/*) · **When/context:** 2026-08-07, триаж 4 полевых
отчётов; класс L-8 — `researches/15` §3. Ирония, названная полем дословно: «Это ровно та пара
«источник ↔ зеркало», реестр которых 2.1 вводит в канон — только разъехавшаяся у автора
фреймворка».

## Симптом (класс, не экземпляр)

Шаблоны одной поставки противоречат друг другу: документ ввёл норму — ведущий его навык о ней
не знает; канон не знает о новой сущности, миграцию в которую требует задание; внутренние
номера беклога истока уехали в downstream-проекты; у новых навыков нет входов из точек выбора
задач.

## Проявления по проектам (2 проекта; свод класса)

| Проект | Проявление | Где в отчёте |
|---|---|---|
| KLAS | `EXPERIENCE.md` несёт «REQUIRED since 2.1», `**Trigger:**`, `mechanized` — навык `/experience` не содержит ни одного из трёх (0/3) | D10 |
| KLAS | `AGENT_GUIDE.md` п. 16: из списка перечитывания выпал `TESTING_FRAMEWORK.md`; `PROJECT_HISTORY` — 0 вхождений в канон при обязательной миграции в него | D10 |
| KLAS | `(idea 17 §2)` в трёх навыках циклов — внутренняя нумерация беклога истока, висячая ссылка в любом downstream; `plans/homework_*` при существующем `homeworks/`; KAIF_REFERENCE не попал в «куда копать глубже» help-kaif; в `check-backlog` предусловие записано ПОСЛЕ действия; `/plan-task`/`/plan-epic` не прошиты в `refresh-context`/`what-next` | D10 |
| KrinikCam | судья: `/experience` остался с «Repro — опускай», хотя шаблон EXPERIENCE.md в том же релизе объявил Repro ОБЯЗАТЕЛЬНЫМ (файл даже не попал в изменившиеся — sha 1.6 == 2.1) | Г12 (§4) |
| KrinikCam | судья: `description` фронтматтера противоречит перемержённому телу навыка (грань bugs/43 — механизм; здесь — как класс рассинхрона пары) | суд. |

## Repro (полевые)

KLAS: подсчёты вхождений по каждой строке таблицы D10 (`AGENT_GUIDE.md 2.1 :: "Repro" → 0` и
т. д.) — все воспроизводятся грепом по шаблонам поставки 2.1.

## Root cause

Пары «документ ↔ ведущий его навык» и «канон ↔ вводимая сущность» никем не сверяются перед
релизом: реестр пар «истина↔зеркало» (канон 2.1) на САМ бандл не распространён. Дрейф ловится
только сверкой пар — никогда чтением одного файла (AGENT_GUIDE, «Гигиена документов»).

## Fix plan

Фаза **L4** родителя `plans/28_EPIC_kaif_2.2_L_update_field_fixes.md` (якорь: «### L4. Шум и
честность аудита»): линт бандла перед релизом — пары «документ ↔ ведущий
навык», сверка норм релизных нот с шаблонами, греп внутренних номеров беклога истока в
payload; конкретные 8 рассинхронов KLAS D10 закрываются содержательно тем же заходом. Гейт —
критерий приёмки эпика №8 («линт бандла существует и зелёный») и инвариант §9.7; сквозной
критерий релиза №7 зонтика `plans/26`.

## ✅ STATUS: DONE (2026-08-07, фаза L4 — `plans/43`)

`[TESTED: 2026-08-07 · node tools/build-framework.mjs — check-framework OK (линт 5e внутри);
красное доказательство: реплика линта против ДО-фиксных блобов HEAD — 23 находки по всем
8 строкам KLAS D10 (воспроизведено судьёй фазы независимо — ровно 23)]`:

- **Линт бандла 5e в `tools/check-framework.mjs`** (по образцу 5d): пары «документ ↔ ведущий
  навык» полными строками-токенами (EXPERIENCE-нормы — ОБЕ стороны пары после находки судьи;
  TESTING_FRAMEWORK в перечитывании; PROJECT_HISTORY в каноне; клауза нот «never patched in
  place»; KAIF_REFERENCE в help-kaif; лестница в refresh-context/what-next; /guarded-loop в
  AGENT_GUIDE); FORBIDDEN-скан утечек («(idea N §M)», `plans/homework_`) по ВСЕМУ framework/
  (.md+.mjs — расширено по находке судьи); order-guard «предусловие ДО git mv» в check-backlog.
- **Все 8 рассинхронов KLAS D10 закрыты контентно** — в шаблонах framework/* И в русской
  обвязке `.claude/skills/` + корневом AGENT_GUIDE (п.13 + граница STATUS/HISTORY — находка
  судьи): /experience несёт три нормы; TESTING_FRAMEWORK в списках перечитывания;
  PROJECT_HISTORY знаком канону; `(idea 17 §…)` вычищено из payload (7 вхождений — правило
  самодостаточно живёт в AGENT_GUIDE «drive-by rule»); `plans/homework_*` → `homeworks/`;
  help-kaif ведёт к KAIF_REFERENCE; check-backlog — предусловие первым; /plan-task,
  /plan-epic прошиты в точки выбора задач.

## Decisions made without the owner

Триаж: KCam-грань «frontmatter при мерже» учтена здесь классом
рассинхрона, механизм — bugs/43 (взаимные ссылки, фиксы в разных фазах L4/L2). Закрытие:

1. **Линт — кураторская таблица пар, не парсер нот:** каждое новое обещание релизных нот
   добавляет свою строку в PAIRS руками (дисциплина `/release`); автогенерация из текста нот
   признана хрупкой (KISS).
2. **Русская обвязка `.claude/skills` — вне охвата линта** (осознанная граница: обвязка не
   деплоится; её синхронизация — ручная дисциплина + судья фаз; найденные судьёй рассинхроны
   обвязки закрыты этим же заходом).
3. **Двусторонность** — истинная сторона запинена для ведущей пары (EXPERIENCE-нормы);
   остальные истины держит сама структура (docs-список check-framework: документ обязан
   существовать). Пары-цитаты остаются короче полного зеркалирования — цена ложных зелёных
   признана и смягчена уникальностью токенов (проверена счётом вхождений).
4. Внутренние координаты «(bug NN)» в комментариях машинерии НЕ запрещены — это код-комментарии
   к истории истока (конвенция с 1.5), не инструкции downstream-агенту; запрещены только
   инструкционные якоря в телах md.

## Links

Реестр триажа: `plans/36` (строки K9, C12 + наблюдения KLAS D10, KCam Г12/суд.) ·
`researches/15` §3 (L-8), §9.7 · bugs/37 (ноты↔код) · bugs/43 (frontmatter) · AGENT_GUIDE
«Реестр пар „истина↔зеркало"» (норма, которую распространяем на бандл).
