# Bug 39 — класс L-9: вердикт судьи принимается ТОЛЬКО CLI-аргументом — машинерия нарушает собственную норму «текст через файлы»

**Status:** ✅ DONE (фикс L3, 2026-08-07; полигон 9/9 — свод s09 G12)
**Version/build:** машинерия 2.1 (`checkpoint judge`) · **When/context:** 2026-08-07, триаж
4 полевых отчётов; класс L-9 — `researches/15` §3.

## Симптом (класс, не экземпляр)

`checkpoint judge` требует `--verdict "<многострочный текст с кириллицей, ёлочками, тире>"` —
файлового варианта нет. Релиз 2.1 сам ввёл в канон правило «TEXT TRAVELS THROUGH FILES, NEVER
THROUGH COMMAND-LINE ARGUMENTS» с четырьмя лицами класса; «Фреймворк требует от проектов
дисциплины, которой не следует в собственном API».

## Проявления по проектам (2 проекта)

| Проект | Проявление | Где в отчёте |
|---|---|---|
| Unliminium | `die('checkpoint judge requires --verdict` — файлового пути нет; в Git Bash + Node 24 кириллица прошла, «но норму писали не для везучих: в PowerShell 5.1 или при `python -c`-подобной обвязке тот же вызов даёт порчу с зелёным кодом выхода» | Г12 |
| KrinikCam | «Вердикт судьи — многострочный текст на языке владельца (у нас кириллица, кавычки-ёлочки, стрелки), и единственный способ его записать — то, от чего канон предостерегает»; на macOS/zsh прошло, на Windows PS5 — «лицо 1» | Г6 |

## Repro (полевые)

Любой `checkpoint judge --verdict "…"` с не-ASCII на Windows PowerShell 5.1 — класс
«текст-через-CLI» из AGENT_GUIDE («Гигиена документов и текста», симптом 1: тихая моджибака
при зелёном exit-коде).

## Root cause

API спроектирован до канонизации нормы (норма — 2.1, API — тоже 2.1: разошлись внутри одного
релиза); файловый путь не был предусмотрен.

## Fix plan

Фаза **L3** родителя `plans/28_EPIC_kaif_2.2_L_update_field_fixes.md` (якорь: «### L3.
CLI-безопасность и зелёная ложь» — строка «`--verdict-file` (L-9 — заодно, тот же
слой CLI)»): `--verdict-file <path>` рядом с `--verdict`; строковый вариант остаётся для
ASCII-односложных случаев. Сшивка с решением №42: вердикт едет в коммитимую квитанцию —
файловый путь становится ещё и носителем приёмки.

## ✅ STATUS: DONE (2026-08-07, фаза L3 — `plans/42`)

`checkpoint judge --verdict-file <путь>` существует
(`[TESTED: 2026-08-07 · s09 G12a/G12b: кириллица + ёлочки + BOM + многострочник]`):

- файл читается UTF-8, BOM-терпимо, EOL-нормализованно; первая непустая строка — в
  чекпоинт-строку задания, ПОЛНЫЙ текст — в коммитимую квитанцию `.kaif/last-update.json`
  ключом `judgeVerdict` (сшивка с решением №42: квитанция — носитель приёмки; схема —
  KAIF_REFERENCE §12.3);
- `--verdict "<ascii-однострочник>"` остаётся (совместимость заданий, напечатанных в поле);
  оба флага сразу — отказ;
- подсказки в тексте задания и в гейтах verify переведены на файловый вариант первым.

**TWINS:** searched другие не-ASCII-тексты, принимаемые аргументами CLI: `sphere <name>`
(машинное имя, ASCII по конвенции) и `checkpoint <id>` (машинные id) — класса не несут;
`accept <file>` провенанса принимает ПУТЬ, не текст. Класс закрыт единственным носителем.

## Decisions made without the owner

Триаж: «нет таких». Закрытие: (1) в чекпоинт-строку идёт только ПЕРВАЯ строка вердикта —
однострочный формат `^TAG: judge verdict: ` вшит в гейты и полевые задания, менять его в
минорной фазе рискованно; полный текст не теряется (квитанция); (2) рекомендованный путь
для файла вердикта — `.kaif/install/verdict.md` (транзиентная зона, самоочистится; копии
живут в задании и квитанции).

## Links

Реестр триажа: `plans/36` (строки U11, C5 + наблюдения Unlim Г12, KCam Г6) · `researches/15`
§3 (L-9) · AGENT_GUIDE «Гигиена документов и текста» (нарушаемая норма) · решение №42
(квитанция с вердиктом).
