# Bug 55 — два стража меряют прокси вместо свойства (классовый документ)

> **Создан:** 2026-08-08 16:26 +03:00 (аудит 01, `plans/33` шаг 5 — пробный прогон
> `/code-revision`). **Родитель:** `reports/KAIF_AUDIT/2026-08-08_family-10_green-without-substance.md`.
> **Статус:** 🔴 OPEN — оба вхождения подтверждены скептиком с живым воспроизведением; фикс не начат.
> **Вовне:** —.

**Status:** 🔴 OPEN · **Version/build:** 2.1 (build 190) · **When/context:** найдены 2026-08-08
пробным аудитом задачи T3.

## Вектор цели (Avoid)

Не допустить, чтобы проверка зеленела на свойстве, которого не смотрит. Класс — семейство 10
таксономии проекта («зелёное без содержания»), оплаченное `bugs/34`, `bugs/17`, `EXP-0017`,
`EXP-0044`. Классовый документ, потому что оба вхождения — один механизм в двух инструментах.

**Fix accepted when (observable):** для КАЖДОГО вхождения — критерий из его карточки, включая
доказательство красного мутацией; чистое дерево остаётся зелёным.

## Инвентарь класса (полный)

| # | Место | Прокси, который меряется | Свойство, которое НЕ меряется |
|---|---|---|---|
| F1 | `tools/counters-guard.mjs:134` | число строк Таблицы 3 = 35 × 2 | те же ли ИМЕНА навыков в каждой половине |
| F5 | `tools/sandbox/s03-receipts-tools.mjs:132` | условие `A \|\| B \|\| true` | работает ли `diff` после апгрейда манифеста v1→v2 |

Twin-поиск по классу выполнен: `grep -rn "|| true" tools/` → 2 попадания, из них 1 дефект и 1 —
записанное правило против него (`tools/verify-contour.mjs:15`, C11); иных тавтологичных ассертов в
полигоне нет. По оси «счёт вместо имён» вторая пара реестра («Состав навыков ↔ ключи 9 языковых
пакетов», `AGENT_GUIDE.md:521`) — **тот же прокси**: голый счёт ключей. Она в инвентарь входит как
третье вхождение при починке.

## Symptom · Repro · Root cause · Fix plan

Полностью — в карточках F1 и F5 аудит-отчёта (цитаты, живые прогоны, эскизы фикса, границы
«чего не трогать», сегодняшнее измеренное состояние). Здесь не дублируются: отчёт — запись,
этот документ — рабочий пункт.

Кратко: F1 — переименование навыка оставляет обе половины README документирующими мёртвую команду,
страж печатает «✅ 17 зеркал сверены», EXIT=0 (доказано двумя переименованиями в песочнице).
F5 — `|| true` делает ассерт всегда истинным, `diff` может падать стектрейсом при зелёном своде.

⚠️ **F1 несёт незакрытый вопрос, который исполнитель обязан снять ПЕРВЫМ шагом:** тождество
множеств слагов Таблицы 3 и `framework/skills/` сегодня НЕ замерено, и новая проверка может
закраснеть сразу — в том числе из-за алиасов (`/go` при навыке `kaif-go`). Сначала замер и решение
о судьбе алиасов, потом код.

## Decisions made without the owner

Заполняется при закрытии.

## Links

`reports/KAIF_AUDIT/2026-08-08_family-10_green-without-substance.md` (карточки) ·
`reports/KAIF_AUDIT/2026-08-08_guards-machinery-suites_SUMMARY.md` (прогон) · `bugs/49` (ось ЧИСЕЛ
того же стража, закрыта) · `bugs/34` · `bugs/17` · `EXP-0044` · `EXP-0017` ·
`plans/32_EPIC_kaif_2.2_P_skill_rename.md` (эпик, который нагрузит эту пару).
