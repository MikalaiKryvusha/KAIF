# Bug 66 — охват стража уже класса, который страж объявил своим: семь слепых зон

**Status:** 🟡 PARTIAL — вхождение №1 (слепок стилометрии, блокер) ЗАКРЫТО 2026-08-09
09:49 +03:00; шесть остальных открыты
**Version/build:** build 238 · **When/context:** круг R1 фазы R (`plans/59` шаг 7), 2026-08-09
**Fix accepted when (observable):** у каждого из семи стражей охват вычисляется, а не
перечисляется фикс-списком; мутация в бывшей слепой зоне краснеет; `--selftest` каждого несёт эту
мутацию.

## ⚠️ ГЛАВНОЕ: утечка оказалась НЕ латентной — она уже опубликована

Круг R1 записал по вхождению №1: «в текущем слепке реализованной утечки нет (0 длинных спанов вне
ёлочек) — дыра латентна». **Это опровергнуто при починке.** Независимая ось приёмки, написанная
в ходе фикса (сверка n-граммами с доказательными блоками источника, не смотрящая на кавычки
вовсе), нашла в опубликованном `AUTHOR_STYLOMETRY.md` **три настоящие утечки** личного текста
владельца — они лежали в публичном репозитории и уехали в GitHub.

Ревизор R1 их не увидел ровно потому, что искал «длинные спаны ВНЕ ёлочек», то есть мерил ту же
примету, что и сломанная чистка. Урок дословный: **проверка, делящая слепое пятно с проверяемым,
не проверяет ничего** — и это верно даже тогда, когда проверку пишет независимый ревизор.

Носители трёх утечек (все — формы, невидимые построчному выражению `«([^«»]+)»`):

| # | Форма носителя | Почему прошла |
|---|---|---|
| 1 | ВЛОЖЕННАЯ: `«внешний текст «токен» продолжение»` | выражение матчит только ВНУТРЕННИЙ короткий спан и признаёт его разрешённым токеном; текст ВНЕШНЕГО спана не матчится вовсе |
| 2 | НЕПАРНАЯ вложенность: `««текст?”»` | глубина не возвращается к нулю — сканеру региона нет; выражению мешает вторая кавычка |
| 3 | МНОГОСТРОЧНАЯ в обычной прозе (не в блок-цитате) И с вложенной внутри | построчный проход не видит спан, а прежний многострочный проход требовал отсутствия кавычек внутри |

**Решение о судьбе git-истории — за владельцем.** Удаление файла историю не отзывает; переписывание
опубликованной истории требует force-push, то есть строки `AUTH:` (`AGENT_GUIDE.md` → Git-процесс),
и агент его не инициирует. Текущий слепок пересобран и чист.

## ✅ Вхождение №1 закрыто — что сделано и чем доказано

- **Чистка:** сканер «ёлочек» с учётом ВЛОЖЕННОСТИ (внешний спан — один спан, а не внутренний),
  плюс страхующее выражение по всем парам кавычек для непарных форм, плюс второй проход по
  АБЗАЦУ (граница спана — пустая строка) для многострочных.
- **Приёмка:** третья ось, НЕЗАВИСИМАЯ от глифов — ни одно окно из восьми слов подряд,
  принадлежащее доказательному блоку источника, не встречается в слепке. Именно она нашла все три
  утечки и именно она переживёт появление новой формы кавычек.
- **`--selftest`:** семь форм утечки схлопнуты, чистая копия молчит (обе стороны доказательства,
  EXP-0059). Формы K6 и K7 в волну R1 не входили — их нашла сверка пересборки с ПОБАЙТНЫМ
  эталоном: первая редакция фикса оказалась регрессией на непарной вложенности, и поймал её диff,
  а не чтение кода.
- **Побайтная сверка:** пересборка против эталона — 114 слов убрано, новых токенов нет ни одного,
  кроме самого знака схлопывания.
- Маркер файла переведён `[NOT-TESTED]` → `[TESTED: …]` по наблюдению (закрывает и вхождение
  `sty-not-tested-underwrites-guarantee` из `bugs/71` в части этого инструмента).

## Symptom

Семь стражей объявляют классом одно, а обходят подмножество. Слепая зона в каждом случае доказана
мутацией: дефект внесён В ЗОНУ, которую страж называет своей, — страж зелёный.

Самое опасное вхождение — публичный слепок портрета голоса владельца: его фильтр приватности
построен как ЧЁРНЫЙ список на одной паре глифов, поэтому личная цитата, набранная любыми другими
кавычками, уезжает в публичный репозиторий дословно.

## Repro (deterministic)

1. **Слепок стилометрии (blocker).** В копию приватного источника вписать пять «канареек» —
   цитаты в «ёлочках», в типографских “ ”, в „ “, в прямых " и голой строкой; собрать слепок
   `node tools/stylometry-snapshot.mjs --source <копия> --report <файл>` → exit 0, приёмка молчит;
   `grep KANARY <копия репо>/AUTHOR_STYLOMETRY.md` → **3 из 5 канареек опубликованы дословно**
   (схлопнуты только те две, что были в «ёлочках»).
2. **Гард 5d (кириллица в теле EN-шаблона).** Дописать кириллическую строку (из ТЕЛА скрипта, не
   из argv) в `framework/templates/_owner-voice-template.md` или в
   `framework/skills/fable-method/references/examples.md` → `node tools/check-framework.mjs` →
   exit 0, «✅ check-framework OK».
3. **Таблица письменностей.** `SCRIPTS` (`framework/installer/KAIF-CORE.mjs:161`) не содержит
   записей для `de`, `es`, `fr`, `pt` — четырёх ОТГРУЖАЕМЫХ пакетов из девяти:
   `localizedAgainst(lang=de …) -> false [SCRIPTS entry: MISSING]`, то же для fr/es/pt.
4. **Имена событий хуков (O5).** В копии подменить у образца Cursor `"sessionStart"` →
   `"SessionStart"`, у Antigravity `"PreInvocation"` → `"PreToolUse"` →
   `node tools/sandbox/s14-refresh-hooks.mjs` → exit 0, все 21 ассерт зелёные.
5. **`counters-guard` и Таблица 1 витрины.** В копии удалить обе строки `AUTHOR_STYLOMETRY.md` из
   Таблицы 1 README → `node tools/counters-guard.mjs` → exit 0, `node tools/showcase-lint.mjs` →
   exit 0. Подмена «14 documents» → «9 documents» тоже проходит зелёной.
6. **`voice-lint` и грамматика.** Сломать в копии релиз-нот падеж, управление и согласование
   («замыкание цикл», «под наблюдение проектов, который им пользуются») →
   `node tools/voice-lint.mjs <копия>` → exit 0, «жёстких нарушений нет».
7. **Форм-листы `showcase-lint`.** `«for every dollar actually paid»` (README.md:532) — зелёный;
   тот же смысл словами `in fact` — красный (BACKSTAGE). «Заплачено **фактически**» — зелёный,
   «на самом деле» — красный.

## Forensics — инвентарь класса (7 вхождений)

| # | Страж | Объявленный охват | Фактический |
|---|---|---|---|
| 1 | `tools/stylometry-snapshot.mjs:16,105,282` | «наружу НЕ ЕДЕТ НИ ОДНА цитата текста» | только спаны в `«…»`; чёрный список на одной паре глифов, приёмка смотрит ТОТ ЖЕ паттерн, что и чистка |
| 2 | `tools/check-framework.mjs:128-134` (гард 5d) | комментарий: «every OTHER payload surface that deploys as English prose» | фикс-список из трёх каталогов: `hooks`, `spheres`, `adapters`; вне охвата — `templates/_owner-voice-template.md`, `skills/code-revision/references/audit-report-template.md`, три `skills/fable-method/references/*.md`, `installer/_thin-intro.md` |
| 3 | `framework/installer/KAIF-CORE.mjs:161` (`SCRIPTS`) | защита переведённого развёртывания | 5 языков из 9; для de/es/fr/pt не работают ни сеть wholesale (`:850`), ни per-file заморозка (`:1077`), ни авто-флаг `i18n` (`:1228`/`:1722`), ни классификация `localized` в аудите (`:1590`) |
| 4 | `tools/sandbox/s14-refresh-hooks.mjs:154-216` | критерий 3 `plans/60`: «имена событий совпадают с подтверждённым контрактом» | 21 ассерт по трём осям, ни один не читает КЛЮЧИ-события (`commandsOf` собирает только значения `command`/`bash`/`powershell`) |
| 5 | `tools/counters-guard.mjs` | «сверяет разом ВСЕ зеркала» (AGENT_GUIDE.md:625) | у живого числа `docs` нет ни одного зеркала в `MIRRORS`; Таблица 1 витрины не названа зоной |
| 6 | `tools/voice-lint.mjs` (жёсткие A2/A3/B9/B12/B5) | `plans/67:49`: «класс держит `voice-lint`» под классом «грамматика авторской трактовки» | ни один жёсткий пункт не касается падежа, согласования и написания слова |
| 7 | `tools/showcase-lint.mjs:58-66` и класс BACKSTAGE | «каждый класс записан формой, а не обобщением» | в форм-листах нет «фактически», «делается», `actually` — притом что «в действительности», «на самом деле», `in fact` есть |

## Root cause

Охват стража задан **фикс-списком, написанным в день рождения стража**, а класс продолжает расти
(появились новые каталоги поставки, четыре латинских языковых пакета, вторая половина README,
новые образцы хуков, новые формы слов от владельца). Список не знает, что он неполон, а зелёный
читается как «класс проверен».

Отдельная причина у №1: фильтр публикации приватного построен как ЧЁРНЫЙ список. Чёрный список
ошибается в сторону утечки, и цена ошибки асимметрична — утечка в git-историю публичного
репозитория не отзывается (EXP-0062 говорит ровно это: фильтр строится БЕЛЫМ списком).

Усиливающий слой у №1 и №5: приёмка стража смотрит тем же паттерном, что и его рабочая часть, —
проверка делит слепое пятно с проверяемым, то есть не проверяет ничего (EXP-0047).

## Fix plan

- [ ] **№1 (первым — blocker):** переписать фильтр слепка БЕЛЫМ списком: наружу едет только то,
      что явно разрешено (`allowSpans`, языковые токены, публичные слова владельца по адресу
      `файл:строка`); всё остальное схлопывается независимо от глифов кавычек. Приёмку построить
      на НЕЗАВИСИМОМ признаке (длина непрерывного спана из источника), а не на том же паттерне.
      Завести `--selftest` с канарейками всех пяти форм.
- [ ] **№2:** охват 5d ВЫЧИСЛЯТЬ (все `framework/**/*.md`, что деплоятся, минус языковые пакеты),
      а не перечислять; мутация в каждой бывшей слепой зоне — в `--selftest`.
- [ ] **№3:** `SCRIPTS` заменить на вычисляемую проверку письменности (латиница ≠ EN — сравнивать
      с телом английского шаблона), либо честно объявить: латинские пакеты защиты не имеют, и
      записать это долгом с адресом.
- [ ] **№4:** добавить в `s14` ось имён событий и промутировать её (подмена ключа обязана краснеть).
- [ ] **№5:** завести зеркало `docs` (Таблица 1 обеих половин: строка на каждый ключевой документ,
      поимённо — счёт один ловит пропажу, но не подмену).
- [ ] **№6:** либо выписать формы дефекта №2 волны Q в жёсткие пункты, либо снять с `voice-lint`
      кредит за этот класс в `plans/67` и назвать честного исполнителя.
- [ ] **№7:** дополнить форм-листы дословными формами владельца («фактически», «делается»,
      `actually`); формы брать из `AUTHOR_STYLOMETRY.md` §5 (АП31) и `plans/68`, а не из головы.

## Decisions made without the owner

*Заполняется при закрытии.*

## Links

`plans/59` · `plans/60` (критерий 3) · `plans/67` (строки 2 и 19 волны) · `bugs/31_DONE` (клаузула
«страж 5d стережёт весь класс навсегда» — опровергнута) · EXP-0062 · EXP-0073 · EXP-0074 ·
`AGENT_GUIDE.md` → «Витрина».
