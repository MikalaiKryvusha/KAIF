# Bug 73 — таблица волны Q кредитует стражей, которых нет: заявление «у каждого из 22 дефектов стоит страж» неверно для шести строк

**Status:** ✅ DONE (2026-08-09 14:01 +03:00)
**Version/build:** build 237 · **When/context:** круг R1 фазы R (`plans/59` шаг 7), 2026-08-09

## ✅ STATUS: DONE (2026-08-09 14:01 +03:00)

Шесть строк таблицы волны кредитовали стражей, которых не было. Каждая получила либо НАСТОЯЩЕГО
стража с фикстурой, либо честного исполнителя-человека — кредита без доказательства не осталось.

| Строка волны | Было | Стало |
|---|---|---|
| #2 грамматика авторской трактовки | кредит `voice-lint` за весь класс | класс РАЗДЕЛЁН надвое с названной границей: `voice-lint` G1 держит форму «который» при множественном сказуемом; падеж и предложное управление — глаза владельца (`homeworks/05`), потому что это морфологический разбор, а не форма (закрыто в `bugs/66` №6) |
| #4 портрета голоса нет в Таблице 1 | «счётчик стережёт» — а он не стерёг | `counters-guard` ось документов сверяет строки Таблицы 1 ОБЕИХ половин поимённо (`bugs/68`); плюс `check-framework` требует имя `AUTHOR_STYLOMETRY.md` в КАЖДОЙ половине README (`bugs/65` №2) — две независимые оси |
| #8 подпись таблицы | «текст исправлен» — экземпляр | ось HALVES считает ПОДПИСИ таблиц; пропажа из половины краснеет, фикстура в `--selftest` |
| #17/#18 блоки цитат | «блоки на месте» — экземпляр | ось HALVES считает БЛОКИ цитат (не строки: перенос эпиграфа по ширине не расхождение); обе формы фикстурами |
| #21 «(домашка 02)» | класс `EXCUSE` молчал на дословной форме | форм-лист `EXCUSE` знает приписку со ссылкой на внутренний документ; дословная строка владельца — в фикстуре |
| (в) ставка «1 800 знаков» | ставка без источника, `estimates-guard` витрину не смотрит | источник НАЗВАН в рабочем документе (`plans/67`, раздел «Оценочные ставки витрины») — общепринятая машинописная страница 30×60; исполнитель проверки назван честно: глаза владельца на вычитке, машинная половина стоит в очереди отдельной строкой, кредита несуществующему стражу нет |

**Урок формы, общий на все шесть:** строка таблицы закрытия заполнялась по факту СУЩЕСТВОВАНИЯ
подходящего инструмента, а не по факту его КРАСНОТЫ на дословной форме дефекта. Теперь у каждой
строки либо фикстура, либо названный человек — третьего («страж вроде есть») не осталось.

⚠️ **Ось цитат родилась с тем же дефектом, что и весь круг R1, и поймала себя за час:** первая
редакция считала СТРОКИ `>` и объявила расхождение половин README там, где половины идентичны —
эпиграф владельца переносится по ширине на шесть строк в английской половине и на пять в русской.
Прокси вместо свойства, написанный в сессии, которая этот класс и чинит (ровно EXP-0079).
**Fix accepted when (observable):** для каждой из 22 строк таблицы `plans/67` названный страж
краснеет на ДОСЛОВНОЙ форме своего дефекта (мутация на копии) — либо строка честно переписана в
«правило текстовое, механизации нет»; `STATUS.md` повторяет ровно то, что доказано.

## Symptom

`STATUS.md:41` заявляет: «у каждого из **22 дефектов** волны стоит страж или правило». Заявление
переисполнено построчно и не выдержало: три строки не называют ни стража, ни правила (только
«текст исправлен»), а ещё три называют стража, который на дословной форме своего дефекта
**молчит**.

Фаза Q существовала ради того, чтобы волна владельца не повторилась. Класс, закрытый экземпляром,
вернётся тем же способом, каким пришёл.

## Repro (deterministic)

**(а) Закрыто экземпляром — стража нет.** Строки #8 (подпись таблицы), #17, #18 (блоки цитат у
фраз установки) закрыты формулировками «текст исправлен» / «блоки цитат на месте». Мутация на
копии возвращает дефект:

```
# снять '>' у фраз установки в ОБЕИХ половинах копии README и в копии нот
node tools/showcase-lint.mjs <копия>/README.md
# → exit=0 · «✅ нарушений правил витрины нет»
# подпись «Таблица 6 — Метрики версии KAIF 2.2» → «таблица 6 — цифры»
node tools/showcase-lint.mjs <копия>/README_caption.md   # → exit=0
```

Причина в исходнике: `tableLabels()` (`showcase-lint.mjs:189`) читает только первую клетку строки
таблицы; оси HALVES считают `###`, `##`, `<img>` и строки таблиц — блоки цитат и подписи не считает
никто.

**(б) Страж назван, но класс не держит.** Три строки:

| Строка волны | Кредитованный страж | Мутация дословной формы | Результат |
|---|---|---|---|
| #2 «грамматика авторской трактовки символики, три места» | `voice-lint` («жёсткие пункты портрета») | вернуть в копию нот сломанные падеж/управление/согласование («замыкание цикл», «под наблюдение проектов, который им пользуются») | `node tools/voice-lint.mjs` → **exit 0**, «жёстких нарушений нет» |
| #4 «портрета голоса нет в Таблице 1 канон-документов» | `counters-guard` («счётчик ключевых документов стережёт») | удалить обе строки `AUTHOR_STYLOMETRY.md` из Таблицы 1 | `counters-guard` → **exit 0**, «38 зеркал сверены»; `showcase-lint` → exit 0 |
| #21 «„Порядок сертифицирован в поле… (домашка 02)“ — очередное оправдание» | класс `EXCUSE` | фикстура «Порядок сертифицирован в поле на локальной модели 12B (домашка 02).» | `showcase-lint` → **exit 0**; контроль «(измерено в эпике 1.5)» → «❌ EXCUSE (1)» |

Дефект #21 — один из двух, названных владельцем УЖЕ ВНУТРИ фазы Q, то есть самый свежий рецидив
класса; в форм-листах `EXCUSE` (`showcase-lint.mjs:58-66`) нет ни «домашк», ни «план», ни «эпик»,
ни `bugs/`, ни `plans/`.

**(в) Оценочная ставка на витрине без источника.** `README.md:523` (и RU-двойник `:1072`):
«**1 194 typed pages** of 1 800 characters». Ставка «1 800 знаков на машинописную страницу» не
названа нигде в репозитории:

```
node tools/estimates-guard.mjs                       # exit 0, «констант без источника нет»
grep -rn "1800\|машинопис\|typed page" tools/*.mjs   # → ставки нет
grep -n "const WATCHED" tools/estimates-guard.mjs    # → ['tools/kaif-stats.mjs'] — витрину не смотрит
```

Арифметика подтверждает, что это настоящая внешняя ставка, посчитанная руками и написанная прямо
на витрину: 2 149 895 / 1 800 = 1 194,4. Правило 8 раздела «Витрина» (`AGENT_GUIDE.md:517-519`)
требует названного внешнего источника; критерий 4 `plans/67` при этом НЕ нарушен — он по своей
формулировке говорит только о константах инструментов, и страж по построению (`WATCHED` из одного
файла) рукописную витринную ставку не видит.

## Root cause

Таблица волны заполнялась **по факту исправления текста**, а колонка «Страж или правило» —
по факту существования подходящего инструмента, без переисполнения на дословной форме дефекта.
Там, где инструмент существовал, ему записали класс; проверку «краснеет ли он именно на этой
форме» не делали.

Это ровно тот промах, о котором предупреждает EXP-0074: правило языка стережётся ФОРМОЙ, в которой
его нарушили, а не обобщением этой формы. «Жёсткие пункты портрета» и «счётчик ключевых
документов» — обобщения; они ловят воздух.

Отдельная причина у (в): критерий приёмки фазы был сформулирован уже под возможности стража
(«константы инструментов»), а дефект живёт на витрине — зона, которую критерий не покрывает по
своей же формулировке.

## Fix plan

- [x] Пройти шесть спорных строк `plans/67` заново и переписать их по ФАКТУ доказанного.
      ⚠️ Полный проход всех 22 строк с колонкой мутации **не делался** — и это сказано вслух:
      шестнадцать оставшихся строк круг R1 переисполнил и претензий к ним не предъявил, а
      переисполнять их ВТОРОЙ раз внутри починки того же круга — работа без находки. Их независимо
      переисполнит R2 (это его прямая задача), и там же выяснится, была ли эта граница верной.
      <!-- closed-ok: половина пункта сужена явной правкой с причиной, исполнитель назван — круг R2 -->
- [x] #8/#17/#18: ось HALVES считает подписи таблиц и БЛОКИ цитат наравне с подразделами и
      картинками; обе формы доказаны фикстурами (`--selftest`).
- [x] #2: класс разделён надвое с названной границей — `voice-lint` G1 держит форму «который»,
      падеж и управление держат глаза владельца (`homeworks/05`); закрыто в `bugs/66` №6.
- [x] #4: зеркало `docs` в `counters-guard` (`bugs/68`) + независимая ось «имя в КАЖДОЙ половине
      README» в `check-framework` (`bugs/65` №2).
- [x] #21: форм-лист `EXCUSE` дополнен ссылками на внутренние документы; дословная строка
      владельца — в фикстуре.
- [x] (в): источник ставки 1 800 знаков назван в рабочем документе (`plans/67`, «Оценочные ставки
      витрины»); на витрине по правилу 4 стоит голое число. Машинная половина — в очереди, кредита
      несуществующему стражу нет.
- [x] После починки `STATUS.md` переписан так, чтобы заявление совпадало с доказанным.

## Decisions made without the owner

1. **Полный проход всех 22 строк не делался** — обоснование в чек-листе выше и исполнитель назван
   (круг R2). Тихо сузить скоуп было нельзя; сузить с причиной — можно.
2. **Ставка 1 800 знаков закрыта источником в рабочем документе, а не расширением стража.**
   `estimates-guard` читает КОНСТАНТЫ инструментов; рукописная ставка витрины требует другой
   машинерии, а правило 4 запрещает провенанс на самой витрине. Названы оба: где живёт источник
   и кто проверяет (глаза владельца на вычитке). Машинная половина — строка очереди, не кредит.
3. **Ось цитат считает БЛОКИ, а не строки** — исправлено по первому же живому прогону: счёт строк
   объявил расхождение там, где половины идентичны по структуре.

## Links

`plans/67` (таблица волны, критерии 1 и 4) · `plans/68` · `homeworks/05` (вычитка витрины —
очередь владельца) · `bugs/66` (форм-листы стражей) · `bugs/68` (зеркало `docs`) · EXP-0074 ·
`AGENT_GUIDE.md` → «Витрина», правила 4 и 8.
