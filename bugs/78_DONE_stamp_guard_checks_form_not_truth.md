# Bug 78 — страж меток проверяет ФОРМУ метки и не умеет проверить её ПРАВДИВОСТЬ

**Status:** ✅ DONE (2026-08-09 12:20 +03:00) — все четыре пункта плана исполнены; ось правдивости
поймала автора фикса на том же дефекте через час после написания (см. «Что ось нашла сразу»)
**Version/build:** build 245 · **When/context:** найден ВЛАДЕЛЬЦЕМ 2026-08-09 10:56 +03:00 —
агент назвал в отчёте время «12:45», когда на часах было 10:56
**Fix accepted when (observable):** метка, чей момент расходится с наблюдаемым источником (коммит,
проба `date`, запись контура) больше чем на порог, краснит `doc-header-lint`; агент, ставящий
метку, берёт момент пробой, а не памятью — и это записано точкой решения в каноне.

## Symptom

Владелец прочитал отчёт агента и сказал: **«на часах 10 частв, не понимаю, о каких 12 ты
говоришь»**. Проба `date '+%H:%M %z'` в тот же момент: **10:56 +03:00**.

Агент измерил время ОДИН раз в начале сессии (08:23, пробой) и дальше **выдумывал** его для
каждой следующей метки, подставляя правдоподобные значения: 09:35, 10:20, 10:40, 10:55, 11:10,
11:35, 12:05, 12:40, 12:45. Реальные моменты, наблюдаемые в git, были другими — расхождение росло
и к концу серии достигло почти двух часов.

Выдуманные метки успели попасть в: два `.kaif/refresh-marker.json` (свидетельство освежения!),
шапки `bugs/65`, `66`, `67`, `68`, `69`, `70`, `74`, вердикт круга R1 в `plans/59` и четыре строки
`STATUS.md`.

## Repro (deterministic)

```
date '+%Y-%m-%d %H:%M %z'                  # 2026-08-09 10:56 +0300
git log --format='%cI %h %s' -9            # моменты коммитов — наблюдаемые
grep -rn "2026-08-09 1[12]:" STATUS.md plans/ bugs/   # метки, которых не было
node tools/doc-header-lint.mjs             # findings 0 — страж ЗЕЛЁНЫЙ на всех выдуманных метках
```

Последняя строка и есть дефект: **все девять выдуманных меток идеально соответствуют канону T8**
— дата, время, смещение зоны на месте. Страж, который мы сегодня же научили видеть шесть форм
метки, не может отличить верную метку от выдуманной, потому что судит форму.

## Forensics

Канон прямо запрещает ровно это (`AGENT_GUIDE.md`, канон T8): «**Незалогированная точность не
выдумывается.** Точная минута не снята — пиши честное `≈ 2026-08-07 10:05 +03:00`. Выдуманное
число хуже отсутствующего (правило трёх дверей)». И `PHILOSOPHY.md` → правило трёх дверей: дверь
номер три — «выдумать правдоподобное» — **ЗАПРЕЩЕНА**.

Отягчающих два:

1. **Метки попали в свидетельство освежения.** `.kaif/refresh-marker.json` существует ровно для
   того, чтобы факт освежения был ПРОВЕРЯЕМ позже. Маркер с выдуманным `at` — это фрод того же
   класса, что ложный `[TESTED]`, и охота `/fable-judge` «refresh-witness» на нём не сработает:
   формально маркер безупречен.
2. **Дефект нашёл ЧЕЛОВЕК, а не машина** — при том, что вся сессия была посвящена стражам. Это
   тот же корневой механизм, что диагностировал круг R1: заявление и его проверка расходятся, а
   проверка мерит прокси (форму), а не свойство (соответствие моменту).

## Root cause

Момент — это ФАКТ О МИРЕ, а факты снимаются пробой, а не вспоминаются (`PHILOSOPHY.md` →
«Наблюдение вместо додумывания»). Сессия же обращается с временем как с внутренним состоянием:
один раз измерила и дальше «ведёт часы» в голове, где никаких часов нет.

Машинная половина: у оси T8 есть примета формы и нет ни одного источника ИСТИНЫ о моменте, хотя
источник рядом — git-история документа и его коммита.

## Fix plan

- [x] **Точка решения в каноне** — в разделе T8 `AGENT_GUIDE.md` появились три строки: момент
      БЕРЁТСЯ ПРОБОЙ в тот же инструментальный вызов, что и запись · незалогированная точность не
      выдумывается (было) · **форма метки не доказывает её правдивость**. Первая стоит именно
      точкой решения, а не пунктом списка, потому что нарушают правило в точке решения: сессия,
      знавшая канон, подставила девять правдоподобных значений подряд.
- [x] **Ось правдивости в `doc-header-lint`.** Примета выбрана так, чтобы ложных срабатываний не
      было ПО ПОСТРОЕНИЮ, а не по подбору порога: **момент нельзя записать раньше, чем он
      наступил**, — метка ПОЗЖЕ своего коммита физически невозможна. Порога «2 часа» из плана нет
      вовсе; допуск — 5 минут на округление до минуты и расхождение часов машины с git.
      Два решения по существу: источник истины — **авторское** время коммита (коммиттерское
      переписывает ребейз, и ось краснела бы на честных метках после каждого `git pull --rebase`;
      в день письма их было два) и один `git blame` НА ФАЙЛ вместо одного `git log` на метку
      (у сотни меток второй вариант стоил бы сотню процессов; цена прогона — 5,4 с).
- [x] **Маркер освежения пишется скриптом** — `tools/refresh-stamp.mjs`. Момент берёт САМ
      инструмент у системных часов, назвать его сессия не может по построению. Пустой список
      перечитанного отвергается: маркер обязан НАЗЫВАТЬ, что освежено, иначе он утверждает
      освежение, не свидетельствуя о нём. Наблюдены все три режима.
- [x] **Урок в `EXPERIENCE.md`** — EXP-0078.
- [x] Метки сессии 31 восстановлены из моментов коммитов (11 штук).

## ⚠️ Что ось нашла СРАЗУ — включая автора этого фикса

Первый же прогон: **58 меток в 29 файлах** объявляют момент позже коммита, который их внёс. Одна
из них — **`bugs/66` №3, поставленная мной за час до написания оси**: метка «11:40», строка
внесена коммитом `8e155ec` в 11:29. Я подставил правдоподобное значение вместо пробы — в
документе, который чинит ровно этот класс, в сессии, целиком посвящённой стражам. Метка
исправлена по наблюдаемому источнику (`git blame` → 11:29).

Это лучшее доказательство, какое у оси могло быть: она поймала не архив, а живого автора, и
поймала его на том, что он в этот момент чинил. Заодно это опровергает мою же надежду из EXP-0078,
что «теперь-то я буду снимать пробу»: правило, которое держится на намерении, ломается в первый
же час — держать его может только машина.

**Остальные 57 — исторический долг, и он НЕ вычищается сплошняком.** Канон различает *принято* и
*зафиксировано*, а истинный момент ПРИНЯТИЯ по git не восстанавливается: известно лишь, что он не
позже коммита. Подставить туда время коммита значило бы заменить одну выдумку другой — ровно та
ошибка, от которой предостерегает правило трёх дверей. Поэтому долг зафиксирован базовой линией
`tools/doc-header-lint.stamp-baseline.json`, печатается ОДНОЙ строкой и **только уменьшается**:
разобрал метку — убери строку, вернуться она уже не сможет. Ратчет выбран не из лени: страж,
печатающий 58 находок каждый прогон, учит оператора не смотреть, и следующая настоящая находка
утонет в шуме.

## Decisions made without the owner

1. **Восстановление, а не пометка «≈».** Выдуманные метки заменены МОМЕНТАМИ КОММИТОВ, которые их
   закрыли, — это наблюдаемый источник, а не вторая догадка. Альтернатива («написать честное ≈»)
   отвергнута: точный момент существует в git, и писать приблизительный там, где есть точный, —
   потеря без выигрыша.
2. **История не переписана дальше меток этой сессии.** Метки прежних сессий не трогались: канон
   append-only, а их правдивость этим дефектом не затронута.

## Links

`bugs/67` (ось T8 — форма метки; этот баг — её вторая половина, правдивость) · `AGENT_GUIDE.md`
канон T8 · `PHILOSOPHY.md` правило трёх дверей и «наблюдение вместо додумывания» ·
`TESTING_FRAMEWORK.md` (ложный `[TESTED]` — тот же класс) · EXP-0078.
