# Bug 85 — полигон и гейты: свод судит НАПИСАННОЕ и ПРИСУТСТВУЮЩЕЕ, а не исполненное; часть осей и вызовов не покрыта ничем

**Status:** 🔬 ПРЕДПОЛОЖИТЕЛЬНЫЙ — не верифицирован, причина не установлена · скоуп **2.3**
**Version/build:** build 279 · **When/context:** круги ревью R1/R2 фазы R, 2026-08-09
**Fix accepted when (observable):** НЕ ОПРЕДЕЛЁН — критерий приёмки задаёт исполнитель после
установления причины (решение №70: разбор и лечение за Fable, не за автором документа)

Десять вхождений одного места сборки — полигона `tools/sandbox*`, гейтов `tools/*-guard.mjs`,
`tools/doc-header-lint.mjs` и вызовов этих машин из ритуальных навыков. Объединяет их то, что
наблюдал прогон: свод проверяет ПРИСУТСТВИЕ строки или ПРИСУТСТВИЕ подстроки-маркера, а не
исполнение проверяемого; либо ось объявлена в своде и в витринных документах, но не имеет
фикстуры вовсе. Ни одно вхождение не верифицировано как дефект: ниже — только что заявлено, что
наблюдалось и где смотреть.

Все десять адресов открыты 2026-08-09 после шести сегодняшних блокеров (`300104b`, `6bf6445`,
`2cb1c33`, `f65107f`, `2079536`). Ни один адрес не пропал; у трёх вхождений сместились номера
строк — смещения названы поимённо.

---

## 1. Вызов гейта публикации `showcase-stats-guard` держится только строкой в `/release`

**Адрес:** `.claude/skills/release/SKILL.md:59`
Также: `tools/sandbox/s12-k5-contour-canon.mjs:109-116` (массив `RITUAL_CALLS`),
`tools/sandbox-suite.mjs`, `AGENT_GUIDE.md:604`

**Заявлено.** Строка ритуала объявляет инструмент гейтом: «**Инвентарные числа витрины — гейт
публикации:** `node tools/showcase-stats-guard.mjs` (красное = стоп)». Для СОСЕДНЕГО гейта того
же шага сторож вызова заведён явно и продан строкой реестра `AGENT_GUIDE.md:605`: «Вызов стережёт
`s12`: снимешь шаг из `/end-chat` или `/release` — свод краснеет (`bugs/69`)». У строки
`AGENT_GUIDE.md:604` про `showcase-stats-guard` такой оговорки нет.

**Наблюдалось.** Ни один свод не сверяет присутствие вызова `showcase-stats-guard` ни в одном
ритуале. В `s12` массив `RITUAL_CALLS` — три строки, все три про `verify-contour`.

**Улика.**
```
grep -rn "showcase-stats" tools/ --include=*.mjs
tools/showcase-stats-guard.mjs:2, :25, :26, :110, :168   — все пять совпадений внутри самого инструмента
```
Прочитан `tools/sandbox/s12-k5-contour-canon.mjs:109-119`: `RITUAL_CALLS` = `/end-chat` ×
`--etalon-only`, `/end-chat` × `--write-etalon`, `/release` × полный `verify-contour`.

**Кого касается.** Гейт между витриной и публикацией держится дисциплиной редактора одного md —
класс, уже оплаченный трижды сработавшим и никем не увиденным красным G7.

**Замечено.** Круг R2, 2026-08-09.

**Состояние на 2026-08-09:** адрес на месте, строка 59 дословно совпадает; наблюдаемое
воспроизводится без изменений.

---

## 2. Страж витрины `showcase-lint` не вызывается автоматически ничем

**Адрес:** `package.json:12`
Также: `tools/counters-guard.mjs:105`, `tools/check-framework.mjs:88`

**Заявлено.** `bugs/80` закрыт как «мусорный символ на витрине больше не проходит мимо всех
гейтов»; `showcase-lint` назван машиной, которая судит витрину.

**Наблюдалось.** `"test": "node tools/check-framework.mjs"` — и всё; каталога `.github/workflows`
нет, активных git-хуков нет; в машинерии `showcase-lint` упомянут только внутри `counters-guard`,
который дёргает его с `--classes` ради ЧИСЛА классов, и в комментарии `check-framework.mjs:88`.
Прогон витрины происходит, только если человек или агент наберёт команду руками.

**Улика.**
```
package.json:10-21 → scripts: build · test · pdf · kaif:check · kaif:version · kaif:update ·
                     kaif:fork · kaif:switch-origin · kaif:remove · kaif:remove-all · test:core
ls .github/workflows → ls: cannot access '.github/workflows': No such file or directory
ls .git/hooks | grep -v sample → (пусто)
grep -rn "showcase-lint" --include=*.mjs --include=*.json .
  tools/check-framework.mjs:88   — комментарий, вызова нет
  tools/counters-guard.mjs:105   — execFileSync(... 'showcase-lint.mjs', '--classes')
  tools/counters-guard.mjs:140   — имя строки реестра
  tools/counters-guard.mjs:480, :562 — копирование инструмента в песочницу СВОЕГО селфтеста
  tools/showcase-lint.mjs:*      — сам инструмент
```

**Кого касается.** Витрина проверяется, только если агент вспомнит команду, — а именно «забыли
прогнать» породило оба живых экземпляра класса.

**Замечено.** Круг R2, 2026-08-09.

**Состояние на 2026-08-09:** адрес на месте (`package.json:12` дословно). Уточнение к записи
круга: упоминаний в машинерии не одно, а четыре, но исполняющий вызов по-прежнему один —
`counters-guard.mjs:105` с `--classes`, то есть ради числа классов, не ради суда над витриной.

---

## 3. Снятие фильтра `_DONE_` с оси меток не имеет ни одной мутации

**Адрес:** `tools/doc-header-lint.mjs:531-535` (`inStampScope`)
Также: `tools/doc-header-lint.mjs:680` (единственная `_DONE_`-фикстура селфтеста; в записи круга
адрес назван как `:645` — строка сместилась), `tools/doc-header-lint.mjs:521` (`DONE_RE` в
`inScope`)

**Заявлено.** Половина фикса `bugs/67`, объявленная в STATUS баг-дока и вписанная строкой в
реестр пар `AGENT_GUIDE`: «Ось МЕТОК смотрит и в `_DONE_`-файлы… 114 документов из 199».

**Наблюдалось.** `inStampScope` действительно не фильтрует по `_DONE_` (фильтр `DONE_RE` остался
только в `inScope`, строка 521). Но единственная `_DONE_`-фикстура селфтеста —
`ideas/06_DONE_closed.md` с содержимым `без H1 — вне дефолтного скоупа\n` — метки времени не
несёт вовсе. Возврат фильтра в `inStampScope` в 19 предсказанных находках ничего не меняет.

**Улика.** Мутант на копии (круг R2): в `inStampScope` возвращён `if (DONE_RE.test(rel)) return
false;` → `--selftest` EXIT=0, «19 предсказанных находок»; тот же мутант по мутированному корпусу
→ «findings 0» вместо красного.

**Кого касается.** Половина корпуса стережётся только тем, что никто не тронул четыре строки
скоупа; метка закрытия, рождающаяся в момент переименования в `_DONE_`, снова перестанет
проверяться бесшумно.

**Замечено.** Круг R2, 2026-08-09.

**Состояние на 2026-08-09:** оба адреса на месте, содержимое `inStampScope` и `_DONE_`-фикстуры
не изменилось. Мутация в этом заведении не повторялась; повторно наблюдался только чистый прогон:
`node tools/doc-header-lint.mjs --selftest` → EXIT=0, «selftest: ok — 19 предсказанных находок».

---

## 4. Ось «закрытие против тела» не имеет ни фикстуры, ни строки в своде

**Адрес:** `tools/doc-header-lint.mjs:438-464` (`lintClosureVsBody`) против селфтеста
`tools/doc-header-lint.mjs:655-823` (в записи круга — `:618-720`, блок сместился)
Также: `tools/sandbox/s13-requirements-lint.mjs`, `STATUS.md:155` (в записи круга — `:115`)

**Заявлено.** Итоговая строка селфтеста цитировалась в фикс-коммите как доказательство; STATUS
продаёт инструмент строкой реестра стражей: «`node tools/doc-header-lint.mjs` | шапка-мета ·
метки времени и их ПРАВДИВОСТЬ · **закрытие против тела** · **PENDING без СУДЬБЫ**».

**Наблюдалось.** У соседних осей — своя фикстура И красного, И молчания (`38_epic29_stamps.md`
для меток, `39_epic29_pendings.md` для PENDING). У `lintClosureVsBody` нет ни одной строки ни в
селфтесте, ни в `s13`: в блоке селфтеста нет ни одного файла, объявляющего себя закрытым, ни
одного `- [ ]`, ни одного `<!-- closed-ok: -->`. Ожидаемые 19 находок не изменятся, если ось
молча сломать.

**Улика.** Мутант круга R2: `return;` в начале `lintClosureVsBody` → `--selftest` «ok — 19
предсказанных находок», EXIT=0; по живому дереву → «scanned 117, findings 0 — all headers green».
Дословный вывод селфтеста при заведении документа перечисляет оси и ЗАКРЫТИЯ ПРОТИВ ТЕЛА среди
них не называет:
```
selftest: ok — 19 предсказанных находок на сломанной фикстуре, +1 под --all, чистые файлы молчат,
композиция со словарём находит стоп-слово, ось правдивости краснеет на метке ПОЗЖЕ своего коммита
и молчит на более ранней, ратчет глушит ровно известную, ось PENDING краснеет на адресе вместо
судьбы и молчит на четырёх законных формах
```

**Кого касается.** Заявление проверки шире покрытия — и именно оно цитировалось как доказательство
закрытия `bugs/76`; ось может умереть при рефакторе бесшумно.

**Замечено.** Круг R2, 2026-08-09.

**Состояние на 2026-08-09:** оба адреса на месте со смещением строк (тело оси 438-464, селфтест
655-823, строка STATUS 155). Отсутствие фикстуры воспроизводится:
`grep -n "closed-ok\|- \[ \]\|ЗАКРЫТЫМ" tools/doc-header-lint.mjs` даёт только строки САМОЙ оси
(427, 431, 435, 459-462) и ничего в блоке селфтеста; `s13-requirements-lint.mjs` строк про
закрытие против тела не содержит.

---

## 5. Фикстура, объявленная доказательством изъятия `FORMAT_TALK_RE`, тавтологична

**Адрес:** `tools/doc-header-lint.mjs:721` (строка фикстуры; в записи круга — `:686`)
Также: `tools/doc-header-lint.mjs:247` (`FORMAT_TALK_RE`),
`tools/doc-header-lint.mjs:724-725` (комментарий-свидетельство)

**Заявлено.** Строка фикстуры объявлена свидетельством самим своим текстом: «эта строка МОЛЧИТ:
она говорит О ФОРМАТЕ», комментарий ниже — «восемь форм молчат — молчание проверяется наравне с
краснотой (EXP-0059)».

**Наблюдалось.** Молчание этой строки обеспечивает другой механизм: образец формата стоит в
обратных кавычках (`insideCode`), а сам токен формата датой не является — примета `STAMP_RE`
требует `\d{4}-\d{2}-\d{2}`, которого в строке нет вовсе. Изъятие «разговор О ФОРМАТЕ»
(`FORMAT_TALK_RE`) не покрыто ни одной фикстурой.

**Улика.** Мутация круга R2: `FORMAT_TALK_RE` → `/zzz_never_zzz/u`, затем `--selftest` → EXIT=0,
ни одна строка вывода не изменилась.

**Кого касается.** Целая ветка изъятий живёт без покрытия, а свод даёт ложное свидетельство
«молчание проверено» — тот класс, который проект чинит в `bugs/65`.

**Замечено.** Круг R2, 2026-08-09.

**Состояние на 2026-08-09:** `FORMAT_TALK_RE` на строке 247 дословно совпадает с записью круга;
строка фикстуры сместилась с 686 на 721, текст не изменился. Наблюдаемое воспроизводится.

---

## 6. Гейт маркеров тестирования проверяет ПРИСУТСТВИЕ строки `[TESTED:`, а не факт прогона

**Адрес:** `tools/sandbox-suite.mjs:121`
Также: `tools/sandbox-suite.mjs:132`

**Заявлено.** Комментарий гейта (`sandbox-suite.mjs:113-120`) объявляет его гейтом, а не
советчиком: «that is the `[TESTED]` fraud one level up, at the rule rather than the artifact.
Debt is ZERO by construction … so this is a GATE, not an adviser». `TESTING_FRAMEWORK.md`
объявляет ложный TESTED фродом для judge.

**Наблюдалось.** `const MARKER_RE = /\[TESTED:|\[NOT-TESTED\]/;` и единственная его работа —
`if (!MARKER_RE.test(readFileSync(p, 'utf8'))) unmarked.push(...)`, то есть проверка наличия
подстроки в файле. Ось правдивости построена только для меток МОМЕНТА (`doc-header-lint`),
соседний носитель того же класса не тронут.

**Улика.**
```
grep -n "MARKER_RE" tools/sandbox-suite.mjs → :121 (объявление), :132 (единственное применение)
grep -rn "TESTED" tools/*.mjs → все совпадения либо сами маркеры в шапках инструментов,
                                либо проза о правиле; ни один инструмент не сверяет [TESTED]
                                с наблюдаемым источником
```

**Кого касается.** Файл, где маркер дописан без прогона, гейт пропускает молча и подтверждает
зелёным — механизировано ровно та половина, которую фрод не задевает.

**Замечено.** Круг R2, 2026-08-09.

**Состояние на 2026-08-09:** оба адреса дословно на месте, номера строк не сместились.

---

## 7. Скоуп гейта тест-статусов — фикс-список трёх каталогов, а печатает он полное покрытие

**Адрес:** `tools/sandbox-suite.mjs:124`
Также (файлы вне охвата): `framework/installer/KAIF-LOADER.mjs`, `framework/kaif-unpack.mjs`,
`framework/tools/kaif-canon-lint.mjs`, `framework/tools/kaif-provenance.mjs`,
`framework/tools/kaif-requirements-lint.mjs`;
`bugs/71_DONE_canon_rule_declared_mechanized_without_executor.md`

**Заявлено.** Гейт печатает «✅ preflight … every machinery file carries a test-status marker» и
объявляет: «Debt is ZERO by construction … so this is a GATE, not an adviser». `bugs/71` №3
засчитал его настоящим исполнителем правила.

**Наблюдалось.** `const dirs = [['tools', REPO], ['tools/lib', REPO], ['tools/sandbox', REPO]];`
— загрузчик, распаковщик и все три ОТГРУЖАЕМЫХ стража поставки без маркеров и гейту невидимы.

**Улика.** Круг R2: `git ls-files | grep -E '\.mjs$'` за вычетом трёх охваченных каталогов → 10
файлов, пять из них без маркера; мутации E/F/G (неотмеченный файл в `framework/tools/`, в новом
`tools/guards/`, в `framework/hooks/`) → все дали «✅ preflight …», EXIT=0.

**Кого касается.** Отгружаемый в чужие проекты страж может уехать без единого наблюдения, а
главный гейт объявит полное покрытие.

**Замечено.** Круг R2, 2026-08-09.

**Состояние на 2026-08-09:** адрес дословно на месте (строка 124). Пересчёт при заведении:
```
git ls-files '*.mjs' | grep -vE '^tools/(lib/|sandbox/)?[^/]+\.mjs$'   → 10 файлов
маркеров нет ровно в пяти: framework/installer/KAIF-LOADER.mjs · framework/kaif-unpack.mjs ·
framework/tools/kaif-canon-lint.mjs · framework/tools/kaif-provenance.mjs ·
framework/tools/kaif-requirements-lint.mjs
```
Совпадает с записью круга дословно, включая поимённый список пяти.

---

## 8. Ловушка дедупа `s04` судит каталог, который сама записала

**Адрес:** `tools/sandbox/s04-anon-legacy.mjs:143-149`
Также: `tools/sandbox/s04-anon-legacy.mjs:126` (`FP_TEMPLATE`),
`tools/sandbox/s04-anon-legacy.mjs:133-135` (вывод разделителя из поставки),
`bugs/65_DONE_assert_judges_proxy_not_claimed_property.md:56-58`

**Заявлено.** `bugs/65`, раздел «Вхождение №3 закрыто»: «контракт читается из РАЗВЁРНУТОГО
установкой навыка… поэтому потеря контракта в поставке красит ЛОВУШКУ».

**Наблюдалось.** Из поставки выводятся только разделитель полей и признак присутствия двух строк
контракта (`DEDUP_RULE`, `FP_TEMPLATE`). Сама процедура дедупа — замыкание `dedupScan` внутри
теста (строки 143-145), читающее каталог, который тест же записал восемью строками выше
(`writeFileSync` на 138-140). Красными от изменений поставки становятся лишь добавленные рядом
ассерты присутствия строк.

**Улика.** Две мутации поставки на копии (круг R2): вырез 4503 символов KAIF-ветвления
`/report-bug` и смена разделителя `' :: '` → `' | '` — обе строки самой ловушки (146-149) остались
зелёными.

**Кого касается.** Свойство «поставленная процедура дедупа находит дубликат» не проверяется
ничем, а операторы считают эту ловушку доказательством.

**Замечено.** Круг R2, 2026-08-09.

**Состояние на 2026-08-09:** все адреса дословно на месте, номера строк не сместились.

---

## 9. `s12` стережёт НАПИСАНИЕ вызова приёмки, а саму приёмку не исполняет

**Адрес:** `tools/sandbox/s12-k5-contour-canon.mjs:104` (секция 5) и `:117-119` (сама проверка)
Также: `tools/sandbox/s12-k5-contour-canon.mjs:109-116` (`RITUAL_CALLS`),
`.claude/skills/end-chat/SKILL.md`, `.claude/skills/release/SKILL.md`, `AGENT_GUIDE.md:605`

**Заявлено.** Комментарий секции 5: «Норма, чей сигнал никто не читает, эквивалентна отсутствию
нормы: намеренный красный G7 сработал ТРИЖДЫ за двое суток и не был увиден никем, потому что
прогон не вызывался ниоткуда». `AGENT_GUIDE.md:605` продаёт: «Вызов стережёт `s12`: снимешь шаг
из `/end-chat` или `/release` — свод краснеет (`bugs/69`)».

**Наблюдалось.** Проверка — `readFileSync(join(ROOT, rel), 'utf8').includes(needle)`: свод
проверяет, что строка НАПИСАНА, и никогда её не запускает. Вычёркивание с сохранением текста
(`~~СНЯТО…~~` — штатный редакторский жест этого репозитория) оставляет свод зелёным. Ни
`/autoloop`, ни `/nightloop`, ни `/dayloop`, ни `/guarded-loop` не зовут ни одного гейта.

**Улика.** Круг R2: копия HEAD с добавленным живым интервью — `verify-contour --etalon-only`
exit=1 (намеренный красный G7), при этом `s12` exit=0 («all 20 checks green»), а
`check-framework`, `sandbox-suite`, `counters-guard`, `questions-guard`, `doc-header-lint` — все
0. Мутация с вычёркиванием при сохранённом тексте → «all 20 checks green». Цена замерена:
`--etalon-only` стоит 0,150 с, а `s12` уже шеллится в 4 селфтеста.

**Кого касается.** За автономную ночь дрейф эталона снова уйдёт незамеченным, а владельцу продано
обратное.

**Замечено.** Круг R2, 2026-08-09.

**Состояние на 2026-08-09:** оба адреса дословно на месте, номера строк не сместились;
`RITUAL_CALLS` по-прежнему три строки `verify-contour`, проверяемые через `.includes(needle)`.
Отдельно перепроверено при заведении:
```
grep -n "verify-contour\|sandbox-suite\|check-framework\|doc-header-lint\|counters-guard\|showcase" \
  .claude/skills/{autoloop,nightloop,dayloop,guarded-loop}/SKILL.md
→ ни одного совпадения ни в одном из четырёх навыков
```

---

## 10. Возврат рукописного счёта в шапку инструмента не ловит никто

**Адрес:** `tools/sandbox/s12-k5-contour-canon.mjs:10-13` (в записи круга — `:10-12`; фраза
занимает четыре строки)

**Заявлено.** Шапка: «Границы серий и их счёт живут В МАССИВЕ ниже и печатаются итоговой строкой
прогона — прозой здесь не дублируются: рукописный счёт в шапке протухает молча ровно тем же
дрейфом, который свод и стережёт (`bugs/68`; EXP-0025)». Пункт плана обещал ЗЕРКАЛО для «s12 и
родни».

**Наблюдалось.** Зеркало снято для `s12`, но механизма против возврата рукописи нет и «родня»
нигде не перечислена: ни `counters-guard`, ни `check-framework` шапки инструментов не читают.

**Улика.** Мутация M6 круга R2 — возврат «Ростер (97 позиций, 3 селфтеста)» в шапку →
`counters-guard` exit 0; `grep -n "шапк" tools/counters-guard.mjs` → пусто.

**Кого касается.** Класс закрыт единичной правкой одного файла, а не формой: следующая шапка с
рукописным счётом протухнет тем же дрейфом.

**Замечено.** Круг R2, 2026-08-09.

**Состояние на 2026-08-09:** адрес на месте, текст шапки не изменился;
`grep -n "шапк" tools/counters-guard.mjs` при заведении по-прежнему пуст.

## Триаж 2.3 (фаза S, 2026-08-14)

> Вердикты двухступенчатого триажа (механика на HEAD → скептик, дефолт REFUTED; сводная таблица — `reports/KAIF_AUDIT/2026-08-14_r2_triage_SUMMARY.md`).

| № | Вердикт | Тяжесть | Эпик | Улика триажа |
|---|---|---|---|---|
| 1 | STALE | hygiene | - | Вхождение снято вместе с самим гейтом: коммит 32504af удалил tools/showcase-stats-guard.mjs целиком и строки вызова из .claude/skills/release/SKILL.md и AGENT_GUIDE.md. Грep по showcase-stats на HEAD — ноль совпадений: незастрахованного вызова не существует, потому что не существует инструмент. |
| 2 | REFUTED | hygiene | - | Модель вызова этого репо — ритуалы, а не CI (CI/хуков нет НИ ДЛЯ ОДНОГО стража; npm test = check-framework для всех). showcase-lint вшит ровно туда, куда проект вшивает гейты: .claude/skills/release/SKILL.md:86 — «Витрину судит МАШИНА… Красное = стоп до починки ТЕКСТА» (гейт публикации) и .claude/skills/end-chat/SKILL.md:30 — «Трогал витрину — node tools/showcase-lint.mjs». Путь витрины к публике идёт через /release, где линт обязателен, — закрытие bugs/80 держится. По мерке вхождения («не вызывается автоматически ничем») пришлось бы осудить каждый страж репозитория разом — это осуждение дизайна, а не дефект. |
| 3 | CONFIRMED | hygiene | T | tools/doc-header-lint.mjs:531-535 — inStampScope без DONE-фильтра (сам фикс bugs/67), но единственная _DONE_-фикстура селфтеста (строка 680: «без H1 — вне дефолтного скоупа», аннотирована как тест скипа inScope) метки времени не несёт; раскладка expected=19 (строка 737) _DONE_-меточной фикстуры не содержит. Возврат фильтра в inStampScope селфтест не красит — свойство, проданное AGENT_GUIDE.md:602 («Ось смотрит и в _DONE_-файлы БЕЗ --all»), не имеет красного доказательства вопреки собственной норме файла (G10, EXP-0059: «молчание проверяется наравне с краснотой»). Дефект покрытия реален, но живого неверного поведения нет — гигиена селфтеста. |
| 4 | CONFIRMED | substantial | T | tools/doc-header-lint.mjs:438-464 — lintClosureVsBody жив, но в блоке селфтеста 655-736 нет ни одного файла, объявляющего себя закрытым, ни одного «- [ ]», ни одного closed-ok; раскладка expected=19 на строке 737 («4+1+2+1+1+2+1+2+4+1») члена оси закрытия не содержит. STATUS.md:105 продаёт ось строкой реестра («закрытие против тела»), при этом `return;` в начале оси оставляет селфтест зелёным, а по живому дереву ось даёт 0 находок — её смерть наблюдением неотличима от жизни. Ось, заявленная стражем и цитированная как доказательство закрытия bugs/76, не защищена ничем. |
| 5 | CONFIRMED | hygiene | T | tools/doc-header-lint.mjs:247 (FORMAT_TALK_RE), применение только на :289 и :299 — ПОСЛЕ совпадения STAMP_RE/STATUS_DATE_RE. Фикстура :721 даты \d{4}-\d{2}-\d{2} не содержит вовсе, поэтому до изъятия исполнение не доходит — её молчание гарантировано отсутствием даты, а не изъятием. Контраст рядом: EXEMPT_MARK_RE честно упражняется фикстурой :722 (настоящая дата + t8-ok). Комментарий :724-725 засчитывает эту строку в «восемь форм молчат — молчание проверяется» ложно: мутация FORMAT_TALK_RE в никогда-не-матч селфтест не меняет. Узкая ветка, живого неверного поведения нет — гигиена. |
| 6 | REFUTED | hygiene | - | Намеренный дизайн, а не нарушенное свойство. Гейт и в комментарии (:113-120), и в печати (:142) заявляет ровно «every machinery file CARRIES a test-status marker» — присутствие маркера, что реализация (:121, :132) исполняет дословно. Текст ошибки :138-139 («never stamp one without a run») прямо адресует правдивость человеку/агенту, а TESTING_FRAMEWORK.md закрепляет ложный TESTED как фрод ДЛЯ JUDGE — суд правдивости каноном отдан судье, не машине. Вхождение требует другого инструмента (сверки маркера с фактом прогона), которого гейт никогда не обещал; собственный текст вхождения это признаёт («механизирована ровно та половина»). |
| 7 | CONFIRMED | substantial | T | tools/sandbox-suite.mjs:124 — скоуп зашит тремя каталогами (tools, tools/lib, tools/sandbox), а строка :142 печатает универсальное «every machinery file carries a test-status marker». На HEAD пять ОТГРУЖАЕМЫХ файлов машинерии без единого маркера (framework/installer/KAIF-LOADER.mjs, framework/kaif-unpack.mjs, framework/tools/kaif-canon-lint.mjs, kaif-provenance.mjs, kaif-requirements-lint.mjs — grep -c = 0 в каждом), и гейт при этом объявляет полное покрытие. Печатаемое утверждение гейта фактически ложно, а правило канона, чьим исполнителем его засчитал bugs/71 №3, не исполняется ровно на файлах, уезжающих в чужие проекты. |
| 8 | REFUTED | hygiene | - | Заявленное bugs/65 свойство («потеря контракта в поставке красит ЛОВУШКУ») на HEAD держится: ассерты s04:127-130 читают РАЗВЁРНУТЫЙ установкой SKILL.md и краснеют при потере контракта — само вхождение это признаёт («красными становятся… ассерты присутствия строк»), то есть обе мутации R2 свод красят, спор лишь о том, КАКИЕ строки краснеют. Поставленная «процедура» дедупа — проза-инструкция для агента, машиной неисполнимая; механизируемый максимум — присутствие полных уникальных строк контракта плюс верная локальная симуляция описанного грепа по surface+class (dedupScan :143-145, разделитель выведен :133-134 под гейтом присутствия шаблона). Вхождение подменяет заявленное свойство более сильным («тест исполняет поставленную процедуру»), которого ни один документ не обещал. |
| 9 | REFUTED | hygiene | - | Три части — и все три либо задокументированный дизайн, либо неверное прочтение. (а) AGENT_GUIDE.md:604 обещает «снимешь шаг из /end-chat или /release — свод краснеет» — для снятия строки это ПРАВДА, и ровно это стережёт needle-проверка s12:117-119; сам заголовок s12 (:14-16) объявляет разделение намеренным: свод стережёт, что приёмка ВЫЗЫВАЕТСЯ ритуалами, а тяжёлый прогон «гоняется на приёмке, не на каждой сборке». (б) Красный verify-contour при зелёном s12 на копии с живым интервью — штатная норма G7: красный срабатывает там, где прогон исполняется, — /end-chat и /release (release/SKILL.md:60-66). (в) «Ни один цикл не зовёт ни одного гейта» — артефакт грепа по именам инструментов: autoloop/SKILL.md:75-76 предписывает «одно осмысленное изменение = один полный прогон гейтов (сборка + проверки) = один коммит», а состав прогона задан STATUS.md:102 (build + test:core: 14 сводов, 4 преполёта). Обход вычёркиванием ~~СНЯТО~~ — умышленная циркумвенция, от которой ни один текстовый страж не обещал защиты. |
| 10 | REFUTED | hygiene | - | На HEAD ни одно свойство не нарушено: рукописный счёт из шапки снят, шапка s12:11-13 документирует форму лечения (счёт живёт в массиве и печатается итоговой строкой прогона), и грep по живому дереву не находит ни одного файла с возвратом рукописного счёта («(N позиций/проверок/селфтестов» в tools/ — ноль совпадений). Жалоба сводится к отсутствию линтера против ГИПОТЕТИЧЕСКОЙ будущей регрессии и к непоименованной «родне» — пожелание усиления, противоречащее KISS/Оккаму (PHILOSOPHY.md), а не дефект: вхождение не называет ни одного живого протухшего экземпляра, обещанное планом снятие зеркала для s12 исполнено. |
