# Bug 86 — заявления о закрытии: девять мест, где документ объявляет работу сделанной, а прогон по адресу видит другое

**Status:** 🔬 ПРЕДПОЛОЖИТЕЛЬНЫЙ — не верифицирован, причина не установлена · скоуп **2.3**
**Version/build:** build 279 · **When/context:** круги ревью R1/R2 фазы R, 2026-08-09
**Fix accepted when (observable):** НЕ ОПРЕДЕЛЁН — критерий приёмки задаёт исполнитель после
установления причины (решение №70: разбор и лечение за Fable, не за автором документа)

Девять вхождений об одном: документ фазы Q или её починок объявляет пункт закрытым, а по названному
адресу лежит то же, что лежало до починки. Разброс носителей — баг-доки `bugs/71`, `bugs/72`,
`bugs/73`, `bugs/78`, таблица волны `plans/67`, канон `AGENT_GUIDE.md`, ядро стилометрии, летопись и
домашка, которую читает владелец. Причина ни по одному вхождению не устанавливалась; ниже — только
адрес, заявление, наблюдение и то, что видно по адресу сегодня.

## 1. Красная фикстура I17 объявлена снятой вместе с проверкой, но живёт в четырёх копиях внутри блока, названного гейтом секции

**Адрес:** `.claude/skills/owner-reviews/SKILL.md:492`.
Также: `framework/skills/owner-reviews/SKILL.md:499` · `dist/KAIF-CORE-BUNDLE.md:5468` ·
`dist/KAIF-FULL.md:6010` · `bugs/71_DONE_canon_rule_declared_mechanized_without_executor.md:111`.

**Заявлено.** `bugs/71`, Fix plan №1, отмечен `[x]`: «Красная фикстура, описанная в навыке для
несуществующей проверки, снята вместе с ней». То же дословно — в теле коммита `4809202`.

**Наблюдалось.** Строка «греп показа (I17) — фикстура ответа со «см. файл X.pdf» → красный» жива во
всех четырёх копиях навыка и поставки. Коммит фикса не тронул ни одного файла под `skills/`. Блок,
в котором она стоит, озаглавлен «**Доказательство красным, страж за стражем (гейт этой секции):**»
и перечисляет фикстуру I17 в одном ряду с существующими красными доказательствами G1, I20, G3, G4,
QA7, I14.

**Улика.**

```
$ grep -rn "I17" --include=*.md .
./.claude/skills/owner-reviews/SKILL.md:492:красный · греп показа (I17) — фикстура ответа со «см. файл X.pdf» → красный · мёртвый сервер
./framework/skills/owner-reviews/SKILL.md:499:show-grep (I17) — a reply fixture saying "see file X.pdf" → red · dead server (QA7) — the run
./dist/KAIF-CORE-BUNDLE.md:5468:show-grep (I17) — a reply fixture saying "see file X.pdf" → red · dead server (QA7) — the run
./dist/KAIF-FULL.md:6010:show-grep (I17) — a reply fixture saying "see file X.pdf" → red · dead server (QA7) — the run

$ git show --stat 4809202 --name-only | grep -i "skill"
(пусто, exit 1)
```

**Кого касается.** Экземпляр класса «правило объявлено механизированным без исполнителя» уехал в
поставку (`dist/KAIF-CORE-BUNDLE.md`, `dist/KAIF-FULL.md`). Слабая сессия читает блок «гейт этой
секции» как перечень существующих красных доказательств.

**Замечено.** Круг R2, 2026-08-09.

**Состояние на 2026-08-09.** Все пять адресов на месте, наблюдаемое воспроизводится дословно после
шести сегодняшних блокеров.

## 2. Норма «у правила либо исполнитель, либо честное „механизации нет“» объявлена записанной в канон обоих слоёв, а её нет ни в одном

**Адрес:** `bugs/71_DONE_canon_rule_declared_mechanized_without_executor.md:122`.
Также (места, где нормы нет): `AGENT_GUIDE.md` · `framework/_intro.md` · `KAIF.md`.

**Заявлено.** `bugs/71:122`: «…норма „у правила либо исполнитель, либо явное „механизации нет““
записана в канон обоих слоёв». Статус документа — ФАЗА ПОЧИНОК R1 ЗАКРЫТА (коммит `4809202`).

**Наблюдалось.** Формулировка встречается только в самом баг-доке и в летописи. Ни в
`AGENT_GUIDE.md`, ни в `framework/_intro.md`, ни в `KAIF.md` её нет ни дословно, ни в варианте по
ключевым словам «механизации нет».

**Улика.**

```
$ grep -rn "либо исполнитель, либо" --include=*.md .
./bugs/71_DONE_canon_rule_declared_mechanized_without_executor.md:122
./PROJECT_HISTORY.md:44

$ grep -rn "механизации нет" AGENT_GUIDE.md framework/_intro.md KAIF.md framework/AGENT_GUIDE.md
(пусто)
```

**Кого касается.** Норма, которой должен подчиняться следующий автор канона, в каноне отсутствует;
класс закрыт заявлением того же вида, против которого писался.

**Замечено.** Круг R2, 2026-08-09.

**Состояние на 2026-08-09.** Адрес на месте, наблюдаемое воспроизводится.

## 3. Долг №3 объявлен «внесённым в STATUS.md», но в STATUS.md его нет ни на момент закрытия, ни сейчас

**Адрес:** `bugs/72_DONE_closure_caveats_and_pendings_without_fate.md:14`.
Также: `bugs/72_DONE_closure_caveats_and_pendings_without_fate.md:82` · `STATUS.md`.

**Заявлено.** `bugs/72:14`: «…её носитель назван — `reports/RELEASE_NOTES_2.2.md`, момент — сборка
нот в `/release`. Строка долга с адресом внесена в `STATUS.md`». `bugs/72:82`: «- [x] №3: route-нота
— **строкой долга с названным носителем**».

**Наблюдалось.** Строки в `STATUS.md` нет — обязательство объявлено переданным носителю, а носитель
о нём не знает. Это ровно тот класс, ради закрытия которого заведён сам `bugs/72`.

**Улика.**

```
$ grep -rn "RELEASE_NOTES_2.2" --include=*.md .
./bugs/72_DONE_closure_caveats_and_pendings_without_fate.md:14
./bugs/72_DONE_closure_caveats_and_pendings_without_fate.md:52
./bugs/72_DONE_closure_caveats_and_pendings_without_fate.md:82
./homeworks/05_review_showcase_2_2_with_eyes.md:18
./plans/59_kaif_2.2_phase_R_release.md:227
                                    ← STATUS.md среди адресов нет

$ grep -n "route" STATUS.md
(пусто)

$ git show 0a1ba09:STATUS.md | grep -n "RELEASE_NOTES_2.2"
(пусто — строки нет и на момент закрытия)
```

**Кого касается.** Долг не доедет до сборки релиз-нот: `/release` читает `STATUS.md` и шаблоны нот,
а не закрытый баг-док.

**Замечено.** Круг R2, 2026-08-09.

**Состояние на 2026-08-09.** Оба адреса в `bugs/72` на месте, наблюдаемое воспроизводится; в
`STATUS.md` (269 строк) строки долга нет.

## 4. Опровергнутое заявление «у каждого из 22 дефектов волны стоит страж или правило» живо в летописи и в домашке, которую читает владелец

**Адрес:** `homeworks/05_review_showcase_2_2_with_eyes.md:40`.
Также: `PROJECT_HISTORY.md:211` · `PROJECT_HISTORY.md:240`.

**Заявлено.** `bugs/73` объявил это заявление неверным дословно (`bugs/73:34`, цитата
`STATUS.md:41`) и поставил критерием приёмки «`STATUS.md` повторяет ровно то, что доказано»;
починка отчиталась, что `STATUS.md` переписан.

**Наблюдалось.** Формулировка перенесена в летопись коммитом `89b3de4` ДО починки и там осталась в
двух видах, а также живёт в `homeworks/05:40` — документе, который контур показывает владельцу.
Носитель сменился, заявление — нет.

**Улика.**

```
$ grep -rn "стоит страж или правило" --include=*.md .
./bugs/73_DONE_wave_Q_rows_credit_absent_guards.md:34   ← цитата дефекта
./homeworks/05_review_showcase_2_2_with_eyes.md:40:Твоя волна разобрана целиком: двадцать два дефекта, у каждого стоит страж или правило (`plans/67`).
./PROJECT_HISTORY.md:211:канон и ядро стилометрии пересмотрены; у каждого из **22 дефектов** волны стоит страж или правило.

$ sed -n '240p' PROJECT_HISTORY.md
- **Двадцать два дефекта волны, у каждого страж или правило** — таблица `plans/67` заполнена
```

**Кого касается.** Владельцу продано, что его волна закрыта механически; вхождения 5–8 ниже
показывают, что построчно это по-прежнему не так.

**Замечено.** Круг R1 (заведение `bugs/73`) и круг R2 по носителям, 2026-08-09.

**Состояние на 2026-08-09.** Все три адреса на месте, наблюдаемое воспроизводится.

## 5. Правило «кодовое имя версии БЕЗ КАВЫЧЕК» помечено механизированным, но исполнителя нет: showcase-lint зелен на обеих формах

**Адрес:** `plans/67_EPIC_kaif_2.2_Q_showcase_and_canon_revision.md:48` (строка волны №1).
Также: `AGENT_GUIDE.md:329` · `tools/showcase-lint.mjs`.

**Заявлено.** `plans/67:48`: «| 1 | Кавычки вокруг кодового имени версии | бренд | ✅ правило в
канон-таблице идентичности + греп |». `AGENT_GUIDE.md:329` требует буквально: «имя не берётся ни в
„ёлочки“, ни в кавычки любого другого вида» и называет проверку —
`grep -n "«[A-Za-z][^»]*KAIF»" README.md reports/RELEASE_NOTES_*.md`.

**Наблюдалось.** Названный греп ловит только «ёлочки» и на ASCII-кавычках молчит, то есть проверяет
уже правила, которое запрещает кавычки любого вида. Машинного стража в `showcase-lint` нет: обе
фикстуры проходят зелёными.

**Улика.** Две фикстуры, прогон 2026-08-09:

```
$ printf 'Релиз KAIF 2.2 «Yolden KAIF» опубликован.\n' > q1.md
$ node tools/showcase-lint.mjs q1.md
  ✅ нарушений правил витрины нет
✅ showcase-lint: витрина чиста по правилам волны.
EXIT=0

$ printf 'Release KAIF 2.2 "Yolden KAIF" is out.\n' > q2.md
$ node tools/showcase-lint.mjs q2.md
  ✅ нарушений правил витрины нет
EXIT=0

$ grep -n "Yolden\|ёлочк\|кавыч" tools/showcase-lint.mjs
(пусто)
```

**Кого касается.** Дефект, найденный владельцем глазами, помечен зелёной галочкой при отсутствии
исполнителя; следующая правка витрины повторит его при зелёных гейтах.

**Замечено.** Круг R2, 2026-08-09; фикстуры переисполнены при заведении документа.

**Состояние на 2026-08-09.** Все три адреса на месте, наблюдаемое воспроизводится живым прогоном.

## 6. Полнота bugs/73 опровергнута: строки #4 и #17 таблицы волны не переписаны, а «Стало» для #4 описывает правку, которой в plans/67 нет

**Адрес:** `plans/67_EPIC_kaif_2.2_Q_showcase_and_canon_revision.md:51` (строка волны №4).
Также: `plans/67:64` (строка волны №17) · `plans/67:31-32` (критерий приёмки 1) ·
`bugs/73_DONE_wave_Q_rows_credit_absent_guards.md:14` и `:16` (таблица закрытия).

**Заявлено.** `bugs/73`: колонка «страж или правило» заполнена у ВСЕХ 22 дефектов, «у каждой строки
либо фикстура, либо названный человек — третьего („страж вроде есть“) не осталось». В таблице
закрытия для #4 стоит «Стало»: «`counters-guard` ось документов сверяет строки Таблицы 1 ОБЕИХ
половин поимённо (`bugs/68`) … — две независимые оси»; для «#17/#18 блоки цитат» — «ось HALVES
считает БЛОКИ цитат … обе формы фикстурами».

**Наблюдалось.**
· `plans/67:51` стоит дословно как прежде: «✅ строка есть; счётчик ключевых документов стережёт
`counters-guard`» — текста про ось документов и «две независимые оси» в `plans/67` нет вовсе.
· `plans/67:64` — это строка «`/kaif-go` объяснён сложно», закрытая экземпляром «✅ текст
исправлен», без стража и без названного человека. Строка про блоки цитат — это №18 (`plans/67:65`),
и она действительно переписана; фикс приписал стража соседнему номеру.
· Критерий приёмки самого `plans/67` (`:31-32`) выдаёт себе ✅ по утверждению, которое `bugs/73`
опроверг.

**Улика.**

```
$ sed -n '51p;64p' plans/67_EPIC_kaif_2.2_Q_showcase_and_canon_revision.md
| 4 | Портрета голоса нет в Таблице 1 канон-документов | полнота витрины | ✅ строка есть; счётчик ключевых документов стережёт `counters-guard` |
| 17 | `/kaif-go` объяснён сложно | адресат | ✅ текст исправлен: «слеш-команда „продолжай“ — простой способ продолжить работу в текущем чате» |

$ grep -n "две независимые оси\|ось документов" plans/67_EPIC_kaif_2.2_Q_showcase_and_canon_revision.md
(пусто, exit 1)

$ git show 6c652e7:plans/67… | sed -n '44,72p'
строка #17 до и после починки совпадает байт в байт
```

**Кого касается.** Закрытие доказано ОПИСАНИЕМ правки вместо самой правки, и сам `bugs/73`
становится источником ложного кредита для того, кто будет читать его как отчёт.

**Замечено.** Круг R2, 2026-08-09.

**Состояние на 2026-08-09.** Адреса в `plans/67` на месте, наблюдаемое воспроизводится. Уточнение
адреса: в `bugs/73` строка таблицы закрытия «#17/#18 блоки цитат» стоит на `:16`, а не на `:17` —
на `:17` строка «#21 „(домашка 02)“».

## 7. Кредит стражу B8 стоит в трёх документах, а названный исполнитель — SOFT-справка, которая не валит прогон никогда

**Адрес:** `plans/67_EPIC_kaif_2.2_Q_showcase_and_canon_revision.md:53` (строка волны №6).
Также: `plans/68_anti_patterns_from_owner_review.md:58` · `AUTHOR_STYLOMETRY.md:3286` ·
`tools/voice-lint.mjs:189`.

**Заявлено.** `plans/67:53`: «| 6 | Описание от противоречия: «а не прошлым тегом» | язык | ✅
`voice-lint` B8 знает три формы, порог «редко»; АП28 |». `plans/68:58` и `AUTHOR_STYLOMETRY.md:3286`
несут одну и ту же строку: «**Механизировано:** `tools/voice-lint.mjs`, пункт B8 — три формы, порог
«редко»».

**Наблюдалось.** `tools/voice-lint.mjs:189` регистрирует пункт уровнем SOFT:
`add(SOFT, 'B8', hits.length <= Math.max(1, Math.round(total / 1000)), …)`. Уровень SOFT в
exit-код не входит — строки 203, 234, 242, 251 считают только HARD. Оговорки «консультирует, не
блокирует» нет ни в одной из трёх строк кредита. Коммит фикса тронул только `plans/67`.

**Улика.** Прогон 2026-08-09, фикстура — дословная форма дефекта владельца:

```
$ printf 'Версия определяется свежим тегом, а не прошлым тегом.\n' > b8.md
$ node tools/voice-lint.mjs b8.md
  ✅ B8: противопоставлений — 1 на 8 слов (порог «редко»: 1); …
✅ voice-lint: жёстких нарушений нет.
EXIT=0

$ (та же фраза ×10) > b8x10.md ; node tools/voice-lint.mjs b8x10.md
  ⚠️  B8: противопоставлений — 10 на 80 слов (порог «редко»: 1); …
EXIT=0
```

**Кого касается.** Читатель канона антипаттернов и ядра стилометрии видит «Механизировано» и
перестаёт проверять глазами то, что машина не валит ни в одном прогоне.

**Замечено.** Круг R2, 2026-08-09; фикстуры переисполнены при заведении документа.

**Состояние на 2026-08-09.** Все четыре адреса на месте, наблюдаемое воспроизводится живым прогоном.

## 8. Ось HALVES судит СИММЕТРИЮ подписей, а дефект владельца был в регистре и смысле — испорченная одинаково подпись проходит зелёной

**Адрес:** `plans/67_EPIC_kaif_2.2_Q_showcase_and_canon_revision.md:55` (строка волны №8).
Также: `tools/showcase-lint.mjs`.

**Заявлено.** `plans/67:55`: «| 8 | Название таблицы со строчной буквы и не по смыслу | оформление |
✅ `showcase-lint` ось HALVES считает ПОДПИСИ таблиц («Table N —» / «Таблица N —») наравне с
подразделами: пропажа подписи из одной половины краснеет, доказано фикстурой |».

**Наблюдалось.** Исходный дефект владельца — подпись со строчной буквы и не по смыслу. Ось судит
только симметрию: подпись, испорченная ОДИНАКОВО в обеих половинах, даёт exit=0. Человек-исполнитель
для этой половины класса в строке не назван.

**Улика.** Мутация копии `README.md` в ОБЕИХ половинах, прогон 2026-08-09:

```
$ sed -i 's/^Table 6 — Metrics of KAIF 2.2$/table 6 — numbers/; \
          s/^Таблица 6 — Метрики версии KAIF 2.2$/таблица 6 — цифры/' README_mut.md
$ node tools/showcase-lint.mjs README_mut.md
  ✅ нарушений правил витрины нет
EXIT=0

контроль (ось на этой копии работает — асимметрия краснеет):
$ sed -i '1039d' README_asym.md   # убрана только русская подпись
$ node tools/showcase-lint.mjs README_asym.md
  ❌ HALVES — половины витрины разошлись по скелету (1)
      …подписей таблиц («Table N —» / «Таблица N —»): EN 6 ≠ RU 5…
EXIT=1
```

**Кого касается.** Строка кредитует стража за прокси (симметрию) вместо названного свойства
(регистр и смысл подписи); следующая правка витрины повторит исходный дефект при зелёных гейтах.

**Замечено.** Круг R2, 2026-08-09; мутация и контроль переисполнены при заведении документа.

**Состояние на 2026-08-09.** Адрес на месте, наблюдаемое воспроизводится живым прогоном.

## 9. bugs/78 закрыт прозой: пять `[x]`, ни одной метки наблюдения, единственный процитированный прогон — ДОФИКСОВЫЙ

**Адрес:** `bugs/78_DONE_stamp_guard_checks_form_not_truth.md` (шапка `:3`, Fix plan `:66`, `:71`,
`:79`, `:83`, `:84`; заявления без прогона — `:82` и `:88`).

**Заявлено.** Шапка: «✅ DONE (2026-08-09 12:20 +03:00) — все четыре пункта плана исполнены».
Документ объявляет класс «заявление и его проверка расходятся» закрытым.

**Наблюдалось.** Пять пунктов помечены `[x]`, при этом ни одной метки наблюдения формы
`TESTED: …` в документе нет — греп по ней пуст. «Наблюдены все три режима» (`:82`) и «58 меток в 29
файлах» (`:88`) стоят голыми утверждениями; единственный процитированный прогон — «первый же
прогон», то есть ДОФИКСОВЫЙ. По `TESTING_FRAMEWORK.md` такой документ сырой. Наблюдение, если оно
было, живёт в `--selftest` инструмента, а не в баг-доке.

**Улика.**

```
$ grep -c "^- \[x\]" bugs/78_DONE_stamp_guard_checks_form_not_truth.md
5

$ grep -n "\[TESTED:" bugs/78_DONE_stamp_guard_checks_form_not_truth.md
(пусто, exit 1)
```

Круг R2 переисполнил ядро фикса отдельно, и оно работает (краснеет на подсунутой метке, молчит на
чистой копии) — то есть наблюдаемое расхождение здесь в ДОКАЗАТЕЛЬСТВЕ закрытия, а не в самом
страже.

**Кого касается.** Документ, чинящий класс «заявление и его проверка расходятся», сам закрыт
заявлением без проверки; верификатор этого не отметил.

**Замечено.** Круг R2, 2026-08-09.

**Состояние на 2026-08-09.** Файл на месте под именем
`bugs/78_DONE_stamp_guard_checks_form_not_truth.md`, наблюдаемое воспроизводится.

## Триаж 2.3 (фаза S, 2026-08-14)

> Вердикты двухступенчатого триажа (механика на HEAD → скептик, дефолт REFUTED; сводная таблица — `reports/KAIF_AUDIT/2026-08-14_r2_triage_SUMMARY.md`).

| № | Вердикт | Тяжесть | Эпик | Улика триажа |
|---|---|---|---|---|
| 1 | CONFIRMED | substantial | T | bugs/71:111-112 заявляет фикстуру снятой, но она стоит в .claude/skills/owner-reviews/SKILL.md:492, framework/skills/owner-reviews/SKILL.md:499, dist/KAIF-FULL.md:6010, dist/KAIF-CORE-BUNDLE.md:5476; git log 2b2cadc..HEAD по обоим SKILL.md пуст, коммит фикса 4809202 не тронул ни одного файла под skills/. DONE-док описывает правку, которой не было, и противоречие уехало в поставку. |
| 2 | CONFIRMED | substantial | T | bugs/71:122-123 заявляет норму записанной в канон обоих слоёв; греп по «механизации нет», «либо исполнитель», «без исполнителя», «executor», «not mechanized» в AGENT_GUIDE.md, framework/_intro.md, framework/AGENT_GUIDE.md, KAIF.md находит только частный переписанный экземпляр I15-I17 (AGENT_GUIDE.md:781-788, framework/AGENT_GUIDE.md:714-717) — общей нормы для будущих авторов канона нет ни в одной формулировке. |
| 3 | CONFIRMED | substantial | T | bugs/72:14 «Строка долга с адресом внесена в STATUS.md» и :82 стоят дословно; в текущем STATUS.md греп RELEASE_NOTES/route/долг даёт только правило трёх мест (:30-32) и чужие долги (:84-90, :155, :167) — строки route-ноты нет, не было её и в 0a1ba09 на момент закрытия; STATUS перестраивался после R2 (88282d5) и долг не восстановили — до /release он не доедет. |
| 4 | CONFIRMED | substantial | T | homeworks/05:40 дословно несёт «двадцать два дефекта, у каждого стоит страж или правило» — документ, который контур показывает владельцу; PROJECT_HISTORY.md:339 и :368 несут то же после перестройки летописи 88282d5. Заявление опровергнуто bugs/73 и остаётся ложным сегодня: plans/67:64 (строка #17) закрыта «текст исправлен» без стража, правила и названного человека. |
| 5 | REFUTED | hygiene | - | Заголовок «исполнителя нет» неверен: строка plans/67:48 кредитует «правило в канон-таблице + греп», и оба существуют — AGENT_GUIDE.md:329 несёт правило и именованную проверку, которая краснеет на ДОСЛОВНОЙ форме дефекта владельца («ёлочки» вокруг кодового имени) — ровно критерий bugs/73:28-29. showcase-lint строкой никогда не кредитовался; зелёная ASCII-фикстура — мутация за пределами закреплённой формы, покрытая текстовым правилом (критерий волны — «страж ИЛИ правило»). |
| 6 | CONFIRMED | substantial | T | bugs/73:105 заявляет шесть строк переписанными, :22 — «третьего не осталось»; проход правки доказуемо был (plans/67:49,55,65,68 несут пометки «Прежняя редакция…»), но строку #4 пропустил — plans/67:51 стоит в старой редакции, текста «две независимые оси» из bugs/73:14 в plans/67 нет вовсе (греп пуст), а #17 перепутан с #18: блоки цитат переписаны в :65 (#18), тогда как :64 (#17, /kaif-go) остался «✅ текст исправлен» — ни фикстуры, ни названного человека; git log 2b2cadc..HEAD -- plans/67 пуст. |
| 7 | CONFIRMED | hygiene | T | tools/voice-lint.mjs:189 регистрирует B8 уровнем SOFT, report() (строки 200-206) считает в exit-код только HARD — прогон с 10-кратным превышением порога «редко» даёт EXIT=0. Три носителя кредита (plans/67:53, plans/68:58, AUTHOR_STYLOMETRY.md:3286) пишут «Механизировано … порог „редко“» без оговорки «консультирует, не блокирует» — вопреки собственной конвенции таблицы «сказано вслух» (строки #2 и #10 plans/67 границу машины называют явно). SOFT-дизайн намеренный (комментарий :174-175), поэтому дефект — умолчание в кредите, не инструмент: hygiene. |
| 8 | REFUTED | hygiene | - | Строка plans/67:55 честно называет ровно то, что покрыто: «пропажа подписи из одной половины краснеет, доказано фикстурой» — ложного заявления в ней нет. Регулярка showcase-lint.mjs:401 /^(?:Table·Таблица)\s+\d+\s+—/ не матчит строчную «таблица», поэтому ДОСЛОВНАЯ историческая форма (испорчена подпись одной половины) даёт асимметрию счёта и красный — что подтверждает контроль самого вхождения (EN 6 ≠ RU 5, EXIT=1). Зелёный прогон вхождения требует испортить ОБЕ половины одинаково — гипотетическая мутация, которой владелец не производил и о которой строка ничего не заявляет. |
| 9 | REFUTED | hygiene | - | Ключевое прочтение неверно: «первый же прогон» (bugs/78:88) — это первый прогон НОВОЙ оси правдивости, по построению ПОСЛЕфиксовый, и он процитирован с уликами (58 меток, коммит 8e155ec, метка 11:40 против 11:29, исправление по git blame); дофиксовый прогон — отдельно в Repro (:31, findings 0). «Наблюдены все три режима» несёт метку наблюдения там, где её кладёт конвенция дома — tools/refresh-stamp.mjs:16 «[TESTED: 2026-08-09 · все три режима наблюдены живьём …]». Внутри-доковых [TESTED:] нет ни в одном закрытом баг-доке репозитория (проверены bugs/71, bugs/76) — их отсутствие в bugs/78 конвенция, а не сырость; ядро фикса R2 переисполнил и подтвердил. |
