# Идея 22 — hook-конфиги освежения для остальных агентских систем, не только Claude Code

> **Родитель:** слово владельца на развилке подключения фазы O4, 2026-08-07 (~20:50 +03:00),
> дословно: «это механизм проекта! Поэтому он подключается в settings.json для Claude, и в
> аналогичные хуки для другиех вгентских систем».
> **Создан:** 2026-08-07. **Вовне:** — (скоуп назначен владельцем; дальше идея живёт планом фазы O5).
> **Статус:** ✅ СКОУП НАЗНАЧЕН 2026-08-07 22:05 +03:00 — **в 2.2, фазой O5** (интервью №010 Q1 =
> **C**, решение №50): узко — только системы с ЖИВЫМ контрактом. Claude Code уже поставлен (O3) и
> подключён (O4); реализация остальных — операционный план фазы O5.

## Решение владельца о скоупе (интервью №010 Q1, 2026-08-07 22:05 +03:00)

Выбран вариант **C** — «в 2.2 фазой O5, но узко», с дополнением владельца дословно:

> и ещё Grok Build нужно добавить в набор наших систем. И посмотреть, есть ли у Meta своя ИИ
> агентская система и у Google есть ли своя агентская система, и есть ли у них эти механизмы.
> Если есть - делаем и под них. Делаем, если поддерживают.

Дополнение ПЕРЕОПРЕДЕЛЯЕТ «узко»: критерий отбора — не короткий список имён (в варианте C стояли
Cursor и Codex), а **наличие подтверждённого живым fetch хук-контракта**. Набор кандидатов растёт
до девяти:

| Система | Что известно на 2026-08-07 | Что делает O5 |
|---|---|---|
| Claude Code | референс, контракт снят живым fetch, конфиг подключён | уже есть — образец-эталон |
| Cursor | `.cursor/hooks.json`: `sessionStart`, `beforeSubmitPrompt`, `preCompact`, `stop` (первичный источник) | живой fetch → образец |
| OpenAI Codex | `.codex/hooks.json`: `SessionStart`, `UserPromptSubmit`, `Stop`, `PreCompact`/`PostCompact` (первичный) | живой fetch → образец |
| **Grok Build** | адаптер поставки есть; заявляет чтение раскладки `.claude/` ЦЕЛИКОМ, включая hooks — паритет правдоподобен, но НЕ ВЕРИФИЦИРОВАН (сверка 2026-07-16, `researches/19` §1.2) | живой fetch; подтвердится паритет — образец = референс-конфиг как есть |
| GitHub Copilot | `userPromptSubmitted`, `preToolUse`, `postToolUse`; аналог `sessionStart` НЕ подтверждён | живой fetch; нет `sessionStart` — честная строка «частичный контур» |
| Windsurf | pre/post-action, вторичный источник | живой fetch решает |
| Cline | lifecycle-хуки, вторичный источник | живой fetch решает |
| **Meta** | своя агентская система — существование не проверялось | разведка живым источником: есть ли система, есть ли хуки |
| **Google** | то же (Gemini CLI / Jules / Antigravity — кандидаты, не проверены) | разведка живым источником |
| Zoo Code | хуков нет (приоритет №1 по адаптерам) | остаётся markdown-фолбэк, и это не красный |

Правило отбора не смягчается дополнением, а становится главным: **система без живого fetch
контракта в день письма образца в модуль не едет** — она остаётся строкой «не верифицирована» в
README модуля. «Делаем, если поддерживают» — это ровно оно, словами владельца.

## Вектор цели (боль + как проверим)

Боль: модуль refresh-hooks поставляет конфиг ровно под ОДНУ агентскую систему (Claude Code),
хотя `researches/19` §1.2 живым обзором показал хуки у Cursor, Codex, Copilot, Windsurf и Cline
— проект на любой из них получает только markdown-ритуал, хотя механический контур там
технически доступен. Хотим (Achieve): у каждой агентской системы с подтверждённым хук-контрактом
есть свой образец конфига в модуле, а сами скрипты остаются одни на всех.

Проверим (при реализации): для каждой системы N — файл-образец в `framework/hooks/` (по
конвенции самой системы: `.cursor/hooks.json`, `.codex/hooks.json`, …) с датой снятия живого
контракта в шапке; свод полигона проверяет, что каждый образец — валидный JSON и адресует
существующие скрипты модуля; README модуля перечисляет системы строкой на систему с честной
пометкой «контракт верифицирован живой докой <дата>» либо «не верифицирован — образец-гипотеза»;
развёртывание на системе без хуков по-прежнему не краснеет.

## Суть

Скрипты модуля (`session-start-refresh` · `prompt-refresh-timer` · `stop-status-guard`) —
обычные Node-программы: читают JSON на stdin, печатают JSON на stdout. Системо-зависима ТОЛЬКО
проводка: имя файла конфига, имена событий и форма выходного поля. Значит цена системы —
один образец конфига плюс, возможно, тонкий адаптер формы вывода, а не второй комплект логики.

Стартовая база уже собрана в `researches/19` §1.2 (снято 2026-08-07): Cursor `.cursor/hooks.json`
(`sessionStart`, `beforeSubmitPrompt`, `preCompact`, `stop`) · Codex `.codex/hooks.json`
(`SessionStart`, `UserPromptSubmit`, `Stop`, `PreCompact`/`PostCompact`) · Copilot agent hooks
(`userPromptSubmitted`, `preToolUse`, `postToolUse`; аналога `sessionStart` не подтверждено) ·
Windsurf pre/post-action (вторичный источник) · Cline lifecycle-хуки (вторичный источник) ·
Zoo Code — хуков нет, честный фолбэк на markdown-ритуал.

## Граница и цена

Заявленный порядок O1 (`researches/19` §1.2) — «Claude Code (референс) → остальные по мере
верификации их контрактов ЖИВОЙ докой; кандидаты пула, не скоуп 2.2». Цена системы — один
живой fetch её доки + образец + строка свода; риск — образец, написанный по вторичному
источнику, выглядит поставкой, а работает гипотезой (класс EXP-0036: карта старше дня — уже
гипотеза). Поэтому правило реализации: **система без живого fetch контракта в день письма
образца в модуль не едет** — она остаётся строкой «не верифицирована» в README.

## Связи

- `researches/19` §1.1–1.2 — карта контрактов (Claude Code — полный референс).
- `framework/hooks/README.md` §«Other agent systems» — уже честно называет проблему и
  отправляет авторить конфиг от образца против живой доки системы.
- `framework/adapters/*` — снимки систем 2026-07-03/07-16 хук-волну ещё не видят; ресинк
  адаптеров — родственная работа того же захода.
- `plans/31` (эпик O) · `plans/58` (фаза O4, где прозвучало слово владельца).

## Решения, принятые агентом без владельца

1. DONE-тег проставлен ревизией W5 (сессия 41, 2026-08-21): скоуп идеи исполнен фазой O5
   (закрыта 2026-08-07 21:47 +03:00, судья эпика O — VERIFIED; `plans/60`): 10 систем сняты
   живыми вендорскими доками, 4 образца хук-конфигов (`--emit`), Grok Build — паритет без
   образца, Windsurf/Cline — честно «не поддержано».
2. Последний висевший хвост — Meta Muse Code «ждёт доку» — закрыт наблюдением (живая проба ≈ 2026-08-21 10:55 +03:00, сессия 40):
   вендорская дока ПОЯВИЛАСЬ (`researches/19` §1.3), впрыска контекста в контракте НЕТ —
   образец hooks не пишется по построению, не по лени. Отрицательный ответ — тоже исход.
3. Долг «деплой НАВЫКОВ в Antigravity» (`deployAgentSystems`) — не скоуп этой идеи (идея — о
   hook-конфигах освежения); он остаётся строкой автономного пула STATUS со своим адресом.

## ✅ STATUS: DONE (2026-08-21 19:19 +03:00)

What was done: скоуп «hook-конфиги под остальные агентские системы» исполнен фазой O5 по
решению №50 (узко, по живому контракту): карта контрактов — `researches/19`; образцы — в
модуле `framework/hooks/`; свод s14 +21 ассерт с красным по мутации.
How verified: судья эпика O — VERIFIED 7/7 (`plans/58`); закрытие O5 — `plans/60`; Muse Code
закрыт наблюдением живой доки (проба ≈ 2026-08-21 10:55 +03:00; `researches/19` §1.3 и таблица адресов).
