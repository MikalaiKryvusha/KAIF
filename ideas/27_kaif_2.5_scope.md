# Идея 27 — KAIF 2.5: скоуп версии (отложенное из 2.4)

> **Создан:** 2026-08-28 00:38 +03:00 (по слову владельца в `ideas/26`: «Остальное, что кажется
> тяжёлым и приносящим мало ценности — переносим на скоуп версии 2.5»).
> **Родитель:** `ideas/26` (реестр 2.4) + `plans/78` § «Парето-разрез» (исполнение разреза).
> **Статус:** 🧊 ОТЛОЖЕНО — реестр наполняется переносами из 2.4; работы не ведутся до открытия
> версии владельцем. Веха: 2026-08-28 15:20 +03:00 — полевые отчёты 2.4 (NDim + KAGO, четыре
> отчёта + пять тикетов) забраны и разложены в три эпика FT/MD/US (§ «Эпики из полевых отчётов
> 2.4»; синтез — `researches/23`). Веха: 2026-08-28 16:29 +03:00 — третья порция (Prompt Modding,
> issues #28/#27: легаси-прыжок 1.6→2.4) разложена — эпик US расширен двумя HIGH (P1
> детерминизм wholesale-вердикта · P2 якорные блоки). Веха: 2026-08-28 16:5x +03:00 — обещанный
> тикет пришёл (issue #29), оформлен эпиком CI «Команда поставляется с CI» (п. 11); проверка
> полноты issues: #24-коммент = известный +1, KUMM #17 обработан циклом 2.3 (`plans/77`) —
> хвостов нет. Скоуп-реестр 2.5 ПОЛОН на 2026-08-28: §1–§7 переносы + четыре полевых эпика
> FT/MD/US/CI. Веха: 2026-09-04 00:09 +03:00 — четвёртая порция поля (issues #30–#38, три
> 🔴 TOP владельца) вписана (§ «Четвёртая порция поля»); разведка Парето-разреза —
> `researches/24`; развилки разреза, ответов в issues и имени — `interviews/interview_020`
> (ждёт владельца). Версия по-прежнему НЕ открыта.
> **Вовне:** зонтичный план 2.5 — когда владелец откроет версию (ответ интервью №020 Q1).

## Вектор цели (Maintain)

Держать в одном месте всё, что Парето-разрезом 2.4 (`plans/78`) отложено на будущее, — с адресами
готовых планов, чтобы открытие 2.5 началось с нулёвок актуализации, а не с археологии.

## Готово, когда (критерии приёмки)

1. Каждый пункт `ideas/26`, не взятый в 2.4, имеет здесь строку с адресом плана/бага — греп по
   `ideas/26` §1–§7 не находит пункта без адреса здесь.
2. Ни один пункт не начат в реализации до открытия 2.5 владельцем.

---

## Унаследовано из `ideas/26` (2.3 → 2.4 → 2.5), адреса планов сохранены

1. **Эпик T «Лёгкие леса»** — целиком (`plans/72`): движок «правила-данные» для стражей формы
   (№73), честные закрытия, замкнутая доказательная петля, 59 CONFIRMED T-вхождений триажа R2 —
   семейства `bugs/84`–`91`, 95, 96, 100, 103. Нулёвка актуализации замера «до» — на открытии.
2. **Ревью `/code-revision` самим Fable** (`plans/63`) — роль «читаю как будущий исполнитель»,
   правка только с дословной цитатой; оба слоя + пересборка.
3. **Эпик V «Порядок в ящике»** (`plans/74`; постановка — `plans/64`) — ревизия всех модулей
   поставки и 35 навыков, снятие удвоения payload/обвязка, страж порядка. Ultracode-точка (№58).
4. **Эпик P «Переименование навыков в `kaif-*`»** (`plans/32` + `plans/40`; №35/№47) — после V.
5. **Остаток эпика U «Поле»** (`plans/73`, фазы U2–U4, полигон-покрытие дыр `researches/20` §2.3,
   R2-вхождения семейств 93/94/97) — ЗА ВЫЧЕТОМ четырёх фиксов, взятых в 2.4 эпиком FP
   (`plans/78`, критерий 5: CRLF при merge · шаг 4 `/report-bug` · маркер stale-claims ·
   сигнатуры модулей в merge-строках).
6. **Продолжение эпика X — конверсия канона «проза → команды»** (issue #22): `PHILOSOPHY` ·
   `BUG_FIXING_FRAMEWORK` · `AGENT_GUIDE` · README директорий + линтер `canon-form-check`.
7. **Расширение охвата селфтестов** — каверза 3 суда W1 (ADDR-охваты четырёх носителей:
   `experience-lint` · `lang-packs-guard` · `stylometry-snapshot` · `s02/S9-crash`) + остаток
   98.3 (класс «маркер с протухшим/ложным обоснованием» без стража) + находки суда RL 2.4
   (панель 1/2, реестр `reports/KAIF_AUDIT/2026-08-28_rl_court_registry.md`): однострочник
   реестра пар сверяет только BEGIN-маркеры молитвенных блоков — END-класс не стережёт никто
   (S1) · `writeMatchingEol` судит по НАЛИЧИЮ CRLF, не по доминированию — смешанный EOL-файл
   конвертируется целиком, фикстура кроет только гомогенный полевой класс (H10).

## Эпики из полевых отчётов 2.4 — NDim + KAGO (синтез: `researches/23`)

Первые ЖИВЫЕ входы скоупа (не переносы): ПЯТЬ полевых отчётов 2026-08-28 — обновление 2.3→2.4
двумя маршрутами (NDim, KAGO) + два прогона `/team-deployment` (сверка живой команды NDim,
greenfield KAGO) + обновление-прыжок 1.6→2.4 легаси-маршрутом (Prompt Modding, issue #28: третий
проект, macOS arm64, сфера prompt-engineering) — и шесть тикетов (пять `bugs/KAIF/` проектов +
origin issue #27). Отчёты забраны в `reports/KAIF_UPDATES/` истока; полный реестр сигналов с
дословными источниками — `researches/23` §2/§2а.

8. **Эпик FT «Команда в поле»** — полевые фиксы `/team-deployment` по первой же паре прогонов:
   adopt-путь для живой команды (инвентарь → трёхведёрная дельта → решение владельца → применение
   без перезаписи; операция 3 сейчас — ловушка перезаписи) + правило «двух слов владельца» на
   один параметр (NDim `bugs/KAIF/02`) · исключение менеджера в нейминг-инварианте §1 текстом,
   не примером (KAGO `bugs/KAIF/11`) · архетип `hardware-lab-small`: физический синглтон под
   тестом как ось размера, донор — §0/`gpu-card` KAGO (KAGO `bugs/KAIF/12`) · замки́ в
   стоп-ритуале §9 — lock и строка «жду QA» пережили пять дней после смены (NDim TD §4.2) ·
   триггер ретроспективы «дремота с несмерженными ветками» · формулировка «first dispatch → at
   first launch» · три better-than-canon практики NDim в шаблоны (граница push-делегирования ·
   ёмкость место≠слот · троттлинг повторной отправки).
9. **Эпик MD «Доставка сигналов машинерией»** — команда `kaif-core report` (или аналог): полевой
   факт двух версий (NDim `bugs/KAIF/01` + наблюдение «reproduced on v2.4») — классификатор
   агентской системы блокирует модуль доставки report-bug по ПРЕДМЕТУ, переформулировка 2.4
   необходима, но недостаточна; выживший фикс — доставка в машинерию, навык говорит только
   «запусти». Приёмка задана полем: перенос модуля 16/16 без divergence note.
10. **Эпик US «Симметрии обновления»** — машинерия обновления, два HIGH из третьей порции +
    точечные фиксы: **детерминизм вердикта translated-wholesale** — два прогона по
    байт-идентичному дереву дали разные вердикты, live дописал 416 английских строк в русский
    гайд; печатать решение по файлу с числами (issue #27, P1 — тяжелейший пункт) · **якорные
    блоки как неделимые единицы** — класс двух шкур: приезд ВНУТРЬ локализованной пары маркеров
    (KAGO R2) и `KAIF:PRAYER:END` без `BEGIN` и без creed вовсе (issue #27, P2) · общий
    quote-skip предикат генератора задания и гейта placeholders (KAGO `bugs/KAIF/10`,
    «инструкция шире гейта») · deprecation-пункт называет преемника для локальных правок
    (NDim R3) · сводка считает неубранные deprecation'ы · честная строка о языке новых файлов
    («16 новых навыков приезжают английскими», P3) · ru-тела семейства церемоний закрытия в
    пакете (легально: ru ведётся, №56) · инвалидация навыков, чей текст кодирует старый режим,
    при anonymous→origin (P4) · предупреждение `project-name` ДО принятия значения (P5) ·
    `< /dev/null` в smoke-строке README hooks (P6).

11. **Эпик CI «Команда поставляется с CI»** — origin issue #29 (NDim, 2026-08-28; тикет,
    обещанный владельцем: «там будет ещё одна огромная дыра - ты не поставляешь с версией 2.4
    правила GitHub Workflows»; приказ владельца дословно в тикете: «Должен поставлять вместе с
    деплойментом команды»). Операция 3 `/team-deployment` дополнительно материализует
    минимальный CI-workflow (`.github/workflows/team-ci.yml`), адаптированный под команды
    самого проекта (читать из `package.json`/канона сборки, не хардкодить): дешёвые гейты на
    push/PR ролевых веток. Обоснование поля: команда умножает пишущих — «main всегда зелёный»
    держится машинным сервер-чеком (DORA/trunk-based), а не дисциплиной каждого агента; QA
    жжёт сессию на то, что сервер делает бесплатно. Констрейнты из тикета: только дешёвые гейты
    (без секретов/эмуляторов/живого стенда — тяжёлое остаётся локально за замком стенда) ·
    красный CI ролевой ветки блокирует merge как отсутствующий вердикт QA (строка в
    git-процессе Конституции) · не-GitHub remote получает тот же job документированным
    локальным pre-push скриптом — деградация изящная, не молчаливая.

Подтверждения поля, работы не требующие (репетиция-предикт 3×, EOL-фикс на CRLF-дереве,
борд-контракт 6/6, owner-gate дословно, легаси-прыжок 1.6→2.4 с паритетом владельца) —
зафиксированы в `researches/23` §2/§2а (U7/T9/P7).

## Четвёртая порция поля — issues #30–#38 (2026-08-28 … 2026-08-31)

Девять тикетов пришли ПОСЛЕ закрытия реестра 28 августа (KAGO ×6, NDim ×2, QA_Engineer ×2 —
один из них полевой отчёт). Каждое заявление сверено с HEAD поставки грепом — таблица
`researches/24` §2 (восемь CONFIRMED, #38 частично протух). Адрес — эпик предлагаемого разреза
`researches/24` §4; работы не ведутся до открытия версии.

12. **#37 🔴 TOP** — бланковое правило AUTH (`AGENT_GUIDE` → гейт авторизации) глотает исключение
    `/report-bug` шага 4: тикеты о дефектах KAIF висят «NOT YET» бессрочно; слово владельца в
    тикете дословно: «АГЕНТ ВИДИТ БАГ В КАИФ — НЕМЕДЛЕННО ИДЁТ ЗАВОДИТЬ И ОТПРАВЛЯТЬ ЕГО В
    ОРИГИН!» → **CN** (исключение inline в обоих слоях; состояние «NOT YET» законно только для
    anonymous) + **SG** (доставка машинерией делает исключение исполнимым).
13. **#36 🔴 TOP** — развилка — не собственность агента: три двери про факт, `/interview` про
    видение, инженерная развилка с ценой ошибки отдана агенту молча; слово владельца: «НЕЛЬЗЯ
    ДОВЕРЯТЬ принятие решений на развилках ИИ модели и ИИ агенту» → **CN** (четвёртая дверь ·
    определение развилки · артефакт `FORK:` · охота судьи; хук — 2.6, когда появится
    механический признак).
14. **#35 🔴 TOP** — гейт 5 доказывает стража против ФИКСТУРЫ, не против УГРОЗЫ (четыре
    экземпляра за вечер, один стоил владельцу машины; «это КАТАСТРОФА») → **CN** (вторая
    половина гейта 5 · блок THREAT/PROVED-AGAINST/GAP/ON-REAL-PATH · DONE стража через реальный
    путь · долговечность улики; линтер — опциональный модуль по донору KAGO `guard-lint.mjs`).
15. **#30** — `/guarded-loop` с названной длительностью закрылся на 25 минут раньше: правило 2.4
    SC живёт прозой без механического хранителя → **CN** (`BOUNDARY:` перед закрытием · `until`
    машиночитаемо · охота «early-finish»).
16. **#31** — пункт `stale-claims` задания исчезает между двумя прогонами по почти идентичному
    дереву; version-lying строки уезжают молча → **US** (безусловный пункт с телом
    `no lines found` + поиск причины односторонней потери).
17. **#32** — полевой отчёт QA_Engineer 2.2→2.4 (модульный маршрут, macOS; забран в
    `reports/KAIF_UPDATES/QA_ENGINEER_KAIF_2.4_UPDATE_REPORT.md`): R-B — #27 на модульном
    маршруте НЕ воспроизвёлся (вход для P1) · R-C 18 из 45 навыков английские при `lang: ru` ·
    пожелания 2–5 → **US** (преемник в deprecation · счёт языковой смеси в `check` · диффы с
    baseline по умолчанию · пин старой версии в `tools/` проекта).
18. **#33** — борд команды: два состояния и колонка «Waiting for», к которой никто не обязан;
    три слова владельца NDim дословно (оффлайн-красный · в работе-синий · «ждунам нужно
    помогать») → **TM** (четыре РОЛИ состояний + пункт контракта 7 `waiting` с exit-кодом;
    референс NDim `auditWaiting`).
19. **#34** — борд как TRACKED-файл делает дерево main грязным по построению (105 коммитов за
    10 дней, 14 — только ради чистоты) → **TM** (борд — состояние сессии вне git, ignore-first;
    снимок в ретроспективу смены; названный опт-аут; прецедент `.kaif/refresh-marker.json`).
20. **#38** — голос контура предписан как тембр, не как разборчивость на языке владельца (слово
    владельца: «голос неверный в интерактивном контуре»); тембровая половина закрыта 2.4 (I35),
    дыра «культура голоса = язык проекта» жива → **CN** (три строки в `/owner-reviews`).

## Разрез 2.5 (решение владельца)

Предложение агента — `researches/24` §4: **CN → SG → TM → US → RL** ≈ 8–10 чатов; за бортом
(реестр 2.6 `ideas/28`) — T · V · P · X-полный · селфтесты · U3/U4 · TD-фазы. **Ждёт ответа
интервью №020 Q1**; до ответа версия не открыта, планы не актуализируются.

## Кандидаты 2.5 из эпика Team Deployment (`ideas/KAIF_2.4_TEAM_DEPLOYMENT_EPIC.md` §42)

Расширения, которые сам эпик-док относит к 2.5+: дополнительные архетипы команд (research, game,
data/ML) · профили способностей моделей · cost-aware routing · сильная автоматическая
реконфигурация · дашборды здоровья команды.

**Отложено Парето-разрезом TD** (зафиксировано судом RL 2026-08-28 10:27 +03:00 — находка S3: обещание
«фиксируется на закрытии эпика TD» провисело незакрытым до суда; разрез — `researches/21` §5,
методологическая база в 2.4 — навык `team-deployment`):

- Phase 1 — машиночитаемые схемы TeamSpec / RoleContract (YAML; справочник §13/§16);
- Phase 5 — генерация `TEAM_SPEC.yaml` из одобренного дизайна (в 2.4 генерируется только
  человекочитаемая конституция);
- Phase 7 — граф-машинерия зависимого диспатча (волны задач; в 2.4 — правилами Менеджера);
- Phase 10 — автоматика `TEAM_RETROSPECTIVE`/`TEAM_EXPERIENCE` (в 2.4 — лёгкая форма вопросов
  ретроспективы в навыке).

## Пункты, ждущие наполнения (перенесены из `ideas/26`)

Кандидаты из автономного пула `STATUS.md` — переезжают в скоуп только словом владельца:
tamper-тест sha256-гейта загрузчика · хвосты интерактивного контура (интервью №008 Q3) · адаптеры
Devin/OpenCode/Aider/Junie · token-F1 ступень `kaif-provenance` · идея fork-bundle · библиотеки
терминов остальных сфер · машинная половина ставки витрины (`bugs/73` «в»).

## Решения, принятые агентом без владельца

1. Форма — краткий реестр по образцу `ideas/26` (форма задана владельцем там же).
2. Готовые планы не переписываются — наследуются с нулёвкой актуализации на открытии (прецедент №76).

## Links

`ideas/26` · `plans/78` (Парето-разрез) · `plans/72`/`63`/`74`/`64`/`32`/`40`/`73` · issue #22 ·
`reports/KAIF_AUDIT/2026-08-14_r2_triage_SUMMARY.md`.
