# Идея 28 — KAIF 2.6: скоуп версии (отложенное Парето-разрезом 2.5)

> **Создан:** 2026-09-04 00:27 +03:00 (по решению владельца №83 — интервью №020 Q1 = A: разрез
> `researches/24` §4 принят, «за бортом» едет сюда).
> **Родитель:** `ideas/27` (реестр 2.5) + `researches/24` §5 (что уходит в 2.6) + `plans/82`
> § «Парето-разрез».
> **Статус:** 🟢 **ВЕРСИЯ ОТКРЫТА 2026-09-05 14:38 +03:00** — интервью №023 Q1 = B (№95):
> разрез `researches/26` §4 принят с дополнением (стретч CR — ревью `/code-revision` Фейблом);
> рабочее имя — Mindful KAIF (Q4 = B, №96); мета-план — `plans/89`; отложенное в 2.7 —
> `ideas/29`. Веха: 2026-09-05 14:02 +03:00 — полевые сигналы 2.5 (issues #40–#48, четыре
> проекта за сутки после релиза) вписаны (§ «Полевые сигналы 2.5»). Два слова владельца тем же
> интервью стали правилами видения (№97 механика отгружается только полной · №98 вопрос
> владельцу — сценарием); Q2/Q3 переспрошены раундом 2.
> **Вовне:** зонтичный план 2.6 — `plans/89` ✅; реестр 2.7 — `ideas/29` ✅.

## Вектор цели (Maintain)

Держать в одном месте всё, что Парето-разрезом 2.5 (`plans/82`) отложено на будущее, — с адресами
готовых планов, чтобы открытие 2.6 началось с нулёвок актуализации, а не с археологии.

## Готово, когда (критерии приёмки)

1. Каждый пункт `researches/24` §3 с вердиктом «2.6» имеет здесь строку с адресом плана/бага —
   греп по вердиктам §3 не находит пункта без адреса здесь.
2. Ни один пункт не начат в реализации до открытия 2.6 владельцем.

---

## Унаследовано из `ideas/27` (2.3 → 2.4 → 2.5 → 2.6), адреса планов сохранены

1. **Эпик T «Лёгкие леса»** — целиком (`plans/72`): движок «правила-данные» для стражей формы
   (№73), честные закрытия, замкнутая доказательная петля, 58 CONFIRMED T-вхождений триажа R2
   (43 substantial · 15 hygiene; семейства `bugs/84`–`91`, 95, 96, 100, 103). Замер «до» —
   нулёвкой на открытии.
2. **Ревью `/code-revision` самим Fable** (`plans/63`) — стретч-хвост 2.5, если чаты останутся;
   иначе здесь.
3. **Эпик V «Порядок в ящике»** (`plans/74`; постановка — `plans/64`) — ревизия модулей
   поставки (сборка печатает живое число) и 37 навыков, снятие удвоения payload/обвязка, страж
   порядка. Ultracode-точка (№58). Сюда же — тяжёлая половина KAGO `bugs/KAIF/09`
   (дайджест/архив для GOAL-класса документов · генерируемый контекст-пакет `/resume` · правила
   схлопывания) — она и есть «ящик не растёт» для канона.
4. **Эпик P «Переименование навыков в `kaif-*`»** (`plans/32` + `plans/40`; №35/№47) — после V.
5. **Остаток эпика U «Поле»** (`plans/73`, фазы U3–U4 + полигон-покрытие дыр `researches/20`
   §2.3) — ЗА ВЫЧЕТОМ среза U2, взятого в 2.5 эпиком US (`plans/82`): паритет обвязка→поставка
   (94.\*, 89.3/89.4, 97.1/97.2) · хвосты U4 (`settings-fragment.json` по вендорской схеме ·
   `sync` в сообщении успеха · паттерн «одно хранилище голоса» · словарь slug'ов `kaif-fp`) ·
   R2-вхождения семейств 93/94/97, не закрытые 2.5.
6. **Продолжение эпика X — полная конверсия канона «проза → команды»** (issue #22): `PHILOSOPHY`
   · `BUG_FIXING_FRAMEWORK` · `AGENT_GUIDE` · README директорий + линтер `canon-form-check`. В
   2.5 конвертированы ТОЧКИ РЕШЕНИЯ (эпик CN); здесь — остальной корпус.
7. **Расширение охвата селфтестов** — каверза 3 суда W1 (ADDR-охваты `experience-lint` ·
   `lang-packs-guard` · `stylometry-snapshot` · `s02/S9-crash`) + остаток 98.3 + находка S1 суда
   RL 2.4 (END-класс молитвенных маркеров не стережёт никто). H10 (смешанный EOL) взят в 2.5
   эпиком US. Добавлено судом RL 2.5 (2026-09-04, E-H3): `language-arrivals` на bootstrap-маршруте
   доказан кодом, сводом не наблюдён — U6в: `install --lang ru --mode anonymous` + бандл 9.9 с
   новым навыком → пункт в `KAIF_UPDATE_TASK.md` после `install --mode standard`; и C-H3: таймаут
   `gh auth status` в `report` даёт exit 2 «not ready», формула Reference «таймаут = exit 3»
   буквально верна лишь для `issue create` — уточнить формулу или код.
8. **TD-фазы 1/5/7/10** (`ideas/KAIF_2.4_TEAM_DEPLOYMENT_EPIC.md`): TeamSpec/RoleContract YAML ·
   генерация `TEAM_SPEC.yaml` · граф-машинерия диспатча · автоматика ретроспектив; архетипы
   research/game/data · профили способностей моделей · cost-aware routing · дашборды здоровья.
9. **Хук `recon-before-decision`** (issue #36 п. 3) — когда появится механический признак
   развилки; в 2.5 — артефакт `FORK:` + охота судьи (эпик CN).

10. ~~**Лечение причины P1** (`bugs/100`)~~ — ✅ закрыто в 2.5 той же сессией (2026-09-04 12:07
    +03:00): H1 вне счёта translated-wholesale + страж s18 U14 «одно дерево в двух папках → один
    вердикт» (проба `tools/sandbox/probes/bugs-100-two-folders.mjs`); в 2.6 не переезжает.
11. **Флейк s07** (`STATUS` 2026-09-04 п. 1.2): `update` exit≠0 сразу после строки language-pack,
    два вхождения на ~10 прогонов полигона; форензика поставлена (`run-fail-N.log` в корне
    прогона s07) — при следующем вхождении заводится баг S2 с полным выводом; если причина в
    ядре — фикс до релиза 2.5 (риск (а) `plans/87`), иначе — сюда.
12. **Меньше ссылок на проекты-доноры в поставке и каноне** (слово владельца №90, 2026-09-04
    14:47 +03:00: «нужно поменьше ссылаться на какие-либо проекты в KAIF, ТЕМ БОЛЕЕ — в поставке.
    Это не особо нужно знать потребителям»): чистка существующих упоминаний — псевдонимы
    `project A…F` в комментариях ядра и навыков (`framework/installer/KAIF-CORE.mjs`,
    `framework/tools/*`, `framework/skills/*`), «field team»/«donor deployment» — оставить только
    там, где источник несёт смысл правила; новые тексты 2.5 доноров уже не называют.
13. **Мёртвые якоря в ОПУБЛИКОВАННЫХ нотах 2.2–2.4** (судья RC 2.5, сессия 52; EXP-0108):
    страницы релизов v2.2, v2.3, v2.4 несут ссылки `README#-quick-start` / `#-быстрый-старт` /
    `#русский` на несуществующие якоря (раздел «Quick start» исчез из README после 2.1, id
    русской половины — `russian`). Лечение — правка трёх файлов нот (шесть ссылок) +
    `gh release edit vX.Y --notes-file reports/RELEASE_NOTES_X.Y.md` — ВНЕШНЕЕ действие, по
    слову владельца; после — `release-body-guard --tag vX.Y`. Ось `NOTES-ANCHORS` судит только
    старшие ноты, исторические — намеренно нет.
14. **Приватные имена в реестрах суда `reports/KAIF_AUDIT/`** (судья RC 2.5: 12 вхождений на
    5 строках реестра 2.5): зона `private-names-guard` — поставка и витрина, рабочие документы
    вне неё; по №90 («поменьше ссылаться на проекты») — чистка псевдонимами при следующей
    ревизии реестров, вместе с п. 12.

## Полевые сигналы 2.5 — issues #40–#48 (2026-09-04 … 2026-09-05)

Первые ЖИВЫЕ входы скоупа: пять тикетов и четыре полевых отчёта обновления до 2.5 (KAGO 2.4→2.5;
NDim 2.4→2.5; Unliminium и KUMM 2.2→2.5 — три версии одним прыжком), все bootstrap-маршрутом,
все Windows 11. Каждое заявление сверено с HEAD поставки — `researches/26` §2; соседи на диске
пробиты — недоставленного нет (§2а). Адрес — эпик предлагаемого разреза `researches/26` §4;
работы не ведутся до открытия версии.

15. **#47 🔴 (Unliminium, слово владельца: «Вопросы владельцу нести в приоритете НОМЕР 1»)** —
    очередь вопросов ПЕЧАТАЕТСЯ, но не ДОСТАВЛЯЕТСЯ: контур записывает, что вопрос есть и что он
    отвечен, но не что он ПОКАЗАН; возраста нет; вопрос, перемещённый в `bugs/`, невидим стражу
    формы; протухший статус переназначает виновника (48 дней, ~40 сессий) → **OQ** (факт
    `shown:` · возраст · условие выхода шага 1б · «кто заблокирован» · страж прозы · охота судьи).
16. **#46 (NDim)** — метрика доставки принуждена к одному скаляру, владелец проекта назвал вектор
    из шести компонент → **OQ** (1..N компонент одной строкой, ведущая берёт `X → Y`; Q2
    интервью №023).
17. **#42 (NDim + Unliminium + KUMM, ×3 за сутки)** — `--rehearsal` недостижим на bootstrap-маршруте,
    который канон рекомендует i18n-развёртываниям; расписка не потребляется; загрузчик ставит ядро
    ДО отказа (дерево version-skewed); наборы кандидатов `diff`/`update` расходятся (16 vs 15) →
    **UR**.
18. **#48 R2/R3 (KUMM)** — файлы, отличающиеся от шаблона только заполнением плейсхолдеров,
    каждый интервал выдаются за правки владельца (3 мерджа + 1 депрекация руками; `update-verify`
    считает плейсхолдер-строки «обещанными») → **UR** (классификация `template + fills`).
19. **#44 (NDim)** — окно `stale-claims` в одну версию: строка, отставшая раньше, невидима
    навсегда (бейдж README на 2.2 три интервала) → **UR**.
20. **#40 (KAGO)** — `report` отвергает `not yet` в нижнем регистре и доставленный вручную тикет
    без URL одним сообщением, не называя законной формы → **UR**.
21. **#41 R1/wish 3/R9, #43 wish 4, #45 wish 2, #48 wish 3** — текст `/kaif-update`: bootstrap
    ОБЯЗАТЕЛЕН для якорных пар под ядром < 2.5 · baseline старого шаблона на bootstrap · `cp`
    расписки в дефолтный путь как штатный ход интервала из < 2.5 · `--source` один раз обоим
    прогонам · `core.longpaths` → **UR**.
22. **#43/#45 wish 3** — бюджеты девятки превышены у трёх проектов ИЗ-ЗА приехавшего канона 2.5;
    правило не говорит, куда выносить приехавшее → **CB** (дешёвая половина; тяжёлая — 2.7).
23. **#41 R5/R6, #48 wish 4** — косметика: `@fork` во фразе линтера, языковая смесь по токенам,
    указатель в предупреждении пакета → **CB** попутно.
24. **#41 R8** — тело issue #23 двоекодировано (BOM, ручная доставка 28 августа) → **HY**,
    внешнее действие по слову (Q3 интервью №023).
25. **#43 R5** — на translated-дереве вердикт `merged` пришивает английские секции в
    полупереведённые `TESTING`/`REQUIREMENTS` (86 строк) → **2.7** (граница требует разведки).
26. **#45 wish 4** — подсказка «модуль пришёл из полевого проекта — если он ваш, у вас пара» →
    **2.7**.
27. **Наброс владельца 2026-09-05 (символ веры)** — EN-носитель говорил «KEEP TRYING», поле
    переводило «продолжаем пытаться» вместо «стараемся» → **починено вне скоупа тем же днём**
    (`bugs/110`: шаблон + русский оригинал в комментарии деплоя + FORBIDDEN-страж); полевой
    тикет ожидается.

Подтверждения поля, работы не требующие: предупреждение заморозки локального `/kaif-update`
сработало (#45 R2) · репетиция предсказала бой побайтно четвёртый релиз подряд (#41 R4, #43 wish
4, #48 wish 5) · `stale-claims`/`check` исполняются чекпоинтами (#41 п. 8) · `report` доставил
#40 без BOM (#41 R8) · `/report-bug` модуль доставки перенёсся 16/16 (#43 R3, +1 в #24).

28. **#50 🔴 (NDim Space, слово владельца: «если я тебе что-то предлагаю, и тебя это смущает, ты не
    понимаешь — это тебе прямой знак, что нужно идти искать в интернете, что я имел в виду, или у
    меня спросить»)** — ПОВЕДЕНИЕ АГЕНТА НА РАЗВИЛКАХ: у канона есть «три двери» для пробелов
    агента и форма вопроса, но нет правила для момента, когда СМУЩАЕТ предложение владельца; агент
    выбирает дешёвый выход «ваше предложение ломает модель» и откатывает работу. Полевой случай:
    «role-playing game и RPG одновременно» прочитано как третий тег без пары, а «РПГ» — русская
    половина «RPG», модель пар цела; три инцидента класса за сутки (теги · «контур пилюле» ·
    «одной второй строкой») → **предложение 2.6:** смущение = обязательный триггер разведки
    (веб-поиск → замер по данным владельца → интервью), блок «Разведка:» в интервью/отчёте как
    предполёт, охота судьи за «ломает / не умеет / невозможно» без него, мутация-фикстура из
    инцидента, словарная запись «РПГ = RPG» как пример. Записано по прямому поручению владельца
    («запиши это импрувментов в ориджин KAIF на версию 2.6 — к поведению агента на развилках»)
    полевым агентом NDim прямо в исток (коммит `f323c6a`, дословно) → **FK** — новый эпик 2.6
    между UR и CB (`plans/89` критерий 13, строка 3 таблицы эпиков), взят словом владельца в чате
    истока 2026-09-05 («там будет еще один запрос на улучшение в ориджин, берем его тоже в 2.6
    версию»); номер пункта поправлен с дубля «27» на 28 сессией 55.
29. **#51 🔴 (Unliminium, 2026-09-05 16:56 +03:00; слово владельца дословно: «Ты открыл сломанный
    интерактивный контур без радиокнопок. Это плохо!!! Баг в KAIF заводи, что интерактивные контуры
    вечно какие-то сломанные в разных проектах получаются. Нужно, чтобы KAIF очень подробно писал
    спецификацию на построение интерактивного контура, или даже имел в своей поставке готовый
    интерактивный контур, которые агенты качают из ориджин. Это должен быть генератор интервью,
    вычитки, отсмотра макетов.»)** — ИНТЕРАКТИВНЫЙ КОНТУР ВЛАДЕЛЬЦА строится каждым проектом заново
    и ломается (четыре дефекта класса в поле: #19, #38, #47, #51); поставка несёт контракт
    `/owner-reviews` I1–I43 и форму вариантов в `/interview`, но не машинную спецификацию страницы
    (парсер вопросов и вариантов · запись ответа · факт показа · страж перед открытием · keep-alive ·
    самопроверка «радиогрупп = вопросов») и не референс-инструмент (решение №34 2.2: докачки
    референс-машинерии из origin нет). Два яруса словом владельца — спецификация в поставке · готовый
    генератор трёх лиц (интервью · вычитка · отсмотр макетов) из ориджина → развилка видения —
    интервью №024, **Q1 = B 2026-09-05 17:32 +03:00 (№101, пересмотр №34): ОБА яруса в 2.6 → эпик
    IC** (`plans/89` критерий 14, строка 4 таблицы эпиков, ≈ +2–2,5 чата); против рекомендации
    агента (генератор в 2.7).

28. **#52 🔴 (NDim Space, слово владельца: «вопиющая халатность тестирования»)** — КАНОН ДВЕРИ
    ВЫКАТА: смоук «под сессией» и гостевой ходят СВЕЖИМИ контекстами и слепы к состоянию, которое
    есть у каждого настоящего посетителя (маркер сессии, память темы/языка, живая сессия). Зелёная
    дверь (24/0 · 81/81) выкатила главную, показывавшую всем возвращающимся вечную «Загрузку»:
    щит поднимал `app.html` по маркеру, опускать на новом корне было некому; поймал владелец за
    минуты. Третий инцидент класса в проекте (bugs/40, bugs/124) → **предложение 2.6:** обязательный
    проход возвращающегося пользователя на каждом публичном входе (сидирование маркера и памяти,
    ожидание «видно или увели внутрь за N с»), «отрисовано» судится видимостью, а не наличием в
    DOM, снятие редиректа/обработчика — шаг «кто на нём стоял» в фейбл-методе, проверка `csr` до
    лечения через `onMount`. Полевые механизмы: проход в `verify-prod-b4` (доказан красным на
    сломанном бою), страж `verify-boot-shield`, `e2e/root.spec.ts`.

## Пункты, ждущие наполнения (перенесены из `ideas/27`)

Кандидаты из автономного пула `STATUS.md` — переезжают в скоуп только словом владельца:
tamper-тест sha256-гейта загрузчика · хвосты интерактивного контура (интервью №008 Q3) · адаптеры
Devin/OpenCode/Aider/Junie · token-F1 ступень `kaif-provenance` · идея fork-bundle · библиотеки
терминов остальных сфер · машинная половина ставки витрины (`bugs/73` «в»).

## Разрез 2.6 (решение владельца)

**Принят владельцем — интервью №023, Q1 = B (2026-09-05 14:38 +03:00; №95):** разрез
`researches/26` §4 плюс стретч — **OQ → UR → CB → HY → CR → RL** ≈ 6–7 чатов; за бортом
(реестр 2.7 `ideas/29`) — T · V · P · X-полный · селфтесты · U3/U4 · TD-фазы · хук #36 ·
#43 R5 · #45 wish 4 · пункты «ждущие наполнения». Мета-план — `plans/89`; операционный план
ближайшего эпика OQ — `plans/90` (раунд 2, 14:57: Q2 = A — строку `DELIVERY:` довести по
пояснению владельца в Unliminium, вектор по механикам, №99; Q3 = A — три правки опубликованного в
HY, №100). Рабочее
имя — Mindful KAIF (Q4 = B, №96), окончательное слово — ближе к релизу. Пп. 1–14 этого реестра
раскладываются так: в 2.6 — 12 (чистка ссылок на доноров, HY) · 13 (мёртвые якоря нот, HY по
Q3 раунда 2) · 14 (имена в реестрах суда, HY) · хвост 7 (E-H3/C-H3, UR) · 2 (ревью
`/code-revision`, CR); в 2.7 — 1 · 3 · 4 · 5 · 6 · 7 (остальное) · 8 · 9; 10 — закрыт в 2.5;
11 — по рецидиву. Пп. 15–27 — по адресам эпиков в их строках.

## Решения, принятые агентом без владельца

1. Форма — краткий реестр по образцу `ideas/26`/`ideas/27` (форма задана владельцем в `ideas/24`).
2. Готовые планы не переписываются — наследуются с нулёвкой актуализации на открытии (№76).

## Links

`ideas/27` · `researches/24` §3/§5 · `plans/82` · `plans/72`/`63`/`74`/`64`/`32`/`40`/`73` ·
issue #22 · `reports/KAIF_AUDIT/2026-08-14_r2_triage_SUMMARY.md` ·
`reports/KAIF_AUDIT/2026-08-28_rl_court_registry.md`.
