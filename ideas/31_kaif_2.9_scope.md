# Идея 31 — KAIF 2.9: скоуп версии (отложенное Парето-разрезом 2.8)

> **Создан:** 2026-09-24 (сессия 70, Claude Opus 5.5; по решениям владельца №124/№125 — интервью №034: Q1 = A — разрез
> `researches/32` §4 принят как предложен, «за бортом» едет сюда; Q2 = D, дословно: «Бери в работу важное и что принесёт
> пользу» — мандат, `[ИИ]` разнёс кочующие переделки ниже).
> **Родитель:** `ideas/30` (реестр 2.8 — разнесён, ✅) + `researches/32` §3/§5 + `plans/117` § «Парето-разрез».
> **Статус:** 🧊 ОТЛОЖЕНО 2026-09-24 19:06 +03:00 (коммит `2d897c5`) — работы не ведутся до открытия версии словом владельца; на открытии —
> нулёвки актуализации (№76); новые тикеты origin после открытия 2.8 — строкой сюда или в `plans/117` словом владельца (тикет #105 — в 2.8, сюда не
> едет: интервью №037, Q1 = A, решение №130, 2026-09-25 16:32 +03:00).
> **Вовне:** зонтичный план 2.9 — когда владелец откроет версию.

## Вектор цели (Maintain)

Держать в одном месте всё, что разрез 2.8 (`plans/117`) отложил, с адресами готовых планов и находок разведки, чтобы
открытие 2.9 началось с нулёвок, а не с археологии.

## Готово, когда (критерии приёмки)

1. Каждый пункт `researches/32` §3 с вердиктом «2.9» и каждый пункт `ideas/30`, не взятый в `plans/117`, имеет здесь строку
   с адресом — греп вердиктов §3 и номеров `ideas/30` не находит пункта без адреса здесь или в `plans/117`.
2. Ни один пункт не начат в реализации до открытия 2.9 владельцем.

---

## Из разведки 2.8 (`researches/32` §3), не взятое разрезом

1. **Хуки знают корень проекта** — #94 (корень из `cwd` события; абсолютный путь в тексте трёх приказов; корень через
   `import.meta.url`) + остатки ревизии хуков `ideas/30` п. 25: `bugs/118` №2/№3 · `bugs/119` №1/№2 + девять мест «внешний
   JSON без снятия BOM» · `bugs/120`. ≈ 0,5 чата.
2. **Красный от аппарата** — #87: класс «красный дал стенд, а не субъект», шаг «закрепи контекст прогона до вердикта»,
   инверсия принципа 4, охота судьи. ≈ 0,25 чата — после лёгкого канона 2.8 (в `TESTING_FRAMEWORK` появится место).
3. **Линтеры под форму языка проекта** — N1 (`kaif-testrun-lint` и жирные метки) · N5 (строка доставки в цитате) · N7
   (`kaif-attribution-lint` и вставное слово) · Q-R5 (`kaif-voice-lint` — путь портрета в строке OK). ≈ 0,5 чата.
4. **Stop-хук над последним ответом агента** — #82 п. 3 · #95 п. 3 · #97 п. 3: сначала разведка вендорского контракта
   (`transcript_path` во входе Stop-события — живая дока), доля ложных срабатываний на полигоне; снял бы и самопроверку
   «показ ссылкой». Разведка ≈ 0,5 чата.
5. **Мелкое без хозяина** — N9 (`check` не видит отсутствующее зеркало локального навыка) · K-R11 (LF в CRLF `.gitignore`) ·
   K-§1a′ (ось конституции команды — только §2 и заголовки) · K5 (пятое лицо «текст через файлы» — heredoc).

## Кочующие переделки истока — «на когда-то» (слово владельца №77: «будет делаться КОГДА-ТО в будущем, но не сегодня»)

По мандату №125 `[ИИ]`: в 2.8 взяты только части, приносящие пользу полю (полигон против ядра прошлой версии — в UP;
перевод руководства агента в шаги — в CK). Остальное — здесь, адреса планов сохранены:

6. **Эпик T «Лёгкие леса»** (`plans/72`) — движок «правила-данные» для стражей формы (№73), 58 CONFIRMED T-вхождений триажа R2
   (`bugs/84`–`91`, 95, 96, 100, 103).
7. **Эпик V «Порядок в ящике»** (`plans/74`; постановка `plans/64`) — ревизия модулей поставки и навыков, снятие удвоения
   payload/обвязка, страж порядка; из тяжёлой половины KAGO `bugs/KAIF/09` часть «архив» и «лёгкий канон» взята в CK 2.8.
8. **Эпик P «Приставка `kaif-*` у навыков»** (`plans/32` + `plans/40`; №35/№47).
9. **Остаток эпика U «Поле»** (`plans/73`, U3–U4) — кроме свода «`update` ядром прошлой версии» (взят в UP 2.8).
10. **Остаток эпика X** (issue #22) — конверсия «проза → команды» `PHILOSOPHY` · `BUG_FIXING_FRAMEWORK` · README директорий +
    линтер `canon-form-check`; руководство агента и канон тестирования — в CK 2.8.
11. **Расширение охвата селфтестов** — каверза 3 суда W1 · остаток 98.3 · S1 суда RL 2.4.
12. **TD-фазы 1/5/7/10** (`ideas/KAIF_2.4_TEAM_DEPLOYMENT_EPIC.md`).

## Унаследовано из `ideas/30` (не взято в `plans/117`)

13. Хук `recon-before-decision` (#36 п. 3) — когда появится механический признак развилки (`ideas/30` п. 8).
14. Флейк `bugs/109` — по рецидиву (п. 9).
15. #43 R5 — `merged` на полупереведённом файле (п. 10) · #45 wish 4 — подсказка «модуль из полевого проекта» (п. 11).
16. #57 п. 3 — отдельный `HANDOVER.md` (п. 12) · #54 п. 3 — документ на раунд интервью (п. 13) · #55 — обход исторического
    долга авторства (п. 14).
17. Навыки поставки — прочитать исполнителем (п. 16; первые кандидаты `/team-deployment` · `/release` · `/autoloop` ·
    `/guarded-loop` · `/kaif-update`).
18. #71 пп. 1–2 — строка доли процесса (п. 17) · #71 п. 4 — правило концентрации (п. 18).
19. Две оси «закрытие» для `doc-header-lint` (п. 20) · остатки судьи дельты `f4915af` (п. 21).
20. Находки суда 2.7 без хозяина в 2.8 (п. 23, кроме A-F3/A-F4/D-F4 → OW и F-F2 → UP): B-F1 · B-F2 · B-F3/B-F4 · E-F4 · C-F3
    · A-F5 · C-F1 · C-F2 · C-F4 · C-F5 · D-F3 · D-F5 · D-F6 · E-F2 · E-F3 · E-F5 · F-F3 · F-F4
    (`reports/KAIF_AUDIT/2026-09-18_rl_court_2.7_registry.md`).
21. Пункты, ждущие наполнения (из `ideas/30`): tamper-тест sha256-гейта загрузчика · хвосты интерактивного контура (№008 Q3) ·
    адаптеры Devin/OpenCode/Aider/Junie · token-F1 `kaif-provenance` · fork-bundle · библиотеки терминов сфер · машинная
    половина ставки витрины (`bugs/73` «в»).

## Найдено в работе над 2.8 (сессия 71, эпик CK) — без хозяина в 2.8

22. **Окно линтера авторства слишком щедрое** — `kaif-attribution-lint` засчитывает основанием любую «ёлочку» или ASCII-кавычку в
    окне ±2 строки: заглушка `"none"` скелета домашних правил зеленила любую строку происхождения, а на корневом руководстве
    адреса строк происхождения проходили «дважды» — адресом и соседней цитатой (лёгкие судьи CK4.5, находка 2, и близнеца CK4.2,
    находка 7; отчёты `testcases/reports/2026-09-24_ck45-house-rules-skeleton.md` · `…_ck-notes-twin.md`). Кандидат: цитата
    основывает свою строку, как адрес коммита с CK4.1.
23. **Ось «момент не позже коммита» для таблиц отчётов прогона и вех ✅ планов** — `doc-header-lint` судит метки при глаголах
    «принято / зафиксировано / закрыт / снят»; метка «≈ 21:55» в коммите 21:54:20 прошла молча (EXP-0153, поле «Механизация»), и
    даже метка при глаголе («зафиксировано ≈ 20:52» в коммите 20:51:45) прошла: допуск вперёд — 5 минут
    (`tools/doc-header-lint.mjs` `STAMP_FUTURE_TOLERANCE_MIN`). Кандидат: допуск только назад во времени, фикстура «на минуту позже».
24. **Остаток тикета #99 (цена входа в чат в токенах)** — интервью №035, Q2 = A: в 2.8 взята дешёвая половина (строка стоимости
    входа справкой, портрет разделами по умолчанию — критерий 21 `plans/117`); сюда — дверь закрытия по доле памяти модели,
    ежечасное перечитывание одной страницы-выжимки вместо ядра и правило «новое правило входит в канон, только вытеснив старое».
25. **Штамп момента — инструментом истока, а не помощником скретчпада** (EXP-0154): сессия 72 трижды поставила метку прогнозом и
    перестала, когда вехи и отчёты пошли через токен `{{T}}`, подставляемый из `date` в том же вызове; помощник жил в скретчпаде и умер
    с сессией. Кандидат: `tools/stamp.mjs` (замена ровно одного вхождения старого текста новым с `{{T}}`) и отказ `tools/commit.mjs` на
    незаменённый токен `{{…}}` в коммитуемых файлах — пара к п. 23. Требование, оплаченное сессией 74: штамп пропускает токен,
    записанный кодом (`` `{{T}}` `` — документация самого правила в `STATUS.md`); помощник, менявший ВСЕ вхождения, затёр его моментом
    (коммит `5255c7c`, восстановлено сессией 74).
26. **Справочник знает каждую строку `check`** (EXP-0156): абзац осей `check` в `KAIF_REFERENCE.md` не знал ни одного изменения эпика
    CK 2.8, пока сессия 72 не дописала оговорку вручную; близнецы в сводах ловит полигон, справочник — никто. Кандидат: гард сборки —
    у каждой строки `⚠`/`ℹ`, которую печатает `cmdCheck` ядра, есть ключевое слово в этом абзаце (список пар «строка → слово» — данными).
    Прежний ответ того же класса — №86 («тяжёлая половина» облегчения входа отложена в 2.5).

## Найдено в работе над 2.8 (сессия 74, функциональный прогон руководства шага CK6) — без хозяина в 2.8

27. **Заготовка команды коммита в навыках закрытия спорит с гигиеной git** — `framework/skills/end-chat-soft/SKILL.md:113` и
    `framework/skills/end-chat-force/SKILL.md:36`: «Otherwise: git add -A && git commit … && git push», а руководство требует
    объявленного и напечатанного уходящего набора (`AGENT_GUIDE.md` → «Non-negotiable git hygiene»). Нашёл свежий агент на установке 2.8
    (отчёт `testcases/reports/2026-09-25_ck6-guide-functional-run.md`). Кандидат: заготовка «объявленный набор → `git diff --stat` → коммит».
28. **Три расхождения поставки, которые назвал свежий агент на 2.7 — сверить с 2.8:** регулярка рекомендации генератора контура не
    читает форму `**Рекомендация агента:** A` (ему пришлось переписать строку под парсер); `readmes/bugs.md` («even small ones») против
    лестницы тяжести (у S3 документа нет); `readmes/plans.md` требует файла плана для нетривиальной работы, а руководство разрешает план
    разделом в документе бага. Тот же отчёт; сначала — проба на поставке 2.8, потом строка в эпик.
29. **Три пробы выпуска истока — в поставочный `/release`** (оговорка (г) судьи релиз-кандидата 2.7, `ideas/30` п. 24; решено RL2 2.8,
    сессия 75, `[ИИ]` — C для 2.8, `plans/125`): `ticket-reply-wrap` (переносы строк в черновиках ответов в трекер) · `ticket-replies-readback`
    (чтение опубликованных ответов обратно) · `release-page-shot` (снимок страницы релиза без окна владельца) — опциональными tool-модулями
    поставки с самопроверками и сводом (вариант A); B — шаг «напиши себе проверку» — по №97 не отгружается.
30. **Остатки тикетов поля, не вошедшие в 2.8** (сверка черновиков ответов RL4, сессия 75, `[ИИ]`; источник каждой строки — тикет и его
    ответ `reports/release_2.8/<N>.md`): #74 — фраза шага 3d `/interview` для поиска руками · #76 R7 (объявлять зеркалирование локальных
    навыков) и R9 (сверка строки версии записи с `kaif.json`) · #79 — двенадцать советов «promised upstream line not found» отчёта · #82 —
    строка «вопросы владельцу в этом ответе» навыков закрытия (дешёвая половина) · #83 R1(а) / пожелание 4.4 (шапка линтера атрибуции:
    адрес принимается без сверки) · #85 — печать текста вокруг указателя на вынесенный раздел · #89 — страж канона против блока сырых
    цитат · #93 §2.4 п. 3 («вода» — линтер) и R8 (неведомое поле `updated` маркера) · #95 — голос по языку на macOS (`say` без выбора
    голоса — не проверено) · #100 — необязательное чтение чужой очереди `{items:[…]}` · #104 п. 3 (картинки и SVG на странице вопросов) и
    необязательный линтер.

## Решения, принятые агентом без владельца

1. `[ИИ]` Форма — краткий реестр по образцу `ideas/29`/`ideas/30`.
2. `[ИИ] по мандату — «Бери в работу важное и что принесёт пользу»` (№125): разнос шести переделок — «польза» прочитана как
   «меняет полевой прогон»; пересматриваемо словом владельца.

## Links

`ideas/30` · `researches/32` §3/§5 · `plans/117` · `interviews/interview_034_kaif_2.8_scope.md` · `plans/72`/`74`/`64`/`32`/`40`/`73`.
