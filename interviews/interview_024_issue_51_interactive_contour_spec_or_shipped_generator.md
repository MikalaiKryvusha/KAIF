# Interview #024 — issue #51: интерактивный контур владельца — подробная спецификация в поставке или готовый генератор из ориджина, и что из этого в 2.6?

> Topic: тикет #51 (Unliminium, 2026-09-05 16:56 +03:00) — ваши слова дословно: «Ты открыл
> сломанный интерактивный контур без радиокнопок. Это плохо!!! Баг в KAIF заводи, что
> интерактивные контуры вечно какие-то сломанные в разных проектах получаются. Нужно, чтобы
> KAIF очень подробно писал спецификацию на построение интерактивного контура, или даже имел в
> своей поставке готовый интерактивный контур, которые агенты качают из ориджин. Это должен быть
> генератор интервью, вычитки, отсмотра макетов.» В поле четыре дефекта этого класса (#19, #38,
> #47, #51). Решение 2.2 №34 говорило обратное: «исполнимый контракт вендорится в навык
> дословно; докачки референс-машинерии из origin нет» — его пересмотр возможен только вашим
> словом. В тикете два яруса («спецификация … или даже готовый контур»); какой из них — в 2.6,
> решаете вы.
> Source of the idea: issue #51 + ваше слово в чате истока 2026-09-05 («тикеты свежие из issues
> видел?»).
> Status: **✅ ANSWERS RECEIVED 2026-09-05 17:32 +03:00** (страницей контура, by Mikalai Kryvusha
> (KOT KRINIK)): Q1 = **B** — спецификация И готовый генератор трёх лиц в 2.6 (пересмотр решения
> №34 → №101; эпик IC в `plans/89`, критерий 14). Тем же часом — слово о форме страницы (чат,
> ≈ 17:30 +03:00, дословно): «вот эту шапку нужно не стики делать, а чтобы скролилась со всей
> страницей» — исполнено в `tools/review.mjs` истока, дефолт для генератора. Разнос по адресатам —
> 2026-09-05 17:33 +03:00 (проба date в тот же ход). Создано 2026-09-05 17:26 +03:00 (сессия 55, посреди эпика UR).

## Context / what I already found in the code

- Спецификация в поставке УЖЕ есть, и подробная: навык `/owner-reviews` несёт 43 инварианта
  контура (I1–I43), порядок сборки, исполнимый контракт сборки по шагам C1–C13, элементы
  страницы P1–P9, нормы стражей G1–G13, приёмочный чек-лист QA1–QA7, ловушки платформы T1–T11 и
  канонические дефолты — около 630 строк. Но это НАВЫК, который читают, когда его вызывают, а
  контур в поле строят по соседу («смотри, как NDim делал») и ломают на своих краевых случаях:
  страница Unliminium открылась с вопросом БЕЗ радиокнопок, потому что варианты были написаны
  абзацами `**A. …**`, а не списком `- **A)** …`, и ни одна проверка перед открытием этого не
  поймала.
- Форму вариантов `/interview` называет (таблица `| **A** | … |` или список `- **A)** …`), но
  машинной проверки «у каждого вопроса есть варианты списком или объявленное свободное поле» перед
  открытием страницы в поставке нет.
- Референс-реализация контура живёт в истоке — `tools/review.mjs` (1215 строк) + `tools/lib/review-core.mjs`
  (534 строки): страница интервью, вычитка, сообщение, очередь, факт показа, отправка. В поставку
  не едет (№34); обобщить её под любой проект (пути, тексты, три лица — интервью · вычитка ·
  отсмотр макетов) — отдельная работа с селфтестом и сводом полигона.

## QUESTIONS

### Q1. Что из #51 берём в 2.6 — спецификацию, генератор или оба?

**Адресат ответа:** `plans/89` (новый эпик и критерий 14 версии) + `ideas/28` п. 29 +
`MASTER_PLAN.md` §7 (пересмотр решения №34 — при B или C) + ответ в issue #51 после релиза.

**Происхождение:** issue #51 — ваше слово дословно (выше). Варианты — предложение агента;
рекомендация помечена. Ниже каждый вариант — сценарием того, что вы увидите.

**A — спецификация доводится до ИСПОЛНИМОЙ и едет в 2.6; генератор — первый эпик 2.7
(рекомендация).**

- Ситуация. Проект на KAIF 2.6 со своим контуром, построенным по соседу; агент написал интервью,
  где варианты первого вопроса набраны абзацами `**A. …**` вместо списка, и собирается открыть
  вам страницу.
- Действие. Агент запускает страницу.
- Результат. Страница НЕ открывается: предполётная проверка поставки (`node
  .kaif/tools/kaif-contour-lint.mjs interviews/interview_052.md`) печатает «Q1: 0 вариантов
  списком и нет объявленного свободного поля — страница откроется без радиокнопок; исправьте
  форму: `- **A)** …`» и завершается красным; агент правит документ, проверка зеленеет, и вы
  видите только исправную страницу с радиокнопками. Тот же документ поставки — одностраничная
  спецификация `.kaif/INTERACTIVE_CONTOUR_SPEC.md` (форма вопросов и вариантов · запись ответа ·
  факт показа · страж перед открытием · keep-alive · самопроверка «радиогрупп = вопросов») — тот
  контракт, который навык `/owner-reviews` держит 630 строками, но в 20 проверяемых строках, на
  которые ссылаются `/interview` и `/owner-reviews`.
- Проверка. `ls .kaif/INTERACTIVE_CONTOUR_SPEC.md .kaif/tools/kaif-contour-lint.mjs` → 2 файла
  после обновления до 2.6; `node .kaif/tools/kaif-contour-lint.mjs selftest` → зелёный, красный
  на фикстуре «варианты абзацами» (ровно случай #052 Unliminium); свод полигона истока.

**B — спецификация И готовый генератор трёх лиц в 2.6 (из ориджина, поставкой).**

- Ситуация. Тот же проект на KAIF 2.6; своего контура у проекта нет — или есть, но сломан.
- Действие. Агент запускает `node .kaif/tools/contour/review.mjs interviews/interview_052.md` —
  инструмент приехал поставкой (обновлением из ориджина), проект дописал только заголовок,
  пути и тексты кнопок.
- Результат. Вы видите страницу с радиокнопками у каждого вопроса, полем свободного ответа,
  кнопкой «Сохранить» и сигналом «сохранено»; ответ записан в `interviews/decisions/*.json`,
  факт показа — в `interviews/decisions/shown.json`; тот же инструмент открывает вычитку
  (комментарии к абзацам) и отсмотр макета (картинка + комментарии). Сломать контур в проекте
  больше нечем — он не пишется в проекте.
- Проверка. `node .kaif/tools/contour/review.mjs --selftest` → зелёный на свежем
  развёртывании; свод полигона гоняет три лица генератора; `/owner-reviews` говорит «запусти»,
  а не «построй».

**C — только генератор в 2.6, без отдельной спецификации** (спецификацией служат сам инструмент,
его комментарии и селфтест).

- Ситуация · Действие · Результат — как в B.
- Проверка — как в B; отдельного документа спецификации нет.

**D — свой вариант** — впишите, что именно и когда.

| Вариант | Что означает | Цена и риск |
|---|---|---|
| **A** (рекомендация) | Спецификация становится ИСПОЛНИМОЙ: одностраничный документ поставки + линтер `kaif-contour-lint` (предполёт перед открытием страницы, селфтест, красный на случае #052) + ссылки из `/interview` и `/owner-reviews`; генератор — первый эпик 2.7 по этой спецификации. | ≈ 0,5–0,75 чата в 2.6 (новый эпик IC между FK и CB); риск — контур по-прежнему строит проект, но по одной проверяемой форме, и сломанная страница не открывается. |
| **B** | A + референс истока (≈ 1 750 строк) обобщается под любой проект и едет поставкой в `.kaif/tools/contour/` с селфтестом и сводом полигона; пересмотр решения №34. | ≈ +2–2,5 чата к 2.6, релиз позже примерно на день; риск — обобщение за один заход (пути, тексты, три лица) и новая зависимость проектов от инструмента ориджина. |
| **C** | Только генератор, без документа спецификации. | ≈ +1,5–2 чата; проект на агентской системе без Node остаётся без письменной формы. |
| **D** | Свой вариант. | — |

**Answer:** B) <!-- owner-review: by Mikalai Kryvusha (KOT KRINIK) · 5 сентября 2026, 17:32 (+03:00) -->
