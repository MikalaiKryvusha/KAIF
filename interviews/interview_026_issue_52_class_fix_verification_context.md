# Interview #026 — класс #52 «дверь зелёная — реальный пользователь видит дефект»: чем и когда чинить КЛАСС в KAIF

> Topic: разведка класса по вашему слову (интервью №025, №103: «Разведка. Пойми источник проблемы как
> класса, и как ее как класс чинить в KAIF») снята — `researches/29`. Источник класса: агент проверяет
> продукт в СТЕРИЛЬНОМ контексте, который строит из ничего (свежий браузер без маркера и памяти, чистый
> чекаут, пустая база, «сегодняшняя» сборка), а настоящий посетитель приходит С ИСТОРИЕЙ (маркер сессии,
> память темы/языка, кеш прошлой сборки, вкладка, открытая через выкат). Дефект живёт в разности этих
> контекстов, и все приборы агента зелены по построению. У соседа на диске — шесть инцидентов класса за
> семь недель, четыре нашли вы сами через минуты после выката. Индустрия закрывает класс не «ещё одним
> смоуком», а ЯВНЫМ перечнем контекстов посетителя, который прибор обязан пройти (файл состояния ·
> тёплый проход · «видно» по цели клика или кадру, а не по DOM · заголовки кеша). Вопрос — форма и версия
> фикса класса в KAIF; кодируется только по вашему слову.
> Source of the idea: issue #52 + п. 30 `ideas/28` + `researches/29` (2026-09-05 22:49 +03:00, сессия 56).
> Status: **🟡 awaiting the owner's answers**
> Created 2026-09-05 22:55 +03:00 (сессия 56; поднято страницей контура через отгружаемый генератор — эпик
> IC закрыт тем же вечером).

## Context / what I already found in the code

- Канон KAIF о проверке «как пользователь» — гейт 6 `TESTING_FRAMEWORK.md` обоих слоёв: «войди той
  дверью, которую продукт предлагает… если у продукта есть аутентифицированное состояние,
  неаутентифицированный смоук — НЕ улика о продукте». Он называет два контекста — гостя и вошедшего — и
  молчит о вернувшемся (с маркером, памятью, кешем) и о вкладке, открытой через выкат. Слов «returning»,
  «возвращающ», «storageState», «кеш прошлой сборки» в `framework/` нет (греп → 0).
- Разведка класса (`researches/29`): §2 — источник (три корня поведения агента: стерильность как
  воспроизводимость · агент всегда «новый посетитель» · «отрисовано» судится присутствием в DOM, а не
  видимостью); §3 — шесть инцидентов одного проекта (`bugs/10` · `40` · `109` · `124` · `162` · #52),
  каждый чинился прибором ПОД СВОЁ состояние; §4 — индустрия: storageState/persona-фикстуры, cold/warm,
  `toBeVisible` слеп к перекрытию (только hit-target клика или кадр), cache-busting для «тех, кто уже
  здесь»; §5 — обобщаемое: К1 матрица контекстов посетителя как явный вход любой проверки, К2 «видно» =
  цель клика/кадр, К3 снятие того, на чём стояли другие, — с грепа «кто на нём стоял», К4 прибор под
  состояние — отдельный и дешёвый, К5 охота судьи. Проектное (`csr`, `onMount`, `data-booting`,
  `firebase.json`) в канон не едет.
- Версия 2.6 сейчас: закрыто 9 из 15 критериев; впереди WN (`plans/94`) → CB → HY → CR → RL, ≈ 5–6 чатов.

## QUESTIONS

### Q1. Чем и когда чинить КЛАСС «контекст проверки ≠ контекст посетителя» в KAIF?

**Адресат ответа:** `plans/89` (при A/B — критерий 16 сценарием и строка эпика между WN и CB; при C —
строка переноса) · `ideas/28` п. 30 · `ideas/29` (при C) · `MASTER_PLAN.md` §7 (журнал решений) ·
`researches/29` §6 · ответ в issue #52 после релиза (№84/№92).

**Происхождение:** ваше слово 2026-09-05 20:53 +03:00 (№103) отклонило мои варианты под один инцидент и
велело сначала понять класс. Разведка снята (`researches/29`); ниже — варианты формы фикса КЛАССА, каждый
сценарием того, что вы увидите. Рекомендация помечена.

**Разведка:** источник класса — три корня поведения агента (`researches/29` §2); индустрия держит класс
явным перечнем контекстов посетителя, а не количеством проверок (§4); в каноне KAIF есть половина этого
перечня (гость · вошедший — гейт 6), нет второй половины (вернувшийся · открытая вкладка) и нет правила
«видно, а не в DOM» (§5).

- **A) (рекомендация)** Канон-правило класса в 2.6 — текстом, без машинерии (≈ 0,5 чата).
  - Ситуация. Ваш проект на KAIF 2.6 выкатывает новую главную; у каждого настоящего посетителя в браузере
    маркер сессии, память темы и кеш прошлой сборки.
  - Действие. Агент прогоняет дверь выката и пишет отчёт перед словом «задеплоено».
  - Результат. Вы видите в отчёте двери ТАБЛИЦУ КОНТЕКСТОВ посетителя — «первый визит · вошедший ·
    вернувшийся (маркер + память + кеш) · вкладка через выкат» — и у каждой строки либо «прибор прошёл С
    этим состоянием: видно за 3 с / увёл внутрь», либо честное «не покрыт»; страница под непрозрачным
    щитом считается НЕ отрисованной; в `TESTING_FRAMEWORK.md` проекта гейт 6 называет четыре контекста и
    правило «отрисовано = видно (цель клика или кадр), а не есть в DOM»; снятие увода/обработчика в
    fable-цикле `AGENT_GUIDE` начинается с грепа «кто на нём стоял»; судья ловит отчёт двери без таблицы.
  - Проверка. После обновления до 2.6: `grep -c "вернувш\|returning" TESTING_FRAMEWORK.md` → `≥ 1`;
    в истоке `grep -c "returning" framework/TESTING_FRAMEWORK.md` → `≥ 1`; охота «green door blind to the
    returning visitor» в обеих копиях `/fable-judge`; policy/notes 2.6; `plans/89` — критерий 16 ✅.
- **B)** Канон-правило (как в A) ПЛЮС машинная половина в 2.6: шаблон отчёта двери и линт (≈ 1 чат).
  - Ситуация. Та же, что в A; агент под давлением релиза склонен пропустить строку таблицы.
  - Действие. Агент пишет отчёт двери по шаблону поставки и прогоняет `node .kaif/tools/kaif-door-lint.mjs
    check <отчёт.md>` в двери выката.
  - Результат. Вы видите тот же отчёт, что в A, — но дверь КРАСНАЯ, пока в таблице нет строки
    «вернувшийся» с итогом или честным «не покрыт», и релиз не уходит; в поставке лежит шаблон
    `.kaif/_door-report-template.md` с таблицей контекстов и tool-модуль `.kaif/tools/kaif-door-lint.mjs`
    (`check` / `selftest`; чужой документ — SKIPPED), свод полигона гоняет его на развёрнутой копии.
  - Проверка. Всё из A плюс `node .kaif/tools/kaif-door-lint.mjs selftest` → зелёный, красный на фикстуре
    отчёта #52 (две строки: под сессией 24/0, гостевой 81/81, строки «вернувшийся» нет); `npm run test:core`
    → `all N suites green`; цена — ещё один чат в 2.6 (релиз позже на ≈ 1 чат).
- **C)** Канон-правило класса — в 2.7 (`ideas/29`); 2.6 выходит без него.
  - Ситуация. Проект поля уже починил себя локально (проход вернувшегося в своей двери, страж щита, e2e
    корня); остальные проекты на KAIF живут с двумя контекстами гейта 6.
  - Действие. Агент обновляет проект до 2.6.
  - Результат. Вы видите прежний гейт 6; правило класса приезжает следующей версией; релиз 2.6 не
    сдвигается; риск — тот же класс у другого вашего проекта до 2.7 (у соседа было шесть инцидентов за
    семь недель).
  - Проверка. `grep -c "returning" framework/TESTING_FRAMEWORK.md` → `0` в 2.6; п. 30 `ideas/28` → строка
    переноса в `ideas/29`; ответ в #52 после релиза: «класс понят (`researches/29`), правило приедет 2.7».
- **D)** Свой вариант — впишите, что именно (форма, версия, имя).

**Answer:**

## Proposed implementation plan (after answers)

- **A:** строка эпика «VC «Контекст посетителя»» между WN и CB в `plans/89` + критерий 16 сценарием
  (по этому Q1); операционный план — на закрытии WN (канон N+1); §7 `MASTER_PLAN` — решение; п. 30
  `ideas/28` — адрес.
- **B:** то же + в плане шаги «шаблон отчёта двери» и «`kaif-door-lint` + свод»; оценка ≈ 1 чат.
- **C:** п. 30 `ideas/28` → строка в `ideas/29`; §7 — решение; черновик ответа #52 — секцией `plans/89`.
- **D:** по вашему слову.
