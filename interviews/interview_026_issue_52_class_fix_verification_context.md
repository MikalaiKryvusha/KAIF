# Interview #026 — класс #52 «дверь зелёная — реальный пользователь видит дефект»: чем и когда чинить КЛАСС в KAIF

> Topic: разведка класса по вашему слову (интервью №025, №103: «Разведка. Пойми источник проблемы как
> класса, и как ее как класс чинить в KAIF») снята — `researches/29`. Источник класса: агент проверяет
> продукт в СТЕРИЛЬНОМ контексте, который строит из ничего (свежий браузер без маркера и памяти, чистый
> чекаут, пустая база, «сегодняшняя» сборка), а настоящий посетитель приходит С ИСТОРИЕЙ (маркер сессии,
> память темы/языка, кеш прошлой сборки, вкладка, открытая через выкат). Дефект живёт в разности этих
> контекстов, и все приборы агента зелены по построению. У соседа на диске — шесть инцидентов класса за
> семь недель, четыре нашли вы сами через минуты после выката. Индустрия закрывает класс не «ещё одним
> смоуком», а ЯВНЫМ перечнем контекстов посетителя, который прибор обязан пройти (файл состояния ·
> тёплый проход · «видно» по цели клика или кадру, а не по DOM · заголовки кеша). Вопрос — форма и версия
> фикса класса в KAIF; кодируется только по вашему слову.
> Source of the idea: issue #52 + п. 30 `ideas/28` + `researches/29` (2026-09-05 22:49 +03:00, сессия 56).
> Status: **🟡 awaiting the owner's answers — раунд 3 (Q3, 2026-09-05 23:2x +03:00)**; раунд 1 (Q1) отклонён комментарием
> 22:58 («терминология проекта NDim»), раунд 2 (Q2) — комментарием 23:11 («я так и не понял, в чем заключается КЛАСС
> проблемы, без привязки к проекту NDim»); Q1/Q2 остаются открытыми как память отказов, отвечать на них не нужно.
> Created 2026-09-05 22:55 +03:00 (сессия 56; поднято страницей контура через отгружаемый генератор — эпик
> IC закрыт тем же вечером).

## Context / what I already found in the code

- Канон KAIF о проверке «как пользователь» — гейт 6 `TESTING_FRAMEWORK.md` обоих слоёв: «войди той
  дверью, которую продукт предлагает… если у продукта есть аутентифицированное состояние,
  неаутентифицированный смоук — НЕ улика о продукте». Он называет два контекста — гостя и вошедшего — и
  молчит о вернувшемся (с маркером, памятью, кешем) и о вкладке, открытой через выкат. Слов «returning»,
  «возвращающ», «storageState», «кеш прошлой сборки» в `framework/` нет (греп → 0).
- Разведка класса (`researches/29`): §2 — источник (три корня поведения агента: стерильность как
  воспроизводимость · агент всегда «новый посетитель» · «отрисовано» судится присутствием в DOM, а не
  видимостью); §3 — шесть инцидентов одного проекта (`bugs/10` · `40` · `109` · `124` · `162` · #52),
  каждый чинился прибором ПОД СВОЁ состояние; §4 — индустрия: storageState/persona-фикстуры, cold/warm,
  `toBeVisible` слеп к перекрытию (только hit-target клика или кадр), cache-busting для «тех, кто уже
  здесь»; §5 — обобщаемое: К1 матрица контекстов посетителя как явный вход любой проверки, К2 «видно» =
  цель клика/кадр, К3 снятие того, на чём стояли другие, — с грепа «кто на нём стоял», К4 прибор под
  состояние — отдельный и дешёвый, К5 охота судьи. Проектное (`csr`, `onMount`, `data-booting`,
  `firebase.json`) в канон не едет.
- Версия 2.6 сейчас: закрыто 9 из 15 критериев; впереди WN (`plans/94`) → CB → HY → CR → RL, ≈ 5–6 чатов.

## QUESTIONS

### Q1. Чем и когда чинить КЛАСС «контекст проверки ≠ контекст посетителя» в KAIF?

**Адресат ответа:** `plans/89` (при A/B — критерий 16 сценарием и строка эпика между WN и CB; при C —
строка переноса) · `ideas/28` п. 30 · `ideas/29` (при C) · `MASTER_PLAN.md` §7 (журнал решений) ·
`researches/29` §6 · ответ в issue #52 после релиза (№84/№92).

**Происхождение:** ваше слово 2026-09-05 20:53 +03:00 (№103) отклонило мои варианты под один инцидент и
велело сначала понять класс. Разведка снята (`researches/29`); ниже — варианты формы фикса КЛАССА, каждый
сценарием того, что вы увидите. Рекомендация помечена.

**Разведка:** источник класса — три корня поведения агента (`researches/29` §2); индустрия держит класс
явным перечнем контекстов посетителя, а не количеством проверок (§4); в каноне KAIF есть половина этого
перечня (гость · вошедший — гейт 6), нет второй половины (вернувшийся · открытая вкладка) и нет правила
«видно, а не в DOM» (§5).

- **A) (рекомендация)** Канон-правило класса в 2.6 — текстом, без машинерии (≈ 0,5 чата).
  - Ситуация. Ваш проект на KAIF 2.6 выкатывает новую главную; у каждого настоящего посетителя в браузере
    маркер сессии, память темы и кеш прошлой сборки.
  - Действие. Агент прогоняет дверь выката и пишет отчёт перед словом «задеплоено».
  - Результат. Вы видите в отчёте двери ТАБЛИЦУ КОНТЕКСТОВ посетителя — «первый визит · вошедший ·
    вернувшийся (маркер + память + кеш) · вкладка через выкат» — и у каждой строки либо «прибор прошёл С
    этим состоянием: видно за 3 с / увёл внутрь», либо честное «не покрыт»; страница под непрозрачным
    щитом считается НЕ отрисованной; в `TESTING_FRAMEWORK.md` проекта гейт 6 называет четыре контекста и
    правило «отрисовано = видно (цель клика или кадр), а не есть в DOM»; снятие увода/обработчика в
    fable-цикле `AGENT_GUIDE` начинается с грепа «кто на нём стоял»; судья ловит отчёт двери без таблицы.
  - Проверка. После обновления до 2.6: `grep -c "вернувш\|returning" TESTING_FRAMEWORK.md` → `≥ 1`;
    в истоке `grep -c "returning" framework/TESTING_FRAMEWORK.md` → `≥ 1`; охота «green door blind to the
    returning visitor» в обеих копиях `/fable-judge`; policy/notes 2.6; `plans/89` — критерий 16 ✅.
- **B)** Канон-правило (как в A) ПЛЮС машинная половина в 2.6: шаблон отчёта двери и линт (≈ 1 чат).
  - Ситуация. Та же, что в A; агент под давлением релиза склонен пропустить строку таблицы.
  - Действие. Агент пишет отчёт двери по шаблону поставки и прогоняет `node .kaif/tools/kaif-door-lint.mjs
    check <отчёт.md>` в двери выката.
  - Результат. Вы видите тот же отчёт, что в A, — но дверь КРАСНАЯ, пока в таблице нет строки
    «вернувшийся» с итогом или честным «не покрыт», и релиз не уходит; в поставке лежит шаблон
    `.kaif/_door-report-template.md` с таблицей контекстов и tool-модуль `.kaif/tools/kaif-door-lint.mjs`
    (`check` / `selftest`; чужой документ — SKIPPED), свод полигона гоняет его на развёрнутой копии.
  - Проверка. Всё из A плюс `node .kaif/tools/kaif-door-lint.mjs selftest` → зелёный, красный на фикстуре
    отчёта #52 (две строки: под сессией 24/0, гостевой 81/81, строки «вернувшийся» нет); `npm run test:core`
    → `all N suites green`; цена — ещё один чат в 2.6 (релиз позже на ≈ 1 чат).
- **C)** Канон-правило класса — в 2.7 (`ideas/29`); 2.6 выходит без него.
  - Ситуация. Проект поля уже починил себя локально (проход вернувшегося в своей двери, страж щита, e2e
    корня); остальные проекты на KAIF живут с двумя контекстами гейта 6.
  - Действие. Агент обновляет проект до 2.6.
  - Результат. Вы видите прежний гейт 6; правило класса приезжает следующей версией; релиз 2.6 не
    сдвигается; риск — тот же класс у другого вашего проекта до 2.7 (у соседа было шесть инцидентов за
    семь недель).
  - Проверка. `grep -c "returning" framework/TESTING_FRAMEWORK.md` → `0` в 2.6; п. 30 `ideas/28` → строка
    переноса в `ideas/29`; ответ в #52 после релиза: «класс понят (`researches/29`), правило приедет 2.7».
- **D)** Свой вариант — впишите, что именно (форма, версия, имя).

**Answer:**


**Комментарий владельца (5 сентября 2026, 22:58 (+03:00)):** гость · вошедший — гейт 6), нет второй половины (вернувшийся · открытая вкладка - это все опять терминалогия проекта NDim. Ты опять дрочишь на проект веб-приложения. ты чинишь болезнь одного проекта, а не КЛАСС проблемы. Как это применимо к KAGO? Как это применимо к Unliminium?

ты не понял проблему, как класс. <!-- owner-review: by Mikalai Kryvusha (KOT KRINIK) · 5 сентября 2026, 22:58 (+03:00) -->

### Q2. Раунд 2 — класс «СТЕНД АГЕНТА ≠ БОЙ ПОТРЕБИТЕЛЯ»: чем и когда чинить его в KAIF?

**Адресат ответа:** `plans/89` (при A/B — критерий 16 сценарием и строка эпика между WN и CB; при C —
строка переноса) · `ideas/28` п. 30 · `ideas/29` (при C) · `MASTER_PLAN.md` §7 · `researches/29` §6 ·
`framework/TESTING_FRAMEWORK.md` (при A/B) · ответ в issue #52 после релиза.

**Происхождение:** ваш комментарий к Q1 (22:58): «терминология проекта NDim… как это применимо к KAGO? к
Unliminium? ты не понял проблему, как класс». Класс переформулирован без слов какой-либо сферы и проверен на
трёх ваших проектах разных сфер (`researches/29` §2–§3, 23:05).

**Разведка:** класс — свойство АГЕНТА: он доказывает «готово» на СТЕНДЕ, который построил сам и из нуля
(свежий каталог, чистый чекаут, виртуальное устройство, песочница, свой маршрут), а потребитель живёт в
БОЮ, который построил мир: с накопленным состоянием, в боевой форме данных, на реальной машине, реальным
маршрутом. Стенд зелен по построению — у него нет состояния, в котором живёт дефект, — и агент красит
зелёным бой, которого не видел. Четыре семейства разности: накопленное состояние · боевая форма · условия
машины · реальный маршрут. Шестнадцать инцидентов в трёх проектах: **KAGO** (стенд видеокарты) — sweep мерил
поверх уже применённого профиля, не проверив заводское состояние карты (`bugs/45`); один профиль писал
разную кривую по состоянию карты в момент нажатия (`bugs/97`); репетиционный стенд игнорировал боевую форму
записи (`bugs/26`); пульс телеметрии уносился кэшем ОС ровно когда машина умирала (`bugs/37`); живая развёртка
требовала от боевого журнала быть песочницей (`bugs/13`). **KAIF** — репетиция обновления только на маршруте
`update`, поле ходит bootstrap (#42 ×3, эпик UR); `update` пишет LF в CRLF-дерево потребителя (тикет 06 из
KAGO); синтетический «старый релиз» в своде вместо реального 2.5. **ndim** — сессия, кеш, форма данных,
маршрут по ссылке (шесть). Unliminium на этом диске нет — сказано честно; класс не зависит от продукта.
Индустрия чинит его одинаково во всех сферах: состояние боя — явный вход стенда, «свежий» и «накопленный»
— два прохода, судится исход потребителя, а не структура стенда (§4).

- **A) (рекомендация)** Закон разности стенда и боя — в канон 2.6 текстом (≈ 0,5 чата).
  - Ситуация. Три ваших проекта на KAIF 2.6: стенд видеокарты перед живой записью кривой; фреймворк перед
    словом «обновление доставлено»; веб-приложение перед словом «задеплоено».
  - Действие. Агент каждого проекта пишет отчёт о «готово» перед тем, как красить зелёным.
  - Результат. В каждом отчёте вы видите одну обязательную строку `СТЕНД ↔ БОЙ:` по четырём семействам —
    например у KAGO: «накопленное: карта в заводском состоянии — проверено до замера; боевая форма:
    запись в реальную карту — НЕ ПОКРЫТ, ждёт вас у машины; машина: смерть под нагрузкой — телеметрия
    пишется без кэша; маршрут: живой журнал, не песочница — прогнано»; у фреймворка: «накопленное:
    обновление поверх дерева с правками и CRLF — прогнано; маршрут: bootstrap, не только update —
    прогнано»; у веба: «накопленное: сидированная сессия и кеш прошлой сборки — прогнано». Зелёное без
    этой строки в `TESTING_FRAMEWORK.md` любого проекта — не улика о бое; «свежий» и «накопленный» — два
    обязательных прохода; результат судится исходом (кривая прочитана обратно, файл прочитан обновлённым
    процессом, действие прошло по цели), не структурой стенда; судья ловит отчёт без строки.
  - Проверка. После обновления до 2.6: `grep -c "СТЕНД ↔ БОЙ\|STAND ↔ FIELD" TESTING_FRAMEWORK.md` → `≥ 1`
    в любом проекте (RU/EN); в истоке `grep -c "STAND ↔ FIELD" framework/TESTING_FRAMEWORK.md` → `≥ 1`;
    охота «stand green, field unseen» в обеих копиях `/fable-judge`; policy/notes 2.6; `plans/89` —
    критерий 16 ✅; релиз 2.6 позже на ≈ 0,5 чата.
- **B)** Закон (как в A) ПЛЮС машинная половина в 2.6: линт строки разности (≈ 1 чат).
  - Ситуация. Те же три проекта; агент под давлением релиза склонен пропустить строку.
  - Действие. Агент прогоняет `node .kaif/tools/kaif-stand-lint.mjs check <отчёт.md>` в двери выката /
    закрытия сессии.
  - Результат. Всё из A — и дверь КРАСНАЯ, пока в отчёте нет строки `СТЕНД ↔ БОЙ:` с четырьмя семействами,
    у каждого — «прогнано» / «наблюдено в бою» / «НЕ ПОКРЫТ»; чужой документ — SKIPPED; свод полигона гоняет
    линт на развёрнутой копии; фикстура #52 (два зелёных смоука без строки) — красная.
  - Проверка. Всё из A плюс `node .kaif/tools/kaif-stand-lint.mjs selftest` → зелёный; `npm run test:core`
    → `all N suites green`; релиз 2.6 позже на ≈ 1 чат.
- **C)** Закон разности — в 2.7 (`ideas/29`); 2.6 выходит без него.
  - Ситуация. Три проекта живут своими местными лечениями (KAGO проверяет заводское состояние карты, ndim
    сидирует сессию, KAIF гоняет `s22 C`).
  - Действие. Агент обновляет проект до 2.6.
  - Результат. Вы видите прежний канон (гейт 6 — два контекста для веба, гейт 7 — смесь сборок); общего
    закона нет до 2.7; релиз 2.6 не сдвигается; риск — тот же класс в любом проекте до следующей версии
    (шестнадцать инцидентов трёх проектов за два месяца).
  - Проверка. `grep -c "STAND ↔ FIELD" framework/TESTING_FRAMEWORK.md` → `0` в 2.6; п. 30 `ideas/28` → строка
    в `ideas/29`; ответ в #52 после релиза: «класс понят (`researches/29`), закон приедет 2.7».
- **D)** Свой вариант — впишите, что именно (форма, версия, имя закона).

**Answer:**


**Комментарий владельца (5 сентября 2026, 23:11 (+03:00)):** я так и не понял,  в чем заключается КЛАСС проблемы, без привязки к проекту NDim <!-- owner-review: by Mikalai Kryvusha (KOT KRINIK) · 5 сентября 2026, 23:11 (+03:00) -->

### Q3. Раунд 3 — класс одним абзацем, без слов какого-либо проекта. Так ли я его понял?

**Адресат ответа:** `researches/29` §2 (формулировка класса — по вашему слову) · `plans/89` (при A —
критерий 16 и строка эпика; при B — строка переноса) · `ideas/28` п. 30 · `MASTER_PLAN.md` §7.

**Происхождение:** ваш комментарий к Q2 (23:11): «я так и не понял, в чем заключается КЛАСС проблемы, без
привязки к проекту NDim». Ниже класс сказан три раза короче и без терминов; примеры — из трёх ваших
проектов, NDim — последним.

**Разведка:** класс снят с шестнадцати инцидентов трёх проектов трёх сфер (`researches/29` §3) и с четырёх
общих принципов индустрии (§4); здесь — только его суть.

**Класс — три предложения.** ИИ-агент проверяет свою работу в мире, который он сам построил для проверки:
чистом, новом, пустом — и в этом мире всё зелёное. Человек пользуется результатом в мире, который
существовал ДО агента: с накопленной историей, со своими настоящими данными, на своей технике, своими
привычными путями. Агент говорит «готово» по зелёному в своём мире и не смотрит, как это выглядит в мире
человека, — а дефект живёт ровно в разнице между этими двумя мирами.

**Как это выглядело в трёх ваших проектах:**

| Проект | Мир агента — где было зелёное | Мир владельца — где жил дефект |
|---|---|---|
| **KAGO** (видеокарта) | агент мерил карту так, будто она в заводском состоянии, как на чистом стенде | на вашей карте уже стоял применённый профиль: замеры и запись кривой шли поверх него, один профиль писал разные кривые (`bugs/45`, `bugs/97`) |
| **KAIF** (фреймворк) | обновление проверено на чистой свежей копии проекта, репетиция — маршрутом самого агента | в ваших проектах в деревьях свои правки и CRLF — обновление ломало местные стражи; поле ходило другим маршрутом (bootstrap), где репетиции не было вовсе (#42 ×3, тикет 06) |
| **NDim** (сайт) | сайт проверен в новом браузере без истории | у вас в браузере старая сессия — вечная «Загрузка» (#52) |
| **Unliminium** | на этом диске проекта нет — примера не выдумываю | тот же класс проявится там, где агент проверяет «на чистом», а вы работаете «на своём» |

**Что чинится в KAIF как класс** (правило поведения агента, одно для сайта, видеокарты, фреймворка и любого
другого проекта): перед словом «готово» агент обязан НАЗВАТЬ, чем мир человека отличается от его стенда —
что там уже накоплено, какие там настоящие данные и техника, каким путём ходит человек, — и либо повторить
проверку С ЭТИМ состоянием, либо честно написать «в мире человека не проверял». Зелёное без этой строки —
не улика.

- **A) (рекомендация)** Да, класс понят верно — чинить правилом в каноне 2.6 (≈ 0,5 чата).
  - Ситуация. Любой ваш проект на KAIF 2.6; агент закончил работу и собирается сказать «готово».
  - Действие. Агент пишет отчёт о готовности.
  - Результат. Вы видите в отчёте одну строку «мир человека ≠ стенд агента: накоплено — …; данные и
    техника — …; путь человека — …» с пометками «проверено с этим состоянием» или «не проверял»; в
    `TESTING_FRAMEWORK.md` проекта это правило стоит над всеми проверками «как пользователь», а судья
    ловит отчёт без строки.
  - Проверка. `grep -c "мир человека\|the human's world" TESTING_FRAMEWORK.md` → `≥ 1` после обновления;
    охота в обеих копиях `/fable-judge`; `plans/89` критерий 16 ✅; релиз 2.6 позже на ≈ 0,5 чата.
- **B)** Да, класс понят верно, но правило — в 2.7; 2.6 выходит без него.
  - Ситуация. Те же проекты; каждый живёт своим местным лечением.
  - Действие. Агент обновляет проект до 2.6.
  - Результат. Вы видите прежний канон; правило приезжает следующей версией; релиз 2.6 не сдвигается.
  - Проверка. `grep -c "the human's world" framework/TESTING_FRAMEWORK.md` → `0` в 2.6; строка в `ideas/29`.
- **C)** Нет, класс другой — впишите своими словами, в чём он; я перепишу разведдок по вашему слову.
  - Ситуация. Три абзаца выше названы неверно.
  - Действие. Вы пишете в поле ответа, в чём класс на самом деле.
  - Результат. `researches/29` §2 переписан вашими словами, следующий вопрос — по нему.
  - Проверка. Ваши слова дословно в §2 `researches/29` и в `MASTER_PLAN.md` §7.
- **D)** Свой вариант.

**Answer:**


**Комментарий владельца (5 сентября 2026, 23:16 (+03:00)):** сократи описание проблемы в 10 раз, простым поведенческим описанием, чтобы я понял, что это за класса проблемы, и как ты спрашиваешь у меня, как можем починить <!-- owner-review: by Mikalai Kryvusha (KOT KRINIK) · 5 сентября 2026, 23:16 (+03:00) -->

## Proposed implementation plan (after answers)

- **A:** строка эпика «VC «Контекст посетителя»» между WN и CB в `plans/89` + критерий 16 сценарием
  (по этому Q1); операционный план — на закрытии WN (канон N+1); §7 `MASTER_PLAN` — решение; п. 30
  `ideas/28` — адрес.
- **B:** то же + в плане шаги «шаблон отчёта двери» и «`kaif-door-lint` + свод»; оценка ≈ 1 чат.
- **C:** п. 30 `ideas/28` → строка в `ideas/29`; §7 — решение; черновик ответа #52 — секцией `plans/89`.
- **D:** по вашему слову.
