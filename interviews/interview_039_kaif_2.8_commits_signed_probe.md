# Interview #039 — 31 коммит в репозитории KAIF подписан не вами, а тестовым именем «probe»: что сделать с историей

> Topic: этой ночью (26 сентября) судья нашёл, что с 25 сентября 17:27 все коммиты KAIF — 31 штука, уже на GitHub — подписаны
> автором «probe <probe@example.invalid>» вместо вас. Причина найдена и устранена, дальше коммиты снова идут от вашего имени и
> проверяются стражем. Осталось решить судьбу уже опубликованных 31 коммита — это ваша история, поэтому решение ваше.
> Source of the idea: находка повторного судьи эпика «обновление ничего не теряет молча» вне предмета суда, ≈ 2026-09-26 02:55 +03:00;
> разбор — `bugs/124_repo_identity_rewritten_by_probe_worktree.md`.
> Status: ⏳ ЖДЁТ ОТВЕТА (заведено ночью, владелец спит; поднять страницей на первом шаге следующего чата).
> Created 2026-09-26 03:12 +03:00 (сессия 74, Claude Opus 5.5).

## Context / what I already found

- В журнале коммитов с 25 сентября 17:27 по 26 сентября 02:55 автор — «probe»; ваш последний подписанный коммит — 25 сентября 16:46.
- Причина: тестовая проверка записала своё имя в общие настройки репозитория и не убрала его. Имя снято, проверка переписана, а
  инструмент коммита теперь отказывает коммиту под тестовым именем, прежде чем тот уйдёт на GitHub.
- Рядом стоит ваш прежний долг того же рода: в истории git остались имена полевых проектов (строка «Судьба приватных имён…» в
  `STATUS.md`) — переписывание истории может закрыть оба сразу.

## QUESTIONS

### Q1. Что сделать с 31 коммитом, подписанным «probe»?

**Адресат ответа:** `bugs/124` (решение и закрытие), при переписывании — строка `AUTH:` в работе агента и журнал решений `MASTER_PLAN.md` §7.

**Происхождение:** ваше решение №54 (интервью №012, Q1 = B): «по `git log` видно, кто что делал» — поэтому подпись коммита значима.

<!-- archaeology: search "автор|коммит|probe|mailm|переписа|истор" → 905 hits; search "mailm|force|push" → 84 hits · read: the hits in interviews/ (012 — the trailer carries the model's name, №54) and STATUS.md · prior: unrelated — the hits are about the trailer's model name (№54), the handover wording and the private names in history (STATUS owner debt, force-push waiting for an AUTH: line); no answer on WHO signs the commits -->

- **A)** Переписать историю — коммиты снова подписаны вами.
  - Ситуация. На GitHub 31 коммит KAIF подписан «probe».
  - Действие. Агент переписывает автора этих коммитов на ваше имя и принудительно отправляет историю на GitHub.
  - Результат. В журнале коммитов везде ваше имя; у всех, кто успел скачать репозиторий, история разойдётся с GitHub, и им
    придётся скачать её заново; ссылки на старые номера коммитов в документах перестанут открываться.
  - Проверка. `git log --format='%an' 7bc3266..HEAD | sort | uniq -c` печатает только ваше имя.
- **B) (рекомендация `[ИИ]`)** Оставить коммиты как есть, но показывать в журнале ваше имя.
  - Ситуация. Та же.
  - Действие. Агент добавляет в репозиторий файл сопоставления имён: «probe» показывается как вы.
  - Результат. `git log` и `git shortlog` показывают ваше имя; сами коммиты и их номера не меняются, у скачавших ничего не ломается;
    на странице GitHub коммиты по-прежнему подписаны «probe».
  - Проверка. `git log --use-mailmap --format='%aN' 7bc3266..HEAD | sort | uniq -c` печатает только ваше имя.
- **C)** Ничего не менять — случай остаётся записанным в разборе.
  - Ситуация. Та же.
  - Действие. Агент оставляет историю и закрывает разбор случая записью о вашем решении.
  - Результат. В журнале 31 коммит остаётся подписан «probe»; в разборе случая записано, почему так вышло и что теперь это ловит.
  - Проверка. `git log --format='%an' 7bc3266..HEAD | sort | uniq -c` печатает «31 probe» и более поздние коммиты с вашим именем.
- **D)** Свой вариант — впишите.

**Answer:**

## Links

`bugs/124_repo_identity_rewritten_by_probe_worktree.md` · `tools/commit.mjs` (преполёт 0) · `STATUS.md` → «ДОЛГИ НА ВЛАДЕЛЬЦЕ» ·
`interviews/interview_012_*.md` (решение №54)
