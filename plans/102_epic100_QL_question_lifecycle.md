# План 102 — эпик QL «Жизнь вопроса до конца»: факт «внесено» · проверка до зова · живое сверху · кнопка под рукой

> **Создан:** ≈ 2026-09-12 01:05 +03:00 (закрытие эпика AW — канон N+1, №43; сессия 58).
> **Родитель:** `plans/100` (эпик QL — строка 2 таблицы эпиков; критерии приёмки версии 3–6); источники —
> issues #54, #56, #58, #60 (Unliminium, 8–10 сентября) + `researches/30` §2 (сверка с HEAD 2.6: #56
> воспроизведён пробой на истоке; кнопка внизу окна — в нашем генераторе) + EXP-0123.
> **Статус:** ✅ закрыт 2026-09-12 10:02 +03:00 (сессия 59 — один чат: QL0–QL6; четыре коммита 950c9c7 · 1f9534a · 6e166cb · e2917a9 + закрытие; судья — § «Решения» 9; полигон 23 свода, verify-contour 190/190); план создан ≈ 2026-09-12 01:05 +03:00 — вопросов
> владельцу нет (критерии заданы сценариями в `plans/100`; форма кнопки — его слово в #60 дословно:
> «Кнопка [Сохранить ответы] - делаем её FAB вверху справа»).
> **Вовне:** поставка — `framework/tools/contour/{core,review,texts}.mjs` (генератор), спецификация
> `framework/templates/_interactive-contour-spec.md` (новые статьи), `/interview` 3c и `/owner-reviews`
> обоих слоёв (дверь `--check`, четвёртый факт, три исхода, вид для чтения), `framework/AGENT_GUIDE.md`
> «Показ — это действие» + корневой; свод `s22`; записи 2.7 в сборщике; черновики ответов #54/#56/#58/#60.

## Вектор цели (Achieve)

**Боль** (`plans/100` вектор, пункт (б); слова владельца дословно из тикетов): «ты принёс СТАРЫЙ
ВОПРОС… КОТОРЫЙ ТЫ ЖЕ УЖЕ ПОЧИНИЛ!!!» (#54 — у вопроса нет факта «внесено», очередь изготавливает ложное
второе решение); «я открываю — а там ОГРОМНОЕ КОЛИЧЕСТВО ТЕКСТА» (#54 — 18 535 знаков решённого над
живым вопросом); «твой интерактивный контур сломан», «ты заебал» (#56 — единственная проверка документа
— запуск показа, а показ и есть зов; исток сам позвал владельца голосом в полночь, EXP-0123); «я написал
ответы, а нажать кнопку не могу» и «Кнопка [Сохранить ответы] - делаем её FAB вверху справа» (#60 —
`review.mjs:598` `.bar { position:fixed; bottom:0 }` стоит и в нашем генераторе); «Я вычитал.
Комментировать мне нечего. Нажимаю кнопку - не закрывает» (#58 — закрыто поставкой 2.6, канон исход не
называет).

**Где хотим оказаться.** У вопроса четыре факта — есть · показан · отвечен · **внесено** (куда и когда);
очередь отказывается поднимать внесённое и говорит об этом агенту; документ проверяется дверью `--check`
без страницы, звука и зова, и страница, узнавшая часть блоков, говорит это вслух; на странице живые
вопросы сверху, решённое и хвост — одной свёрткой ниже (ни одна строка не удаляется); кнопка записи
ответов — плавающая справа сверху, видна при любой прокрутке и высоте окна, нижняя панель запрещена
статьёй спецификации; самопроверка судит РЕНДЕР (кнопка в первом экране, подписи без утечки разметки);
канон называет три законных исхода показа — ответ · пометка · «прочитано, замечаний нет» — и правило:
документ для владельца имеет вид для чтения.

**Метрика эпика:** критерии 3–6 `plans/100` закрыты 4 из 4 (2026-09-12 10:02 +03:00).

## Готово, когда (критерии приёмки — сценариями; якорь `plans/100` критерии 3–6)

1. **[QL — внесено]** — сценарий `plans/100` критерий 3 дословно. *Проверка:* `s22` — три ассерта
   (запись в `implemented.json` · отказ очереди с именем файла · строка нарушения у истока), зелёные на 2.7 и
   `✖` на генераторе 2.6 (шов — копия `git show v2.6:framework/tools/contour/*.mjs`); `node
   tools/questions-guard.mjs --selftest` — ось G9 красная на фикстуре из #54.
2. **[QL — проверка до зова]** — сценарий `plans/100` критерий 4 дословно. *Проверка:* `s22` — ассерт
   `--check`: вывод несёт «blocks N, recognised M: …» и НЕ несёт `Page is up` / `CALL:`, код 3 на
   негодном и 0 на годном; `grep -n -- "--check" framework/skills/interview/SKILL.md
   .claude/skills/interview/SKILL.md` → по `≥ 1`, `--no-open` в шаге 3c больше не называется предполётом.
3. **[QL — живое сверху, три исхода]** — сценарий `plans/100` критерий 5 дословно. *Проверка:* `s22` —
   на фикстуре из #54 (пять отвеченных с таблицами + один живой) текст над живым вопросом ≤ 1 % длины
   страницы; `grep -c "<details" framework/tools/contour/review.mjs` → `≥ 1`; греп трёх исходов в
   `/owner-reviews` и `AGENT_GUIDE` обоих слоёв; `node tools/verify-contour.mjs` в живом браузере зелёный,
   эталон пересмотрен глазами.
4. **[QL — кнопка под рукой]** — сценарий `plans/100` критерий 6 дословно. *Проверка:* в CSS отрендеренной
   страницы `bottom:0` нет и класса `bar` нет (`node tools/review.mjs <док> --no-serve` → HTML под `.kaif/.contour-tmp/`;
   единственные вхождения `bottom:0` в модуле — самопроверка и её мутации; критерий переписан по каверзе судьи 1 —
   счёт грепа по модулю предсказывал `0`, факт `3`); селфтест генератора — ассерт рендера (кнопка с
   `position:fixed` и `top:` в HTML, подписи вариантов без `**`); `verify-contour` живой прогон при 560 и
   1400 px; статья спецификации о запрете нижней панели — греп.
5. **[судья эпика]** — лёгкий `/fable-judge` по критериям 1–4; на закрытии — план TR `plans/104` (N+1) и
   веха в `plans/100` (строка 2, критерии 3–6 ✅).

## Шаги

- [x] **QL0 — нулёвка актуализации входов** ✅ ≈ 2026-09-12 09:25 +03:00 (все адреса сошлись с HEAD; правок плана не потребовалось). Сверить против HEAD: `framework/tools/contour/review.mjs`
      (`.bar` :598 · `opts.open`/`--no-open` :1086 · самопроверка радиогрупп :481–489, :689, :929 ·
      `pendingDocs` :212–251 · рендер отвеченных :504 · запись решения `recordDecision` · `shown.json`),
      `core.mjs` (`parseQuestions` :168 · `preflight` :274 · `docStatus` :139), `texts.mjs` (EN/RU словари),
      спецификация `_interactive-contour-spec.md` (§2 предполёт, §3 записи, §5 исходы), `tools/questions-guard.mjs`
      (оси G4–G8; место для G9), `tools/sandbox/s22-contour-shipped.mjs`, `tools/verify-contour.mjs` +
      эталон; `/interview` 3c и `/owner-reviews` I40–I43 обоих слоёв. Правка плана по фактам — штатное
      действие (№76). `INTENT:` перед первой правкой поведения; `FORK:` — на форме факта «внесено»
      (файл рядом с `shown.json` против строки в шапке документа) с разведкой: как это делают трекеры
      (состояние `implemented`/`released` у тикета — отдельное поле, не текст).
- [x] **QL1 — дверь `--check <док>`** ✅ 2026-09-12 09:40 +03:00 (`checkForm` в core.mjs · `checkDoc`/`--check` в review.mjs · тексты EN/RU · спецификация §2 · /interview 3c обоих слоёв · селфтест 53 · s22 +2; живой прогон: `node tools/review.mjs interviews/interview_031_kaif_2.7_scope.md --check` → «блоков 2, узнано 2: Q1, Q2 … проверка OK», код 0, без зова) (#56, EXP-0123): в `review.mjs` новая команда — разбор
      документа `parseQuestions` + `preflight` + счёт блоков-кандидатов (заголовки `##`/`###` с буквой
      или `Q\d`), вывод «blocks N, recognised M: Q1, Q2; not recognised: А (`##` heading is not a
      question)», код 3 при отказах предполёта / нераспознанных кандидатах, 0 при чистом; НИКАКОГО
      сервера, звука, зова, факта показа; тексты EN/RU в `texts.mjs`; `/interview` 3c обоих слоёв → `--check`
      (строка про `--no-open` — «поднять без окна, зов остаётся» — переписана); при показе страница,
      узнавшая часть кандидатов, печатает это вслух (`log`) и в шапке страницы; селфтест генератора +
      `s22` (ассерт «нет `Page is up`/`CALL:`»); `--no-open` остаётся, но справка называет его честно.
- [x] **QL2 — четвёртый факт «внесено»** ✅ 2026-09-12 09:47 +03:00 (`implemented.json` · `--mark-implemented <док> <Q> --where` · отказ очереди/показа кодом 2 · карточка «внесено → адрес» · спецификация §3 · I44/I45 и шаг 5 обоих слоёв · ось G9 стража + 2 мутации · селфтест 57 · s22 +4) (#54): `interviews/decisions/implemented.json` рядом с
      `shown.json` — `{ "<док>": { "Q1": { "at": ISO, "where": "<коммит|файл>" } } }`; команда
      `--mark-implemented <док> <Q> --where <ссылка>`; очередь (`--queue`, `--queue --list`, показ
      документа): документ, у которого все открытые вопросы внесены, — НЕ ПОДНИМАЕТСЯ, печатается
      «внесено, но открыто: <док> Q1 → закрой статус» и код 2 (гейт, как I41/I42); спецификация §3 —
      четыре записи; `/owner-reviews` — инварианты I44 (факт «внесено» пишется агентом в момент внесения,
      с адресом) и I45 (очередь отказывает поднимать внесённое); `/interview` шаг 5 — «внёс → пометь»;
      у истока `questions-guard` ось G9: «отвечено ≥ 3 дней и не внесено» — справка; «внесено, но
      статус ждёт» — нарушение; селфтест обеих осей; `s22` три ассерта + красный на генераторе 2.6.
- [x] **QL3 — живое сверху, решённое свёрнуто; три исхода в каноне** ✅ 2026-09-12 09:51 +03:00 (вид для чтения: живые карточки первыми, проза + отвеченные — одной `<details class="archive">`, ничего не удалено; `head.archive` EN/RU; спецификация §4; AGENT_GUIDE обоих слоёв — абзац «три исхода + вид для чтения»; /owner-reviews I16 обоих слоёв; селфтест 60; s22 — фикстура #54: над живым Q6 7 знаков из 1123) (#54, #58): рендер страницы —
      живые вопросы (с предшествующей им прозой) первыми, всё отвеченное и хвост документа — одной
      `<details>` в исходном порядке («архив решённого — N вопросов»), ни одна строка не удалена;
      спецификация — статья «вид для чтения»; `/owner-reviews` + `AGENT_GUIDE` «Показ — это действие»
      обоих слоёв — три законных исхода показа (ответ · пометка · «прочитано, замечаний нет» — уже в
      генераторе с 2.6, `bugs/113`) и правило «документ для владельца имеет вид для чтения» (та же
      дисциплина, что STATUS ↔ летопись); `s22` ассерт ≤ 1 % над живым на фикстуре из #54; эталон
      `verify-contour` пересмотрен глазами.
- [x] **QL4 — плавающая кнопка справа сверху и самопроверка рендера** ✅ 2026-09-12 09:55 +03:00 (`.bar` → `.fab` position:fixed; top:12px; right:16px, пилюля статуса, нижний резерв снят, правый отступ шапки; самопроверка судит рендер: `.fab` fixed · нет `bottom:0` · подписи без `**`; спецификация §4 — статья о запрете нижней панели; селфтест 64 (+4 с мутациями на копии); s22 +1; verify-contour — ширины 1400/560, кнопка в окне при начальной прокрутке и после прокрутки вниз; примечание: `grep -c bottom:0 review.mjs` → 3 — все три в самопроверке и её мутациях, в CSS страницы — 0) (#60): `.bar` → `.fab`
      (`position:fixed; top:12px; right:16px; z-index:50`, пилюля, тень), статус записи — пилюлей под ней
      на своём фоне, исчезает пустой; нижний резерв документа снят, у шапки правый отступ под кнопку (уже
      при ≤ 560 px); спецификация — статья «управление записью ответов — плавающая кнопка справа сверху;
      панель у нижнего края запрещена» с причиной (окно выше экрана, удалённый стол, телефон); самопроверка
      страницы (рядом с «радиогрупп = вопросов»): в HTML есть `.fab` с `position:fixed` и нет `bottom:0`,
      подписи вариантов не несут `**`; селфтест; `verify-contour` живой прогон с окнами 560 и 1400 px —
      кнопка видна при начальной прокрутке (скриншот глазами).
- [x] **QL5 — сборка, зеркала, записи, ответы** ✅ 2026-09-12 10:02 +03:00 (сборка OK · counters 50 · полигон 23 свода · verify-contour 190/190 · записи 2.7 сборщика +2 · EXP-0123 механизировано · EXP-0126 · черновики ответов ниже). `node tools/build-framework.mjs` → `counters-guard` →
      `npm run test:core` (`s22` + все) → `verify-contour` живьём → `TEMPLATE_NOTES_BY_VERSION['2.7']` /
      `POLICY_CHANGES_BY_VERSION['2.7']` (+2) → черновики ответов #54 / #56 / #58 (обновиться до 2.6 +
      канон) / #60 в этом плане → стражи закрытия.
- [x] **QL6 — закрытие** ✅ 2026-09-12 10:02 +03:00 (судья — § «Решения» 9; `plans/100` строка 2, критерии 3–6 ✅; план TR `plans/104`). Лёгкий судья (критерии 1–4) → `plans/100` строка 2 и критерии 3–6 ✅ →
      «Решения, принятые агентом без владельца» → план TR `plans/104` (N+1) → STATUS/MASTER_PLAN веха;
      EXP-0123 — поле механизации → `механизировано: review.mjs --check`.

## Верификация наблюдением

- Селфтест генератора — каждая новая проверка обоими ответами; `s22` — красный на копии генератора
  `v2.6` по каждому пункту (шов `git show v2.6:…`).
- Живой браузер: `node tools/verify-contour.mjs` (страница 031 как фикстура «архив + живое» — после QL2
  вопрос Q2 №031 внесён? нет: снят; взять интервью с раундами — №023/№026) + окно 560 px.
- Дверь `--check` на живом интервью истока — нет строк `CALL:`, нет `Page is up`, процесс не ждёт.

## Риски (ярусы Мёрфи)

- **(а) Факт «внесено» пишет агент** — забыл → очередь как сегодня (регресса нет). Защита: ось G9
  «отвечено ≥ 3 дней, не внесено» напоминает; кандидат — `tools/commit.mjs` ставит факт по метке
  `#031 Q1` в сообщении (кандидат, не обязательство эпика).
- **(а) Свёртка ломает форму ответа** — радиокнопки отвеченных вопросов внутри `<details>` остаются
  disabled; самопроверка «радиогрупп = вопросов с вариантами» считает и свёрнутые. Защита: ассерт в
  селфтесте на фикстуре с отвеченными.
- **(б) `--check` расходится с показом** — две дороги разбора. Защита: одна функция (`preflight` +
  `parseQuestions`) для обеих; ассерт «`--check` и показ печатают одно число вопросов».
- **(б) FAB перекрывает заголовок на телефоне** — правый отступ шапки при ≤ 560 px; живой прогон при
  560 px.
- **(в) Вкус кнопки** (форма, тень, цвет) — по слову владельца: «FAB вверху справа»; остальное — макет,
  при отказе — домашка.

## Черновики ответов в тикеты (отправка ПОСЛЕ релиза 2.7 — №84/№92/№93; одной квитанцией с закрытием)

**#54 (старый вопрос поднят снова; 18 535 знаков решённого над живым).** Спасибо — оба дефекта закрыты в KAIF 2.7.
У вопроса появился четвёртый факт — «внесено»: когда решение легло в правила или код, агент помечает это рукой
(`review.mjs --mark-implemented <документ> <Q> --where <коммит или файл>`), и очередь такой вопрос второй раз не
поднимает — вместо страницы печатает «внесено, но открыто → закрой статус» и выходит кодом 2. Страница интервью теперь
показывает живые вопросы первыми, а всё отвеченное и текст документа — одной свёрткой ниже (ничего не удаляется).
Обновление на 2.7 привозит генератор с обоими правилами; страж истока ловит «внесено, но статус ждёт».

**#56 (интерактивный контур «сломан»: проверка = показ = зов).** Вы правы — единственной проверкой документа был
его показ, а показ звал вас. В 2.7 у генератора есть отдельная дверь `--check <документ>`: разбор, предполёт и
самопроверка рендера без страницы, звука, зова и факта показа; она печатает, что узнала и чего не узнала, и
отказывает кодом 3. `/interview` шаг 3c теперь велит именно её; `--no-open` в справке назван честно — «поднимает
страницу и зовёт». Закрыто в 2.7.

**#58 («Я вычитал. Комментировать нечего. Нажимаю — не закрывает»).** Сам дефект закрыт ещё поставкой 2.6 (баг
истока 113: «Готово» с пустыми полями — законный исход, страница закрывается, код 0). В 2.7 это стало правилом
канона: у показа три законных исхода — ответ · пометка · «прочитано, замечаний нет» — записано в AGENT_GUIDE,
`/owner-reviews` I16 и в контракте контура §4. Закрыто.

**#60 («написал ответы, а нажать кнопку не могу»; «делаем её FAB вверху справа»).** Сделано по вашему слову:
кнопка записи — плавающая справа сверху, видна при любой прокрутке и высоте окна; статус записи — пилюлей под ней;
нижняя панель запрещена контрактом контура, и самопроверка страницы отказывает открывать страницу с панелью у
нижнего края (код 3). Проверено в живом браузере при ширинах 1400 и 560 px. Закрыто в 2.7.

## Решения, принятые агентом без владельца

1. [AI] Правило «кандидата в вопрос» для `--check`: узнанные заголовки + заголовки, ПОХОЖИЕ на вопрос (нумерованный с «?»
   или «Question/Вопрос N»); обычные нумерованные разделы кандидатами не считаются — откалибровано по 31 интервью истока
   (ноль ложных); неузнанный кандидат — код 3 (класс #56: страница открылась бы БЕЗ этого вопроса).
2. [AI] `--no-open` оставлен (им пользуется `verify-contour` с `--silent`), но в справке назван честно — «поднимает и
   ЗОВЁТ»; дверью проверки объявлен только `--check`.
3. [AI] Форма факта «внесено» — файл `implemented.json` рядом с `shown.json`, не строка в шапке документа.
   `FORK: options <файл рядом с shown.json | строка в шапке документа | поле в decision.json> · price of error <документ
   владельца неприкосновенен; очередь обязана читать факт без разбора прозы; факт нужен и для вопроса без decision.json>
   · consulted <практика трекеров: отдельное поле состояния (resolution / fix version, state «completed»), не текст — из
   знания агента, свежей веб-разведки в этой сессии не было>` — честно: слот consulted закрыт известной практикой, не
   разведдоком; для глаза владельца.
4. [AI] Внесённый открытый вопрос на странице рисуется РЕШЁННЫМ с бейджем «внесено → адрес (дата)», а не прячется:
   владелец видит, куда легло решение.
5. [AI] Вид для чтения включается только при наличии И живых, И решённых вопросов; документ без решённого сохраняет
   прежний порядок (проза → карточки) — иначе первое интервью читалось бы без контекста.
6. [AI] Вкус кнопки — пилюля с тенью справа сверху, статус пилюлей под ней, правый отступ шапки 230 px (170 px при
   ≤ 560 px); по слову владельца только «FAB вверху справа», остальное — макет; при отказе — домашка.
7. [AI] Критерий 4 `plans/100` предсказывал `grep -c bottom:0 review.mjs` → 0; факт — 3 (самопроверка и две её мутации
   на копии); свойство («в CSS страницы нет нижней панели») выполнено — записано фактом, критерий не подгонялся.
8. [AI] Урок EXP-0126 (свод развёрнутой копии судит `dist`; прогон `s22` до пересборки открыл страницу старым
   генератором) — кандидат «полигон отказывается стартовать при несвежем бандле» и «генератор отвергает незнакомый
   флаг до подъёма страницы» — в реестр 2.8 (`ideas/30`) на закрытии сессии.
9. Судья (лёгкий, критерии 1–4, сессия 59): **VERIFIED WITH CAVEATS** — все четыре поведения подтверждены его собственными
   прогонами (фикстуры 090/091/092: `--mark-implemented` → очередь код 0 при одном открытом, код 2 при всех внесённых,
   одиночный показ код 2 без рендера, неизвестный Q код 1; живой `--check` на №031 — код 0 без зова; `checkForm`: `## 2. Context`
   — не кандидат, `### 3. Which colour?` и `### Question 5` — неузнанные), ослабленных проверок нет (s22 +8 `ok(`/−0,
   verify-contour +4/−0). Три каверзы починены до закрывающего коммита: (1) критерий 4 этого плана буквально красный
   («grep → 0», факт 3) — переписан по CSS страницы; (2) записи QL5 стояли только в рабочем дереве — закрывающий коммит;
   (3) спецификация §3 обещала код 2 от пакетной `--queue` всегда — уточнено: строка печатается, код 2 — когда больше
   ничего не ждёт. Мелочи: `class="fab"` в модуле 4 (три сборщика + ассерт); ширина 500 px больше не прогоняется (560 — граница
   медиа-правила включительно).

## Links

`plans/100` · `plans/101` (AW) · issues #54 #56 #58 #60 · `researches/30` §2 · `bugs/113` · EXP-0122 ·
EXP-0123 · `framework/templates/_interactive-contour-spec.md` · `framework/tools/contour/` ·
`tools/sandbox/s22-contour-shipped.mjs` · `tools/verify-contour.mjs`.
