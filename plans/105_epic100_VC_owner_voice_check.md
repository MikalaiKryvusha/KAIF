# План 105 — эпик VC «Текст владельца — по его портрету, независимая проверка, потом владелец»: контракт письма получает три шага и исполнимый носитель

> **Создан:** ≈ 2026-09-12 11:20 +03:00 (закрытие эпика TR — канон N+1, №43; сессия 60).
> **Родитель:** `plans/100` (эпик VC — строка 9 таблицы эпиков; критерий приёмки версии 14 — добавлен словом владельца
> ≈ 2026-09-12 10:48 +03:00, №111: «бери его тоже в планирование и в версию 2.7»); источник — issue #61 (Unliminium,
> 2026-09-12 10:10 +03:00; слово владельца в тикете дословно: «то, что ты пишешь текста, и не пользуешься стилометрией -
> это ВОПИЮЩИЙ БАГ методологии. Заводи в KAIF это багом») + разбор `researches/30` §2б.
> **Статус:** ✅ **ЗАКРЫТ ≈ 2026-09-12 15:22 +03:00** (сессия 61 — поставка и коммит `93ec9ff`; формальное закрытие
> оформлено сессией 62 в 15:42 +03:00 — долг форс-закрытия, переисполнение прогоном 18 отчёта). Запланирован
> ≈ 2026-09-12 11:20 +03:00 — старт с шага VC0 после закрытия TR; вопросов владельцу нет
> (критерий задан сценарием в `plans/100`, критерий 14; форма носителя паттернов — развилка агента с разведкой, VC0).
> 🔧 **VC0–VC6 ✅ 2026-09-12 14:20 +03:00 (сессия 61).** ⚠️ **Постановка ПЕРЕПИСАНА словом владельца ≈ 2026-09-12
> 14:25 +03:00 (№112)** — прежняя редакция плана `[AI]` «проверка голоса командой до первого показа» была половиной
> (проверка ПОСЛЕ письма); **[OWNER] дословно:** «неправильно сформулирвоана задача. Я прочитал ее постановку как "ИИ агент
> пишет как он умеет, его нативным языком, а потом это сверяет со стилометрией"» · «Правильная постановка задачи, которую
> я ожидаю в поведении ии агента при работе по KAIF: "ИИ агент пишет текст голосом и правилами, которые ему предписывает
> стилометрия владельца. После написания, по этой эе стилометрии ИИ агент проводит независимую проверку написанного,
> правит, и только после этого считает текст написанным и несут это владельцу на согласование"» · «перепиши планы по
> этому эпику так, как я написал выше. Переделываем» — переделка шагом VC9 ниже.
> **Вовне:** поставка — tool-модуль `framework/tools/kaif-voice-lint.mjs` (`check <файлы…>` / `selftest`, `SKIPPED=3`
> без портрета или без таблицы §8), скелет портрета `framework/templates/_owner-voice-template.md` §7A/§8 в
> машиночитаемой форме, режим `check` навыка `/owner-voice` обоих слоёв, четвёртое обязательство KAIF в точке вызова
> fable-цикла и строка в «Показ — это действие» (`AGENT_GUIDE` обоих слоёв), чек-лист обоих слоёв → команда, пункт
> `owner-voice` задания установки (`KAIF-CORE.mjs`), охота судьи «owner text shown without a voice check», свод
> полигона (первый свободный после s25), записи 2.7 в сборщике, черновик ответа #61.

## Вектор цели (Achieve)

**Боль** (`plans/100` вектор, критерий 14; #61 дословно): обязательство «пишешь в артефакт владельца → открой портрет
голоса и прогони его чек-лист» живёт ПРОЗОЙ в чек-листе, читаемом на старте сессии; команды у него нет. Полевой агент
семь раундов переписывал лист для игрока под живым взглядом владельца и ни разу не открыл портрет; владелец поймал по
языку и спросил: «сколько раз ты этот текст сравнивал с стилометрией владельца?» — ноль и ноль. Канон сам предсказывает
этот класс («Форма обязательства — команда, шаг или чекбокс»: правило прозой под нагрузкой не исполняется), и у соседних
правил команды уже есть (`--queue --list`, `kaif-attribution-lint`), а у гейта голоса — нет. Шире тикета: у истока СВОЙ
`tools/voice-lint.mjs` (T12, 2.2) — механизация построена для себя и не отгружена.

**Где хотим оказаться** (постановка — слово владельца ≈ 2026-09-12 14:25 +03:00, №112; прежняя редакция этого абзаца
`[AI]` «команда в точке письма до первого показа» была половиной). Текст, который владелец читает как свой, агент
**(1) пишет ПО портрету** — правила §2, словник §2-C, анти-портрет §5, пары §6 открыты ДО первого слова, — затем
**(2) независимо проверяет по тому же портрету**: машинная минута `node .kaif/tools/kaif-voice-lint.mjs check <файлы…>`
(таблица стоп-паттернов §8 портрета `AUTHOR_STYLOMETRY.md`; попадания поимённо — строка · паттерн · подсказка; без
портрета или без таблицы — `SKIPPED=3` с границей вслух «похожесть не судится — вердикт владельца») плюс смысловой
проход §7Б чистым экземпляром, — **(3) правит, и только тогда текст написан и идёт владельцу на согласование**. Три
шага стоят в точке вызова fable-цикла обоих слоёв и повторены в «Показ — это действие», чек-листе и роутере; `/owner-voice`
несёт контракт письма и режим `check` (машинную половину); скелет портрета несёт машиночитаемую форму §8 и чистый
проход §7Б; судья охотится на текст владельцу, прошедший мимо портрета; полевой проект получает всё это обновлением.

**Метрика эпика:** критерий 14 `plans/100` закрыт **1 из 1** (≈ 2026-09-12 15:22 +03:00).

## Готово, когда (критерии приёмки — сценариями; якорь `plans/100` критерий 14)

1. **[VC — три шага и команда в точке письма]** — сценарий `plans/100` критерий 14 дословно (редакция №112). *Проверка:*
   `selftest` линтера красный на фикстуре с попаданием и зелёный на чистой, оба языка поставки; свод полигона на установке
   2.7 (портрет с таблицей §8 → попадание названо строкой, паттерном и подсказкой; без портрета → `SKIPPED=3`; файл,
   написанный до `load` или позже часа после последней загрузки, → «written past the portrait»); грепы трёх шагов и
   обеих команд в `framework/AGENT_GUIDE.md` («Write BY the portrait — with it in your working context.» · «Check
   INDEPENDENTLY by the same portrait.» · `kaif-voice-lint.mjs load` · `kaif-voice-lint.mjs check <file…>`) и корневом
   («Пиши ПО портрету — с ним в своём рабочем контексте.» · «Проверь НЕЗАВИСИМО по тому же портрету.») — пары
   `check-framework`; охота судьи «Owner text past the portrait (KAIF 2.7)» в обеих копиях; на ядре 2.6 — `✖`.

2. **[VC — паттерны живут в портрете, форма машиночитаема]**
   - Ситуация. Проект скопировал скелет в `AUTHOR_STYLOMETRY.md`; в §8 портрета — таблица из 12 строк «паттерн ·
     класс · подсказка · законное исключение»; в `sheet/steps.md` одна строка содержит «Помни, что…».
   - Действие. Агент запускает `node .kaif/tools/kaif-voice-lint.mjs check sheet/steps.md`.
   - Результат. Печатается одна строка попадания: `sheet/steps.md:7 — «Помни, что» → <подсказка из той же строки
     таблицы>`, код 1; на портрете, чей §8 остался прозой, — `SKIPPED=3` со словами «§8 несёт не таблицу паттернов —
     линтеру нечего читать»; скелет §7A называет команду, а §8 — форму таблицы с двумя строками-примерами в
     плейсхолдерах, которые линтер не читает.
   - Проверка. `selftest` (фикстура портрета в памяти, оба языка: попадание · чистый текст · портрет прозой → 3);
     свод — ассерты «попадание названо с подсказкой» и «прозаический §8 → SKIPPED»; `grep -c "kaif-voice-lint" 
     framework/templates/_owner-voice-template.md` → ≥ 1.

3. **[судья эпика]** — лёгкий `/fable-judge` по критериям 1–2; на закрытии — план HO `plans/106` (N+1) и веха в
   `plans/100` (строка 9, критерий 14 ✅).

## Шаги

- [x] **VC0 — нулёвка актуализации входов** ✅ ≈ 2026-09-12 14:02 +03:00 (сессия 61). Сверено против HEAD `720717c`:
      скелет `_owner-voice-template.md` §7 «7A — the machine minute (greps of stop-patterns and required positives)» и §8
      «≥10 grep patterns … graduate into a project guard once calibrated» — проза, формы нет; `/owner-voice` обоих слоёв —
      режимы `portrait` · `rewrite`, «When to invoke» велит «write with the portrait open and run its checklist» без
      команды; `framework/AGENT_GUIDE.md` — шаг 19 чек-листа и строка роутера прозой, три обязательства KAIF в точке
      вызова fable-цикла (`:335` · `:341` · `:353`), «Showing is an action» `:886`; корневой — шаг 16 `:109`, роутер
      `:134`, обязательства `:324`–`:350`, «Показ — это действие» `:953`; `KAIF-CORE.mjs` — пункт `owner-voice` `:816`,
      чекпоинт `:3146`–`:3158` (портрет на диске ИЛИ запись `no voice portrait`); ключи маркера схемой не ограничены
      (`testdocs` линтер TR читает напрямую — `voicePortrait` пойдёт так же); `tools/voice-lint.mjs` истока — переносима
      половина «стоп-паттерн как данные» (A2, B9), непереносимы жёсткие пункты одного портрета (A3 · B12 · B5 · G1 ·
      коридоры); образцы — `kaif-testrun-lint` (правила как данные · `SKIPPED=3` · селфтест в памяти EN/RU) и
      `kaif-scenario-lint` (CLI `check <paths…>`), свод `s25`; номер свода — **s26**; `CYRILLIC_DATA_CARRIERS` —
      `check-framework.mjs:38–47`, пары «portrait canon name ↔ …» — `:349–:356`; охоты 2.7 судьи —
      `fable-judge/SKILL.md:14` (ростер) и `:58–:59` (блок), копии побайтно равны. Полевой портрет проекта тикета
      (публичный репозиторий): §8 «МАШИННЫЕ ЭВРИСТИКИ для линтера» уже ведётся ТАБЛИЦЕЙ «Правило линтера | Паттерн
      (ripgrep) | Комментарий» (16 строк стоп-конструкций; `\|` внутри паттернов — альтернация), а его страж хранит тройки
      `[name, re, allow]` (`tools/voice-stop.mjs`).
      `INTENT: code does <no voice command in the payload: checklist step 19/16 and the router row are prose, /owner-voice
      has portrait + rewrite, the skeleton's §8 is a prose requirement>; the failing check/task expects <a command at the
      point of writing that reads the portrait's §8 patterns and prints hits, SKIPPED=3 without a portrait or a table —
      plans/100 criterion 14>; the spec says <AGENT_GUIDE → «Форма обязательства»: правило, рождающее артефакт, называет
      команду, которая его производит — нет команды, правило неполно; #61 предложения (a)–(c)>` — три стороны сходятся.
      `FORK: options <A таблица в §8 самого портрета | B отдельный .kaif/voice-patterns.json | C fenced-блок «регэксп на
      строку» в §8> · price of error <форма, которую владелец не станет вести, или вторая копия правил, расходящаяся с
      портретом> · consulted <Vale (docs.vale.sh/topics/styles: правило = YAML-запись — extends existence|substitution ·
      message · level · tokens|swap · exceptions · ignorecase) · полевой портрет проекта тикета — §8 ТАБЛИЦЕЙ с
      ripgrep-паттернами и комментарием · его же voice-stop.mjs — тройки name · re · allow>` → **DECIDED [AI]: A** —
      таблица «паттерн · класс · подсказка · законное исключение» в §8 портрета: владелец правит там, где читает (поле
      уже пишет так), второй копии правил нет, а четыре колонки — ровно поля записи Vale (tokens · existence/substitution
      · message · exceptions). Синтаксис ячейки паттерна = синтаксис поля: `\|` — альтернация, `/…/i` — флаги,
      `\b` — Unicode-граница слова (JS-`\b` слеп к кириллице); исключение — `/regex/` гасит попадание на своей строке,
      проза печатается рядом с попаданием.
- [x] **VC1 — скелет портрета** ✅ ≈ 2026-09-12 14:08 +03:00 — `_owner-voice-template.md`: шапка называет
      `.kaif/kaif.json` → `voicePortrait` для иного имени; §7A называет команду `node .kaif/tools/kaif-voice-lint.mjs check
      <file…>` (до первого показа и перед сдачей, `SKIPPED` — в отчёт); §8 — таблица `pattern · class · hint · legal
      exception` с двумя строками-примерами в `<…>` (линтер их не читает — селфтест: скелет незаполненный → `no-rules`),
      контракт колонок (`\|` — альтернация, голый паттерн регистрозависим, `/…/i` складывает регистр, `\b`/`\w` Unicode;
      `/regex/`-исключение гасит строку, проза печатается) и строка о калибровке → `--warn`; `grep -c kaif-voice-lint` → 2.
- [x] **VC2 — tool-модуль** ✅ 2026-09-12 14:19 +03:00 — `framework/tools/kaif-voice-lint.mjs` (`check <файлы…> [--warn]` ·
      `selftest`): портрет — `AUTHOR_STYLOMETRY.md` или `.kaif/kaif.json` → `voicePortrait`; §8 — по ключевому слову
      заголовка обоих языков ИЛИ по номеру `8.`; таблица — по шапке с колонками паттерн/подсказка (класс и исключение
      опциональны, порядок любой), чужие таблицы в §8 называются поимённо; плейсхолдеры `<…>` и пустые ячейки — не правила;
      паттерн — ripgrep-синтаксис (`\|` → альтернация, `/…/i` → флаги, `\b`/`\w` → Unicode через lookaround'ы и `\p{L}`),
      несобираемая строка — предупреждение с номером строки, остальные правила работают; стоп-правила построчно (фенсы,
      код-спаны, HTML-комментарии невидимы, номера строк сохраняются), позитивные — по файлу (отсутствие → предупреждение);
      печать `файл:строка — «фрагмент» → подсказка (exception: проза)`; коды 1/0, `--warn` → 0, `SKIPPED=3` — портрета нет ·
      §8 нет · в §8 нет таблицы паттернов (проза или чужие колонки — с советом перенести в таблицу) · одни плейсхолдеры;
      usage → 2; граница «likeness is not judged — that verdict is the owner's» печатается каждым прогоном; `selftest` —
      35 кейсов, 2 языка (первый прогон зелёный; свод затем поймал дефект счёта строк — пустой хвост файла считался
      строкой, починено 14:19); `CYRILLIC_DATA_CARRIERS` пополнен (RU-ключевые слова и фикстуры); живой прогон истока —
      `SKIPPED=3` с названием двух чужих таблиц §8 слепка (граница, названная в «Верификации»).
- [x] **VC3 — `/owner-voice` обоих слоёв** ✅ ≈ 2026-09-12 14:10 +03:00 — описание «Три режима — portrait · rewrite ·
      check»; «Когда вызывать» зовёт `check` до первого показа; новый раздел «Режим `check` — машинная минута перед
      показом» (что читает · что печатает · `SKIPPED` — исход для отчёта, не зелёный · на попадании — переписать или
      ответить в колонке исключений, никогда глушить · граница · проводка в сборочные скрипты); конвейер `rewrite` —
      «переписчик → check по единице → судья»; ru-алиасы «проверь голос», «сверь со стилометрией» (пины EN-описаний
      передвинуты `--update-pins` после сверки глазами); пары `check-framework` «portrait canon name ↔ …» несут токен
      `kaif-voice-lint.mjs check` (навык обоих слоёв · скелет).
- [x] **VC4 — точки решения `AGENT_GUIDE` обоих слоёв** ✅ ≈ 2026-09-12 14:11 +03:00 — четвёртое обязательство KAIF к
      шагу 5 fable-цикла («текст владельца проверяется командой до первого показа»; `SKIPPED` — теми же словами в отчёте;
      похожесть — вердикт владельца; охота судьи названа), строка в «Showing is an action» / «Показ — это действие», шаг
      19/16 чек-листа и строка роутера — командой; пункт `owner-voice` задания установки (`KAIF-CORE.mjs`) — «проведи
      команду в сборочный путь каждого класса текста владельцу; §8 — таблицей, грепы прозой → SKIPPED» (#61 (c)); пары
      `check-framework` «voice check ↔ AGENT_GUIDE call point» (payload + wrapper); грепы: `kaif-voice-lint.mjs check` —
      payload 4 · root 5.
- [x] **VC5 — охота судьи** ✅ ≈ 2026-09-12 14:11 +03:00 — «Owner text shown without a voice check (KAIF 2.7)» в
      KAIF-блоке `/fable-judge` обеих копий (побайтно равны) + ростер «(7) the KAIF 2.7 hunts»: показан/сдан/закоммичен
      без строки прогона команды в отчёте — находка; также: «машинная минута прошла» при §8 прозой · попадание «починено»
      расширением исключения или удалением строки · похожесть, заявленная от зелёного прогона; проект без портрета —
      не находка, отчёт обязан это сказать; пара `check-framework` «voice check ↔ /fable-judge».
- [x] **VC6 — свод полигона `s26`** ✅ 2026-09-12 14:20 +03:00 — 27 ассертов (счёт печатает свод): selftest · EN- и
      RU-проект — попадание «sheet/steps.md:7 — «Помни, что» → …» (критерий 2 дословно) exit 1, итог считает файлы/строки/
      правила · чистый файл 0 · `--warn` 0 · фенс невидим · §8 прозой → 3 («carries no pattern table … prose only») · без
      портрета → 3 · путь из маркера · usage 2 · развёрнутая копия: модуль и скелет (команда + шапка таблицы) приехали,
      незаполненная копия скелета → SKIPPED «placeholders are not rules», одна заполненная строка → попадание с подсказкой;
      «all 27 checks green» на свежем dist; на ядре 2.6 (`KAIF_DIST` → `git show v2.6:dist/…`) — «5 of 27 check(s)
      failed», все пять адресованы отсутствующей фиче, свод доходит до вердикта; полигон «all 25 suites green».
- [x] **VC9 — переделка по слову владельца (№112) ✅ ≈ 2026-09-12 14:40 +03:00.** Постановка «ИИ агент пишет ПО
      стилометрии → независимая проверка по ней → правит → только тогда написан → владельцу» проведена во все носители,
      где стояла прежняя редакция «проверка командой до показа»: `framework/AGENT_GUIDE.md` — четвёртое обязательство
      переписано ТРЕМЯ шагами (к шагам 4 и 5 fable-цикла: «Write BY the portrait…» (после VC10 — «Write BY the portrait
      — with it in your working context.») · «Check INDEPENDENTLY by the same portrait.» · «Fix — only then it is
      written.», слово владельца в переводе), шаг 19 чек-листа, строка роутера,
      «Showing is an action» («shown only AFTER it is written BY his portrait, checked independently by it and fixed»);
      корневой `AGENT_GUIDE.md` — те же четыре места, слово владельца дословно (№112); `/owner-voice` обоих слоёв —
      описание («Контракт письма…»), «Когда вызывать», новый раздел «Контракт письма — по портрету, независимо проверено,
      потом владельцу» (три шага · что несёт отчёт о единице), режим `check` переименован в «машинную половину
      независимой проверки» с пунктом «чем команда НЕ является», конвейер `rewrite` — «переписчик пишет ПО портрету →
      check → судья по анти-портрету и парам»; `/fable-judge` обеих копий — охота переименована в **«Owner text past the
      portrait (KAIF 2.7)»** (написан без портрета · без независимого прохода · показан до правок; также «написан нативно
      и перепет» под видом «по портрету»), ростер (7) обновлён; скелет §7 — «чек-лист есть НЕЗАВИСИМАЯ проверка после
      письма ПО портрету», §7Б — чистым экземпляром; шапка модуля — «машинная половина независимой проверки, не шаг
      письма»; `KAIF_REFERENCE` §5 и строка модуля; записи 2.7 сборщика (`TEMPLATE_NOTES` + `POLICY_CHANGES`) —
      трёхшаговый контракт; пары `check-framework` — токены трёх шагов обоих слоёв и новое имя охоты; `plans/100`
      критерий 14 и строка 9 — переписаны с цитатой владельца; `MASTER_PLAN` №112. Модуль `kaif-voice-lint` и свод `s26`
      кодом не менялись — команда была и остаётся машинной половиной шага 2.
- [x] **VC10 — «с ней в своём рабочем кеше» механизировано (№112, второе слово ≈ 14:35) ✅ ≈ 2026-09-12 14:50 +03:00.**
      **[OWNER] дословно:** «" агент должен писать текст" - ПИСАТЬ ПО СТИЛОМЕТРИИ!!!!!!!!!!!» · «Это блять важно - писать
      ПО СТИЛОМЕТРИИ, С НЕЙ В СВОЕМ РАБОЧЕМ КЕШЕ». `[AI]` Механизация по образцу маркера освежения: команда
      `node .kaif/tools/kaif-voice-lint.mjs load [--sections <regex>]` печатает портрет (или только совпавшие H2-разделы;
      голова портрета едет всегда) В РАБОЧИЙ КОНТЕКСТ агента и пишет свидетеля `.kaif/voice-marker.json` (`at` · `firstAt`
      · `portrait` · `sha256` · `sections` · `lines`; момент снимает сам инструмент; в ignore истока и в ignore-first наборе
      установщика — как `refresh-marker.json`); `check` судит свидетеля: нет маркера → находка «written past the portrait —
      no load witness», файл написан раньше `firstAt` → находка «the file was last written at … the portrait was first
      loaded at …», портрет изменился после загрузки → предупреждение «reload», загрузка старше 60 минут → предупреждение
      «the working context has moved on»; итог зелёного прогона называет момент загрузки. Носители: шаг 1 четвёртого
      обязательства обоих слоёв («Write BY the portrait — with it in your working context.» / «Пиши ПО портрету — с ним в
      своём рабочем контексте.» — слово владельца дословно в RU), шаг 19/16 чек-листа, роутер, контракт письма и режим
      `check` навыка обоих слоёв (свидетель), охота судьи (нет свидетеля / файл раньше загрузки — `check` называет оба),
      скелет («Load, do not consult»), записи 2.7, справочник, пары `check-framework` (токены `load`), свод `s26`
      (`load` печатает портрет и свидетеля · файл до `load` → находка · `--sections "^8"` · `load` без портрета → SKIPPED ·
      путь из маркера · чужая команда → usage · развёрнутый `load` · «changed since it was loaded» на копии); селфтест
      44 кейса (свидетель — пять чистых вердиктов, `--sections`). **После судьи (каверзы 7–14, ≈ 15:15):** свидетель
      несёт ИСТОРИЮ загрузок, «написано позже часа после последней загрузки» — находка (правило часа), свидетель чужого
      портрета — находка, `--sections` без значения/без совпадений — usage без свидетеля, `--warn` не гасит свидетеля,
      `\B` → Unicode, оба момента в локальной зоне, «нет свидетеля» — одной строкой на прогон; селфтест 48 кейсов, свод
      `s26` 47 ассертов («all 47 checks green», на ядре 2.6 — «7 of 47»).
- [x] **VC7 — сборка · counters · полигон · записи 2.7 ✅ ≈ 2026-09-12 15:20 +03:00.** Записи 2.7 сборщика
      (`TEMPLATE_NOTES` + `POLICY_CHANGES` — трёхшаговый контракт + «перенеси греп-строки §8 своего портрета в таблицу —
      линтер читает таблицу»); черновик ответа #61 — в этом плане (отправка после релиза, №84/№92/№93); финальная сборка
      «180 file blocks … 790 modules», `counters` «OK — 50 зеркал», полигон «all 25 suites green»; стражи закрытия
      зелёные; отчёт прогона — `testcases/reports/2026-09-12_polygon-2.7-VC.md` (канон TR исполнен на самом истоке,
      `kaif-testrun-lint check` → «OK — 2 report(s)»).
- [x] **VC8 — закрытие ✅ ≈ 2026-09-12 15:22 +03:00** (формальности оформлены сессией 62 в 15:42 — долг форс-закрытия).
      Лёгкий судья эпика (субагент с чистым контекстом, 9 заявлений переисполнены) — **VERIFIED WITH CAVEATS**, ослабленных
      сводов нет; 14 каверз починены до коммита и переисполнены (прогон 17 отчёта). `plans/100` — строка 9 и критерий 14 ✅,
      метрика «закрыто 10 из 14»; «Решения без владельца» — 11 пунктов выше; план HO `plans/106` написан (N+1);
      `MASTER_PLAN` §5/§8 и `STATUS` — веха VC, следующий шаг HO. Переисполнение на закоммиченном дереве `93ec9ff` —
      прогон 18 отчёта (сессия 62): формальное ✅ ставится по собственному наблюдению, а не по памяти закрывшейся сессии.

## Верификация наблюдением

- Селфтест линтера — попадание · чистый · прозаический §8 · исключение · код-фенс, оба языка; свод на развёрнутой
  копии; красный на ядре 2.6.
- Живой прогон истока: `node framework/tools/kaif-voice-lint.mjs check <документ обвязки>` — у истока портрет
  `AUTHOR_STYLOMETRY.md` ГЕНЕРИРУЕТСЯ из приватного ядра (`tools/stylometry-snapshot.mjs`, №60–№62) и таблицы §8 в форме
  VC1 может не нести → честный `SKIPPED=3` со словом о границе; собственный `tools/voice-lint.mjs` истока остаётся;
  перенос его стоп-половины в таблицу слепка — только словом владельца (генератор слепка — приёмка №62), кандидат в 2.8.
- Отчёт прогона этого эпика — `testcases/reports/<дата>_polygon-2.7-VC.md`, линтер TR зелёный на нём.

## Риски (ярусы Мёрфи)

- **(а) Шум:** паттерны портрета красят законный текст → колонка «законное исключение» в таблице, режим `--warn` для
  калибровки (скелет: «warning mode first; noise above signal = no guard»), базовой линии нет — таблица правится владельцем.
- **(а) Портреты поля несут §8 прозой (Unliminium — греп-строки):** до переноса в таблицу линтер честно `SKIPPED=3` и
  говорит, что читать; policy-строка 2.7 велит перенести; ничего не ломается.
- **(б) Слепок истока генерируется:** VC не трогает генератор (№62); исток ест поставку ровно настолько, насколько слепок
  несёт таблицу — граница названа в «Верификации».
- **(в) Похожесть:** линтер не судит вкус — граница печатается в `SKIPPED` и в шапке модуля; вердикт «звучит как
  владелец» остаётся за владельцем (`AGENT_GUIDE` → «Класс „вкус“»).

## Черновик ответа в #61 (отправка после релиза 2.7 — №84/№92/№93)

Shipped in KAIF 2.7 (epic VC), by the owner's statement of the expected behaviour: *the agent writes the text in the
voice and by the rules the owner's stylometry prescribes; after writing, by that same stylometry, it runs an independent
check of what it wrote, fixes it, and only then counts the text as written and brings it to the owner for approval.*

- `AGENT_GUIDE.md` (both layers) gains a FOURTH KAIF obligation at the fable loop's call point, three named steps: **write
  BY the portrait** (`AUTHOR_STYLOMETRY.md` — its rules, lexicon, anti-portrait and before/after pairs open BEFORE the first
  word) → **check INDEPENDENTLY by the same portrait** (the machine minute + the §7B pass by a clean instance — the writer's
  own glance is not one) → **fix, only then written, only then shown**; the same contract stands in "Showing is an action",
  checklist step 19 and the router row. Your proposal (b) — one line at step 5 and in the show ritual — is in, as the
  contract rather than a line.
- Your proposal (a): NEW optional tool module `.kaif/tools/kaif-voice-lint.mjs` (`check <files…> [--warn]` / `selftest`) —
  the machine half of step 2. It reads the §8 TABLE of the portrait — `pattern · class (stop / positive) · hint · legal
  exception` — and prints every hit as `file:line — «fragment» → hint`; `\|` inside the pattern cell is alternation, a
  bare pattern is case-sensitive, `/…/i` folds case, `\b`/`\w` are Unicode-aware (JavaScript's own are blind to Cyrillic);
  a row's `/regex/` exception silences a hit on its line, prose is printed beside it; fenced code, inline code and HTML
  comments are invisible; `SKIPPED=3` without a portrait, without a §8 table or with placeholder rows only — likeness is
  never judged, that verdict is the owner's. Your portrait's §8 already keeps its stop-constructions as a table; the
  linter wants the four columns above (the skeleton `.kaif/_owner-voice-template.md` shows the form) — a table with other
  columns is named in the `SKIPPED` line rather than half-read.
- `/owner-voice` carries the writing contract and gains mode `check`; the skeleton's §7 makes the checklist the
  INDEPENDENT check after writing by the portrait and §7B a clean-instance pass.
- Your proposal (c): the installer's `owner-voice` item now asks to wire the command into the build path of EVERY
  owner-facing text class the deployment has (a sheet generator, a string table, a README section) — a hit stops the
  build or is answered in the portrait's exception column.
- `/fable-judge` hunts **"owner text past the portrait"**: written without the portrait open, checked by no independent
  pass, or shown before the fixes — and a draft written natively and re-voiced later presented as "by the portrait".

- The owner's second word — write BY the stylometry WITH IT IN THE WORKING CACHE — is mechanized the way the context-refresh
  witness is: `node .kaif/tools/kaif-voice-lint.mjs load [--sections <regex>]` prints the portrait (or the modules the
  unit needs) INTO the agent's working context before the first word and leaves the witness `.kaif/voice-marker.json`
  (a history of load moments, the portrait's path and sha; session state, ignored by git); `check` refuses a text with no
  witness, with a witness for another portrait, last written before the first load, or written more than an hour after
  the last load ("written past the portrait" — the hour rule; never muted by `--warn`). A draft written natively and
  re-voiced later is the very class the contract closes.

Proof: `kaif-voice-lint selftest` — 48 cases in two languages (patterns, table parsing, the witness verdicts, `\b`/`\B`
Unicode); sandbox suite s26 — 47 checks green on 2.7 (an EN and a RU project red on `sheet/steps.md:7 — «Помни, что» →
…`, a file written before `load` → "written past the portrait", clean green with the load moment, prose §8 / no portrait
SKIPPED for `check` and `load`, the deployed copy carries the module and the skeleton with both commands and the table
form) and red on the 2.6 core (7 of 47 addressed to the absent module and skeleton form); the origin's own run is filed
as `testcases/reports/2026-09-12_polygon-2.7-VC.md`. Your 2.5 → 2.7 update task carries the policy line. Thank you —
the ticket named the class exactly, and the owner's correction of the statement (write BY the portrait first, with it in
the cache, then check independently) is what shaped the contract.

## Решения, принятые агентом без владельца

1. **[AI] Форма носителя паттернов — таблица в §8 самого портрета** (`FORK:` в VC0; разведка — Vale, полевой портрет
   проекта тикета, его `voice-stop.mjs`); отвергнуты отдельный JSON (вторая копия правил) и fenced-блок (некуда класть
   подсказку и исключение).
2. **[AI] Синтаксис ячейки паттерна = синтаксис поля (ripgrep):** голый паттерн регистрозависим, `/…/i` складывает
   регистр, `\|` — альтернация, `\b`/`\w` переводятся в Unicode-формы; исключение `/regex/` гасит, проза печатается;
   несобираемая строка — предупреждение с номером, а не отказ всего прогона.
3. **[AI] Заголовок §8 ищется по ключевому слову обоих языков ИЛИ по номеру `8.`** — портрет на третьем языке не
   теряется; таблица опознаётся по колонкам паттерн/подсказка (порядок любой), чужие таблицы в §8 называются поимённо.
4. **[AI] `SKIPPED=3` и на таблице из одних плейсхолдеров** — незаполненная копия скелета не может быть зелёной.
5. **[AI] Коды выхода:** 1 — попадания, 0 — чисто, 3 — не судилось, 2 — usage/нет файла (у модулей-соседей usage → 1;
   здесь 1 занят находками, и «нет такого файла» не должен читаться попаданием).
6. **[AI] Описание навыка `/owner-voice` переписано (третий режим) → пины EN-описаний передвинуты `--update-pins` после
   сверки ru-алиасов глазами; в ru-пакет добавлены алиасы «проверь голос», «сверь со стилометрией».**
7. **[AI] Первая редакция постановки эпика («проверка командой до первого показа») — моя, и она была половиной**: владелец
   поправил постановку ≈ 14:25 (№112) — письмо ПО портрету идёт первым, проверка — независимая, показ — после правок;
   переделка шагом VC9. Урок класса — в `EXPERIENCE.md` на закрытии: «гейт после» ≠ «дисциплина производства по правилу».
8. **[AI] Модуль и свод s26 при переделке №112 (три шага) не менялись** — команда и была машинной половиной проверки;
   менялись носители контракта (канон обоих слоёв, навык, судья, скелет, записи, планы).
9. **[AI] «С ней в своём рабочем кеше» механизировано командой `load` со свидетелем `.kaif/voice-marker.json`** (шаг VC10)
   — по образцу маркера освежения (`refresh-marker.json`): загрузка печатает портрет в контекст и оставляет
   машиночитаемый факт с ИСТОРИЕЙ загрузок; `check` отказывает тексту без свидетеля, со свидетелем чужого портрета,
   написанному раньше первой загрузки или ПОЗЖЕ ЧАСА после последней загрузки перед ним (каденция освежения контекста —
   находка, не предупреждение: маркер живёт на диске между сессиями, и «предупреждение о возрасте» отказывало бы
   один раз на дерево — каверза судьи 7); смена портрета после загрузки — предупреждение; `--warn` калибрует паттерны и
   свидетеля не гасит (каверза 9); `--sections` без значения или без совпадений — отказ без свидетеля (каверзы 8, 12).
   Свидетель не доказывает, что агент ЧИТАЛ напечатанное (скопированный маркер, пересохранённый файл — форма
   проходит), — это граница класса маркеров, названная в шапке модуля и в навыке (как у освежения: охота судьи
   «refresh-witness»); доказательство чтения — модули, названные в отчёте о единице, и сам текст против словника.
10. **[AI] План HO (`plans/106`) и урок EXP-0128 написаны ПАРАЛЛЕЛЬНО работе судьи** (каверза 6): канон N+1 не зависит от
    вердикта по VC, а судья судил замороженный дифф (sha названа в его вердикте); `plans/106` его судом не покрыт —
    покрывается нулёвкой HO0 и судьёй HO.
11. **[AI] Четырнадцать каверз судьи закрыты до коммита без второго суда** — по прецеденту TR: переисполнение сводов и
    стражей после починок (прогоны 16–17 отчёта) вместо второго субагента; протухшие числа плана/отчёта/STATUS/черновика
    — только из вывода инструментов (EXP-0025).

## Links

`plans/100` (критерий 14, строка 9) · issue #61 · `researches/30` §2б · `plans/104` (TR — образцы модуля и свода) ·
`framework/templates/_owner-voice-template.md` · `framework/skills/owner-voice/SKILL.md` · `tools/voice-lint.mjs` ·
`framework/tools/kaif-scenario-lint.mjs` · `framework/tools/kaif-testrun-lint.mjs`.
