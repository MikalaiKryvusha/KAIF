# План 112 — эпик SF «Ложь исправляется там, где оставлена»

> **Создан:** 2026-09-18 ≈ 00:45 +03:00 (сессия 65, Fable 5.1 — по слову владельца №120; исполняется после LP,
> `plans/100` § «Решения» 9).
> **Родитель:** `plans/100` (эпик SF — строка 14; критерий приёмки версии 21). Источник — issue #67 (QA_Engineer, macOS,
> KAIF 2.4, сфера qa-engineering; 2026-09-14 13:37 UTC; слово владельца проекта дословно: «запиши баг в KAIF — агент
> оставляет ложь и забывает исправить ложь там, где она её оставила, после того, когда выяснила, что что-то в прошлом было
> ложью»); разбор — `researches/30` §2е. Место в версии — №118.
> **Статус:** 🔧 **SF0–SF2, SF4, SF5 ИСПОЛНЕНЫ 2026-09-18 00:55 +03:00 → 2026-09-18 01:10 +03:00** (агент эпика в отдельном git worktree
> `agent-a513d874b5bf458e2`, ветка `worktree-agent-a513d874b5bf458e2` от HEAD `2b7e9e8`; коммит в своей ветке, без пуша).
> Открыто интегратору (главной сессии): SF3 — охота судьи; пары `check-framework`; `TEMPLATE_NOTES['2.7']`; три зеркала
> счётчика модулей (`README.md` обе половины, `STATUS.md`); SF6 — закрытие. Передача — `plans/handoff_SF_DONE.md`.
> **Вовне:** поставка — раздел `AGENT_GUIDE` обоих слоёв, шаг `/end-chat-soft` и `/end-chat-force` обоих слоёв, охота
> `/fable-judge`, строка в библиотеках сфер (`_template`, `programming`), записи 2.7 сборщика, черновик ответа #67.

## Вектор цели (Achieve)

**Боль.** Агент узнаёт — от владельца, замером, поздним прогоном, — что его прошлое утверждение ложно, соглашается В ЧАТЕ,
поправляет свои заметки и идёт дальше; утверждение остаётся стоять там, где было опубликовано (комментарий трекера под
именем владельца, строка отчёта, статус, документ), и владелец приходит второй раз. Канон требует наблюдения при
рождении заявления (`[TESTED]`, «заявление не шире наблюдения») и ничего — при пережитом опровержении: шага, который
возвращает агента к месту заявления, нет; `/end-chat-soft` о таких заявлениях не спрашивает; судья ловит ложь в момент
рождения, ретроактивный случай не назван; библиотека сферы не называет команду отзыва для внешнего канала
(`researches/30` §2е).

**Где хотим оказаться.** Критерий 21 `plans/100`: в момент, когда утверждение опознано ложным, канон обоих слоёв ведёт
агента упорядоченной процедурой с командами — стоп → перечень всех мест публикации → поправка или отзыв в каждом → чтение
обратно → «исправлено: где» в ответе владельцу — ДО продолжения работы; класс назван («стоячая ложь»), ритуал закрытия
спрашивает о нём по имени, судья охотится, библиотеки сфер называют команду отзыва на каждый внешний канал записи.

**Метрика эпика:** критерий 21 `plans/100` закрыт **0 из 1**.

## Готово, когда (критерии приёмки)

Сценарий — дословно критерий **21** `plans/100`; нулёвка SF0 вправе уточнить строки «Результат»/«Проверка» под HEAD дня с
обоснованием здесь.

## Шаги

- [x] **SF0 — нулёвка актуализации входов ✅ 2026-09-18 00:55 +03:00** (исполнитель — агент эпика SF в отдельном git
      worktree `agent-a513d874b5bf458e2`, ветка `worktree-agent-a513d874b5bf458e2` от HEAD `2b7e9e8`).
      **Входы сверены против HEAD:** `framework/AGENT_GUIDE.md` — точка вызова fable-цикла `:330–430`, пятое обязательство
      «claim is never wider than the observation» `:401–423`, раздел «Document & text hygiene» `:637`, «The form of an
      obligation» `:651`; `AGENT_GUIDE.md` (RU) — `:319–424`, пятое обязательство `:396–417`, «Гигиена документов и текста»
      `:671`, «Форма обязательства» `:685`; `/end-chat-soft` — EN шаг 5 «The farewell report» `:79`, RU шаг 5 `:93` (три
      строки прозы, списка нет); `/end-chat-force` — строка долга церемоний в шаге 1 обоих слоёв (маркированный список);
      KAIF-блок судьи — охоты 2.7 `framework/skills/fable-judge/SKILL.md:58–64`, форма «- **<Имя> (KAIF 2.7).** <класс> …
      Re-run: <команда>»; сводная строка охот — `:14`; `framework/spheres/_template.md` — «Verification by observation» `:44`,
      «Fraud table» `:49`, «Craft recipes» `:69`; `programming.md` — `:45` / `:59` / `:89`; `_index.md` перечисляет слоты
      абзацем «Since KAIF 1.5 …» `:14–18` (значит, слот дописывается и туда); `tools/check-framework.mjs` — `const PAIRS`
      `:418`, форма `['метка', 'файл', ['улика', …]]`, улики — ПОЛНЫЕ уникальные строки; `tools/build-framework.mjs` —
      `TEMPLATE_NOTES_BY_VERSION['2.7']` `:281–292` (десять записей). Собственные случаи класса на диске: `bugs/115`
      (ложное «grep → пусто» — поправлено той же сессией), `bugs/116` (ложь агента в чате и в промпте судьи — записана в
      баг), `bugs/70_DONE:45` ← **стоит до сих пор**, опровергнуто `bugs/84` вхождением 2 (взято в функциональный прогон SF5).
      **Проба ДО кода** (скретчпад `sf0-probe.mjs`, 2026-09-18 00:55 +03:00, три оси): греп двенадцати файлов канона,
      ритуалов, судьи и сфер на `retract / falsehood / standing false / стоячая ложь / ложь исправляется` → **0 попаданий**;
      фикстура отчёта сессии с поправкой в чате и без строки исправления написана, 32 охоты судьи прочитаны → **ни одна не
      достаёт ретроактивный случай**; четыре ритуала закрытия о ложном утверждении сессии не спрашивают → **RED, 6 красных
      осей**.
      `INTENT: code does <канон обоих слоёв нормирует РОЖДЕНИЕ заявления (пятое обязательство) и молчит о пережитом
      опровержении; ритуалы закрытия о ложном утверждении не спрашивают; библиотека сферы не называет команду отзыва ни на
      один внешний канал>; the failing check/task expects <процедура с командами в минуту опознания, ДО продолжения работы;
      класс назван; строка ритуала по имени; «канал → команда отзыва» в сферах — plans/100 критерий 21>; the spec says
      <#67: «Add to the corrections canon an explicit, ordered retraction procedure … (a) stop … (e) name in the reply what
      was corrected and where» · «A named artifact class … a standing falsehood» · «Sphere hook … so step (c) is executable
      rather than imagined»; AGENT_GUIDE → «Форма обязательства» (#22): правило прозой копит долг, правило с командой
      исполняется само>` — сходятся; расхождение X/Y — ровно дыра тикета.
      `FORK: options <A шестым обязательством KAIF в точке вызова fable-цикла, вплотную к пятому | B отдельным разделом в
      «Гигиене документов и текста» | C разделом в TESTING_FRAMEWORK рядом с фродом ложного [TESTED]> · price of error
      <A: длинная секция обязательств растёт ещё на абзац; B: узкое правило стоит вдали от правила о заявлениях, которое
      оно продолжает, — ровно класс, проигравший дважды на #37/#65; C: канон тестов говорит о прогонах, а класс — о
      заявлениях в докладе и в артефактах, и читают его не там> · consulted <issue #67 — предложение автора тикета, три
      пункта из четырёх адресованы канону и ритуалу; researches/30 §2е — «зеркальная грань #63»; AGENT_GUIDE → строка ворот
      `AUTH:` и её обоснование (#65): «узкое исключение, записанное вдали от правила, которое оно исключает, не удерживает
      поведение»; AGENT_GUIDE → «Форма обязательства» (#22)>` → **DECIDED [AI]: A** — шестым обязательством вплотную к
      пятому: #63 нормирует заявление при рождении, #67 — то же заявление после опровержения, и читаются они одним взглядом.
      Имя класса — «стоячая ложь» / standing falsehood — не развилка: его дал тикет (`A named artifact class … a standing
      falsehood`) и критерий 21. Сила — **правило + строка ритуала + охота судьи, без новой машинерии** (S2 лестницы
      тяжести; риск (а) плана): машинного признака у «утверждения, которое сессия сама позже опровергла», нет — его видит
      только судья и владелец.
- [x] **SF1 — раздел канона обоих слоёв ✅ 2026-09-18 00:57 +03:00** (`framework/AGENT_GUIDE.md:425–453` — "A FALSEHOOD IS
      CORRECTED WHERE IT STANDS", шестое обязательство KAIF; `AGENT_GUIDE.md:419–448` — «ЛОЖЬ ИСПРАВЛЯЕТСЯ ТАМ, ГДЕ
      ОСТАВЛЕНА»; слово владельца проекта — дословно по-русски в обвязке, rendered from Russian в поставке, как у пятого
      обязательства; перенос заголовка EN убран в одну строку 01:08, чтобы улика пары матчилась — класс SD3 `plans/109`).
      Прежняя формулировка: «Ложь исправляется там, где оставлена» / "A falsehood is corrected where it
      stands": триггер (утверждение опознано ложным — словом владельца, замером, поздним прогоном) → пять шагов с командами,
      ДО продолжения работы: (1) стоп текущей задаче; (2) перечень мест — `git grep -n "<фраза>"` по репозиторию + внешние
      каналы командой из библиотеки сферы (трекер, wiki, chat-ops, страница владельцу); (3) поправка или отзыв в каждом
      (правка · комментарий «поправка: …» · удаление, где канал позволяет); (4) чтение обратно; (5) в ответе владельцу —
      «исправлено: <где>»; граница — утверждения, доставленные наружу или записанные в документы/канон/статус; черновики с
      пометкой гипотезы — нет; имя класса — «стоячая ложь»; слово владельца тикета дословно.
- [x] **SF2 — ритуалы закрытия обоих слоёв ✅ 2026-09-18 00:58 +03:00** (`framework/skills/end-chat-soft/SKILL.md:82–92` и
      `.claude/skills/end-chat-soft/SKILL.md:96–106` — вопрос по имени плюс дословная строка `Standing falsehood: none` /
      «Стоячая ложь: нет»; `framework/skills/end-chat-force/SKILL.md:21–25` и `.claude/skills/end-chat-force/SKILL.md:21–26`
      — та же строка одной фразой в шаге 1, рядом со строкой долга церемоний). Прежняя формулировка: `/end-chat-soft` шаг 5: строка прощального отчёта «Стоячая ложь: нет | <список
      мест и что исправлено>» с вопросом по имени («какое утверждение этой сессии оказалось ложным и где оно ещё стоит?»);
      `/end-chat-force` — та же строка одной фразой.
- [x] **SF3 — охота судьи ✅ 2026-09-18 08:34 +03:00 (интегратор — сессия 66, Fable 5.1):** текст `plans/handoff_SF_DONE.md` § 1
      вставлен в KAIF-блок 2.7 обеих копий `fable-judge/SKILL.md` (после охоты «Resume word ignored»), сводная строка `:14`
      дополнена; копии сверены `cmp` — равны; девять пар `check-framework` (§ 3) и запись 2.7 сборщика (§ 2) внесены, сборка
      зелёная. Прежняя формулировка: **SF3 — судья** — НЕ ИСПОЛНЕН в ветке эпика по заданию: обе копии `fable-judge` закреплены за интегратором (главной
      сессией). Готовый текст охоты «Standing falsehood (KAIF 2.7)» в форме соседних охот 2.7 передан — `plans/handoff_SF_DONE.md`
      § 1. Охота «Standing falsehood (KAIF 2.7)» в KAIF-блоке обеих копий: утверждение, которое сама сессия
      позже опровергла (в чате, в заметке, в отчёте) и оставила стоять в доставленном или записанном артефакте; отчёт
      сессии без строки «стоячая ложь» при наличии поправки в записи — та же находка; re-run: греп фразы по репозиторию и
      по названным каналам.
- [x] **SF4 — библиотеки сфер ✅ 2026-09-18 01:00 +03:00** (`framework/spheres/_template.md:49–61` — слот с таблицей и
      правилом «канал без известной команды называется вслух»; `programming.md:59–74` — девять каналов, каждая команда
      сверена с `--help` установленного `gh 2.95.0` и не выдумана: `gh issue comment --edit-last --body-file`,
      `--delete-last --yes`, `gh issue edit --body-file`, `gh release edit <tag> --notes-file`, `git commit --allow-empty`;
      `_index.md:16–17` — слот в перечне слотов библиотеки сферы). Прежняя формулировка: `_template.md` — слот «Outward write channels → retraction command» (канал → команда
      поправки/отзыва); `programming.md` — git (поправка следующим коммитом с «correction:» в сообщении; комментарий к
      PR/issue «correction: …»); `_index.md` — если индекс перечисляет слоты; сферы поля дописывают свои.
- [x] **SF5 — сборка и функциональный прогон ✅ 2026-09-18 01:10 +03:00; пары и записи 2.7 — интегратору.** Сборка
      `node tools/build-framework.mjs` зелёная дважды (00:59 и 01:08 — «check-framework OK … 794 modules»); полигон
      `npm run test:core` — «all 26 suites green» (01:00); `counters-guard` после сборки покраснел на четырёх зеркалах числа
      модулей (792 → 794 от двух новых секций сфер) — зеркало в `AGENT_GUIDE.md` поправлено здесь, три оставшихся
      (`README.md` обе половины, `STATUS.md`) закреплены за интегратором; `attribution-lint check` — new 0; `doc-header-lint`
      — 0 новых находок; `testrun-lint check` — «8 report(s), 0 findings». **Функциональный прогон на реальном состоянии
      (01:05–01:06):** процедура исполнена по РЕАЛЬНОМУ утверждению истока — `bugs/70_DONE_owner_queue_zero_can_be_false_
      green.md:45` «обе половины доказаны фикстурами (краснеет / молчит)», опровергнутому ревизией R2 2026-08-14 (`bugs/84`
      вхождение 2, CONFIRMED · substantial) и простоявшему на своём месте 35 дней. Шаг 2 — `git grep -n "доказаны
      фикстурами"` (5 вхождений: 1 утверждение, 3 опровержения, 1 чужое); переисполнение опровержения сегодня — мутант
      `tools/questions-guard.mjs` со скоупом `if (iv.waiting)` дал `--selftest` **exit 0** («все 41 мутаций сбылись по
      предсказанию»), то есть фикстуры половины (б) по-прежнему нет; шаг 3 — поправка вписана ПОД утверждением; шаг 4 —
      прочитано обратно; шаг 5 — «исправлено: `bugs/70_DONE…:47–59`». Отчёт прогона —
      `testcases/reports/2026-09-18_standing-falsehood.md`, вердикт **partial** (охоты судьи и пар в этой ветке нет).
      Прежняя формулировка: Пары `check-framework` (AGENT_GUIDE оба слоя ↔ `/end-chat-soft` оба
      слоя ↔ `/end-chat-force` оба слоя ↔ судья ↔ `_template`); `TEMPLATE_NOTES['2.7']`; `KAIF_REFERENCE`; сборка → полигон
      → счётчики; **функциональный прогон на реальном состоянии** — процедура исполнена по одному РЕАЛЬНОМУ утверждению
      истока, опровергнутому позже и стоящему до сих пор (кандидаты — SF0: грепом сессионных поправок в `PROJECT_HISTORY`,
      `bugs/`, отчётах прогонов; нет ни одного → `Functional run: NONE`, вердикт `partial`, и так и сказано); отчёт прогона
      `testcases/reports/<дата>_standing-falsehood.md`.
- [ ] **SF6 — черновик ответа #67; закрытие.** Судья без окон; `plans/100` строка 14 и критерий 21; STATUS п. 0; MASTER_PLAN
      §5; урок в `EXPERIENCE.md`; коммит.

## Верификация наблюдением

| Утверждение | Артефакт | Где живёт |
|---|---|---|
| раздел, шаг ритуала и охота на месте в обоих слоях | пары `check-framework` | `tools/check-framework.mjs` |
| судья видит стоячую ложь | фикстура отчёта с поправкой без строки исправления — находка судьи | отчёт прогона |
| процедура исполнима на реальном случае | строка `Functional run:` отчёта | `testcases/reports/<дата>_standing-falsehood.md` |

## Риски (ярусы Мёрфи)

- **(а) Правило остаётся прозой** — форма обязательства: пять нумерованных шагов с командой в каждом, строка ритуала
  закрытия как чекбокс, охота судьи как проверка; новой машинерии не заводится (S2 лестницы тяжести).
- **(б) Команда отзыва зависит от канала** — библиотека сферы называет её на канал; неизвестный канал → «команда не
  названа» в ответе, не молчание.
- **(в) Ложное срабатывание судьи на гипотезах** — граница «черновик с пометкой гипотезы — не стоячая ложь» записана в
  разделе и в охоте.

## Решения, принятые агентом без владельца

1. **[AI] Место раздела — шестое обязательство KAIF в точке вызова fable-цикла, вплотную к пятому** (`FORK:` в SF0):
   #63 нормирует заявление при рождении, #67 — то же заявление после опровержения; узкое правило, записанное вдали от
   правила, которое оно продолжает, дважды проиграло широкому рефлексу (#37/#65). Раздел «Гигиена документов и текста»
   и `TESTING_FRAMEWORK` отвергнуты там же.
2. **[AI] Сила — правило + строка ритуала + охота судьи; новой машинерии нет.** Машинного признака у «утверждения,
   которое сессия сама позже опровергла», не существует: опровержение живёт в чате и в суждении. Страж, который
   попытался бы его угадать, красил бы честные поправки. S2 лестницы тяжести, риск (а) плана.
3. **[AI] Граница класса расширена одним случаем против формулировки плана:** к «черновику с пометкой гипотезы» добавлена
   запись append-only журнала — там поправка ЕСТЬ новая запись (канон `AGENT_GUIDE` о журнале решений), но новая запись
   обязана назвать ту, которую поправляет. Без этой оговорки процедура столкнулась бы с правилом append-only.
4. **[AI] Строка ритуала закрытия несёт и отказ:** «место поправить не удалось → назови место и ненайденную команду
   отзыва вместо ответа „нет“». Правило (б) рисков плана требует не молчать о канале без команды; ответ «нет» при
   непоправленном месте был бы тем же классом.
5. **[AI] Команды в `programming.md` сверены с `--help`, а не взяты по памяти:** `gh 2.95.0` на этой машине подтвердил
   `--edit-last`, `--delete-last --yes`, `--body-file`, `gh release edit --notes-file` (правило трёх дверей: выдуманная
   команда хуже отсутствующей). Каналы, чьей команды у истока нет (chat-ops чужого бота), описаны ходом, а не командой.
6. **[AI] `KAIF_REFERENCE` в этой ветке НЕ правился:** эпик не отгружает ни команды, ни модуля, ни файла — справочник
   устройства описывает машинерию, а обязательства попадают в него через инструмент. Предложение одной фразой (место —
   `framework/KAIF_REFERENCE.md:136`, описание `/end-chat-soft` + `/end-chat-force`) передано интегратору в
   `plans/handoff_SF_DONE.md` § 6 — решение за ним.
7. **[AI] Функциональный прогон взял ЧУЖОЕ старое утверждение, а не собственную свежую ошибку:** кандидаты из `bugs/115`
   и `bugs/116` были поправлены своими сессиями (класс уже закрыт), а `bugs/70_DONE:45` — единственное найденное
   утверждение, которое опровергнуто НА ДИСКЕ и до сих пор стоит; опровержение переисполнено сегодня мутацией, чтобы
   поправка не оказалась заявлением шире наблюдения.
8. **[AI] Зеркало счётчика модулей поправлено только в `AGENT_GUIDE.md`:** `README.md` и `STATUS.md` закреплены за
   интегратором заданием; `counters-guard` остаётся красным на трёх строках, и это сказано в отчёте прогона и в передаче,
   а не спрятано.

## Links

`plans/100` строка 14, критерий 21 · `researches/30` §2е · issue #67 · issues #63 / #65 (родня класса) · `bugs/115` ·
`plans/107` (CL — пятое обязательство, форма).
