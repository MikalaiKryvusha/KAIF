# План 114 — эпик EL «Урок, повторившийся дважды, краснеет»

> **Создан:** 2026-09-18 08:30 +03:00 (сессия 66, Fable 5.1 — разрез AU по мандату владельца №121; `plans/100` § «Решения» 12).
> **Родитель:** `plans/100` (эпик EL — строка 18; критерий приёмки версии 24). Источник — issue #69 (агент-аудитор проекта
> Unliminium, KAIF 2.5; 2026-09-17 22:18 UTC; замер по журналу проекта целиком: «7 из 120 записей о провалах механизированы,
> 14 из 15 классов провалов повторились ПОСЛЕ записи урока, 4 — ПОСЛЕ построенного стража; пять уроков записаны по 6–17 раз
> разными словами»; тикет сам называет рецидив закрытого **#14** — эпик X 2.3); разбор — `researches/30` §2и. Слово
> владельца KAIF о тикетах аудита — №121 («самые критические, самые ценные и важные — в 2.7»).
> **Статус:** 🔲 запланирован 2026-09-18 08:30 +03:00 — исполняется субагентом в отдельном git worktree параллельно
> интеграции SF/FR (порядок — `plans/100` § «Решения» 12); старт с нулёвки EL0 (№76). Файлы, которые worktree НЕ правит
> (за интегратором, текстами в `plans/handoff_EL.md`): обе копии `fable-judge/SKILL.md`, `plans/100`, `STATUS.md`,
> `MASTER_PLAN.md`, `EXPERIENCE.md` (кроме шапки-списка классов — см. EL2), `README.md`, `tools/build-framework.mjs`,
> `tools/check-framework.mjs`.
> **Вовне:** поставка — новый tool-модуль `framework/tools/kaif-experience-lint.mjs` (→ `.kaif/tools/`), `/experience` и
> `/end-chat-soft` обоих слоёв, шаблон `framework/EXPERIENCE.md`; исток — `tools/experience-lint.mjs` становится обёрткой над
> модулем поставки; охота `/fable-judge`; запись 2.7 сборщика; черновик ответа #69 (отправка после релиза — №84/№92/№93).

## Вектор цели (Achieve)

**Боль.** Правило KAIF «два ожога → механизм, третьего напоминания не бывает» существует прозой шапки журнала и полем
механизации у ОДНОЙ записи; повтор класса никто не считает — теги свободные, «тот же класс» видит только человек, прочитавший
журнал целиком. Полевой замер тикета: 5,8 % механизации, 14 из 15 классов повторились после урока; на пяти развёртываниях
на диске поле механизации стоит у 1–19 % записей, поля класса нет ни в одном (проба 2026-09-18 08:18 +03:00). Страж истока
`tools/experience-lint.mjs` (X 2.3, #14) судит наличие поля и живёт только в обвязке — механизация #14 не уехала полем
(`researches/30` §2и). Класс: рецидив закрытого тикета в форме «правило ушло в прозу и в один инструмент истока» (родня
#37 → #65).

**Где хотим оказаться.** Критерий 24 `plans/100`: у каждой записи журнала есть `class: <слаг>`; модуль поставки
`kaif-experience-lint` краснеет на ВТОРОЙ записи ❌ одного класса без `mechanized:` и называет класс и обе записи по номеру
(починка — назвать страж, не третья запись); предупреждает о `mechanized:` с несуществующей командой; `--shrink <id>`
схлопывает механизированную запись до строки с указателем; без полей класса — `SKIPPED=3` вслух; команда стоит в
`/end-chat-soft` обоих слоёв и в итерации циклов; исток ест свою поставку (обёртка `tools/experience-lint.mjs`), журнал
истока размечен классами; судья охотится на третью запись класса без механизма.

**Метрика эпика:** критерий 24 `plans/100` закрыт **0 из 1**.

## Готово, когда (критерии приёмки)

Сценарий — дословно критерий **24** `plans/100`; нулёвка EL0 вправе уточнить строки «Результат»/«Проверка» под HEAD дня с
обоснованием здесь.

## Шаги

- [ ] **EL0 — нулёвка актуализации входов (№76).** Сверить против HEAD: `tools/experience-lint.mjs` (разбор записей,
      поля И1/И4, baseline, `--selftest`); `/experience` обоих слоёв (шаг 0, поле **Mechanization**, где стоит формат записи);
      шаблон `framework/EXPERIENCE.md` (шапка — формат записи, абзац «Two strikes»); шапка `EXPERIENCE.md` истока;
      `/end-chat-soft` обоих слоёв (строка `experience-lint` — есть у истока `:44`, в поставке — проверить); навыки циклов
      (`/autoloop` итерация — где стоят линтеры); образец tool-модуля с `SKIPPED=3` и `selftest` — `kaif-requirements-lint`
      и `kaif-testrun-lint` (форма шапки, `check`/`selftest`, ключевые слова по языкам, правила как данные); как сборщик
      встраивает `framework/tools/*.mjs` (`s05`/`s06` — своды tool-модулей); `TEMPLATE_NOTES_BY_VERSION['2.7']` (`tools/build-framework.mjs`,
      только чтение — запись текстом в handoff). Соседи на диске — только чтение: журналы Unliminium (`class:` в EXP-0168 —
      формат, который проект уже принял: `class: <слаг> · mechanized: <команда>` строкой после тегов), ndim (63 поля механизации на
      328 записей — на нём же прикинуть, как лягут классы). `INTENT:` + `FORK:` (форма поля класса: строка `class:` отдельно | слаг
      в теге `#class:…` | в строке заголовка записи; consulted — формат Unliminium, `/experience` шаг 0, `kaif-testrun-lint`
      «правила как данные»). **Проба ДО кода** (скретчпад): фикстура журнала с двумя ❌ одного класса без механизации →
      `node .kaif/tools/kaif-experience-lint.mjs check` на копии `dist` 2.6/HEAD → модуля нет → красный по построению;
      `node tools/experience-lint.mjs` истока на той же фикстуре → зелёный (поле есть у каждой записи) — дыра тикета воспроизведена.
- [ ] **EL1 — модуль поставки `framework/tools/kaif-experience-lint.mjs`** (критерий 24, «Проверка» пп. 1–4). Перенос логики
      истока (разбор `### EXP-NNNN`, И1/И4, baseline `--write-baseline` только руками) + оси: **repeat** — по `class:` ≥ 2 записи ❌
      или ❌→✅ без `mechanized:`/`механизировано:` → код 1, строка `class <слаг>: EXP-A, EXP-B — name the guard, not a third
      record`; **dangling** — `mechanized:` называет команду/файл, которого нет (`package.json` scripts · `tools/` · `.kaif/tools/`
      · путь в кавычках) → предупреждение; `--shrink <id>` — тело записи с `mechanized:` заменяется одной строкой «урок → страж
      <инструмент>» + указателем на репро (правило `BUG_FIXING` «механизированный урок схлопывается» получает команду; сам
      текст записи уезжает в git-историю); `SKIPPED=3` с фразой вслух, когда в журнале нет ни одного `class:`; ключевые слова
      обоих языков (как `kaif-testrun-lint`); `selftest` — красный на «два ❌ одного класса», на «висячей команде», молчание на
      чистом и на записи с `mechanized:`; блок `@guard` четырьмя полями; `[NOT-TESTED]` в шапке до функционального прогона.
      Список классов — в шапке журнала (`<!-- classes: … -->` или таблица «Классы уроков»), новые слаги разрешены (модуль
      предупреждает о слаге вне списка, не отказывает).
- [ ] **EL2 — поле `class:` в записи обоих слоёв + разметка журнала истока.** `/experience` обоих слоёв: шаг записи — строка
      `class: <слаг>` (после тегов, перед `Repro:`; слаг из списка шапки или новый — тогда добавить в список); шаблон
      `framework/EXPERIENCE.md` — формат записи + список классов-заготовок (из тикета #69 таблицы: `question-already-answered` ·
      `guard-not-proven-against-threat` · `shown-as-link` · `claim-before-evidence` · `owner-decision-not-applied` ·
      `text-in-agents-world` · `etalon-from-dirty-tree` · `shell-lied` · `escaping-layer` · `twins-missed` · `field-dropped-in-rebuild`
      — как ЗАГОТОВКА, не догма); шапка `EXPERIENCE.md` истока — список классов; **разметка записей истока** — `class:` у
      каждой из 136 записей скриптом по тегам (слабая сессия: код классифицирует по тегам, спорные — рукой; список спорных
      в отчёте прогона); после разметки `node tools/experience-lint.mjs` (обёртка) — ожидаемо КРАСНЫЙ на реальных повторах
      истока → это и есть функциональный прогон EL5: каждую красную пару либо механизировать (назвать существующий страж —
      у истока их десятки), либо `none-cheap:` с причиной; результат — список пар и их судьба в отчёте.
- [ ] **EL3 — исток ест свою поставку.** `tools/experience-lint.mjs` → обёртка ≤ 40 строк над `framework/tools/kaif-experience-lint.mjs`
      (как `tools/review.mjs` → контур): baseline истока остаётся у обёртки; `/end-chat-soft` обоих слоёв — строка команды
      (у истока уже есть, обновить путь/смысл: «повтор класса без механизма — красный»); `/autoloop`/`/dayloop`/`/nightloop`
      обоих слоёв — там, где стоят линтеры итерации, та же команда (если линтеров в итерации нет — только `/end-chat-soft` и сказать это).
- [ ] **EL4 — свод полигона и красное доказательство.** Свод в `tools/sandbox/` (новый `s28-experience-lint.mjs` или ассерты в
      `s05`/`s06` рядом с соседями-модулями — по образцу EL0): модуль доехал в `.kaif/tools/`; фикстуры — два ❌ одного класса без
      механизации → код 1 с обоими номерами; журнал без `class:` → `SKIPPED=3`; висячая команда → предупреждение; `--shrink`
      на копии → одна строка + указатель; на ядре 2.6 (`KAIF_DIST`) — модуля нет → ✖ по имени; мутант модуля (ось repeat
      сломана) → ровно ассерты repeat красные. `node tools/build-framework.mjs` → `node tools/counters-guard.mjs` (число
      модулей/блоков сдвинется — зеркала переписать с вывода сборки; README трогать нельзя → число в handoff) → `npm run test:core`
      (`env: quietEnv()` в своде — `bugs/116`).
- [ ] **EL5 — функциональный прогон по реальному состоянию + отчёт.** Реальное состояние — журнал истока после разметки EL2
      (не фикстура): прогон обёртки, чтение КРАСНЫХ пар, судьба каждой (страж назван | `none-cheap`), повторный прогон —
      зелёный; отчёт `testcases/reports/2026-09-18_experience-lint.md` по шаблону (семь полей, `Hygiene:`/`Functional run:`
      раздельно; `node framework/tools/kaif-testrun-lint.mjs check` → 0). Соседи на диске — только чтение: прогон модуля по
      КОПИИ журнала ndim или Unliminium как второй функциональный кейс (сколько классов, сколько красных пар — числа в отчёт;
      исходник не трогать).
- [ ] **EL6 — тексты интегратору (`plans/handoff_EL.md`).** Охота судьи «Lesson repeated without a mechanism (KAIF 2.7)» для
      обеих копий побайтно (форма — как соседи в KAIF-блоке 2.7: класс · где правило · Re-run: команда) + сводная строка `:14`;
      запись `TEMPLATE_NOTES_BY_VERSION['2.7']` (одна строка-литерал, стиль соседей); пары `check-framework` (полные уникальные
      улики: модуль · `/experience` обоих слоёв · `/end-chat-soft` обоих слоёв · шаблон журнала · охота); фраза в
      `framework/KAIF_REFERENCE.md` (перечень tool-модулей — править ИСТОЧНИК, корневая генерируется); строки `plans/100`
      (строка 18, критерий 24, метрика); черновик ответа #69 (EN; что взято, что нет, почему; «one honest boundary» — что
      не наблюдалось); черновик урока EXPERIENCE (с полем `class:` — первая запись нового формата); `README.md` — если
      счётчики сдвинулись, числа с вывода сборки. `kaif-scenario-lint check plans/114…` — `SKIPPED` законен (сценарий живёт
      в `plans/100`), сказать это.
- [ ] **EL7 — закрытие (главная сессия).** Слияние ветки, вставка текстов handoff, пересборка, счётчики, полигон, судья без
      окон; `plans/100` строка 18 и критерий 24 ✅; STATUS; MASTER_PLAN §5; удаление handoff.

## Верификация наблюдением

| Утверждение | Артефакт | Где живёт |
|---|---|---|
| вторая запись класса без механизма — красная, с обоими номерами | свод полигона + `selftest` модуля | `tools/sandbox/`, `framework/tools/kaif-experience-lint.mjs` |
| модуль доехал в поставку; на 2.6 его нет | свод, `KAIF_DIST` красный | `tools/sandbox/` |
| исток размечен и проходит свой же гейт | отчёт прогона, строка `Functional run:` | `testcases/reports/2026-09-18_experience-lint.md` |
| правило стоит в навыках обоих слоёв | пары `check-framework` (текст в handoff) | `tools/check-framework.mjs` |

## Риски (ярусы Мёрфи)

- **(а) Разметка 136 записей истока классами — ручная работа сильной модели.** Защита: код классифицирует по тегам
  (таблица тег → класс), рукой — только спорные; список спорных — в отчёт; разметка не обязана быть идеальной — ось судит
  повторы внутри класса, ошибочный класс даёт ложный зелёный, не ложный красный.
- **(а) Красный на реальном журнале истока на закрытии чата** — это цель, не дефект: каждая пара получает судьбу в EL5;
  baseline обёртки НЕ растёт (линия только убывает — прецедент `doc-header-lint`).
- **(б) `--shrink` теряет текст урока** — текст остаётся в git-истории; команда печатает удалённое тело в stdout и
  требует `--yes`; без флага — показ, не запись.
- **(б) Слаги классов расходятся между проектами** — список в шапке журнала свой у проекта; модуль предупреждает о слаге
  вне списка, не отказывает; заготовка списка — из тикета #69 (то, что аудитор уже измерил).
- **(в) Ложное срабатывание на ❌→✅ записях** — запись, чей маркер ❌→✅ и есть `mechanized:`, повтором не считается;
  повтор — только записи БЕЗ `mechanized:` (в том числе `none-cheap:` — две `none-cheap` одного класса тоже красный:
  «дёшево невозможно» дважды — повод перепроверить цену).

## Решения, принятые агентом без владельца

Заполняется исполнителем при закрытии (каждое — `[ИИ]` с тем, как выбирал).

## Links

`plans/100` строка 18, критерий 24, § «Решения» 12 · `researches/30` §2и · issue #69 · issue #14 (родня) · `tools/experience-lint.mjs`
(исток, X 2.3) · `framework/skills/experience/SKILL.md` · `framework/tools/kaif-testrun-lint.mjs` (образец формы модуля) ·
`plans/109` (образец исполненного эпика) · `plans/113` + `plans/handoff_FR.md` (образец работы в worktree и передачи).
