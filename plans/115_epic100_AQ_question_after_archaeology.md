# План 115 — эпик AQ «Вопрос владельцу — после археологии» <!-- questions-guard:allow имя эпика в заголовке плана, а не очередь вопросов; место вопросов — interviews/ -->

> **Создан:** 2026-09-18 08:30 +03:00 (сессия 66, Fable 5.1 — разрез AU по мандату владельца №121; `plans/100` § «Решения» 12).
> **Родитель:** `plans/100` (эпик AQ — строка 19; критерий приёмки версии 25). Источник — issue #70 (агент-аудитор проекта
> Unliminium, KAIF 2.5; 2026-09-17 22:18 UTC; «13 вопросов принесены владельцу, хотя его прошлые ответы, `GOAL.md` или прогон
> стенда их уже решили; один — через 44 дня после ответа»; слово владельца проекта дословно в тикете: «ты меня спрашиваешь? В
> GOAL.md заглядывал, умник, прежде чем вопрос задать?» · «Ты задаёшь мне вопросы — совершенно не ознакомившись с историей
> решений… Мы уже обсуждали. Ищи» · «мы уже исправили левелинг, а ты опять несёшь мне этот вопрос» · «Уже поднимали вопрос! И
> переименовывали!»); тикет привязывает себя к #56 (QL1: проверка формы до зова) как «вторую ось той же двери»; разбор —
> `researches/30` §2к.
> **Статус:** ✅ закрыт 2026-09-18 10:17 +03:00 (AQ7 — интегратор, сессия 66; ветка слита после LP; критерий 25 `plans/100` ✅ с оговорками) · шаги AQ0–AQ6 ИСПОЛНЕНЫ 2026-09-18 09:12 +03:00 (субагент, worktree `agent-a35ba6da0323858be`,
> ветка `worktree-agent-a35ba6da0323858be`; отчёт `testcases/reports/2026-09-18_question-archaeology.md`, передача
> `plans/handoff_AQ_DONE.md`); AQ7 — за главной сессией, слияние ПОСЛЕ LP. Прежний статус: 🔲 запланирован 2026-09-18 08:30 +03:00 — исполняется субагентом в отдельном git worktree; в очереди версии —
> ПОСЛЕ LP (оба эпика трогают генератор контура; `plans/100` § «Решения» 12), но независимая работа (навык, стража истока,
> статья спецификации, свод) может идти раньше. Старт с нулёвки AQ0 (№76). Файлы, которые worktree НЕ правит (за
> интегратором, текстами в `plans/handoff_AQ_DONE.md`): обе копии `fable-judge/SKILL.md`, `plans/100`, `STATUS.md`, `MASTER_PLAN.md`,
> `EXPERIENCE.md`, `README.md`, `tools/build-framework.mjs`, `tools/check-framework.mjs`.
> **Вовне:** поставка — `framework/tools/contour/core.mjs` (`checkForm`/`preflight`) и `review.mjs`, `/interview` обоих слоёв,
> статья `framework/templates/_interactive-contour-spec.md`; исток — ось G9 `tools/questions-guard.mjs`; охота `/fable-judge`;
> запись 2.7 сборщика; черновик ответа #70 (отправка после релиза — №84/№92/№93).

## Вектор цели (Achieve)

**Боль.** Правило «не спрашивай решённого» стоит в `/interview` дважды прозой (`:36`, `:97`) без команды; дверь `--check`
(QL1, #56) судит только ФОРМУ вопроса; ось стража истока G4–G8 археологии не знает; ни одно из 223 интервью пяти
развёртываний на диске не несёт следа поиска (проба 2026-09-18 08:18 +03:00) — включая интервью №033 самого истока этой
сессии. Владелец проекта ответил на один и тот же вопрос дважды тринадцать раз; цена — его время и доверие (51 из 68 его
дословных цитат в журнале проекта — поправки). Класс: «вопрос владельцу — утверждение „этого ещё не решено“, и оно не
проверяется как утверждение» (родня пятого обязательства и оси G7).

**Где хотим оказаться.** Критерий 25 `plans/100`: перед вариантами каждого живого вопроса стоит строка-аттестация
`<!-- archaeology: grep -rniE "<существительные вопроса>" interviews/ GOAL.md MASTER_PLAN.md plans/ → N hits · read: <файлы |
none> · prior: <none | «прошлый ответ …» + адрес> -->`, вставленная из реального прогона; дверь `checkForm`/`preflight`
генератора отказывает открыть живой вопрос без неё (код 3, печатает команду) — вперёд по дате шапки документа
(≥ 2026-09-18), отвеченные вопросы и документы без вопросов — вне оси; `/interview` шаг 3 несёт команду и формат вместо прозы;
у истока — ось G9 стража того же класса; судья охотится на вопрос, показанный мимо археологии, и на аттестацию с N > 0 без
строки `prior:`.

**Метрика эпика:** критерий 25 `plans/100` закрыт **1 из 1** (✅ 2026-09-18 10:17 +03:00, интегратор — сессия 66) (отметка внесена сессией 67 при нулёвке RL0 2026-09-18 13:00 +03:00: тело плана осталось «не закрыто» после закрытия в `plans/100`). Прежняя строка: закрыт **0 из 1** — машинная половина сделана и наблюдена (отчёт
прогона), клетка ✅ ставится в `plans/100` интегратором после AQ7: до внесения охоты судьи, записей сборщика и пар
реестра критерий закрытым не считается.

## Готово, когда (критерии приёмки)

Сценарий — дословно критерий **25** `plans/100`; нулёвка AQ0 вправе уточнить строки «Результат»/«Проверка» под HEAD дня с
обоснованием здесь.

## Шаги

- [x] **AQ0 — нулёвка актуализации входов (№76)** — сделано 2026-09-18 08:41 +03:00 (субагент, worktree
      `agent-a35ba6da0323858be`; ветка от `803ef45` доведена до `df8110b` — `merge --ff-only main`, иначе плана 115 в дереве не
      было). **Сверка входов против HEAD `df8110b`:** `framework/tools/contour/core.mjs` (было 489 строк) — `parseMetaBlock:115`,
      `docStatus:139` (голова документа — `HEAD_LINES = 30`), `parseQuestions:168` (живой вопрос = `!q.answered`; `answered` =
      закрытый статус или непустой `**Answer:**`), `finishQuestion:215` (варианты двух форм, `optionTableLines` — один разбор
      странице и стражу), `preflight:274` (массив строк-проблем → `gateForOpen` → код 3), `checkForm:291` (данные для `--check`);
      `review.mjs` — `gateForOpen:1018` (печать «PRE-FLIGHT REFUSED TO OPEN (spec §2, exit 3)»), `checkDoc:1041` (строки
      `t.check.*`, код 3/0, ни сервера, ни зова), селфтест `:1071`; `/interview` — проза `framework:36` («Look at past
      interviews (`ls interviews/`) so you don't duplicate accepted decisions»), `framework:97` («Don't ask what's already
      decided in `plans/`/`MASTER_PLAN.md` or past interviews»), те же места ru-копии `:36`/`:96`, шаг 3c `framework:171`;
      статья 2 спецификации — `framework/templates/_interactive-contour-spec.md:29` (файл 119 строк; **s22 требует ≤ 120** —
      бюджет статьи одна строка); `tools/questions-guard.mjs` — оси G4 `:398`, G5/G6 `:441`, G7 `:267`, G8 `:447`, `headDate`
      `:198` (первая ISO-дата в первых 12 строках), `@guard` четырьмя полями `:142`, селфтест мутациями `:632`.
      **Три поправки плана, найденные нулёвкой:**
      (1) **имя оси стража — `G11`, не `G9`:** в HEAD `G9` уже занята (2.7 QL2, issue #54 — «ВНЕСЕНО, НО ОТКРЫТО»,
      `tools/questions-guard.mjs:90`), `G10` — селфтест мутациями (`:18`); следующее свободное имя — `G11`;
      (2) **`.kaif/INTERACTIVE_CONTOUR_SPEC.md` у истока НЕ лежит** — страница едет только бандлом
      (`tools/build-framework.mjs:499`, `embedBundle`, «bundle-only»): пересобирать копию нечего, правится источник
      `framework/templates/_interactive-contour-spec.md`; **номерованный инвариант `I46` в `/owner-reviews` НЕ заводится** —
      нумерация живёт в обеих копиях того навыка (он вне «Вовне» этого плана и вне списка интегратора), а §2 статьи уже несёт
      соседнюю дверь `--check` (#56) БЕЗ номера: новая ось встаёт той же формой, номер — кандидат интегратору (handoff);
      (3) **свод — `s22`** (а не новый): контур поставкой судит именно он, у него уже стоит шов `KAIF_DIST`, развёртывание из
      бандла и тихое окружение `quietEnv()`; новый свод дублировал бы развёртывание ради пяти ассертов.
      `INTENT:` дверь показа получает ВТОРУЮ ось того же предполёта (не новый инструмент): ось археологии — свойство ВОПРОСА,
      и живёт там, где уже судится форма вопроса, одним разбором на дверь, стража и судью.
      `FORK: options <A: HTML-комментарий перед вариантами | B: видимая строка «**Археология:**» | C: поле шапки документа> ·
      price of error <A — владелец аттестации не видит (плюс: словарь агента не у него, G8), но и не может возразить; B —
      владелец читает кухню агента в своём вопросе (ровно класс bugs/112, за который заплатили четырежды); C — шапка одна на
      документ, а вопросов в нём до десяти: аттестация перестаёт быть свойством ВОПРОСА и первая же пара вопросов ломает ось> ·
      consulted <тикет #70 предлагает комментарий дословно · `questions-guard` G8 и слово владельца №106 («Я Заказчик») — текст
      владельцу не несёт словаря агента · статья §4 спецификации и `renderMd` I24: комментарии вне код-блоков со страницы
      срезаются, значит носитель A по построению не доедет до владельца> · decided A`.
      `FORK: options <A: дата шапки = первая ISO-дата головы (как `headDate` стража) | B: дата строки `Created`/`Создан`, при
      её отсутствии — первая ISO-дата головы> · price of error <A: у живого интервью в голове стоит ДАТА ОТВЕТА или правки
      статуса (проба: `interviews/interview_032…` — первая дата головы `2026-09-18`, строка `Created` — `2026-09-13`): старый
      документ становится «новым» и краснеет — ровно риск (а) плана> · consulted <риск (а) самого плана: «прошлые интервью поля
      красным не становятся»; проба по живому корпусу истока 08:39> · decided B` (ассерт селфтеста назван поимённо).
      **Проба ДО кода (08:35–08:37, дыра воспроизведена):** фикстура живого вопроса «Как назвать валюту игры?» с датой шапки
      сегодня (скретчпад `aq0-fixture-live-question.md`) → `node framework/tools/contour/review.mjs <фикстура> --check` →
      **код 0**, «проверка OK — страницу можно открывать»; `node tools/questions-guard.mjs --root <копия>` по той же фикстуре →
      «Новых нарушений: 0». Ни дверь, ни страж об археологии не знают — оба плеча дыры наблюдены.
      **Прежняя формулировка шага (дословно, история не переписывается):** «AQ0 — нулёвка актуализации входов (№76). Сверить против HEAD: `framework/tools/contour/core.mjs` — `parseQuestions`
      (что такое «живой вопрос», где отвеченные), `preflight` (форма проблем: массив строк → код 3), `checkForm`, `parseMetaBlock`/
      `docStatus` (дата шапки — откуда брать порог «вперёд»); `review.mjs` — где печатается отказ `--check` и отказ перед показом
      (`:1023`, `:1044`), как `s22` его судит; `/interview` обоих слоёв — шаг 3 (`:36`, `:97`), шаг 3c (дверь `--check`); статья 2
      спецификации контура «Pre-flight» (`framework/templates/_interactive-contour-spec.md:26`) — форма статей и нумерация
      инвариантов; `tools/questions-guard.mjs` — оси G6–G8 как образец (форма, маркер исключения, «вперёд по дате шапки», `@guard`);
      `interviews/` истока — сколько живых документов попадут под ось (порог по дате делает старые немыми); `TEMPLATE_NOTES_BY_VERSION['2.7']`
      и `POLICY_CHANGES_BY_VERSION` (только чтение — тексты в handoff; ужесточение двери — policy change 2.7?). Соседи на диске —
      только чтение: интервью Unliminium (`interview_088` — написано ПОСЛЕ грепа, формат аттестации ещё не принят — как выглядит их
      «Разведка:»/«Archaeology:» блок, если есть), KAGO (2 файла со словом «археология» — что там). `INTENT:` + `FORK:` (носитель
      аттестации: HTML-комментарий перед вариантами | строка `**Археология:**` видимая владельцу | поле шапки документа; consulted —
      тикет #70 (предлагает комментарий), `questions-guard` G7 (блок `Разведка:` видимый), спецификация контура (что рендерится
      владельцу — комментарий не рендерится, и владелец его не читает: это ПЛЮС, ось G8 «словарь агента»). **Проба ДО кода**
      (скретчпад): фикстура интервью с живым вопросом без аттестации, дата шапки сегодня → `node framework/tools/contour/review.mjs
      <фикстура> --check` → код 0 (дыра воспроизведена); `node tools/questions-guard.mjs` на той же фикстуре → тишина».
- [x] **AQ1 — дверь генератора ✅ 2026-09-18 08:47 +03:00** (критерий 25, «Проверка» пп. 1–3). Сделано в
      `framework/tools/contour/core.mjs`: именованные константы `ARCHAEOLOGY_SINCE = '2026-09-18'`, `ARCHAEOLOGY_PATHS`,
      `ARCHAEOLOGY_MIN_LETTERS = 4` (+ фолбэк 3 для заголовка из одних коротких слов), `ARCHAEOLOGY_MAX_WORDS = 6`,
      `ARCHAEOLOGY_STEM_FROM = 6` / `ARCHAEOLOGY_STEM_CUT = 2`; функции `headerDate` (строка `Created`/`Создан` сильнее первой
      ISO-даты головы — FORK AQ0), `archaeologyWords`, `archaeologyGrep` (ГОТОВАЯ команда), `archaeologyOf` (зона — от заголовка
      вопроса до `q.firstOptionLine`, новое поле разбора), `archaeology(md)` (ДАННЫЕ: `{judged, headerDate, live, attested,
      exempt, problems}` — один разбор двери, стражу и судье) и `archaeologyProblems` (текст отказа на языке машинерии, как у
      соседа #51). `preflight` получил вторую ось; `checkForm` — поле `archaeology`; `checkDoc` печатает
      `t.check.archaeology` / `t.check.archaeologyOld` (обе — EN и RU в `texts.mjs`, там же данные `PARSER.createdLabels` и
      `PARSER.archaeologyStopWords`: кириллица законна ТОЛЬКО в этом файле — он объявлен носителем данных в
      `tools/check-framework.mjs:43`). **Четвёртая проблема сверх плана — `malformed`** (аттестация без `→ N hits` или без
      `prior:`): без неё любой комментарий `<!-- archaeology: искал -->` проходил бы дверь как аттестация — fail-open.
      Наблюдение: `node framework/tools/contour/review.mjs --selftest` → **73 проверки зелёные**, из них десять новых оси
      (красный без аттестации с напечатанной командой · зелёный с ней · красный на `N hits` > 0 с `prior: none` · зелёный на
      `prior: unrelated — …` · красный на кривой форме · зелёный на `n/a — причина` · зелёный на отвеченном · зелёный и строка
      «not judged» на дате шапки до порога · дата шапки берётся со строки `Created`).
      **Прежняя формулировка шага:** «AQ1 — дверь генератора (критерий 25, «Проверка» пп. 1–3). `core.mjs`: `preflight(md)` получает ось `archaeology` —
      для каждого ЖИВОГО вопроса документа с датой шапки ≥ `2026-09-18` (именованная константа `ARCHAEOLOGY_SINCE`) ищется строка
      `<!-- archaeology: … → N hits · read: … · prior: … -->` между заголовком вопроса и первым вариантом; нет — проблема с
      текстом отказа, несущим ГОТОВУЮ команду `grep -rniE "<слова заголовка вопроса>" interviews/ GOAL.md MASTER_PLAN.md plans/`
      (слова — существительные заголовка `### Q…`, машинно: токены ≥ 4 букв без стоп-слов); аттестация с `N hits`, где N > 0, и
      `prior: none` — тоже проблема («hits found, no prior line»: либо назвать прошлый ответ, либо `prior: unrelated — <почему>`);
      объявленное исключение — `<!-- archaeology: n/a — <причина> -->` (вопрос-имя, класс «вкус»). `checkForm` печатает ось в
      отчёте `--check` («archaeology: N of M live questions attested»). Отвеченные вопросы и документы без вопросов — молчание.
      Селфтест генератора (`review.mjs --selftest`) — оба ответа».
- [x] **AQ2 — `/interview` обоих слоёв: шаг 3 — командой ✅ 2026-09-18 08:49 +03:00.** Проза получила носителя в
      ОБОИХ слоях: новый **шаг 3d** (`framework/skills/interview/SKILL.md`, перед шагом 4) / **шаг 3г**
      (`.claude/skills/interview/SKILL.md`) — пять нумерованных пунктов: (1) выполнить команду, которую печатает
      дверь, (2) ПРОЧИТАТЬ попадания (файлы, не число), (3) вписать аттестацию между заголовком и ПЕРВЫМ вариантом,
      (4) прошлый ответ найден → вопрос не идёт владельцу: снять и перенести решение (`--mark-implemented`) либо
      переформулировать «прошлый ответ был X; изменилось Y», (5) что делает дверь без аттестации (код 3, команда
      напечатана, ничего не показано и никто не позван; `n/a — причина`; документы старше порога не судятся).
      Прозаические строки `:36` и `:97` обоих слоёв стали УКАЗАТЕЛЯМИ на этот шаг («посмотреть — не искать»,
      «правило держится не решимостью»); в шаге 3c/3в названа вторая ось той же двери. Слово владельца полевого
      проекта — в ru-копии дословно, в EN-копии переводом (инвариант §9.11: в поставке кириллицы нет).
      Описание навыка (`description`) НЕ менялось — пины ru-пакета не двигались.
      **Прежняя формулировка шага:** «AQ2 — `/interview` обоих слоёв: шаг 3 — командой. Проза `:36` и `:97` заменяется шагом: (1) команда грепа по
      существительным вопроса (та же, что печатает дверь), (2) чтение попаданий, (3) строка аттестации перед вариантами с N,
      списком прочитанного и `prior:` — прошлый ответ цитируется с адресом, и вопрос либо СНИМАЕТСЯ (решение переносится из
      старого ответа — `--mark-implemented` по старому документу), либо переформулируется как «прошлый ответ был X; изменилось Y —
      подтвердите или измените»; (4) дверь `--check` отказывает без аттестации — что делать. Ru-копия — перевод от смысла.
      Ru-пакет алиасов не трогать (описание навыка не меняется — пины не двигать; если меняется — `--update-pins` после сверки глазами).»
- [x] **AQ3 — ось G11 (не G9!) стража истока + статья спецификации ✅ 2026-09-18 08:52 +03:00.**
      `tools/questions-guard.mjs`: ось **G11** (имя G9 занято в HEAD — QL2 issue #54; G10 — селфтест; нулёвка AQ0),
      строка в шапке модуля, блок `@guard questions-guard-G11` четырьмя полями (THREAT · PROVED-AGAINST · GAP ·
      ON-REAL-PATH), три вида нарушения с текстом на языке проекта и напечатанной командой, ЧЕТЫРЕ мутации
      селфтеста (без аттестации → красный · с аттестацией → зелёный · `→ 4 hits` при `prior: none` → красный ·
      дата шапки до порога → зелёный). Разбор НЕ дублируется: страж зовёт `archaeology()` ядра поставки через шим
      `tools/lib/review-core.mjs` — один парсер двери и стражу (дубль уже расходился с ядром, пилот 008).
      Наблюдение: `node tools/questions-guard.mjs --selftest` → 45 мутаций из 45 сбылись по предсказанию;
      `node tools/questions-guard.mjs` по живому истоку → ни одного нарушения G11 (корпус старше порога молчит,
      №033 отвечено). Статья §2 спецификации контура — абзац об оси; бюджет страницы (`s22`: ≤ 120 строк) был
      исчерпан, поэтому шапка страницы переписана на ширину её же таблицы (7 строк → 3, тот же текст), и сам
      бюджет назван В СТРАНИЦЕ; итог 119 строк. `.kaif/INTERACTIVE_CONTOUR_SPEC.md` у истока не лежит — страница
      bundle-only (AQ0), пересобирать копию нечего.
      **Прежняя формулировка шага:** «AQ3 — ось G9 стража истока + статья спецификации. `tools/questions-guard.mjs`: G9 — живой вопрос в документе с датой
      шапки ≥ 2026-09-18 без аттестации → нарушение поимённо; аттестация с N > 0 и `prior: none` → нарушение; маркер исключения
      `<!-- archaeology: n/a — причина -->`; `--selftest` оба ответа; блок `@guard`. Статья 2 спецификации контура — новый инвариант
      (следующий свободный номер `I…`): «a live question is shown only after its archaeology line; the door prints the grep».
      `INTERACTIVE_CONTOUR_SPEC` в `.kaif/` истока — генерируется? (проверить в AQ0; если копия — пересобрать).»
- [x] **AQ4 — свод `s22` и красное доказательство ✅ 2026-09-18 08:54 +03:00.** Блок `s22 E` — пять ассертов на
      развёрнутой из бандла копии (`--check` с `quietEnv()`): без аттестации → код 3 + `grep -rniE` в выводе + Q1,
      ни «Page is up», ни «CALL:»; с аттестацией → 0 и «аттестовано 1 из 1»; `→ 5 hits` при `prior: none` → 3;
      отвеченный → 0; дата шапки до порога → 0 и «не судится» вслух. Абзац (E) в шапке свода. **Красное
      доказательство:** (а) шов `KAIF_DIST` на релизном ядре `v2.6` (`git show v2.6:dist/…` в скретчпад) — все
      пять ассертов `s22 E` красные (17 красных / 27 зелёных всего; прочие красные — известные CL/IW/QL);
      честная оговорка: на ядре 2.6 двери `--check` нет вовсе, поэтому первый ассерт красный отсутствием оси, а
      остальные — попыткой старого ядра ПОКАЗАТЬ страницу (код 2); окна и звука не случилось — `Window: none`,
      «beeps failed (no PowerShell?)», «engine not installed» в логе (`bugs/116`, тихое окружение); (б) мутант
      «порог = 2099-01-01» на КОПИИ `dist` — красные РОВНО три ассерта оси (+ два ассерта, судящих селфтест
      развёрнутой копии: он ловит мутанта сам); (в) мутант «регэксп `archeology`» — красные два ассерта оси.
      Сборка: 798 модулей / 181 блок, `check-framework` OK; счётчики: `AGENT_GUIDE.md:634` и `STATUS.md:43`
      поправлены по выводу (797 → 798), две строки README — интегратору (handoff); полигон целиком —
      «sandbox suite: all 26 suites green», 978 зелёных строк.
      **Прежняя формулировка шага:** «AQ4 — свод `s22` и красное доказательство. Ассерты в `s22` (контур поставкой): фикстура живого вопроса без аттестации,
      дата сегодня → `--check` код 3 и команда в выводе; с аттестацией → 0; `N hits` > 0 + `prior: none` → 3; отвеченный → 0; дата
      шапки до порога → 0; на ядре 2.6 (`KAIF_DIST`) — ассерты «код 3» красные по имени; мутант (порог сломан) — ровно они.
      `node tools/build-framework.mjs` → `counters-guard` → `npm run test:core` (`env: quietEnv()` — `bugs/116`; страницу не
      поднимать, звук не звать — `--check` по построению без зова).»
- [x] **AQ5 — функциональный прогон по реальному состоянию + отчёт ✅ 2026-09-18 09:05 +03:00.** Кейс 1 — КОПИЯ
      живого интервью истока №033 (исходник не менялся: он отвечён 08:14) с датой шапки сегодня и вопросом,
      возвращённым в живое состояние: `--check` → **код 3** с готовой командой → команда выполнена как напечатана
      (**844** попадания в **135** файлах) → прочитаны три места → прошлый ответ НАЙДЕН (ответ владельца
      2026-09-18 08:14, «A) — только навык ревизии кода в 2.7»; внесено в `ideas/30:72`) → аттестация вписана →
      **код 0**, «археология: аттестовано 1 из 1»; та же копия с `prior: none` → **код 3** с числом 844 в отказе.
      Кейс 2 — копия ЖИВОГО интервью соседа `interview_089` (заведено 2026-09-18 08:33, шапка сама говорит
      «переспрашиваю В6 из #088» — ровно класс #70): `--check` → **код 3**. Замер по 90 интервью соседа (копии,
      исходники только читались): живых документов 5, живых вопросов 15, аттестаций 0; у 15 из 15 греп находит
      попадания в других интервью, у 15 из 15 слово заголовка стоит в тексте непустого прошлого ответа (ПРОКСИ —
      читать обязан агент). Прогон нашёл две правки, которых гигиена не видела: `ARCHAEOLOGY_STEM_MIN = 5`
      (895 → 844 попадания) и +24 стоп-слова. Отчёт `testcases/reports/2026-09-18_question-archaeology.md`
      (семь полей, `Hygiene:`/`Functional run:` раздельно, вердикт `partial` с названными границами);
      `node framework/tools/kaif-testrun-lint.mjs check` → 0 находок в 11 отчётах.
      **Прежняя формулировка шага:** «AQ5 — функциональный прогон по реальному состоянию + отчёт. Реальное состояние истока: интервью №033 (этой сессии, без
      аттестации) — прогнать `--check` на КОПИИ с датой шапки сегодня → код 3; выполнить напечатанную команду грепа по
      существительным вопроса («навык», «переписание», «исполнитель») → прочитать попадания → вписать аттестацию в копию →
      код 0; отдельно — по одному живому интервью соседа на диске (копия, только чтение): сколько вопросов из скольких несут
      прошлый ответ по грепу (число в отчёт — это и есть замер тикета на втором проекте). Отчёт
      `testcases/reports/2026-09-18_question-archaeology.md` (семь полей, `Hygiene:`/`Functional run:`; `kaif-testrun-lint check` → 0).»
- [x] **AQ6 — тексты интегратору ✅ 2026-09-18 09:12 +03:00** — `plans/handoff_AQ_DONE.md`: построчная таблица правок,
      охота судьи «Question asked past its archaeology (KAIF 2.7)» для обеих копий побайтно + сводная строка `:14`,
      запись `TEMPLATE_NOTES_BY_VERSION['2.7']` одной строкой-литералом, `POLICY_CHANGES_BY_VERSION['2.7']`
      (дверь ужесточена ВПЕРЁД — сказано прямо, что старые интервью не краснеют), шесть пар `check-framework`,
      строки `plans/100` (строка 19, критерий 25, метрика, § «Решения» 12), черновик ответа #70 (EN, с честной
      границей и замером по корпусу соседа), черновик урока EXPERIENCE, числа счётчиков README (797 → 798),
      двенадцать пунктов «решения `[ИИ]`, оговорки и не сделанное». Фразу в `framework/KAIF_REFERENCE.md`
      (описание двери `--check` и её второй оси) внёс САМ — этот файл источник, корневая копия пересобрана.
      **Прежняя формулировка шага:** «AQ6 — тексты интегратору (`plans/handoff_AQ_DONE.md`). Охота судьи «Question asked past its archaeology (KAIF 2.7)» для
      обеих копий побайтно + сводная строка `:14`; запись `TEMPLATE_NOTES_BY_VERSION['2.7']` (и `POLICY_CHANGES_BY_VERSION['2.7']`,
      если дверь ужесточилась для существующих проектов — вперёд по дате, поэтому старые интервью не краснеют: сказать это в
      записи); пары `check-framework` (полные уникальные улики: `core.mjs` ось · `/interview` обоих слоёв · статья спецификации ·
      охота); фраза в `framework/KAIF_REFERENCE.md` (описание двери `--check`); строки `plans/100` (строка 19, критерий 25,
      метрика); черновик ответа #70 (EN); черновик урока EXPERIENCE (с `class:` — если EL уже слит, иначе без); числа
      счётчиков с вывода сборки для README (если сдвинулись). `kaif-scenario-lint check plans/115…` — `SKIPPED` законен.»
- [x] **AQ7 ✅ 2026-09-18 10:17 +03:00 (интегратор, сессия 66: охота судьи в обе копии побайтно · запись и политика 2.7 · шесть пар · строки `plans/100` · урок EXP-0140 · передача → `plans/handoff_AQ_DONE.md`). Прежняя формулировка: AQ7 — закрытие (главная сессия).** Слияние ветки ПОСЛЕ LP (конфликты в `core.mjs`/`review.mjs` — глазами), вставка
      текстов handoff, пересборка, счётчики, полигон, `verify-contour --etalon-only` (новая ось может уронить эталон — `--write-etalon`
      с пересмотром чисел глазами), судья без окон; `plans/100` строка 19 и критерий 25 ✅; STATUS; MASTER_PLAN §5; удаление handoff.

## Верификация наблюдением

| Утверждение | Артефакт | Где живёт |
|---|---|---|
| дверь отказывает без аттестации и печатает команду; старые документы молчат | `s22` ассерты, `review.mjs --selftest` | `tools/sandbox/s22-*.mjs`, `framework/tools/contour/review.mjs` |
| на 2.6 двери нет | `KAIF_DIST` красный | `tools/sandbox/s22-*.mjs` |
| исток судит себя (G9) | `questions-guard --selftest` + прогон по `interviews/` | `tools/questions-guard.mjs` |
| правило исполнимо на реальном вопросе | отчёт прогона, `Functional run:` | `testcases/reports/2026-09-18_question-archaeology.md` |

## Риски (ярусы Мёрфи)

- **(а) Дверь краснеет на всех старых интервью поля после обновления** — порог по дате шапки (`ARCHAEOLOGY_SINCE`), как G5/G6;
  документ без даты в шапке — считается старым (молчит) и говорит это в `--check`.
- **(а) Грепом по существительным ничего не найти, хотя ответ есть** (синонимы, другой язык) — аттестация честно несёт
  `N = 0`; ось не обещает найти — она обещает, что агент ИСКАЛ и записал, чем; судья охотится на N > 0 без `prior:`, не на
  N = 0.
- **(б) Конфликт с LP в `core.mjs`/`review.mjs`** — AQ сливается после LP, конфликты глазами; ось — отдельная функция с одним
  вызовом из `preflight`.
- **(б) Комментарий не рендерится владельцу** — намеренно: аттестация — для агента и судьи; владелец видит только вопрос,
  которому она разрешила существовать (G8 — словарь агента не у владельца).
- **(в) Вопрос-имя (класс «вкус») без археологии** — законное исключение `n/a — <причина>`, с причиной.

## Решения, принятые агентом без владельца

Каждое — `[ИИ]`, с тем, как выбирал (полный список и оговорки — `plans/handoff_AQ_DONE.md` § 9).

1. **`[ИИ]` носитель аттестации — HTML-комментарий перед вариантами** (не видимая строка, не поле шапки).
   Как выбирал: `FORK:` в AQ0 — цена ошибки каждого варианта плюс три consulted (тикет #70 предлагает
   комментарий дословно · ось G8 стража и слово владельца №106 «Я Заказчик» запрещают словарь агента в
   тексте владельцу · §4 спецификации и `renderMd` I24: комментарии вне код-блоков со страницы срезаются,
   значит носитель по построению не доедет до владельца).
2. **`[ИИ]` дата шапки берётся со строки `Created`/`Создан`**, и только при её отсутствии — первой ISO-датой
   головы. Как выбирал: проба по живому корпусу истока — у №032 первая дата головы это дата ОТВЕТА
   (2026-09-18), а создан он 2026-09-13; вариант «первая дата» красил бы старый документ, что запрещено
   риском (а) плана. Ассерт селфтеста назван поимённо.
3. **`[ИИ]` имя оси стража — `G11`** (план говорил `G9`). Как выбирал: пробой по HEAD — `G9` занята (QL2,
   issue #54), `G10` — селфтест. Оговорка об одноимённой оси в `verify-contour` — в handoff § 9 п. 1.
4. **`[ИИ]` четвёртый вид проблемы — `malformed`** (аттестация без `→ N hits`/`prior:` отказывается). Как
   выбирал: без него `<!-- archaeology: искал -->` проходил бы дверь — fail-open, а дверь обязана быть
   fail-closed (правило трёх дверей, `C7/I4` ядра).
5. **`[ИИ]` основа слова не короче 5 знаков + 24 стоп-слова.** Как выбирал: замером на живом корпусе
   (895 → 844 попадания) и на живом вопросе соседа («выше», «того», «первы» в команде) — не рассуждением.
6. **`[ИИ]` номерованный инвариант `I46` в `/owner-reviews` не заводился** (статья §2 несёт ось той же
   формой, что соседнюю дверь #56 — без номера). Как выбирал: нумерация живёт в обеих копиях того навыка,
   он вне «Вовне» этого плана; номер — предложение интегратору.
7. **`[ИИ]` свод — `s22`, не новый** (обоснование в AQ0).
8. **`[ИИ]` шапка статьи спецификации переписана на ширину её таблицы**, чтобы новая статья уложилась в
   бюджет `s22` (≤ 120 строк). Как выбирал: альтернатива — поднять число в ассерте, то есть подогнать тест
   под поведение; бюджет — критерий 1 `plans/93`, и трогать его вправе владелец эпика, а не эта ветка.

## Links

`plans/100` строка 19, критерий 25, § «Решения» 12 · `researches/30` §2к · issue #70 · issue #56 (родня — QL1) · `plans/102`
(QL — образец работы с дверью `--check` и `s22`) · `framework/tools/contour/core.mjs` · `tools/questions-guard.mjs` ·
`framework/skills/interview/SKILL.md` · `plans/109` (образец исполненного эпика) · `plans/113` + `plans/handoff_FR.md`
(образец работы в worktree и передачи).
