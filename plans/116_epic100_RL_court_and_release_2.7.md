# План 116 — эпик RL «Суд и релиз 2.7»: суд одним кругом по всему скоупу версии, витрина Audited KAIF, публикация по слову владельца, ответы в восемнадцать тикетов после релиза

> **Создан:** 2026-09-18 10:57 +03:00 (сессия 66; канон N+1 — №43: план последнего эпика пишется на закрытии предпоследнего —
> здесь написан, пока машинная половина CB исполняется субагентом; нулёвка RL0 сверит входы на старте, №76).
> **Родитель:** `plans/100` (эпик RL — строка 7 таблицы эпиков, критерий приёмки версии 12 «[RL — суд и релиз]»); прецеденты
> формы — `plans/99` (RL 2.6) и `plans/87` (RL 2.5); навык `/release` (шаги 0–7, гейт 6.9 — страница глазами); слова владельца:
> №117 (имя — Audited KAIF, вопрос имени снят), №13 (релиз — только по явному «да»), №84/№92/№93/№94 (ответы в issues — после
> релиза, по тикету, без вычитки, закрытие агентом; полевой отчёт закрывается квитанцией), №74/№88 (суд одним кругом, без
> workflows — четыре судьи-субагента по кластерам + панель скептиков), №90 (проекты-доноры в поставке и витрине не называются).
> **Статус:** 🔧 В РАБОТЕ с 2026-09-18 12:53 +03:00. **Сессия 69 (с 2026-09-18 16:49 +03:00, Claude Opus 5):** RL2 ✅ — пятый блокер D-F2
> починен коммитом выпуска `32ed37e` 2026-09-18 17:09 +03:00 (страница обещает забор только в окне контура; во вкладке — кольцо спасения с текстом ответа;
> красный на сборке до починки — ровно ассерт вкладки `s22 D`); README 2.7 применён и номер версии поднят тем же коммитом; лёгкий судья
> релиз-кандидата — VERIFIED WITH CAVEATS 2026-09-18 17:21 +03:00; публикация — следующим ходом. **Сессия 68 (≈ 16:10 → 16:50 +03:00):** RL1 ✅ — суд шестью судьями, реестр
> `reports/KAIF_AUDIT/2026-09-18_rl_court_2.7_registry.md`: 138 = 116 · 17 · 5, блокеров пять; RL2 — четыре починены (A-F1 · E-F1 · F-F1 · D-F1) +
> A-F2, пятый (D-F2) на закрытии сессии 68 был открыт: её починка сломала `s22` и была откачена (закрыт сессией 69, выше); RL4 — ноты `reports/RELEASE_NOTES_2.7.md` готовы, README 2.7 — патч
> `reports/release_2.7/README_2.7.patch` (на закрытии сессии 68 в дереве не применён; применён в `32ed37e`), логотип `assets/KAIF_2.7_GitHub_LOGO.*`; RL3 — 18 ответов `reports/release_2.7/NN.md`;
> чат закрыт словом владельца («не уложились в этот чат. В новый переносим дела»). Было: RL0 ✅ 12:53 · решение RL0 исполнено 14:13 → 14:57 —
> класс `bugs/122` нормализован (39 мест → 0 символов в 886 файлах), страж стоит в сборке, полигон и полный прогон контура
> зелёные · судья сессии 14:37 — VERIFIED WITH CAVEATS, двенадцать находок починены до пуша (срез 15:02) → дальше RL1 (суд
> шестью судьями). Было: 🔲 запланирован 2026-09-18 10:57 +03:00 — старт с RL0 ПОСЛЕ закрытия CB (`plans/95`, критерий 10 версии).
> **Вовне:** тег `v2.7` + GitHub Release с ассетами (`latest` → 2.7) — по слову владельца в моменте; ответы в issues #54–#71
> после релиза с закрытием тикетов; README EN/RU, ноты 2.7 EN/RU, логотип с подписью версии, `README.pdf`, story-card.

## Вектор цели (Achieve)

**Боль.** Версия 2.7 собрана девятнадцатью эпиками за четыре рабочих дня (AW · DR · QL · TR · VC · HO · CL · IW · SD · RS · SF ·
FR · LP · AQ · EL · CR · CB и разрез AU); каждый судился лёгким судьёй, полного суда всего скоупа не было, а лёгкие судьи этой
версии находили дефекты КОДА, которых не видел ни один ассерт (LP: убийство pid из файла, недолговечный носитель ответа, забор
при открытом окне). Витрина всё ещё говорит «2.6 — Mindful KAIF»; восемнадцать открытых тикетов поля (#54–#71) ждут ответов
«починено, приедет 2.7»; проекты-потребители ждут обновления.

**Где хотим оказаться.** Один круг суда по всему скоупу 2.7 с нулём блокеров (найденное починено и пересужено); README EN/RU
и ноты 2.7 называют версию `KAIF 2.7 — Audited KAIF` и числа живой сборки; логотип подписан версией; `v2.7` опубликован по
слову владельца, страница релиза прочитана глазами, `release-body-guard` зелёный; критерии `plans/100` — 25 из 25; ответы в
восемнадцать тикетов отправлены и тикеты закрыты; эстафета 2.8 — `ideas/30` с нулёвкой на открытии.

## Готово, когда (критерии приёмки — сценариями; якорь `plans/100` критерий 12)

1. **[RL — суд одним кругом]**
   - Ситуация. Эпики 2.7 закрыты, у каждого свой лёгкий судья; заявления живут в планах 101–115, 63, 95 и в отчётах
     прогонов `testcases/reports/2026-09-1*`.
   - Действие. Оркестратор собирает реестр заявлений (строка на заявление: команда переисполнения · ожидаемое), делит его
     на шесть кластеров, судьи-субагенты с чистыми контекстами переисполняют кластеры, панель скептиков из двух линз
     опровергает каждую находку (вердикт по умолчанию — «не дефект»).
   - Результат. Владелец видит в `reports/KAIF_AUDIT/<дата>_rl_court_2.7_registry.md` таблицу заявлений с исходами и таблицу
     находок с судьбой; выжившие находки починены и пересужены; блокеров 0; сводные числа посчитаны скриптом по строкам.
   - Проверка. `grep -c "REFUTED" reports/KAIF_AUDIT/<дата>_rl_court_2.7_registry.md` равно числу из сводки, блокеров `0`;
     `npm run test:core` печатает `all N suites green`; четырнадцать охот судьи 2.7 названы в реестре (греп имён).
2. **[RL — витрина одним именем]**
   - Ситуация. `version.json` 2.6; README EN/RU и логотип говорят «2.6 — Mindful KAIF»; нот 2.7 нет.
   - Действие. Агент собирает витрину: README обеих половин, ноты `reports/RELEASE_NOTES_2.7.md` EN+RU, логотип с подписью
     версии, story-card, `README.pdf`.
   - Результат. Владелец видит README с версией `KAIF 2.7 — Audited KAIF` без кавычек, строку v2.7 в таблице вех 8.1, числа
     сборки из вывода инструментов, ноты EN и RU зеркально — в них правило `@fork` названо фразой линтера
     `OPTIONS · COST · RECON · DECIDED` (первая половина критерия 3 `plans/95`), логотип с подписью версии.
   - Проверка. `node tools/counters-guard.mjs` OK; `node tools/showcase-lint.mjs` OK; `node tools/private-names-guard.mjs` OK;
     `node tools/build-team-logo.mjs --check` OK; `grep -c "OPTIONS · COST · RECON · DECIDED" reports/RELEASE_NOTES_2.7.md` → `2`
     (по одной на половину); `grep -c "«[A-Za-z][^»]*KAIF»" README.md reports/RELEASE_NOTES_2.7.md` → `0`.
3. **[RL — публикация по слову]**
   - Ситуация. Витрина собрана, суд закрыт, `version.json` ещё 2.6.
   - Действие. Владелец говорит «да» на публикацию; агент исполняет `/release` шаги 5–7: бамп `version.json` 2.6 → 2.7 с
     `codename: "Audited KAIF"`, сборка, полигон, коммит, тег `v2.7`, `gh release create` с ассетами из `dist/`.
   - Результат. Владелец видит страницу `https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.7` с заголовком
     `KAIF 2.7 — Audited KAIF`, телом из нот и шестью ассетами; `latest` указывает на 2.7.
   - Проверка. `gh release view v2.7 --json name` печатает имя; `node tools/release-body-guard.mjs --tag v2.7` ✅; задание
     обновления 2.6 → 2.7 на РЕАЛЬНОМ интервале (`git show v2.6:dist/*` → установка → `update` из новой сборки) несёт записи
     2.7 и раздел решений владельцу — ассерт сводов маршрута обновления зелёный.
4. **[RL — ответы в поле]**
   - Ситуация. Релиз опубликован; открыты восемнадцать тикетов #54–#71; черновики лежат в планах эпиков и в передачах.
   - Действие. Агент отправляет ответ по каждому тикету со ссылкой на релиз и закрывает тикеты.
   - Результат. Владелец видит 0 открытых тикетов версии в origin; каждый ответ написан результатом, без кодов эпиков и
     без имён проектов-доноров.
   - Проверка. `gh issue list --repo MikalaiKryvusha/KAIF --state open` печатает пустой список; `gh issue view <N> --comments`
     на выборке из трёх тикетов показывает ссылку на `v2.7`.
5. **[судья релиза]** — `/fable-judge` на релиз-кандидате (шаг 4.5 `/release`) и после публикации (шаг 7); закрытие версии в
   `plans/100` (25 из 25), STATUS, летопись (`/end-chat-soft`), эстафета 2.8 — `ideas/30`.

## Шаги

- [x] **RL0 ✅ 2026-09-18 12:53 +03:00 (сессия 67) — нулёвка входов; итог — абзацем «RL0: итог» под списком шагов.** Прежняя
      постановка шага: **RL0 — нулёвка входов (№76).** Состояние HEAD: все ли эпики закрыты (`plans/100` — 24 из 25 до релиза), открытые
      баги версии (`bugs/118`–`bugs/121` — находки ревизии кода: решить `FORK:`-строкой, чинятся ли дешёвые F1 и F9 ДО релиза
      или едут в 2.8 — цена: поле получает образец конфига хуков, который никто не стережёт); где лежит каждый из восемнадцати
      черновиков ответов (таблица ниже) и не протух ли он после поздних правок эпиков (LP — носитель IndexedDB; CR — семь правок);
      `gh issue list` — не пришли ли новые тикеты (новый сигнал → строка реестра, в 2.7 только словом владельца); состояние
      витрины (`grep -n "2\.6" README.md | wc -l`, таблица 8.1, логотип в `assets/`); `FORK:` по форме суда (шесть кластеров
      судьями-субагентами без workflows — прецедент №88 | workflows по №79) — без workflows, пока владелец не выдаст.
- [ ] **RL1 — суд одним кругом (критерий 1).** Реестр заявлений `reports/KAIF_AUDIT/<дата>_rl_court_2.7_registry.md`
      (псевдонимы проектов, №90). Кластеры: **A** слово и вопрос владельца (AW · QL · AQ) · **B** прогон и заявление (TR · CL · VC) ·
      **C** передача, сигнал, ложь, команда (HO · SD · SF · FR) · **D** контур и страница (IW · LP · RS — хук) · **E** журнал и
      ревизия (EL · CR) · **F** бюджет канона и снятый вектор доставки (CB · DR) + сквозное: поставка обновляется с 2.6 на
      реальном интервале. В промпт каждому судье — правила тишины (без окон и звука, без `KAIF_DIST` старых генераторов,
      соседние развёртывания только чтением, полигон — одному судье и в одиночку). Панель скептиков — две линзы («это не
      дефект, а объявленная граница» · «это дефект, но не блокер релиза»). Сводка — скриптом по строкам (EXP-0107).
- [ ] **RL2 — починки по суду и пересуд.** Блокеры — до нуля; каверзы — на месте, с прежними формулировками; что не чинится в
      2.7 — строкой в `ideas/30` с адресом находки.
- [ ] **RL3 — черновики ответов в #54–#71 сведены и вычитаны агентом** (EN, как тикеты; языком результата; имена проектов не
      называются; охота «owner text in agent vocabulary» — глазами по каждому). Адреса черновиков: #54 #56 #58 #60 — `plans/102` ·
      #55 — `plans/101` · #57 — `plans/106` · #59 — `plans/104` · #61 — `plans/105` · #62 #63 — `plans/107` · #64 — `plans/108` ·
      #65 — `plans/109` · #66 — `plans/111` · #67 — `plans/handoff_SF_DONE.md` § 4 · #68 — `plans/handoff_FR_DONE.md` § 6 · #69 —
      `plans/handoff_EL_DONE.md` § 8 · #70 — `plans/handoff_AQ_DONE.md` § 7 · #71 — передача CB.
- [ ] **RL4 — витрина (критерий 2).** README обеих половин скриптом (бейджи, строки версии с датой, вехи 8.1 — строка v2.7);
      ноты `reports/RELEASE_NOTES_2.7.md` EN+RU (дельта версии; якоря README сняты с живой страницы — EXP-0108; фраза линтера о
      `@fork`); логотип `node tools/build-team-logo.mjs --title "KAIF 2.7 — Audited KAIF"` (арт прежний — прецедент №89; иной
      арт — только словом владельца); story-card; `node tools/readme-pdf.mjs`; линты витрины; портрет голоса владельца —
      `node framework/tools/kaif-voice-lint.mjs load` ДО письма русской половины и `check` после (четвёртое обязательство).
- [ ] **RL5 — показ владельцу и публикация (критерий 3, слово №13).** Один показ страницей контура: что выходит в 2.7
      сценариями того, что владелец увидит на странице релиза, и один вопрос — публиковать ли сейчас (вопрос несёт аттестацию
      поиска прошлого ответа — ось AQ). По «да»: `/release` шаги 5–7 — контрольная сборка · судья RC · бамп версии и имени ·
      полигон на бампе · коммит · тег · `gh release create` с ассетами · гейт 6.9 (страница глазами) · `release-body-guard` ·
      `kaif-stats`.
- [ ] **RL6 — ответы в поле и закрытие версии (критерии 4, 5).** Восемнадцать ответов по тикетам, закрытие тем же движением
      (№93/№94); `plans/100` критерий 12 ✅ — 25 из 25; `ideas/30` — эстафета 2.8; STATUS; `/end-chat-soft` с летописью и строкой
      «Стоячая ложь: …»; удаление слитых веток `worktree-agent-*` и чужих detached-worktree (`git worktree prune`).

### RL0: итог (2026-09-18 12:53 +03:00, сессия 67)

- **Состояние HEAD.** Эпики закрыты — 24 из 25 (`plans/100`, срез 12:46): CB слит и внесён (`39c9988`, локально; пуш — после
  судьи сессии, он идёт). Полигон на слитом дереве — «all 27 suites green» дважды (12:39–12:42 до коммита, 12:49–12:52 после),
  отпечаток свежести `dist` воспроизводится из коммита на трёх деревьях (проба `dist-fresh-on-clean-exports`, 12:49).
- **Новых тикетов нет:** `gh issue list --state open` → 18 (пробы 11:26 и 12:53), те же #54–#71.
- **Находки ревизии кода по хукам.** До суда починены сессией 67 (`5123d34`): `bugs/121` целиком, `bugs/118` №1, `bugs/119` №3 в
  хуках; по дороге найден класс `bugs/122` (39 мест исходников, 25 в поставке: снятие BOM записано невидимым символом).
  `FORK: options <A остатки и bugs/122 целиком в 2.8 | B до релиза — только bugs/122 (нормализация скриптом + страж в сборке),
  остатки bugs/118 №2–№3 · bugs/119 №1–№2 и девять мест вне хуков · bugs/120 — в реестр 2.8 | C чинить всё до релиза> ·
  price of error <A: поле получает 25 мест поставки, где снятие BOM читается глазом как пустое выражение, — первая ручная
  переписка такой строки при слиянии модуля молча выключает BOM-терпимость ядра, и на Windows `check`/`update` умирают на
  разборе `kaif.json`, сохранённого с BOM; C: два-три лишних круга «правка → сборка → полигон» в только что судимой зоне
  ради находок, которых владелец руками не увидит> · consulted <уже оплаченные правила самого проекта — EXP-0007 (BOM на
  Windows ломал установку) · EXP-0135 (не-ASCII в поставке пишется escape скриптом; у кириллицы страж есть, у невидимых
  символов нет) · лестница тяжести `BUG_FIXING_FRAMEWORK` (S2 → документ бага + страж, без новой машинерии) · слово владельца
  №109 («80% ценности за 20% усилий»)>` → **B** `[ИИ]`; нормализация трогает файлы контура → полный `node tools/verify-contour.mjs`
  (без окна) в том же шаге. Суд вправе поднять любой из остатков до блокера — тогда он чинится в RL2.
- **Черновики ответов — на месте все восемнадцать** (адреса — в шаге RL3). Протухшее, названное заранее: #71 — последний
  абзац («the origin itself fails its own gate today») неверен с 10:51 (`plans/handoff_CB_DONE.md`, шапка); #66 — сверить с
  носителем IndexedDB после суда эпика LP; остальные сверяются с кодом HEAD на шаге RL3.
- **Витрина.** `version.json` — 2.6 Mindful KAIF; в `README.md` восемь строк с «2.6» (логотип `KAIF_2.6_GitHub_LOGO.webp` в обеих
  половинах, строка версии, строка v2.6 таблицы вех); нот 2.7 нет; счётчики сборки не сдвигались — 182 блока · 798 модулей ·
  37 навыков · 27 сводов.
- `FORK: options <A шесть судей-субагентов по кластерам + панель скептиков, без workflows | B workflows> · price of error
  <B без слова владельца — расход его бюджета, которого он не выдавал; A — суд дольше по часам> · consulted <решения владельца
  №88 (интервью №021, Q2 = A — суд 2.5 без workflows: судьи-субагенты и две линзы скептиков) и №79 (интервью №019, Q1 = A;
  его комментарий дословно: «позовёшь, когда какой режим включать»)>` → **A**; общий бриф судьи написан заранее (правила тишины, полигон — одному судье и в одиночку).

### Решение RL0 исполнено (2026-09-18 14:13 → 15:02 +03:00, сессия 67)

- **`bugs/122` — 🔧 фикс сделан и проверен:** 39 символов в 20 исходниках → видимая запись, после пересборки 0 символов в 886
  отслеживаемых файлах; гард 5h сборки (отказ со строкой `<кодовая точка> at <файл>:<строка>` и готовым ходом) увиден
  красным на живом дереве до нормализации и на копии с возвращённым символом; готовый ход доказан пробой 14/14 и четырьмя
  адресными мутантами; полигон в одиночку — all 27 suites green (14:51 → 14:54), полный прогон контура без окна — 198
  зелёных (14:55). Отчёт — `testcases/reports/2026-09-18_invisible-characters.md` (pass).
- **Судья сессии** (диапазон `95c7883..39c9988`, вердикт 14:37): VERIFIED WITH CAVEATS — 19 подтверждено, 3 ослаблено, 0
  опровергнуто, полигон им не переисполнялся (снят оркестратором, прогнан оркестратором после него). Двенадцать находок
  починены до пуша; три существенные — свод хуков обещал ловить граблю, которую скрытый запуск не видит (F1 → статический
  ассерт + мутант М5), потерян страж роста поставки (F2 → ассерт `s16` + мутант М6), перенесённая проба бюджета читала один
  поток (F3 → оба потока + режим `--fixture`). Урок — EXP-0148; дополнения отчётов — `…_hooks-optin-smoke.md` (прогоны
  14–17) и `…_canon-budget.md` (дополнение после вердикта).
- **Попутно, тем же классом «закрыто в мета-плане, не применено к телу»:** ось «закрытие против тела» линта шапок не видела
  ни одной живой формы закрытия — расширена по замеру, шесть фикстур, восемь документов разобраны дверью оси (EXP-0147);
  две оси-кандидата — `ideas/30` п. 20.

### План выпуска за два часа (слово владельца №122, 2026-09-18 16:02 +03:00; заменяет порядок RL1 → RL6 по времени, не по содержанию)

**[OWNER] дословно (чат, ≈ 15:57 +03:00):** «не годится.ты говорил, что версия готова, остались церемонии, а сейчас мне даешь оценку до вечеа. не пойдет. версия должна быть выпущена в слудующем чате в теченеи 2 часов, не более. этот чат закрыть».

`[ИИ]` Как шаги RL1–RL6 укладываются в 120 минут следующего чата — параллельностью и резом, без ускорения самих проверок:

| Минуты | Шаг | Что именно |
|---|---|---|
| 0 | ~~вопрос владельцу~~ | снят: слово на выпуск дано (№123) — «выпускай релиз, показывать не нужно» (чат сессии 67) и «У тебя времени 2 часа, не больее, чтобы выпустить версию 2.7 в релиз» (чат сессии 68); прежняя строка — «ПЕРВЫМ сообщением: вопрос о публикации» |
| 0–10 | `/resume` | полный проход канона — приказ слова `resume`, не сокращается |
| 10–15 | RL1, запуск | шесть судей ФОНОМ, по кластерам A–F; брифы — `reports/KAIF_AUDIT/2026-09-18_rl_court_2.7_briefs.md`; реестр — `reports/KAIF_AUDIT/2026-09-18_rl_court_2.7_registry.md` |
| 15–60 | RL4, параллельно суду | ноты 2.7 EN/RU · README EN/RU · подпись логотипа · `TEMPLATE_NOTES` и `codename` · story-card; письмо — ПО портрету (`kaif-voice-lint load` → письмо → `check` → §7Б чистым субагентом) |
| 60–80 | RL2 | находки суда: чинятся ТОЛЬКО блокеры релиза; остальное — `ideas/30` и баг-документы (№70); панель скептиков — только на находках, названных блокерами |
| 80–95 | `/release` 3–4.5 | сборка · полигон · стражи · полный `verify-contour` · PDF · лёгкий судья релиз-кандидата; число суда в нотах — пересчётом по строкам реестра |
| 95–120 | RL5–RL6 | публикация по «да» · гейт 6.9 · RL3: ответы в 18 тикетов из черновиков и закрытие · `plans/100` критерий 12 ✅ · STATUS |

Риски срока (ярус а): лимит использования при шести параллельных судьях — если оборвёт, судьи возобновляются сообщением, а не запускаются заново; флейк `bugs/109` — полигон после сессии 67
герметичен по сети, красный с кодом `3221226505` повторяется один раз. Что режется первым, если минуты кончаются: story-card → панель скептиков → вторые углы судей (в таком порядке); НЕ
режутся: судья релиз-кандидата, гейт 6.9, слово владельца на публикацию.

## Верификация наблюдением

| Утверждение | Артефакт | Где живёт |
|---|---|---|
| суд прошёл весь скоуп, блокеров нет | реестр суда со сводкой, посчитанной скриптом | `reports/KAIF_AUDIT/<дата>_rl_court_2.7_registry.md` |
| поставка обновляется с 2.6 на реальном интервале | прогон `update` от установки из `git show v2.6:dist/*` | отчёт прогона `testcases/reports/<дата>_release-2.7.md` |
| витрина говорит одним именем и живыми числами | `counters-guard` · `showcase-lint` · `private-names-guard` · `build-team-logo --check` | вывод в отчёте прогона |
| страница релиза равна нотам | `release-body-guard --tag v2.7` + страница, прочитанная глазами | `/release` шаги 6.9 и 7 |
| тикеты отвечены и закрыты | `gh issue list --state open` | отчёт закрытия версии |

## Риски (ярусы Мёрфи)

- **(а) Суд шести кластеров не помещается в один чат.** Защита: реестр — заявления, уже переисполненные лёгкими судьями, плюс
  их каверзы; судьи идут параллельно; полигон гоняет один судья и в одиночку (`bugs/109`).
- **(а) Судья поднимет окно или звук у владельца.** Защита: правила тишины в промпте КАЖДОГО судьи (EXP-0132); генератор
  контура — только `--check`/`--selftest`; видимое окно — только главной сессией и только объявленное в чате.
- **(б) Обновление 2.6 → 2.7 на реальном интервале спотыкается о новые оси `check`** (археология, бюджет по собственным
  строкам, конституция). Защита: прогон на установке из `v2.6` ДО бампа; все новые оси — предупреждения или судят вперёд по дате.
- **(б) Черновик ответа протух после поздней правки эпика.** Защита: RL3 сверяет каждый черновик с кодом HEAD.
- **(в) Флейк полигона `bugs/109`.** Защита: форензика `run-fail-N.log`; повтор в одиночку — не «зелёный по памяти».

## Решения, принятые агентом без владельца

1. `[ИИ]` (при написании плана) Шесть кластеров суда вместо четырёх — скоуп 2.7 вдвое шире 2.6; форма суда — прецедент №88.
2. `[ИИ]` Логотип — прежний арт с новой подписью (прецедент №89 для 2.5); иной арт — только словом владельца.
3. `[ИИ]` Судьба `bugs/118`–`bugs/121` (чинить до релиза или в 2.8) решается в RL0 строкой `FORK:`, а не здесь: цена зависит от
   того, что найдёт суд.

## Links

`plans/100` (критерий 12, строка 7) · `plans/99` (RL 2.6 — форма) · `.claude/skills/release/SKILL.md` · `plans/95` (критерий 3 —
фраза `@fork` в нотах) · `ideas/30` (реестр 2.8) · `HOUSE_RULES.md` §1–§2 (инструменты витрины и реестр пар) · `bugs/109`.
