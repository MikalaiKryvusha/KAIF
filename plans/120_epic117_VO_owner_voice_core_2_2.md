# План 120 — эпик VO версии 2.8: голос владельца 2.2 — слепок ядра в KAIF и в проекты заменой, правила знают свой жанр

> **Создан:** 2026-09-25 (сессия 74, Claude Opus 5.5; операционный план эпика мета-плана `plans/117`, начат словом владельца №129 раньше
> OW — `plans/117` § «Решения» 7).
> **Родитель:** `plans/117_EPIC_kaif_2.8.md` критерии 25–26, строка 6 таблицы эпиков · тикеты истока #102 (жанр у строк §8) и #103 (ядро 2.2
> заменой) · решения №60 (слепок: правила едут, цитаты — нет), №62 (слепок свежий по явному требованию владельца), №128, №129.
> **Статус:** 🟢 в работе с 2026-09-25 15:00 +03:00 — VO0 ✅ 2026-09-25 15:00 +03:00 (нулёвка: замеры ядра 2.2, отчёт `testcases/reports/2026-09-25_vo0-core22.md`);
> VO1–VO4 — ниже.
> **Вовне:** слепок `AUTHOR_STYLOMETRY.md` истока (публичный); пункт задания обновления 2.8 — проектам-потребителям ядра; ответы в #102 и
> #103 — на RL (№84/№92/№93).

## Вектор цели

**Боль.** Ядро голоса владельца переписано целиком (1.2 → 2.2, 2026-09-25, принято по мандату владельца), а KAIF и проекты владельца
держат слепок ядра 1.x: агент, пишущий текст владельца, пишет по старому портрету. Линтер голоса поставки применяет каждую строку §8 к
каждому тексту — на прозе владельца 214 остановок из 217 дают правила рабочих текстов (#102). Генератор слепка знает только раскладку 1.x:
на ядре 2.2 он распознаёт «правил 1» и схлопывает 72 спана (VO0).

**Где хотим оказаться (Achieve).** Слепок истока собран из ядра 2.2 — правила целиком, личные фразы владельца схлопнуты по №60; проекты-
потребители ядра (реестр `consumers` в `version.json` ядра: KAIF, NDim, Unliminium) получают его при обновлении на 2.8 заменой, с
названной прежней версией в отчёте; линтер голоса знает метки жанра ядра и на эссе молчит о рабочих правилах. **Avoid:** портрет
ЧУЖОГО проекта (владелец не Николай) обновлением не трогается никогда; ни строки рабочего слоя и модуля прозы в публичном не появляется.
**Maintain:** шаблон руководства поставки не выше 1080 строк (сейчас 1078).

## Критерии приёмки

Сценарии — `plans/117` → «Готово, когда», критерии **25 · 26** (источник истины; здесь не копируются — DRY). Метрика эпика — сколько из
двух закрыто, с адресом отчёта прогона у каждого.

## Шаги

### VO0 — нулёвка: что такое ядро 2.2 и чем его везти (✅ 2026-09-25 15:00 +03:00)

- [x] Ядро получено: `git fetch` клона `d:\work\krinik_voice` (рабочее дерево не тронуто) → `origin/main` = `2c822d5`, `version.json` →
      2.2 `accepted` 2026-09-25 14:35 +03:00; файл ядра — 615 строк, 82 754 байта; поле `propagation`: «Проекты KAIF (KAIF, ndim,
      unliminium) получают ядро только через версию KAIF: при обновлении на 2.8 файл заменяется целиком, не сливается … Рабочий слой и
      модуль прозы приватны и не едут никуда, кроме приватных проектов автора».
- [x] Раскладка 2.x: `## 0` · `## 0-M` · `## 1` · `## 2` (2.1–2.6) · `## 2-C` · `## 3` · `## 4` · `## 5` · `## 6` · `## 7` · `## 8` (21 строка
      таблицы) · `## 9` (журнал) · приложения А и Б; метка жанра — первое слово колонки «подсказка»: `[работа]`, `[документ]`, без метки.
- [x] Эталон меток — инструмент хранилища `tools/voice-check.mjs` (там же, `origin/main`): `GENRES = document · ticket · comment ·
      message · reply · essay`; «[работа] — все жанры, кроме essay; [документ] — только document; [проза] — только essay; без метки — везде».
- [x] Генератор слепка истока на ядре 2.2 (`--check`, отчёт в скретчпад; слепок истока не тронут — sha до и после равны): «правил 1»,
      схлопнуто 72 спана (66 уникальных); из 66 публичны в KAIF 11; из 55 остальных — шаблоны оборотов («Если …, то …», «Таким образом, …»,
      «Настоящий документ является руководством пользователя») и непубличные фразы владельца из переписки и текстов его проектов (здесь не
      приводятся — решение №60; первая редакция этой строки их цитировала, исправлено 2026-09-25 15:09 +03:00) — вход VO1.

### VO1 — генератор слепка знает раскладку 2.x; слепок истока из ядра 2.2 (≈ 0,3 чата)

`FORK: options <A байт в байт — ядро 2.2 как есть в публичный KAIF | B генератор под 2.x: правила целиком, непубличные фразы владельца
схлопнуты, шаблоны оборотов и публичные цитаты — белым списком> · price of error <A — личные фразы владельца из переписки публикуются в
открытом репозитории, необратимо (история git); B — пример в правиле становится «…» там, где фраза владельца непублична> · consulted
<[OWNER] решение №60 (интервью №014 Q1 = B: «правила едут целиком, цитаты — нет»; в силе — #103: «decision №60 stays in force»);
предложение самого #103: «rebuild the public snapshot from core 2.2 … The snapshot generator targets the 1.x layout; it needs the 2.x
sections»>` → **B**. Утверждение хранилища «public-safe core» — слово агента того проекта, не владельца; решение №60 — слово владельца.

- [ ] Генератор `tools/stylometry-snapshot.mjs` различает раскладку ядра: 1.x — как прежде; 2.x — разделы целиком по номерам (§0 · §0-M ·
      §1 · §2.x · §2-C · §3–§8 · приложения), журнал §9 — строкой-адресом; таблица §8 — побайтно, метки в подсказке целы.
- [ ] Фильтр цитат на 2.x: схлопывается фраза владельца, не публичная в KAIF; белый список — шаблоны оборотов (не слова владельца: канцелярит
      и машинные штампы, приведённые как примеры) — каждый пунктом в конфиге генератора, с причиной; публичные — по адресу `файл:строка`.
- [ ] Отказ на приватные слои (#103 п. 3): источник с именами `AUTHOR_STYLOMETRY_WORK.md` / `AUTHOR_STYLOMETRY_PROSE.md` или их блоками
      образцов — генератор отказывает; ось n-грамм сверяет тело слепка и с рабочим слоем, и с модулем прозы.
- [ ] `--selftest` — случаи раскладки 2.x (разделы целы · §8 побайтно · фраза владельца схлопнута · шаблон из белого списка цел · блок
      рабочего слоя → отказ); мутант на каждый; пересборка слепка истока; `--check` зелёный; реестр пар `HOUSE_RULES.md` §2 и §5 (версия 2.2).
- **Проверка:** функциональный прогон — `kaif-voice-lint load` и `check` на новом слепке (таблица разбирается: 21 строка; разделы для
  письма грузятся), `private-names-guard` зелёный, ось n-грамм против трёх файлов хранилища — 0.

### VO2 — линтер голоса знает жанр (критерий 25, #102; ≈ 0,2 чата)

- [ ] `framework/tools/kaif-voice-lint.mjs`: метка — первое слово подсказки в квадратных скобках; `check --genre <жанр>` с жанрами эталона
      хранилища и тем же правилом применимости; без `--genre` — все строки, как прежде, и строка «N строк несут метку жанра — `--genre …`».
- [ ] Селфтест — случаи меток; `s26` — ассерты жанра на развёрнутой копии (ТЕСТ ИЗМЕНИТСЯ с обоснованием); мутант «метки игнорируются»;
      близнецы — навык `/owner-voice` обоих слоёв, `KAIF_REFERENCE.md`, заметка 2.8 шаблонов.
- **Проверка:** функциональный прогон — проза владельца (публичные тексты истока) с `--genre essay`: строк `[работа]` 0; рабочий текст с
  `--genre ticket` — попадания названы.

### VO3 — проекты-потребители получают слепок 2.8 заменой (критерий 26, #103; ≈ 0,2 чата)

`FORK: options <a пункт задания обновления: агент проекта сам скачивает слепок | b машинерия update сама узнаёт портрет-потребитель по
шапке и заменяет файл слепком релиза, называя прежнюю версию | c слепок едет в поставке каждому> · price of error <портрет ЧУЖОГО проекта
затёрт портретом Николая (потеря данных владельца проекта); потребитель владельца молча остаётся на 1.x; с — портрет владельца KAIF в
каждой установке> · consulted <[OWNER] интервью 039 Q4 полевого проекта (дословно — `plans/117` критерий 26): «при одновлении на 2.8 нужно
будет и обновить ядно на новое, не мержем, а заменой!»; поле `propagation` ядра (потребители поимённо); №97 «механика отгружается только
полной»; разведка маршрута update — первым делом шага>` → решается разведкой маршрута update в начале шага; граница «чужой портрет не
трогается» — в любом варианте.

- [ ] Разведка маршрута: где `update` 2.8 может узнать портрет-потребитель (шапка слепка KAIF «Портрет голоса владельца KAIF — публичный
      слепок правил», шапка ядра «Ядро голоса и мышления Николая Кривуши», преамбула NDim) и откуда взять слепок релиза.
- [ ] Замена с отчётом («портрет: слепок ядра 1.2 → 2.2»); оболочка жанра (Unliminium) — пункт задания пере-вывести её поверх нового
      слепка; чужой портрет — ни байта; свод полигона: три фикстуры (слепок KAIF · оболочка · чужой портрет) и мутант «замена без узнавания».
- **Проверка:** функциональный прогон на КОПИЯХ полевых развёртываний владельца (только чтение исходников) и на чужой фикстуре.

### VO4 — сборка, полигон, судья, коммит (≈ 0,1 чата)

Сборка → полигон в одиночку → лёгкий судья эпика чистым экземпляром → починки → отчёт → коммит → полигон ещё раз; критерии 25–26
`plans/117` ✅ с адресами отчётов; возврат к OW1 (`plans/119`).

## Риски

- **(а) Личное владельца в публичном** — ось n-грамм против трёх файлов хранилища, `private-names-guard`, фильтр по №60; слепок пересобирается
  только кодом (EXP-0049).
- **(а) Чужой портрет затёрт обновлением** — узнавание по шапке, фикстура чужого портрета в своде, мутант.
- **(б) Белый список шаблонов шире шаблонов** — каждый пункт с причиной, ревизия списка глазами в отчёте VO1.
- **(в) Слепок хуже ядра для письма** (примеры «…») — класс «вкус»: судит владелец, при слове владельца «ядро как есть» — вариант A VO1.

## Решения, принятые агентом без владельца

1. `[ИИ]` VO1 — вариант B по решению владельца №60 (строка `FORK:` выше); ядро байт в байт в публичный KAIF — только словом владельца.
2. `[ИИ]` Имена жанров `--genre` — те же, что у инструмента хранилища ядра (`document · ticket · comment · message · reply · essay`): одна метка
   значит одно в обоих инструментах.

## Links

`plans/117` критерии 25–26 · `plans/119` (припаркован) · тикеты #102 #103 · `tools/stylometry-snapshot.mjs` · `framework/tools/kaif-voice-lint.mjs` ·
`HOUSE_RULES.md` §2, §5 · `MASTER_PLAN.md` §7 №60, №62, №128, №129.
