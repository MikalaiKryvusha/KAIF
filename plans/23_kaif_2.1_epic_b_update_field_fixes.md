# План 23 — эпик B (2.1): полевые фиксы обновления 2.0

> **Родитель:** `plans/22_kaif_2.1.md` §2 (зонтик 2.1). **Вовне:** —. **Research:** `researches/13` §2 + первоисточники:
> `ideas/ai_agents_reports/23_kaif_20_update_mechanics_from_ndim.md` (ndim 1.6→2.0, находки K1–K6)
> и `ideas/ai_agents_reports/KAIF_2.0_UPDATE_REPORT.md` (анонимная установка 1.6→2.0, добавлен
> владельцем 2026-07-31 — интервью 005 Q3; дефекты Д1–Д7 + предложение A → bugs/26–30).
> **Создан:** 2026-07-31. **Статус:** ✅ ЗАКРЫТ ЦЕЛИКОМ 2026-07-31 (сводка ниже). История хода:
> Фазы B0–B2 ✅ (builds 73–76: баги классами,
> свод s07 красным до/зелёным после, фиксы K1–K5, канонизация «Predict the pass» в /kaif-update +
> KAIF_REFERENCE §10.8). Судейский проход по B1: VERIFIED WITH CAVEATS → F1 закрыт ужесточением
> сетки + страж T6, F2 → bugs/25. **Фаза B3a ✅ (2026-07-31, третий отчёт):** Д1 (снимок values +
> H1-предохранители), Д2 (resync до гейтов), Д3 (email-слот + live-пункт placeholders), Д4 (скип
> шаблон-тождественных в stale-claims), Д6 (canon-lint «not configured» = exit 0), Д7
> (де-экспансия) — bugs/26–28 DONE, bugs/30 partial (грани 1–3); судья B3a: VERIFIED WITH
> CAVEATS → обе находки закрыты до пуша (live-фильтр placeholders; values в записке §12.2);
> полигон 7/7.

## ✅ ЭПИК ЗАКРЫТ ЦЕЛИКОМ (2026-07-31, builds 73–80)

**B3 ✅:** аудит K6 (bugs/24 DONE — TWINS-инвентарь: 3 живых вхождения починены: unpack blockRe ·
runFinalGates · checkpoint; красное доказательство — HEAD-unpack на CRLF: exit 1) + bugs/25 DONE
(fail-closed гейт незавершённого задания в update + bootstrap-грань: superseded-сохранение —
находка F1 судьи). **B4 ✅:** честный размер («framework changed N of M») · сводка size jumps ·
`source` в расписке · модульный аудит в update-verify (предложение A, try/catch по находке F2) ·
Д5/bugs/29 DONE (анонимизированное ядро: origin+аккаунт+авторские токены → плейсхолдеры,
node --check страховка; sync/check/diff/adopt-current живут у анонимов) · грани 30.4–30.6 DONE.
**B5 ✅:** полигон 7/7 сводов на каждом коммите; три судейских прохода (B1, B3a, B3/B4) — все
VERIFIED WITH CAVEATS, все подтверждённые находки закрыты до пушей; счётчики сборки 12/47/139/570
(единственное намеренное изменение против эталона B0: +1 модуль — §10.8 записки); весь беклог
эпика bugs/20–30 ✅ DONE. **Отчёт владельцу: обновление 2.0+фиксы готово к раскатке на парк**
(с main-канала до релиза 2.1, либо дождаться релиза — решение №22 допускает оба).

**Формула эпика:** обновление обязано быть пригодным к сдаче и на переведённом развёртывании, а
предсказание прохода — доступным тем, кто обновляется впервые.

**Контракт «не сломать»** (из отчёта, держать при каждом фиксе): sha256-сверка · разделение
`templateShas`/`shas` (комментарий в `runFinalGates` — «держать в коде вечно») · наследование
параметров из маркера · чекпоинты, исполняющие свою проверку · святой инвариант «контент владельца
байт в байт».

---

## Фаза B0 — беклог и красные стражи (до любого фикса) <!-- closed-ok: документ закрыт ДО появления оси bugs/76; чекбоксы плана остались неотмеченными, а отмечать их задним числом значило бы утверждать за прошлые сессии ненаблюдённое — канон append-only: поправка это новая запись -->

- [ ] Завести баги по классам (BUG_FIXING: класс, не экземпляр): K1+K2 (перевод против модульного <!-- closed-ok: эпик закрыт судьёй ДО появления оси bugs/76; чекбоксы фаз остались неотмеченными, а отмечать их задним числом значило бы утверждать за прошлые сессии ненаблюдённое (append-only) -->
  слияния — один класс) · K3 (слепое первое обновление) · K4 (no-op перезапись) · K5 (stale-claims
  против истории) · K6 (класс `\n`-регекспов на CRLF — наш EXP-0005 в машинерии).
- [ ] Новый свод полигона `s07-translated-deployment`: фикстура развёртывания с ПЕРЕВЕДЁННЫМИ <!-- closed-ok: эпик закрыт судьёй ДО появления оси bugs/76; чекбоксы фаз остались неотмеченными, а отмечать их задним числом значило бы утверждать за прошлые сессии ненаблюдённое (append-only) -->
  заголовками (по механике K1: перевести сигнатуры 2–3 навыков и 1 ключевого документа в
  фикстуре), обновление на тестовую версию. **Стражи доказываются красными на текущем коде:**
  удвоение файлов (размер до/после, двуязычные пары заголовков) · «merged N» в логе при факте
  вставки дубля · отсутствие `## Module diffs` в задании при `i18n: translated`.
- [ ] Снять эталоны «до» (счётчики блоков/модулей, полигон 6/6) — инварианты домена для фазы B5. <!-- closed-ok: эпик закрыт судьёй ДО появления оси bugs/76; чекбоксы фаз остались неотмеченными, а отмечать их задним числом значило бы утверждать за прошлые сессии ненаблюдённое (append-only) -->

## Фаза B1 — K1 + K2: переведённое развёртывание

- [ ] **K1, сетка файла** (фикс №1 отчёта + №2): перед вставкой «новых» модулей — если из N <!-- closed-ok: эпик закрыт судьёй ДО появления оси bugs/76; чекбоксы фаз остались неотмеченными, а отмечать их задним числом значило бы утверждать за прошлые сессии ненаблюдённое (append-only) -->
  дисковых сигнатур в базе найдено ≤1 (кроме `<preamble>`) И диск содержит письменность рабочего
  языка, а вставляемые модули — нет → файл «переведён целиком»: ничего не вставлять,
  `diverged` с пометкой «translated wholesale» (эвристика письменности уже есть —
  `localizedAgainst`; переиспользовать, не дублировать).
- [ ] **K2, dry-run**: под `i18n: translated` разделить «не заменять» и «не анализировать» — <!-- closed-ok: эпик закрыт судьёй ДО появления оси bugs/76; чекбоксы фаз остались неотмеченными, а отмечать их задним числом значило бы утверждать за прошлые сессии ненаблюдённое (append-only) -->
  `mergeModules` в режиме dry-run: на диск не писать, но диффы модулей (где апстрим менял) отдать
  в задание (`## Module diffs`); флаг применять только к файлам, реально содержащим письменность
  рабочего языка (нетронутые EN-файлы продолжают обновляться механически — вторая цена флага из
  отчёта снимается).
- [ ] Лог и задание говорят правду о размере работы (сшивка с B4). <!-- closed-ok: эпик закрыт судьёй ДО появления оси bugs/76; чекбоксы фаз остались неотмеченными, а отмечать их задним числом значило бы утверждать за прошлые сессии ненаблюдённое (append-only) -->
- [ ] Свод s07: после фиксов — зелёный (нет удвоения; diverged с пометкой; диффы в задании; EN-файлы <!-- closed-ok: эпик закрыт судьёй ДО появления оси bugs/76; чекбоксы фаз остались неотмеченными, а отмечать их задним числом значило бы утверждать за прошлые сессии ненаблюдённое (append-only) -->
  заменены), старые своды s01–s06 зелёные.

## Фаза B2 — K3: предсказание первого обновления

- [ ] `diff --source <версия/путь>`: при манифесте v1 строить синтетическую базу тем же кодом, что <!-- closed-ok: эпик закрыт судьёй ДО появления оси bugs/76; чекбоксы фаз остались неотмеченными, а отмечать их задним числом значило бы утверждать за прошлые сессии ненаблюдённое (append-only) -->
  `install --baseline` (код есть) — вместо `die(...)`.
- [ ] Канонизировать приём «песочная копия + настоящий bootstrap» (git archive → песочница → прогон; <!-- closed-ok: эпик закрыт судьёй ДО появления оси bugs/76; чекбоксы фаз остались неотмеченными, а отмечать их задним числом значило бы утверждать за прошлые сессии ненаблюдённое (append-only) -->
  «это не модель прохода, а сам проход») — в `/kaif-update` SKILL.md (оба слоя) и
  `framework/KAIF_REFERENCE.md` §10.
- [ ] Свод: `diff` на v1-манифесте возвращает картину, совпадающую с последующим реальным проходом. <!-- closed-ok: эпик закрыт судьёй ДО появления оси bugs/76; чекбоксы фаз остались неотмеченными, а отмечать их задним числом значило бы утверждать за прошлые сессии ненаблюдённое (append-only) -->

## Фаза B3a — дефекты третьего отчёта (bugs/26–28, 30.1–30.3; стражи красными до)

- [ ] **Д1 / bugs/26 (🔴):** снимок `values` в deploy-манифест, на апдейте значения — оттуда <!-- closed-ok: эпик закрыт судьёй ДО появления оси bugs/76; чекбоксы фаз остались неотмеченными, а отмечать их задним числом значило бы утверждать за прошлые сессии ненаблюдённое (append-only) -->
  (пересчёт только для бывших unresolved); НЕ вставлять отсутствующие adaptive-модули (отдавать
  диффом); предохранитель «второго H1 не бывает, H1 в конец не дописывается». Свод: дрейф
  package.json.name между деплоем и апдейтом → H1 один.
- [ ] **Д2 / bugs/27:** `resyncCopies()` перед гейтами `runFinalGates` (механичен и идемпотентен); <!-- closed-ok: эпик закрыт судьёй ДО появления оси bugs/76; чекбоксы фаз остались неотмеченными, а отмечать их задним числом значило бы утверждать за прошлые сессии ненаблюдённое (append-only) -->
  в тексте ошибки плейсхолдера — назвать `sync`.
- [ ] **Д3 / bugs/28:** слот `<YOUR AGENT'S noreply EMAIL>` → в канонический список (или единый <!-- closed-ok: эпик закрыт судьёй ДО появления оси bugs/76; чекбоксы фаз остались неотмеченными, а отмечать их задним числом значило бы утверждать за прошлые сессии ненаблюдённое (append-only) -->
  `<AGENT_EMAIL>`); пункт `placeholders` в `writeUpdateTask` при непустом unresolved; TWINS-греп
  всех слотов шаблонов против PLACEHOLDERS — инвентарь в bugs/28.
- [ ] **Д4 / bugs/30.1:** stale-claims пропускает пути, тождественные текущему шаблону <!-- closed-ok: эпик закрыт судьёй ДО появления оси bugs/76; чекбоксы фаз остались неотмеченными, а отмечать их задним числом значило бы утверждать за прошлые сессии ненаблюдённое (append-only) -->
  (`fileShaNorm(p) === templateShas[p]`).
- [ ] **Д6 / bugs/30.2:** canon-lint selftest: «не сконфигурирован» ≠ «гвардия не сработала» <!-- closed-ok: эпик закрыт судьёй ДО появления оси bugs/76; чекбоксы фаз остались неотмеченными, а отмечать их задним числом значило бы утверждать за прошлые сессии ненаблюдённое (append-only) -->
  (exit-коды развести). **Д7 / bugs/30.3:** формулировка KAIF_REFERENCE устойчива к де-экспансии.

## Фаза B3 — K4 + K5 + K6-класс

- [ ] **K4:** запись `package.json` (и любых конфигов) — только при реальном отличии <!-- closed-ok: эпик закрыт судьёй ДО появления оси bugs/76; чекбоксы фаз остались неотмеченными, а отмечать их задним числом значило бы утверждать за прошлые сессии ненаблюдённое (append-only) -->
  сериализованного текста от прочитанного.
- [ ] **K5:** `stale-claims` не сканирует директории знаний (`researches/ interviews/ homeworks/ <!-- closed-ok: эпик закрыт судьёй ДО появления оси bugs/76; чекбоксы фаз остались неотмеченными, а отмечать их задним числом значило бы утверждать за прошлые сессии ненаблюдённое (append-only) -->
  bugs/ ideas/`) и `EXPERIENCE.md`; в `STATUS.md` пропускает строки с «предыдущ/previous»; вывод
  делится на «вероятно протухло» / «вероятно история».
- [ ] **K6-класс:** аудит ВСЕХ регекспов машинерии (`installer/*`, `framework/tools/*`, <!-- closed-ok: эпик закрыт судьёй ДО появления оси bugs/76; чекбоксы фаз остались неотмеченными, а отмечать их задним числом значило бы утверждать за прошлые сессии ненаблюдённое (append-only) -->
  `kaif-unpack.mjs`), привязанных к `\n` (фронтматтер, сигнатуры модулей, разбор заданий,
  чекпоинты) → `\r?\n`/нормализация при чтении; инвентарь вхождений — в документ бага (TWINS).
- [ ] Стражи на каждый фикс — красные до, зелёные после (K4: фантомный дифф; K5: легитимная <!-- closed-ok: эпик закрыт судьёй ДО появления оси bugs/76; чекбоксы фаз остались неотмеченными, а отмечать их задним числом значило бы утверждать за прошлые сессии ненаблюдённое (append-only) -->
  история не в «протухло»; K6: CRLF-фикстура).

## Фаза B4 — запросы функциональности

- [ ] Задание печатает честный размер: «фреймворк изменил N файлов из M» (из `templateShas` базы <!-- closed-ok: эпик закрыт судьёй ДО появления оси bugs/76; чекбоксы фаз остались неотмеченными, а отмечать их задним числом значило бы утверждать за прошлые сессии ненаблюдённое (append-only) -->
  против нового бандла) — одной строкой в шапке.
- [ ] Финальная сводка размеров: `файл: было → стало`, топ-10 прироста (K1-класс виден мгновенно). <!-- closed-ok: эпик закрыт судьёй ДО появления оси bugs/76; чекбоксы фаз остались неотмеченными, а отмечать их задним числом значило бы утверждать за прошлые сессии ненаблюдённое (append-only) -->
- [ ] `.kaif/last-update.json` дополняется URL (или путём) бандла предыдущей версии — агент может <!-- closed-ok: эпик закрыт судьёй ДО появления оси bugs/76; чекбоксы фаз остались неотмеченными, а отмечать их задним числом значило бы утверждать за прошлые сессии ненаблюдённое (append-only) -->
  посчитать настоящую дельту без ручного скачивания двух релизов.
- [ ] **Предложение A третьего отчёта:** модульный аудит в `update-verify` — пересплит диска по <!-- closed-ok: эпик закрыт судьёй ДО появления оси bugs/76; чекбоксы фаз остались неотмеченными, а отмечать их задним числом значило бы утверждать за прошлые сессии ненаблюдённое (append-only) -->
  `moduleShas` манифеста → списки MODULE DIFFERS (с классом) / ABSENT / OURS (~40 строк; ловит
  класс «модуль потерял идентичность» — именно он мгновенно показал Д1 в поле).
- [ ] **Д5 / bugs/29:** анонимная самоочистка оставляет АНОНИМИЗИРОВАННОЕ ядро (ORIGIN/SOURCES <!-- closed-ok: эпик закрыт судьёй ДО появления оси bugs/76; чекбоксы фаз остались неотмеченными, а отмечать их задним числом значило бы утверждать за прошлые сессии ненаблюдённое (append-only) -->
  вырезаны, origin-команды отвечают «unavailable») — check/sync/diff/adopt-current живут; греп-гейт
  анонимности зелёный на оставленном ядре (страж в s04).
- [ ] **bugs/30.4–30.6:** предупреждение о самоудалении в thin KAIF.md · сводная строка дрейфа <!-- closed-ok: эпик закрыт судьёй ДО появления оси bugs/76; чекбоксы фаз остались неотмеченными, а отмечать их задним числом значило бы утверждать за прошлые сессии ненаблюдённое (append-only) -->
  зеркал в check · семантика `kept` (выправить или задокументировать).
- [ ] **bugs/25:** судьба невлитой dry-run-дельты (решение: не пересвежать снапшоты dry-run-файлов <!-- closed-ok: эпик закрыт судьёй ДО появления оси bugs/76; чекбоксы фаз остались неотмеченными, а отмечать их задним числом значило бы утверждать за прошлые сессии ненаблюдённое (append-only) -->
  ИЛИ реестр недоставленных дельт в расписке — выбрать по Оккаму, страж двух-хоповым сводом).

## Фаза B5 — сводная верификация

- [ ] Полигон целиком (s01–s07) зелёный; сборка зелёная; счётчики — из вывода сборки. <!-- closed-ok: эпик закрыт судьёй ДО появления оси bugs/76; чекбоксы фаз остались неотмеченными, а отмечать их задним числом значило бы утверждать за прошлые сессии ненаблюдённое (append-only) -->
- [ ] Инварианты «до/после» фазы B0 сошлись (кроме намеренных изменений — названы поимённо). <!-- closed-ok: эпик закрыт судьёй ДО появления оси bugs/76; чекбоксы фаз остались неотмеченными, а отмечать их задним числом значило бы утверждать за прошлые сессии ненаблюдённое (append-only) -->
- [ ] Судья по диапазону эпика; баги фазы B0 → DONE со секциями решений. <!-- closed-ok: эпик закрыт судьёй ДО появления оси bugs/76; чекбоксы фаз остались неотмеченными, а отмечать их задним числом значило бы утверждать за прошлые сессии ненаблюдённое (append-only) -->
- [ ] Отчёт владельцу: можно катить 2.0+фиксы на парк (или дождаться релиза 2.1 — его решение). <!-- closed-ok: эпик закрыт судьёй ДО появления оси bugs/76; чекбоксы фаз остались неотмеченными, а отмечать их задним числом значило бы утверждать за прошлые сессии ненаблюдённое (append-only) -->

## Решения, принятые агентом без владельца

- **bugs/25:** из трёх вариантов фикса выбран fail-closed гейт (а не заморозка снапшотов или
  реестр дельт) — дешевле, закрывает класс; bootstrap-грань — сохранение в superseded-файл, а не
  die (умирать после уже применённой механики хуже).
- **bugs/26:** «не вставлять adaptive» реализован H1-предохранителем (класс на стороне деплоя
  вычисляется по заполненному тексту и ненадёжен); порог сетки — функция размера базы.
- **bugs/28:** слот оставлен как есть (переименование сломало бы поле); пункт placeholders — с
  live-фильтром по диску.
- **bugs/29:** анонимизация плейсхолдерами-не-токенами (`<removed>` и т.п.) вместо слова
  'anonymous' (само стало бы токеном — находка F4); фолбэк на удаление при провале node --check.
- **Правки фикстур сводов** (s02/S5, s03/S9, s04/S13, PH-списки) — каждая с обоснованием в
  коммите: своды стерегли поведение, признанное полевым дефектом.
- Контр-страж «placeholders-пункт не появляется на полностью заполненном развёртывании» и фикстура
  «манифест без values» в полигон не заведены (исполнены судьями вручную) — честно названо в
  bugs/26/28.
