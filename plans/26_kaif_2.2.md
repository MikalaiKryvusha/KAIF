# План 26 — KAIF 2.2: зонтичный план (эпики → фазы → операционные планы)

> **Родитель:** идея 20 (`ideas/20_kaif_2.2_scope.md` — 21 пункт владельца: 1.0–1.12 и 2–22,
> №20 отсутствует в нумерации) + 9 полевых документов (отчёт NDim о контуре · отчёт 25 о разносе ·
> баги Unliminium 54 / ndim 112 · GitHub issue #2 · 4 отчёта об обновлении 2.1).
> **Вовне:** релиз 2.2 (Фаза R: полный судья всего 2.2 → релиз; закрытие issue #2 через
> `send-outbound`).
> **Research-ступень:** `researches/15_kaif_2.2_scope_metasynthesis.md` (метасинтез; инвентарь §1 —
> все пункты покрыты). **Развилки:** `interviews/interview_007_kaif_2.2.md`.
> **Создан:** 2026-08-07. **Статус:** ИСПОЛНЕН, кроме Фазы R. Вехи: планирование завершено
> 2026-08-07, интервью 007 отвечено целиком (решения №32–43) · эпики **L, K, M, N, O закрыты
> 2026-08-07** · эпик **P — ⛔ NOT TO BE DONE в 2.2** (решение №47, кандидат 2.3) · **все десять
> задач T1–T10 закрыты** (последняя — T3, 2026-08-08 16:50 +03:00, чем и открыла гейт Фазы R) ·
> **остаток — только §9, Фаза R** (`plans/59`): полный адверсарный суд всего 2.2 → закрытие
> issue #2 через `send-outbound` → `/release`. Единственные незакрытые чекбоксы документа — семь
> пунктов §9, и это ровно тот остаток.
>
> Указание владельца дословно (преамбула идеи 20): «Все пункты N) изучить, определить, какие их
> них тянут по сложности и масштабу на эпик, какие тянут на просто задачу. Эпики запланировать и
> нарезать на фазы по /plan-epic + их фазы нарезать и запланировать в дочерних операционных
> планах по /plan-task».
>
> ⚙️ Конвенция имён (п. 9 скоупа, применена досрочно): эпики — `NN_EPIC_*`, дочерние
> операционные планы фаз — `NN_epicMM_*` (ссылка на родителя номером). Канонизация конвенции —
> задача T2 (§3 здесь).

---

## 0. Вектор цели и критерии приёмки релиза

**Вектор цели (куда хотим попасть):** KAIF 2.2 — фреймворк, замыкающий петли. Интерактивный
контур доведён до отказоустойчивости уровня «работа человека не пропадает никогда» и замкнут
разносом ответов; поле само кормит развитие фреймворка (обязательные отчёты, баги KAIF, аудиты —
«closed-loop feedback system»); документы становятся проверяемыми (вектор цели + критерии
приёмки + требования по стандарту); агент всегда знает своё окружение и держит канон свежим в
контексте механизмами, а не прозой.

**Критерии приёмки релиза (сводные; наблюдаемые):**

1. Полигон `npm run test:core` зелёный целиком, включая новые своды L (классы обновления), M
   (reports/ + bugs/KAIF доезжают), P (rename-миграция не плодит дубликаты); каждый новый страж
   доказан мутацией с предсказанием ДО прогона (инвариант §9.1 researches/15).
2. Все 21 пункт идеи 20 (включая подпункты 1.0–1.12) трассируются в закрытые эпики/задачи
   (таблица §1 researches/15 → DONE); дыра нумерации №20 зафиксирована.
3. GitHub issue #2 закрыт через `gh` от имени владельца — с выполненными условиями закрытия
   (дайджест issue: 11 условий; гейт приёмки эпика K).
4. Единый реестр инвариантов контура без коллизий номеров; греп-проверка новых канон-правил в
   развёрнутой песочнице (EN) и обвязке (RU).
5. Наши собственные документы 2.2 (этот план, эпики, задачи) исполняют дисциплину п. 13 —
   вектор цели + критерии приёмки в начале (dogfooding досрочно).
6. Минимальный интерактивный контур построен в ЭТОМ репозитории (закрыт дефер D5 из 2.1) (оправдано: «минимальный» — имя скоупа дефера, не мера);
   первое живое интервью после постройки отрендерено им (007, если к тому моменту ещё
   открыто, иначе очередное).
7. Обещания релиза = код: линт бандла (пары «документ ↔ ведущий навык») зелёный; ни одного
   обещанного-но-несуществующего стража (класс L-7 закрыт).
8. Судья: VERIFIED (или все находки закрыты) перед публикацией; явное «да» владельца.

**Сквозные инварианты релиза** — §9 researches/15 (11 штук), действуют на каждый эпик; главные:
каждый страж доказан мутацией · ложная тревога опаснее пропуска · метрики петель двуплечие ·
baseline унаследованного долга · зелёное без содержания хуже красного · новая сущность = полный
круг регистрации доставки · правило-как-проза → точка решения/страж · вендорить дословно.

## 1. Нарезка эпиков и порядок

| # | Эпик/задача | Пункты идеи 20 | План | Статус |
|---|---|---|---|---|
| 1 | **L. Полевые фиксы обновления 2.1** (11 классов + уникальные + 46 запросов — пересчёт L1) | 2 | `plans/28_EPIC_kaif_2.2_L_update_field_fixes.md` | ✅ **ЭПИК ЗАКРЫТ 2026-08-07** (L1–L5 за день; bugs/31–44 все DONE; панель эпика 4× VERIFIED WITH CAVEATS; полигон 11/11) |
| 2 | **K. Интерактивный контур** (реестр инвариантов · исполнимый контракт · стражи обоих плеч · dogfooding · issue #2) | 1.0–1.12 (кроме 1.11) | `plans/27_EPIC_kaif_2.2_K_interactive_contour.md` | ✅ **ЭПИК ЗАКРЫТ 2026-08-07** (K1–K5 за два дня календаря одной датой; реестр 92 позиции · канон I1–I36/M/P/G/T/C/QA/DEF вшит · контур этого репо построен и принят ЖИВЫМ пилотом владельца (интервью 008) · судья VERIFIED WITH CAVEATS 0 блокеров · §12 issue #2 11/11, тикет — Фаза R) |
| 3 | **M. Контур обратной связи** (bugs/KAIF · origin issues · reports/ · обязательные отчёты · «дефект в САМОМ KAIF» · принципы) | 8, 10, 11, 14, 1.11 | `plans/29_EPIC_kaif_2.2_M_feedback_loop.md` | ✅ **ЭПИК ЗАКРЫТ 2026-08-07 15:25 +03:00** (M1–M5; формы `researches/18` · reports/ в поставке, миграция №41 · протоколы в навыках/машинерии · принцип п. 14 · таблица критериев 1–8 в `plans/51`; судья №48: VERIFIED; PENDING приёмки владельца — в `plans/29`) |
| 4 | **N. Канон-архитектура** (REQUIREMENTS_FRAMEWORK · вектор цели/критерии · шапки-мета · таксономия · «контур») | 13, 15, 4, 18, 16 | `plans/30_EPIC_kaif_2.2_N_canon_requirements.md` | ✅ **ЭПИК ЗАКРЫТ 2026-08-07 18:55 +03:00** (N1–N5 за день; REQUIREMENTS_FRAMEWORK — 14-й ключевой + страж s13 · вектор цели в шаблонах, dogfooding 27/27 · шапка-мета 153→0 + doc-header-lint · таксономия 5 ярусов + «контур», инвентарь 9/0 · линтер целевых документов с композицией словаря, 13→0 + bugs/48; судья эпика №48: VERIFIED 7/7, 4 охоты пусты — `plans/30`) |
| 5 | **O. Контекст и окружение** (механизм освежения · досье окружения · **+ O5: хук-конфиги под остальные агентские системы**) | 3, 7 | `plans/31_EPIC_kaif_2.2_O_context_environment.md` | ✅ **ЭПИК ЗАКРЫТ ЦЕЛИКОМ 2026-08-07 — все пять фаз.** O5 (внесена решением №50 через 18 минут после первого закрытия) закрыта тем же днём: 10 систем сняты живыми источниками, 4 образца конфигов (Codex/Cursor/Copilot/Antigravity), флаг формы `--emit`, Grok Build подтвердил паритет и образца не требует, Windsurf и Cline честно «не поддержано», Meta Muse Code найдена и ждёт вендорскую доку; свод s14 +21 ассерт с красным по мутации; адаптеры ресинкнуты + новый `google-antigravity.md` (**Gemini CLI закрыт 2026-06-18**). Прежняя запись первого закрытия: ✅ 2026-08-07 21:47 +03:00 (O1–O4 за день: карта механизмов `researches/19` · канон освежения обоих слоёв + маркер-свидетельство + охота судьи «Refresh witness» · модуль `.kaif/hooks/` в поставке со сводом s14 + досье окружения + `bugs/49` · O4 — хуки ПОДКЛЮЧЕНЫ (`.claude/settings.json`) и ВСЕ ТРИ наблюдены живьём каждый своим событием: страж STATUS (блок + изолированный cooldown) · таймер обеими половинами · впрыск `SessionStart:clear` 21:47; досье переснято 13/13 без дрейфа; судья эпика **VERIFIED** 7/7 — `plans/58`) |
| 6 | **P. Переименование kaif-*** (схема · rename · миграция парка) | 6 | `plans/32_EPIC_kaif_2.2_P_skill_rename.md` | ⛔ NOT TO BE DONE в 2.2 (решение №47; кандидат 2.3) |
| 7 | T1. Навык-«пинок» продолжения | 5 | §2 здесь | ✅ **ЗАКРЫТА 2026-08-08 00:40 +03:00** (`plans/62`): навык `kaif-go` (алиас `/go`) в обоих слоях + 9 языковых пакетов; ось навыков взята под стража (`counters-guard` 5 → 17 зеркал); критерий 9 снят полевым наблюдением 2026-08-08 06:00 +03:00 — голый пинок в новом чате повёл сессию |
| 8 | T2. Конвенция EPIC-имён в plans/ | 9 | §3 здесь | ✅ **ЗАКРЫТА 2026-08-08 06:11 +03:00**: конвенция в README обоих слоёв + 9 языковых пакетов, `/plan-epic` и `/plan-task` обоих слоёв; ось ИМЁН взята под стража в `doc-header-lint` (селфтест 12 → 14, свод s13 — три состояния на живом корне); живой прогон 25 детей / 6 эпиков / 0 нарушений |
| 9 | T3. Ревизия /code-revision → reports/KAIF_AUDIT | 12 | `plans/33_kaif_2.2_task_audit_revision.md` | ✅ **ЗАКРЫТА 2026-08-08 16:50 +03:00**: все 5 шагов и все 7 критериев сняты; судья **VERIFIED WITH CAVEATS**, 0 фродов, обе оговорки против собственной работы агента; артефакты — навык обоих слоёв (74 → 100 строк) · `references/audit-report-template.md` обоих слоёв · пробный аудит `reports/KAIF_AUDIT/2026-08-08_*` (урожай — `bugs/55`–`59`). Её закрытие сделало **гейт Фазы R зелёным** |
| 10 | T4. Канонизация стилометрии (портрет-файл) | 17 | `plans/34_kaif_2.2_task_stylometry_canon.md` | ✅ **ЗАКРЫТА 2026-08-08 12:55 +03:00**: канон-имя `AUTHOR_STYLOMETRY.md` (решение №39, гейт Q8 снят 2026-08-07) и оно стерегомо; веха — разблокирована 2026-08-08 11:30 +03:00, числившаяся блокировка «нужен живой владелец» оказалась ложной (см. §Закрытие в `plans/34`) |
| 11 | T5. Origin-режим по умолчанию | 19 | §4 здесь | ✅ **ЗАКРЫТА 2026-08-08 08:40 +03:00** (№53 = A, малой кровью): дефолт был origin и до задачи — закрыл её СТРАЖ, а не факт кода: свод `s01` +5 ассертов (маркер установки без флагов = origin с адресом · анонимность по явному флагу без поля origin · update на анонимном отказывается), красный доказан мутацией дефолта; README обеих половин §2.3 и `KAIF_REFERENCE` §11.3/§12.1 называют дефолт |
| 12 | T6. Test suite — обязательство разработчика | 21 | §5 здесь | ✅ **ЗАКРЫТА 2026-08-08 10:05 +03:00**: секция «Работа производит и средство своей проверки» в `TESTING_FRAMEWORK` обоих слоёв (применение принципа 3 к производству, второго канона рядом нет) + строка в «Стражах» `BUG_FIXING` обоих слоёв + поле **Верификация** `/plan-task` и фаза тестирования `/plan-epic` обоих слоёв; карта модулей 679 → 680 |
| 13 | T7. «Код прежде когниции» в PHILOSOPHY | 22 | §6 здесь | ✅ **ЗАКРЫТА 2026-08-08 00:25 +03:00**: принцип в `PHILOSOPHY` обоих слоёв за Парето; `AGENT_GUIDE` ссылается на него вместо второго канона; карта модулей 671 → 672 |
| 14 | **T8. Канон временных меток: дата+время** | — (наброс владельца 2026-08-07 ≈10:05, КАНОН) | §7 здесь | ✅ **ЗАКРЫТА 2026-08-08 09:35 +03:00**: канон в `AGENT_GUIDE` обоих слоёв · ось меток в `doc-header-lint` (селфтест 14 → 16; на живом корне 0 → мутация → 1 → откат → 0) · `localStamp()` в машинерии (расписка, `history`, `verifiedAt`) · конвенция `DONE (date + time)` в точках решения обоих слоёв и **9 языковых пакетах** (потребовал страж протухания `bugs/44`) · своды s13 +3 и s03 +3 |
| 16 | **T9. Определение «KAIF — это…» в README** | — (наброс владельца 2026-08-07 ≈15:45) | §8 здесь | ✅ **ЗАКРЫТА 2026-08-08 00:10 +03:00**: определение владельца вернулось в обе половины README побайтно (извлечено из `git show 1dd1747:README.md`), PDF пересобран |
| 15 | **T10. Информативный зов контура** (сообщать владельцу, не только спрашивать) | — (наброс владельца 2026-08-07 ≈12:20 → `ideas/21`) | `plans/61_task_T10_informative_contour_call.md` (`ideas/21` → DONE) | ✅ **ЗАКРЫТА 2026-08-07 23:30 +03:00**: все 8 шагов и все 8 критериев приёмки сняты — класс «сообщение» (`--notice`), явная пометка «ОК, прочитано» штатным исходом (код 0), непрочитанные отдельной группой ПОД вопросами с повторной доставкой как долгом агента, инварианты **I37/I38** в обоих слоях контракта (ростер s12 — 38 позиций), три стража доказаны красным. Внесена решением №51 (интервью №010 Q2 = B) |

> ✅ **Состав задач подтверждён владельцем — интервью №011, Q1 = C** (решение №52, 2026-08-07
> 22:16 +03:00): **все девять задач T1–T9 остаются в 2.2**, перенесённых в 2.3 нет. Рекомендация
> агента была противоположной (минимум T9+T8); владелец выбрал полноту и назвал критерий на весь
> остаток версии дословно: «мы не торопимся. Мы делаем хорошо и полно, качественно, и тестируем,
> и полируем, чтобы 2.2 получилась полноценной и крепкой версией». Трактовка выполнения T5 —
> интервью №011, **Q2 = A** (решение №53): см. §4.
>
> **Действующий порядок остатка 2.2: O5 → T10 → T1–T9 → Фаза R** (`plans/59`).

**Рекомендуемый порядок: L → K → M → N → O → P → Фаза R** (обоснование — §10 researches/15:
L горит на каждом обновлении парка; K — критичнейший блок скоупа с готовыми полевыми
контрактами; M даёт reports/ для T3 и отчётов; N — стандарты; O зависит от таксономии N;
P — одним скриптовым проходом по готовому дереву). Задачи T1–T7 — параллельно, в дырках между
фазами. Порядок — рекомендация; финальное слово — интервью 007 (Q6).

> ⛔ **2026-08-07, скоуп облегчён (решение №47):** эпик **P — NOT TO BE DONE в 2.2**,
> возможно, переедет на версию 2.3 (слово владельца дословно — в MASTER_PLAN §7 №47).
> Действующий порядок исполнения 2.2: **L → K → M → N → O → Фаза R**. Решения №35/№36
> остаются в силе, исполнение сдвигается вместе с эпиком; `plans/32` и дочерний `plans/40` —
> готовая заготовка на реанимацию.

**Гейт интервью 007 — СНЯТ (отвечено 2026-08-07, решения №32–43 в MASTER_PLAN §7):** кодовое
имя — **«KAIF 2.2 — Yolden KAIF»** (Фаза R) · имя контура — **«Интерактивный контур»**
(канонизируется прижившееся; артефакты K именуются им) · поставка реализации — контракт
вендорится в навык, без докачки из origin (K3) · схема rename — все кроме fable-*, старые имена
алиасами (эпик P разблокирован) · навык-пинок — `kaif-go`/`/go` + естественные триггеры
«дальше»/«продолжай»/«continue» (T1) · константы утверждены, пульсы смягчены до «допустимо раз
в минуту» — числа фиксирует K1 сверкой с кодом NDim, вшивание — K2/K3 · портрет-файл —
`AUTHOR_STYLOMETRY.md` (T4) · REQUIREMENTS_FRAMEWORK — 14-й ключевой документ (эпик N) ·
reports/ у нас — мигрировать `ideas/ai_agents_reports/` (эпик M, фазы M2/M5) ·
KAIF_UPDATE_TASK — ignore + коммитимая квитанция с вердиктом (L/M) · детализация — канон N+1
подтверждён.

**Дисциплина детализации** (канон лестницы; подтверждено ответом Q12 — решение №43): у каждого
эпика операционный план ТОЛЬКО ближайшей фазы — отдельным дочерним файлом по конвенции T2
(K→`plans/35` · L→`plans/36` · M→`plans/37` · N→`plans/38` · O→`plans/39` · P→`plans/40`);
эпик-файлы держат архитектуру фаз без операционной детализации (буква п. 9 идеи 20);
операционный план фазы N+1 пишется на закрытии фазы N — со всем, чему научила фаза N.

## 2. Задача T1 — навык-«пинок» продолжения (п. 5)

**Вектор цели:** владельцу достаточно одной короткой команды, чтобы «пнуть» агента продолжать по
плану — вместо набора «Да», «продолжай», «делай», «одобрено».
**Готово, когда:** навык существует в payload + обвязке; произнесение команды в чате возобновляет
работу с точки паузы по STATUS/плану БЕЗ повторного вопроса «продолжать ли»; алиасы в 9 языковых
пакетах; README-строка в обеих половинах; сборка регистрирует навык во всех точках доставки.

- [x] T1.1. Имя навыка — из интервью 007 (Q5; кандидаты: `/go` · `/continue` · `/kaif-go`).
      До ответа — рабочее имя `/go`. Семантика: «продолжай текущую работу по плану; если пауза
      была на развилке — прими рекомендованный вариант и продолжай» (граница: НЕ заменяет
      «да» владельца на решениях уровня видения — write-gate и AUTH-строки остаются).
- [x] T1.2. `framework/skills/<имя>/SKILL.md` (EN, generic) + копия в `.claude/skills/` (RU-плейсхолдеры
      заполнены); сшивка с /resume (продолжение ≠ вход в сессию) и /pause (парковочная заметка —
      точка возврата).
- [x] T1.3. Триггер-алиасы 9 языковых пакетов; README обеих половин (+1 строка таблиц);
      счётчик навыков пересчитает сборка.
- [x] T1.4. Верификация: сборка зелёная · навык в развёрнутой песочнице · греп алиасов ·
      наблюдение — «пинок» в живом чате возобновляет работу (dogfooding в первой же сессии).

**Риски:** топ-1 — семантический конфликт с «да» на развилках видения (лечение: явная граница в
навыке, T1.1); ложное срабатывание алиасов на бытовое «продолжай» в середине фразы — алиасы
только на отдельностоящую команду.

## 3. Задача T2 — конвенция EPIC-имён в plans/ (п. 9)

> ✅ **ЗАКРЫТА 2026-08-08 06:11 +03:00.** Конвенция, которая полгода жила де-факто, записана в
> канон ОБОИХ слоёв и **взята под стража**. Что получилось:
> **T2.1** — абзац «Именование» в README `plans/` payload (`framework/readmes/plans.md`) и обвязки:
> эпик = `NN_EPIC_<имя>.md` (пофазовая архитектура, *никакой операционной детализации*), дети =
> `NN_epicMM_<фаза>_<имя>.md` (`MM` — номер родителя), самостоятельный план = `NN_<имя>.md`,
> и явная строка «конвенция действует вперёд — старые планы не переименовываются» (риск раздела
> закрыт текстом, а не памятью).
> **Скрытая поверхность поставки, вскрытая стражем протухания пакетов (bugs/44):** локализованный
> `plans/README.md` едет в **9 языковых пакетах** — абзац переведён во все девять (ar, de, es, fr,
> hi, ja, pt, ru, zh-Hans), пин `_lang-pack-source-shas.json` передвинут ТЕМ ЖЕ коммитом. Без этого
> стража конвенция доехала бы до поля по-английски в девяти локализациях, и не заметил бы никто.
> **T2.2** — `/plan-epic` в обоих слоях: Ступень 2 пишет мета-план В ФАЙЛ `NN_EPIC_*`, Ступень 3
> называет файл ребёнка, Ступень 4 сшивает имя с уже каноничной цитатой якоря («имя говорит, КАКОМУ
> эпику служит шаг; якорь — КАКУЮ строку он исполняет»); `/plan-task` в обоих слоях получил блок
> «Планируешь ФАЗУ эпика?» — родитель объявляется дважды, именем и цитатой.
> **T2.3 (верификация наблюдением)** — сборка зелёная; конвенция доехала до бандла **10 поверхностями**
> (EN-README + оба навыка + 9 пакетов — греп по `dist/KAIF-CORE-BUNDLE.md`); живой прогон по
> `plans/`: **25 детей, 6 эпиков, 0 нарушений**.
> **Правило-как-проза стало стражем** (сквозной инвариант релиза): ось имён `plans/` вшита в
> `tools/doc-header-lint.mjs` — у каждого `NN_epicMM_*` родитель `MM_` существует И назван
> `MM_EPIC_*`. Скоуп задаёт САМ ПАТТЕРН (старые планы его не используют → молчат), порога по номеру
> не заведено — константа без работы. Красный доказан мутацией: селфтест 12 → **14** предсказанных
> находок (сирота + плоский родитель), свод **s13** проверяет все три состояния на живом корне,
> включая обратное — переименовал родителя в `30_EPIC_probe.md`, страж замолчал и посчитал ребёнка.
> Гейты: «all 14 suites green» · «counters OK — 17 зеркал» · «doc-header-lint 97/0» ·
> «requirements-lint 141/0».
> **Решения агента без владельца:** (1) страж поселён в существующий линтер, а не заведён новым
> инструментом — у `doc-header-lint` уже есть скоуп `plans/`, ритуалы и консультативный режим
> (Оккам: новая сущность не понадобилась); (2) обратную сторону конвенции («этот план — на самом
> деле эпик, а назван плоско») машина НЕ проверяет: по имени эпик не опознать, признак пришлось бы
> выдумать — правило трёх дверей запрещает; (3) de-facto форма `NN_task_TN_*` (планы 61, 62) НЕ
> канонизирована — это частный случай `NN_<имя>.md`, и второе имя одному понятию не заводим;
> (4) чекбоксы закрытых задач T7/T9 отмечены заодно: навык-пинок навигирует по «шагу после
> последнего отмеченного чекбокса», и неотмеченный бокс в закрытой секции — ловушка для следующей
> сессии, а не украшение.

**Вектор цели:** эпики видны в беклоге по имени файла; дочерние операционные планы находят
родителя без чтения содержимого.
**Готово, когда:** README `plans/` обоих слоёв описывают конвенцию; /plan-epic и /plan-task
предписывают её; планы 2.2 этой сессии УЖЕ названы по ней (dogfooding-доказательство).

- [x] T2.1. Конвенция в README `plans/` (payload `framework/readmes/plans.md` + обвязка) — по
      букве п. 9 идеи 20: эпик = `NN_EPIC_<имя>.md` («высокоуровневое пофазовое архитектурное
      описание дорожной карты по реализации фичи, без операционной детализации» — слова
      владельца); операционка живёт у ДЕТЕЙ: `NN_epicMM_<фаза>_<имя>.md` (MM — номер родителя);
      горизонт детей — ближайшая фаза (решение №43); самостоятельные планы без эпика — как
      раньше `NN_<имя>.md`.
- [x] T2.2. /plan-epic: мета-план пишется В ФАЙЛ `NN_EPIC_*`; /plan-task: дочерний план фазы
      ссылается на родителя именем И цитирует строку-якорь мета-плана (трассировка уже в каноне —
      сшить с конвенцией имени). Оба слоя навыков.
- [x] T2.3. Верификация: сборка · греп конвенции в песочнице · существующие планы 2.2
      (27–32 EPIC, 35–36 дети) соответствуют — команда-страж
      `ls plans/ | grep -E '^[0-9]+_epic[0-9]+_'` находит детей, их родители существуют.

**Риски:** переименовывать СТАРЫЕ планы (06–25) — не нужно и вредно (ломает ссылки истории);
конвенция действует с 2.2 вперёд — явная строка в README.

## 4. Задача T5 — origin-режим установки по умолчанию (п. 19)

**Вектор цели:** свежая установка KAIF по умолчанию привязана к origin (обновления, отчёты,
петля обратной связи работают из коробки); анонимный режим — осознанный выбор по явной просьбе.
**Готово, когда:** установка без слов про анонимность даёт маркер с origin-tracking; анонимная —
только при явной просьбе; поведение доказано полигоном.

> ✅ **Трактовка выполнения решена владельцем — интервью №011, Q2 = A** (решение №53, 2026-08-07
> 22:16 +03:00): **закрыть в 2.2 малой кровью — правка кода не нужна.** Проба 2026-08-07
> показала, что дефолт УЖЕ origin (`KAIF-CORE.mjs:28` — `--mode <m> standard (default) |
> anonymous`; `:1670` — `ANON ? { tracking: 'anonymous' } : { origin: ORIGIN, tracking: 'origin' }`),
> то есть шаг T5.2 закрыт по построению. Остаются **свод полигона** «дефолтная установка = origin»
> (он и есть доказательство, что дефолт не уедет молча при следующей правке машинерии) и строки
> в доках (T5.3). Прецедент трактовки: факт кода НЕ закрывает задачу, пока поведение не
> стерегомо.

> ✅ **ЗАКРЫТА 2026-08-08 08:40 +03:00.** Дефолт был origin и ДО задачи — задачу закрывает не
> факт кода, а **страж**: без него следующая правка машинерии уводит дефолт в анонимность молча,
> и поле теряет обновления, отчёты и петлю обратной связи из коробки. Страж стережёт **маркер
> после установки без единого флага**, а не строку помощи (помощь — проза, маркер — поведение),
> и доказан красным мутацией самого дефолта.

- [x] T5.1. Разведка текущего дефолта: `framework/installer/KAIF-CORE.mjs` + `_thin-intro.md` +
      `kaif-unpack.mjs` — где сейчас решается режим и что спрашивается у владельца (читать код,
      не память).
      **Снято чтением кода:** `KAIF-CORE.mjs:54–55` — `let MODE = (val('--mode') || 'standard')`,
      `ANON = MODE === 'anonymous'`; `:1670` — `ANON ? { tracking: 'anonymous' } : { origin:
      ORIGIN, tracking: 'origin' }`; `:113` — post-install команды наследуют режим из маркера.
- [x] T5.2. ~~Правка дефолта~~ — **не требуется** (решение владельца №53: дефолт уже origin;
      правки кода нет по построению). Вопроса установки о режиме нет вовсе: анонимность
      выбирается явным `--mode anonymous`, то есть «без давления» выполнено по построению.
- [x] T5.3. Документы: README **обеих половин** — §2.3 переименована «Anonymous installation» →
      «Install mode: origin by default, anonymity on request» / «Режим установки: origin по
      умолчанию, анонимность по просьбе», первым абзацем назван дефолт и выгода origin, последней
      строкой — что обновление НЕ переводит развёртывание из режима в режим; `KAIF_REFERENCE`
      (источник `framework/`) — §11.3 с тем же переименованием + строка о страже, §12.1 `tracking`
      помечен `(the default, §11.3)`. **policyChanges НЕ тегируется:** тегируются изменения
      политики, а поведение не изменилось — тег объявил бы полю несуществующую смену режима.
- [x] T5.4. Верификация: полигон — свод `s01` получил пять ассертов (S1: `tracking=origin` +
      адрес origin в маркере; S2: `--mode anonymous` → `tracking=anonymous`, поля `origin` НЕТ,
      `update` на анонимном отказывается с названной причиной). Анонимный греп-гейт не тронут и
      по-прежнему зелёный (S2 `check` без флага). **Красный доказан мутацией** дефолта на
      `'anonymous'` с пересборкой: оба ассерта S1 красные, пара S2 остаётся зелёной (мутация
      трогает только дефолт) — мутация откачена, дерево пересобрано.

**Риски:** сломать анонимный путь (лечение: оба пути в полигоне); тихая смена политики для
существующих проектов — обновление НЕ меняет их режим (только свежие установки; строка в нотах).

## 5. Задача T6 — test suite как обязательство разработчика (п. 21)

**Вектор цели:** агент, разрабатывающий код проекта, обязан вместе с кодом производить тестовые
артефакты (test suite, чек-листы, тест-кейсы) и планировать тестирование по ним — «сырому
доверия нет» распространяется с проверки НА ПРОИЗВОДСТВО проверяемого.
**Готово, когда:** обязательство записано в BUG_FIXING_FRAMEWORK и TESTING_FRAMEWORK обоих
слоёв; вплетено в точки решения (/plan-task — шаг «тестовые артефакты», fable-цикл шаг 5);
греп-проверка в песочнице.

> ✅ **ЗАКРЫТА 2026-08-08 10:05 +03:00.** Обязательство записано ОДНОЙ секцией и ДВУМЯ полями
> шаблонов — ровно так, как требовал риск постановки («отдачу имеют гейты и поля шаблонов, не
> абзацы»). Формулировка обязательства: «Сырому доверия нет» связывает ПРОИЗВОДИТЕЛЯ, а не только
> проверяющего — артефакты проверки планируются вместе с работой и ложатся В ТОТ ЖЕ шаг.

- [x] T6.1. Канон-строки: `TESTING_FRAMEWORK` обоих слоёв — новая секция «Работа производит и
      средство своей проверки» (EN: «The work produces its own means of checking») сразу перед
      гейтами наблюдения. **Второго канона рядом не заведено:** секция названа применением
      ПРИНЦИПА 3 к производству вместо инспекции, объясняет существование харнесс-секции ниже и
      ЯВНО не повторяет правило стража («это правило уже живёт в `BUG_FIXING_FRAMEWORK.md` и
      здесь не повторяется»). Гейт тривиальности сохранён дословно. `BUG_FIXING` обоих слоёв —
      одна строка в «Стражах»: то же обязательство работает ВПЕРЁД, а не только после дефекта;
      «страж — то, что дефект оставляет после себя; test suite — то, что работа приносит с собой».
- [x] T6.2. Точки решения: `/plan-task` обоих слоёв — поле **Верификация** требует теперь назвать
      КАКИМИ тестовыми артефактами проверяется утверждение И ГДЕ ОНИ ЖИВУТ (свод/кейс/фикстура/
      страж с путём), и что артефакты производит ЭТОТ ЖЕ план теми же шагами, а не «потом».
      `/plan-epic` обоих слоёв — фаза тестирования планируется ПО написанным артефактам; фаза,
      чьих артефактов ещё нет, «выдумает себе верификацию в последний момент».
- [x] T6.3. Верификация: сборка чиста · строки доехали в поставку (греп `dist/KAIF-CORE-BUNDLE.md`
      и `dist/KAIF-FULL.md` — по 4 вхождения) · бренд-токенов в правленом payload ноль (EXP-0023) ·
      карта модулей 679 → **680** (новая секция = новый модуль), расхождение поймал
      `counters-guard` на четырёх зеркалах прозы и они поправлены по выводу инструмента ·
      полигон 14 сводов зелёный · `doc-header-lint 98/0`.

**Риски:** раздуть канон прозой (антиправило эпика I 2.1: отдачу имеют гейты и поля шаблонов,
не абзацы) — правка идёт полями шаблонов и одной канон-строкой.

## 6. Задача T7 — «код прежде когниции» в PHILOSOPHY (п. 22)

> ✅ **ЗАКРЫТА 2026-08-08 00:25 +03:00.** Принцип **«Код прежде когниции — детерминизм первым,
> модель вторым»** внесён в раздел «Расширенный набор принципов» ОБОИХ слоёв, сразу за Парето
> (пара 80/20 читается вместе): `framework/PHILOSOPHY.md` (EN, поставка — **без бренд-токенов**,
> EXP-0023: греп `Krinik|Kryvusha|Криник|Кривуша` по правленым payload-файлам пуст) и корневая
> RU-копия, где формулировка владельца названа источником дословно (идея 20 п. 22: «Всё, что можно
> сделать кодом — делай кодом… 80 % автоматизации должны быть детерминированными. 20 % — AI»).
> **T7.2 (сшивка):** «Разделение по силе модели» в `AGENT_GUIDE` обоих слоёв теперь ССЫЛАЕТСЯ на
> принцип («операционное лицо одного принципа… формулируется там один раз и применяется здесь») —
> второго канона рядом не заведено. **T7.3 — выполнено ранее:** граница «Оккам НЕ применяется к
> тому, что человек видит и слышит» уже стоит в обоих слоях (`framework/PHILOSOPHY.md:41` и
> корневая копия) — переписывать нечего, проверено грепом. **T7.4:** принцип доехал до поставки
> (`dist/KAIF-FULL.md` — 1 вхождение); сборка чиста; полигон 14 сводов зелёный; страж счётчиков
> поймал разошедшееся зеркало карты модулей (671 → **672**, новый раздел = новый модуль) — три
> строки прозы обновлены, `counters OK — 5 зеркал`; `README.pdf` пересобран.

**Вектор цели:** в PHILOSOPHY закреплён принцип: «Всё, что можно сделать кодом — делай кодом.
ИИ вызывается только там, где нужно творчество или рассуждение. 80 % автоматизации должны быть
детерминированными. 20 % — ИИ» (формулировка владельца из идеи 20 — вносится с минимальной
редактурой, помечается источником).
**Готово, когда:** принцип в PHILOSOPHY обоих слоёв (раздел «Расширенный набор принципов»);
сшит с существующим «Разделением по силе модели» в AGENT_GUIDE («всё машинно-проверяемое
проверяет КОД; за LLM остаются суждения» — не второй канон, а взаимные ссылки); правка «Оккам
не применяется к перцептивному» (issue #2 → эпик K) внесена ТЕМ ЖЕ заходом в PHILOSOPHY.

- [x] T7.1. Текст принципа в `framework/PHILOSOPHY.md` + корневая копия (RU) — с примером
      уровня KAIF (стражи/линтеры/полигон = код; вкус/канон-формулировки = суждение).
- [x] T7.2. Сшивка: AGENT_GUIDE «Режимы строгости → Разделение по силе модели» ссылается на
      принцип; /fable-judge hunt-list не трогаем (уже охотится на ручную работу там, где есть
      команда).
- [x] T7.3. Координация с эпиком K: правка «Оккам не для перцептивного» (класс вкуса уже в
      каноне 2.1 — добавляется строка в сам принцип простоты) — одним коммитом с T7.1, чтобы
      правки принципа простоты шли одним заходом (третье касание PHILOSOPHY — соглашение о
      принципах M4 — другая секция, координируется на фазе M4 отдельно).
- [x] T7.4. Верификация: сборка · греп в песочнице · судья канона.

**Риски:** принцип может читаться как запрет LLM-суждений вообще — граница «творчество или
рассуждение» из формулировки владельца сохраняется дословно.

## 7. Задача T8 — канон временных меток: И дата, И время (наброс владельца, решение №49)

**Постановка владельца дословно** (чат 2026-08-07, ≈10:05, посреди закрытия эпика L;
зафиксировано 2026-08-07 10:09 +03:00): «⛔ 2026-08-07, скоуп облегчён - пометок датами без
контретного локального времени пользователя - недостаточно. Метка должна иметь И ДАТУ И
ВРЕМЯ - это КАНОН соглашений KAIF. Вносим как ещё одну задачу в 2.2» + уточнение: «И ДАТУ И
ВРЕМЯ, КОГДА РЕШЕНИЕ БЫЛО ПРИНЯТО И ЗАФИКСИРОВАНО».

> ✅ **ЗАКРЫТА 2026-08-08 09:35 +03:00.** Канон записан в оба слоя (`AGENT_GUIDE` → «Гигиена
> документов и текста»), стерегомая ось живёт в `doc-header-lint`, машинерия штампует момент, а
> не день. **Скоуп задаёт САМА МЕТКА** (прецедент оси имён T2): примета — узкий глагол
> принятия/фиксации/закрытия ВПЛОТНУЮ к дате, порог — СОБСТВЕННАЯ дата метки. Поэтому канон
> действует вперёд по построению, и базовой линии, которую надо вести и сокращать, не заводится
> (п. 4 скоупа выполнен формой, а не дисциплиной).
>
> **Что сделано по пунктам:**
> 1. **Формат канонизирован:** проза — `ГГГГ-ММ-ДД ЧЧ:ММ ±ЧЧ:ММ`, квитанции машинерии — тот же
>    момент полным локальным ISO 8601; «принято» и «зафиксировано» различаются; незалогированная
>    точность пишется честным `≈`. Обе половины: `framework/AGENT_GUIDE.md` (EN, источник) и
>    корневая RU-копия.
> 2. **Охват.** Журнал решений — шаблон `framework/MASTER_PLAN.md` (колонка `Date` → `Stamp`,
>    плейсхолдер с временем); повествование ядра `_intro.md` («use absolute dates» → «stamp
>    moments with the absolute date AND time»); квитанции машинерии — `KAIF-CORE.mjs` получил
>    `localStamp()` (часы владельца со смещением, не UTC) вместо трёх `toISOString().slice(0,10)`
>    в расписке, `history` маркера и `verifiedAt`; схема квитанции в `KAIF_REFERENCE` §12.1/§12.3;
>    конвенция закрытия `## ✅ STATUS: DONE (date)` → `(date + time)` в точках решения обоих
>    слоёв — `BUG_FIXING_FRAMEWORK` ×2, `readmes/bugs`, навыки `report-bug`/`check-backlog`/
>    `interview` ×2 слоя — и **в девяти языковых пакетах**, чего потребовал страж протухания
>    (`bugs/44`) на первой же сборке: без ресинка конвенция доехала бы в поле по-старому в девяти
>    локализациях, молча. **EXPERIENCE-заголовки в охват НЕ вошли осознанно** — дата в ключе
>    записи это ИДЕНТИФИКАТОР, а не момент принятия решения; граница названа в самом каноне.
> 3. **Страж** — ось в `tools/doc-header-lint.mjs` (не новый инструмент: скоуп файлов и селфтест
>    уже те самые). Красный доказан ДВАЖДЫ: селфтест 14 → **16** находок на фикстуре, где один
>    файл держит все состояния оси разом (без времени · без зоны · полная метка · и три
>    МОЛЧАНИЯ — история до порога, существительное «Закрытие», сводка о наборе «закрыты»); и на
>    ЖИВОМ корне — 0 находок → мутация настоящей метки → 1 → откат → 0. Свод `s13` получил три
>    ассерта тем же жанром, `s03` — три ассерта формы момента в квитанциях.
> 4. **Ретроспектива не переписывается** — 46 меток старше порога молчат (обмер до правки).
>
> Гейты: «all 14 suites green» · сборка чиста · «counters OK — 17 зеркал» · «doc-header-lint 98/0».

**Скоуп задачи (исполнение — отдельной сессией):**
1. Канонизировать формат метки в конвенциях KAIF (кандидат: `YYYY-MM-DD HH:MM ±TZ`,
   локальное время владельца; различать «принято» — момент слова владельца — и
   «зафиксировано» — момент записи/коммита; когда точное время принятия не логировалось —
   честное `≈`, не выдуманная точность).
2. Охват: журнал решений MASTER_PLAN §7 · летопись PROJECT_HISTORY · шапки/статусы планов и
   баг-доков · квитанции машинерии (`last-update.json` date, `history` маркера, `appendHistory`
   пишут только дату — расширить временем) · шаблоны framework/* с конвенцией дат ·
   EXPERIENCE-записи.
3. Страж/линт на новые метки без времени — по канону «фикс без стража — фикс в кредит»
   (форма — решает исполнитель задачи; норм шума — как у стража интервью).
4. Ретроспективно даты БЕЗ времени в летописи не переписываются (append-only; поправка =
   новая запись) — канон действует с момента принятия ВПЕРЁД.

## 8. Задача T9 — вернуть дословное определение «KAIF — это…» в README (наброс владельца)

> ✅ **ЗАКРЫТА 2026-08-08 00:10 +03:00.** Определение возвращено в ОБЕ половины README и
> **побайтно равно канону** (сверка `Buffer.compare` со строкой из `git show 1dd1747:README.md` —
> текст не перепечатывался руками, а извлекался из истории). Место — исходное: сразу после блока
> навигации, перед фразой о жанре руководства; EN-строка снова легла на **строку 28**, как в
> до-2.1 README. Жанр руководства 2.1 (решения №30/№31) не тронут. Приёмка: грепы
> `strategic-operational` (стр. 28, EN-половина) и `стратегическо-операционный` (стр. 484,
> RU-половина, граница половин — стр. 461) непусты · класс EXP-0018 (`^[+*] ` после врапа) — 0
> совпадений · `README.pdf` пересобран · `counters OK — 5 зеркал` · сборка чиста.

**Постановка владельца дословно** (чат 2026-08-07, принято ≈15:45 +03:00, посреди фазы N1;
зафиксировано 2026-08-07 15:50 +03:00): «запланируй дополнительную задачу на 2.2 - вернуть
описание "KAIF - это" которое было до 2.1, которое криник писал вручную, что это
стратего-операционный методологичесуий фреймворк - это было дословное определение Криника -
нужно его вернуть в 2.2 README».

**Канон для возврата — дословные строки до-2.1 README** (источник: `git show 1dd1747:README.md`;
определение потеряно свапом README в жанр руководства на релизе 2.1, коммит `1b3f6aa`; в
текущем README грепом не находится — проверено 2026-08-07):

- EN (строка 28): «**KAIF — Krinik AI Framework — a context-resilient, fundamental
  strategic-operational methodological framework for AI agents: resilience to context loss and
  discipline of autonomy.**»
- RU (строка 460): «**КАИФ — Криник АИ Фреймворк — контекстоустойчивый фундаментальный
  стратегическо-операционный методологический фреймворк для ИИ-агентов: устойчивость к потере
  контекста и дисциплина автономности.**»

**Скоуп задачи (исполнение — на Фазе R вместе с README 2.2 или отдельной сессией до неё):**
1. Вернуть определение в ОБЕ половины README (EN + RU зеркально) ДОСЛОВНО — это текст
   владельца: не редактировать, не «улучшать», не переврапывать (первоисточник владельца
   неприкосновенен — git-гигиена AGENT_GUIDE).
2. Место — открывающий блок «что это» каждой половины; жанр руководства 2.1 (решения №30/№31)
   сохраняется — определение встраивается в скелет, не ломая его; точную вёрстку выбирает
   исполнитель по текущей витрине.
3. После правки: `node tools/readme-pdf.mjs` (PDF-зеркало) + команды реестра пар + греп-класс
   EXP-0018 (`^[+*] ` после любого врапа).
4. Приёмка: грепы `strategic-operational` (EN-половина) и `стратегическо-операционный`
   (RU-половина) по README.md непусты, найденные строки побайтово совпадают с каноном выше;
   README.pdf перегенерирован.

## 9. Фаза R — релиз 2.2 (скелет; операционный план — на закрытии эпиков)

- [ ] Все эпики K–P и задачи T1–T9 закрыты; ответы интервью 007 вшиты; беклог-ревизия.
- [ ] TEMPLATE_NOTES['2.2'] — **ДОПОЛНИТЬ существующий массив** (заведён задачей T4 вместе с нотой
      стилометрии, 2026-08-08) машинной методичкой слияния (rename-карта P · reports/ и bugs/KAIF ·
      REQUIREMENTS_FRAMEWORK · инварианты контура · шапки-мета) — по прецедентам 2.0/2.1; сюда же
      замороженная строка кодового имени 2.2. **Не пересоздавать ключ** — молча перезапишет ноту T4,
      и ассерт `s04` «нота 2.2 доехала до задания» покраснеет.
- [ ] Линт бандла (инвариант §9.7): пары «документ ↔ ведущий навык», нормы нот vs шаблоны.
- [ ] Судейский проход по всему диапазону 2.2 (инкрементальные судьи по эпикам уже пройдены).
- [ ] Кодовое имя — слово владельца с артефактом источника (Q1 интервью 007); заголовок
      `KAIF 2.2 — <имя>`; публикация по /release после явного «да».
- [ ] Закрытие issue #2 через `gh` (гейт K, но исполняется на релизе: тикет закрывается, когда
      исправления ОТГРУЖЕНЫ).
- [ ] Пост-релизный смок с дефолтного канала (прецедент 2.1).

## Решения, принятые агентом без владельца

*Заполняется при закрытии плана. Текущие (планирование):*
1. Нарезка 6 эпиков + 7 задач и рекомендуемый порядок — рекомендация, вынесена владельцу
   (интервью 007 Q6).
2. Пункт 1.11 → эпик M, пункт 12 → ревизия /code-revision, пункт 17 → задача при формально
   тяжёлом тесте — обоснования в researches/15 (§1, §8, «Решения»).
3. Конвенция T2 применена досрочно к планам этой же сессии (dogfooding; старые планы не
   переименовываются).
4. Детализация: ближайшая фаза каждого эпика — детально, дальние — скелетами (канон лестницы);
   трактовка преамбулы идеи 20 вынесена владельцу (Q12) — ответ «А» получен 2026-08-07
   (решение №43): канон N+1 подтверждён.
5. Рабочие имена до ответов 007: `/go` (T1), `AUTHOR_STYLOMETRY.md` (T4), «интерактивный контур»
   (K) — все финальные имена только от владельца; ответами 007 все три утверждены
   (№36, №39, №33).
6. Пакет планирования уехал в push вместе с дословным коммитом ответов владельца (build 93)
   ДО вердикта судьи — вынужденно: фиксация первоисточника первична (чек-лист, шаг 15), а
   `commit.mjs` коммитит всё дерево. Разовое отступление от решения №13 названо; все находки
   судьи (5 подтверждённых) закрыты следующим же коммитом.
