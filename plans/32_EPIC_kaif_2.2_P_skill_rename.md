# План 32 — ЭПИК P: переименование навыков в `kaif-*` — мета-план (2.2 → исполняется в 2.3)

> **Создан:** 2026-08-07. **Родитель:** `plans/26_kaif_2.2.md` §1 (зонтик 2.2); с 2.3 —
> `plans/70_EPIC_kaif_2.3.md`, строка эпика P. **Статус:** ♻️ РЕАНИМИРОВАН в составе 2.3
> (2026-08-14 07:18 +03:00, решения №74/№76) — исполняется ПОСЛЕ эпика V; вехи: снят с 2.2
> решением №47 (2026-08-07 09:53 +03:00, коммит `ed93b56`); актуализация 2.3 — вторым блоком
> ниже; ⏸ **2026-08-21: перенесён в 2.4 решением №77** (`ideas/26`) — не исполнять до открытия
> 2.4 владельцем. **Вовне:** payload (`framework/skills/` — публичный контракт имён) + миграция
> машинерией на живом парке.

> ♻️ **АКТУАЛИЗАЦИЯ 2.3 (2026-08-14 07:18 +03:00, решения №74/№76).** Место в порядке — **после
> эпика V** (S → T → U → V → **P** → W, №74): переименовывать стоит только навыки, ПЕРЕЖИВШИЕ
> ревизию V5 «зарабатывает ли место», поэтому вход rename-карты (фаза P1, `plans/40`) — ростер
> навыков ПОСТ-V, не сегодняшний. Что изменилось со времени написания: **(а)** навыков теперь 35
> (был 34 — добавился `kaif-go`, сам уже в схеме №35); **(б)** блокер `bugs/55` снят 2026-08-08
> 18:09 +03:00 (ось имён: `counters-guard` сверяет состав навыков ПОИМЁННО в обеих половинах
> витрины и девяти пакетах — критерий 3 этого плана получил исполнителя); **(в)** развилка Q4
> давно закрыта решением №35 (все навыки KAIF → `kaif-*`, кроме вендоренных `fable-*`; короткие
> имена — триггер-алиасами), раздел «Развилки» ниже — история; **(г)** миграция машинерией (P3)
> целится в обновление **2.2 → 2.3** живого парка (8 проектов на 2.2); **(д)** старт эпика —
> нулёвка по №76: сверить этот план и `plans/40` против пост-V ростера и HEAD, поправить по
> фактам. Остальное тело плана действительно как написано.
> **Research-ступень:** `researches/15` §7.
> **Постановка владельца** — идея 20 п. 6: «переименовать все навыки, введённые KAIF, так,
> чтобы они начинались с приписки "kaif-" для лучшей видимости этих навыков в общем списке
> навыков ИИ агента. Вероятно, затронет многие документы KAIF, в которых на названия навыков
> ссылались».
> **Создан:** 2026-08-07. **Статус:** ⛔ **NOT TO BE DONE в 2.2** (решение №47, слово
> владельца дословно: «пока P помечаем как NOT TO BE DONE - возможно, переедет на версию 2.3.
> облегчаем скоуп 2.2») — эпик ИСКЛЮЧЁН из скоупа 2.2, кандидат на 2.3; порядок исполнения
> 2.2 сужен до L → K → M → N → O. Решения №35 (схема `kaif-*`) и №36 (`kaif-go`) остаются в
> силе — сдвигается только исполнение, вместе с эпиком. План и дочерний `plans/40` сохраняются
> как готовая заготовка на реанимацию.
> До №47 было: мета-план; гейт Q4 СНЯТ (2026-08-07, решение №35: все
> навыки KAIF → `kaif-*` КРОМЕ вендоренных fable-*; короткие старые имена — триггер-алиасы) —
> эпик разблокирован; операционный план ближайшей фазы P1 — дочерний
> `plans/40_epic32_P_phase1_rename_map.md` (конвенция п. 9); P2–P4 — скелеты.
> **Место в порядке релиза:** предпоследним, ПОСЛЕ содержательных эпиков (обоснование —
> `researches/15` §7: один скриптовый проход по готовому дереву 2.2 правит и их тексты).

**Формула эпика:** механическая волна с нетривиальной МИГРАЦИЕЙ. Переименовать в payload —
дёшево; сделать так, чтобы обновление 2.1→2.2 на живом парке переименовало навыки НА МЕСТЕ,
уважая локализованные тела и владельческие правки, и не оставило «двухголовый» парк из
34 старых + 34 новых навыков — вот настоящая работа (класс SEMANTIC REPLACE из полевых отчётов L, ожог
`/pause` в KLAS/KCam — тот же механизм).

---

## Вектор цели

Все навыки KAIF видны в общем списке навыков агента единым префиксом по утверждённой владельцем
схеме; ни одна ссылка в документах/зеркалах/пакетах не протухла; обновление существующего
проекта переименовывает навыки на месте без дубликатов и потерь локализации.

## Критерии приёмки эпика (наблюдаемые)

1. Схема имён утверждена владельцем (Q4) и зафиксирована rename-картой «старое → новое» в
   бандле (машиночитаемо).
2. Payload: директории `framework/skills/*`, все взаимные ссылки, README обеих половин,
   таблицы, канон-документы, TEMPLATE_NOTES, help-kaif, KAIF_REFERENCE — на новых именах;
   греп старых имён по payload = 0 (кроме rename-карты и заметок миграции).
3. Триггер-алиасы 9 языковых пакетов согласованы со схемой (реестр пар: состав навыков ↔ ключи
   пакетов — зелёный).
4. Полигон-свод миграции: обновление 2.1→2.2 на профилях (EN-чистый · переведённый ·
   локализованный с владельческими правками навыков) — навыки переименованы НА МЕСТЕ, дубликатов
   нет, локализованные тела и правки владельца сохранены байт-в-байт (сверка содержимого), детектор
   двухголовости (`>1 H1`) чист.
5. Анонимный профиль: rename-миграция работает без origin (карта едет в бандле).
6. Наша обвязка `.claude/skills/` переименована тем же механизмом (dogfooding); вендоренные
   fable-навыки — по решению Q4 (атрибуция апстрима не ломается ни при каком ответе).
7. Реестр пар «истина↔зеркало» прогнан; сборка/полигон целиком зелёные; судья эпика.

## Фазы

### P1. Схема имён и rename-карта — операционный план: `plans/40_epic32_P_phase1_rename_map.md`

Архитектурно: инвентарь всех навыков с классификацией (lifecycle уже-`kaif-*` · вендоренные
fable-* без изменений · остальные → `kaif-<имя>`) → машиночитаемая rename-карта в поставку
(старое → новое → класс миграции, вкл. rename+semantic для тел, меняемых эпиками K/M) →
алиасный слой (старые имена — триггеры; 9 языковых пакетов той же картой).
**Гейт закрытия:** карта покрывает все навыки (счёт = счёту сборки); судья: ни одно имя не
изобретено сверх схемы №35.

### P2. Payload rename (скелет)

Скриптовый проход по `framework/` (директории + все ссылки; текст правится инструментом с
чтением записанного обратно — класс «текст через CLI» не воспроизводить), пересборка, README
обеих половин, зеркала `.claude/skills/`, диаграммы (build-diagrams счётчики), PDF.

### P3. Миграция машинерией (скелет)

KAIF-CORE: обработка rename-карты в update (перенос файла со старого имени на новое С СОХРАНЕНИЕМ
локального содержимого и провенанса; шаблонная часть обновляется обычным правом template-sha;
класс задания «SKILL RENAME» — расширение SEMANTIC REPLACE из эпика L); полигон-своды по
критерию 4–5; TEMPLATE_NOTES['2.2'] — нота миграции.

### P4. Сведение и судья (скелет)

Греп старых имён по всему дереву (0 вне карты/нот); реестр пар; беглый прогон всех навыков по
триггерам (алиасы срабатывают); судья эпика.

## Развилки владельцу (интервью 007)

Q4 — схема: (А) все невендоренные навыки → `kaif-*`, fable-* остаются как есть (атрибуция);
(Б) вообще все, включая `kaif-fable-method`; (В) только «незаметные» служебные, витринные
(/resume, /pause) остаются короткими. Плюс судьба коротких алиасов. Рекомендация агента — А с
сохранением коротких имён как алиасов (привычки владельца в 8 проектах не ломаются).

## Вход из эпика L (дефер фазы L5, 2026-08-07)

- **SEMANTIC REPLACE как класс задания обновления** (смена СМЫСЛА навыка при том же имени —
  KLAS/KCam-ожог `/pause`; скелет L5 родителя `plans/28`). Причина дефера: класс проявляется
  ровно при массовой смене семантики/имён навыков — это ткань эпика P (rename-карта в бандле
  уже спроектирована от этого класса); пункт задания «навык X сменил смысл — перечитай, не
  мержи вслепую» строится там же, где rename-карта, одним механизмом.
  *(2026-08-07, после №47: эпик P исключён из 2.2 — дефер едет ВМЕСТЕ с эпиком (кандидат
  2.3); класс остаётся записанным долгом здесь, адрес не потерян.)*

## Решения, принятые агентом без владельца

*Заполняется при закрытии. Текущие (планирование):* место эпика в порядке (предпоследним) —
рекомендация §10 researches/15; форма миграции через rename-карту в бандле (не «удалить+создать»)
— из полевого класса SEMANTIC REPLACE; сами имена НЕ предлагаются вне схемы Q4 (идентичность —
владельцу).
