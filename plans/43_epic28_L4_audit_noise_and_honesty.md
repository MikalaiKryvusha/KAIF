# План 43 — дочерний операционный план: эпик 28 (L), фаза L4 — шум и честность аудита

> **Родитель:** `plans/28_EPIC_kaif_2.2_L_update_field_fixes.md` → фаза L4 (якорь: «### L4.
> Шум и честность аудита»). **Создан:** 2026-08-07, на закрытии фазы L3 (канон N+1,
> решение №43) — с её уроками. **Статус:** готов к исполнению В НОВОМ ЧАТЕ (решение владельца
> 2026-08-07: L3 закрывает чат, L4 стартует свежим контекстом). Интервью не блокируют.
> **Баг-доки фазы:** `bugs/35` (L-5: stale-claims — точность 19 %) · `bugs/36` (L-6:
> переведённые красны по построению в аудите) · `bugs/37` (L-7: обещания ≠ код —
> warning-guard STATUS) · `bugs/38` (L-8: рассинхроны бандла — линт).

## Вектор цели

Предупреждающие сканеры перестают быть шумом: stale-claims попадает ≥ 50 % (сейчас ≈19 % —
критерий эпика №6, исчислимый), аудит `modules`/`update-verify` на переведённом развёртывании
не красен по построению («локализован ≠ absent» — критерий №7), каждое обещание релиза
существует как код (warning-guard длины STATUS — одна строка в `check`), и линт бандла перед
релизом ловит рассинхроны «документ ↔ ведущий его навык» у АВТОРА, а не у полевого агента
(критерий №8).

## Готово, когда (критерии приёмки — родительские №6–8 дословно)

1. `stale-claims`: точность на полевых фикстурах (образцы из 4 отчётов) — доля полезных
   срабатываний ≥ 50 %; цитаты владельца и журналы решений не трогаются — критерий №6.
2. `update-verify`/аудит `modules` на переведённом развёртывании не красный по построению
   (отдельный режим или честная классификация «локализован ≠ absent») — критерий №7.
3. Warning-guard длины STATUS СУЩЕСТВУЕТ (одна строка в `check`; «обещания релиза = код») —
   часть критерия №8.
4. Линт бандла перед релизом существует и зелёный: пары «документ ↔ ведущий его навык»,
   нормы релизных нот против шаблонов (класс «обещанный guard не существует» закрыт) —
   критерий №8.
5. Свод фазы (s10) — каждый страж красный до фикса с предсказанием (EXP-0016/0017);
   полигон 10/10; `node tools/build-framework.mjs` чистый.

## Шаги

- [ ] 1. **Разведка кода:** `scanStaleClaims` (скоуп версии, исключения журналов/цитат/зеркал,
      кап по файлам — что уже есть после K5 2.1, чего не хватает по полевым фикстурам);
      `moduleAudit` (печать «MODULE ABSENT» на переведённых — bugs/36); `check` (куда одна
      строка warning-guard STATUS ~200 строк — обещание релиза 2.1, эпик H); сборщик
      (куда линт бандла: check-framework или отдельный шаг).
- [ ] 2. **Полевые фикстуры точности stale-claims:** выборка строк из 4 отчётов
      (`ideas/ai_agents_reports/2.1_update_reports/` — образцы полезных и ложных
      срабатываний, замер ДО фикса числом) + красные стражи на классы ложных: цитаты
      владельца · журналы решений · зеркала · летопись до миграции. Точность считает СКРИПТ.
- [ ] 3. **Фиксы bugs/35:** скоуп — версия ФРЕЙМВОРКА (не проекта); пропуск цитат/журналов/
      зеркал; миграция летописи ДО скана; кап по файлам; замер точности ≥ 50 % на фикстурах.
- [ ] 4. **Фикс bugs/36:** аудит `modules`/`update-verify` при `i18n: translated` — «absent»
      переведённого модуля классифицируется «localized», не печатается дефектом; счётчики
      сводки честные.
- [ ] 5. **Фикс bugs/37:** warning-guard длины STATUS в `check` (мягкий ориентир ~200 строк,
      предупреждение — решение №27); свод-инвентарь «обещание релиза → строка кода» для
      остальных обещаний 2.1 (что найдено мёртвым — фикс или честное снятие обещания).
- [ ] 6. **Фикс bugs/38:** линт бандла у автора — пары «ключевой документ ↔ ведущий его
      навык» (14-й документ REQUIREMENTS приедет эпиком N — линт готовится к нему), сверка
      релизных нот с шаблонами; место — `tools/check-framework.mjs` (по образцу гарда 5d).
- [ ] 7. **Верификация:** полигон целиком (10 сводов) · чек-лист регрессии родителя ·
      пересборка без дрейфа · `git diff --stat` построчно.
- [ ] 8. **Закрытие:** баг-доки 35–38 → DONE с секциями решений · отметка L4 в родителе ·
      судья фазы · оп-план L5 (`plans/44`, канон N+1) — стартовая точка: bugs/40/41/42 +
      грань Q13 (bugs/44 — языковые пакеты; **Q13 отвечен «А» 2026-08-07, решение №44** —
      честно неполные + страж протухания) + грань Q14 из bugs/34_DONE (**отвечен «А»,
      решение №45** — parseMarks учится заголовочной форме) + хвосты скелета L5
      (SEMANTIC REPLACE · backup · метка маршрута · .gitattributes · инвентарь скиллов) ·
      **завершение чата навыком `/end-chat`** (STATUS/летопись, реестр пар, коммит+пуш,
      эстафета; AUTH владельца дословно, чат 2026-08-07: «мой AUTH: да, даю добро»).

## Верификация

Критерий 1 — число (скрипт замера точности на полевых фикстурах, до/после); критерии 2–4 —
красные-до/зелёные-после стражи; критерий 5 — `npm run test:core`.

## Риски (с уроками L3)

1. **Точность stale-claims — метрика на малой выборке** → фикстуры из ВСЕХ 4 отчётов, не
   одного; порог 50 % — родительский критерий, не самодеятельность.
2. **Ложные красные нового линта бандла у автора** → линт доказывается красным на
   синтетически сломанной паре ДО включения в сборку (EXP-0017).
3. **Цитаты полевых проектов в деплоящихся текстах** → только обезличенные ссылки («KCam»,
   «field report») — anonLeakScan сканирует tool-модули сырыми (урок L3, регрессия s04/S13).
4. **Правка не-ASCII файлов — ТОЛЬКО файловыми инструментами** (Write/Edit): шелл-конвейер
   PS5.1 `Get-Content -Raw | -replace | Set-Content` читает UTF-8-без-BOM как ANSI и молча
   перекодирует файл в моджибейк (инцидент L3 с plans/42, восстановлен из git; класс —
   AGENT_GUIDE «Гигиена документов и текста», симптом 1 в READ-варианте).
5. **Чужие своды стерегут семантику** (EXP-0022) → перед изменением формата вывода аудита —
   полный полигон, падение читать как решение.

## Решения, принятые агентом без владельца

*Заполняется при закрытии.*
