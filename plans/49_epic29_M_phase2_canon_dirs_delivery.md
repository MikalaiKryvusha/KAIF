# План 49 — дочерний операционный план: эпик 29 (M), фаза M2 — канон-директории и доставка

> **Родитель:** `plans/29_EPIC_kaif_2.2_M_feedback_loop.md` → фаза M2 (якорь: «### M2.
> Канон-директории и доставка»). **Создан:** 2026-08-07 14:05 +03:00, на закрытии M1 (канон N+1).
> **Вовне:** —. **Статус:** готов к исполнению. **Формы фазы** — из `researches/18` (§6 классификатор, §8
> шаблоны); решение №41 (Q10): миграция `ideas/ai_agents_reports/` → `reports/` — исполняется
> здесь.

## Вектор цели

Канон-директория `reports/` (+ `reports/KAIF_UPDATES`, `reports/KAIF_AUDIT`) и поддиректория
`bugs/KAIF` существуют как ЧАСТЬ ПОСТАВКИ — README-шаблоны, полный круг регистрации доставки,
полигон-покрытие; наш репозиторий мигрирует свои отчёты первым потребителем (№41), так что фаза
M3 вшивает протоколы в навыки поверх уже существующих директорий.

## Готово, когда (критерии приёмки)

1. `framework/readmes/reports.md` существует (EN, по канону остальных README директорий):
   назначение · жанровый контракт отчётов (секции шаблонов C/D одной строкой каждая, ссылка-якорь
   на полные формы — без дублирования тел) · поддиректории `KAIF_UPDATES` (update+install отчёты —
   решение агента M1, закрепляется здесь) и `KAIF_AUDIT` (потребитель T3) · правило «строго EN».
2. Шаблон `framework/readmes/bugs.md` несёт заметку о поддиректории `bugs/KAIF` (сигналы о САМОМ
   фреймворке; режимы 8.1/8.2 одной строкой, ссылка на протокол — навык появится в M3).
3. Полный круг регистрации доставки (EXP-0003, инвариант §9.6 зонтика): строка в карте readmes
   `tools/build-framework.mjs` · пересборка (бандл/манифест/карта модулей — сами) · перечни
   директорий знаний в шаблонах payload (`AGENT_GUIDE`, `CLAUDE.md`-шаблон, `KAIF_REFERENCE`,
   `PROJECT_STRUCTURE_EXTERNAL_MAP`, `_intro` при упоминании числа README) · таблицы README.md
   (обе половины) · `/help-kaif`. Греп «reports» по точкам — ни одна не забыта.
4. Счётчики после пересборки обновлены ИЗ ВЫВОДА сборки (блоков станет больше на 1 README):
   строка реестра пар в `AGENT_GUIDE.md` («= N; бандл …; карта — …»), без ручной арифметики
   (EXP-0025; класс bugs/09).
5. Полигон: свежая установка создаёт `reports/` с README (+ существование заметки bugs/KAIF);
   обновление ДОНОСИТ директорию существующему развёртыванию; красный доказан мутацией (свод без
   README падает). Свод — расширение существующего профиля или новый s13 — по канону «свод в тот
   же шаг» (EXP-0016).
6. Dogfooding-миграция (№41): `git mv ideas/ai_agents_reports → reports/` с раскладкой по жанрам
   (4 отчёта 2.1 → `reports/KAIF_UPDATES/`; остальные полевые отчёты — корень `reports/`;
   история git сохранена); все внутренние ссылки на старый путь выправлены грепом (класс
   EXP-0020 — протухшие перекрёстные ссылки), 0 остатков `ideas/ai_agents_reports` в живых
   документах (кроме летописи/архивов — там законно).
7. Сборка чистая; полигон целиком зелёный; лёгкий судья фазы (№48); закрытие в родителе с датой
   и временем (№49); план M3 написан по урокам M2 (канон N+1).

## Шаги

- [x] 1. `framework/readmes/reports.md` (критерий 1) + заметка в `framework/readmes/bugs.md`
      (критерий 2); пересборка. ✅ + ресинк 5f: 9× KAIF_FRAMEWORK (строка путей) и 9× bugs/README
      (заметка переведена на все языки пакетов) + пины перенесены тем же коммитом.
- [x] 2. Круг регистрации (критерий 3): карта readmes сборки → греп «reports» по перечням
      директорий во всех шаблонах payload и витринах (AGENT_GUIDE-шаблон, CLAUDE.md-шаблон,
      KAIF_REFERENCE, EXTERNAL_MAP-шаблон, README EN+RU, /help-kaif) → правки → пересборка;
      счётчики — цитатой вывода сборки (критерий 4). ✅ 10 поверхностей payload (вкл. kaif-remove
      ×2 и SKIP_DIRS сканера stale-claims в ядре; CONTEXT_POINTER директорий не перечисляет —
      закрыт по построению) + корневая обвязка; счётчики: «55 embedded files (13 docs + 7 readmes
      + 34 skills + 1 tools) · bundle 148 blocks · 651 modules»; попутно протухший «тридцать один
      навык» → «тридцать четыре» (класс bugs/09).
- [x] 3. Полигон-покрытие (критерий 5): установка создаёт / обновление доносит; мутация красным
      ДО фикса свода. ✅ красным-вперёд: 3 ассерта (s01/S1 ×2 — README существует и называет
      KAIF_UPDATES+KAIF_AUDIT; s02/S5 — update доносит после сноса) красные на до-M2 dist,
      зелёные после; полигон целиком «all 12 suites green».
- [x] 4. Миграция №41 (критерий 6): git mv + раскладка по жанрам + греп-выправка ссылок.
      ✅ 5 отчётов жизненного цикла → `reports/KAIF_UPDATES/` (4× 2.1 + KAIF_2.0), 11 полевых →
      `reports/`; wrapper `reports/README.md` (RU, с оговоркой истока) + заметка bugs/KAIF в
      wrapper `bugs/README.md`; 110 вхождений ссылок выправлено механически (STATUS ·
      MASTER_PLAN №23 · researches/13/15/16/18 · EXPERIENCE), решенческие строки (№41, Q10)
      нетронуты; реестр цитат researches/16 переповерен: «Цитат проверено: 189; промахов: 0»
      (заодно закрыт до-M2 дрейф цитаты I7 — K2 исполнил замену буквы, отмечено).
- [x] 5. Верификация фазы: сборка + полигон целиком + судья лёгкий; закрытие в родителе
      (дата+время); план M3 (`plans/50`) по урокам M2. ✅ см. секцию закрытия.

## ✅ Закрытие фазы M2 (2026-08-07 14:31 +03:00)

Все 7 критериев закрыты наблюдением (пометки ✅ по шагам выше; каждый — с числом из вывода
инструмента). **Лёгкий судья фазы (№48): VERIFIED** — сборка «check-framework OK — 55 embedded
files»; полигон «all 12 suites green» с red-first доказательством трёх новых стражей; реестр
пар «навыки 34, таблицы 68 — ok»; цитаты реестра 189/0; CJK-скан правленых русских файлов чист;
бренд-скан readmes пуст. Коммиты: поставка `7a74b96`, миграция — следующий.

## Решения, принятые агентом без владельца

1. Ресинк пакетов вместо сознательного переноса пина без синка (гард 5f давал обе двери) —
   выбран честный ресинк: заметка короткая, дрейф пакетов — ровно полевой класс bugs/44.
2. Раскладка миграции: 5 lifecycle-отчётов (вкл. KAIF_2.0_UPDATE_REPORT) → `KAIF_UPDATES/`,
   остальные 11 — корень `reports/`; поддиректории KAIF_UPDATES/KAIF_AUDIT рождаются первым
   отчётом (git пустых директорий не хранит; KAIF_AUDIT появится с первым аудитом T3).
3. Правки ссылок — только операционные указатели; решенческие строки и записи о самой миграции
   оставлены со старым путём (это их содержание). Летопись/архивы/оригиналы владельца нетронуты.
4. Оговорки истока в wrapper README (reports/ и bugs/): этот репозиторий — сам разработчик KAIF,
   дефекты фреймворка живут в `bugs/` без поддиректории; в `reports/KAIF_UPDATES/` лежат отчёты
   ПАРКА. — ✅ **ПОДТВЕРЖДЕНО ВЛАДЕЛЬЦЕМ** 2026-08-09 13:45 +03:00 (интервью №017 Q3 = A,
   решение №68): оговорки остаются как есть.

## Верификация

Каждый критерий — наблюдением: README существует и встроен (вывод сборки — число блоков);
греп-круг регистрации пуст от пропусков; полигон-свод красный на мутации и зелёный на целом;
`git log --follow` показывает историю мигрированных отчётов.

## Риски

1. **Протухание счётчиков** (класс bugs/09): блоков станет 55 — все зеркала чисел брать ТОЛЬКО из
   вывода сборки; реестр пар в `AGENT_GUIDE.md` — тем же коммитом.
2. **Ссылки на старый путь** (класс EXP-0020): `ideas/ai_agents_reports` упоминается в STATUS,
   планах, исследованиях — финальный греп по репо (вне летописи и dist/) обязателен.
3. **Пустые директории git не хранит**: `reports/KAIF_UPDATES`/`KAIF_AUDIT` в поставке должны
   рождаться README-ками или создаваться установщиком — решить при исполнении, не оставлять
   молча.
4. **Дубль шаблонов** (DRY): README ссылается на формы `researches/18` §8 / будущие носители M3,
   не копирует тела — иначе три копии разойдутся.
