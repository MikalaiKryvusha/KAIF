# План 51 — дочерний операционный план: эпик 29 (M), фазы M4+M5 — соглашение о принципах + dogfooding и закрытие эпика

> **Родитель:** `plans/29_EPIC_kaif_2.2_M_feedback_loop.md` → фазы M4 (якорь: «### M4. Соглашение
> о принципах») и M5 (якорь: «### M5. Dogfooding, полигон, судья»). **Создан:** 2026-08-07 15:09
> +03:00, на закрытии M3 (канон N+1; решение агента при закрытии `plans/50`: две лёгкие финальные
> фазы — одним дочерним планом, церемония не перевешивает работу; развилка «возможно одним
> планом» заложена критерием 6 плана 50). **Статус:** готов к исполнению.
> Формы — `researches/18`: шаблон B как канал предложений (§6 строка «Предложение принципа»);
> Гудхарт-предохранитель §7.

## Вектор цели

Канон даёт агентам явное распоряжение привносить проверенные боем методологии (и рекомендовать
отказ от неработающих) с каналом через контур M; этот репозиторий подтверждён первым
потребителем контура; эпик M закрыт лёгким судьёй со сверкой критериев приёмки 1–8 родителя.

## Готово, когда (критерии приёмки)

1. **M4:** распоряжение о принципах — в payload (место — по смыслу: PHILOSOPHY или AGENT_GUIDE;
   слово владельца, идея 20 п. 14: методики, «которые РЕАЛЬНО ПРОТЕСТИРОВАНЫ В МИРЕ БОЕМ и
   ПРОДАКШЕН применениями» — в RU-обвязке дословно, в EN — переводом): канал = шаблон B
   (`/report-bug`, поле «где проверено боем»), судьба предложений — решение владельца KAIF;
   blameless-рамка (провал слабой модели = недостающий гвардрейл). RU-зеркала обвязки.
2. **M5, dogfooding:** этот репозиторий — первый потребитель контура: заметки истока в
   `reports/README` и `bugs/README` (с M2) сверены с фактическим потоком; вход сигналов парка
   (issues origin + отчёты `reports/KAIF_UPDATES/`) назван в беклог-ритуале (`/check-backlog`
   или README — по месту, без новой сущности).
3. **M5, эвал-ловушка дедупа:** симулированный дефект с существующим fingerprint → процедура
   дедупа находит дубликат (проверка — по тяжести: ассерт в существующем своде s03/s04 или
   скриптовый прогон с записью результата; НЕ новый тяжёлый свод — Оккам). Анонимные ассерты
   s04 (аноним не тянется к origin) — уже стоят с M3, сверить и процитировать.
4. **Судья эпика M лёгкий (№48)** + сверка критериев приёмки 1–8 родителя таблицей
   «критерий → улика» + сверка «не Гудхарт» (метрика петли — «волны владельца мельчают», без
   KPI на число тикетов).
5. Закрытие M4, M5 и ЭПИКА M в родителе `plans/29` и зонтике `plans/26` (даты+время, №49);
   секции решений агента; план ближайшей фазы эпика N по канону N+1; STATUS.

## Шаги

- [x] 1. M4: канон-строки в payload + RU-зеркала; пересборка; гард 5d (кириллицы в EN-телах нет).
      ✅ PHILOSOPHY: принцип «The principle set is battle-tested, not sacred — propose and prune»
      (пара к «Best practices»); AGENT_GUIDE: распоряжение «Proposing principles — a standing
      order» рядом с контуром (канал — шаблон B, судьба — решение владельца, blameless-рамка);
      RU-зеркала с цитатами владельца п. 14 ДОСЛОВНО; сборка «55 … 148 blocks … 655 modules»
      (карта +1 модуль-принцип; счётчики-зеркала обновлены цитатой), check (вкл. гард 5d) зелёный.
- [x] 2. M5: эвал-ловушка дедупа (форма — по критерию 3); сверка dogfooding-заметок истока и
      входа сигналов парка.
      ✅ ловушка — 2 ассерта в s04/S13 (аноним = режим 8.2, дедуп строго локальный):
      симулированный тикет с fingerprint → существующий класс НАЙДЕН по surface+class, чужой
      класс НЕ матчится (негативный контроль — ловушка умеет краснеть по построению); оба
      зелёные живым прогоном. Dogfooding: заметки истока в `reports/README` и `bugs/README`
      сверены (стоят с M2/M3); вход сигналов парка (issues origin + `reports/KAIF_UPDATES/`)
      дописан строкой в обвязочный `/check-backlog` (шаг 1) — по месту, без новой сущности.
- [x] 3. Таблица «критерий приёмки эпика (1–8) → улика» по родителю `plans/29` — см. закрытие.
- [x] 4. Судья эпика лёгкий; закрытие M4/M5 и эпика M (родитель + зонтик, №49); STATUS; план
      ближайшей фазы эпика N (канон N+1).
      ✅ судья: VERIFIED (ниже); эпик N — дочерний план N1 уже существует (`plans/38`, готов к
      исполнению, Q9/№40 отвечены) — по канону N+1 перед стартом сверить свежесть, новый план
      не пишется (решение агента №3 ниже).

---

## ✅ Закрытие фаз M4+M5 и ЭПИКА M — 2026-08-07 15:25 +03:00

### Таблица «критерий приёмки эпика (plans/29, 1–8) → улика»

| # | Критерий (кратко) | Улика (команда → результат, наблюдено 2026-08-07) |
|---|---|---|
| 1 | `reports/` в поставке + круг регистрации + свод установки | M2: сборка печатает «7 readmes»; `grep -c 'FILE: \`reports/README.md\`' dist/KAIF-CORE-BUNDLE.md` → 1; s01/S1 ×2 + s02/S5 («update доносит») зелёные |
| 2 | Протокол баг/запрос KAIF, режимы 8.1/8.2 | `grep -c 'gh issue list' dist/KAIF-FULL.md` → 2 (разведка 8.1); «never reach for the origin» ×2 (граница 8.2); аноним живьём — s04/S13 (пункт без origin + локальный дедуп). Оговорка: 8.1 живьём (gh/сеть) полигоном не гоняется — покрыт текстом канона и локальной половиной; полный суд — Фаза R |
| 3 | Гейт качества отправителя + автозахват | `grep -o 'Sender quality gate' dist/KAIF-FULL.md` → есть; `grep -c 'Autocapture' dist/KAIF-FULL.md` → 2 (шаблоны A и B) |
| 4 | Контур 5 шагов в payload AGENT_GUIDE + сшивки | «A defect in KAIF ITSELF — the five-step contour» в dist/KAIF-FULL.md; сшивка K — «through the project's send gate»; сшивка /kaif-update — шаг 5 навыка; до M3 греп по payload — 0 совпадений (проверено в M3) |
| 5 | Update И install финишируют обязательным отчётом; полигонные стражи | M3: 8 ассертов s02/s03/s04 (красные ДО → зелёные ПОСЛЕ); чекпоинт без файла — отказ живым прогоном |
| 6 | Соглашение о принципах (п. 14) | «The principle set is battle-tested, not sacred» + «Proposing principles — a standing order» в dist/KAIF-FULL.md (M4, этот план) |
| 7 | Вектор целей + допустимая ошибка документом; вектор ошибки полями | `researches/18` §2 (9 инвариантов) + §4 (режимы ошибки); поля «Expected per canon» / «Got in the field» в dist/KAIF-FULL.md (шаблон A) |
| 8 | Dogfooding: исток — первый потребитель | миграция №41 исполнена (M2, git mv): `ls reports/KAIF_UPDATES/ | wc -l` → 5; корневых отчётов 12; ретро-прогон шаблона C на отчёте NDim — `researches/18` прил. B; вход парка — в `/check-backlog` (этот план) |

**Сверка «не Гудхарт»:** KPI на число тикетов/закрытий нигде не введён (греп канона M — только
метрика «волны владельца мельчают, классы не повторяются», `researches/18` §7); эвал-ловушка
меряет НАХОДИМОСТЬ дубликата, не количество сигналов. ✅

**Судья эпика M лёгкий (№48): VERIFIED** — заявленные верификации фаз переисполнены в этой
сессии: сборка чистая (655 модулей цитатой), полигон целиком «all 12 suites green» (после M4),
s04 с ловушкой дедупа — зелёный отдельным прогоном; таблица критериев 1–8 — каждая улика
наблюдена командой. Полный адверсарный суд всего 2.2 — один раз в Фазе R (№48).

## Решения, принятые агентом без владельца

1. **Размещение M4-строк:** принцип — в PHILOSOPHY (сосед «Best practices»: тот же класс
   «метод раньше изобретения»), операционное распоряжение с каналом — в AGENT_GUIDE рядом с
   контуром M (родительский скелет называл оба документа — исполнено обоими).
2. **Форма эвал-ловушки — ассерты в существующем s04/S13** (не новый свод и не разовый прогон):
   дедуп в анонимном профиле строго локален (8.2), фикстура самодостаточна, негативный контроль
   даёт ловушке способность краснеть по построению; семантика грепа surface+class реализована
   node-сканом (полигон не зависит от наличия POSIX-grep на машине).
3. **План эпика N не пишется заново:** дочерний `plans/38` (N1) уже существует и свеж (создан
   при планировании 2.2, единственная развилка Q9 отвечена — №40); канон N+1 исполняется
   сверкой свежести на старте N1, как это делал эпик M со своим `plans/37`.
4. **Вход сигналов парка** дописан в ОБВЯЗОЧНЫЙ `/check-backlog` (оговорка истока), payload
   навыка не тронут: полевые проекты — производители сигналов, коллектор — только исток.
5. **Оговорка к критерию 2:** режим 8.1 (gh/сеть) полигоном живьём не гоняется — сеть вне
   песочницы; отмечено в таблице явно, добор — полевой прогон/Фаза R.

PENDING-строк нет.

## Верификация

Наблюдением: греп канон-строк в dist; полигон целиком + сборка после любой правки payload;
эвал-ловушка — исполненным прогоном с процитированным выводом; таблица критериев эпика — с
уликой-командой на строку.

## Риски (уроки M1–M3)

1. **Тяжёлое судейство** — №48 держит лёгкое: полный адверсарный суд всего 2.2 один раз в
   Фазе R; здесь — переисполнение заявленных верификаций.
2. **Письмо в payload** — EN-тела без кириллицы (гард 5d); цитата владельца дословно живёт в
   RU-обвязке, EN несёт перевод с указанием источника (класс EXP-0023: без авторских токенов).
3. **Эвал-ловушка** — не плодить постоянный тяжёлый свод ради одного класса: дедуп — это греп
   по fingerprint, проверка может жить одним ассертом или разовым записанным прогоном.
4. **CJK-подмены** в машинном русском (EXP-0030) — скан правленых RU-файлов перед коммитом.
