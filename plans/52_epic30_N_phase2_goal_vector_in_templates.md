# План 52 — дочерний операционный план: эпик 30 (N), фаза N2 — вектор цели в шаблонах и навыках

> **Родитель:** `plans/30_EPIC_kaif_2.2_N_canon_requirements.md` → фаза N2 (якорь: «### N2. Вектор
> цели + критерии приёмки — в шаблоны и навыки (скелет)»). **Создан:** 2026-08-07 (на закрытии N1,
> канон N+1). **Статус:** ✅ ЗАКРЫТ 2026-08-07 16:55 +03:00 (все 5 шагов; судья лёгкий №48 —
> VERIFIED; секция закрытия внизу).

## Вектор цели

Каждый целевой документ, который агент заводит по шаблонам KAIF (план, эпик, баг, идея),
ОТКРЫВАЕТСЯ вектором цели и критериями приёмки — блок стоит ПЕРВЫМ в шаблонах навыков
планирования и README директорий, с оговоркой модифицируемости; написание блока направляет
`REQUIREMENTS_FRAMEWORK.md` (доставлен фазой N1).

## Готово, когда (критерии приёмки)

1. `/plan-task` (EN шаблон + RU обвязка): план открывается блоком «Вектор цели» + «Критерии
   приёмки» (существующее «Готово, когда» усилено/переименовано по канону N1); ссылка на
   REQUIREMENTS_FRAMEWORK в точке письма блока.
2. `/plan-epic` (EN+RU): мета-план эпика открывается вектором цели эпика; операционные планы фаз
   наследуют блок.
3. `/report-bug` (EN+RU): шаблон документа бага несёт критерий приёмки фикса (наблюдаемый).
4. `/propose-idea` (EN+RU): шаблон идеи открывается «какую боль решает + как проверим».
5. README `plans/` `bugs/` `ideas/` (payload `framework/readmes/`): конвенция «вектор цели +
   критерии приёмки первыми» — строкой; локализации всех трёх README ресинканы в 9 языковых
   пакетах ТЕМ ЖЕ коммитом + перепин `_lang-pack-source-shas.json` (доктрина 5f; все три файла —
   в пин-списке).
6. Канон-строка в AGENT_GUIDE обеих половин (чек-лист/роутер): «заводишь план/баг/идею → вектор
   цели и критерии приёмки первыми, по REQUIREMENTS_FRAMEWORK»; оговорка модифицируемости
   («меняются по мере работы») — в шаблонах при блоке.
7. Dogfooding-проверка: греп-скан живых планов 2.2 (`plans/26–52`) на блок вектора
   цели/критериев — счёт «открываются/всего» в закрытии фазы (наши планы уже исполняют канон
   досрочно — подтвердить числом).
8. Кандидаты, отложенные из N1 (греп «TESTING без REQUIREMENTS»): `/release` и сфера
   `programming` — решить по месту: нужна ли им ссылка на канон требований (решение с одной
   строкой обоснования в закрытии; линтер-вплетение — скоуп N5, не тянуть сюда).
9. Сборка зелёная (счётчики цитатой вывода); полигон зелёный; судья фазы; закрытие N2 в
   родителе + план N3 по канону N+1.

## Шаги

- [x] 1. `/plan-task` + `/plan-epic` — блок первым (EN шаблоны + RU пары; критерии 1–2).
- [x] 2. `/report-bug` + `/propose-idea` — критерий приёмки фикса / боль+проверка (критерии 3–4).
- [x] 3. README plans/bugs/ideas (payload) + ресинк 27 локализаций + перепин тем же коммитом
      (критерий 5).
- [x] 4. Канон-строка AGENT_GUIDE ×2 + оговорка модифицируемости + dogfooding-греп с числом
      (критерии 6–7) + решения по кандидатам N1 (критерий 8).
- [x] 5. Верификация: пересборка · полигон · судья фазы · закрытие в родителе + план N3
      (критерий 9).

## Верификация

Критерии 1–6 — чтением шаблонов против REQUIREMENTS_FRAMEWORK (блок стоит первым) + пересборкой;
критерий 5 — страж протухания пакетов зелёный в том же коммите; критерий 7 — грепом с числом в
закрытии; сборка/полигон — цитатой вывода (EXP-0025).

## Риски (уроки N1)

1. Кириллица в EN-телах шаблонов → гард 5d красный по построению: примеры в EN-шаблонах только
   латиницей, русские формы — в RU-зеркалах обвязки (грабля N1: EARS-ключи и RU-колонка словаря).
2. Правка `plans/bugs/ideas` README (пин-лист пакетов) без ресинка 9 локализаций → страж
   протухания красный: ресинк + перепин ТЕМ ЖЕ коммитом (доктрина 5f; в N1 перепин потребовался
   дважды).
3. Канон-документ — не летопись: без провенанс-пометок (чаты/номера идей) и без мета-воды
   («пару дополняет до тройки» — замечания владельца в N1); механизм связи, не этикетка.
4. Церемония перевесит работу (шаблоны маленьких документов) → блок в /plan-task для «совсем
   маленькой» задачи остаётся секцией в документе идеи/бага, не отдельным планом (канон
   /plan-task не ломать).

---

## ✅ ЗАКРЫТИЕ ФАЗЫ N2 (2026-08-07 16:55 +03:00)

Все 5 шагов исполнены одной сессией (двенадцатой 2026-08-07). Улики по критериям приёмки:

| Критерий | Улика (команда → наблюдение) |
|---|---|
| 1. /plan-task EN+RU | чтение Step 2/Шаг 2 обоих слоёв: структура открывается `**Goal vector:**`/`**Вектор цели:**` + `**Acceptance criteria (done when):**`/`**Критерии приёмки (готово, когда):**`; после блока — абзац REQUIREMENTS_FRAMEWORK + модифицируемость |
| 2. /plan-epic EN+RU | Rung/Ступень 2 — первый буллет «meta-plan OPENS with the epic's goal vector»/«ОТКРЫВАЕТСЯ вектором цели эпика»; Rung/Ступень 3 — «наследует открывающий блок» |
| 3. /report-bug EN+RU | шаблон бага несёт `**Fix accepted when (observable):**` (греп в обоих слоях) |
| 4. /propose-idea EN+RU | секция `## Goal vector — the pain it solves + how we check` стоит ПЕРЕД `## Essence` в обоих слоях |
| 5. README + 27 локализаций + перепин | греп `REQUIREMENTS_FRAMEWORK` — payload 3/3, пакеты **27/27** (9 языков × 3); пин 3/3 MATCH sha256 EN-истоков (нормализация 5f), перепин ТЕМ ЖЕ коммитом; страж протухания в сборке молчит |
| 6. AGENT_GUIDE ×2 | EN шаг 8: «Filing a plan/bug/idea → goal vector + acceptance criteria FIRST, per REQUIREMENTS_FRAMEWORK.md»; RU шаг 7: «Заводишь план/баг/идею → вектор цели и критерии приёмки ПЕРВЫМИ»; оговорка модифицируемости — в шаблонах при блоке (критерий 1/2) |
| 7. Dogfooding | скан `plans/26–52` (маска 2[6-9]/[3-5][0-9]): секция «Вектор цели» — **27/27**, критерии приёмки — **27/27** (наши планы исполняли канон досрочно — подтверждено числом) |
| 8. Кандидаты N1 | решения ниже (№1, №2) |
| 9. Сборка/полигон/судья | сборка: «56 embedded files (14 docs + 7 readmes + 34 skills + 1 tools) · bundle 150 blocks · 662 modules» (цитата вывода); полигон: «✅ sandbox suite: all 13 suites green» (цитата); судья лёгкий №48: **VERIFIED** — все 10 заявлений переисполнены независимо, 3 адверсарных охоты пусты; попутная красная находка судьи опровергнута как дефект его же команды (git-bash grep -i не кейс-фолдит кириллицу — класс EXP-0026, Unicode-скан нодой авторитетен) |

Попутно (синхрон слоёв, сверх буквы плана): корневые README обвязки `plans/ bugs/ ideas/`
получили ту же конвенцию-строку — слои движутся вместе (AGENT_GUIDE); CJK-скан правленых RU-файлов
чист (EXP-0030); греп EXP-0018 (`^[+*] `) и EXP-0031 (провенанс-шум в каноне) по диффу — чисто;
анонимность payload — в добавленных строках 0 авторских токенов (EXP-0023).

### Решения, принятые агентом без владельца

1. **`/release` — ссылку на REQUIREMENTS_FRAMEWORK НЕ добавлять** (критерий 8): релиз проверяет
   СДЕЛАННОЕ (гейты наблюдения по TESTING) и требований не пишет; ссылка была бы шумом канона.
2. **Сфера `programming` — НЕ добавлять** (критерий 8): сфера адаптирует верификацию домена, а
   письмо требований роутится в REQUIREMENTS_FRAMEWORK строкой роутера AGENT_GUIDE независимо от
   сферы; вплетение линтера — скоуп N5.
3. **Имя блока — «Acceptance criteria (done when)»**: старое «Done when» сохранено скобкой, а не
   удалено — непрерывность словаря для сессий, помнящих старую форму; судья подтвердил: голого
   «Done when» в payload не осталось, конфликтов нет.
4. **Форма критерия фикса в /report-bug — строка шапки** (`**Fix accepted when (observable):**`
   после Version/build), не отдельная `##`-секция: критерий виден с первого экрана и не ломает
   каноническую структуру существующих баг-доков.
5. **В /propose-idea секция «Goal vector» поставлена ПЕРЕД «Essence»** (боль → что предлагается):
   буква критерия 4 «открывается болью» исполнена дословно; Essence остаётся вторым.
6. **Синхрон корневых README обвязки** — сделан тем же заходом без отдельного пункта плана
   (двухслойная доктрина AGENT_GUIDE прямо требует держать слои вместе).
7. **RU-заголовки секций шаблона идеи оставлены EN** («Goal vector — …» в RU-обвязке): шаблонные
   заголовки документов ideas/ в этом репо канонически английские (Essence, Links…) — новая
   секция следует конвенции соседей.

PENDING-строк нет.
