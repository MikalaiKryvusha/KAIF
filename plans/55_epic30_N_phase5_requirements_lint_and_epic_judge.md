# План 55 — дочерний операционный план: эпик 30 (N), фаза N5 — линтер требований в полигон + судья эпика

> **Родитель:** `plans/30_EPIC_kaif_2.2_N_canon_requirements.md` → фаза N5 (якорь: «### N5.
> Линтер требований, полигон, судья (скелет)»). **Создан:** 2026-08-07 (на закрытии N4, канон
> N+1). **Вовне:** —.
> **Статус:** ✅ исполнен целиком — все 5 шагов закрыты 2026-08-07 18:55 +03:00 (взят свежей
> сессией по рекомендации закрытия N4); судья эпика N — VERIFIED; фаза закрыла ЭПИК N.

## Вектор цели

Критерии качества требований работают ИСПОЛНЯЕМЫМ линтером и рубрикой судьи по написанному — не
благими намерениями и не входным шлагбаумом (Achieve; критерий эпика 7 — антипаттерн DoR не
воспроизводится). Эпик N закрывается судьёй с уликами по всем семи критериям приёмки.

## Критерии приёмки (готово, когда)

1. Линтер целевых документов существует: проверяет НАЛИЧИЕ блока «Вектор цели» + «Критерии
   приёмки» в живых целевых документах обвязки (plans/ideas; диалекты bugs/interviews — свои
   поля из N2/N3) и гоняет стоп-словарь `kaif-requirements-lint` по их секциям требований.
   Носителя выбрать по месту (расширение `tools/doc-header-lint.mjs` / отдельный шаг — KISS,
   одна строка обоснования); красный доказан на сломанной фикстуре ДО применения.
2. Полигон-свод покрывает новое поведение (расширение s13 или новый свод — по месту); свод
   печатает свой счёт; `npm run test:core` зелёный целиком (число сводов цитатой).
3. Антипаттерн-проверка исполнена: линтер КОНСУЛЬТИРУЕТ (exit-код/режим не блокирует старт
   работы; формулировка «переформулируй или оправдай на месте» — в выводе), критерий эпика 7.
4. Судья эпика N (лёгкий, №48): таблица «критерий эпика 1–7 → улика» в закрытии `plans/30`;
   эпик N закрыт в родителе и зонтике `plans/26`; следующий шаг — эпик O (мета-план O пишется
   на старте O, не здесь — канон N+1).

## Шаги

- [x] 1. Носитель линтера целевых документов: решить по месту + написать проверку «блок вектора
      цели/критериев присутствует» для живых plans/ideas; селфтест красным на сломанной
      фикстуре (критерий 1). ✅ 2026-08-07 — носитель: расширение `tools/doc-header-lint.mjs`
      (обоснование — в закрытии); «selftest: ok — 12 предсказанных находок» (цитата).
- [x] 2. Прогнать стоп-словарь по секциям требований целевых документов (интеграция с
      `.kaif/tools/kaif-requirements-lint.mjs` — скоуп и вызов решить по месту); ложные
      срабатывания — оправдание на месте, не глушение словаря (критерий 1). ✅ 2026-08-07 —
      композиция в линтере (исток: модуль из `framework/tools/`); 13 находок → 0 тремя родами
      (фикс класса цитат — bugs/48 · оправдания имён-терминов · переформулировка в живом
      plans/34); ретрофиты нормы: ideas/21 + bugs/02 + bugs/45.
- [x] 3. Полигон-свод нового поведения + прогон всего полигона (критерий 2). ✅ 2026-08-07 —
      s13 расширен обеими половинами (красный класс цитат до фикса + ассерты обвязочного
      линтера); «sandbox suite: all 13 suites green» (цитата).
- [x] 4. Антипаттерн-проверка: консультирующий режим доказан (критерий 3). ✅ 2026-08-07 —
      ассерт s13: exit 1 при находках, в выводе «консультативно: поправь или оправдай на
      месте, старт работы не блокируется»; в ритуалах коммита/сборки линтер не стоит.
- [x] 5. Судья эпика N: переисполнение критериев 1–7 эпика независимыми командами; закрытия в
      `plans/30` + `plans/26`; STATUS (критерий 4). ✅ 2026-08-07 — VERIFIED 7/7, 4 охоты
      пусты; таблица улик — в `plans/30`; закрытия проставлены; STATUS обновлён.

## Верификация

Каждый счёт — цитата вывода инструмента (EXP-0025): селфтест линтера («красный: N предсказанных
находок»), полигон («all N suites green»), счёт целевых документов с блоком вектора
(инструментом, не руками). Кириллические проверки — node-скан свойствами Unicode (EXP-0032),
не git-bash `grep -i`.

## Риски (уроки N1–N4)

1. Страж, не красневший на сломанном, ничего не доказывает — селфтест ДО применения (канон
   Стражей; так делали doc-header-lint и requirements-lint).
2. Линтер-шлагбаум = антипаттерн DoR (критерий эпика 7): консультирует по написанному, никогда
   не запрещает начинать работу; стоп-слово в цитате/❌-примере/оправдании — легально.
3. Смешение скоупов инструментов — дефект (решение N3 №7): payload-модуль про требования ≠
   обвязочный линтер меты; расширяя, не сращивать ответственности.
4. Числа в коммит/закрытие — только цитатой вывода (EXP-0025); счёт документов с блоком —
   скриптом.
5. Канон N+1: мета-план эпика O в этой фазе НЕ пишется — только строка «где продолжить» на O.

---

## Закрытие фазы N5 (2026-08-07 18:55 +03:00) — фаза закрывает ЭПИК N

| Критерий приёмки фазы | Улика (цитата инструмента) |
|---|---|
| 1. Линтер целевых документов, красный доказан | Носитель — расширение `tools/doc-header-lint.mjs`; «selftest: ok — 12 предсказанных находок на сломанной фикстуре, +1 под --all, чистые файлы молчат, композиция со словарём находит стоп-слово»; живой финал: «doc-header-lint: scanned 86, findings 0 — all headers green» + «requirements lint OK — 130 file(s), 6 word classes, 0 findings» (13 находок первого прогона → 0) |
| 2. Полигон-свод; полигон целиком | s13 расширен обеими половинами (красный класса многострочных «…»-цитат доказан на дофиксовом модуле — bugs/48; ассерты обвязочного линтера: селфтест-проход · красный/зелёный через `--root` · консультативная формулировка); «sandbox suite: all 13 suites green» |
| 3. Линтер консультирует, не DoR-шлюз | Ассерт s13: при находках exit 1 и «консультативно: поправь или оправдай на месте, старт работы не блокируется» в выводе; греп `doc-header-lint` по package.json / commit.mjs / build-framework.mjs / check-framework.mjs — 0 вхождений (ни в одном гейте старта не стоит) |
| 4. Судья эпика N; закрытия | VERIFIED — 7/7 критериев эпика переисполнены независимыми командами, 4 охоты пусты; таблица улик — `plans/30`; эпик закрыт в `plans/30` и `plans/26`; STATUS обновлён; следующий шаг — эпик O (мета-план детализируется на старте O — канон N+1) |

## Решения, принятые агентом без владельца

1. Носитель линтера — расширение `tools/doc-header-lint.mjs`, не новый инструмент (KISS/DRY:
   тот же обход тех же файлов с теми же скоуп-правилами; словарь остаётся в payload-модуле —
   композиция, не сращивание, решение N3 №7).
2. Пороги нормы: plans ≥ 26 · ideas ≥ 21 — норма N2 (2026-08-07) не ретроактивна, закрытое
   прошлое не переписывается (тот же принцип, что T8); безномерные файлы
   (kaif_implementation_plan) — донормовые справочные, вне скоупа. Обмер до схемы: живые
   планы эпохи 30/30 уже несут блок (EXP-0033).
3. bugs-диалект расширен полем `Fix accepted when` (норма N2); живые bugs/02 и bugs/45
   ретрофичены наблюдаемыми критериями; DONE-баги — только под `--all` (ратчет ретрофита).
4. У идей отдельный блок критериев не требуется: шаблон /propose-idea складывает «как
   проверим» в сам Goal vector; ideas/21 ретрофичена блоком «Вектор цели (боль + как
   проверим)» — дословные строки владельца в её шапке не тронуты.
5. Ложный класс многострочных «…»-цитат закрыт фиксом payload-модуля (bugs/48), а не вечными
   аннотациями («закрывай класс, не экземпляр»); прямые "-кавычки сознательно не тронуты —
   половинки неотличимы от дюймов/апострофов, риск ложной легализации выше пользы.
6. Находки словаря закрыты ТРЕМЯ родами по классификации (EXP-0035); закрытые планы
   аннотируются, не переформулируются (история — журнал); единственная переформулировка — в
   живом plans/34 («минимальная» → «точечная редактура»).
7. Известная граница точности принята: H2+-заголовок, УПОМИНАЮЩИЙ «Вектор цели» прозой,
   теоретически даёт ложный зелёный присутствия блока — граница advisory-инструмента
   (измеренный корпус чист: 30/30 настоящими блоками; ужесточение сломало бы легальную форму
   «## 0. Вектор цели и критерии приёмки релиза» зонтика).

PENDING-строк нет.
