# План 57 — дочерний операционный план: эпик 31 (O), фаза O3 — механизмы и досье окружения

> **Создан:** 2026-08-07 (на закрытии O2, канон N+1).
> **Родитель:** `plans/31_EPIC_kaif_2.2_O_context_environment.md` → фаза O3 (якорь: «### O3.
> Механизмы и досье»).
> **Статус:** готов к исполнению — проекты готовы в `researches/19` §3 (hook-контур) и §4
> (досье); брать свежей сессией. **Вовне:** —.

## Вектор цели

Опциональный hook-контур освежения и досье окружения переходят из проектов O1 в поставку и в
это репо: система с хуками получает механические впрыски-приказы по проекту `researches/19` §3,
любая система — процедуру и шаблон датированного досье окружения по §4; развёртывание без хуков
не краснеет нигде (инвариант родителя §9.10).

## Критерии приёмки (наблюдаемые)

1. Опциональный модуль хуков в поставке: три скрипта по `researches/19` §3 — канон-после-сжатия
   `SessionStart` (matcher `compact|clear`) · таймер `UserPromptSubmit` (предикат: возраст
   `.kaif/refresh-marker.json` > 60 мин) · Stop-страж STATUS (cooldown 1 раз/сессию,
   единственный блокирующий) — каждый с предикатом и cooldown; в комплекте фрагмент-образец
   конфига; машинерия чужие `settings.json` не редактирует — подключение хуков только явным
   шагом с согласием владельца проекта (норма §3).
2. Регистрация модуля по инварианту поставки (Reference §9.6) + свод полигона: деплой с
   модулем и деплой без него — оба зелёные (инвариант §9.10: отсутствие хуков не краснеет).
3. Досье окружения: процедура сбора по 6 осям §4 + формат секции AGENT_GUIDE (таблица
   «факт → значение → проба-команда», дата снятия, команда перегенерации, staleness-правило
   «факты старше 4 недель — гипотеза») — в payload-шаблоне; носитель процедуры решён по Оккаму
   в фазе (склонность §4 — расширение `/refresh-context`, не отдельный навык).
4. AGENTS.md-совместимость оценена: форма «ссылка на AGENT_GUIDE + досье» сделана, если её
   цена — один шаблонный файл и строка регистрации; иначе отклонена с обоснованием в закрытии.
5. Сборка + полигон + doc-header-lint зелёные (счётчики печатают инструменты); судья фазы
   (лёгкий, №48) — критерии переисполнены независимыми командами; закрытие O3 в родителе;
   операционный план O4 написан по канону N+1.

## Шаги

- [x] 1. Скрипты хуков (3 по критерию 1, предикаты + cooldown) + образец конфига. ✅ 2026-08-07
      `framework/hooks/`: `session-start-refresh.mjs` (matcher `compact|clear`, предикат — сама
      редкость события), `prompt-refresh-timer.mjs` (предикат: возраст маркера > 60 мин,
      `--minutes N`; cooldown — сам маркер), `stop-status-guard.mjs` (предикат: работа в сессии
      И STATUS старше 3 ч; cooldown — файл сессии в OS-temp; единственный блокирующий) +
      `settings-fragment.json` + README с инструкцией опт-ина. **Контракт уточнён живым
      fetch по ходу шага:** `additionalContext` ВЛОЖЕН в `hookSpecificOutput` с
      `hookEventName` (верхнеуровневым полем не работает) — карта `researches/19` §1.1
      исправлена тем же заходом.
- [x] 2. Регистрация в поставке (§9.6) + свод полигона «с хуками / без хуков» (критерий 2).
      ✅ 2026-08-07 `build-framework` (блоки `.kaif/hooks/*`), `check-framework` (счёт
      `hookFiles` в ожидании блоков бандла), Reference §14 (соседний абзац о модуле),
      AGENT_GUIDE обоих слоёв, обе карты проекта; свод **s14** (`tools/sandbox/s14-refresh-hooks.mjs`)
      в `sandbox-suite`. **Красный доказан ДО поставки:** прогон s14 против HEAD-бандла (в нём
      FILE-блоков `.kaif/hooks/*` ещё нет) — свод падал на всех деплой-ассертах; судья фазы
      переисполнил процедуру трижды детерминированно: «**❌ ПРОВАЛОВ: 20**» (моё первое число
      «19» относилось к ранней редакции свода, до добавления терпимого чтения и переписывания
      половины инварианта, — и в живом дереве уже не воспроизводилось: класс EXP-0025, число
      обязано быть цитатой ТЕКУЩЕГО инструмента). После пересборки — зелёный, 27 ассертов.
- [x] 3. Досье: процедура 6 осей + шаблон секции в payload AGENT_GUIDE; решение о носителе
      процедуры (критерий 3). ✅ 2026-08-07 секция «Environment dossier» в payload (таблица
      «факт → значение → проба», шапка с датой снятия / командой перегенерации / staleness
      4 недели, 6 осей, DRY-граница с гигиеной текста); RU-зеркало в корневом гиде заполнено
      ЖИВЫМИ фактами (пробы пересняты наблюдением — дельта против O1: **chocolatey установлен**,
      в первом смоке назван только winget; `researches/19` §4 поправлен). Носитель процедуры —
      расширение `/refresh-context` (шаг 3) в обоих слоях.
- [x] 4. AGENTS.md-оценка (критерий 4). ✅ 2026-08-07 форма «ссылка на AGENT_GUIDE + досье»
      УЖЕ действует по построению: `AGENTS.md` пишется машинерией всегда
      (`writeIfNew('AGENTS.md', CONTEXT_POINTER)`) и указывает на `AGENT_GUIDE.md`, досье —
      его секция. Отдельный шаблонный файл НЕ заводится (обоснование — в решениях ниже);
      проверено наблюдением в песочной установке.
- [x] 5. Сборка/полигон/линтер; судья фазы; закрытие в родителе + план O4 (критерий 5).
      ✅ 2026-08-07 цитаты финальных прогонов: «56 embedded files (14 docs + 7 readmes +
      34 skills + 1 tools) · bundle 155 blocks · 671 modules» · «sandbox suite: all 14 suites
      green» · «doc-header-lint: scanned 89, findings 0 — all headers green» · «requirements
      lint OK — 134 file(s), 6 word classes, 0 findings» · страж вопросов «Ждут ответа: 0 ·
      Новых нарушений: 0» · «counters OK — 5 зеркал сверены с живыми числами». Попутная
      находка шага — **`bugs/49`** (счётчики README протухли, страж стерёг 1 зеркало из 5):
      закрыта новым стражем `tools/counters-guard.mjs` (красный доказан мутацией).

## Верификация

Наблюдением: полигон-свод деплоя с хуками и без — оба прогона зелёные (счёт печатает полигон);
живой смок хука `SessionStart` на машине истока (Claude Code — референс-контракт
`researches/19` §1.1: JSON на stdin → `additionalContext` на stdout, кап 10 000 символов);
формат досье — сверка секции грепом по полям «дата снятия / команда перегенерации / staleness».

## Риски

1. Кап 10 000 символов `additionalContext` — впрыск приказом перечитать, никогда телами
   документов (`researches/19` §3, анти-шум).
2. Мутация чужих `settings.json` — только явный опциональный шаг с согласием владельца
   (прецедент дефера `.gitattributes` из L5 — вход эпика O в `plans/31`).
3. Шум хуков — предикат срабатывания и cooldown обязательны у каждого; частота пульса
   ограничена канон-требованием §3 (плотность инструкций сама разрушает следование).
4. Смок хука на этой машине может упереться в версию/поведение живого Claude Code —
   расхождение с картой §1.1 фиксируется правкой `researches/19` (карта — живой справочник),
   не обходом. **Реализовался:** карта O1 описывала `additionalContext` верхнеуровневым полем,
   живой контракт требует вложения в `hookSpecificOutput` — карта исправлена, обхода не было.

## Решения, принятые агентом без владельца

1. **Носитель процедуры досье — расширение `/refresh-context`, не отдельный навык** (склонность
   §4 `researches/19` подтверждена): отдельный глагол не нужен — сбор досье и есть форма
   освежения знания об окружении, а шаг навыка ставит его в ТОЧКУ РЕШЕНИЯ (правило-как-проза
   не работает, EXP-0001).
2. **«Команда перегенерации» — не новый скрипт, а колонка проб + шаг навыка.** Скрипт-сборщик
   пришлось бы писать под ОДИН шелл, а главная ценность досье ровно в том, что пробы гоняются
   в КАЖДОМ шелле отдельно (разные шеллы — разные миры): единый скрипт убил бы предмет
   измерения. Оккам: сущность не заводится.
3. **RU-досье заполнено живыми фактами уже в O3, хотя dogfooding — критерий фазы O4.** Пустая
   таблица-заглушка в ЖИВОМ гиде, при том что факты сняты и переисполнены судьёй ещё в O1,
   читалась бы следующей сессией как «не снято» — ложь по умолчанию. O4 остаётся: подключение
   хуков живьём, наблюдение впрыска после `/clear`, судья эпика.
4. **AGENTS.md: отдельный шаблонный файл не заводится.** `AGENTS.md` уже пишется всегда и
   указывает на `AGENT_GUIDE.md`, досье — его секция; второй указатель был бы вторым носителем
   одного факта (DRY) и вторым местом протухания — против прямого правила самих адаптеров
   («не дублируй канон между `AGENTS.md` и always-on правилом — выбери одно, указывай на доки»).
5. **Инвариант §9.10 прочитан как «опциональность = АКТИВАЦИЯ, не наличие файлов».** Первая
   формулировка свода требовала зелёный `check` после УДАЛЕНИЯ файлов модуля — и получила
   честный MISSING. Проверка прецедентом (удаление `kaif-provenance.mjs` даёт ровно тот же
   MISSING) показала: у tool-модулей опциональность тоже означает «не настроен», а не
   «файлов нет». Свод переписан на обе половины: без подключения — всё зелено; удаление файлов —
   честный MISSING как у соседей.
6. **`bugs/49` заведён и закрыт тем же заходом** (born DONE): находка родилась внутри шага 5,
   фикс без стража был бы фиксом в кредит — страж написан, красный доказан мутацией.
7. **Строка реестра пар заменена вызовом стража**, а не расширена третьим грепом: пять зеркал
   одного факта — это класс, а не три экземпляра.

PENDING: нет (неразрешённых допущений не осталось).

## Судьи фазы (лёгкие, №48) — двое параллельно

**Судья критериев: VERIFIED WITH CAVEATS.** Все пять критериев переисполнены независимыми
командами: три хука, их предикаты, cooldown и форму вывода судья прогнал сам и сверил с живой
докой (кап 10 000 символов подтверждён дословно); «14/14 фактов досье совпали» с его
собственными пробами; «all 14 suites green»; главный подозреваемый — переформулировка
инварианта §9.10 — **снят**: судья воспроизвёл прецедент (`rm kaif-provenance.mjs` → тот же
MISSING) и отметил, что переосмысление ЗАДЕКЛАРИРОВАНО в решении 5, а не спрятано. Гейт
`check-framework` признан **усиленным**, не ослабленным.

**Судья интеграции: блокеров нет** (3 major + 4 minor). Чисто: анонимность (в payload-шаблоне
только `— not probed yet —`, факты машины живут ТОЛЬКО в RU-обвязке), CJK, целостность двух
слоёв (дрейфа генератов нет), нумерация шагов навыка в обеих половинах, честность счёта
`hookFiles`.

**Находки судей и что с ними сделано** (все закрыты этим же заходом):

1. **Ложный `[TESTED]` в файле поставки** (`prompt-refresh-timer.mjs`): маркер утверждал
   «malformed → приказ», а код (верно) молчит при свежем mtime. Воспроизведено, текст маркера
   исправлен на наблюдённое поведение. Это фрод-класс — самая тяжёлая находка.
2. **Гард 5d слеп к новой поверхности** (доказано мутацией: кириллица в
   `framework/hooks/README.md` → сборка зелёная). Гард расширен на `hooks`/`spheres`/`adapters`;
   `framework/tools/*.mjs` исключён ЯВНО с обоснованием (там кириллица — сам payload: RU-половина
   стоп-словаря). Красный доказан той же мутацией судьи.
3. **Модуль отсутствовал в release notes 2.2** (ритуал релиза требует записи; прецедент — эпик N
   свою добавил). Добавлены две записи `TEMPLATE_NOTES_BY_VERSION['2.2']` — про контур освежения
   с ЯВНЫМ «DECISION FOR THE OWNER: активация ваша» и про досье с миграционной строкой.
4. **Секция досье классифицирована `static`**, хотя её положено ЗАПОЛНЯТЬ в каждом проекте →
   `diff` вечно рапортовал бы «апстрим изменил модуль». Заведён оверрайд в
   `framework/module-classes.json` → `adaptive` (потери данных не было и до фикса: `mergeModules`
   судит по sha).
5. **Число «ПРОВАЛОВ: 19» не воспроизводится** (судья трижды получил 20). Проза исправлена с
   указанием, чем получено число; моё «19» относилось к ранней редакции свода — класс EXP-0025.
6. **Счётчики линтеров сняты до появления `plans/58`** (89/134 против живых 90/135). Исправлены —
   иронично, в том же шаге, что поставил страж против этого класса.
7. **Шапка `s14` утверждала обратное телу** («удалена → зелёный»); переписана под обе половины.
8. **`--selftest` стража доказывал 2 зеркала из 5**, заявляя «на обоих». Расширен на зеркало
   AGENT_GUIDE + мутация теперь обязана НАЙТИ цель (не найдена → селфтест падает).
9. **Придирки:** счёт зеркал в `bugs/49` (четыре → пять), фенс `json` в бандле, фильтр файлов
   при чтении `framework/hooks/` в обоих местах (подкаталог больше не роняет сборку `EISDIR`) —
   закрыты.
10. **Опровергнуто переисполнением:** «висячая ссылка §9.10». Нотация читается как «§9,
    пункт 10», и пункт 10 существует — «Опциональность: развёртывание без новых режимов не
    краснеет» (`researches/15`). Судья искал строку «9.10» дословно — класс ложной тревоги
    грепа (EXP-0032). Зерно принято: нотация двусмысленна, в своде s14 ссылка развёрнута.

**Каветат принят как есть:** `counters-guard` — ручная команда (как `doc-header-lint` и
`questions-guard`), автоматическим гейтом не enforced; он стоит в реестре пар, который гоняют
`/end-chat` и `/release`.
