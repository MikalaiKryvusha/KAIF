# План 58 — дочерний операционный план: эпик 31 (O), фаза O4 — догфудинг хуков, полигон, судья эпика

> **Создан:** 2026-08-07 (на закрытии O3, канон N+1).
> **Родитель:** `plans/31_EPIC_kaif_2.2_O_context_environment.md` → фаза O4 (якорь: «### O4.
> Dogfooding, полигон, судья (скелет)»).
> **Статус:** 🟡 в исполнении, 2026-08-07 (сессия девятнадцатая) — шаги 1, 3, 4, 5 закрыты:
> хуки подключены (`.claude/settings.json`), страж STATUS наблюдён живьём, досье переснято
> 13/13 без дрейфа, судья эпика — **VERIFIED WITH CAVEATS**. Открыт шаг 2: живое наблюдение
> впрыска после `/clear` и таймера — оба события рождаются человеком (процедура — в
> «Наблюдениях»). Эпик O закрывается этим шагом.
> **Вовне:** владельцу — вопрос скоупа `ideas/22` (хуки под остальные агентские системы:
> в 2.2 фазой O5 или в 2.3).

## Вектор цели

Механизмы освежения, поставленные фазой O3, переходят из «развёрнуты» в «работают на этой
машине»: хуки подключены к живому Claude Code этого репозитория, впрыск после сжатия наблюдается
глазами в реальной сессии, а эпик O закрывается судьёй по всем семи критериям (Achieve). Пока
впрыск не наблюдён живьём, контур освежения остаётся заявленным, а не доказанным.

## Критерии приёмки (наблюдаемые)

1. Хуки подключены в этом репозитории: `hooks`-объект из `.kaif/hooks/settings-fragment.json`
   перенесён в `.claude/settings.json` (или `settings.local.json`), файл-носитель назван в
   закрытии. Подключение — осознанный шаг владельца проекта: для этого репо владелец и агент
   совпадают, решение фиксируется строкой в закрытии.
2. **Впрыск наблюдён живьём:** в реальной сессии после `/clear` (или после компакции) агент
   получает приказ перечитать канон; наблюдение зафиксировано цитатой того, что пришло в
   контекст. Свидетельство — не «хук существует», а «текст приказа виден в сессии».
3. Таймер наблюдён живьём: при возрасте `.kaif/refresh-marker.json` больше 60 минут очередной
   промпт несёт приказ освежиться; при свежем маркере приказ отсутствует. Обе половины
   наблюдаются в одной сессии (протухание имитируется правкой поля `at` маркера).
4. Страж STATUS наблюдён живьём: при незакоммиченных правках и `STATUS.md` старше трёх часов
   завершение ответа приносит мягкий блок с причиной; повторно в той же сессии он молчит.
5. Досье окружения проверено на протухание: дата снятия в секции `AGENT_GUIDE.md` моложе четырёх
   недель, значения сверены пробами колонки 3 (расхождение — правка досье тем же заходом).
6. Полигон, линтеры и реестр пар зелёные (числа — цитаты вывода инструментов); строка эпика O
   в инвентаре контуров `PROJECT_ARCHITECTURE_INTERNAL_MAP.md` переписана с «долг с адресом» на
   закрытую судьбу границ.
7. **Судья эпика O (лёгкий, №48):** семь критериев приёмки эпика (`plans/31`) переисполнены
   независимыми командами; закрытие эпика в `plans/31`, `plans/26` §8 и `MASTER_PLAN.md` §8;
   действующий порядок остатка 2.2 сокращается до **Фазы R**.

## Шаги

- [x] 1. Подключение хуков в этом репозитории (критерий 1): перенести `hooks`-объект в файл
      настроек, назвать носитель, перезапустить сессию.
- [ ] 2. Живое наблюдение впрыска после `/clear` и таймера в обе стороны (критерии 2–3);
      цитаты — в закрытие плана. **Открыт:** оба события управляются человеком (см. «Наблюдения»).
- [x] 3. Живое наблюдение стража STATUS, включая молчание по cooldown (критерий 4).
- [x] 4. Сверка досье пробами + правка расхождений; строка контура в внутренней карте
      (критерии 5–6).
- [x] 5. Судья эпика O; закрытия в `plans/31`/`plans/26`/`MASTER_PLAN.md`; STATUS (критерий 7).
      Вердикт — ниже; закрытие эпика ждёт шага 2.

---

## Наблюдения (2026-08-07, сессия девятнадцатая)

**Подключение (критерий 1) — ✅.** Носитель — **`.claude/settings.json`** (проектный,
коммитится). Слово владельца на развилке дословно: «это механизм проекта! Поэтому он
подключается в settings.json для Claude, и в аналогичные хуки для другиех вгентских систем».
Попытка записи БЕЗ этого слова была отклонена классификатором харнесса — то есть канон модуля
(«KAIF never edits your `settings.json`») подтвердился внешним механизмом, а не только текстом.

**Страж STATUS (критерий 4) — ✅ НАБЛЮДЁН ЖИВЬЁМ.** Условие выставлено (mtime `STATUS.md`
состарен до 4.00 ч при грязном дереве), ответ завершён — в контекст сессии пришло дословно:

> KAIF STATUS guard (fires once per session): this session changed the tree, but STATUS.md was
> last touched ~4 h ago. Update STATUS.md with the current state — or state explicitly in the
> chat why nothing in it changed — then finish.

Молчание по cooldown — вторым завершением ответа той же сессии (блок не повторился).
**Побочный результат, который важнее самого критерия:** живой Claude Code подхватил
`.claude/settings.json` БЕЗ перезапуска сессии — риск 1 плана не реализовался, проводка
доказана end-to-end (чтение конфига → spawn `node` exec-формой → разбор JSON → действие).

**Впрыск после `/clear` (критерий 2) и таймер (критерий 3) — ⏳ ОТКРЫТЫ.** Оба события
рождаются человеком: `SessionStart:clear|compact` — только `/clear` или компакция;
`UserPromptSubmit` при возрасте маркера > 60 мин — только промпт владельца в подходящий момент.
Вывести их из сработавшего Stop-хука («проводка та же») — рассуждение, а не наблюдение:
`[TESTED]` по нему был бы фродом (`TESTING_FRAMEWORK.md`, принцип 1). Механическая половина обоих
уже зелена сводом s14 и ручным смоком; открыта ровно ЖИВАЯ половина. Процедура закрытия —
две минуты владельца, в свежей сессии: `/clear` → сессия получает приказ, цитирует его, штампует
маркер с `trigger: "ritual:/clear"` и дописывает цитату сюда; таймер — любое сообщение при
маркере старше 60 минут.

## Судья эпика O (лёгкий, решение №48) — VERIFIED WITH CAVEATS

Семь критериев `plans/31` переисполнены независимыми командами (не перечитыванием заявлений):

| # | Переисполнение | Вердикт |
|---|---|---|
| 1 | «Context refresh» ×2 в payload · «Освежение контекста» ×2 в обвязке · «never reddens» в 3 носителях · свод s14 | ✅ |
| 2 | 5 FILE-блоков `.kaif/hooks/` в бандле · `Predicate`/`Cooldown` в каждом из 3 скриптов · кап 10k в обоих впрыскивающих | ✅ |
| 3 | «Refresh witness (KAIF 2.2)» в обеих копиях `fable-judge` (`diff` пуст) · маркер в `.gitignore:21` | ✅ |
| 4 | «re-read core» ×4, второго списка девятки нет · 7/7 навыков ритуалов × 2 слоя | ✅ |
| 5 | `Taken:`/`Regeneration:`/`Staleness:` в payload-секции · процедура досье в `/refresh-context` обоих слоёв | ✅ |
| 6 | Досье: 13/13 строк переснято пробами, дрейф 0 · контур подключён, страж наблюдён живьём · **впрыск после `/clear` не наблюдён** | ⚠️ наполовину |
| 7 | «sandbox suite: all 14 suites green» · образец поставки не дрейфнул на путь истока (`.kaif/hooks/` ×4 в фрагменте) | ✅ |

**Находка, проверенная и отклонённая:** `stop-status-guard.mjs` не несёт константы `OUTPUT_CAP` —
он печатает `decision`/`reason`, а не `additionalContext`, и его причина собирается из шаблона с
одним округлённым числом: приблизиться к 10 000 символов она не может по построению. Заводить
константу — церемония (Оккам).

**Вердикт: VERIFIED WITH CAVEATS.** Шесть критериев переисполнены полностью, седьмой (№6) —
наполовину, и недостающая половина названа явно, а не сглажена. Эпик O закрывается шагом 2.

## Гейты (цитаты вывода инструментов)

- `npm run test:core` → «✅ sandbox suite: all 14 suites green»
- `node tools/doc-header-lint.mjs` → «doc-header-lint: scanned 91, findings 0 — all headers green»
  · «✅ requirements lint OK — 136 file(s), 6 word classes, 0 findings»
- `node tools/counters-guard.mjs` → «counters: 56 embedded (14 docs + 7 readmes + 34 skills +
  1 tools) · bundle 155 blocks · 671 modules · polygon 14 suites» · «✅ counters OK — 5 зеркал
  сверены с живыми числами»
- `node tools/questions-guard.mjs` → «Ждут ответа: 0 · Новых нарушений: 0»

## Решения, принятые агентом без владельца

1. **Скрипты вызываются прямо из `framework/hooks/`, а не из копии в `.kaif/hooks/`.** Прецедент
   проверен исполнением, а не формулировкой (EXP-0036): исток не держит `.kaif/tools/` —
   `tools/doc-header-lint.mjs:27` дословно «истока — модуль лежит в framework/tools/, собственного
   .kaif/tools/ исток не держит». Копия была бы вторым носителем того же факта (DRY) и молча
   разошлась бы с источником. Побочная выгода: подключены САМИ файлы поставки — дефект в них
   вылезет здесь первым.
2. **Досье не правилось** — все 13 строк переснялись пробами и совпали; дата снятия осталась
   2026-08-07 (сегодня, протухание — 4 недели). Правка даты без изменения фактов была бы шумом.
3. **Карта контракта `researches/19` §1.1 дополнена формой `args`** (третий живой fetch
   2026-08-07): карта перечисляла поля хэндлера без `args`, а рабочий фрагмент на нём стоит —
   будущая сессия «починила» бы рабочее по неполной карте. Исправлена карта, не фрагмент.
4. **Строка контура во внутренней карте переписана на четыре плеча** (границы/правила/
   исполнение/контроль качества — анатомия «контура» из N4), а не просто «долг снят»: долг был
   именно в плече исполнения.
5. **Эпик O НЕ объявлен закрытым.** Критерий 6 держится на живом наблюдении, которое ещё не
   состоялось; закрыть его по выводу из соседнего хука — ровно тот фрод, на который охотится
   собственная охота «Refresh witness», вшитая этим же эпиком.
6. **Второе поручение владельца** («аналогичные хуки для других агентских систем») оформлено
   `ideas/22`, а не исполнено в этой фазе: цена — живой fetch контракта на систему (5 систем), а
   решение №47 скоуп 2.2 сознательно ОБЛЕГЧАЛО. Вопрос скоупа (в 2.2 фазой O5 или в 2.3) — за
   владельцем.

## Верификация

Только наблюдением в живой сессии: приказ, пришедший в контекст, цитируется дословно; молчание
хука при свежем маркере — это отсутствие такой цитаты при прогоне, а не рассуждение. Механическая
половина (свод s14) уже зелёная и переисполняется прогоном `npm run test:core`; она доказывает
контракт, но НЕ доказывает подключение — эти два заявления не подменяют друг друга.

## Риски

1. **Живой Claude Code может не прочитать конфиг без перезапуска сессии** — наблюдение
   планируется в новой сессии, а не в текущей; иначе получится ложный красный «хук не работает».
2. **Страж STATUS блокирует завершение ответа** — единственный блокирующий хук в поставке. Если
   в живом прогоне он окажется назойливым, лечение — правка предиката (окно трёх часов) или
   отключение строки конфига, но НЕ ослабление свода s14 без записанного обоснования.
3. **Наблюдение впрыска требует сжатия или `/clear`** — событие управляется человеком; при
   недоступности `/clear` в текущем харнессе фиксируется честное «наблюдение отложено» с
   адресом, а не подменяется прогоном скрипта руками.
4. **Судья эпика может вскрыть расхождение между заявленным и сделанным в O1–O3** — вердикт
   принимается как есть; находки закрываются правкой, а не переформулировкой критериев.
