# План 73 — ЭПИК U «Поле»: жизненный цикл в поле и паритет поставки

> **Создан:** 2026-08-14 07:14 +03:00 (сессия планирования 2.3); **развёрнут из скелета в полный
> операционный план 2026-08-14 07:18 +03:00** (решение №76 — слово владельца: «запланировать
> полноценно все эпики. И операционные планы по ним»).
> **Родитель:** `plans/70_EPIC_kaif_2.3.md`, строка нарезки «**Эпик U** | Поле: жизненный цикл и
> паритет поставки (темы C+D)».
> **Статус:** 🆕 готов к исполнению ПОСЛЕ эпика T (порядок — решение №74); первый шаг любого
> старта — нулёвка U0 (актуализация входов, №76). ✂️ **2026-08-21: скоуп 2.3 сужен решением №77
> до подмножества U′** (поимённо — `ideas/24` § «Сокращение скоупа»: U1-фиксы + новые issues
> #19/#15/#16/#20 + семейство 98 + заморозка пакетов из U5); фазы U2–U4 и остаток — 2.4
> (`ideas/26`).
> **Вовне:** машинерия поставки (`framework/installer/`) и payload-канон — публичные контракты
> версии; ответы в issues origin — наружу от имени владельца через send-гейт (строка `AUTH:`).

## Вектор цели (Achieve)

Поле получает то, что исток уже выстрадал: машинерия обновления перестаёт терять состояние и
молчать о деградации, задание обновления становится самодостаточным носителем, уроки и гейты
истока доезжают до поставки (паттерн P4 закрыт), полевые сигналы 2.2 закрыты фиксами или явными
решениями. Якорь мета-плана: «U до V — поле ЖДЁТ фиксов (crash-safety, --lang, tracking — живые
проекты владельца)».

## Готово, когда (критерии приёмки эпика)

1. **17/17 CONFIRMED U-вхождений** триаж-таблицы (семейства 92, 93, 94, 97, 99) несут исход
   (`починено+страж` · `точечно с причиной` · `отклонено решением владельца`) — колонка
   «Исход U», счёт скриптом.
2. **21/21 полевой пункт** `researches/20` §2.2 адресован (фикс · «не делаем» с причиной ·
   перенос с адресом) — таблица адресации в §U5, греп по номерам пунктов.
3. **Issues origin #3, #4, #6, #8 (+#10) закрыты** фиксом или ответом-решением; каждая отправка
   наружу — через `send-outbound` со словом владельца.
4. **Заморозка восьми языковых пакетов исполнена** (№56/№57): явное объявление (версия ·
   состояние · причина · оживление по запросу сообщества) в README обеих половин и в поставке;
   оси `counters-guard`/`check-framework` знают заморозку ПОИМЁННО; деградация состава пакетов
   видима гейтам (лечит слепоту семейства 92).
5. **Каждый новый механизм машинерии несёт полигон-свод**, доказанный красным на сломанной
   версии; `npm run test:core` зелёный на закрытии каждой фазы.
6. Дерево зелёное целиком + лёгкий судья эпика (№48).

## Фазы и шаги

### U0 — нулёвка: актуализация входов (№76)

- [ ] Сверить входы против HEAD: триаж-таблица (17 U-вхождений не закрыты попутно эпиком T?),
      issues #3/#4/#6/#8/#10 ещё открыты?, список §2.3 «полем не прогнано» не сужен ли полем.
- [ ] Поправить этот план по фактам сверки (правка плана — штатное действие) и объявить дельту
      в чате одной строкой.

### U1 — машинерия жизненного цикла (семейства 92, 99 + полевые пп. 1–7)

- [ ] 1. **Crash-safety `update`** (KLAS п. 2): staging в temp-каталог → атомарный swap, ЛИБО
      журнальная запись до первой замены + команда `resume`; выбор формы — по факту чтения
      `cmdUpdate`; свод: прогон, убитый на середине, оставляет либо нетронутое дерево, либо
      видимый журнал — полуобновлённого дерева без следов не существует.
- [ ] 2. **Переход anonymous→origin командой** (issue #8, HIGH): явный `--mode` на легаси-ветке
      пишет `tracking`/`origin` и логирует переход; противоречие флага и маркера без явного
      намерения — ОТКАЗ с именем команды; свод по шести строкам репро из issue.
- [ ] 3. **Валидация `--lang`** (92.1): код не из известного набора — отказ со списком кодов;
      слово («Russian») — подсказка «возможно, вы имели в виду ru»; свод: `--lang Russian` не
      разворачивает молча английское дерево.
- [ ] 4. **`diff --source` по голому URL** (#10, 3 проекта): `github.com/<o>/<r>` резолвится в
      `releases/latest/download` (как уже делает `update`); 404 печатает ОЖИДАЕМУЮ форму URL;
      libuv-аборт после ошибки гасится (закрыть handle до exit).
- [ ] 5. **Превью называет модуль и не считает адаптивное апстримом** (KAGO `bugs/KAIF/04`):
      в выводе `diff` — сигнатура модуля на каждую дельту; снапшот-адаптивные модули — в `kept`.
- [ ] 6. **Квитанция — МОМЕНТ по §12.3** (KLAS п. 5): `date`/`verifiedAt` — полный локальный
      ISO 8601; свод сверяет формат.
- [ ] 7. **Счётчики финальной строки считают ДИСК** (99.1/99.2: «8 owner docs» при 6 на диске;
      «35 skill bodies» до фильтра анонимности) и **неизвестный канал — отказ** (99.3), не
      молчаливый `release`; своды на оба.
- **Гейт U1:** все своды фазы красные на сломанной версии и зелёные на живой; `test:core`
  целиком зелёный.

### U2 — задание обновления как самодостаточный носитель (пп. 8–12, 14; 97.3)

- [ ] 1. Для wholesale-переводов: строка на каждый diverged-файл — точный путь апстрима +
      готовая команда диффа (`git diff v2.2 v2.3 -- <файл>`) прямо в `KAIF_UPDATE_TASK.md`
      (Unliminium п. 9).
- [ ] 2. При `language ≠ en`: пункт задания «эти N файлов приехали английскими — переведи, если
      проект переводит wholesale» + СПИСОК неполноты пакета переезжает из строки лога в пункт
      задания с чекпоинтом (QA п. 11, класс issue #6).
- [ ] 3. Boilerplate `merge-diverged` — одна фраза + голый список (NDIM/QA п. 8).
- [ ] 4. `stale-claims` сканирует и СКРИПТЫ проекта на пин старой версии, не только прозу
      (QA п. 14).
- [ ] 5. Гейт плейсхолдеров = списку пункта: генератор дописывает `.kaif/spheres/<sphere>.md` в
      строку `<BUILD_COMMAND>` (issue #3, вариант 1 — предпочтён самим полем).
- [ ] 6. `diff --source` показывает и НОВЫЕ файлы релиза (97.3: сейчас «63 из 79», ~18 % скрыто
      при утвердительном итоге) — или итоговая строка честно называет невидимое.
- **Гейт U2:** песочная установка/обновление печатает задание, в котором есть все шесть свойств;
  свод-греп по сгенерированному заданию.

### U3 — паритет обвязка→поставка (семейства 93, 94; 89.3/89.4; 97.1/97.2)

- [ ] 1. **Файловая дверь текста владельца — в поставку** (94.*): контракт `<COMMIT_COMMAND>` не
      предписывает argv-форму; задания установки/обновления и текст отказа чекпоинта называют
      `--name-file`; `sphere <имя>` принимает не-ASCII только через дверь; измерение «аргумент —
      текст человека» появляется в спецификации CLI (`COMMANDS`).
- [ ] 2. **Гейт неожиданного файла** (89.3/89.4): подметающие предписания в поставочных
      `/end-chat` и `/release` заменены на инструмент/явный набор; дефолт `<COMMIT_COMMAND>`
      установщика больше не `git add -A && …` — либо честная строка «гейта нет: назови набор
      руками» (правило «механизация без исполнителя не обещается» — bugs/71-класс).
- [ ] 3. **Урок bugs/69 — в payload**: норма G7 в поставочном `/owner-reviews` едет ВМЕСТЕ с
      дешёвой безбраузерной приёмкой (`--etalon-only`-эквивалент) и предписанием ставить её в
      частый ритуал; противоречие I17 внутри одного бандла (93.2) снято в обе стороны.
- [ ] 4. **Полевой отчёт рождается и на маршруте обновления** (97.1): фикс `f65107f`
      ПЕРЕПРОВЕРИТЬ наблюдением (песочное обновление СТАРЫМ ядром), две оставшиеся половины
      наблюдения закрыть; смены правил 2.2/2.3 — в `POLICY_CHANGES_BY_VERSION` (97.2).
- **Гейт U3:** греп argv-форм по payload = 0 вне rename-карт; песочное обновление печатает
  field-report пунктом; своды красные на мутации.

### U4 — канон поставки: язык, голос, хуки (issues #4, #6; пп. 13, 15–20)

- [ ] 1. **Язык — по АУДИТОРИИ** (#6): секция Languages обоих слоёв → вопрос-роутер «читает ли
      это владелец?» + таблица аудиторий + две границы (promotion rewrites · recon остаётся EN);
      `/plan-epic` называет язык мета-плана в точке письма.
- [ ] 2. **Портрет голоса — шаг установки** (#4): десятый пункт задания адаптации ДО первого
      owner-текста, с ignore-решением тем же шагом; «у владельца портрета нет» фиксируется в
      AGENT_GUIDE проекта, чтобы не переспрашивать.
- [ ] 3. **Паттерн «одно хранилище голоса, проекты ссылаются»** благословляется канонично
      (симлинк/ссылка + gitignore; KLAS п. 6, Unliminium: реестр consumers) — правка канона
      портрета в поставке.
- [ ] 4. **`settings-fragment.json` хуков** сверяется с живой вендорской схемой (NDIM п. 17:
      `command`+`args` против единственной строки) — форма исправляется по документации,
      сверка — живым fetch в день правки (норма O5).
- [ ] 5. **`sync` назван в сообщении успеха деплоя** (NDIM п. 18) — одна строка.
- [ ] 6. **Хвосты `/owner-reviews`** — семь строк недосказанности из отчёта KAGO (минимальный
      контур назван · QA-блоки=контракт · инвариант-близнец I27 для открытия · средство к I31 ·
      тихие часы — параметр владельца на странице · ссылка C2→G8 · мета-блок только исходящим).
- [ ] 7. **Открытый словарь slug'ов `kaif-fp`** (NDIM wish 4): файл в поставке + ссылка из
      шаблонов `/report-bug`.
- **Гейт U4:** каждое из семи — правка в `framework/*` + пересборка + строка «чем наблюдалось»;
  межпроектный дедуп-словарь виден в бандле.

### U5 — заморозка пакетов и закрытие эпика (№56/№57; семейство 92-остаток)

- [ ] 1. Заморозка восьми пакетов: объявление по формуле №56 (версия · состояние · причина ·
      оживление по запросу) в README обеих половин + в поставке; пакеты остаются в бандле
      замороженными, НЕ удаляются (форма — уточнить у владельца ТОЛЬКО если чтение №56/№57
      оставит развилку).
- [ ] 2. Оси стражей: `counters-guard`/`check-framework` учат статус «frozen» поимённо; гейты
      видят перетасовку состава (92.3), выпотрошенную локализацию (92.4), потерю пакета (92.5);
      триггеры сверяются не голым счётом (92.2).
- [ ] 3. Таблица адресации: 21 полевой пункт × исход; 17 U-вхождений × исход — счёт скриптом
      (критерии 1–2).
- [ ] 4. Ответы в issues #3/#4/#6/#8/#10 — черновики → слово владельца → `send-outbound`.
- [ ] 5. Полигон-покрытие дыр §2.3 по рискам: минимум — маршрут `update` старым ядром ·
      восстановление из бэкапа · anonymous-2.2; остальное — явным «не покрываем, причина».
- [ ] 6. Замер лесов (леса не выросли от U — критерий №75), лёгкий судья эпика, уроки в
      `EXPERIENCE.md`.
- **Гейт U5 = критерии эпика 1–6 зелёные.**

## Риски

- **Топ-1: эпик безразмерен.** Лечение: U0-нулёвка вправе вынести пункты в «не делаем/2.4»
  решением владельца; фазы закрываются по своим гейтам независимо.
- **Топ-2: правки машинерии ломают парк.** Лечение: каждый механизм — со сводом, красным на
  сломанной версии; `test:core` на закрытии каждой фазы; песочные прогоны до боевых.
- **Топ-3: внешние ответы в issues.** Только send-гейт + слово владельца (стоячая авторизация
  пушей НЕ покрывает внешние публикации).
- **Топ-4: U переоткрывает класс P1** («закрыт формой»). Лечение: стражи U пишутся на движке
  правил из T2 там, где это стражи формы; закрытие класса — только со стражем класса (норма T3).

## Решения, принятые агентом без владельца

1. **Скелет развёрнут в полный план в день создания** — исполнение решения №76; защита от
   фикции — нулёвка U0 (сверка входов против HEAD на старте эпика).
2. **Пофазная разбивка 17 U-вхождений задана семействами** (92, 99 → U1; 97.3 → U2; 93, 94,
   97.1/97.2, 89-U-часть → U3; 92-остаток → U5); поимённый сплит делает исполнитель по
   триаж-таблице (колонка «Исход U») — второго зеркала таблицы не заводим (DRY).

## Links

`plans/70` (зонтик, эпик U) · `plans/72` (эпик T — механизмы, на которые U опирается) ·
`reports/KAIF_AUDIT/2026-08-14_r2_triage_SUMMARY.md` (17 вхождений U) ·
`researches/20` §2.2/§2.3 (полевой синтез и непрогнанное) · issues origin #3/#4/#6/#8/#10 ·
`MASTER_PLAN.md` §7 (№56/№57/№72/№74/№76) · `bugs/92`–`94`, `97`, `99` (первоисточники).
