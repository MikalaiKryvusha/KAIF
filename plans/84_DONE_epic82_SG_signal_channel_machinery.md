# План 84 — эпик SG «Канал сигналов машинерией»: команда `kaif-core report`

> **Создан:** 2026-09-04 01:33 +03:00 (на закрытии эпика CN — канон N+1, №43).
> **Родитель:** `plans/82` (эпик SG; якорь — критерий приёмки 2: «Команда `node
> .kaif/kaif-core.mjs report <тикет>` существует»).
> **Статус:** 🟡 в работе с 2026-09-04 01:45 +03:00 (сессия 48, слово владельца «эпик SG»);
> **✅ ЭПИК ЗАКРЫТ 2026-09-04 02:00 +03:00** — SG0–SG5 за один защищённый цикл: команда
> `report` в ядре, свод s17 (25 проверок, красный на HEAD-ядре), шаг 4 навыка «запусти» в
> EN-поставке, Reference §10, ноты/policy; судья VERIFIED WITH CAVEATS (таблица ниже); план
> TM — `plans/85`.
> **Вовне:** новая команда машинерии поставки (`framework/installer/KAIF-CORE.mjs`) + правка шага 4
> `/report-bug` обоих слоёв — публичный контракт версии 2.5; полевым проектам приезжает
> обновлением как дельта модулей; сама команда совершает ВНЕШНЕЕ действие (issue в origin) под
> стоячей авторизацией владельца KAIF (issue #15), оформленной в CN1 исключением из гейта AUTH.

## Вектор цели (Achieve)

**Боль** (источники — NDim `bugs/KAIF/01` ×2 версии, issue #37; сверка с HEAD —
`researches/24` §2/§4): доставка тикета о дефекте KAIF живёт ПРОЗОЙ шага 4 навыка `/report-bug`,
и классификатор агентской системы отказывает не формулировке, а самому ПРЕДМЕТУ шага — на
Claude Code модуль шага 4 дважды (2.3 и 2.4) не удалось даже записать на диск; три тикета KAGO
и два тикета NDim лежали `Delivered upstream: NOT YET` часами и днями. CN1 сделал исключение из
гейта AUTH ЗАКОННЫМ; SG делает его ИСПОЛНИМЫМ: одна команда, которую система разрешений
человека allowlist-ит один раз, а навык говорит только «запусти».

**Где хотим оказаться.** `node .kaif/kaif-core.mjs report <тикет>` доставляет локальный тикет
`bugs/KAIF/NN_*.md` в origin через `gh`, вписывает URL в строку `Delivered upstream:`, отказывает
с названной причиной на `tracking: anonymous`, без `gh`, на уже доставленном тикете и на тикете
без шапки; шаг 4 `/report-bug` обоих слоёв говорит «запусти команду» и цитирует авторизацию
одной строкой; свод полигона доказывает оба ответа на фикстурном `gh`.

## Готово, когда (критерии приёмки)

1. **Команда существует и доставляет.** `report <путь>` в таблице `COMMANDS` (`mutating: true`,
   `pos: 1`, флаг `--dry-run`): читает `.kaif/kaif.json` → `tracking`/`origin`; читает тикет
   (H1 → заголовок issue; тело файлом через `--body-file`, канон гигиены текста); зовёт
   `gh issue create --repo <owner/repo из origin> --title … --body-file …` со сроком (по образцу
   `tools/send-outbound.mjs`: три исхода — успех · отказ · НЕИЗВЕСТНО при таймауте, и
   «неизвестно» никогда не называется отказом); на успех переписывает строку
   `**Delivered upstream:** NOT YET` на URL и печатает его. *Meter:* свод полигона с подменным
   `gh` на PATH (шим печатает URL и пишет аргументы в файл): после прогона строка тикета несёт
   URL, аргументы шима — `issue create` с заголовком H1.
2. **Четыре отказа названы.** `tracking: anonymous` → «сигнал остаётся локальным; NOT YET здесь
   законно» (exit 2) · `gh` не найден / `gh auth status` красный → причина и команда лечения
   (exit 2) · тикет уже доставлен (в строке URL) → «уже доставлен <URL>» без второго issue
   (exit 0, идемпотентность) · нет H1 или строки `Delivered upstream:` → «не тикет KAIF» (exit 1).
   *Meter:* четыре ассерта свода, каждый по своей фикстуре.
3. **Трейлер авторства в теле.** Issue заканчивается строкой «Filed by the project's agent
   under the KAIF owner's standing authorization (origin issue #15); transport — the `gh`
   account of the deploying machine» — транспорт и авторство как две оси (шаг 4 навыка сегодня).
   *Meter:* греп трейлера в теле, переданном шиму.
4. **Навык говорит «запусти».** Шаг 4 `/report-bug` обоих слоёв (`framework/skills/report-bug`,
   `.claude/skills/report-bug`): на `origin` — `node .kaif/kaif-core.mjs report <тикет>` в том
   же движении, раньше текущей работы (формулировка CN1 сохранена); авторизация — одной строкой
   со ссылкой на issue #15; длинный абзац про классификаторы и оси переезжает в комментарий
   команды и `KAIF_REFERENCE` §10 (предмет шага в тексте навыка сжат до минимума — гипотеза NDim
   «классификатор реагирует на предмет, не на императив»). *Meter:* греп `kaif-core.mjs report`
   в шаге 4 обоих слоёв; `wc -l` шага 4 меньше прежнего (число снять ДО правки, EXP-0053).
5. **Поставка и документы.** `KAIF_REFERENCE` §10 — команда в списке с четырьмя отказами;
   ноты шаблонов и policy-изменение 2.5 в сборщике (INERT до version 2.5); счётчики поставки
   зелёные; `KAIF-CORE.mjs` help печатает команду. *Meter:* `node tools/build-framework.mjs`
   EXIT 0 · `node tools/counters-guard.mjs` · греп `report` в help.
6. **Приёмка полем — отложенный meter.** Обновление 2.4→2.5 на Claude Code переносит модуль
   шага 4 16/16 без divergence note (NDim `bugs/KAIF/01`, «Expected effect»). Проверяется ТОЛЬКО
   полем после релиза — в этом репо не воспроизводимо; фиксируется как ожидание в ответе на
   тикет (эпик RL, черновики ответов).
7. Дерево зелёное целиком: сборка · `npm run test:core` (новый свод s17) · `counters-guard` ·
   реестр пар · `doc-header-lint` · `questions-guard`; лёгкий судья эпика (№48).

## Шаги

- [x] **SG0. ✅ 01:48. Нулёвка актуализации входов (№76).** Сверено против HEAD: шаг 4 после CN1
      (EN — 25 строк, RU-оговорка доставки — 10 строк, замер ДО правки), таблица `COMMANDS`
      (`flags: {'--x': true}` = значение, `false` = булев; `pos`), `tools/send-outbound.mjs` (три
      исхода — форма взята, код не копирован), шаблоны A/B со строкой `Delivered upstream:`.
- [x] **SG1. ✅ 01:52. Команда `report`.** `cmdReport` + `ghSpawn` + `refuse` в `KAIF-CORE.mjs`
      (импорты `spawnSync`, `mkdtempSync`, `tmpdir`): разбор тикета (H1 → заголовок; строка
      `Delivered upstream:` регулярным выражением) · отказы: tracking ≠ origin exit 2 · нет `gh`
      (ENOENT) exit 2 · `gh auth status` красный exit 2 · не тикет / не найден exit 1 · уже доставлен
      exit 0 (идемпотентно) · `gh` отказал exit 2 словами `gh` · таймаут exit 3 «OUTCOME UNKNOWN»
      с командой ручной проверки · `--dry-run` печатает repo/title/body без вызова · тело —
      временный каталог `mkdtemp` (bugs/59) с трейлером авторства (#15) и LF · URL пишется
      `writeMatchingEol`. Швы для полигона: `KAIF_GH` (скрипт-подмена `gh`), `KAIF_GH_TIMEOUT_MS`.
      Help-шапка и строка `COMMANDS` (`mutating`, `pos: 1`, `--dry-run`).
- [x] **SG2. ✅ 01:56. Свод s17** (`tools/sandbox/s17-report.mjs`): развёрнутая копия; подменный
      `gh` — скрипт на шве `KAIF_GH` (журнал argv + тела; режимы ok/noauth/refuse/hang) — живой `gh`
      полигон не зовёт; 25 проверок: help · anonymous · нет gh · noauth · не тикет · нет файла ·
      dry-run ×3 · доставка ×7 (URL в тикете, `--repo` из origin маркера, `--title` = H1, трейлер
      #15 + кириллица без потерь, `(this issue)` в теле, `auth status` до create) · идемпотентность
      ×2 · отказ gh ×2 · таймаут ×3. Красный доказан на HEAD-ядре до SG1: 18 проверок
      `unknown command: report`. Полигон «all 17 suites green»; зеркала числа сводов 17 (6 мест).
- [x] **SG3. ✅ 01:58 (ЧАСТИЧНО — см. решение 3). Навык.** EN-поставка
      `framework/skills/report-bug/SKILL.md` шаг 4 — «run the machinery» + одна строка авторизации
      (#15, #37) + отказы + таймаут + «дай спросить и дождись»: 25 → **14 строк** (критерий 4,
      число из `awk`). **RU-копия обвязки `.claude/skills/report-bug/SKILL.md` — правка ОТКЛОНЕНА
      классификатором автоправ Claude Code** («Blocked by classifier») — живое воспроизведение
      NDim `bugs/KAIF/01` (+1) в самом истоке: реагирует на предмет, не на слова; RU-оговорка
      оставлена в форме 2.4 (сути не противоречит: стоячая авторизация, оси, гейт), обход не
      делался. Ростер навыков 37 не менялся.
- [x] **SG4. ✅ 01:58. Поставка.** Reference §10 — `report <ticket>` с отказами, exit 3 и швом
      `KAIF_GH` · нота шаблонов 2.5 (+1 = 9) и policy 2.5 (+1 = 6) в сборщике (INERT) · пересборка
      168/750 · `npm run test:core` 17/17 · `counters-guard` 50 OK.
- [x] **SG5. ✅ 02:00. Закрытие.** Судья ниже; решения ниже; план TM —
      `plans/85_epic82_TM_team_in_the_field_and_ci.md`; STATUS + `plans/82` + `MASTER_PLAN` §8.

## Лёгкий судья эпика (№48, 2026-09-04 02:00 +03:00) — VERIFIED WITH CAVEATS

| Заявление (критерий) | Что наблюдено |
|---|---|
| 1 команда существует и доставляет | s17: `✔ delivered: https://github.com/example-owner/example-kaif/issues/999`, строка тикета = URL, argv шима `issue create --repo example-owner/example-kaif --title "<H1>" --body-file …` |
| 2 четыре отказа названы | s17: anonymous exit 2 «stays LOCAL» · нет gh exit 2 «not on PATH» · noauth exit 2 · не тикет exit 1 · нет файла exit 1 · уже доставлен exit 0 без второго вызова |
| 3 трейлер авторства | s17: тело шима содержит «standing authorization (origin issue #15)» и кириллицу тикета |
| 4 навык говорит «запусти» | греп `kaif-core.mjs report` EN = 1; шаг 4 EN 25 → 14 строк; RU = 0 (классификатор — решение 3) |
| 5 поставка и документы | Reference §10 греп `report` ✓ · `build-framework` EXIT 0 · `counters-guard` 50 OK · help печатает `report ⚠` (s17 проверка 2) |
| 6 приёмка полем 16/16 | НЕ ПРОВЕРЯЕМО в истоке — ожидание в ответе на тикет (эпик RL) |
| 7 дерево зелёное | полигон 17/17 · `doc-header-lint` · `questions-guard` · `private-names-guard` · реестр пар — прогон на закрытии (см. коммит) |

**Оговорки (не блокеры):** живой `gh` командой не вызывался ни разу (первый живой вызов —
доставка следующего тикета из поля или из истока по слову владельца) · RU-копия шага 4 не
переписана (классификатор) · критерий 6 — только полем.

## Решения, принятые агентом без владельца

1. Доставка — действие команды по умолчанию, `--dry-run` — флаг (а не наоборот, как у
   `send-outbound` с `--apply`): у `send-outbound` гейт одобрения владельца, у `report` —
   стоячая авторизация #15; команда и есть тот allowlist-один-раз, о котором просило поле.
2. Дедуп против origin по `kaif-fp` отложен (сеть в полигоне не зовётся; идемпотентность по
   строке тикета закрывает повтор на одной машине).
3. **RU-копия шага 4 оставлена в форме 2.4** после отказа классификатора: обход (правка тем же
   текстом через shell) не делался намеренно — отказ пермишен-слоя исполняется, как велит сам
   шаг 4; EN-поставка (то, что едет полем) записана. Владелец может внести RU-фразу рукой:
   «origin — командой машинерии `node .kaif/kaif-core.mjs report <тикет>`…» (текст — в диффе
   EN-версии). Это ЖИВОЕ подтверждение гипотезы NDim +1 — в ответ на тикет (RL).
4. Таймаут `gh` — exit 3 (пятый исход сверх четырёх отказов плана): семантика «не доказано»
   по образцу SKIPPED=3 стражей (bugs/34).
5. Швы `KAIF_GH` / `KAIF_GH_TIMEOUT_MS` — переменные окружения, а не флаги команды: полигон
   получает подмену, а поверхность CLI для человека не растёт.

## Верификация наблюдением

Команда — на развёрнутой копии с подменным `gh` (живой `gh` в своде не зовётся: полигон не
совершает внешних действий); каждый отказ — своей фикстурой; красный свода — на HEAD-ядре до SG1
(`unknown command report`); навык — грепом обоих слоёв и замером длины шага 4 до/после; сборка и
полигон — прогоном, лог в файл (EXP-0052).

## Риски (ярусы Мёрфи)

- **(а)** Классификатор агентской системы реагирует на ПРЕДМЕТ, а не на слова (NDim +1) — даже
  «запусти команду» может не записаться на диск при обновлении. Смягчение: текст шага 4 сжат до
  минимума предмета; если поле снова откажет — следующий шаг 2.6: доставку зовёт хук/чекпоинт
  машинерии, а навык не упоминает доставку вовсе.
- **(б)** `gh issue create` от аккаунта владельца выглядит как СЛОВО владельца — трейлер
  авторства обязателен в теле (критерий 3), иначе нарушается ось «идентичность без автора»
  (охота судьи).
- **(б)** Дубли issue при повторном вызове — идемпотентность по строке `Delivered upstream:`
  (критерий 2); дедуп по `kaif-fp` против списка issues — НЕ в этом срезе (сетевой вызов
  списка — 2.6, если поле попросит).
- **(в)** Таймаут `gh` после фактической публикации — исход «неизвестно», не «отказ»; пользователю
  печатается команда ручной проверки (`gh issue list --repo … --search "<H1>"`).

## Решения, принятые агентом без владельца

1. Доставка — действие команды по умолчанию, `--dry-run` — флаг (а не наоборот, как у
   `send-outbound` с `--apply`): у `send-outbound` гейт одобрения владельца, у `report` —
   стоячая авторизация #15; команда и есть тот allowlist-один-раз, о котором просило поле.
2. Дедуп против origin по `kaif-fp` отложен (сеть в полигоне не зовётся; идемпотентность по
   строке тикета закрывает повтор на одной машине).

## Links

`plans/82` (критерий 2) · `researches/24` §2 (#37), §4 строка SG · NDim `bugs/KAIF/01` (донор
формулировки «навык говорит „запусти“», чтение) · `tools/send-outbound.mjs` (форма трёх исходов
дочернего вызова) · `framework/skills/report-bug/SKILL.md` шаг 4 и шаблоны A/B · `AGENT_GUIDE.md`
→ гейт авторизации (исключение CN1) · issue origin #15 (стоячая авторизация), #37.
