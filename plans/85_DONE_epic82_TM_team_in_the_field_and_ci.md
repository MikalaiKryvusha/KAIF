# План 85 — эпик TM «Команда в поле + CI»: adopt-путь, четыре состояния борда, борд вне git, `team-ci.yml`

> **Создан:** 2026-09-04 01:57 +03:00 (на закрытии эпика SG — канон N+1, №43).
> **Родитель:** `plans/82` (эпик TM; якорь — критерий приёмки 3: «`/team-deployment` несёт
> adopt-операцию · исключение менеджера · замки́ в стоп-ритуале · триггер „дремота“ · „at first
> launch“ · три практики · архетип `hardware-lab-small` · четыре роли состояний + пункт 7 `waiting` ·
> секцию „где живёт борд“ · шаблон `team-ci.yml`»).
> **Статус:** 🟡 в работе с 2026-09-04 02:03 +03:00 (сессия 48, защищённый цикл до 02:20 —
> TM0–TM6 до границы). Веха: 2026-09-04 09:11 +03:00 — TM7а (`6827489`): adopt-шаг вынесен в
> референс `team-adopt.md`, навык 178 строк при потолке 179; RU-обвязка навыка догнана до
> поставки. **✅ ЭПИК ЗАКРЫТ 2026-09-04 09:12 +03:00** — TM7: лёгкий судья VERIFIED WITH CAVEATS
> (таблица ниже), решения без владельца закрыты, план US — `plans/86`.
> **Вовне:** правки навыка `/team-deployment` и трёх его референсов (конституция · библиотека
> ролей · борд) плюс два новых референса (`team-ci-template.md`, `team-adopt.md`) — публичный
> контракт версии 2.5; новый шаблон `team-ci.yml` материализуется
> операцией 3 в проектах владельцев (файл в ИХ репозитории — только по слову владельца проекта,
> как и остальные артефакты операции 3); полевым проектам приезжает обновлением как дельты
> модулей.

## Вектор цели (Achieve)

**Боль** (источники — NDim `bugs/KAIF/02`, `03` (= issue #29, приказ владельца), KAGO
`bugs/KAIF/11`, `12`, issues #33, #34; сверка с HEAD — `researches/24` §2/§4, `researches/23`
§2): навык знает только greenfield — живая команда, из которой канон и был дистиллирован, не
имеет пути принятия и операция 3 велит переписать её конституцию шаблоном; нейминг-инвариант §1
ломается собственной таблицей на менеджере; библиотека ролей знает только web-архетипы, а
hardware-проект отвечал на вопросы о синглтоне без поддержки; борд знает два состояния и колонка
«Waiting for» никого ни к чему не обязывает (три места из шести стояли «busy», стоя́); борд —
tracked-файл, и дерево `main` грязное по построению (105 коммитов борда за 10 дней, 14 — только
чтобы очистить дерево); команда умножает пишущих, а CI с ней не едет — «main всегда зелёный»
держится дисциплиной, а не машиной (приказ владельца: «Должен поставлять вместе с деплойментом
команды»).

**Где хотим оказаться.** `/team-deployment` 2.5 принимает живую команду без перезаписи, его
инварианты исполнимы как написаны (исключение менеджера названо), борд знает четыре состояния
как РОЛИ с аудитом ждущих, живёт вне git со снимком в ретроспективу, а операция 3 материализует
`team-ci.yml` на один job из команд проекта (не более трёх шагов-гейтов) с изящной деградацией без GitHub.

## Готово, когда (критерии приёмки)

1. **[T1, NDim 02] Adopt-путь.** В навыке — операция (или именованный подпуть операции 2)
   «adopt: reconcile a live team with the canon»: инвентарь против девяти инвариантов
   конституции + контракта борда + библиотеки ролей + нейминг-инварианта → трёхведёрная дельта
   (matches / bring-to-canon / better-than-canon — последнее ведро = СИГНАЛ В ORIGIN, не дефект) →
   решение владельца ДО любого изменения → применение без перезаписи слов владельца; правило
   «двух слов владельца» на один параметр: слово владельца проекта побеждает, развилка явная
   (`FORK:`). Шапки трёх шаблонов несут строку «живая команда: adopt-путь, не копируй поверх».
   *Meter:* греп `adopt` в SKILL.md и во всех трёх референсах.
2. **[T2, KAGO 11] Исключение менеджера** в §1 конституции текстом после инварианта: место
   менеджера ЕСТЬ главная копия (каталог `<project>`, ветка `main`), форму
   `<project>-team-manager` несёт только адрес сессии; инструменты, выводящие роль из каталога,
   считают главную копию менеджером. *Meter:* греп `Exception — the manager` в §1.
3. **[T3, KAGO 12] Архетип `hardware-lab-small`** в библиотеке ролей (2–3 места: manager с
   единственным правом записи в устройство и правилом присутствия человека, engineer ×0–1 только
   без устройства, qa-verifier по журналам, никогда не трогая устройство) + фраза преамбулы
   «физический синглтон под тестом — ось размера»; донор — §0/`gpu-card` KAGO (чтение).
   *Meter:* греп `hardware-lab-small` в библиотеке и в операции 2 навыка.
4. **[T4–T7, NDim TD §4.2 / KAGO / NDim] Однострочники:** замки́ в стоп-ритуале §9
   (снять lock и строку «жду …» при остановке смены) · триггер ретроспективы «дремота с
   несмерженными ветками» в операции 5 · «first dispatch» → «at first launch» в операции 3 п. 5 ·
   три better-than-canon практики NDim в шаблоны (граница push-делегирования · ёмкость
   место ≠ слот · троттлинг повторной отправки). *Meter:* греп каждой формулировки по
   `framework/skills/team-deployment/**`.
5. **[#33] Четыре состояния борда как РОЛИ** (🟢 free · 🔴 busy · 🟡 blocked · ⚫ offline) с
   обязательством каждой + пункт 7 контракта борда: `audit-waiting` — инструмент перечисляет
   строки «Waiting for» старше порога и возвращает ненулевой exit-код, менеджер обязан
   отреагировать (референс-реализация NDim `auditWaiting`, 4 мутации — читать, форму брать).
   *Meter:* греп `blocked` · `offline` · `audit-waiting` в шаблоне борда; контракт — 7 пунктов.
6. **[#34] Где живёт борд:** секция в шаблоне борда — борд есть состояние сессии ВНЕ git
   (ignore-first, как `.kaif/refresh-marker.json`), снимок борда прикладывается к
   ретроспективе смены (операция 5), названный опт-аут для команд, которым нужен tracked-борд
   (с ценой: грязное дерево по построению). *Meter:* греп `gitignore` / `ignore-first` в
   шаблоне борда и в навыке; операция 3 п. 2 не велит коммитить борд.
7. **[CI, #29 — приказ владельца] `team-ci.yml`** в операции 3: новый референс
   `references/team-ci-template.md` (fenced YAML) — workflow на один job, не более трёх шагов-гейтов (push ролевых веток + PR в main;
   checkout · setup по стеку · install · тестовая/линт/typecheck команды ЧИТАЮТСЯ из
   `package.json`/канона сборки проекта, не хардкодятся); констрейнты тикета вслух: только
   дешёвые гейты (без секретов/эмуляторов/живого стенда), красный CI ролевой ветки блокирует
   merge как отсутствующий вердикт QA (строка в §5 конституции), не-GitHub remote получает тот же
   job документированным локальным pre-push скриптом. *Meter:* файл референса существует;
   греп `team-ci` в операции 3 и в §5 конституции; шаблон — валидный YAML (проба `node -e` с
   разбором или страж сборки).
8. **Поставка и дерево:** счётчики (`counters-guard`: новый референс — ось модулей/блоков
   бандла), `check-framework`, `npm run test:core`, ноты и policy 2.5 в сборщике (INERT), реестр
   пар; лёгкий судья эпика (№48) — критерии 1–7 пробами.

## Шаги

- [x] **TM0. ✅ 2026-09-04 02:03 +03:00 (сессия 48, защищённый цикл). Нулёвка актуализации
      входов (№76).** Сверено против HEAD: конституция §1 «Team map» · §4 борд · §5 git · §7
      синглтоны · §9 «Launch and stop»; библиотека — два архетипа (`web-product-small`,
      `web-product-medium`); контракт борда — 6 пунктов; операции 1–5 навыка; прочитаны NDim
      `bugs/KAIF/02`/`03`, KAGO `11`/`12` (чтение). **Замер ДО правки:** навык 153 строки ·
      конституция 166 · библиотека 156 · борд 69 (итого 544); семейство навыков n=37, медиана 74,
      Q3 100, хвост 145–213 (без `owner-reviews` 591). Потолок: навык ≤ 179 (`report-bug`, следующий
      в хвосте), референсы — рост ≤ +40 строк каждый; больше — выносить. NDim `auditWaiting` —
      прочитать в TM2.
- [x] **TM1. ✅ 2026-09-04 02:06 +03:00 (`adac2e9`). Конституция** 166 → 193 строк: §1 исключение
      менеджера (T2) · §5 «красный CI блокирует merge» + pre-push для не-GitHub (CI) и граница
      push-делегирования (T7а) · §2 троттлинг повторной отправки (T7в) · §7 ёмкость N lock-строк,
      место ≠ слот (T7б) · §9 замки́ и «Waiting for» в стоп-ритуале + свип менеджера (T4) · шапка
      «живая команда — adopt-путь» (T1).
- [x] **TM2. ✅ 2026-09-04 02:06 +03:00. Борд** 69 → 102 строки: таблица четырёх состояний-ролей с
      обязательствами места и менеджера (#33) · пункт 7 контракта `audit-waiting` (форма NDim
      `auditWaiting`: адрес по границе слова с учётом письменности, «адресат не назван» / «работа не
      ведётся» = тревога, exit ≠ 0) + команды `--blocked`, `audit-waiting`, N слотов · секция «Где
      живёт борд» (ignore-first · снимок в ретроспективу · именованный опт-аут с ценой) (#34) ·
      шапка adopt (T1).
- [x] **TM3. ✅ 2026-09-04 02:07 +03:00. Библиотека ролей** 156 → 178 строк: архетип
      `hardware-lab-small` (manager — единственное право записи в устройство под замком, engineer
      0–1 только без устройства, qa-verifier по журналам; требования к конституции — §0 правило
      устройства + замок в §7; анти-паттерны) · фраза преамбулы «физический синглтон — ось размера»
      (T3) · шапка adopt (T1).
- [x] **TM4. ✅ 2026-09-04 02:09 +03:00. Навык** 153 → **179 строк (= потолок, снято два раза по
      две строки — EXP-0053)**: операция 2 шаг 0 «adopt» с трёхведёрной дельтой и правилом двух
      слов (T1) · третий архетип в шаге 1 (T3) · операция 3: борд в `.gitignore` в том же
      движении + `audit-waiting` (#33/#34), «at first launch» (T6), новый шаг 6 «CI travels with
      the team» (CI) · операция 5: триггер «дремота» + снимок борда (T5, #34) · «Done when»: CI +
      борд вне git, at first launch, adopt без перезаписи.
- [x] **TM5. ✅ 2026-09-04 02:09 +03:00. Референс `references/team-ci-template.md`** (не `.yml`:
      сборщик встраивает в бандл только `.md` — проверено сборкой: `.yml` не дал нового блока,
      `.md` дал 169-й): fenced-блок YAML на один job / три гейта + пять констрейнтов + секция
      pre-push для не-GitHub. Проба валидности YAML — отложена в TM6 (парсера YAML в тулчейне
      нет; страж формы — греп `on:`/`jobs:`/отсутствие `secrets.` — кандидат в `check-framework`).
- [x] **TM6. ✅ 2026-09-04 02:13 +03:00 (`dca68d8`, build 414; чекбокс проставлен в TM7 — сессия
      48 закрылась по границе цикла). Поставка.** Ноты/policy 2.5 (10 / 7, INERT) · пересборка
      169/756 · `npm run test:core` 17/17 · `counters-guard` 50 OK · реестр пар · `doc-header-lint` 0.
- [x] **TM7. ✅ 2026-09-04 09:12 +03:00 (сессия 49). Закрытие.** (а) навык 180 → **178**: шаг 0
      adopt операции 2 → новый референс `references/team-adopt.md` (81 строка: когда действует ·
      инвентарь-таблица по 12 пунктам канона · три ведра · решение владельца с `FORK:` двух слов ·
      применение · done when · антипаттерны); в навыке — указатель с правилом двух слов, таблица
      шапки — пять референсов · пересборка **170 блоков / 764 модуля**, четыре зеркала счётчиков
      переписаны из вывода · `npm run test:core` 17/17 · `counters-guard` 50 OK (`6827489`,
      build 416); (б) RU-копия обвязки `.claude/skills/team-deployment/` догнана до поставки
      (SKILL.md + три референса + RU `team-ci-template.md` и `team-adopt.md`; состав референсов
      совпадает по всем трём навыкам с `references/`) — реестр пар получил строку «состав
      референсов навыка ↔ обвязка» (доказана красным на фикстуре), урок EXP-0103; (в) судья и
      решения — ниже; (г) план US — `plans/86`; (д) `plans/85` → DONE, `plans/82` веха,
      `MASTER_PLAN` §8 метрика 3 из 8, STATUS.

## Лёгкий судья эпика (№48, 2026-09-04 09:12 +03:00) — VERIFIED WITH CAVEATS

| Заявление (критерий) | Что наблюдено грепом по `framework/skills/team-deployment/**` (переисполнено в TM7) |
|---|---|
| 1 adopt-путь | `adopt`: навык 4 · референс `team-adopt.md` 2 · конституция 2 · библиотека 1 · борд 1; `FORK:` навык 1 + референс 1 (шаг 0 операции 2 — указатель с правилом двух слов; процедура — в референсе) |
| 2 исключение менеджера | `Exception — the manager` = 1 в §1 |
| 3 `hardware-lab-small` | библиотека 2 · навык 1 (операция 2 шаг 1) |
| 4 однострочники T4–T7 | `RELEASES every lock` 1 (§9) · `dormancy` 1 (операция 5) · `at first launch` 1 (операция 3 п. 5) · `Push delegation has an explicit boundary` 1 (§5) · `Capacity is N lock rows` 1 (§7) · `Re-send throttle` 1 (§2) |
| 5 четыре состояния + пункт 7 | борд: `blocked` 4 · `offline` 2 · `audit-waiting` 3 · пунктов контракта 7 |
| 6 борд вне git | борд `gitignore/ignore-first` 1 (секция) · навык `.gitignore` 1 (операция 3 п. 2) |
| 7 `team-ci` | навык 3 (таблица шапки, операция 3 п. 6, Done when) · конституция §5 1 · референс `team-ci-template.md` есть; форма fenced-блока: `on:` 1 · `jobs:` 1 · `secrets.` 0 |
| 8 поставка | `build-framework` EXIT 0 (170 блоков / 764 модуля) · `npm run test:core` 17/17 (лог в файл, сессия 49) · `counters-guard` 50 OK · `doc-header-lint` 0 · `questions-guard` 0 новых · `private-names-guard` OK (в `team-adopt.md` имён проектов 0) · стилометрия совпадает · контур 44/0 · пары fable / молитв / версии / `lang-packs-guard` GREEN |
| размеры против потолков TM0 | навык **178 ≤ 179** · конституция 193 (+27) · борд 102 (+33) · библиотека 178 (+22) — все ≤ +40 · новые референсы: CI 48, adopt 81 (потолка нет — новые файлы) |

**Оговорки (не блокеры):** YAML-валидность референса CI — только по форме (парсера YAML в
тулчейне нет; `on:`/`jobs:`/без `secrets.`); живая приёмка (adopt на реальной команде, CI на
реальном репо) — полем после релиза, доноры NDim/KAGO; RU-копия навыка в обвязке — 180 строк
(переводная копия вне семейства замера EXP-0053: потолок 179 задан для поставки, RU длиннее EN на
1 %).

## Решения, принятые агентом без владельца

1. CI едет ОДНИМ референсом-шаблоном + пунктом операции 3, а не отдельным навыком (KISS; приказ
   владельца — «вместе с деплойментом команды»).
2. Состояния борда — четыре, символы 🟢🔴🟡⚫ (третий и четвёртый — по словам владельца ×3 в
   #33; точные глифы — предложение агента).
3. Референс CI — `team-ci-template.md` с fenced-блоком, а не `.yml`: сборщик встраивает только
   `.md` (наблюдено сборкой), расширение эмбеддера — лишняя машинерия ради одного файла.
4. Adopt — шаг 0 операции 2, а не шестая операция: навык на потолке хвоста семейства (179),
   новая операция раздула бы его; правило двух слов владельца — через `FORK:` (CN2).
5. Борд по умолчанию ВНЕ git (ignore-first) — смена дефолта 2.4 объявлена policy-изменением 2.5
   в сборщике; опт-аут именован.
6. На закрытии adopt-шаг вынесен в отдельный референс `team-adopt.md` (навык стоял на 180 при
   потолке 179 после строки четвёртого референса; EXP-0053 — выносить, не поднимать): в навыке
   остаётся указатель с правилом двух слов как `FORK:`, процедура — в референсе; критерий 1
   («операция или именованный подпуть») исполнен подпутём плюс референсом.
7. RU-копия обвязки `.claude/skills/team-deployment/` синхронизирована с поставкой в рамках
   закрытия эпика, хотя план этого шага не называл (канон «оба слоя двигаются вместе»); реестр пар
   получил строку сверки СОСТАВА референсов — тексты переводных копий побайтно не сверяются
   (перевод от смысла), пропавший файл референса ловится списком.
8. RU-копия навыка — 180 строк: потолок 179 задан для семейства поставки (EXP-0053, n=37 в
   `framework/skills/`), переводная копия в замер не входила — не ужималась.

## Верификация наблюдением

Каждый пункт — грепом по `framework/skills/team-deployment/**` (строки названы в критериях);
YAML-шаблон — разбором; сборка и полигон — прогоном, лог в файл; счётчики — из вывода
инструментов. Живая приёмка (adopt на реальной команде, CI на реальном репо) — только полем после
релиза: NDim и KAGO — доноры и первые пользователи (ответ на их тикеты — эпик RL).

## Риски (ярусы Мёрфи)

- **(а)** Навык раздуется за хвост семейства (153 строки сегодня) — adopt-операция и CI-пункт
  выносятся в референсы, в навыке остаются команда и указатель (EXP-0053).
- **(б)** `team-ci.yml` с хардкодом команд стека — шаблон читает `package.json`/канон сборки и
  честно оставляет `<…>` там, где стек неизвестен; операция 3 велит заполнить, не угадывать.
- **(б)** Четыре состояния как роли ломают существующие борд-инструменты поля (NDim, KAGO) —
  adopt-путь классифицирует их как better-than-canon/bring-to-canon, слово владельца проекта.
- **(в)** Материализация файла в `.github/` чужого репозитория — внешний по духу артефакт: как и
  остальные артефакты операции 3, по слову владельца проекта (гейт операции 2 уже стоит).

## Links

`plans/82` (критерий 3) · `researches/24` §2 (#33, #34), §4 строки FT/CI · `researches/23` §2
(T4–T7) · `ideas/27` пп. 8, 11 · NDim `bugs/KAIF/02`, `03` · KAGO `bugs/KAIF/11`, `12` · issues
origin #29, #33, #34 · `framework/skills/team-deployment/SKILL.md` + `references/*`.
