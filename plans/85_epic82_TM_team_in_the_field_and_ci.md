# План 85 — эпик TM «Команда в поле + CI»: adopt-путь, четыре состояния борда, борд вне git, `team-ci.yml`

> **Создан:** 2026-09-04 01:57 +03:00 (на закрытии эпика SG — канон N+1, №43).
> **Родитель:** `plans/82` (эпик TM; якорь — критерий приёмки 3: «`/team-deployment` несёт
> adopt-операцию · исключение менеджера · замки́ в стоп-ритуале · триггер „дремота“ · „at first
> launch“ · три практики · архетип `hardware-lab-small` · четыре роли состояний + пункт 7 `waiting` ·
> секцию „где живёт борд“ · шаблон `team-ci.yml`»).
> **Статус:** 🔲 ожидает старта — первый чат после закрытия SG (`plans/84`); старт — нулёвка
> актуализации входов (№76).
> **Вовне:** правки навыка `/team-deployment` и трёх его референсов (конституция · библиотека
> ролей · борд) — публичный контракт версии 2.5; новый шаблон `team-ci.yml` материализуется
> операцией 3 в проектах владельцев (файл в ИХ репозитории — только по слову владельца проекта,
> как и остальные артефакты операции 3); полевым проектам приезжает обновлением как дельты
> модулей.

## Вектор цели (Achieve)

**Боль** (источники — NDim `bugs/KAIF/02`, `03` (= issue #29, приказ владельца), KAGO
`bugs/KAIF/11`, `12`, issues #33, #34; сверка с HEAD — `researches/24` §2/§4, `researches/23`
§2): навык знает только greenfield — живая команда, из которой канон и был дистиллирован, не
имеет пути принятия и операция 3 велит переписать её конституцию шаблоном; нейминг-инвариант §1
ломается собственной таблицей на менеджере; библиотека ролей знает только web-архетипы, а
hardware-проект отвечал на вопросы о синглтоне без поддержки; борд знает два состояния и колонка
«Waiting for» никого ни к чему не обязывает (три места из шести стояли «busy», стоя́); борд —
tracked-файл, и дерево `main` грязное по построению (105 коммитов борда за 10 дней, 14 — только
чтобы очистить дерево); команда умножает пишущих, а CI с ней не едет — «main всегда зелёный»
держится дисциплиной, а не машиной (приказ владельца: «Должен поставлять вместе с деплойментом
команды»).

**Где хотим оказаться.** `/team-deployment` 2.5 принимает живую команду без перезаписи, его
инварианты исполнимы как написаны (исключение менеджера названо), борд знает четыре состояния
как РОЛИ с аудитом ждущих, живёт вне git со снимком в ретроспективу, а операция 3 материализует
`team-ci.yml` на один job из команд проекта (не более трёх шагов-гейтов) с изящной деградацией без GitHub.

## Готово, когда (критерии приёмки)

1. **[T1, NDim 02] Adopt-путь.** В навыке — операция (или именованный подпуть операции 2)
   «adopt: reconcile a live team with the canon»: инвентарь против девяти инвариантов
   конституции + контракта борда + библиотеки ролей + нейминг-инварианта → трёхведёрная дельта
   (matches / bring-to-canon / better-than-canon — последнее ведро = СИГНАЛ В ORIGIN, не дефект) →
   решение владельца ДО любого изменения → применение без перезаписи слов владельца; правило
   «двух слов владельца» на один параметр: слово владельца проекта побеждает, развилка явная
   (`FORK:`). Шапки трёх шаблонов несут строку «живая команда: adopt-путь, не копируй поверх».
   *Meter:* греп `adopt` в SKILL.md и во всех трёх референсах.
2. **[T2, KAGO 11] Исключение менеджера** в §1 конституции текстом после инварианта: место
   менеджера ЕСТЬ главная копия (каталог `<project>`, ветка `main`), форму
   `<project>-team-manager` несёт только адрес сессии; инструменты, выводящие роль из каталога,
   считают главную копию менеджером. *Meter:* греп `Exception — the manager` в §1.
3. **[T3, KAGO 12] Архетип `hardware-lab-small`** в библиотеке ролей (2–3 места: manager с
   единственным правом записи в устройство и правилом присутствия человека, engineer ×0–1 только
   без устройства, qa-verifier по журналам, никогда не трогая устройство) + фраза преамбулы
   «физический синглтон под тестом — ось размера»; донор — §0/`gpu-card` KAGO (чтение).
   *Meter:* греп `hardware-lab-small` в библиотеке и в операции 2 навыка.
4. **[T4–T7, NDim TD §4.2 / KAGO / NDim] Однострочники:** замки́ в стоп-ритуале §9
   (снять lock и строку «жду …» при остановке смены) · триггер ретроспективы «дремота с
   несмерженными ветками» в операции 5 · «first dispatch» → «at first launch» в операции 3 п. 5 ·
   три better-than-canon практики NDim в шаблоны (граница push-делегирования · ёмкость
   место ≠ слот · троттлинг повторной отправки). *Meter:* греп каждой формулировки по
   `framework/skills/team-deployment/**`.
5. **[#33] Четыре состояния борда как РОЛИ** (🟢 free · 🔴 busy · 🟡 blocked · ⚫ offline) с
   обязательством каждой + пункт 7 контракта борда: `audit-waiting` — инструмент перечисляет
   строки «Waiting for» старше порога и возвращает ненулевой exit-код, менеджер обязан
   отреагировать (референс-реализация NDim `auditWaiting`, 4 мутации — читать, форму брать).
   *Meter:* греп `blocked` · `offline` · `audit-waiting` в шаблоне борда; контракт — 7 пунктов.
6. **[#34] Где живёт борд:** секция в шаблоне борда — борд есть состояние сессии ВНЕ git
   (ignore-first, как `.kaif/refresh-marker.json`), снимок борда прикладывается к
   ретроспективе смены (операция 5), названный опт-аут для команд, которым нужен tracked-борд
   (с ценой: грязное дерево по построению). *Meter:* греп `gitignore` / `ignore-first` в
   шаблоне борда и в навыке; операция 3 п. 2 не велит коммитить борд.
7. **[CI, #29 — приказ владельца] `team-ci.yml`** в операции 3: новый референс
   `references/team-ci-template.yml` — workflow на один job, не более трёх шагов-гейтов (push ролевых веток + PR в main;
   checkout · setup по стеку · install · тестовая/линт/typecheck команды ЧИТАЮТСЯ из
   `package.json`/канона сборки проекта, не хардкодятся); констрейнты тикета вслух: только
   дешёвые гейты (без секретов/эмуляторов/живого стенда), красный CI ролевой ветки блокирует
   merge как отсутствующий вердикт QA (строка в §5 конституции), не-GitHub remote получает тот же
   job документированным локальным pre-push скриптом. *Meter:* файл референса существует;
   греп `team-ci` в операции 3 и в §5 конституции; шаблон — валидный YAML (проба `node -e` с
   разбором или страж сборки).
8. **Поставка и дерево:** счётчики (`counters-guard`: новый референс — ось модулей/блоков
   бандла), `check-framework`, `npm run test:core`, ноты и policy 2.5 в сборщике (INERT), реестр
   пар; лёгкий судья эпика (№48) — критерии 1–7 пробами.

## Шаги

- [ ] **TM0. Нулёвка актуализации входов (№76).** Сверить против HEAD: заголовки трёх
      референсов (§1/§4/§5/§7/§9 конституции · архетипы библиотеки · контракт борда 6 пунктов),
      операции 1–5 навыка; прочитать NDim `bugs/KAIF/02`/`03`, KAGO `11`/`12`, `researches/23`
      §2 (T4–T7), NDim `auditWaiting` (чтение — форму, не код); `wc -l` навыка и референсов ДО
      правки (EXP-0053: потолок числом до работы — навык 153 строки, референсы по замеру).
- [ ] **TM1. Конституция.** §1 исключение менеджера (T2) · §5 строка про красный CI (CI) ·
      §9 замки́ в стоп-ритуале (T4) · шапка «живая команда: adopt-путь» (T1) · три практики
      NDim там, где их место (§2/§5/§8) (T7).
- [ ] **TM2. Борд.** Четыре состояния-роли + пункт 7 `audit-waiting` (#33) · секция «где живёт
      борд» + опт-аут (#34) · шапка adopt (T1).
- [ ] **TM3. Библиотека ролей.** Архетип `hardware-lab-small` + фраза преамбулы (T3) · шапка
      adopt (T1).
- [ ] **TM4. Навык.** Операция «adopt» + правило двух слов (T1) · операция 2 называет третий
      архетип (T3) · операция 3: «at first launch» (T6), борд не коммитится (#34), п. 6
      материализация `team-ci.yml` с деградацией (CI) · операция 5: триггер «дремота» (T5) и
      снимок борда (#34). Замер длины ПОСЛЕ — навык не должен уехать за хвост семейства
      (`wc -l` по всем 37 навыкам, EXP-0053).
- [ ] **TM5. Референс `team-ci-template.yml`** + проба валидности YAML в полигоне или
      `check-framework` (страж: файл разбирается, содержит `on:`/`jobs:`, не содержит
      `secrets.`).
- [ ] **TM6. Поставка.** Ноты/policy 2.5 · пересборка · `npm run test:core` · `counters-guard`
      (новые числа — из вывода) · реестр пар · `doc-header-lint`.
- [ ] **TM7. Закрытие.** Лёгкий судья (критерии 1–8 пробами); «Решения без владельца»; план
      следующего эпика US (`plans/86_epic82_US_*`, канон N+1); STATUS + `plans/82` +
      `MASTER_PLAN` §8 (метрика: критерий 3).

## Верификация наблюдением

Каждый пункт — грепом по `framework/skills/team-deployment/**` (строки названы в критериях);
YAML-шаблон — разбором; сборка и полигон — прогоном, лог в файл; счётчики — из вывода
инструментов. Живая приёмка (adopt на реальной команде, CI на реальном репо) — только полем после
релиза: NDim и KAGO — доноры и первые пользователи (ответ на их тикеты — эпик RL).

## Риски (ярусы Мёрфи)

- **(а)** Навык раздуется за хвост семейства (153 строки сегодня) — adopt-операция и CI-пункт
  выносятся в референсы, в навыке остаются команда и указатель (EXP-0053).
- **(б)** `team-ci.yml` с хардкодом команд стека — шаблон читает `package.json`/канон сборки и
  честно оставляет `<…>` там, где стек неизвестен; операция 3 велит заполнить, не угадывать.
- **(б)** Четыре состояния как роли ломают существующие борд-инструменты поля (NDim, KAGO) —
  adopt-путь классифицирует их как better-than-canon/bring-to-canon, слово владельца проекта.
- **(в)** Материализация файла в `.github/` чужого репозитория — внешний по духу артефакт: как и
  остальные артефакты операции 3, по слову владельца проекта (гейт операции 2 уже стоит).

## Решения, принятые агентом без владельца

1. CI едет ОДНИМ референсом-шаблоном + пунктом операции 3, а не отдельным навыком (KISS; приказ
   владельца — «вместе с деплойментом команды»).
2. Состояния борда — четыре, символы 🟢🔴🟡⚫ (третий и четвёртый — по словам владельца ×3 в
   #33; точные глифы — предложение агента).

## Links

`plans/82` (критерий 3) · `researches/24` §2 (#33, #34), §4 строки FT/CI · `researches/23` §2
(T4–T7) · `ideas/27` пп. 8, 11 · NDim `bugs/KAIF/02`, `03` · KAGO `bugs/KAIF/11`, `12` · issues
origin #29, #33, #34 · `framework/skills/team-deployment/SKILL.md` + `references/*`.
