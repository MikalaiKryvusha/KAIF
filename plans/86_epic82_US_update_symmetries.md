# План 86 — эпик US «Симметрии обновления»: детерминизм wholesale-вердикта, неделимые якорные блоки, честное задание обновления

> **Создан:** 2026-09-04 09:12 +03:00 (на закрытии эпика TM — канон N+1, №43).
> **Родитель:** `plans/82` (эпик US; якорь — критерий приёмки 4: «Машинерия: вердикт
> translated-wholesale печатается по файлу с числами и детерминирован на байт-идентичном дереве ·
> якорный блок приезжает целиком или уходит в задание · `stale-claims` эмитится на каждом
> версионном обновлении, с телом `no lines found` при пустом скане · диффы модулей с baseline по
> умолчанию · английские новинки — пунктом задания · `stale-claims` по скриптам проекта ·
> преемник в deprecation и счёт неубранных · счёт языковой смеси в `check` · `project-name`
> предупреждает до акта · смешанный EOL по доминированию»).
> **Статус:** 🔲 написан 2026-09-04 09:12 +03:00; старт — нулёвка US0 в следующем чате.
> **Вовне:** правки машинерии поставки (`framework/installer/KAIF-CORE.mjs`) — публичный контракт
> обновления 2.5; полевым проектам приезжает ядром при обновлении 2.4→2.5; ответы на issues
> #27/#31/#32 — эпик RL после релиза (№84).

## Вектор цели (Achieve)

**Боль** (источники — issues #27, #28 §2 R1–R5, #31, #32 R-A–R-D, KAGO `bugs/KAIF/10` (чтение);
сверка с HEAD — `researches/24` §2/§3, `researches/23` §2/§2а, `ideas/27` п. 10, `plans/73` U2):
обновление на i18n-развёртываниях недетерминировано — два прогона по байт-идентичному дереву
классифицировали `AGENT_GUIDE.md` по-разному (sandbox: 20 wholesale, kept intact; live: 19,
«11 modules merged in-place»), и живой прогон дописал 416 строк английского канона в конец
русского гайда (~2 ч перевода), обесценив саму репетицию; `mergeModules` вставляет якорный блок
молитвы без парности маркеров (`KAIF:PRAYER:END` без `BEGIN`; у KAGO — приезд ВНУТРЬ
локализованной пары), и `/resume` не имеет что произносить; пункт `stale-claims` задания молча
исчезает между прогонами, а отсутствующий пункт неотличим от «ничего не найдено»; для
переведённых развёртываний диффы — вся доставка, но задание не даёт готовой команды диффа на
diverged-файл, молчит о новых английских навыках при `language: ru` и не сканирует скрипты
проекта на пин старой версии; генератор пункта `placeholders` называет цитаты летописи местами
заполнения, а гейт цитаты пропускает — асимметрия сменила сторону; deprecation не называет
преемника, сводка не считает неубранные; `project-name` предупреждает о non-ASCII argv после
принятия значения; `writeMatchingEol` судит по наличию CRLF, а не по доминированию; smoke-строка
README хуков виснет на stdin.

**Где хотим оказаться.** Вердикт по каждому файлу-кандидату i18n-развёртывания ПЕЧАТАЕТСЯ с
числами и одинаков на байт-идентичном дереве (при расхождении с репетицией файл замораживается и
уходит в задание); якорные блоки `KAIF:*:BEGIN/END` — неделимые единицы: приезжают целиком в
заявленной позиции или целиком уходят в задание; задание обновления — самодостаточный носитель
(безусловный `stale-claims`, команда диффа на diverged-файл с baseline по умолчанию, английские
новинки пунктом, скрипты в скане, преемник у deprecation, счёт неубранных); `check` считает
языковую смесь; `project-name` предупреждает ДО акта; EOL — по доминированию. Каждый фикс — со
сводом полигона, красным на до-фиксовом поведении.

## Готово, когда (критерии приёмки)

1. **[P1, #27/#32 R-B] Детерминизм wholesale-вердикта + печать решения.** Классификация update
   печатает по каждому файлу-кандидату на wholesale (baseN > 2) строку
   `⟳ <файл>: baseFound N of M, ceiling K → frozen | merged` (числа из `WHOLESALE_SURVIVOR_SHARE`);
   два прогона `update --dry-run` по байт-идентичной копии дают один вердикт по каждому файлу;
   расхождение вердикта с репетицией (вердикт из квитанции репетиции или `--rehearsal <dir>`)
   замораживает файл и выносит его пунктом задания, а не мержит. Причина односторонней потери
   (легаси-baseline 1.6 / переход anonymous→origin, #32 R-B) — найдена и закрыта либо названа в
   документе бага с вердиктом «не воспроизводится в полигоне» и адресом 2.6. *Meter:* свод
   полигона: два прогона на одной копии → диффы выводов пусты; фикстура с синтетическим baseline
   даёт печать чисел по каждому кандидату; мутант с отключённой заморозкой краснит ассерт.
2. **[P2, #27 + KAGO R2] Якорные блоки неделимы.** Сплиттер и мерж знают маркеры
   `<!-- KAIF:<NAME>:BEGIN -->`…`<!-- KAIF:<NAME>:END -->`: модуль, содержащий один маркер пары,
   расширяется до целой пары — либо пара целиком уходит в задание с диффом; в дерево никогда не
   пишется `END` без `BEGIN` и не вставляется текст ВНУТРЬ локализованной пары; `check` краснит
   документ с непарным маркером. *Meter:* свод: фикстура «переведённый гайд с локализованной парой
   молитвы» → после update пара парна и не удвоена; фикстура «END без BEGIN» → `check` красный;
   красный до фикса на HEAD-ядре.
3. **[#31] `stale-claims` безусловный.** Пункт задания эмитится на КАЖДОМ версионном обновлении;
   при пустом скане тело — `no lines found` с названной старой версией (счёт файлов скан не
   ведёт — правка критерия по факту US4а); чекпоинт `stale-claims` перепрогоняет скан, как
   сейчас. *Meter:* свод: обновление без клеймов → пункт есть с телом `no lines found`; красный на
   HEAD (пункт отсутствует).
4. **[срез U2 — `plans/73` U2 пп. 1, 2, 4] Задание — самодостаточный носитель.** (а) для
   diverged/translated-файла — строка с точным путём апстрима и ГОТОВОЙ командой диффа с baseline
   по умолчанию (без ручного поиска `--baseline`); (б) при `language ≠ en` — пункт «эти N файлов
   приехали английскими — переведи, если проект переводит wholesale» со списком; (в) `stale-claims`
   сканирует и скрипты проекта (`*.mjs` · `*.js` · `*.py` · `*.sh` · `scripts` в `package.json`)
   на пин старой версии. *Meter:* свод-греп по сгенерированному `KAIF_UPDATE_TASK.md`: три
   свойства; красный на HEAD.
5. **[точечные] U2 quote-skip · U4/U5 deprecation · P3 · P4 · P5 · P6 · H10.** (а) один общий
   предикат quote-skip у генератора пункта `placeholders` и у гейта placeholders (KAGO
   `bugs/KAIF/10`: цитата летописи не называется местом заполнения); (б) deprecation называет
   преемника (`{ path, reason, successor }`), сводка печатает счёт неубранных deprecation; (в)
   `check` печатает счёт языковой смеси при `language ≠ en` («N of M skills are English») —
   предупреждение, не отказ; (г) переход anonymous→origin выносит пунктом задания список файлов,
   чей текст кодирует старый режим (`kaif-version`, `kaif-remove`); (д) `project-name` предупреждает
   о non-ASCII argv ДО принятия значения, пункт задания называет `--name-file`; (е) `< /dev/null` в
   smoke-строке README модуля хуков; (ж) `writeMatchingEol` — по доминированию (CRLF ≥ LF → CRLF).
   *Meter:* свод или ассерт на каждый пункт, красный на HEAD там, где меняется поведение
   (а, б, в, г, д, ж); (е) — греп README.
6. **Поставка и дерево.** Ноты и policy 2.5 в сборщике (INERT): строка эпика US; `KAIF_REFERENCE`
   §10 обновлён (печать вердикта, неделимые блоки, безусловный `stale-claims`);
   `node tools/build-framework.mjs` EXIT 0 · `npm run test:core` зелёный с новым сводом s18 (плюс
   правки s07/s10 там, где класс уже крыт) · `counters-guard` (число сводов — из вывода) · реестр
   пар · `doc-header-lint`; лёгкий судья эпика (№48) — критерии 1–5 пробами.

## Шаги

- [x] **US0. ✅ 2026-09-04 09:24 +03:00 (сессия 49, защищённый цикл до 10:00). Нулёвка
      актуализации входов (№76).** Сверено против HEAD `9c2fe5e`: `mergeModules` :1060–1197
      (сеть wholesale :1089–1093, `WHOLESALE_SURVIVOR_SHARE` :261 = 0.15; строка лога о wholesale
      :1324 — БЕЗ чисел; когда файл НЕ wholesale — строки нет вовсе → P1-печать подтверждена) ·
      вставка новых модулей :1160–1196 «по порядку шаблона» — модуль вставляется целиком, а
      маркер `KAIF:PRAYER:BEGIN` живёт в H1-модуле шаблона (`framework/AGENT_GUIDE.md`: BEGIN
      перед заголовком `## 🙏`, END — последняя строка модуля молитвы): diverged H1 + новый модуль
      молитвы = END без BEGIN → механизм P2 подтверждён чтением · классификация :1255–1364 ·
      синтетический baseline :990–1035 (`--baseline <dir|url>`, версия артефакта обязана равняться
      версии развёртывания; `stableValues()` из манифеста режет baseline «как деплой») ·
      `scanStaleClaims` :760–840 (walk — только `*.md`; `package.json`/скрипты не сканируются →
      U2в подтверждён) и `writeUpdateTask` :901 `if (staleClaims.length)` → #31 подтверждён ·
      `handleDeprecations` :848 — `{ path, reason }` без преемника, сводка печатает только
      `removed` → U4/U5 подтверждены · `readOwnerText` :2570 предупреждает о non-ASCII, но
      принимает значение; пункты задания :698/:896 называют только argv-форму → P5 подтверждён ·
      `writeMatchingEol` :1051 — `includes('\r\n')` → H10 подтверждён · `check` :2400–2470 — нет
      счёта языковой смеси (регексы `SCRIPTS` :203 есть у `moduleAudit` :1954) → P3 подтверждён ·
      генератор `placeholders` — `unresolvedOnDisk` :557 / `fmtSlots`, гейт — `scanPlaceholders`
      :1653 (KAGO 10: два сканера, один предикат нужен) · сплиттер `tools/module-map-lib.mjs`
      (106 строк) ↔ `splitModules` ядра :168 — пин в `check-framework`. Прочитаны: Prompt Modding
      §2 R1–R5 + §4 пожелания 1–5; QA_Engineer §2 R-A–R-D + §4 + поправка к R-A («sandbox = live +
      stale-claims, одно направление»); KAGO `bugs/KAIF/10` (чтение). **Гипотеза для US3** (P1):
      репетиция шла на git-archive-копии — в ней нет игнорируемых файлов состояния, и
      `stableValues()`/маркер могли отличаться от боевого дерева → синтетический baseline режется
      с другими значениями → `baseFound` отличается на 1 и пересекает потолок; проверяется
      фикстурой «копия без `.kaif/deploy-manifest.json` values». **Замер ДО:** `KAIF-CORE.mjs`
      2933 строки · сводов 17 · проверок s07 = 56, s10 = 30, s02 = 43. Правка плана по фактам:
      критерий 1 уточнён — печать только для кандидатов (baseN > 2); критерий 5(в) — форма строки
      `⚠ language mix: N of M skills are English (language: ru)`.
- [x] **US1. ✅ 2026-09-04 09:29 +03:00. Свод `tools/sandbox/s18-update-symmetries.mjs` написан
      ДО фиксов** (EXP-0016/0017; `[NOT-TESTED]` в шапке, в `SUITES` не вписан до зелёного).
      Фикстуры: v1-манифест + `--baseline` старой версии БЕЗ молитвы + диверджен H1 (U1) ·
      переведённый навык на ru-развёртывании (U2/U3/U4/U8) · `package.json` с пином старой
      версии + LF-файл с одной CRLF-строкой (U5/U7) · ru-развёртывание + новый английский навык в
      9.9 (U6). **Красный прогон на HEAD `9c2fe5e` — ровно по предсказаниям: 9 красных**
      (U1 `PRAYER BEGIN=0 END=1` — полевой дефект #27 R1b воспроизведён буквально · U2 нет
      чисел · U4 ×2 пункта нет · U8 нет команды · U5 `package.json` не сканируется · U7 `CRLF 218 ·
      LF 0` — файл целиком стал CRLF · U6 ×2), U3 зелёный по построению (страж регрессии). Лог —
      скретчпад сессии `s18-red1.log`; при US6 свод вписывается в `SUITES` и становится 18-м.
- [ ] **US2. P2 — якорные блоки** (критерий 2). `FORK: options <A: маркер-пара = собственный модуль
      в splitModules обоих сплиттеров | B: правило неделимости в mergeModules без смены сплиттера> ·
      price of error <A: меняются сигнатуры модулей AGENT_GUIDE — на первом обновлении 2.4→2.5
      снимок манифеста не узнаёт молитву, изменение директивы (CN6, каденция) уезжает диффом
      вместо механической замены, а H1 даёт ложный дифф; B: пара остаётся невидимой карте модулей,
      неделимость держится только в точке вставки/замены> · consulted <#27 §4 п. 2 (пожелание
      поля: «в заявленной позиции — или уходит в задание»), KAGO R2 (приезд внутрь пары),
      инвариант карты модулей №16 «сигнатурные якоря, тегов в документах не появляется»,
      PHILOSOPHY → Оккам>` → **решено B** (ничего не меняет в адресах модулей и манифестах поля).
      (а) ✅ 2026-09-04 09:32 +03:00 — `check` краснит документ с непарным маркером:
      `unpairedAnchors()` в ядре (fence-aware; END без BEGIN, BEGIN внутри открытой пары, счёт не
      сходится), блок `@guard anchor-parity`; свод s18 U9 ×3 зелёный (красный на END без BEGIN ·
      зелёный на чистой установке · маркер в code fence не считается), остальные 9 стражей s18 —
      по-прежнему красные до фиксов (б) и US3–US5;
      (б) `mergeModules`: модуль с несбалансированным маркером применяется ТОЛЬКО вместе со всеми
      модулями-носителями той же пары (двухфазно: план действий → пары, чей носитель не
      применяется, откатываются в keep + один пункт задания с диффом ВСЕЙ пары); точка вставки
      внутри открытой пары на диске переносится за модуль, закрывающий пару (KAGO R2).
- [ ] **US3. P1 — детерминизм + печать вердикта** (критерий 1): печать чисел по кандидату;
      сравнение с репетицией/квитанцией; заморозка при расхождении; охота за причиной по #32 R-B
      (легаси-baseline) — результат в `bugs/` или секцию плана; релиз не блокирует (фолбэк принят
      `plans/82`).
- [ ] **US4. Задание обновления** (критерии 3, 4): (а) ✅ 2026-09-04 09:38 +03:00 — безусловный
      `stale-claims` (#31): пункт эмитится при любой смене версии (`if (fromVersion)`), пустой
      скан — телом `no lines found — the scan for claims of the OLD version (X) ran over the tree
      and found nothing…`; s18 U4 ×2 зелёный, полигон 17/17 · (б) ✅ 2026-09-04 09:48 +03:00 —
      у wholesale-файла в пункте `merge-diverged` путь апстрима и готовая команда
      `git diff v<from> v<to> -- <src>`: карта dest → src едет в мета-блоке бандла (`sources`,
      сборщик перерендеривает мета-блок после сбора записей; ключи отсортированы), старый бандл без
      карты деградирует до прежней ноты; s18 U8 зелёный, полигон 17/17 · (в) ✅ 2026-09-04 09:52
      +03:00 — пункт `language-arrivals`: новые `.md` релиза без письменности владельца на
      не-английском развёртывании перечисляются в задании (`addedPaths` из классификации;
      `languageArrivalsOf` — оба маршрута, core-update и bootstrap, TWINS: 2 вызова
      `writeUpdateTask`); s18 U6а зелёный, полигон 17/17 · (г) скрипты проекта в скане (s18 U5) —
      следующий чат.
- [ ] **US5. Точечные** (критерий 5): quote-skip предикат · преемник и счёт deprecation · (в) ✅
      2026-09-04 09:45 +03:00 — `check` печатает `⚠ language mix: N of M skills are English
      (language: ru)` по телу навыков (регекс `SCRIPTS`, преамбула не судится); предупреждение, не
      отказ; s18 U6б зелёный, полигон 17/17 · файлы старого режима при anonymous→origin ·
      `project-name` до акта · (е) ✅ 2026-09-04 09:38 +03:00 — `< /dev/null` в smoke-строке `framework/hooks/README.md`
      с объяснением, почему (греп) · (ж) ✅ 2026-09-04 09:41 +03:00 — `writeMatchingEol` по
      ДОМИНИРОВАНИЮ (CRLF ≥ LF → CRLF, ничья — CRLF; файл без CRLF остаётся LF); s18 U7 зелёный
      (LF-файл с одной CRLF-строкой остался LF), полигон 17/17 (S5b package.json CRLF держится).
- [ ] **US6. Поставка** (критерий 6): ноты/policy 2.5 — строка эпика US ОБЯЗАНА назвать два
      изменения поведения, уже отгруженных в US2а/US4а: `check` КРАСНЕЕТ на непарном якорном
      маркере (полевое дерево с END без BEGIN станет красным после обновления — это и есть
      сигнал) · пункт `stale-claims` теперь всегда в задании · Reference §10 · пересборка · полигон
      18 сводов (s18 в `SUITES`, шапка `[TESTED: …]`) · счётчики из вывода · реестр пар ·
      `doc-header-lint`.
- [ ] **US7. Закрытие.** Лёгкий судья (критерии 1–6 пробами); «Решения без владельца»; план эпика
      RL (`plans/87_epic82_RL_*`, канон N+1); STATUS + `plans/82` + `MASTER_PLAN` §8 (метрика:
      критерий 4 → 4 из 8).

## Верификация наблюдением

Каждый критерий — сводом s18 на развёрнутой копии в OS-temp (полигон `npm run test:core`);
детерминизм — двумя прогонами и диффом выводов; красный — на HEAD-ядре до фикса (лог в файл);
диффы модулей и задания — грепом по сгенерированному `KAIF_UPDATE_TASK.md`; сплиттер — пином
«ядро == сборка» в `check-framework`. Живая приёмка — полем после релиза (Prompt Modding,
QA_Engineer, KAGO — доноры; ответы на их тикеты — эпик RL).

## Риски (ярусы Мёрфи)

- **(а)** P1 не воспроизводится в полигоне (модульный маршрут детерминирован — #32 R-B) — принят
  фолбэк `plans/82`: печать решения по файлу с числами + заморозка при расхождении с репетицией;
  охота за причиной не блокирует релиз.
- **(а)** Marker-aware мерж меняет сплиттер, а сплиттер запинен «ядро == сборка»
  (`tools/module-map-lib.mjs` ↔ `KAIF-CORE.mjs`) — правка идёт в ОБА, карта модулей
  пересобирается, пин в `check-framework` краснит рассинхрон.
- **(б)** Безусловный `stale-claims` вернёт шум класса bugs/35 — тело `no lines found` пустого
  скана шума не несёт; точность скана не трогаем.
- **(б)** Печать вердикта по каждому файлу раздует вывод update на больших развёртываниях —
  печатать только для кандидатов на wholesale (baseN > 2), остальное — счётчиком.
- **(в)** EOL по доминированию при равном счёте — тай-брейк в пользу CRLF на дереве с
  `core.autocrlf=true`, иначе LF; назван в комментарии функции.

## Решения, принятые агентом без владельца

*(заполняется по ходу; на закрытии — полный список)*

## Links

`plans/82` (критерий 4) · `researches/24` §2 (#31, #32), §3 строки US, §7 риски · `researches/23`
§2 (U2–U6), §2а (P1–P6) · `ideas/27` п. 10 · `plans/73` U2 (пп. 1, 2, 4) · issues origin #27, #28,
#31, #32 · KAGO `bugs/KAIF/10` · `reports/KAIF_AUDIT/2026-08-28_rl_court_registry.md` (S1, H10) ·
`framework/installer/KAIF-CORE.mjs` · `tools/module-map-lib.mjs` · `tools/sandbox/s07-translated.mjs`,
`s10-l4-audit-noise.mjs`.
