# План 89 — EPIC KAIF 2.6 (рабочее имя Mindful KAIF): зонтичный мета-план версии

> **Создан:** ≈ 2026-09-05 14:45 +03:00 (версия ОТКРЫТА словом владельца — интервью №023 Q1 = B,
> 2026-09-05 14:38 +03:00: разрез `researches/26` §4 принят со стретчем CR; решение №95).
> **Родитель:** `ideas/28` § «Разрез 2.6 (решение владельца)» + `researches/26` (разведка:
> реестр × issues #40–#48 × Парето) + `interviews/interview_023` (Q1/Q4 отвечены; Q2/Q3 — раунд 2
> сценариями, ждут владельца).
> **Статус:** 🟢 принят к исполнению ≈ 2026-09-05 14:45 +03:00. Веха: 2026-09-05 14:57 +03:00 —
> раунд 2 интервью №023 отвечен: Q2 = A (строку `DELIVERY:` ДОВЕСТИ, мерить по пояснению владельца
> в Unliminium — вектор по механикам; №99), Q3 = A (три правки опубликованного — HY; №100).
> Операционный план ближайшего эпика OQ — `plans/90` ✅ (≈ 15:05 +03:00; разведдок донора —
> `researches/27`).
> **Вовне:** релиз v2.6 в GitHub Releases (эпик RL, слово владельца в моменте — №13) · ответы в
> issues #40–#48 — после релиза, по тикету, без вычитки (№84/№92, стоячая авторизация №93) ·
> релизное имя — окончательное слово владельца ближе к релизу (№96) · внешние правки
> опубликованного (ноты 2.2–2.4, тело #23, topics) — по Q3 раунда 2, строкой `AUTH:` · правки
> канона поставки едут полевым проектам обновлением (i18n-дельты).

## Вектор цели (Achieve)

**Боль.** (а) Интерактивный контур записывает, что вопрос владельцу ЕСТЬ и что он ОТВЕЧЕН, но
не что он ПОКАЗАН, — 48 дней и ~40 сессий докладывали «здоровье» при вопросе, о котором владелец
не знал (#47, его слово: «Вопросы владельцу нести в приоритете НОМЕР 1»). (б) Механика
`DELIVERY:` 2.5 отправила агентов четырёх обновившихся проектов спрашивать владельцев, «чем
мерить» (слово владельца №97: «или делаешь механики полно, или не делаешь механики без
критериев приёмки»); вопросы владельцу сам исток приносит техническими пояснениями вместо
сценариев, которые он же и ввёл (№98, `bugs/111`). (в) Заглавная симметрия 2.5 — репетиция
связывает бой — недостижима на bootstrap-маршруте, который канон рекомендует локализованным
проектам (#42, три проекта за сутки); машинерия каждый интервал выдаёт файлы «шаблон +
заполнения» за правки владельца (#48); `stale-claims` слеп к строке, отставшей раньше одной
версии (#44); `report` отвергает тикеты поля одним сообщением без формы (#40). (г) Бюджеты
девятки превышены у трёх проектов ИЗ-ЗА приехавшего канона, и правило не говорит, куда его
выносить (#43/#45). (д) Слово владельца №90 о ссылках на проекты-доноры ждёт исполнения.

**Где хотим оказаться.** KAIF 2.6 — версия, которая ставит двигатель на ракету: механизмы 2.5
доведены до реального пути поля. Вопрос владельцу имеет третий факт — «показан», — возраст и
условие выхода ритуалов; метрика доставки либо выводится из критериев приёмки без вопроса
владельцу, либо снята (по его слову); каждый вопрос владельцу — сценарий того, что он увидит;
обновление на bootstrap-маршруте принимает репетицию, заполнения не мерджатся руками, старые
строки версии видны; бюджет канона знает про приехавший канон; поставка не называет
проекты-доноры; `/code-revision` прочитан самим Fable; релиз опубликован по слову владельца.

**Рамка исполнения** (по прецеденту 2.5 и слову владельца в чате 2026-09-05): ≈ 6–7 чатов ·
«80 % улучшения за 20 % усилий» · режим MAX без workflows — оркестровку просить точечно (№77),
кандидат — суд RL.

**Метрика доставки версии (правило №97 применено к самому истоку):** `критерии приёмки 2.6
закрыты N из 14` (знаменатель 12 → 13 → 14 — критерий 13 [FK] по #50 и критерий 14 [IC] по #51,
двумя словами владельца 2026-09-05) — по списку ниже; критерий закрыт, когда его строка «Проверка» исполнена и под
ним стоит `✅` с меткой момента. Владельца о метрике не спрашивали.

## Готово, когда (критерии приёмки версии — сценариями, `REQUIREMENTS_FRAMEWORK.md`)

1. **[OQ — вопрос показан]**
   - Ситуация. Развёртывание KAIF 2.6; в `interviews/` документ со статусом «ждёт владельца»,
     созданный три дня назад и ни разу не показанный.
   - Действие. Агент запускает `/resume`.
   - Результат. Шаг очереди печатает первой строкой `interview_NNN — ждёт 3 дн. · показан:
     НИКОГДА`; шаг не считается пройденным, пока документ не поднят страницей или чатом либо не
     записана одна строка «почему не показан»; после показа в документе интервью стоит строка
     `shown: <дата и время> · <транспорт>`, и очередь печатает её вместо `НИКОГДА`.
   - Проверка. `node tools/review.mjs --queue` печатает возраст и `НИКОГДА` первым;
     `node tools/questions-guard.mjs` красный на фикстуре «ждёт + никогда не показан + ритуал
     закрыт зелёным» и на вопросе владельцу, живущем прозой в `bugs/`; контракт `/owner-reviews`
     обоих слоёв несёт инварианты факта «показан» и возраста; свод s12 зелёный с новым ростером.
   ✅ 2026-09-05 15:21 +03:00 — OQ1 (`plans/90` критерии 1–4, коммит `f1fee48`): `--queue --list`
   печатает возраст и `НИ РАЗУ НЕ ПОКАЗАН` первым с кодом 2; `questions-guard` G4/G5 красные на
   фикстурах (селфтест 29/29); I40–I43 в обоих слоях; s12 `all 20 checks green` (ростер 43).

2. **[OQ — строка доставки]** (Q2 раунда 2 = A, №99: довести; форма — пояснение владельца в
   Unliminium, вектор по механикам; разведка донора — `researches/27`)
   - Ситуация. Проект на KAIF 2.6 без реестра механик; в `GOAL.md`, `MASTER_PLAN.md` и двух
     картах описаны 14 логически обособленных механик; владелец про метрику не говорил ни слова.
   - Действие. Агент запускает `/resume` в первой сессии после обновления.
   - Результат. Агент сам, без вопроса владельцу, готовит проект реестра механик
     (`MECHANICS_REGISTRY.md` или раздел `MASTER_PLAN.md`: по строке на механику — имя ·
     завершённость % · вплетённость % · обоснование границы) из `GOAL.md`, `MASTER_PLAN.md` и карт
     и отдаёт владельцу на утверждение как видение; закрытие сессии печатает `DELIVERY: механик
     14 · завершено 62 % · вплетено 31 % · дыр 9 · противоречий 3 · багов 41; сдвинуто: … |
     блокер: …`; пока реестр не утверждён — `DELIVERY: реестр механик строится (проект: 14
     механик, ждёт утверждения)`. В задании обновления 2.5 → 2.6 нет пункта «спросите владельца о
     метрике»; проектам, где агент уже задал этот вопрос, задание велит закрыть его и построить
     реестр.
   - Проверка. `node .kaif/kaif-core.mjs delivery` печатает строку вектора из реестра (или
     «реестр строится» с числом строк проекта) — то же, что в отчёте закрытия;
     `grep -c "ask the owner" KAIF_UPDATE_TASK.md` → `0` на фикстуре обновления; свод полигона
     `s20` красный на ядре 2.5 (нет команды `delivery`).

3. **[OQ — вопрос сценарием]**
   - Ситуация. Агент истока заводит интервью с вопросом-развилкой из двух вариантов.
   - Действие. `node tools/questions-guard.mjs`.
   - Результат. Вопрос, у которого хотя бы один вариант не несёт четырёх строк «Ситуация ·
     Действие · Результат · Проверка», печатается нарушением «вопрос без сценария»; `/interview`
     обоих слоёв велит сценарий на каждый вариант (расширение шага 3а на все вопросы).
   - Проверка. `node tools/questions-guard.mjs --selftest` красный на фикстуре без сценария и
     тихий на интервью №023 раунда 2; греп новой формулировки шага в `framework/skills/interview/`
     и `.claude/skills/interview/`.
   ✅ 2026-09-05 16:14 +03:00 — OQ1 + OQ4 (`plans/90` критерии 4 и 9): ось G6 стража (селфтест
   `все 29 мутаций сбылись по предсказанию`, мутации G6 красный/зелёный; живой прогон `Новых
   нарушений: 0` — интервью №023 раунда 2 молчит); шаг 3a `/interview` обоих слоёв — «every
   question and every option opens with a four-line scenario» / «Каждый вопрос и каждый вариант —
   сценарием»; охоты судьи question-without-scenario и mechanic-asks-the-owner в KAIF-блоке обеих
   копий.

4. **[UR — репетиция на bootstrap]**
   - Ситуация. Локализованное развёртывание 2.5 с распиской репетиции из песочной копии.
   - Действие. `node KAIF-LOADER.mjs --lang ru --rehearsal <copy>/.kaif/last-update.json`.
   - Результат. Лог печатает `rehearsal verdicts loaded from <copy>/… (N file(s))`; после
     обновления файла `.kaif/update-rehearsal.json` на диске нет; на неизвестном флаге загрузчик
     отказывает ДО записи ядра — `.kaif/kaif-core.mjs` побайтно прежний; `diff --source` и
     `update` печатают одно и то же число кандидатов.
   - Проверка. `npm run test:core` → `all 21 suites green` — новый свод `s21` несёт по ассерту на
     каждый из четырёх пунктов и на ядре 2.5 (копия до фикса) печатает `✖` по каждому (якорь
     поправлен нулёвкой UR0: `s20` занят `delivery`).
   ✅ 2026-09-05 17:46 +03:00 — UR1/UR7 (`plans/91` критерий 1, коммит `882db80`): `all 21 suites
   green`; `s21` A1–A3 — 7 ассертов `✖` на копии `cb42039` через `KAIF_DIST`/`KAIF_LOADER`; каверза —
   половина A4 (документ владельца не кандидат превью) зелёная на обеих сборках, `plans/91` § «Решения» 8.

5. **[UR — заполнения]**
   - Ситуация. Развёртывание, где `/autoloop` отличается от шаблона только заполнением
     `<BUILD_COMMAND>` и `<TEST_HARNESS>`.
   - Действие. Обновление до 2.6.
   - Результат. Задание НЕ содержит `/autoloop` в `merge-modules`; навык заменён механически с
     сохранёнными заполнениями; депрекация такого файла выполнена машинерией; `update-verify` не
     печатает «promised upstream line not found» на плейсхолдер-строках; модуль, чей шаблон не
     менялся между версиями, не называется «upstream changed it».
   - Проверка. `npm run test:core` → `all 21 suites green` — ассерт свода `s21`
     `fills: /autoloop kept out of merge-modules`; на ядре 2.5 тот же ассерт печатает `✖`.
   ✅ 2026-09-05 17:46 +03:00 — UR2/UR7 (`plans/91` критерий 2): `s21` B1–B6 зелёные, все шесть `✖` на
   копии `cb42039`; заполнения ВЫВОДЯТСЯ с диска (`matchFills`/`deriveFills`), кэш `fills` манифеста;
   каверзы — вывод из старых текстов требует артефакта предыдущего релиза или `--baseline`; сосед `s11`
   U1 сужен с сохранением контракта в U1s (`plans/91` § «Решения» 9); урок EXP-0112.

6. **[UR — старые строки версии]**
   - Ситуация. README проекта на 2.5 с бейджем `Framework-KAIF%202.2`.
   - Действие. `node .kaif/kaif-core.mjs diff --source <релиз 2.6>`.
   - Результат. Пункт `stale-claims` задания называет `README.md:22` и версию `2.2`.
   - Проверка. `grep -c "README.md:22" KAIF_UPDATE_TASK.md` → `1`; свод полигона; `gt` — одна
     функция модульной области (дубли `:760`/`:891` сняты; якоря пересняты нулёвкой UR0).
   ✅ 2026-09-05 17:46 +03:00 — UR3/UR7 (`plans/91` критерий 3): `s21` C1 — `README.md:22 — … (asserts
   2.2)` в пункте задания, `README.md:23` назван, изъятия как прежде; C2 — `grep -c '^const gt = '` → 1,
   локальных 0; оба `✖` на копии `cb42039`; попутно — `%20` → пробел и «скобка после `]` — цель ссылки»
   (ровно полевой бейдж #44); каверза — шум замерен на фикстуре, не на живом README поля.

7. **[UR — доставка тикета]**
   - Ситуация. Тикет со строкой `**Delivered upstream:** not yet — …` (нижний регистр) и второй
     тикет со строкой `… — origin #37` без URL.
   - Действие. `node .kaif/kaif-core.mjs report <тикет> --dry-run` на каждом.
   - Результат. Первый — dry-run проходит (не отказ); второй — `already delivered … #37`; отказ на
     нераспознанной строке называет обе законные формы и точную правку.
   - Проверка. `npm run test:core` → `all 20 suites green` — свод команды `report` дополнен тремя
     ассертами (`not yet` в нижнем регистре · `#NN` = доставлено · отказ называет обе формы); на
     ядре 2.5 каждый печатает `✖`.
   ✅ 2026-09-05 17:46 +03:00 — UR4/UR7 (`plans/91` критерий 4): `s17` 32/32 (+4 UR4 +1 C-H3), шов
   `KAIF_DIST` добавлен в свод; на копии `cb42039` четыре ассерта UR4 `✖`, C-H3 зелёный на обеих
   (правилась формула Reference §10.7, не код); контракт читается абзацем — перенос на 100 колонок
   законен.

8. **[UR — текст навыка обновления]** `/kaif-update` обоих слоёв говорит: bootstrap ОБЯЗАТЕЛЕН
   для дерева с якорными парами под ядром < 2.5 · `cp` расписки песочницы в
   `.kaif/update-rehearsal.json` — штатный ход интервала ИЗ версии < 2.5 · ассеты скачать один раз
   и отдать `--source` обоим прогонам · `git config core.longpaths true` в рецепте песочницы на
   Windows; задание bootstrap-маршрута несёт baseline старого шаблона (модуль, отсутствующий на
   диске, показывает `−`/`+`, а не одни `+`). *Проверка:* греп четырёх формулировок по обоим
   слоям; свод полигона на baseline; своды E-H3 (`language-arrivals` на bootstrap) и C-H3
   (формула таймаута `report`) из суда RL 2.5.
   ✅ 2026-09-05 17:46 +03:00 — UR5/UR7 (`plans/91` критерий 5): грепы в обоих слоях — MANDATORY 3 ·
   `update-rehearsal.json` (cp) 2 · `BOTH runs` 1 · `core.longpaths true` 1, копии побайтно равны; `s21`
   D1 — дифф на bootstrap несёт старый шаблон контекстом (`✖` на копии `cb42039`); D2 (E-H3) —
   `language-arrivals` на bootstrap, зелёный на обеих (покрытие); C-H3 — `s17`.

9. **[CB — бюджет канона]**
   - Ситуация. Развёртывание, где `AGENT_GUIDE.md` 1655 строк при бюджете 1200, из них 300 —
     строки модулей, побайтно равных шаблону поставки 2.6.
   - Действие. `node .kaif/kaif-core.mjs check`.
   - Результат. Предупреждение считает бюджет ПО СОБСТВЕННЫМ строкам проекта (строки модулей,
     совпадающих с шаблоном, не считаются) и называет документ, его собственную длину, бюджет и
     адрес выноса для этого документа (летопись · `researches/` · файл домашних правил);
     языковая смесь навыков считается по доле токенов, не по единичному вхождению; страница
     релиза называет `@fork` в фразе о линтере; предупреждение о неполном языковом пакете стоит
     ПОСЛЕ строки успеха с указателем.
   - Проверка. Свод полигона `check` на фикстуре с приехавшим каноном, красный на ядре 2.5.

10. **[HY — гигиена по слову владельца]**
    - Ситуация. Посторонний читает поставку и витрину.
    - Действие. `grep -rniE "project [A-F]\b|donor|полевой проект-донор" framework/ README.md
      dist/KAIF-FULL.md` и чтение реестров суда `reports/KAIF_AUDIT/`.
    - Результат. Псевдонимов проектов-доноров в поставке нет, кроме мест, где источник несёт
      смысл правила, — каждое помечено `<!-- source-kept: причина -->`; реестры суда несут
      псевдонимы вместо имён; три внешние правки (ноты 2.2–2.4 · тело #23 · topics) сделаны —
      `AUTH: интервью №023 Q3 раунда 2 = A, 2026-09-05 14:57 +03:00, №100` (все три).
    - Проверка. `private-names-guard` с новой осью «псевдонимы доноров» зелёный, доказан красным
      на копии; `release-body-guard --tag` по каждой правленной ноте; `gh repo view --json
      repositoryTopics`.

11. **[CR — ревью `/code-revision` Фейблом]**
    - Ситуация. Fable 5.1 читает `/code-revision` как будущий исполнитель (`plans/63`).
    - Действие. Ревью навыка по поручению владельца 2026-08-08.
    - Результат. Каждая правка навыка несёт дословную цитату меняемой строки и причину из
      собственного опыта ревью кода; то, что оставлено, названо оставленным с причиной; оба слоя
      синхронны; сборка зелёная.
    - Проверка. `git diff` навыка в обоих слоях + реестр пар + судья эпика.

12. **[RL — суд, витрина, релиз]**
    - Ситуация. Эпики OQ–CR закрыты; `version.json` ещё 2.5.
    - Действие. `/release` по шагам, суд одним кругом (форма №74/№88) до него.
    - Результат. Реестр суда в `reports/KAIF_AUDIT/` — 0 REFUTED-блокеров; README EN/RU и ноты
      2.6 называют версию одним именем (окончательное слово владельца по №96) и числами сборки;
      логотип с подписью версии; тег `v2.6`, GitHub Release с ассетами, `latest` → 2.6, страница
      прочитана глазами; ответы в issues #40–#48 отправлены по тикету; судья охотится на
      «механику с вопросом к владельцу проекта» (№97).
    - Проверка. `gh release view v2.6`; `release-body-guard --tag v2.6`; `counters-guard` ·
      `showcase-lint` зелёные; строка `DELIVERY: критерии приёмки 2.6 закрыты 14 из 14`.

13. **[FK — смущение агента — знак разведки]** (issue #50, NDim Space, 2026-09-05 16:46 +03:00;
    слово владельца дословно: «если я тебе что-то предлагаю, и тебя это смущает, ты не понимаешь —
    это тебе прямой знак, что нужно идти искать в интернете, что я имел в виду, или у меня
    спросить»; взят в 2.6 словом владельца в чате истока 2026-09-05: «там будет еще один запрос на
    улучшение в ориджин, берем его тоже в 2.6 версию»)
    - Ситуация. Владелец ответил на вопрос интервью словами своей области («нужно писать
      role-playing game и RPG одновременно»), у проекта модель пар тегов «один русский — один
      английский», и агент читает ответ как третий тег без пары.
    - Действие. Агент пишет владельцу «ваше предложение ломает модель пар тегов».
    - Результат. Такое сообщение не считается готовым: `/interview` обоих слоёв велит шаг
      «смущение → веб-поиск → замер по данным владельца → вопрос» перед любым «не могу», и тело
      интервью или отчёта несёт блок `Разведка:` (запрос · нашёл · замер); `AGENT_GUIDE` обоих слоёв
      несёт правило рядом с «Место вопросов» («смущение — знак искать, а не отказывать»);
      `/fable-judge` обеих копий охотится на «вердикт без разведки» — слова «ломает / не умеет /
      невозможно / противоречит» о предложении владельца без блока `Разведка:`; у истока ось стража
      вопросов краснеет на том же классе; ru-пакет несёт словарную запись «РПГ = RPG (ролевая игра)».
    - Проверка. `node tools/questions-guard.mjs --selftest` красный на фикстуре из инцидента («ломает
      модель пар тегов… третий тег роняет корпус») и тихий на том же тексте с блоком `Разведка:`;
      `grep -c "Разведка\|Recon:" framework/skills/interview/SKILL.md .claude/skills/interview/SKILL.md`
      → `≥ 1` в каждом; греп охоты в KAIF-блоке обеих копий `fable-judge`; сборка зелёная.

14. **[IC — интерактивный контур поставкой]** (issue #51, Unliminium, 2026-09-05 16:56 +03:00;
    слово владельца дословно: «Нужно, чтобы KAIF очень подробно писал спецификацию на построение
    интерактивного контура, или даже имел в своей поставке готовый интерактивный контур, которые
    агенты качают из ориджин. Это должен быть генератор интервью, вычитки, отсмотра макетов»;
    интервью №024 Q1 = B, 2026-09-05 17:32 +03:00 — решение №101, пересмотр №34)
    - Ситуация. Проект на KAIF 2.6; своего интерактивного контура у проекта нет — или есть, но
      построен по соседу и сломан; агент написал интервью с вопросом-развилкой из трёх вариантов.
    - Действие. Агент запускает `node .kaif/tools/contour/review.mjs interviews/interview_001.md` —
      инструмент приехал поставкой из ориджина, проект дописал только заголовок, пути и тексты кнопок.
    - Результат. Владелец видит страницу с радиокнопками у каждого вопроса, полем свободного
      ответа, кнопкой «Сохранить» и сигналом «сохранено»; шапка страницы скроллится вместе с ней
      (слово владельца 2026-09-05); ответ записан в `interviews/decisions/*.json`, факт показа — в
      `interviews/decisions/shown.json`; тот же инструмент открывает вычитку (комментарии к
      абзацам) и отсмотр макета (картинка + комментарии); страница с вопросом без вариантов списком
      и без объявленного свободного поля НЕ открывается — предполёт печатает, что исправить;
      одностраничная спецификация `.kaif/INTERACTIVE_CONTOUR_SPEC.md` — контракт формы, на который
      ссылаются `/interview` и `/owner-reviews`.
    - Проверка. `ls .kaif/INTERACTIVE_CONTOUR_SPEC.md .kaif/tools/contour/review.mjs` → 2 файла
      после обновления до 2.6; `node .kaif/tools/contour/review.mjs --selftest` → зелёный на свежем
      развёртывании и красный на фикстуре «варианты абзацами» (случай #052 Unliminium); свод
      полигона гоняет три лица генератора на развёрнутой копии; `grep -c "contour/review.mjs"
      .claude/skills/owner-reviews/SKILL.md` → `≥ 1` («запусти», а не «построй»).

## Парето-разрез скоупа (исполнение слова владельца 2026-09-05)

**В 2.6 идут** 14 строк реестра `researches/26` §3 с вердиктом «В 2.6» плюс стретч CR (слово
владельца Q1 = B). **В 2.7 уходят** (реестр `ideas/29`) эпики T · V (+ тяжёлая половина KAGO 09)
· P · X-полный · селфтесты · U3/U4 · TD-фазы · хук #36 · #43 R5 · #45 wish 4 — ≈ 18–24 чата
внутреннего лифта, ни один пункт которого не разблокирует полевой проект сегодня.

## Эпики и порядок

| # | Эпик | Что делает | Бюджет чатов | Веха |
|---|---|---|---|---|
| 1 | **OQ «Вопрос владельцу — приоритет №1»** | #47: факт `shown:` (донор — Unliminium `interviews/decisions/shown.json`) · возраст очереди · условие выхода шага 1б и церемоний закрытия · «кто заблокирован» · страж вопроса вне `interviews/` по прозе · охота судьи «напечатал, не доставил» · строка доставки ДОВЕДЕНА (№99): реестр механик из GOAL/MASTER_PLAN/карт → вектор (механик · завершено % · вплетено % · дыры · противоречия · баги) → команда `delivery`, «реестр строится» до утверждения, задание обновления без вопроса владельцу · №98: сценарий на каждый вопрос и вариант в `/interview` обоих слоёв + ось `questions-guard` · ответ проектам, уже спросившим владельца о метрике | 2–2,5 | `plans/90` ✅ OQ0–OQ1 закрыты 2026-09-05 15:21 +03:00 (критерий 1 версии ✅); ✅ OQ2 закрыт 2026-09-05 15:51 +03:00 (шаблон реестра систем · команда `delivery` · форма вектора · policy-change 2.6 · `Kind:` · свод `s20`; критерий 2 версии закрывается OQ3 — реестр истока и строка §8); ✅ OQ3 закрыт 2026-09-05 16:06 +03:00 (`SYSTEMS_REGISTRY.md` истока, 9 систем, `npm run kaif:delivery`, строка вектора в §8 MASTER_PLAN; критерий 2 версии — 🟡: строка и свод есть, интервальный ассерт задания обновления — на бампе); ✅ OQ4 закрыт 2026-09-05 16:14 +03:00 (вопрос сценарием — `/interview` 3a обоих слоёв, фраза «Место вопросов», две охоты судьи 2.6; критерий 3 версии ✅); ✅ OQ5 16:20 (карты, черновики ответов #46/#47, реестр показан владельцу); **✅ ЭПИК OQ ЗАКРЫТ 2026-09-05 16:23 +03:00 — судья VERIFIED WITH CAVEATS, 0 REFUTED; реестр систем истока утверждён владельцем 16:22 («пробежался глазами, ок»)** |
| 2 | **UR «Обновление на реальном маршруте»** | #42 (`--rehearsal` у `install` · `consumeRehearsal` на bootstrap · отказ загрузчика ДО записи ядра · один набор кандидатов) · #48 (заполнения ВЫВОДЯТСЯ с диска, `template + fills` = нетронут, `update-verify` без ложных «unmerged») · #44 (окно «старше устанавливаемой», `gt` в модуль) · #40 (`/not yet/i`, `#NN`, отказ с формой) · baseline на bootstrap · текст `/kaif-update` · своды E-H3/C-H3 | 2 | **✅ ЭПИК UR ЗАКРЫТ 2026-09-05 17:46 +03:00** (`plans/91`, сессия 55, один чат): UR0 16:53 · UR1–UR6 коммитом `882db80` (свод `s21` 41 ассерт, красный 20/41 на HEAD через швы `KAIF_DIST`/`KAIF_LOADER`; `s17` +5; гард 10 `check-framework`; `all 21 suites green`) · UR7 лёгкий судья VERIFIED WITH CAVEATS, 0 REFUTED (каверза A4 — `plans/91` § «Решения» 8); критерии версии 4–8 ✅ |
| 3 | **FK «Смущение — знак разведки»** (issue #50, взят в 2.6 словом владельца 2026-09-05) | канон обоих слоёв: правило «смущение агента предложением владельца — триггер разведки, не вердикт» (веб-поиск → замер по данным владельца → вопрос) рядом с «Место вопросов» в `AGENT_GUIDE` + строка у трёх дверей `PHILOSOPHY` · шаг в `/interview` обоих слоёв с блоком `Разведка:` (запрос · нашёл · замер) как предполётом любого «не могу» · охота `/fable-judge` «confusion delivered as verdict» в KAIF-блоке обеих копий · у истока — ось G7 стража вопросов с фикстурой из инцидента (красный/зелёный) · пример «РПГ = RPG» внутри правила (не новый файл пакета) · черновик ответа в #50 | 0,5 | 🔲 операционный план `plans/92` написан 2026-09-05 17:46 +03:00 (канон N+1, на закрытии UR); старт — FK0 |
| 4 | **IC «Интерактивный контур поставкой»** (issue #51; интервью №024 Q1 = B, №101 — пересмотр №34) | одностраничная исполнимая спецификация `.kaif/INTERACTIVE_CONTOUR_SPEC.md` (форма вопросов и вариантов · запись ответа · факт показа · страж перед открытием · keep-alive · самопроверка «радиогрупп = вопросов») · генератор `.kaif/tools/contour/` трёх лиц — интервью · вычитка · отсмотр макетов — обобщённый из референса истока (`tools/review.mjs` + `tools/lib/review-core.mjs`), с предполётом (страница со сломанным вопросом не открывается), селфтестом и сводом полигона; шапка страницы скроллится со страницей (слово владельца 2026-09-05) · `/owner-reviews` и `/interview` обоих слоёв: «запусти», а не «построй» · исток переходит на поставляемый генератор (dogfooding до отгрузки, EXP-0111) · черновик ответа в #51 | 2–2,5 | 🔲 критерий 14; операционный план — по канону N+1 после FK |
| 5 | **CB «Бюджет канона»** | бюджет по собственным строкам проекта · адрес выноса по документу · языковая смесь по токенам · `@fork` во фразе линтера · указатель в предупреждении пакета | 0,5 | — |
| 6 | **HY «Гигиена по слову владельца»** | №90: чистка псевдонимов доноров в поставке (`framework/installer`, `framework/tools`, `framework/skills`, README) с пометкой `source-kept` там, где источник несёт смысл · ось стража · псевдонимы в реестрах суда · внешние действия по Q3 раунда 2 | 0,5–1 | — |
| 7 | **CR «Ревью `/code-revision` Фейблом»** | `plans/63` — стретч, взятый словом владельца (Q1 = B) | 1 | — |
| 8 | **RL «Суд и релиз»** | суд одним кругом · охота №97 · витрина · окончательное имя (№96) · публикация по слову · ответы в issues #40–#51 после релиза | 1 | — |

**Порядок: OQ → UR → FK → IC → CB → HY → CR → RL, ≈ 9–10 чатов.** Первым — то, что владелец назвал
приоритетом номер один и что действует на каждую сессию каждого проекта; вторым — то, за что три
проекта заплатили за сутки; третьим — слово владельца о поведении на развилках (#50, пришло
2026-09-05 16:46 +03:00 в ход эпика UR и взято в 2.6 его словом в чате — действует на каждый
разговор владельца с агентом, цена полчата); четвёртым — интерактивный контур поставкой (#51,
16:56 +03:00; интервью №024 Q1 = B в 17:32 — против рекомендации агента отложить генератор в 2.7:
четыре дефекта класса в поле и его слово «или даже имел в своей поставке готовый интерактивный
контур»); дешёвое и внешнее — перед стретчем и судом; стретч — перед судом, чтобы суд судил и его.
Знаменатель метрики версии вырос с 12 до 14 сценариев двумя словами владельца одного часа.

**Канон N+1 действует (№43):** детальный операционный план пишется ТОЛЬКО ближайшему эпику;
план следующего — на закрытии текущего. Старт каждого эпика — нулёвка актуализации входов
(№76): сверка входов против HEAD, правка плана по фактам — штатное действие. Гейт каждого эпика
— лёгкий судья (№48/№88); полный суд — один раз в RL.

**Входы владельца (дословно, интервью №023, 2026-09-05 14:38 +03:00):** Q1 = B (разрез + стретч
CR); Q4 = B (Mindful KAIF); Q2 = D: «Delivery metric - какая-то непонятная для меня хуета, от
которой офигели все проекты, которые обновились до 2.5, и агенты ринулись ко мне спрашивать
меня, чем это мерить. Ты это придумал, а я блять должен рассказывать агентам, чем это мерить.
Или ты делаешь механики так, чтобы они не ставили в тупик агентов - полно, или нахуй не делаешь
такие механики, у которых нет критериев приемки.» (№97); Q3 = D: «не понимаю проблему. Я просил
описывать требования поведенческими сценариями, и ты сам блять этим не пользуешься, из-за этого
приносишь мне какие-то технические пояснения, которые я не понимаю, как заказчик.» (№98). Слово
в чате: «там некоторые вопросы нужно переформулировать и мне перепринести» — раунд 2 Q2/Q3.

## Риски (ярусы Мёрфи)

- **(а) Факт «показан» требует транспорта, который в поставке не едет** — контур страницы строит
  агент проекта (`/owner-reviews`: KAIF фиксирует методологию). Защита: инварианты контракта
  (что обязано быть) + референс истока в `tools/review.mjs`; чат-показ пишет факт строкой в
  документ интервью рукой агента — не машинерией.
- **(а) Метрика из сценариев на проекте без сценариев в плане** — `0 из 0` читается как «ничего
  не сделано». Защита: строка честно называет причину («критерии ещё не написаны») и следующий
  шаг агента — написать их из `GOAL.md`; никогда — вопрос владельцу.
- **(а) Классификация с учётом заполнений меняет манифест деплоя** (схема `values`). Защита:
  обратная совместимость — отсутствующие fills = поведение 2.5; своды на дереве 2.5 без fills и
  2.6 с fills.
- **(б) `--rehearsal` на bootstrap открывает путь «расписка чужого дерева»** — `loadRehearsal`
  уже отвергает несовпадение `from`/`to`; добавить сверку `projectName`.
- **(б) Чистка ссылок на доноров стирает источник, несущий смысл правила** — правило владельца
  само оставляет исключение; каждое оставленное помечено `source-kept` с причиной, судья
  проверяет, что смысл не потерян.
- **(в) Имя версии** — рабочее Mindful KAIF, окончательное слово владельца ближе к релизу (№96).

## Решения, принятые агентом без владельца

1. Форма критериев приёмки версии — сценарии (Ситуация · Действие · Результат · Проверка), не
   Scale/Meter/Target: правило владельца №98 применено к собственному плану истока; счёт
   `12` — знаменатель метрики доставки версии по №97 (владельца не спрашивали).
2. Порядок эпиков OQ → UR → CB → HY → CR → RL; стретч CR поставлен перед RL, чтобы суд судил и
   его (владелец порядок не задавал).
3. Правило бюджета канона (критерий 9) выбрано агентом из двух названных полем: «по
   собственным строкам проекта» вместо «приезд поднимает число» — меньше состояний, и
   машинерия уже знает, какие модули совпадают с шаблоном (карта модулей).
4. `plans/90` (операционный план OQ) отложен до ответа Q2 раунда 2 — исполнение канона N+1 с
   поправкой на открытую развилку владельца, чтобы план не стал фикцией.

## Операционный план ближайшего эпика

`plans/90` — эпик OQ; входы: разведдок донора `researches/27` (реестр механик Unliminium —
слово владельца о векторе, интервью №043 того проекта; факт «показан» `shown.json` и очередь с
возрастом там же), Q2 раунда 2 = A (№99), Q3 = A (№100).

## Links

`ideas/28` · `ideas/29` · `researches/26` · `interviews/interview_023` · `plans/82` (прецедент
формы) · `plans/63` (CR) · issues #40 #42 #44 #46 #47 #48 · `bugs/110` · `bugs/111` ·
`MASTER_PLAN` §7 №95–№98.
