# План 93 — эпик IC «Интерактивный контур поставкой»: одностраничная исполнимая спецификация и генератор трёх лиц из ориджина, чтобы контур не строился в каждом проекте заново и не ломался

> **Создан:** 2026-09-05 20:31 +03:00 (закрытие эпика FK, канон N+1 — №43; сессия 55).
> **Родитель:** `plans/89` (эпик IC; якоря — критерий приёмки 14 «[IC — интерактивный контур
> поставкой]», строка 4 таблицы эпиков); источник — issue #51 (Unliminium, 2026-09-05 16:56 +03:00)
> и интервью №024 Q1 = B (2026-09-05 17:32 +03:00, решение №101 — пересмотр №34 2.2); реестр —
> `ideas/28` п. 29.
> **Статус:** 🔲 запланирован 2026-09-05 20:31 +03:00 — старт с шага IC0 (нулёвка актуализации
> входов, №76) в новом чате; вопросов владельцу нет — оба яруса выбраны его словом (№101), форма
> шапки страницы — его словом («шапку… чтобы скроллилась со всей страницей», 2026-09-05 ≈ 17:30).
> **Вовне:** поставка — шаблон спецификации `framework/templates/_interactive-contour-spec.md` →
> `.kaif/INTERACTIVE_CONTOUR_SPEC.md` и генератор `framework/tools/contour/*` → `.kaif/tools/contour/`;
> навыки `/owner-reviews` и `/interview` обоих слоёв («запусти», а не «построй»); `KAIF_REFERENCE`
> (новый подраздел §10 или §11 — по месту машинерии контура); `POLICY_CHANGES_BY_VERSION['2.6']` /
> `TEMPLATE_NOTES_BY_VERSION['2.6']`; свод полигона `s22`; черновик ответа в issue #51 — отправка
> после релиза (№84/№92).

## Вектор цели (Achieve)

**Боль** (слово владельца дословно, issue #51): «Ты открыл сломанный интерактивный контур без
радиокнопок. Это плохо!!! Баг в KAIF заводи, что интерактивные контуры вечно какие-то сломанные в
разных проектах получаются. Нужно, чтобы KAIF очень подробно писал спецификацию на построение
интерактивного контура, или даже имел в своей поставке готовый интерактивный контур, которые
агенты качают из ориджин. Это должен быть генератор интервью, вычитки, отсмотра макетов». Четыре
дефекта класса в поле (#19, #38, #47, #51): поставка несёт КОНТРАКТ (`/owner-reviews`, 43
инварианта, C1–C13, P1–P9, G1–G13, QA1–QA7, T1–T11, DEF1–DEF8 — около 630 строк навыка) и форму
вариантов в `/interview`, но контур каждый проект СТРОИТ заново по соседу и ломает на своих краевых
случаях; машинной проверки «у каждого вопроса есть варианты списком или объявленное свободное поле»
перед открытием страницы в поставке нет; референс истока (`tools/review.mjs` 1218 строк +
`tools/lib/review-core.mjs` 534) в поставку не едет (№34).

**Где хотим оказаться.** Проект на KAIF 2.6 контур НЕ пишет: обновление привозит одностраничную
исполнимую спецификацию (`.kaif/INTERACTIVE_CONTOUR_SPEC.md` — форма вопросов и вариантов · запись
ответа · факт показа · предполёт перед открытием · keep-alive · самопроверка «радиогрупп = вопросов»
· шапка скроллится со страницей · три исхода и коды выхода) и генератор трёх лиц
(`node .kaif/tools/contour/review.mjs <док>` — интервью · `--proofread` — вычитка с комментариями к
абзацам · `--mockup` — отсмотр макета: картинка + комментарии) с селфтестом, который краснеет на
случае #051 («варианты абзацами» → страница без радиокнопок), и со сводом полигона на развёрнутой
копии; навыки говорят «запусти», а не «построй»; исток ест свою поставку — `tools/review.mjs`
становится тонкой обёрткой над отгружаемым генератором, а полный QA в живом браузере
(`verify-contour`) остаётся у истока приёмкой. Проектные параметры (имя проекта, имя владельца,
каталог решений, язык текстов) генератор ЧИТАЕТ из `.kaif/kaif.json`, а не спрашивает (№97).

## Готово, когда (критерии приёмки — сценариями; якорь `plans/89` критерий 14)

1. **[IC — спецификация едет поставкой, и на неё ссылаются навыки]**
   - Ситуация. Проект на KAIF 2.5 с собственным контуром, построенным по соседу; агент собирается
     открыть владельцу интервью.
   - Действие. Агент обновляет проект до сборки 2.6 (`node .kaif/kaif-core.mjs update --source <dist>`)
     и открывает `/owner-reviews`.
   - Результат. В `.kaif/` лежит `INTERACTIVE_CONTOUR_SPEC.md` — одна страница (не более 120 строк):
     форма вопросов и вариантов (таблица `| **A** | … |` или список `- **A)** …`; свободное поле —
     объявленное), схема записи ответа (`interviews/decisions/<док>.decision.json` + архив), схема
     факта показа (`shown.json`: когда · транспорт), предполёт перед открытием (что печатает и с каким
     кодом), keep-alive и три исхода (решено 0 · закрыто без ответа 2 · прервано 130), самопроверка
     страницы «радиогрупп столько же, сколько вопросов», шапка скроллится со страницей; навык
     `/owner-reviews` открывается строкой «запусти `node .kaif/tools/contour/review.mjs <док>`;
     контракт — `.kaif/INTERACTIVE_CONTOUR_SPEC.md`», и `/interview` ссылается на ту же страницу.
   - Проверка. `ls .kaif/INTERACTIVE_CONTOUR_SPEC.md` → 1 файл после обновления; `wc -l` → `≤ 120`;
     `grep -c "INTERACTIVE_CONTOUR_SPEC" .claude/skills/owner-reviews/SKILL.md
     .claude/skills/interview/SKILL.md framework/skills/owner-reviews/SKILL.md
     framework/skills/interview/SKILL.md` → `≥ 1` в каждом.

2. **[IC — предполёт красный на случае #051]**
   - Ситуация. В `interviews/` проекта лежит интервью, где варианты первого вопроса набраны
     абзацами `**A. …**` вместо списка — ровно так, как в инциденте Unliminium.
   - Действие. Агент запускает `node .kaif/tools/contour/review.mjs interviews/interview_052.md`.
   - Результат. Страница НЕ открывается: инструмент печатает «Q1: 0 вариантов списком и нет
     объявленного свободного поля — страница откроется без радиокнопок; исправьте форму: `- **A)** …`»
     и завершается кодом 3; после правки документа страница открывается с радиокнопками у каждого
     вопроса.
   - Проверка. `node .kaif/tools/contour/review.mjs --selftest` → зелёный, в выводе строка о красной
     фикстуре «варианты абзацами» (код 3); `node .kaif/tools/contour/review.mjs <фикстура> --no-open`
     → код 3 и текст выше.

3. **[IC — генератор трёх лиц открывает страницу с радиокнопками, записывает ответ и факт показа]**
   - Ситуация. Проект на KAIF 2.6 без собственного контура; агент написал интервью с одним вопросом
     и тремя вариантами списком; тот же проект держит черновик документа на вычитку и PNG макета.
   - Действие. Агент запускает генератор тремя лицами: `review.mjs interviews/interview_001.md` ·
     `review.mjs docs/DRAFT.md --proofread` · `review.mjs assets/mock.png --mockup`.
   - Результат. Владелец видит: (1) страницу интервью с радиокнопками A/B/C, полем свободного
     ответа, кнопкой «Сохранить» и сигналом «сохранено», шапка уезжает при прокрутке вместе с
     текстом; (2) страницу вычитки — абзацы документа с полем комментария у каждого и кнопкой «Готово»;
     (3) страницу макета — картинка и поле комментариев. После «Сохранить» появляются
     `interviews/decisions/interview_001.decision.json` (ответ, кем, когда) и запись показа в
     `interviews/decisions/shown.json`; в чате агента — строка «решение записано: Q1 = B».
   - Проверка. `node .kaif/tools/contour/review.mjs --selftest` → зелёный на свежем развёртывании
     (три лица рендерятся без браузера через `--no-serve`, парсер вопросов и вариантов, запись
     решения и показа на фикстуре); свод полигона `s22` на развёрнутой копии → «радиогрупп = вопросов»
     по HTML, `decision.json`/`shown.json` появились, `header{position:static}` в CSS страницы.

4. **[IC — обновление 2.5 → 2.6 привозит контур целиком и один раз]**
   - Ситуация. Проект на 2.5, где контура нет; проект на 2.5, где свой `tools/review.mjs` есть.
   - Действие. Оба обновляются до сборки 2.6.
   - Результат. В обоих появляются `.kaif/tools/contour/*` и `.kaif/INTERACTIVE_CONTOUR_SPEC.md`;
     собственный `tools/review.mjs` второго проекта НЕ тронут (машинерия пишет только в `.kaif/`);
     задание обновления пунктом `policy-changes` называет контур поставкой и говорит «переведите
     навыки проекта на `.kaif/tools/contour/`, свой инструмент оставьте или снесите — по слову
     владельца проекта» без единого вопроса к владельцу о параметрах (имя проекта, владелец, язык,
     каталог решений — из `.kaif/kaif.json`).
   - Проверка. Свод `s22`: после `update` `ls .kaif/tools/contour/ .kaif/INTERACTIVE_CONTOUR_SPEC.md`
     → файлы на месте, sha совпадают с манифестом; собственный `tools/review.mjs` фикстуры побайтно
     прежний; `grep -ci "contour" .kaif/KAIF_UPDATE_TASK.md` → `≥ 1`; `npm run test:core` →
     `all 22 suites green`.

5. **[IC — исток ест свою поставку]**
   - Ситуация. Исток KAIF после IC; сессия поднимает владельцу интервью, как всегда.
   - Действие. `node tools/review.mjs interviews/interview_026_*.md`.
   - Результат. Открывается та же страница, что у проектов-потребителей: `tools/review.mjs` истока —
     тонкая обёртка (не более 60 строк) над `framework/tools/contour/`, дополненная только голосом
     и приватными настройками истока через окружение; очередь, `--queue --list`, `--mark-shown`,
     `--notice`, `--enqueue` работают как прежде; приёмка контура в живом браузере зелёная.
   - Проверка. `wc -l tools/review.mjs` → `≤ 60`; `node tools/verify-contour.mjs` (полный, в браузере)
     → зелёный; `node tools/verify-contour.mjs --etalon-only` → зелёный; свод `s12` (канон контура)
     зелёный; `node tools/private-names-guard.mjs` → зелёный (пути голоса и имена проектов-доноров в
     поставку не уехали).

6. **[судья эпика]** — `/fable-judge` по критериям 1–5, таблица вердиктов здесь; на закрытии по канону
   N+1 — план следующего эпика WN (`plans/94`), а ПЕРЕД ним — разведдок класса #52 `researches/29`
   (интервью №025 Q1 = C, №103: «Разведка. Пойми источник проблемы как класса»), ≈ 0,5 чата.

## Шаги (каждый цитирует якорь мета-плана — `plans/89` критерий 14 / строка 4 таблицы)

- [x] **IC0 — нулёвка актуализации входов (№76) — ✅ 2026-09-05 21:02 +03:00 (сессия 55, час
      автономной работы).** Сверено против HEAD `f5ef335`: `tools/review.mjs` 1218 строк ·
      `tools/lib/review-core.mjs` 534 (экспорты `PROJECT_NAME` · `OWNER_NAME` · `DECISIONS_DIR` ·
      `ARCHIVE_DIR` · `QUIET_FROM/TO` · `normalize` · `bodyHash` · `sha256hex` · `provenance` ·
      `inQuietHours` · `parseMetaBlock` · `docStatus` · `parseQuestions` · `renderMd` · `decisionPaths` ·
      `recordDecision` · `readDecision` · `checkApproval`) · `tools/verify-contour.mjs` 1108 ·
      `tools/review-gate.mjs` 27 · `tools/send-outbound.mjs` 171; контракт `/owner-reviews` — свод `s12`
      печатает «roster 104 positions × dist EN + RU mirror, red-proof, 4 tool selftests»; CSS шапки —
      `header { position:static; …}` (`review.mjs:617`, слово владельца), `#banner` остаётся sticky
      (аварийная плашка); счётчики — 773 модуля, 21 свод, 172 блока бандла. Флаги референса:
      `--enqueue --include-stale --list --mark-shown --no-open --no-serve --notice --play --queue
      --selftest --silent --timeout --transport --voice`. Классы страницы: интервью (вопросы) ·
      «сообщение» (`--notice`, документ без вопросов показывается этим классом — `review.mjs:864`) ·
      «очередь» (пачка). **Лица «вычитка с комментариями к абзацам» и «отсмотр макетов» в референсе
      ОТСУТСТВУЮТ** — вычитка есть только как одно поле комментария к документу; оба лица проектируются
      в IC3 минимально (контракт — сценарий B №024). Приватное в референсе: `VOICE_TOOL` по умолчанию
      `F:\KLAS\…` и имена доноров в комментариях (`review.mjs:60–65`) — в поставку не едут (страж
      приватных имён). Параметры истока `.kaif/kaif.json`: `framework · version · released · origin ·
      tracking · sphere · agents · language` — ключа `project` НЕТ: имя проекта генератор выводит из
      `contour.projectName` → иначе `basename(root)`; владелец — из таблицы идентичности `AGENT_GUIDE`
      → иначе «owner»; каталог решений — `interviews/decisions` (канон). **Развилка сборки решена
      разведкой:** `FORK: options <A вложенный путь .kaif/tools/contour/ | B два плоских файла
      .kaif/tools/kaif-contour*.mjs | C директорный модуль .kaif/contour/ по образцу hooks> · price of
      error <путь в сценарии владельца №101 и в спецификации/навыках; поломка сборки или установки для
      всех проектов при неверной правке цикла сборщика> · consulted <build-framework.mjs:388–399 — цикл
      tools плоский (readdirSync + endsWith('.mjs')), hooks — файлы директории без рекурсии;
      check-framework.mjs:532–538 — зеркальный счёт; KAIF-CORE.mjs:622/727/782/2140 — запись по dest с
      mkdirSync(dirname, {recursive:true}), .mjs едут verbatim без заполнений и анонимизации; слово
      владельца №101 — путь .kaif/tools/contour/review.mjs>` → **A**: вложенный путь — ядро вложенные
      dest уже пишет, правка сборщика — рекурсивный обход `framework/tools/` (зеркало — счёт в
      `check-framework`, одна пара реестра), семантика «опциональный tool-модуль в `.kaif/tools/`»
      сохраняется, путь совпадает со словом владельца. B отклонён (уводит от названного пути ради
      экономии двух строк сборщика), C — новый класс директории без нужды (Оккам).
- [x] **IC1 — инвентарь паритета референса (`researches/28`) — ✅ 2026-09-05 21:07 +03:00 (сессия 55):**
      ядро (10 строк) · страница и контур (17 строк) · инварианты I1–I43 построчно (43 из 43: 36 кодом
      генератора, 3 — правила агента в навыке/каноне, остальное — контракт окружения/деплоя с честным
      фолбэком) · остаётся истоку (гейт и отправитель исходящих, `verify-contour`, движок голоса) ·
      чего нет в референсе (предполёт формы, вычитка по абзацам, отсмотр макета, самопроверка
      «радиогрупп = вопросов») · параметры из `kaif.json` без вопроса владельцу · оценка объёма
      ≈ 1 600–1 700 строк. **Постановка шага была:** строка на элемент референса истока →
      едет в генератор / остаётся истоку / выпадает: страница интервью · вычитка · сообщение (`--notice`)
      · очередь и `--list` · факт показа · отправка (`review-gate`/`send-outbound` — остаются истоку) ·
      писки 880/660/990 · голос (Silero — ТОЛЬКО через окружение, путь `F:\KLAS\…` в поставку не едет) ·
      тихие часы · автозакрытие · вахта тишины · замок/порт · окно `--app=`. Третье лицо — отсмотр
      макетов — в референсе истока НЕТ: проектируется по контракту сценария B №024 (картинка +
      комментарии) минимально. Правило: нет строки инвентаря — нет кода.
- [x] **IC2 — спецификация (критерий 1) — ✅ 2026-09-05 21:16 +03:00 (сессия 55; коммит `b8cc6e1`):**
      полигон `all 21 suites green`; наблюдение критерия 1 — свежая установка из бандла в temp-корень →
      `.kaif/INTERACTIVE_CONTOUR_SPEC.md` на месте, `wc -l` → 106 ≤ 120; грепы четырёх навыков → 1/1/1/1.
      Каверзы: наблюдён маршрут `install`, маршрут `update` 2.5 → 2.6 — сводом `s22` (IC4); подраздел
      `KAIF_REFERENCE` — с IC6. Ход работы (был 🟡 21:11): страница
      написана (`framework/templates/_interactive-contour-spec.md`, 106 строк, EN, без кириллицы — гард 5d
      зелёный; восемь секций: источник · предполёт код 3 · записи · страница · процесс · зов · лица и
      флаги · приёмка за минуту), проводка в сборщик явным блоком рядом с `_systems-registry-template`
      (dest `.kaif/INTERACTIVE_CONTOUR_SPEC.md`, bundle-only), сборка `173 file blocks · 783 modules`,
      четыре зеркала счётчиков подняты, абзац 2.6 в `/owner-reviews` обоих слоёв («запусти, а не строй») и
      шаг 3c/3в в `/interview` обоих слоёв. **Зависимость, названная честно:** страница и шаги навыков
      называют команды генератора `.kaif/tools/contour/review.mjs`, которого до IC3 нет — 2.6 не выходит без
      IC3 (порядок шагов плана). До ✅: полигон зелёный, `KAIF_REFERENCE` — подраздел (IC6), коммит.
      **Постановка шага была:** `framework/templates/_interactive-contour-spec.md` →
      dest `.kaif/INTERACTIVE_CONTOUR_SPEC.md` (роль в манифесте — как у `_systems-registry-template`),
      ≤ 120 строк, EN (гард 5d), формы — из `review-core` дословно; ссылки из `/owner-reviews` и
      `/interview` обоих слоёв (RU-копии — перевод от смысла); `KAIF_REFERENCE` — подраздел.
- [ ] **IC3 — генератор трёх лиц (критерии 2–3).** `framework/tools/contour/` (или плоско — по IC0):
      обобщённые `review.mjs` + `core.mjs`: параметры из `.kaif/kaif.json` (`contour: { projectName,
      ownerName, decisionsDir, quietFrom, quietTo }` с дефолтами — имя проекта из `kaif.json.project`,
      владелец — из `AGENT_GUIDE` таблицы идентичности или «owner», каталог — `interviews/decisions`),
      тексты страницы по языку развёртывания (RU/EN из пакета; прочие — EN), предполёт (код 3, текст
      критерия 2), три лица (`--proofread`, `--mockup`), `--selftest` без браузера (парсер · рендер
      `--no-serve` · предполёт красный на фикстуре #051 · запись решения и показа во временном корне),
      маркер `[NOT-TESTED]` → `[TESTED]` по наблюдению; T7 (без бэктиков в шаблонных строках).
      **Ограничения, снятые в IC0–IC2 (сессия 55):** гард 5d сборки запрещает кириллицу в модулях
      поставки — комментарии генератора пишутся по-английски, тексты страницы и фразы зова живут
      словарём RU/EN внутри модуля (ключ — `language` из `kaif.json`), а не строковыми константами
      референса; `.mjs` едут verbatim без заполнения плейсхолдеров — все проектные параметры читаются
      в рантайме (`contour.*` с дефолтами, § «Решения» 4); путь `framework/tools/contour/*.mjs` →
      `.kaif/tools/contour/*.mjs` уже достижим (рекурсивный обход сборщика — IC4-часть); контракт лиц и
      флагов — §7 спецификации `.kaif/INTERACTIVE_CONTOUR_SPEC.md` (страница уже отгружена IC2 и
      называет команды генератора — IC3 обязан им соответствовать, а не наоборот); инвентарь
      `researches/28` — построчный чек-лист сдачи (нет строки — нет кода).
- [ ] **IC4 — сборка, полигон, поставка (критерий 4).** 🟡 Часть сделана 2026-09-05 21:17 +03:00 (сессия
      55): сборщик обходит `framework/tools/` РЕКУРСИВНО (`walkToolModules`, канонический порядок), счёт
      tool-модулей в `check-framework` зеркалит обход (`countToolMods`); красное доказательство — пробная
      поддиректория `framework/tools/_probe/probe.mjs` даёт `174 file blocks` и блок
      `.kaif/tools/_probe/probe.mjs` в бандле при зелёном `check-framework`, после удаления — снова 173;
      цель спецификации проведена в IC2. Остаток IC4 — после IC3: свод `s22`, `policy-changes`, счётчики.
      **Постановка была:** Сборщик: цели генератора и спецификации,
      роли манифеста, карта модулей, `check-framework` (счёт файлов `framework/tools/`), счётчики
      четырёх зеркал (модули вырастут); свод `s22-contour-shipped.mjs`: свежая установка → `--selftest`
      зелёный; `update` 2.5-фикстуры с собственным `tools/review.mjs` → файлы поставки на месте, свой
      инструмент побайтно прежний, `policy-changes` называет контур; красное доказательство — фикстура
      #051 через `--no-open` (код 3) и мутация «радиогрупп ≠ вопросов» на копии HTML; `SUITES` += s22.
- [ ] **IC5 — исток ест свою поставку (критерий 5).** `tools/review.mjs` → обёртка над генератором
      (голос, приватные пути — окружение; очередь/показ/сообщение — через ядро поставки); `verify-contour`
      полный прогон в браузере, эталон пересмотрен глазами; `s12` зелёный; `private-names-guard` зелёный.
- [ ] **IC6 — документы.** `POLICY_CHANGES_BY_VERSION['2.6']` + `TEMPLATE_NOTES_BY_VERSION['2.6']`
      (контур поставкой, пересмотр №34); карты (`PROJECT_STRUCTURE_EXTERNAL_MAP`,
      `PROJECT_ARCHITECTURE_INTERNAL_MAP` — контур становится поставочным); `KAIF_FRAMEWORK.md`;
      README обеих половин (Таблица инструментов поставки, если она есть — только факт); черновик
      ответа в #51 — секцией здесь; `ideas/28` п. 29 — веха.
- [ ] **IC7 — лёгкий судья эпика (критерий 6)** → таблица вердиктов → план следующего эпика (`plans/94`:
      WN) → веха в `plans/89` (критерий 14 ✅, строка 4). Следом, до WN, — разведдок класса #52
      (`researches/29`, №103; строка 6 таблицы `plans/89`).

## Верификация наблюдением

`node tools/build-framework.mjs` EXIT 0 (числа зеркал — из вывода); `npm run test:core` → `all 22
suites green`; `node .kaif/tools/contour/review.mjs --selftest` на развёрнутой копии (внутри `s22`);
`node tools/verify-contour.mjs` полный в браузере (исток) + `--etalon-only`; грепы критериев 1 и 5;
`node tools/counters-guard.mjs`; реестр пар `AGENT_GUIDE` (fable-копии, генераты, счётчики,
референсы навыков); `node tools/private-names-guard.mjs`.

## Риски (ярусы Мёрфи)

- **(а) Обобщение референса за один заход раздувает поставку и ломает исток.** Защита: инвентарь
  паритета IC1 решает построчно, что едет; исток переводится на поставку ПОСЛЕДНИМ шагом (IC5) и
  только при зелёном `verify-contour`; голос и приватные пути — окружение, не код.
- **(б) Путь `.kaif/tools/contour/` не поддержан сборщиком (плоский `framework/tools/`).** Защита:
  `FORK:` в IC0 — разведкой сборщика; при плоской поставке спецификация и навыки называют реальный
  путь, а сценарий №024 остаётся исполненным по смыслу (инструмент приехал поставкой).
- **(в) Генератор без браузера в полигоне — «зелёный», а страница в живом браузере сломана.**
  Защита: `s22` судит HTML-инварианты (радиогруппы = вопросы, форма ответа, CSS шапки), а живой
  браузер — приёмка истока (`verify-contour`, критерий 5) — та же пара «полигон + приёмка», что у
  контура истока с 2.2.
- **(г) Третье лицо (макеты) спроектировано без референса и не нужно полю в такой форме.** Защита:
  минимальная форма по сценарию B №024 (картинка + комментарии), помечена как первая версия;
  расширение — по полевым тикетам.
- **(д) Бюджеты документов ядра** (`check` предупреждает): спецификация — отдельный файл `.kaif/`,
  канон-документы растут только ссылкой.

## Решения, принятые агентом без владельца

1. (при написании плана) решений не было — план написан на закрытии FK без вопросов к владельцу.
2. **IC0, развилка сборки → A, вложенный путь `.kaif/tools/contour/`** (строка `FORK:` в шаге IC0):
   ядро пишет вложенные dest без правок; сборщик получает рекурсивный обход `framework/tools/`
   (зеркало — счёт tool-модулей в `check-framework`, строка реестра пар); путь совпадает со словом
   владельца (№101). Отклонены: B (два плоских файла — уход от названного пути ради двух строк),
   C (директорный модуль по образцу `hooks/` — новый класс без нужды).
3. **IC0: лица «вычитка по абзацам» и «отсмотр макетов» — новые, проектируются минимально** (в
   референсе истока их нет: вычитка = одно поле комментария к документу классом «сообщение»);
   контракт — сценарий B интервью №024 дословно (абзацы с полем комментария у каждого и кнопкой
   «Готово»; картинка + поле комментариев), расширение — по полевым тикетам.
4. **IC0: параметры генератора без вопроса владельцу (№97)** — `contour.*` в `.kaif/kaif.json` с
   дефолтами: имя проекта → `basename(root)`, владелец → таблица идентичности `AGENT_GUIDE` → «owner»,
   каталог решений → `interviews/decisions`, тихие часы → нет, язык → `language`; голос — только
   контракт окружения (`KAIF_VOICE_TOOL` · `KAIF_VOICE` · `KAIF_SAPI_VOICE`), пути и имена доноров
   в поставку не едут.

## Черновик ответа в issue #51 (отправка — после релиза 2.6, №84/№92)

> Taken into KAIF 2.6 by the owner's word (decision №101, revising 2.2's №34). The contour no
> longer gets rebuilt per project: the update brings a one-page executable spec
> (`.kaif/INTERACTIVE_CONTOUR_SPEC.md` — question/option form, decision and shown records,
> pre-flight, keep-alive, the "radiogroups = questions" self-check) and a shipped generator
> (`.kaif/tools/contour/`) with three faces — interview, proofreading, mockup review — reading the
> project's parameters from `.kaif/kaif.json`, never asking the owner. Its self-test goes red on
> exactly your case: options typed as paragraphs open a page without radio buttons — the pre-flight
> now refuses to open it and prints the form to fix. `/owner-reviews` says "run it", not "build it".
> The origin itself now runs the shipped generator. Thank you — the fourth ticket of this class made
> the machinery ship.

## Links

`plans/89` (критерий 14, эпик IC) · issue #51 · `ideas/28` п. 29 · `interviews/interview_024`
(№101) · `MASTER_PLAN.md` §7 №34/№101 · `framework/skills/owner-reviews/SKILL.md` (контракт
I1–I43) · `tools/review.mjs` · `tools/lib/review-core.mjs` · `tools/verify-contour.mjs` ·
`tools/sandbox/s12-k5-contour-canon.mjs` · `interviews/interview_025` (#52 — сосед по порядку эпиков).
