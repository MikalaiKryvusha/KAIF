# План 94 — эпик WN «Свежее слово — не топ приоритета»: `/what-next` открывается метрикой и главной фазой из документов, свежие слова владельца лежат на отдельной полке, форму ответа стережёт линт

> **Создан:** 2026-09-05 22:49 +03:00 (закрытие эпика IC, канон N+1 — №43; сессия 56).
> **Родитель:** `plans/89` (эпик WN; якоря — критерий приёмки 15 «[WN — свежее слово владельца — не топ
> приоритета]», строка 5 таблицы эпиков); источник — issue #53 (NDim Space, 2026-09-05 20:35 +03:00) и
> слово владельца в чате истока ≈ 20:34 +03:00 («смежие предложения владельца сразу воспринимаются ИИ
> агентом как ТОП приоритет - это неправильно» · «берем тоже в 2.6 это фиксить», №102); реестр —
> `ideas/28` п. 31.
> **Статус:** ✅ **ЗАКРЫТ 2026-09-05 23:19 +03:00** (сессия 56, один чат параллельно с раундами интервью №026; WN0–WN6 ✅; судья VERIFIED WITH CAVEATS, 0 REFUTED; критерий 15 `plans/89` ✅ — закрыто 10 из 15); следующий эпик — CB (`plans/95`); был 🔲 запланирован 2026-09-05 22:49 +03:00 — старт с шага WN0 ПОСЛЕ разведдока класса #52
> (`researches/29`, строка 6 таблицы `plans/89`, слово владельца №103); вопросов владельцу нет — форма
> ответа задана его словом в тикете, правило применено к себе (WN не ставится первым).
> **Вовне:** поставка — навык `/what-next` обоих слоёв, правило в `AGENT_GUIDE` обоих слоёв («Набросы
> владельца посреди другой задачи»), tool-модуль `framework/tools/kaif-ranking-lint.mjs` →
> `.kaif/tools/kaif-ranking-lint.mjs`, охота `/fable-judge` обеих копий; `POLICY_CHANGES_BY_VERSION['2.6']`
> / `TEMPLATE_NOTES_BY_VERSION['2.6']`; свод полигона `s23`; черновик ответа в issue #53 — отправка после
> релиза (№84/№92).

## Вектор цели (Achieve)

**Боль** (слово владельца в поле дословно, issue #53): «неси это в KAIF в виде методологического бага, что
ты смежее сразу занёс в топ приоритетов». Полевой агент процитировал правило `/what-next` («новейшая боль
— НЕ приоритет сама по себе») и тем же ответом его нарушил: мессенджер и перепись из свежих слов владельца
— наверху; фаза «ГЛАВНОЕ СЕЙЧАС», 1 новый человек за 30 дней, 8 потерянных дней ряда, 87 открытых багов —
не названы. Правило стоит ПРОЗОЙ, а форма ответа его не принуждает: ни одна строка ответа не обязана нести
метрику, и первым шагом законно встаёт что угодно.

**Где хотим оказаться.** Ответ `/what-next` на KAIF 2.6 НЕ МОЖЕТ открыться свежим словом: он начинается
строками `METRIC:` (вектор `DELIVERY:` из `SYSTEMS_REGISTRY.md`, текущее значение) и `MAIN PHASE:` (фаза
`MASTER_PLAN.md`, помеченная «ГЛАВНОЕ СЕЙЧАС»), прочитанными из документов; шаги — таблица фиксированной
формы `| шаг | moves | closes | трудоёмкость |`, где у каждого шага `moves:` (какая компонента метрики
сдвигается или «—») и `closes:` (какие баги/планы закрываются); шаг с `moves: —` и пустым `closes` не
стоит выше шага, у которого есть хотя бы одно; слова владельца за последние 48 ч, ещё не вписанные в
`GOAL`/`MASTER_PLAN`, лежат отдельной полкой «свежие слова владельца — не ранжированы метрикой» с
указателем `/fix-vision`; строка техдолга с числами стоит всегда. Форму стережёт tool-модуль поставки
`kaif-ranking-lint` (черновик ответа → красный, когда строка 1 таблицы несёт `moves: —` с пустым
`closes`, или нет `METRIC:`/`MAIN PHASE:`, или нет полки/строки долга), `/fable-judge` обеих копий
охотится на «recency ranked over metric», `AGENT_GUIDE` обоих слоёв несёт правило ранжирования там, где
уже лежит «Набросы владельца посреди другой задачи». Симметрия к строке `DELIVERY:` закрытия (OQ 2.6).

## Готово, когда (критерии приёмки — сценариями; якорь `plans/89` критерий 15)

1. **[WN — ответ открывается метрикой и фазой, свежие слова на полке]**
   - Ситуация. Проект на KAIF 2.6: час назад владелец мимоходом сказал «нужен MVP мессенджера»; фаза
     `MASTER_PLAN.md` «ГЛАВНОЕ СЕЙЧАС» — трафик; открытых багов 87, красных 30; вектор доставки снят.
   - Действие. Владелец спрашивает «что дальше?», агент исполняет `/what-next`.
   - Результат. Владелец видит ответ, который открывается строками `METRIC: DELIVERY: …` и
     `MAIN PHASE: …` из документов; таблицу `| шаг | moves | closes | трудоёмкость |`, где первый шаг
     сдвигает метрику или закрывает баг; полку «свежие слова владельца — не ранжированы метрикой» с
     «MVP мессенджера» и указателем `/fix-vision`; строку техдолга «открытых 87 · красных 30 · пар
     разъехалось N».
   - Проверка. `grep -c "METRIC:\|MAIN PHASE:" framework/skills/what-next/SKILL.md
     .claude/skills/what-next/SKILL.md` → `≥ 1` в каждом; `grep -c "fix-vision" framework/skills/what-next/SKILL.md`
     → `≥ 1`; `grep -c "moves" framework/skills/what-next/SKILL.md` → `≥ 1`.
2. **[WN — линт формы ответа красный на инциденте #53]**
   - Ситуация. Агент набросал ответ `/what-next` в черновик `.kaif/.what-next-draft.md` (или любой md):
     первым шагом «MVP мессенджера» с `moves: —` и пустым `closes`, строки `METRIC:` нет.
   - Действие. Агент запускает `node .kaif/tools/kaif-ranking-lint.mjs check <черновик.md>`.
   - Результат. Линт печатает, что не так (строка 1 — свежее слово без метрики; `METRIC:` отсутствует;
     полки нет), завершается кодом 1; после исправления (серия трафика → Яндекс → каталог → строка
     долга) — код 0; на документе без таблицы `/what-next` — `SKIPPED`, код 3 (как у `kaif-scenario-lint`).
   - Проверка. `node framework/tools/kaif-ranking-lint.mjs selftest` → зелёный, называет фикстуру #53
     красной и исправленный ответ зелёным; свод `s23` на развёрнутой копии → `.kaif/tools/kaif-ranking-lint.mjs`
     на месте, `check` фикстуры → 1, исправленного → 0, чужого md → 3.
3. **[WN — правило и охота в обоих слоях]**
   - Ситуация. Исток после WN; судья читает отчёт сессии, где первый шаг цитирует слово владельца того
     же дня без метрики.
   - Действие. `/fable-judge` по отчёту; агент читает «Набросы владельца посреди другой задачи».
   - Результат. Охота «recency ranked over metric» (KAIF 2.6) стоит в блоке охот обеих копий
     `/fable-judge` и называет находку; правило ранжирования («свежее слово получает ранг по метрике,
     а не по дате; до `/fix-vision` оно лежит на полке») стоит в `AGENT_GUIDE` обоих слоёв рядом с
     «Набросы владельца посреди другой задачи».
   - Проверка. `grep -ci "recency" framework/skills/fable-judge/SKILL.md .claude/skills/fable-judge/SKILL.md`
     → `≥ 1` в каждом; `grep -c "полк\|shelf" AGENT_GUIDE.md framework/AGENT_GUIDE.md` → `≥ 1` в каждом
     (в абзаце «Набросы владельца…» / «Owner asides…»).
4. **[WN — обновление привозит линт и записи]** — `s23`: `update` → `.kaif/tools/kaif-ranking-lint.mjs` на
   месте; мета бандла — policy-change/notes 2.6 называют `/what-next` и линт без «спросите владельца».
5. **[судья эпика]** — `/fable-judge` по критериям 1–4, таблица вердиктов здесь; на закрытии по канону N+1 —
   план следующего эпика CB (`plans/95`) и веха в `plans/89` (критерий 15 ✅, строка 5).

## Шаги (каждый цитирует якорь мета-плана — `plans/89` критерий 15 / строка 5 таблицы)

- [x] **WN0 — нулёвка актуализации входов (№76) — ✅ 2026-09-05 23:09 +03:00 (сессия 56).** Снято: `/what-next` 43/42 строки, шаг 3 — три пункта прозой; правило набросов — RU строка 845, EN 746; блок охот судьи — EN-текст, побайтно одинаковый в обеих копиях (вендорено), последняя — «Confusion delivered as verdict»; образец tool-модуля `kaif-scenario-lint.mjs` (248 строк: KEYWORDS по языкам, RULES как данные, `parseScenarios`/`lint`, `check` с `SKIPPED=3`, `selftest` «мутация N → правило N» в EN/RU); разметки «ГЛАВНОЕ СЕЙЧАС» нет ни в `MASTER_PLAN` истока, ни в шаблоне поставки — `MAIN PHASE:` берёт помеченную фазу, иначе первую незакрытую, и говорит об этом; `FORK: options <линт читает файл | stdin> · price of error <форма вызова в навыке обоих слоёв> · consulted <kaif-scenario-lint: check <paths>, файл>` → файл. **Постановка была:** Сверить против HEAD: `/what-next` обоих слоёв (43/42
      строки на 2026-09-05), место правила в `AGENT_GUIDE` обоих слоёв (RU: «Набросы владельца посреди
      другой задачи», строка ≈ 845), блок охот `/fable-judge` обеих копий (последняя — «Confusion delivered
      as verdict (KAIF 2.6)»), форму tool-модуля с `SKIPPED=3` (`kaif-scenario-lint.mjs` — образец: правила
      как данные, ключевые слова по языкам, селфтест с фикстурами), запись `DELIVERY:` (`kaif-core delivery`)
      как источник `METRIC:`, разметку «ГЛАВНОЕ СЕЙЧАС» в `MASTER_PLAN` истока и в шаблоне поставки
      (есть ли — если нет, `MAIN PHASE:` берёт первую незакрытую фазу и говорит об этом); `FORK:` — линт
      читает черновик ФАЙЛОМ (образец `kaif-scenario-lint`) vs. stdin — по разведке.
- [x] **WN1 — навык `/what-next` обоих слоёв (критерий 1) — ✅ 23:05:** шаг 3 «Ответь в чате — ФИКСИРОВАННОЙ ФОРМОЙ» с блоком формы (`METRIC:` · `MAIN PHASE:` · таблица · полка · строка долга), правила таблицы, линт черновика до печати; `grep -c METRIC:` → 2/2. **Постановка была:** Шаг 3 «Ответь в чате» → фиксированная форма:
      `METRIC:` · `MAIN PHASE:` · таблица `| шаг | moves | closes | трудоёмкость |` · полка «свежие слова
      владельца — не ранжированы метрикой» (источник — слова за 48 ч, не вписанные в `GOAL`/`MASTER_PLAN`;
      указатель `/fix-vision`) · строка техдолга с числами (открытые/красные баги — `ls bugs/*.md | grep -vc DONE`,
      разъехавшиеся пары — реестр пар `AGENT_GUIDE`) · правило порядка (шаг с `moves: —` и пустым `closes` не
      выше шага с одним из них); шаг 2 — «свежий инцидент получает ранг по метрике» уже есть, дополнить
      полкой; RU — перевод от смысла; черновик ответа перед печатью — через линт (`check`).
- [x] **WN2 — правило в `AGENT_GUIDE` обоих слоёв (критерий 3) — ✅ 23:05:** «Записанный наброс ранжируется метрикой, а не датой» / «A recorded note is ranked by the metric, not by its date» в абзаце о набросах обоих слоёв. **Постановка была:** Абзац «Набросы владельца посреди другой
      задачи» → добавить: наброс, вписанный в беклог, до `/fix-vision` ранжируется метрикой, а не датой;
      в `/what-next` он виден на полке, не в таблице; EN — в `framework/AGENT_GUIDE.md` тем же местом.
- [x] **WN3 — tool-модуль `kaif-ranking-lint` (критерий 2) — ✅ 23:05:** `framework/tools/kaif-ranking-lint.mjs` — семь правил как данные (`no-metric` · `no-main-phase` · `no-table` · `recency-first` · `order` · `no-shelf` · `no-debt`), `parseAnswer` (fenced и `>` невидимы), `check` с `SKIPPED=3`, `selftest` 19 кейсов зелёный (фикстура #53 красная: пять правил; мутация N → правило N в EN/RU; чужой документ — не ответ); первый прогон краснел на `recency-first` (мутация задевала и `order`) — правило `order` судит строки со второй, строка 1 — `recency-first`. RU-якоря (полка, строка долга) — модуль назван в `CYRILLIC_DATA_CARRIERS`. **Постановка была:** `framework/tools/kaif-ranking-lint.mjs`
      (EN, гард 5d; ключевые слова RU/EN как данные — в `CYRILLIC_DATA_CARRIERS` поимённо): `check <md>`
      → разбор блока ответа (`METRIC:` · `MAIN PHASE:` · таблица · полка · строка долга) → находки с номером
      строки и кодом 1; `SKIPPED` код 3 без таблицы `/what-next`; `selftest` — фикстура #53 (мессенджер
      первым, `moves: —`, без `METRIC:`) красная, исправленный ответ зелёный, чужой md — SKIPPED; маркер
      `[NOT-TESTED]` → `[TESTED]` по наблюдению; сборка подхватит модуль сама.
- [x] **WN4 — охота судьи обеих копий (критерий 3) — ✅ 23:05:** «Recency ranked over metric (KAIF 2.6)» после «Confusion delivered as verdict», обе копии побайтно равны (`diff` пуст). **Постановка была:** «Recency ranked over metric (KAIF 2.6)» в блок охот
      после «Confusion delivered as verdict»: первый шаг ответа `/what-next`/отчёта цитирует слово владельца
      того же дня, а `METRIC:`/`moves:` строки нет — находка; RU-копия — перевод от смысла; `diff` формы блока.
- [x] **WN5 — сборка, полигон, поставка (критерий 4) — ✅ 2026-09-05 23:19 +03:00 (сессия 56; полигон — строка STATUS):** записи `TEMPLATE_NOTES_BY_VERSION['2.6']`/`POLICY_CHANGES_BY_VERSION['2.6']` о форме `/what-next` и линте (первая редакция уронила сборку неэкранированным апострофом в `owner's` — переписано без апострофов); модуль назван в `CYRILLIC_DATA_CARRIERS` (RU-якоря полки и строки долга); карты и `AGENT_GUIDE` перечисляют `kaif-ranking-lint` по имени; сборка `177 file blocks`, `counters-guard` OK после подъёма четырёх зеркал 176 → 177; свод `tools/sandbox/s23-ranking-lint.mjs` в `SUITES` (23 свода; зеркала «22 → 23» подняты) — зелёный стоя: установка → модуль на месте, `selftest OK`, черновик #53 → exit 1 с пятью находками, исправленный → 0, чужой → SKIPPED 3, `check` без путей → usage 1, мета бандла — записи 2.6 без «спросите владельца». **Постановка была:** `node tools/build-framework.mjs` → счётчики
      зеркал по выводу (модули вырастут, tool-модули ПО ИМЕНАМ в трёх картах — `counters-guard`); свод
      `tools/sandbox/s23-ranking-lint.mjs` (свежая установка → `selftest` зелёный · `check` фикстуры → 1 ·
      исправленного → 0 · чужого → 3; `update` привозит модуль; мета бандла — записи 2.6) + `SUITES`;
      `POLICY_CHANGES_BY_VERSION['2.6']`/`TEMPLATE_NOTES_BY_VERSION['2.6']`; `npm run test:core`.
- [x] **WN6 — документы и судья (критерий 5) — ✅ 2026-09-05 23:19 +03:00 (сессия 56):** карты (внешняя — строка модуля; внутренняя — `kaif-ranking-lint` и `contour/` в строке tool-модулей), `AGENT_GUIDE` дерево, `KAIF_REFERENCE` (источник + корневая копия) — строка таблицы tool-модулей, `ideas/28` п. 31 — веха, черновик ответа #53 — ниже; судья эпика — таблица ниже; `plans/89` критерий 15 ✅, строка 5; план CB — `plans/95` (канон N+1). **Постановка была:** Карты (внешняя — строка модуля; внутренняя — контур
      планирования/`/what-next`), `KAIF_REFERENCE` (источник) — строка о линте, `KAIF_FRAMEWORK`; черновик
      ответа #53 — ниже; `ideas/28` п. 31 — веха; судья эпика → таблица; `plans/89` критерий 15 ✅, строка 5;
      план CB — `plans/95` (канон N+1).

## Судья эпика — критерии 1–4 (WN6, сессия 56, 2026-09-05 23:19 +03:00)

Лёгкий судья по канону `/fable-judge`: каждое «готово, когда» — заявление; улика — переисполненная команда из
строки «Проверка» (числа — из вывода команд 23:1x–23:2x).

| Критерий | Вердикт | Улика (переисполнено) | Каверза |
|---|---|---|---|
| 1. Ответ открывается метрикой и фазой, свежие слова на полке | VERIFIED | `grep -c "METRIC:\|MAIN PHASE:"` → 3 / 3 (EN / RU); `fix-vision` → 2; `moves` → 3 в `framework/skills/what-next/SKILL.md` | живой ответ `/what-next` в этой сессии не давался — форма судится по тексту навыка и линтом; первый живой ответ проверит `check` |
| 2. Линт красный на инциденте #53 | VERIFIED | `node framework/tools/kaif-ranking-lint.mjs selftest` → «19 cases, 7 rules × 2 languages… the #53 fixture red»; `s23` стоя и в полигоне: #53 → exit 1 (пять правил), исправленный → 0, чужой → SKIPPED 3 | `MAIN PHASE:` судится присутствием строки, не верностью фазы — пометки «ГЛАВНОЕ СЕЙЧАС» в шаблонах нет (WN0) |
| 3. Правило и охота в обоих слоях | VERIFIED | `grep -ci recency` → 1 / 1 в копиях `/fable-judge` (побайтно равны — `diff` пуст); `grep -c "полк\|shelf"` → 1 / 1 в `AGENT_GUIDE` обоих слоёв | охота — текст; живой судейский прогон по отчёту с «recency» не делался |
| 4. Обновление привозит линт и записи | VERIFIED | `s23 A/C`: модуль на месте после `install`, мета бандла — policy-change и notes 2.6 называют `/what-next`, `METRIC:`, `kaif-ranking-lint`, без «спросите владельца»; `npm run test:core` → «all 23 suites green» | маршрут `update` в `s23` не гонялся (только `install`) — модуль едет тем же блоком бандла, что и остальные tool-модули (маршрут доказан `s22 C` для соседа); полигон 23/23 прогнан ДО маркера `[TESTED]` в модуле (правка — комментарий), повторный прогон — на закрытии чата |

Итог: **VERIFIED WITH CAVEATS, 0 REFUTED**. Критерий 15 `plans/89` ✅ — закрыто 10 из 15.

## Верификация наблюдением

Грепы критериев 1 и 3; `node framework/tools/kaif-ranking-lint.mjs selftest` EXIT 0 с именованной красной
фикстурой; `node tools/build-framework.mjs` EXIT 0; `npm run test:core` → `all 23 suites green`;
`node tools/counters-guard.mjs` (tool-модули по именам); `node tools/check-framework.mjs --selftest` (гард
5d с новым носителем); реестр пар `AGENT_GUIDE` (обе копии навыка и судьи); `node tools/private-names-guard.mjs`.

## Риски (ярусы Мёрфи)

- **(а) Линт формы над прозой — ложные красные на живых ответах.** Защита: линт судит ТОЛЬКО блок с
  таблицей `| шаг | moves | closes |` и строками `METRIC:`/`MAIN PHASE:`; документ без них — `SKIPPED`,
  не красный (образец `kaif-scenario-lint`); селфтест — обе стороны (красный на #53, зелёный на исправленном).
- **(б) `MAIN PHASE:` не из чего читать** (в проекте нет пометки «ГЛАВНОЕ СЕЙЧАС»). Защита: навык говорит,
  откуда берётся фаза (пометка → иначе первая незакрытая фаза `MASTER_PLAN`) и печатает источник; линт
  требует строку, а не конкретную фазу.
- **(в) Правило применяется к себе и WN встаёт первым «потому что свежий».** Защита: порядок IC → [#52
  разведдок] → WN зафиксирован словом владельца и STATUS; этот план написан на закрытии IC, исполняется
  после `researches/29`.
- **(г) Бюджеты документов** (`check` предупреждает): навык растёт формой ответа (≈ +20 строк), правило —
  одним абзацем.

## Решения, принятые агентом без владельца

1. (при написании плана) решений не было — форма ответа взята из тикета #53 дословно (предложение полевого
   агента, принятое словом владельца №102); развилка чтения черновика линтом (файл vs stdin) — в WN0 разведкой.

## Черновик ответа в issue #53 (отправка — после релиза 2.6, №84/№92)

> Taken into KAIF 2.6 by the owner's word (decision №102). The rule "the newest pain is not a priority
> claim" already stood in `/what-next` as prose — and prose does not rank. In 2.6 the answer has a
> FORM: it opens with `METRIC:` (the delivery vector) and `MAIN PHASE:` read from the documents; every
> step carries `moves:`/`closes:`; the owner's words of the last 48 hours that are not yet in
> GOAL/MASTER_PLAN sit on a separate shelf ("not ranked by the metric", pointer `/fix-vision`); the
> tech-debt line with numbers is mandatory. A shipped lint (`.kaif/tools/kaif-ranking-lint.mjs`) goes
> red on exactly your case — the messenger MVP on line 1 with no metric — and `/fable-judge` hunts
> "recency ranked over metric". Applied to itself: this epic was NOT put first because it was fresh.

## Links

`plans/89` (критерий 15, строка 5) · issue #53 · `ideas/28` п. 31 · `framework/skills/what-next/SKILL.md` ·
`.claude/skills/what-next/SKILL.md` · `framework/tools/kaif-scenario-lint.mjs` (образец tool-модуля) ·
`framework/skills/fable-judge/SKILL.md` (блок охот) · `plans/90` (строка `DELIVERY:` — симметрия) ·
`researches/29` (идёт перед WN — слово владельца №103).
