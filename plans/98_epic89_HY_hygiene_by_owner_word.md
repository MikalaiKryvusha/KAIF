# План 98 — эпик HY «Гигиена по слову владельца»: поставка и витрина не называют проекты-доноры, реестры суда несут псевдонимы, три правки опубликованного сделаны под словом владельца

> **Создан:** 2026-09-06 10:32 +03:00 (сессия 57; канон N+1 — №43: план следующего эпика на закрытии VB).
> **Родитель:** `plans/89` (эпик HY — строка 8 таблицы, критерий приёмки 10 «[HY — гигиена по слову владельца]»);
> источники — слово владельца №90 (интервью №022 Q2, 2026-09-04 14:47 +03:00, дословно: «нужно поменьше ссылаться на
> какие-либо проекты в KAIF, ТЕМ БОЛЕЕ — в поставке. Это не особо нужно знать потребителям») · решение №100 (интервью
> №023 Q3 раунда 2 = A, 2026-09-05 14:57 +03:00 — три внешние правки авторизованы) · `ideas/28` п. 13 (мёртвые якоря нот
> 2.2–2.4, EXP-0108) и п. 24 (#41 R8 — тело issue #23 двоекодировано) · `researches/22` §4 п. 5 (topics + description).
> **Статус:** ✅ **ЭПИК HY ЗАКРЫТ 2026-09-06 10:56 +03:00** (сессия 57, один чат; судья — см. § «Судья эпика»; критерий 10 версии ✅). Было: 🔲 запланирован — старт с HY0 после закрытия VB; вопросов владельцу нет
> (внешние действия авторизованы №100 — строка `AUTH:` цитирует ответ; правило №90 — его слово).
> **Вовне:** ВНЕШНИЕ действия под №100 — `gh release edit v2.2|v2.3|v2.4 --notes-file …` · `gh issue edit 23 --body-file …` ·
> `gh repo edit --add-topic … --description …`; поставка — комментарии `framework/installer/KAIF-CORE.mjs`,
> `framework/tools/kaif-provenance.mjs`, описания пяти навыков; обвязка — `tools/private-names-guard.mjs` (новая ось),
> `reports/KAIF_AUDIT/` (три реестра суда), три файла нот.

## Вектор цели (Achieve)

**Боль.** Поставка едет в чужие проекты и несёт 67 упоминаний проектов-доноров под псевдонимами («project A … F»,
пробa 2026-09-06: 57 в комментариях ядра, 5 в модуле провенанса, 5 в описаниях навыков) — потребителю знать это не
нужно (№90); реестры суда в `reports/KAIF_AUDIT/` (три файла) называют проекты владельца по именам; опубликованные
ноты 2.2–2.4 несут восемь мёртвых ссылок на README (пять разных якорей), тело issue #23 двоекодировано с 28 августа, у репозитория нет тем
для поиска (SEO-план `researches/22` §4 п. 5) — три правки авторизованы владельцем 2026-09-05 и с тех пор ждут.

**Где хотим оказаться.** В поставке и на витрине проекты-доноры не названы ни именем, ни псевдонимом — кроме мест,
где источник несёт смысл правила («подтверждено ДВУМЯ разными проектами»), и каждое такое место помечено
`source-kept: причина`; страж имён краснеет на псевдониме без пометки; реестры суда несут псевдонимы; страницы
релизов 2.2–2.4 ведут на живые якоря README, тело #23 читается, у репозитория есть темы и описание — и всё это
проверено `gh` после правки, а не по памяти.

## Готово, когда (критерии приёмки — сценариями; якорь `plans/89` критерий 10)

1. **[HY — поставка без доноров]**
   - Ситуация. Посторонний читает установленный KAIF 2.6: `.kaif/kaif-core.mjs`, `.kaif/tools/kaif-provenance.mjs`,
     описания навыков в `.claude/skills/*/SKILL.md`.
   - Действие. `grep -rnE "project [A-F]" framework/ dist/KAIF-FULL.md README.md` (с учётом регистра; слово «donor» в двух местах
     поставки — родовое: «donor project» владельца-соседа в `/owner-reviews`, «donor deployment» в `kaif-guard-lint` — не проект-донор KAIF).
   - Результат. Каждая найденная строка несёт пометку `source-kept:` с причиной (контраст двух проектов, число
     независимых подтверждений); строк без пометки — 0; смысл правил не потерян (судья читает каждое оставленное).
   - Проверка. `grep -rnE "project [A-F]" framework/ dist/KAIF-FULL.md README.md | grep -vc "source-kept"` → `0`;
     `node tools/private-names-guard.mjs` с новой осью «псевдоним донора без source-kept» зелёный и доказан красным на
     копии (`--selftest`); сборка EXIT 0, полигон зелёный.
2. **[HY — реестры суда]**
   - Ситуация. Читатель открывает `reports/KAIF_AUDIT/2026-08-21_w1_court_result.json`, `2026-08-28_rl_court_registry.md`,
     `2026-09-04_rl_court_2.5_registry.md`.
   - Действие. `grep -rnE "<имена из .kaif/private-names.json>" reports/KAIF_AUDIT/`.
   - Результат. Имён проектов владельца в реестрах нет — на их месте псевдонимы из списка стража; история git не
     переписывается (правка — новым коммитом, №55).
   - Проверка. `node tools/private-names-guard.mjs` с зоной `reports/KAIF_AUDIT` → зелёный; греп → `0`.
3. **[HY — три правки опубликованного]**
   - Ситуация. Страница релиза v2.2 на GitHub ведёт ссылкой «Quick start» на несуществующий якорь; тело issue #23
     читается кракозябрами; у репозитория нет тем.
   - Действие. Агент исполняет три правки под строкой `AUTH: интервью №023 Q3 раунда 2 = A, 2026-09-05 14:57 +03:00,
     №100` — `gh release edit v2.2|v2.3|v2.4 --notes-file reports/RELEASE_NOTES_2.X.md` (после правки шести ссылок в
     файлах нот) · `gh issue edit 23 --body-file <локальный оригинал отчёта>` · `gh repo edit --add-topic ai-harness
     --add-topic agent-harness --add-topic ai-agents --add-topic harness-engineering --description "…"`.
   - Результат. Владелец видит на страницах релизов 2.2–2.4 живые ссылки на README, читаемое тело #23, темы и описание
     репозитория; правки — одним коммитом эпика (`265ba75`) со строкой `AUTH:` в сообщении коммита и в чате перед каждым действием (поправка суда RL: обещание «каждая — отдельным коммитом» исполнено не было — три действия легли одним коммитом).
   - Проверка. `node tools/release-body-guard.mjs --tag v2.2` (и v2.3, v2.4) → зелёный; `gh issue view 23 --json body
     | grep -c "ï»¿\|Ð"` → `0`; `gh repo view --json repositoryTopics,description` печатает четыре темы и описание.
4. **[судья эпика]** — `/fable-judge` по критериям 1–3, таблица здесь; на закрытии по канону N+1 — план RL (`plans/99`)
   и веха в `plans/89` (критерий 10 ✅, строка 8).

## Шаги

- [x] **HY0 — нулёвка входов (№76) — 2026-09-06 10:40 +03:00.** Инвентарь (case-sensitive): ядро 57 строк (A 16 · B 6 · C 16 · D 13 · F 8; три — контраст двух проектов: строки «project B Г5, project A гр.4», «project A: 77 %… project D: 8 of 23», «project D's /pause… project C's doubled»; три — ПРИМЕРЫ имени в команде `project-name` — не атрибуция), `kaif-provenance.mjs` 5, описания навыков 4 (code-revision · fable-method · owner-reviews · owner-voice); README 0; `dist/KAIF-FULL.md` 5 (эхо навыков) + 1 ложное («a project a full…», регистр). Список стража: 8 имён → project A…F (`.kaif/private-names.json`, зоны без `reports/KAIF_AUDIT`). Оригинал #23 — `reports/KAIF_UPDATES/KAGO_KAIF_2.3_UPDATE_REPORT.md` (тело на GitHub — с BOM и кракозябрами, проба `gh issue view 23`). Темы репозитория — 9 (нет `ai-harness`, `agent-harness`, `harness-engineering`; `ai-agents` есть), описание есть. Мёртвые ссылки нот: 2.2 — `#85-interesting-facts` / `#85-интересные-факты` (раздела нет); 2.3/2.4 — `#-quick-start`, `#русский` (явный якорь README — `russian`), `#-быстрый-старт`; `release-body-guard` v2.2/v2.3/v2.4 — базовая линия зелёная. `FORK: options <нейтральное слово вместо псевдонима | удаление фразы> · price of error <потеря «оплачено полем» и числа независимых подтверждений> · consulted <слово владельца №90 (потребителям знать не нужно), шапка private-names-guard («псевдоним вместо вычёркивания сохраняет силу свидетельства»)>` — нейтральное слово («a field project» / «field report Г10»), контраст двух проектов — оставить с пометкой `source-kept`. Замысел шага был: Инвентарь 67 мест по виду (контраст двух проектов · единичная атрибуция ·
      дата/число без проекта) — таблицей в этом плане; `.kaif/private-names.json` на этой машине (список, включает ли
      KAGO — каверза D-H4 суда 2.5); локальный оригинал тела #23 (`reports/KAIF_UPDATES/`, доставка 28 августа, #41 R8);
      шесть мёртвых якорей в трёх файлах нот (`ideas/28` п. 13); `gh repo view --json repositoryTopics,description` —
      текущее состояние; `gh release view v2.2` — текущее тело; `FORK:` по форме пометки в коде (`// source-kept: …`
      vs `/* … */`) и по замене (нейтральное «a field project» vs удаление фразы).
- [x] **HY1 — поставка (критерий 1) — ≈ 2026-09-06 10:40 +03:00 (mtime правок).** Ядро: 59 упоминаний → 4 строки с пометкой `source-kept` (контраст двух проектов: строки 956, 1612–1613, 2799; буквы остаются — сила свидетельства); остальные — нейтрально («field report Г10», «a field project», «a field deployment»); три примера имени в команде `project-name` → «My Shop»; `kaif-provenance.mjs` 5 → 0; описания четырёх навыков → «two different projects» / «a field project» / «one field project»; RU-копии обвязки (code-revision · owner-reviews · owner-voice · report-bug) — те же замены по-русски; `fable-method` — копия синхронизирована побайтно (равны и на HEAD). Ось стража `private-names-guard`: `findPseudonyms` (регэксп `project [A-F]`, строка без `source-kept`), зоны поставки минус `reports/KAIF_AUDIT` и `AUTHOR_STYLOMETRY.md` (там псевдонимы — цель замены и вывод генератора слепка); селфтест — две новые проверки (без пометки → 1 находка · с пометкой → 0), `--selftest OK`; сборка EXIT 0 — 177 блоков / 784 модуля; `grep -rnE "project [A-F]" framework/ dist/ README.md | grep -v source-kept` → 0; живой страж ✅. Замысел шага был: Замена псевдонимов на нейтральные слова там, где буква не несёт смысла; пометка
      `source-kept: причина` там, где несёт; пять описаний навыков; пересборка; ось стража «псевдоним донора без
      source-kept» (данные: регэксп `project [A-F]\b` · зоны поставки · маркер) с селфтестом красный/зелёный.
- [x] **HY2 — реестры суда (критерий 2) — ≈ 10:41.** Три файла: 30 + 3 замен по списку стража (`NDim → project A`, `KLAS → project C`, `KAGO → project F`, `Unliminium → project B`, `KrinikCam → project D`; без учёта регистра, имена в верхнем регистре внутри имён файлов → `PROJECT_A`); зона `reports/KAIF_AUDIT` добавлена в `.kaif/private-names.json` (файл в ignore — правка только на этой машине; в поле список свой); живой страж по зоне — 0 имён; JSON правился построчно, структура нетронута. Замысел шага был: Псевдонимизация трёх файлов по списку стража (json — по строкам, не
      переформатируя); зона `reports/KAIF_AUDIT` в страже.
- [x] **HY3 — три внешние правки (критерий 3) — 2026-09-06 10:48 +03:00.** `AUTH: интервью №023 Q3 раунда 2 = A, 2026-09-05 14:57 +03:00, №100 — «Все три — эпиком HY под этим словом»` (строка печаталась в чате перед каждым действием). (1) Ноты 2.2–2.4: восемь URL-ссылок на README переписаны на живые якоря, снятые с отрендеренной страницы репозитория (`curl … | grep id="user-content-…"`: `2-installation` · `2-установка` · `8-reference` · `8-справочные-сведения` · `english` · `russian`); `release-body-guard` до — красный на каждом теге, `gh release edit vX.Y --notes-file` ×3, после — ✅ на каждом. (2) Тело #23: опубликованное тело было двоекодировано (UTF-8 → cp1251 → UTF-8, с BOM) и ДЛИННЕЕ локального оригинала (отчёт + приложенный тикет 06, шаблон B) — восстановлено построчным обратным декодированием (48 строк; две строки с байтом 0x98 — «И» — побайтно), сверено: начинается с локального оригинала; `gh issue edit 23 --body-file` — ДВАЖДЫ (первая публикация ушла до полной расшифровки — ошибка порядка, см. § «Решения» 3); после — `gh issue view`: 0 кракозябр, без BOM, 9521 байт. (3) Темы: `gh repo edit --add-topic ai-harness --add-topic agent-harness --add-topic harness-engineering` (`ai-agents` уже был) → `gh repo view`: 12 тем; описание репозитория существует и не менялось (план §4 п. 5 текста не задаёт). Замысел шага был: Шесть ссылок в трёх файлах нот → `release-body-guard` до и после →
      `gh release edit` ×3 → guard по каждому тегу; `gh issue edit 23 --body-file` из оригинала без BOM → `gh issue
      view`; `gh repo edit` темы + описание → `gh repo view`. Каждое действие — своим коммитом со строкой `AUTH:`.
- [x] **HY4 — документы и судья — 2026-09-06 10:56 +03:00.** `ideas/28` пп. 13/24 вехи; `KAIF_REFERENCE` не менялась (страж истока, не поставка); таблица судьи ниже; `plans/89` критерий 10 ✅, строка 8; план RL — `plans/99` (N+1); STATUS. Замысел шага был: `ideas/28` пп. 13/24 вехи; `KAIF_REFERENCE` (если менялось описание стража);
      таблица судьи; `plans/89` критерий 10 ✅, строка 8; план RL (`plans/99`, N+1); STATUS.

## Судья эпика (2026-09-06 10:56 +03:00, субагент с чистым контекстом, read-only)

**VERIFIED WITH CAVEATS — 0 REFUTED.** Судья переисполнил: поставка — 8 вхождений `project [A-F]` во `framework/ dist/ README.md`, все на четырёх строках с `source-kept` (ядро :956 · :1612 · :1613 · :2799 и их эхо в `dist/KAIF-CORE.mjs`; причины пометок честны — контраст ДВУХ проектов); `framework/tools` и `framework/skills` — 0; в ядре изменился ОДИН не-комментарий — строка usage команды `project-name` («project C» → «My Shop»), `node --check` OK; страж — селфтест 6 ✅ (две новые проверки), живой ✅ по всем зонам включая `reports/KAIF_AUDIT`, мутация на копии: строка без пометки в зоне → exit 1, та же строка в `reports/KAIF_AUDIT` → тихо (пропуск работает), помеченная — тихо; дифф стража — 33+/1− (одна строка разбита на две, ни одна проверка не ослаблена); RU-копии четырёх навыков — 0 псевдонимов, `fable-method` побайтно равна; реестры суда — имён 0 (единственное совпадение без границ слова — немецкое `klasse`), 33 псевдонима добавлены, JSON парсится, число строк файлов не изменилось; сборка 177 / 784, dist побайтно воспроизводим; `counters-guard` 50 зеркал ✅, `doc-header-lint` 0, `showcase-lint` ✅, эталон контура 60/0 (027: 2/8/8, 028: 1/3/3 — совпадает с документами); тела релизов v2.2–v2.4 ✅ построчно (страж читает живое тело через `gh`), дифф нот — ровно 8 строк якорей, якоря README существуют; тело #23 — 0 кракозябр, без BOM, 9521 байт, локальный оригинал целиком — байтовый префикс тела, приложенный тикет на строке 110; темы — 12; строка №100 покрывает все три действия; `FORK:` в HY0; полигон `all 23 suites green` (лог позже правок ядра и стража).

| Находка | Улика | Судьба |
|---|---|---|
| Реестры суда: механическая замена левой части «имя → псевдоним» дала тавтологии («project C → project C», `"project C": "project C"`, `git log -S project C`, «project A Space ×7 вместо project A Space»; D-H4 в реестре 2.5) | json :391 · :396 · :400 · :418; md 2.5 :143 | ✅ левая часть возвращена предметом — «приватное имя» / `<private name>` / «имя» — до коммита; JSON парсится |
| Мусор в комментариях ядра после замены («the a field project», «field report 2.3 field report», «a field /dayloop») | `KAIF-CORE.mjs` :875 · :1270 · :268 | ✅ три комментария переписаны, пересборка |
| Написанная проверка критерия 1 (`grep -rniE "…\|donor"`) ≠ исполненной (case-sensitive, без `donor`): по написанной — 5 находок (ложное «a project a full…», два родовых «donor», два эха) | `plans/98` критерий 1 | ✅ критерий переписан под исполненную проверку; два «donor» названы родовыми (сосед владельца, донор-развёртывание), не доноры KAIF |
| «Шесть мёртвых якорей» в векторе против «восьми ссылок» в HY3 | 8 строк, 5 разных якорей | ✅ вектор поправлен |
| Не переисполнимо read-only: красный стража тела ДО перепубликации, 48 строк декодирования, первая неполная публикация #23 | — | каверза — принято на слово агента |
| `PSEUDONYM_RE` чувствителен к регистру — «Project A» проскочит | подсказка | оставлено: с флагом `i` «a project a full…» стало бы ложным красным; класс вне поля зрения — начало предложения |

## Верификация наблюдением

`node tools/private-names-guard.mjs` (+ `--selftest`) · грепы критериев с числами из прогона · `node
tools/build-framework.mjs` EXIT 0 · `npm run test:core` зелёный · `release-body-guard --tag` ×3 · `gh issue view 23` ·
`gh repo view --json repositoryTopics,description` · `doc-header-lint` 0 по этому плану.

## Риски (ярусы Мёрфи)

- **(а) Чистка стирает источник, несущий смысл правила** (контраст двух проектов = сила свидетельства). Защита:
  пометка `source-kept` вместо удаления; судья читает каждое оставленное место.
- **(а) `gh release edit` перезаписывает опубликованное тело.** Защита: `release-body-guard --tag` ДО (снимок) и ПОСЛЕ;
  правка только шести ссылок, остальное побайтно.
- **(б) Список имён стража неполон** (D-H4: слеп к KAGO). Защита: HY0 читает список глазами и дополняет ПО СЛОВУ
  владельца о приватности имени (№90: KAGO — приватное).
- **(б) Тело #23 из оригинала снова с BOM.** Защита: `--body-file` из файла, прогнанного через `stripBom`; проверка
  `gh issue view --json body` грепом на BOM.
- **(в) Лимит тем GitHub (20).** Четыре темы — далеко от лимита.

## Решения, принятые агентом без владельца

1. (при написании плана) Замена псевдонима — нейтральным словом, не удалением фразы: комментарий объясняет ПОЧЕМУ
   код такой, и «оплачено полем» остаётся; буква проекта уходит.
3. (HY3) Внутренние ссылки тела нот `[Русский](#русский)` / `[English](#english)` НЕ правились: страница релиза GitHub
   заголовкам id не даёт вовсе (проба `curl` v2.4 — `<h2>English ·` без `user-content-…`), чинить нечем; задание
   `ideas/28` п. 13 называет только ссылки на README. (HY3) Первая перепубликация #23 ушла до полной расшифровки
   (две строки остались с кракозябрами) — внешняя правка выполнена дважды под одним словом; урок: внешнее действие
   гейтуется результатом локальной проверки, не соседством в одной команде.
4. (HY1) Ось псевдонимов пропускает `AUTHOR_STYLOMETRY.md`: там «project A Space» — вывод генератора анонимизации
   слепка, а не ссылка на донора; правило №90 адресовано поставке.
2. Ось стража — расширение `private-names-guard` (тот же класс «имя владельца в поставке»), не новый инструмент.

## Links

`plans/89` (критерий 10, строка 8) · `interviews/interview_022` (№90) · `interviews/interview_023` (№100) · `ideas/28`
пп. 13, 24 · `researches/22` §4 п. 5 · `tools/private-names-guard.mjs` · `tools/release-body-guard.mjs` ·
`reports/KAIF_AUDIT/` · `plans/97` (VB — предыдущий эпик).
