# Handoff CR — шаги CR5–CR6 эпика CR (функциональный прогон переписанного навыка `/code-revision`)

> **Создан:** 2026-09-18 09:30 +03:00 · **Кем:** агент в отдельном worktree
> `D:\work\ai_sandbox\KAIF\.claude\worktrees\agent-a3e6a3faf66ceda0c`, ветка
> `worktree-agent-a3e6a3faf66ceda0c` от HEAD `main` `df8110b` (ветка была на `803ef45` и доведена
> `git merge --ff-only main`). **Не пушено, `gh` не вызывался.**
> **Родитель:** `plans/63` (шаги CR5 и CR6 отмечены; CR1 закрыт интервью №033, CR4 — см. § 6).
> **ВНЕСЕНО интегратором 2026-09-18 10:36 +03:00 (сессия 66, Fable 5.1):** § 2 строка 6 и критерий 11 `plans/100` ✅ · § 3 урок → EXP-0142 ✅ (номер из черновика был занят; шаг 5 навыка тем самым закрыт) · § 4 все семь правок навыка внесены в оба слоя ✅ (`plans/63` CR7) · § 6 п. 1 CR4 ✅, п. 2 фраза `KAIF_REFERENCE` ✅ + прозаическая запись 2.7 сборщика (её НЕ было — в `tools/build-framework.mjs` стояла только карта переименования заголовка) ✅, п. 4 STATUS/MASTER_PLAN ✅ · § 5 фиксы `bugs/118`–`bugs/121` — отдельной задачей (в STATUS).
> **Статус:** 🟡 передача — тексты готовы к вставке, вставку делает главная сессия.
> **Вовне:** — (ничего не уходит наружу: не пушено, `gh` не вызывался, поставка не менялась).
> **Отчёт прогона:** `testcases/reports/2026-09-18_code-revision-rewrite.md` — вердикт **partial**.
>
> Этот файл — ТОЛЬКО для интегратора: тексты ниже готовы к вставке в файлы, которые worktree править
> не имел права (`plans/100`, `STATUS.md`, `MASTER_PLAN.md`, `EXPERIENCE.md`, сам навык
> `code-revision` обоих слоёв, `README.md`, `tools/build-framework.mjs`, `tools/check-framework.mjs`,
> обе копии `fable-judge/SKILL.md`). После вставки — удалить этот файл или пометить тегом `DONE`.

## 1. Что изменено в ветке — построчно

| Файл | Что | Проверка |
|---|---|---|
| `reports/KAIF_AUDIT/2026-09-18_hooks_SUMMARY.md` | НОВЫЙ. Сводка прогона 2 по скелету §2: вердикт до улик · таблица методологии (включая строку effective-FP и строку о базовой линии) · карта покрытия пофайлово · четыре семейства · инвентарь 11 подтверждённых и опровергнутого · «Пределы» · шесть пунктов «что сменить следующему прогону» | `grep -c "^| " reports/KAIF_AUDIT/2026-09-18_hooks_SUMMARY.md` |
| `reports/KAIF_AUDIT/2026-09-18_hooks_guard-judges-a-copy-of-truth.md` | НОВЫЙ. Семейство 1, карточки F1–F3 (8 полей каждая) | — |
| `reports/KAIF_AUDIT/2026-09-18_hooks_state-from-unchecked-input.md` | НОВЫЙ. Семейство 2, карточки F4–F6 | — |
| `reports/KAIF_AUDIT/2026-09-18_hooks_order-asserts-the-unobserved.md` | НОВЫЙ. Семейство 3, карточки F7–F8 | — |
| `reports/KAIF_AUDIT/2026-09-18_hooks_opt-in-doc-promises-otherwise.md` | НОВЫЙ. Семейство 4, карточки F9–F11 | — |
| `reports/KAIF_AUDIT/2026-08-08_guards-machinery-suites_SUMMARY.md` | ДОПИСАН раздел «Дописано 2026-09-18 08:41 +03:00 — effective-FP этого прогона замерен»: таблица пяти находок, вердикт **0 из 5**, отметка, что пп. 1–3 «что сменить» остаются открытыми (жанр append-only соблюдён — ничего не переписано) | `git diff` по файлу: только добавления |
| `bugs/118_hooks_guards_judge_a_copy_of_truth.md` | НОВЫЙ класс-док (S2), 3 вхождения, `TWINS:`, `Fix accepted when` сценарием, `Hygiene:`/`Functional run:` | `node tools/doc-header-lint.mjs` |
| `bugs/119_hooks_state_from_unchecked_input.md` | НОВЫЙ класс-док (S2), 3 вхождения + связанная дыра покрытия от опровергнутой находки | — |
| `bugs/120_hooks_order_asserts_the_unobserved.md` | НОВЫЙ класс-док (S2), 2 вхождения | — |
| `bugs/121_hooks_optin_doc_promises_otherwise.md` | НОВЫЙ класс-док (S2), 3 вхождения | — |
| `testcases/reports/2026-09-18_code-revision-rewrite.md` | НОВЫЙ отчёт прогона, семь полей; таблица «Прогоны» из 22 строк с моментами и командами; строки `Hygiene:`/`Functional run:`; таблица сравнения с прогоном 2026-08-08; § 6 «Места навыка, где исполнитель остановился» (семь мест) | `node framework/tools/kaif-testrun-lint.mjs check` → **11 отчётов, 0 находок** |
| `plans/63_fable_reviews_code_revision_skill.md` | CR5 и CR6 отмечены с моментами и прежними формулировками | — |
| `plans/handoff_CR.md` | этот файл | — |

**Правок в ЗОНЕ (`framework/hooks/`) и в тексте навыка `/code-revision` НЕ делалось** — контракт навыка
«verify, file, fix separately» и правило задачи. Поставка не пересобиралась: ни один файл `framework/`
не тронут, `dist/` не тронут (мутанты — на копиях в скретчпаде).

**Прогоны гигиены (2026-09-18, 08:31 → 09:31 +03:00):** `s14` **68/68** · `check-framework` **0** ·
`counters-guard` **0** (50 зеркал) · `lang-packs-guard` **0** · `kaif-guard-lint check framework/hooks`
**3 (SKIPPED — деклараций в зоне нет)** · `kaif-testrun-lint check` **0** · полигон целиком НЕ запускался
(правило тишины, `bugs/116`).

## 2. Строки для `plans/100` — строка 6 (эпик CR) и критерий 11

**В таблицу эпиков, строка 6 — заменить клетку статуса:**

```
| 6 | **CR «Навык `/code-revision` переписан самим Fable под себя и агентов его класса»** (`plans/63`; …) | … | 1–1,5 | ✅ **закрыт 2026-09-18** — CR0–CR3 (сессия 65), CR1 = интервью №033 Q1 = A, CR5–CR6 (worktree `agent-a3e6a3faf66ceda0c`): навык прогнан ИСПОЛНИТЕЛЕМ на зоне `framework/hooks/` — 12 карточек → 11 выживших у шести независимых скептиков → `bugs/118`–`bugs/121`; выход по контракту (`reports/KAIF_AUDIT/2026-09-18_hooks_*`), отчёт прогона `testcases/reports/2026-09-18_code-revision-rewrite.md` (partial), семь мест остановки исполнителя названы; ОСТАТОК — CR4 (пины/`KAIF_REFERENCE`/чекбокс) и решение по семи предложениям правок навыка |
```

**Критерий 11 — дописать абзац приёмки (сам критерий не менять):**

```
   **Исполнено 2026-09-18.** Оба файла навыка переписаны Фейблом (CR3, сессия 65: 13 мест с цитатой и
   названным провалом исполнения, 6 оставлено с «почему» — `reports/KAIF_AUDIT/2026-09-18_code-revision-skill-by-its-executor.md`),
   и переписанный текст ПРОВЕРЕН ИСПОЛНЕНИЕМ (CR5): прогон по шагам 0–5 на реальной зоне `framework/hooks/`
   дал 12 карточек, 11 выживших после шести независимых скептиков и четыре класс-документа
   `bugs/118`–`bugs/121`; контракт выхода §§1–6 соблюдён (`reports/KAIF_AUDIT/2026-09-18_hooks_SUMMARY.md`
   + четыре FAMILY-документа); effective-FP прошлого прогона замерен по процедуре §5 — 0 из 5.
   Второй результат CR5 — СЕМЬ мест, где исполнитель остановился, с цитатой и правкой (§ 6 отчёта
   прогона); решение по ним — за владельцем/главной сессией, правок в навык worktree не вносил.
   Сборка, счётчики, полигон и реестр пар зелёные (CR4 машинной половиной закрыт; чекбокс CR4 в
   `plans/63` остаётся снятым — за ним пины `lang-packs-guard --update-pins` после сверки ru-алиасов
   глазами, абзац `KAIF_REFERENCE` и запись `TEMPLATE_NOTES['2.7']` — последняя УЖЕ есть).
```

## 3. Черновик урока `EXPERIENCE.md` (вставить как новую запись EXP-0137)

```markdown
### EXP-0137 · 2026-09-18 · ✅ · #skills #executor #instruction-executability #briefs #subagents #baseline #code-revision #cr #hooks
**Ситуация:** навык `/code-revision`, переписанный своим же исполнителем (EXP-0136), надо было проверить
ЕДИНСТВЕННЫМ способом, каким проверяется инструкция, — исполнением: прогон по шагам 0–5 на реальной малой
зоне (`framework/hooks/`, 10 файлов, 564 строки), с реальными субагентами и реальным выходом.
**Что делали:** прогон дал 12 карточек (два ревизора по кластерам заявлений + одну поднял сам исполнитель),
11 выживших у шести независимых скептиков, четыре класс-документа `bugs/118`–`bugs/121`, полный контракт
выхода и замер effective-FP прошлого прогона (0 из 5). Параллельно записывалось ВТОРОЕ: каждое место, где
исполнитель остановился или не понял, что взять, — семь мест (§ 6 отчёта прогона).
**Итог:** ✅ текст исполним и даёт заявленный результат; вердикт отчёта **partial** — из-за двух мест, где
инструкция не держит: шаг «найди базовую линию», помеченный *[mechanical]*, на реальном каталоге даёт НЕ
ревизию (новейший `*_SUMMARY.md` оказался триажной таблицей другой фазы), и «потолок токенов», которого
исполнителю нечем измерить.
**Урок:** (1) инструкцию, переписанную под исполнителя, проверяет только ИСПОЛНЕНИЕ, и проверять надо ДВА
результата сразу — продукт прогона и список мест, где исполнитель остановился; без второго списка правка
текста снова делается наблюдателем. (2) Шаг, помеченный *[mechanical]*, обязан быть верен МЕХАНИЧЕСКИ:
«новейший файл по маске» — это не «запись прошлой работы», пока в маску не добавлен признак жанра;
ошибка такого шага не видна, потому что сильная модель молча берёт правильный файл головой. (3) Письменный
бриф субагенту (форма §7/§8) окупился сразу: восемь субагентов, ни одного уточняющего вопроса, ни одной
стилевой придирки — и ни одной дублирующей карточки между ревизорами, нарезанными по КЛАСТЕРАМ ЗАЯВЛЕНИЙ,
а не по каталогам. (4) Ось, которую не увидел ни один ревизор, увидел исполнитель — тот, у кого в руках
одновременно проводка и контракт вендора; поэтому «одна карточка от исполнителя» — не нарушение
процедуры, а его законная доля. (5) Норма отсева 8 % против полевых 43–44 % — не повод радоваться:
карточки рождались с УЖЕ ИЗМЕРЕННОЙ достижимостью, и скептику почти нечего было убивать; следующий прогон
обязан проверить эту гипотезу порцией неизмеренных карточек.
**Repro:** `node framework/tools/kaif-testrun-lint.mjs check` → отчёт прогона в форме;
`grep -n "Места навыка, где ИСПОЛНИТЕЛЬ остановился" testcases/reports/2026-09-18_code-revision-rewrite.md`;
проверка исполнимости ЛЮБОГО навыка — действием: пройти его шаги на реальном материале и записывать не
результат, а МЕСТА ОСТАНОВКИ.
**Trigger:** навык переписан «под исполнителя» → до объявления готовности прогнать его на реальной малой
зоне и записать места остановки; пишешь навык, оркеструющий субагентов → бриф в справочник, не в голову;
помечаешь шаг *[mechanical]* → проверь, что он верен на РЕАЛЬНОМ каталоге, а не на идеальном.
**Not for:** навыков, чей прогон требует внешних ресурсов или окон на машине владельца (там прогон
заказывается отдельно); вендоренных навыков — их текст побайтно равен апстриму.
**Механизация:** механизации нет: исполнимость инструкции грепом не ловится. Частичная — `kaif-testrun-lint`
требует форму отчёта, а `plans/100` требует отчёт на каждый прогон; кандидат 2.8 (`ideas/30` п. 16) —
«навыки поставки прочитать исполнителем», теперь с уточнением: не прочитать, а ПРОГНАТЬ.
```

## 4. Предложения правок навыка — семь мест, каждое с цитатой и НАЗВАННЫМ провалом исполнения

Правок в навык worktree НЕ вносил (правило задачи: дефект текста навыка — находка отчёта и строка
передачи). Полные формулировки — § 6 отчёта прогона; здесь сжато, по убыванию цены:

| # | Цитата места | Провал исполнения | Предлагаемая правка |
|---|---|---|---|
| S1 | `SKILL.md:27-28` «the newest `reports/KAIF_AUDIT/*_SUMMARY.md` IS the record of the last revision» (шаг помечен *[mechanical]*) | новейший по имени — `2026-08-14_r2_triage_SUMMARY.md`, триажная таблица фазы S: ни карты покрытия, ни «Пределов», ни «что сменить» — ни одного из трёх входов, которые шаг запрашивает; механически шаг даёт неверный ответ | назвать признак принадлежности (таблица методологии с осями и ревизорами / имя навыка в шапке) и что делать, когда новейший SUMMARY его не несёт |
| S2 | `SKILL.md:47-49` «`node .kaif/kaif-core.mjs check`, every `.kaif/tools/*` module (`check` and `selftest`)…» | в ИСТОКЕ каталога `.kaif/` нет (поставка в `framework/`, сводка `tools/check-framework.mjs`); «every module × 2 вызова» противоречит собственному требованию бюджета — правила «только пересекающие зону» нет | дать вторую строку для истока/монорепо и правило отбора модулей по пересечению с зоной |
| S3 | `SKILL.md:36-39` «the token or time ceiling … The summary records what was actually spent» | счётчика токенов у агента нет (число первого прогона снято владельцем из интерфейса) — исполнитель либо выдумает число, либо промолчит | назвать допустимую форму записи расхода: «агенты · стенное время · зоны», токены — если счётчик есть |
| S4 | `SKILL.md:57` «One reviewer per zone» против §7 «a reviewer cut by claim clusters … converged on the same defect from four sides» | одна малая зона → арифметика даёт одного ревизора, а полевая улика и EXP-0076 хвалят кластеры; форма «двое на одну зону» текстом не предусмотрена | разрешить нарезку по кластерам заявлений внутри зоны и назвать её предпочтительной для малых зон (улика прогона: две карточки-дубля — ноль, а третью ось увидел только исполнитель) |
| S5 | `SKILL.md:84-86` «`kaif-fp: <surface> :: <symptom-class> :: v<major.minor>` … (`/report-bug`)» | в `/report-bug` отпечаток принадлежит тикетам `bugs/KAIF/` полевых развёртываний; в истоке баги ложатся в `bugs/` и строки `kaif-fp:` в шапке не несут (проверено грепом) | одна оговорка: отпечаток живёт в КАРТОЧКЕ аудита всегда; в баг-доке — по правилу `/report-bug` того слоя, где баг заводится |
| S6 | `references/audit-report-template.md:19-20` `<date>_<scope>_SUMMARY.md` против `reports/README.md:5` «по одному `NN_<имя>.md` на отчёт» | два правила имени для одного каталога; решено по прецеденту | оговорка в §1: «в `KAIF_AUDIT/` индекс — дата, а не номер» |
| S7 | `SKILL.md:50-51` «Their output is evidence» (о детерминированном слое) | `kaif-guard-lint check framework/hooks` → EXIT 3 «nothing was linted»: улика ОТСУТСТВИЯ деклараций или неприменимость инструмента? текст не говорит | одна строка: третий код («нечего проверять») — это улика об ОТСУТСТВИИ, и она сама становится осью, если канон обещает декларацию |

**Места, где текст держал** (тоже результат, назван вслух): брифы §7/§8 · карточка из восьми полей ·
правило «поля 6 и 7 друг против друга» · процедура effective-FP · пятый запрет (окна и звук).

## 5. Баг-документы прогона

- `bugs/118_hooks_guards_judge_a_copy_of_truth.md` — S2, 3 вхождения (F1 Act · F2 Attend · F3 Attend).
- `bugs/119_hooks_state_from_unchecked_input.md` — S2, 3 вхождения (F4 · F5 · F6, все Attend) + связанная
  дыра покрытия от опровергнутой находки.
- `bugs/120_hooks_order_asserts_the_unobserved.md` — S2, 2 вхождения (F7 Attend · F8 Track).
- `bugs/121_hooks_optin_doc_promises_otherwise.md` — S2, 3 вхождения (F9 Act · F10 Attend · F11 Track).

Фиксы — ОТДЕЛЬНОЙ задачей (контракт навыка). Кандидат на первую: `bugs/118` F1 (Act) и `bugs/121` F9
(Act) — обе дешёвые и обе про то, что владелец увидит руками.

## 6. Остаток эпика CR для главной сессии

1. **Чекбокс CR4 в `plans/63` снят**, хотя машинная половина зелёная (сборка/счётчики/полигон/пары
   проверены в этом прогоне). За ним остаются: `node tools/lang-packs-guard.mjs --update-pins` ПОСЛЕ сверки
   ru-алиасов навыка глазами (сейчас `lang-packs-guard` зелёный, 37 пинов) и абзац `KAIF_REFERENCE`.
   `TEMPLATE_NOTES['2.7']` для `code-revision` УЖЕ есть (`tools/build-framework.mjs:355-357`).
2. **`KAIF_REFERENCE` не знает о переписи:** `framework/KAIF_REFERENCE.md:154-161` (и генерат
   `KAIF_REFERENCE.md:155-161`) описывает навык как «(2.1)» и не называет брифы ревизора/скептика (§7/§8),
   новейший SUMMARY как базовую линию и процедуру effective-FP. Предлагаемая вставка — одно предложение
   после «…skeletons: the skill's `references/audit-report-template.md`»:
   «Rewritten by its executor in 2.7 (epic CR): the reference now carries the reviewer and skeptic briefs
   and the effective-FP procedure, and the newest summary is named as the run's baseline.»
3. **Решение по семи предложениям § 4** — правка навыка поставки меняет публичный контракт версии, поэтому
   её место — главная сессия (и, для S1/S4, возможно слово владельца: они меняют ПРОЦЕДУРУ, а не формулировку).
4. **Строка в `STATUS.md` п. 0** и `MASTER_PLAN §5` — по форме сессии; worktree их не трогал.
5. **Следующий прогон ревизии** — шесть пунктов «что должен сменить СЛЕДУЮЩИЙ прогон» в конце
   `reports/KAIF_AUDIT/2026-09-18_hooks_SUMMARY.md` (первым — проверка гипотезы о норме отсева).

## 7. Судья — `VERIFIED WITH CAVEATS`

`/fable-judge` субагентом чистым контекстом: запущен 2026-09-18 09:25 +03:00, вернулся 09:44 (100 вызовов
инструментов). Полный разбор — § 7а отчёта прогона `testcases/reports/2026-09-18_code-revision-rewrite.md`.

**Не опровергнуто ничего по существу:** судья переисполнил своей командой все 14 технических замеров
(базовая линия свода без `KAIF_DIST`, все шесть мутантов, `at` в будущем, `cwd` подкаталога, BOM, домен
`source`, страж на 72-часовой грязи, битый маркер + старый mtime, проба README в трёх оболочках,
effective-FP с теми же числами коммитов, размеры зоны, `TWINS:`) и получил ТЕ ЖЕ результаты; плюс снял
КОНТРОЛЬ, что его собственный шов краснеет. Состав агентов, письменность брифов по §7/§8 и вердикты
скептиков он проверил по журналам субагентов, а не по словам отчёта: «скептик опроверг» не превратилось в
«скептик согласился».

**Починено по десяти пунктам судьи (все внесены до коммита):** (1) «ни одного внешнего вызова» → 31
`WebFetch` + 9 `WebSearch` на `code.claude.com` внутри двух разведчиков доков, и контракт Claude Code тем
самым переснят живьём (§ 2 отчёта и «Пределы» SUMMARY); (2) момент судьи в таблице был ИЗ БУДУЩЕГО →
09:25 по `meta.json`, источник назван; (3) две ссылки в пустоту → исход линта в клетке, § 7а создан;
(4) «шаги 0–5 буквально» → «0–4 буквально, шаг 5 частично» (урок `EXPERIENCE.md` и крафт-рецепты — за
главной сессией), кейс шага 5 `pass` → `partial`; (5) «сверка ВСЕХ цитат» → «поля 1, 42 пары», плюс
названо, что адреса полей 2–8 не сверялись; (6) состояние дерева → «чистая в 08:31, с 08:41 — дописанный
замер», замечание скептика S6 принято; (7) адрес `framework/tools/kaif-canon-lint.mjs:66` в FAMILY-документе
4; (8) строки отзыва опыта (11 уроков строкой) и четыре `FORK:` в § 2 отчёта; (9) `doc-header-lint` в
строке гигиены (6 находок, ни одна — не этого прогона); (10) формулировка CR6 в `plans/63`.

**Для интегратора важное:** пункт (4) — это ПРЯМОЕ следствие контракта worktree, а не небрежность: шаг 5
навыка требует урок `EXPERIENCE.md` с `Repro:` и точкой срабатывания первым пунктом, а `EXPERIENCE.md`
worktree править не имел права. Пока урок § 3 не вставлен главной сессией, **шаг 5 прогона остаётся
частично исполненным** — и это единственная часть CR5, которую закрывает не worktree, а интегратор.

## 8. Решения, принятые агентом без владельца `[ИИ]`

1. **Ветка доведена `git merge --ff-only main`** с `803ef45` до `df8110b`: иначе зона читалась бы без
   флипа маркера `[TESTED]` хука RS, и прогон судил бы вчерашний файл. Цена ошибки — ноль (fast-forward,
   своя ветка, чужие деревья не тронуты).
2. **Базовой линией взят `2026-08-08_guards-machinery-suites_SUMMARY.md`**, а не новейший по имени
   `2026-08-14_r2_triage_SUMMARY.md` — и это названо остановкой S1, а не молча исправлено.
3. **Два ревизора на одну зону** (нарезка по кластерам заявлений) вместо одного по букве шага 1.
4. **Шесть скептиков по семействам** (форма, разрешённая §8) вместо двенадцати по находке — независимость
   от РЕВИЗОРА сохранена (чистый контекст, только карточка).
5. **Одна карточка поднята самим исполнителем** (ось `cwd` ниже корня) — её судил тот же независимый
   скептик, что и остальные.
6. **Четыре класс-документа вместо одиннадцати по находке** — по правилу «закрывай класс, а не экземпляр».
7. **Замер effective-FP дописан в отчёт прошлого прогона** (жанр append-only это разрешает, и сам отчёт
   этого требовал п. 4 своего «что сменить»).
8. **Полигон целиком не запускался** — правило тишины `bugs/116`; вместо него поимённо свод зоны.
9. **Тяжесть всех четырёх баг-доков — S2**, не S1: теряются прогон и час, железо/данные/доверие владельца
   не затронуты.
