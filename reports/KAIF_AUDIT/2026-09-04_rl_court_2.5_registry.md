# Реестр суда RL — заявления версии KAIF 2.5 (Experienced KAIF)

> **Создан:** 2026-09-04 12:12 +03:00 (эпик RL, `plans/87` шаг RL1 — СКЕЛЕТ реестра, суд не
> начат; форма — прецедент №74: `reports/KAIF_AUDIT/2026-08-28_rl_court_registry.md`).
> **Родитель:** `plans/82_EPIC_kaif_2.5.md` → эпик RL «Суд и релиз»; операционный план — `plans/87`.
> **Статус:** ✅ СУД ПРОШЁЛ 2026-09-04 13:56 +03:00 (сессия 51) — вердикт **VERIFIED WITH
> CAVEATS** (секция «Вердикты» ниже): 32 строки = 26 CONFIRMED · 6 WEAKENED · 0 REFUTED; сырых
> находок 14 → панель скептиков (2 линзы, выживание 2/2) пропустила 5 (B-H1 · D-H2 · D-H4 · E-H1 ·
> E-H2) — все починены и пересужены тем же часом; кластер A переисполнен на починенном дереве
> целиком зелёным. Каверзы поимённо: E-H3 · C-H3 — строки `ideas/28` п. 7; флейк s07 — `ideas/28`
> п. 11 (в этом суде не повторился: полигон 18/18 с первого прогона); публичность имени KAGO —
> слово владельца (STATUS п. 3).
> **Вовне:** —.
>
> **Предмет:** критерии приёмки версии `plans/82` («Готово, когда» 1–4, 6; 5 — сама работа RL,
> судится после витрины; 7–8 — витрина и публикация) + заявления закрытия эпиков CN (`plans/83`),
> SG (`plans/84`), TM (`plans/85`), US (`plans/86`) + «Решения без владельца» четырёх эпиков (33
> пункта, сведены в `plans/87` RL0) — судятся как ЗАЯВЛЕНИЯ О СЕБЕ.
>
> **Оркестрация — БЕЗ workflows (решение №88, интервью №021 Q2 = A, 2026-09-04 13:37 +03:00;
> суд начат по прецеденту №74 в 13:2x и подтверждён ответом):** 4 судьи-субагента с чистыми
> контекстами по кластерам B–E (параллельно) + панель скептиков из двух линз по находкам (вердикт
> «не дефект» по умолчанию, EXP-0025/0052); кластер A — оркестратор ПОСЛЕ починок; кластер F —
> греп-сверка оркестратора.

Правила суда (по W1/№74): каждое заявление ПЕРЕИСПОЛНЯЕТСЯ (прогон · счёт · дифф · греп), не
перечитывается; вердикты — CONFIRMED / WEAKENED (заявление живо, но проверка слабее заявленного
или дыра рядом) / REFUTED (+улика); каждая находка несёт дословную цитату `файл:строка`; мутации
для доказательства — только на КОПИЯХ в OS-temp; числа — из вывода инструментов; логи прогонов —
в файл. Судьи репозиторий НЕ правят и не коммитят; `build-framework` и полный `test:core` гоняет
только оркестратор (кластер A).

## Кластер A — полное дерево (оркестратор)

| ID | Заявление (источник) | Переисполнение | Вердикт (сессия 50, 12:0x–12:1x +03:00, HEAD `872df93`) |
|---|---|---|---|
| A1 | Пересборка чистая: генераты = f(`framework/`); 14 документов · 7 README · 37 навыков · 59 embedded · 170 блоков · 764 модуля (STATUS) | `node tools/build-framework.mjs` → `git status --short` пуст | ✅ CONFIRMED — check-framework OK, 59 embedded (14+7+37+1), 170 blocks, 764 modules; дерево чистое после коммита build 435 |
| A2 | Полигон «all 18 suites green» на HEAD (s18 — 18-й свод, U1–U14) | `npm run test:core` → лог в файл | ✅ CONFIRMED — прогон 11 сессии 50: `sandbox suite: all 18 suites green`, exit 0 (лог `polygon-11.log` скретчпада); ⚠ прогоны 2 и 9 того же дня — красный s07 (T1, T2) флейком `update` exit≠0 после строки language-pack, причина не названа (`STATUS` п. 1.2) — WEAKENED-кандидат для панели |
| A3 | counters-guard: 50 зеркал, ось навыков поимённо | `node tools/counters-guard.mjs` | ✅ CONFIRMED — «counters OK — 50 зеркал сверены с живыми числами» |
| A4 | lang-packs-guard: 8 frozen целы, живые ru/en | `node tools/lang-packs-guard.mjs` | ✅ CONFIRMED — «lang-packs-guard OK — заморожено 8 пакетов (состояние 2.2, побайтно)» |
| A5 | Линтеры: doc-header 0 находок · requirements-lint 0 · стилометрия совпадает с ядром | `node tools/doc-header-lint.mjs` · `node tools/stylometry-snapshot.mjs --version-check` | ✅ CONFIRMED — «scanned 157, findings 0», «requirements lint OK — 230 file(s), 0 findings», «слепок совпадает с ядром (5265de5)» |
| A6 | Контур: эталон интервью | `node tools/verify-contour.mjs --etalon-only` | ✅ CONFIRMED — «44 зелёных, 0 красных» |
| A7 | Реестр пар: fable-пары побайтно · составы референсов навыков · `kaif-update` обвязка = поставка | однострочники реестра (`AGENT_GUIDE.md` → «Реестр пар») | ✅ CONFIRMED — pair ok ×4 (fable-*), refs ok ×3 (code-revision, fable-method, team-deployment), `cmp` kaif-update равны |

## Кластер B — эпик CN «Канон точек решения» (судья 1; `plans/83`, критерий 1 `plans/82`)

| ID | Заявление | Переисполнение |
|---|---|---|
| B1 | Пять носителей в ОБОИХ слоях: исключение AUTH в гейте `AGENT_GUIDE` (#37) · четвёртая дверь в `PHILOSOPHY` + `FORK:` в `AGENT_GUIDE` (#36) · вторая половина гейта 5 в `TESTING_FRAMEWORK` + `@guard`/`@forensic` (#35) · `BOUNDARY:` + `until` в `/guarded-loop` (#30) · «голос по языку» в `/owner-reviews` (#38) | грепы по `framework/*` И корню/`.claude/skills/*` (строки названы в `plans/83`) |
| B2 | `kaif-guard-lint` — линтер объявлений стражей: красный на `@guard` без GAP и на `@forensic` без DURABLE-AT, SKIPPED=3 без маркеров (свод s15) | `node tools/sandbox/s15-guard-lint.mjs` + мутация на копии |
| B3 | Бюджеты девятки в `check` (s16): предупреждение над бюджетом, exit 0 | `node tools/sandbox/s16-doc-budgets.mjs` |
| B4 | Каденция молитвы — фразой внутри директивы блока `KAIF:PRAYER` обоих слоёв, 16 пунктов и граница не тронуты (решение 1) | греп + счёт пунктов в обоих слоях |
| B5 | `DELIVERY:` — первая строка отчёта `/end-chat-soft`, `/end-chat-force` и четырёх циклов обоих слоёв; `MASTER_PLAN` §8 называет одну метрику | греп 6 навыков × 2 слоя |
| B6 | Лестница тяжести S1/S2/S3 в `BUG_FIXING_FRAMEWORK` обоих слоёв; `/report-bug` ветвится по ней | греп |

## Кластер C — эпик SG «Канал сигналов машинерией» (судья 2; `plans/84`, критерий 2)

| ID | Заявление | Переисполнение |
|---|---|---|
| C1 | `node .kaif/kaif-core.mjs report <тикет>` доставляет через `gh` под стоячей авторизацией (#15), пишет URL в строку `Delivered upstream`; `--dry-run` ничего не зовёт | свод s17 на фикстуре `KAIF_GH` (подставной `gh`) |
| C2 | Отказы названы: anonymous · нет `gh` · не тикет · `gh` отказал; таймаут = exit 3 «исход неизвестен», не отказ | s17 + ручной прогон на копии с `KAIF_GH_TIMEOUT_MS` |
| C3 | `/report-bug` шаг 4 EN говорит «запусти»; RU-копия обвязки — в форме 2.4 (решение 3, отказ классификатора) — честно названо в STATUS п. 3 | греп обоих слоёв: EN = «run», RU ≠ EN (задекларированный дрейф, не дефект) |
| C4 | Идемпотентность на доставленном тикете (второй вызов не создаёт второй issue) | s17 |

## Кластер D — эпик TM «Команда в поле + CI» (судья 3; `plans/85`, критерий 3)

| ID | Заявление | Переисполнение |
|---|---|---|
| D1 | `/team-deployment` несёт: adopt (референс `team-adopt.md`, шаг 0 операции 2) · исключение менеджера §1 · замки́ в стоп-ритуале §9 · триггер «дремота» · «at first launch» · три практики NDim · архетип `hardware-lab-small` · четыре состояния борда + пункт 7 `audit-waiting` · «где живёт борд» (вне git · снимок · опт-аут) · `team-ci-template.md` с констрейнтами #29 | греп каждого пункта по `framework/skills/team-deployment/**` + сборка |
| D2 | RU-копия обвязки `.claude/skills/team-deployment/` синхронизирована с поставкой (состав референсов = поставка; 180 строк при потолке 179 — решение 8) | строка реестра пар (составы) + `wc -l` |
| D3 | Policy-строки 2.5 в сборщике: борд вне git · CI едет с командой | греп `tools/build-framework.mjs` → `POLICY_CHANGES_BY_VERSION['2.5']` |
| D4 | Приватные имена проектов владельца в PAYLOAD навыка отсутствуют | `private-names-guard` + греп имён соседних проектов по `framework/skills/team-deployment/` |

## Кластер E — эпик US «Симметрии обновления» (судья 4; `plans/86`, критерий 4)

| ID | Заявление | Переисполнение |
|---|---|---|
| E1 | Вердикт translated-wholesale печатается с числами по каждому кандидату; детерминирован на байт-идентичном дереве; H1 вне счёта (`bugs/100`); репетиция ОБЯЗЫВАЕТ (`diff --source` → `.kaif/update-rehearsal.json`, `update --rehearsal`, заморозка при `frozen`→`merged`, пункт `verdict-mismatch`, квитанция `verdicts`) | s18 U2/U3/U3б/U14 на копии; мутация `counted()` → U14 красный |
| E2 | Якорный блок неделим: пара, сломанная планом, откатывается и уходит одним пунктом `(anchored block KAIF:NAME)`; вставка не попадает внутрь открытой пары; `check` краснит непарный маркер (fence-aware) | s18 U1/U1б/U9; мутация: убрать откат → U1 красный |
| E3 | `stale-claims` безусловен (`no lines found`), сканирует скрипты (`package.json` + белый список, lock вне) | s18 U4/U5 |
| E4 | Wholesale-файл в задании называет апстрим и готовый `git diff v<from> v<to> -- <src>` (карта `sources` в мете) | s18 U8 + греп мета-блока бандла |
| E5 | `language-arrivals` в задании обоих маршрутов; `check` печатает `language mix` (советник) | s18 U6 |
| E6 | Точечные: EOL по доминированию (U7) · преемник и счёт deprecation (U10) · `project-name` до акта, искажённое имя отвергается (U11) · `placeholders` = поверхности гейта (U12) · `mode-switch` при anonymous→origin (U13) · `< /dev/null` в README hooks | s18 U7/U10–U13 + греп README |
| E7 | Каждый страж s18 доказан красным на ядре до фикса (числа в шапке свода) | пересмотр шапки s18 + выборочная мутация 2 стражей на копии |
| E8 | Ноты/policy 2.5 эпика US в сборщике (INERT): «UPDATE SYMMETRIES» · «FOUR behaviours» | греп `tools/build-framework.mjs` + `dist/KAIF-CORE-BUNDLE.md` |

## Кластер F — скоуп (критерий 6; греп-сверка оркестратора)

| ID | Заявление | Переисполнение |
|---|---|---|
| F1 | `ideas/28` существует; каждая строка `researches/24` §3 с вердиктом «2.6» имеет там адрес (пп. 1–9); пп. 10–11 добавлены на закрытии US (п. 10 закрыт в 2.5) | греп-сверка списков (сделана в RL0 — повторить судьёй) |

## Вердикты

Суд исполнен 2026-09-04 13:2x–13:56 +03:00 (сессия 51): 4 судьи-субагента с чистыми контекстами
(кластеры B–E, параллельно, ~7–9 мин каждый) → панель скептиков из 2 линз (материальность ·
фактическая корректность; вердикт по умолчанию «не дефект», выживание 2/2) → починки строго по
списку выживших (+ дешёвые починки 1/2 мандатом RL) → точечный пересуд починенного → кластер A
(оркестратор, механические гейты на починенном дереве). Полные отчёты судей и скептиков —
транскрипты сессии; логи прогонов — скретчпад сессии (`judge-B/…E/`, `s18-red-E-H1.log`,
`s18-green-E-H1.log`, `polygon-A2.log`, `clusterA-gates.log`, `verify-contour-full.log`).

### Строки реестра: 32 = 26 CONFIRMED · 6 WEAKENED · 0 REFUTED

| Кластер | CONFIRMED | WEAKENED | REFUTED | Находок сырых |
|---|---|---|---|---|
| A — полное дерево (оркестратор, после починок) | 7/7 | 0 | 0 | 0 |
| B — CN | 4 | 2 (B3 — таблица бюджетов не печатается; B6 — «ветвится по ней» навык не нёс) | 0 | 3 |
| C — SG | 4 | 0 | 0 | 3 |
| D — TM | 4 | 0 | 0 | 5 |
| E — US | 5 | 3 (E3 — фильтр lock по имени; E5 — bootstrap-маршрут не наблюдён; E7 — U9/U14 без красного в шапке) | 0 | 3 |
| F — скоуп | 1 | 0 | 0 | 0 |

Кластер A на починенном дереве (HEAD после починок, build 441 → сборка сессии 51): пересборка
детерминирована (три сборки подряд — `check-framework OK`, 59 embedded · 170 blocks · 764
modules, дифф — только намеренные файлы) · полигон `sandbox suite: all 18 suites green` с
первого прогона (лог `polygon-A2.log`) · counters 50 зеркал · lang-packs 8 frozen + 37 пинов ·
doc-header 0 (после поправки двух меток) · requirements-lint 0 · стилометрия совпадает с ядром
(`5265de5`) · контур: эталон 46/0 и ПОЛНЫЙ прогон в браузере 166/0 (`review.mjs` тронут — I39) ·
реестр пар: fable-пары побайтно · составы референсов · молитвы · версия — GREEN ·
`private-names-guard` зелёный ПОСЛЕ починки D-H4 (до неё, с пополненным списком, — красный на
`framework/tools/kaif-provenance.mjs` ×2 и бандле ×2) · experience-lint 0 новых · showcase чист ·
questions-guard 0 новых.

Судьи по кластерам (вердикты по строкам — в таблицах выше пометкой не правились; итог здесь):
B1 CONFIRMED (по критерию плана — WEAKENED, см. B-H2) · B2 CONFIRMED (M1/M2 адресны: ровно
названные проверки) · B3 WEAKENED · B4 CONFIRMED (16/16 пунктов, маркеры 1/1, каденция в обоих) ·
B5 CONFIRMED (12/12 файлов по 1 `DELIVERY:`) · B6 WEAKENED · C1–C4 CONFIRMED (s17 25/25; M0 на
ядре `8f19275` — 17 красных; M1 → ровно 1; M2 → ровно 1; таймаут `issue create` exit 3, тикет не
тронут; CRLF-тикет 7/7 до и после) · D1–D4 CONFIRMED (6 блоков бандла побайтно = источник; YAML
шаблона CI парсится; метры EN=RU 5/5 · 3/3 · 9/9 · 4/4 · 7/7 · 5/5 · 13/13 · 8/8; однострочник
реестра пар доказан красным удалением референса на копии) · E1 CONFIRMED (M1 «H1 в счёт» → ровно
U14) · E2 CONFIRMED (M2 «без отката» → ровно U1; M3 «check не краснит» → ровно U9) · E3 WEAKENED ·
E4 CONFIRMED · E5 WEAKENED · E6 CONFIRMED · E7 WEAKENED · E8 CONFIRMED · F1 CONFIRMED (§5
разведки ↔ `ideas/28` пп. 1–9 + секция ожидания, все с адресами).

### Панель скептиков: 14 находок → 5 выживших (2/2), все починены

| Находка | Панель | Судьба |
|---|---|---|
| **B-H1** — лестница тяжести в каноне, но `/report-bug` (точка заведения) без `Severity:` и без правила S3 | SURVIVE 2/2 (S3) | ✅ строка `Severity:` в шаблоне шага 3 обоих слоёв + «When to call»: S3 → строка в EXPERIENCE, документа нет; формулировка B6 реестра поправлена поправкой `plans/83` |
| **D-H2** — критерий 5 `plans/85` «старше порога», пункт 7 `audit-waiting` судит адресата, порога нет | SURVIVE 2/2 (S3) | ✅ append-only поправка `plans/85`: порог заменён семантикой адресата (решение без владельца), `--older-than` — кандидат 2.6 |
| **D-H4** — страж приватных имён слеп к KAGO и сокращению KCam; скептики нашли 8 KAGO-комментариев в ядре и 6+ KCam (судья грепал только `dist/*.md`) | SURVIVE 2/2 (S2) | ✅ псевдонимизировано (KAGO → project F, KCam → project D) в `framework/installer/KAIF-CORE.mjs` и `framework/tools/kaif-provenance.mjs`; список стража пополнен (ignore-файл); страж покраснел на пополненном списке ДО починки и зелен после; публичность KAGO — слово владельца |
| **E-H1** — `stale-claims` пропускает ЛЮБОЕ имя со словом «lock» (регрессия против 2.4: `BLOCKERS.md`, `lockstep.mjs`) | SURVIVE 2/2 (S2) | ✅ фильтр → только `*lock.ya?ml` среди скриптов; страж s18 U5б доказан красным на `4958dd1` (ровно 2 красных), зелёным после (57 ✅); поправка `plans/86` |
| **E-H2** — шапка s18 не называла красного U9/U14 | SURVIVE 2/2 (S3) | ✅ две строки в шапку s18 (улики судьи 4 и `bugs/100`) |
| B-H2 — stale-строка CN1 `plans/83` о RU-копии `/report-bug` | 1/2 (мат. KILL: изъятие задекларировано; корр. SURVIVE) | ✅ append-only поправка `plans/83` |
| B-H3 — «`check` печатает таблицу бюджетов» | 1/2 (мат. KILL: число печатается в момент действия; корр. SURVIVE) | ✅ формулировка обоих `AGENT_GUIDE` по факту машинерии |
| C-H1 — «18 проверок» s17 = 17 + итог | 1/2 (мат. KILL: определение счётчика; корр. SURVIVE, 10 из 17 — «unknown command») | ✅ шапка s17 + поправка `plans/84` |
| C-H2 — `report --dry-run <тикет>` → usage | 1/2 (мат. KILL: отказ назван; корр. SURVIVE, фикс безопасен) | ✅ путь — первый не-флаговый аргумент (мандат RL) |
| C-H3 — таймаут `auth status` exit 2, не 3 | 1/2 (мат. KILL: внешнего действия нет; корр. SURVIVE: формула Reference шире кода) | принято; строка `ideas/28` п. 7 |
| D-H1 — §4 конституции без слота опт-аута борда | 1/2 (мат. KILL: делегировано документу борда; корр. SURVIVE) | ✅ строка-слот в §4 обоих слоёв (мандат RL) |
| D-H3 — RU `SKILL.md` 180 из-за обрывка | 1/2 (мат. KILL: задекларировано решением 8; корр. SURVIVE) | ✅ слито в 179 |
| D-H5 — «adopt: навык 4» | 1/2 (мат. KILL: `grep -ci` = 4; корр. SURVIVE: способ счёта не назван) | ✅ поправка `plans/85` |
| E-H3 — `language-arrivals` на bootstrap не в своде | 1/2 (мат. KILL: заявление уровня кода, TWINS названы; корр. SURVIVE) | принято; строка `ideas/28` п. 7 (U6в) |

### Кластер G — эпик SF «Сценарная форма критериев» (исполнен ПОСЛЕ суда по слову владельца №91, 14:47; лёгкий судья с чистым контекстом, 15:1x)

| ID | Заявление | Вердикт |
|---|---|---|
| G1 | Подраздел «The scenario form / Сценарная форма» в обоих слоях `REQUIREMENTS_FRAMEWORK`: четыре строки в fenced-блоке · мост Given/When/Then + четвёртая · EARS → сценарий · семь правил · две границы · строка чек-листа · граница «Not Gherkin-everywhere» · стыковка; EN без кириллицы (`check-framework` OK, кириллицы в EN-теле 0 по node-счёту) | ✅ CONFIRMED (формулировки — H2/H4, починены) |
| G2 | `kaif-scenario-lint.mjs`: селфтест 33 кейса, правило N ← ровно мутация N (две мутации на копиях — по одному красному каждая); `check` на истоке — SKIPPED exit 3; донор в `framework/` не назван (`Unlim` = 0) | ✅ CONFIRMED |
| G3 | Свод s19 зелёный, в ростере полигона (19) и в шапке | ⚠ WEAKENED — проверок 15, не 17 (H1: число в `[TESTED]` и в `plans/88` починено) |
| G4 | Четыре строки в шаблонах четырёх навыков ×2 слоя, шаг 3a/3а в `/interview` ×2; линтер на деревьях навыков — SKIPPED (fence-aware) | ✅ CONFIRMED (H3: буквально четыре строки — в `/plan-task`, в трёх остальных названы; формулировки ноты/`plans/82`/нот починены) |
| G5 | Зеркала: строка `KAIF_REFERENCE` (cmp с генератом IDENTICAL) · карты · нота 2.5 в сборщике и бандле · `FILE:` блок модуля в бандле (diff с истоком 0) · релиз-ноты п. 6 обеих половин · README Таблица 5 · counters 171/766/19; стражи не ослаблены | ✅ CONFIRMED |

Находки судьи G: H1 (15 ≠ 17) · H2 (линтер стережёт правила 1–6 + порядок, правило 7 — судья; канон говорил «семь») · H3 («четыре строки в четырёх шаблонах» — буквально в одном) · H4 («семь правил с ❌/✅» — пары у трёх) · H5 (`and then` избыточно при `then` — словарь подрезан) — все пять починены тем же получасом; дефектов поведения нет.

**Попутно (не строка реестра):** `bugs/109` (S2) — третий за день флейк `update` в полигоне (s10 S2, после s07 ×2 в сессии 50): обрыв после строки версии, повтор в оставленном корне зелёный; причина не названа, потому что 18 сводов выбрасывали вывод упавшей команды — класс закрыт формой (`coreRunner`/`failed` в `tools/lib/sandbox-run.mjs`, селфтест, 17 сводов переведены скриптом; следующее вхождение оставит `run-fail-N.log`). Гипотезы ранжированы в документе; фикс причины — по первому логу.

**Попутно (не строка реестра):** `bugs/108` — пачка контура, поднятая для №021, показала владельцу
протухшую домашку `homeworks/06`; слово владельца на странице → страж I39 (`review.mjs`), s12
ростер 39, домашка снята, очередь вычищена. Уроки: EXP-0106.

**Чего не смотрели (оси второго круга, если он будет):** живой `gh` и разрешение `gh.exe` на
Windows PATH · псевдонимизация обвязки `tools/` (там KAGO/KCam остаются — не поставка) ·
bootstrap-маршрут наблюдением (language-arrivals, stale-claims) · калибровка `FORK:` по существу
(паралич/пустой ритуал) · `guarded-watchdog --arm` пробой · живая приёмка adopt/CI на реальной
команде · флейк s07 (в этом суде не воспроизвёлся).

## Links

`plans/82` · `plans/83`–`86` · `plans/87` · `reports/KAIF_AUDIT/2026-08-28_rl_court_registry.md`
(форма №74) · `tools/sandbox/s15`–`s18` · `bugs/100`.
