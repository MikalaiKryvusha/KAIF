# Ревью навыка `/code-revision` его исполнителем — таблица находок (2026-09-18)

> **Создан:** 2026-09-18 ≈ 01:03 +03:00 (сессия 65, Claude Fable 5.1 — адресат поручения владельца от 2026-08-08 и слова №113).
> **Родитель:** `plans/63` (шаг CR2). **Статус:** запись; правки применены шагом CR3 в обоих слоях.
> **Вовне:** —. Отчёт — ЗАПИСЬ (`reports/README.md`): тегом `DONE` не помечается, не переписывается, поправки дописываются.

**Вердикт (до улик).** Навык написан наблюдателем аудитов (Opus, эпик I 2.1; переработан T3 2.2) и как ОПИСАНИЕ метода верен:
оси, «нет цитаты — нет находки», скептик с дефолтом «не дефект», контракт карточки — всё это подтверждено двумя полевыми
аудитами и первым прогоном истока (5 из 9 находок выжили, 4 из 9 опровергнуты — 44 %, как в поле). Как ИНСТРУКЦИЯ
ИСПОЛНИТЕЛЮ он неполон в тринадцати местах: три из них исполнитель класса Fable закрывает своей головой каждый прогон
(брифы ревизора и скептика, поиск базовой линии), а модель послабее — не закроет и получит стилевые придирки вместо
находок. Правка без названного провала исполнения отклонялась (вердикт по умолчанию — «оставить»); оставлено шесть мест.

## Рамка чтения

Читал как БУДУЩИЙ ИСПОЛНИТЕЛЬ: «смогу ли я это исполнить и получу ли заявленный результат?», а не «хорошо ли написано».
Входы: `framework/skills/code-revision/SKILL.md` (100 строк, последняя правка `265ba75` 2026-09-06), `references/audit-report-template.md`
(144), `.claude/skills/code-revision/SKILL.md` (перевод, 97), `plans/33_DONE` (T3: что тест исполнимости вернул в шаблон),
`reports/KAIF_AUDIT/2026-08-08_guards-machinery-suites_SUMMARY.md` (первый прогон: 13 агентов, ≈ 1,18 М токенов, 9 → 5), урожай
`bugs/55`–`59`, пункт пула STATUS «следующий прогон», EXP-0076 (рой ревизоров по кластерам заявлений), EXP-0069 (калибровка нового
стража на живом материале), `/fable-judge` (роль скептика), `bugs/116` (тишина на машине владельца).

## Находки — переписать (каждая с цитатой и названным провалом исполнения)

| # | Место (цитата дословно) | Провал исполнения | Правка |
|---|---|---|---|
| F1 | Step 0: «Scope: the zones touched since the last revision (git log since the last revision's record)»; Step 5: «Record the revision (date, scope, found/refuted/fixed counts)» | Запись прошлой ревизии не имеет АДРЕСА: на втором прогоне исполнитель ищет «the last revision's record» и не находит ничего названного; на истоке ею де-факто служит `*_SUMMARY.md` первого прогона (его «Что должен сменить СЛЕДУЮЩИЙ прогон») — навык об этом молчит | Step 0 п. 1: новейший `reports/KAIF_AUDIT/*_SUMMARY.md` И ЕСТЬ запись и базовая линия (карта покрытия · пределы · «что сменить»); скоуп = `git log <sha базовой линии>..HEAD`; Step 5 закрывается «записью, которую читает следующий прогон» |
| F2 | Step 1: «one reviewer per zone; parallel where the harness allows» | Брифа ревизора нет: исполнитель собирает инструкцию ревизору из всего навыка, а навык сам предупреждает — «a reviewer handed bloated instructions silently drops part of them»; в первом прогоне брифы писались руками (13 агентов); EXP-0076: ревизоров режут по кластерам заявлений и запрещают перечитывать — этого знания в навыке нет | §7 справочника — скелет брифа ревизора (зона · читать целиком · оси · оплаченные классы с Repro-грепами · вывод детерминированного слоя · исключения · бюджет · форма вывода · строка покрытия); Step 1 велит вручать бриф, не навык |
| F3 | Step 3: «Every finding goes to a SEPARATE skeptic» | Инвариант — НЕЗАВИСИМОСТЬ от ревизора, а текст читается как «один агент на находку» (первый прогон: 9 скептиков на 9 находок); исполнитель без сабагентов шаг пропустит целиком | Step 3: скептик независим по построению — на находку, или на семейство с вердиктом по каждой; без сабагентов — свежий проход, не видевший обоснования ревизора; §8 справочника — бриф скептика с тремя линзами (адрес · собственное воспроизведение · покрытие решением/стражем) |
| F4 | Вступление: «(`AGENT_GUIDE.md` → "Strictness modes", the model split)» | Сноска ведёт в раздел о РЕЖИМАХ ПИСЬМА (`framework/AGENT_GUIDE.md:1132` — «Name the mode a piece of writing runs under»); «model split» там нет — исполнитель идёт по ссылке и не находит правила | Сноска снята; разделение названо на месте: *[judgment]* — сильная модель, *[mechanical]* — скрипт или любая модель |
| F5 | Step 0: «Run the code first [mechanical]: linters, guards, pairs-registry commands, the greps that encode already-paid classes» | Где взять список — не сказано; полевой исполнитель не знает стражей проекта; «greps that encode paid classes» — не названо, что это `Repro:`-строки записей опыта | Step 0 п. 6: `node .kaif/kaif-core.mjs check` · модули `.kaif/tools/*` (`check`/`selftest`) · таблица инструментов `AGENT_GUIDE` · реестр пар · `Repro:`-строки записей `EXPERIENCE.md` по тегам зоны |
| F6 | Step 2: «reusing the feedback loop's deduplication fingerprint and its attestation line, never a second key minted here» | Ключ не назван по имени — исполнитель обязан знать, что это `kaif-fp:` и `Dedup attestation:` из `/report-bug` | Названы дословно: `kaif-fp: <surface> :: <symptom-class> :: v<major.minor>` и строка `Dedup attestation:` с командами |
| F7 | Step 4: «same-class findings → ONE class doc with a full inventory» | Инвентарь класса требует поиска близнецов; как — не сказано, а канон требует строку `TWINS:` (`BUG_FIXING_FRAMEWORK.md`) | Step 4: у каждой подтверждённой находки `TWINS: searched <pattern> — found <N>: …` — инвентарь и есть список близнецов |
| F8 | Справочник §5: «Count it on the NEXT run — findings from the previous report that produced no action — and record the number in the summary» | Что считать «действием», не сказано — исполнитель гадает; первый отчёт истока так и остался без замера (пункт пула STATUS «замерить effective-FP») | §5: действие = баг-док находки DONE или коммит называет семейство/баг; иначе — нет действия; число и перечень — в таблицу методологии нового SUMMARY; Step 5 — пункт |
| F9 | Вступление и Step 0 — бюджета и правила остановки нет | Первый прогон: 13 агентов, ≈ 1,18 М токенов на три зоны — открытый прогон исполнителя класса Fable; слабый исполнитель — наоборот, один агент «на всё» | Step 0 п. 3: строка бюджета (ревизоры · скептики · потолок токенов/времени) и правило остановки (зоны исчерпаны или бюджет шума); SUMMARY записывает фактический расход |
| F10 | Step 1: «Cut the scope into zones by language/layer/subsystem» | Охота объявлена шире багов — «the omission of something the canon promised», — а зонирование ДОКУМЕНТОВ канона не предусмотрено: обещание без механизма ищется со стороны документа | Step 0 п. 2 и ось Step 1: зона «канон-документ ↔ машинерия» (реестр пар — её детерминированный слой) |
| F11 | Вступление: «reading at strength is exactly what a periodic revision … buys» | «Чтение в силу» не операционализировано: слабый ревизор грепает; полевые прогоны читали файлы ЦЕЛИКОМ (265 + 407 + 314, 2 323 строки), и именно это находило незаявленное | Step 1: ревизор читает зону ЦЕЛИКОМ — грепы находят известное, чтение — незаявленное; карта покрытия пишет «целиком / частично / не читали» поимённо |
| F12 | Step 0: «Owner-triggered or on the project's recorded cadence» | Каденция нигде не записана; новая настройка запрещена правилом полной механики (№97) | Step 0: срок — новейший SUMMARY старше каденции из `AGENT_GUIDE` (по умолчанию четыре недели) или слово владельца; конфигурации не заводится |
| F13 | Step 4: «Each surviving finding is verified by REPRODUCTION before any fix» | Кем — не сказано; попытка опровержения скептиком принимается за воспроизведение | Step 4: воспроизводит ИСПОЛНИТЕЛЬ; команда воспроизведения — поле 6 карточки; отказ скептика — не воспроизведение |

## Оставлено — почему

| # | Место | Почему оставлено |
|---|---|---|
| K1 | «no quote, no finding» (Step 2) | доказано скриптовой сверкой 5/5 цитат первого прогона; правило убивает и настоящие находки с неверным адресом (`s10`, `:617` vs `:637`) — это осознанная цена, теперь названная в тексте |
| K2 | скептик с дефолтом «не дефект» (Step 3) | 4 из 9 (44 %) на истоке, 9 из 21 (43 %) в поле — отсев работает; оставлено, добавлена независимость по построению |
| K3 | восемь полей карточки и правило «поля 6 и 7 читаются друг против друга» (§3) | дыра найдена тестом исполнимости T3 и закрыта там же; не трогать |
| K4 | формула адресной мутации фикса (Step 4) | канон стражей `BUG_FIXING_FRAMEWORK.md`, цитируется, не переписывается |
| K5 | «класс, увиденный второй раз, уходит механизмом» (Step 5) | правило `EXPERIENCE.md`, оплачено (EXP-0126 → EXP-0132) |
| K6 | нумерация шагов 0–5 и повествовательное вступление | решение T3 («расширен, не пересоздан»); новое ложится в существующие шаги и в справочник, тело навыка держится у коридора 60–96 строк (`plans/33`: хвост от 111) |

**Добавлено вне таблицы:** пятый запрет — ревизор и скептик не поднимают окно и звук на машине владельца (`bugs/116`: свод из
ревизии позвал бы владельца; живая проверка называется исполнителю, который запускает её объявленно).

## Пределы

- Это ревью текста исполнителем, не прогон; исполнимость переписанного текста проверяется функциональным прогоном одной зоны
  (`plans/63` CR5) — его отчёт лежит рядом.
- Оставленные места K1–K6 не переисполнялись заново — приняты по уликам первого прогона и T3.
