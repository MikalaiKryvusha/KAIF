# Бриф лёгкого судьи эпика CH 2.8 — «Честные строки: отчёт в KAIF, язык и часы владельца»

> **Создан:** 2026-09-26 (сессия 74, ночной цикл; Claude Opus 5.5). **Родитель:** `plans/121` шаг CH5 · `plans/117` критерии 10–13.
> **Статус:** бриф для судьи чистым экземпляром. **Вовне:** —

## Что судить

Эпик CH закрыт четырьмя коммитами (плюс план и нулёвка):

| Шаг | Коммит | Критерий `plans/117` | Отчёт прогона |
|---|---|---|---|
| CH0 | `e90311e` | — (нулёвка) | `testcases/reports/2026-09-25_ch0-inputs.md` |
| CH1 | `19a06c5` | 10 — полевой отчёт уходит в KAIF сам | `testcases/reports/2026-09-25_ch1-field-report-delivered.md` |
| CH2 | `154066c` | 11 — обязательные строки владельцу на его языке | `testcases/reports/2026-09-26_ch2-owner-language-lines.md` |
| CH3 | `7d72041` | 12 — время и названный час — с часов | `testcases/reports/2026-09-26_ch3-time-from-the-clock.md` |
| CH4 | (последний коммит эпика — `git log --oneline -1` на момент суда) | 13 — снятие функции не оставляет стоять тексты | `testcases/reports/2026-09-26_ch4-withdrawal-leaves-no-standing-texts.md` |

Сценарии критериев 10–13 — в `plans/117` (строка «Проверка» каждого — первая команда суда). Операционный план — `plans/121`.

## Правила тишины (обязательны)

- Ни одного окна у владельца: не запускай контур без `--no-open`, не поднимай страниц `--visible`, не открывай макеты и кадры.
- Ни одного звука: не зови `review.mjs --call` без `--dry-run`; своды, поднимающие контур, — только те, что уже в полигоне (`s22` и
  `verify-contour` работают без головы и без звука — их можно).
- Ни одного действия наружу: `kaif-core report` — только `--dry-run` или через подменный `gh` сводов (`KAIF_GH`); никаких `gh issue …`.
- Файлы репозитория не правишь; развёртывания поля (`D:\work\...` вне `KAIF`) — только чтение.
- Полигон целиком (`npm run test:core`) не гоняй — его гоняет исполнитель в одиночку; отдельные своды и пробы — можно.

## Реестр заявлений (переисполнить, не перечитать)

1. `grep -c "stays LOCAL" framework/readmes/reports.md` → 0; README директории `reports/` говорит, что отчёт доставляется тем же движением.
2. `node tools/sandbox/s17-report.mjs` — зелёный; случаи CH1 (5) и CH4 (2) на месте; с `KAIF_DIST=scratchpad…v27` (путь спроси у
   исполнителя или пропусти) — не проверяй, если нет старой сборки.
3. `node tools/sandbox/probes/ch1-report-mutants.mjs` — «3 mutants red exactly on their named addressees».
4. Задание установки на `tracking: anonymous` не тянется к origin (`s04` S13-M3 зелёный), на `origin` — несёт строку доставки
   (`s17` «задание установки на tracking: origin»).
5. `grep -n "owner's language" framework/skills/end-chat-soft/SKILL.md framework/skills/end-chat-force/SKILL.md framework/AGENT_GUIDE.md` —
   строка правила в каждом; английского «verbatim» у строки о стоячей лжи в навыках поставки нет.
6. `grep -c "%H:%M %z" framework/AGENT_GUIDE.md` ≥ 1; `grep -n BOUNDARY` в `/end-chat-soft`, `/dayloop`, `/nightloop`, `/autoloop` поставки —
   по строке; охота судьи «Early finish» называет эти навыки.
7. `wc -l framework/AGENT_GUIDE.md` ≤ 1080; `node tools/canon-inventory.mjs --diff v2.7 --with framework/KAIF_REFERENCE.md --declared
   plans/118_epic117_CK_light_canon_budget_exits.md` → `lost 0`; две строки «дополнен» в реестре `plans/118` честны (правило в новой
   редакции действительно стоит).
8. `node framework/tools/contour/review.mjs --selftest` — зелёный; случаи `--mark-withdrawn` на месте; `--mark-withdrawn` на ОТВЕЧЕННОМ
   вопросе — отказ.
9. `node tools/sandbox/s04-anon-legacy.mjs` — зелёный; случай «S14d/CH4» на месте; шов `KAIF_DIST` в `s04` не меняет его поведение без
   переменной.
10. Мета-блок бандла (`dist/KAIF-CORE-BUNDLE.md`, JSON меты) несёт у снятия 2.7 поля `since` и `search`.
11. Отчёты прогонов CH0–CH4: каждая строка «Прогоны» с моментом и командой; вердикты `partial` там, где функциональный прогон не
    дошёл до поля; «РЕАЛЬНЫЙ МИР» — у каждого; числа в отчётах совпадают с выводом команд, которые ты переисполнил.

## Охоты KAIF-блока (`/fable-judge`)

- Заявление шире наблюдения (отчёт говорит «проверено на реальном» о том, что проверено на копии; «доставлено» о сухом прогоне).
- Ослабленный тест без блока «ТЕСТ ИЗМЕНЁН» в сообщении коммита (`git show <коммит> --stat` + сообщение).
- Стоячая ложь: число или утверждение в `STATUS.md`, `plans/117`, `plans/121`, `MASTER_PLAN.md` §8, которое расходится с
  переисполненным (например, «закрыто N из 29», число проверок сводов).
- Развилка без строки `FORK:` (CH1 · CH3 — строки в `plans/121`); решение агента в одежде слова владельца.
- Кириллица в теле поставки (`framework/` кроме `texts.mjs` и языковых пакетов) — новые вхождения.

## Что вернуть

Вердикт (VERIFIED / VERIFIED WITH CAVEATS / REFUTED) и реестр: по строке на заявление — CONFIRMED / WEAKENED / REFUTED с командой и её
выводом (первые строки). Находки — с адресом (файл:строка) и предлагаемой починкой. Не чини сам.
