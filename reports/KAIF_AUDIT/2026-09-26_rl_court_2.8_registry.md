# Суд версии KAIF 2.8 — реестр (RL1)

> **Создан:** 2026-09-26 06:01 +03:00 (сессия 74, ночной цикл; Claude Opus 5.5 — оркестратор). **Родитель:** `plans/125` шаг RL1 · брифы
> `reports/KAIF_AUDIT/2026-09-26_rl_court_2.8_briefs.md`. **Статус:** все пять кластеров вернулись. **Вовне:** —
>
> Пять судей-субагентов чистым экземпляром, каждый в своём клоне на `fef29d5`, запущены ≈ 2026-09-26 05:29 +03:00. Витрину 2.8 судят
> после RL3. Сводные числа — скриптом по строкам таблицы ниже (EXP-0107). Отчёты судей вернулись ответами субагентов; здесь — их вердикты,
> счёты и находки с судьбой каждой.

## Вердикты по кластерам

| Кластер | Эпики | Вердикт | Заявлений | CONFIRMED | WEAKENED | REFUTED | Блокеры выпуска |
|---|---|---|---|---|---|---|---|
| A | CK · VO | VERIFIED WITH CAVEATS | 23 | 21 | 2 | 0 | 0 |
| B | OW | VERIFIED WITH CAVEATS | 21 | 18 | 3 | 0 | 0 |
| C | CH · UP | VERIFIED WITH CAVEATS | 22 | 17 | 5 | 0 | 0 |
| D | SC · TB | REFUTED | 23 | 17 | 5 | 1 | 0 |
| E | поставка и сборка | VERIFIED WITH CAVEATS | 23 | 17 | 6 | 0 | 0 |

**Итого** (скриптом по строкам таблицы): 5 кластеров · 112 заявлений = 90 CONFIRMED · 21 WEAKENED · 1 REFUTED · блокеров выпуска 0.

## Находки и их судьба

Починено тем же утром (коммит починок RL1; каждая — на месте, с пометкой «исправлено … по суду RL1»):
- **D-F1** (единственное опровержение суда) — вердикт «не воспроизвелось» узнавался списком фраз: 14 форм из 18 проходили с одним вариантом,
  среди них «не удаётся воспроизвести», и на полевых копиях. Теперь — шаблоны «отрицание, до двух слов, основа» обоих языков; самопроверка
  несёт все 18 форм судьи (95 случаев), мутанты M6 · M8 строят адресатов из того же списка, `s25` +1 на развёрнутой копии. Ложное заявление
  «любая форма» исправлено в отчёте TB3 и в STATUS.
- **D-F3 / E-F5** — строка успеха самопроверки гарда 5m говорила обратное доказанному — переписана.
- **D-F4** — «Проверка» критерия 28 вела в `.kaif/`, канон развёрнутого проекта лежит в корне — поправлена с пометкой.
- **C-F3** — «Результат» критерия 14 был шире `update-verify` — переписан по `plans/122`.
- **C-F5** — «5 провалов» в отчёте CH5 при выводе «6 failure(s)» — поправлено.
- **C-F6** — неотмеченный чекбокс UP6 в `plans/122`; «закрыты N из 21» в `plans/117` при знаменателе 29 — поправлено.
- **E-F4** — кириллица вернулась в комментарии ядра поставки (пример части B SC4 и две буквы-ссылки эпика CK), а отчёт части A говорил,
  что её нет, — все три латиницей, отчёт поправлен.
- **E-F6** — записка §10.7: `stale-claims` выходит с 1 и тогда, когда интервал неизвестен — дописано.
- **E-F2** — вариант D интервью №040 («без имени») встретит гард сборки, требующий кодового имени, — сказано внутри варианта.
- **B-F2** — записка §14 описывала первую редакцию стража слова владельца в настоящем времени — переписана.
- **B-F5** — шапка `plans/119` говорила «ПРИПАРКОВАН» — поправлена.
- Попутно (кластер D) — `plans/117` «у пяти tool-модулей» → шести; `plans/123` «дальше — часть B» → часть B ✅.
- **A-F2** — шапка вендоринга `/fable-judge` (обоих слоёв, едет полю) называла одну охоту 2.8 из четырёх — пункт (8) дописан.
- **A-F4** — `tools/canon-inventory.mjs` писал «ON-REAL-PATH: NOT YET» и `[NOT-TESTED]`, хотя реальный путь пройден в CK3 и каждом срезе — поправлено.
- **A-F5** — `[TESTED]` пробы калибровки токенов без адреса отчёта — адрес дописан.

Починено вторым шагом того же утра (дешёвые пункты RL2): **A-F1** (проба паритета жанров импортирует модуль напрямую — «GOOD», 126 вердиктов,
0 расхождений; красный на мутантной копии после смены импорта не перепроверялся) · **C-F4** (флаг репетиции держит окончания строк, «unless a
`.gitattributes` `eol` pins them» — оба слоя `/kaif-update` и записка) · устаревшие `[TESTED]` (`kaif-testrun-lint` 95 случаев,
`templates-own-scan` пять ответов, `up-mutants` двадцать мутантов, блок KAIF-WALK — сторона чтения) — ниже они отмечены «✅ (починено вторым шагом)».

В RL2 (не блокеры; порядок — по цене для поля):
- ✅ (починено сессией 75) **D-F2** — метка строки записи о развёртывании знает только en и ru: в восьми лицах поставки датированная строка записи молчит.
  Теперь подписи всех десяти языковых шаблонов — данными в ядре (`RECORD_LABELS`), страж сборки 5n держит список равным пакетам; `s29` C14,
  мутант M19 (`testcases/reports/2026-09-26_rl2-record-labels-all-faces.md`).
- ✅ (починено сессией 75) **C-F1** — на маршруте `update` пункт полевого отчёта пишет ядро 2.7 без строки доставки; заметке CH нужна оговорка «когда»; `check`
  узнаёт версию только по каноническому имени отчёта. Теперь отметка `checkpoint field-report` свежего ядра печатает команду доставки
  (не отказывает), ось `check` судит отчёт и по его строке доставки, заметка версии несёт оговорку; `s03` S12-M3 · `s17` CH1.
- ✅ (починено сессией 75) **E-F1** — на маршруте `update` скан ядра 2.7 называет три строки шаблонов 2.8 с перенесённой скобкой (AGENT_GUIDE · owner-reviews ·
  report-bug) — свести скобки в одну строку в обоих слоях. Скобки закрыты на своих строках в поставке (слой истока ядром 2.7 не
  сканируется — `[ИИ]`); проба `old-core-own-scan` — RED 3 → GREEN. Отчёт обоих — `testcases/reports/2026-09-26_rl2-field-report-delivery-and-wrapped-attributions.md`.
- ✅ (починено сессией 75) **B-F1** — сторож `--wait` ждёт вечно, если контур кончился до его старта (канон обещает код 2). Теперь без
  живого контура за минуту — код 2 со строкой «No live contour…»; самопроверка контура +1, мутант контура на адресате.
- ✅ (починено сессией 75) **C-F2** — снятие 2.7 не ищется у развёртываний 2.7 (`since` против `from`). Теперь «не старше версии
  развёртывания»; `s04` S14d/CH4. Отчёт обоих — `testcases/reports/2026-09-26_rl2-waiter-window-and-withdrawal-since.md`.
- ✅ (починено сессией 75) **D-F5** — вариант охоты списком без исхода засчитывается. Теперь вариант — только строка настоящей таблицы с
  заполненным исходом; отказ линтера называет форму таблицы (шаблон поставки её и задаёт), записка истока `/report-bug` говорит, что
  пункт списка вариантом не считается.
- ✅ (починено вторым шагом) **C-F4** — фраза рецепта репетиции «флаг держит окончания строк» ложна при `.gitattributes eol=crlf`.
- ✅ (починено сессией 75) **E-F3** — записка §4 «five artifacts» при шести ассетах релиза; роль карты модулей в манифесте (оговорка (б) `ideas/30` п. 24 —
  пины двух файлов: замысел, загрузчик проверяет ровно то, что исполняет, — E18). Теперь «six artifacts» и строка таблицы карты модулей;
  у карты роль в манифесте («informative … neither fetched nor pinned»); фраза о двух пинах сверена с `ARTIFACTS` загрузчика.
- ✅ (закрыто наблюдением сессией 75) **B-F3** — заметка 2.8 о страже: «never stops the work» — живой прогон ещё не наблюдался. Наблюдён на
  пути владельца 2026-09-26 07:53–09:25 +03:00: восемь сообщений посреди хода, отказ — после шести (одно ещё не легло в стенограмму,
  одно перекрыто более новым; исправлено 2026-09-26 11:08 +03:00 по судье J-F3: стояло «шесть сообщений»), семь отказов в шести раундах, следующий вызов прошёл
  через 3,9–8,1 с (один раз — через 27,7 с: между ними был отказ для следующего сообщения владельца), второго отказа нет; строка
  ON-REAL-PATH стража дописана (исправлено 2026-09-26 10:22 +03:00: стояло «через 2–8 с» — число выведено на глаз, пересчитано скриптом).
- ✅ (починено сессией 75) **B-F4** — мигающий красный `s22 D` (LP `--close`), вывод генератора в отказ случая не печатается. Теперь свод ждёт
  выхода генератора и снятия замка (до 10 с) вместо паузы 1,5 с, провал печатает вывод генератора; мигание по требованию не воспроизводилось.
- ✅ (починено вторым шагом) **B-F6** — дефис не граница слова в хуке первого слова («Стоп-слова …» даёт приказ).
- ✅ (починено сессией 75) **D-F6** — `## **Description**` и значение метки на следующей строке; шаблон C не называет принимаемых русских слов.
  Теперь жирный заголовок — тот же раздел, значение читается со следующей строки, `kaif-testrun-lint bug --keywords` печатает слова
  каждого языка, и шаблон C его называет; `tb-mutants` M12–M14; 7078 документов багов поля — вердикт не изменился ни у одного.
- ✅ (починено вторым шагом) **A-F1** — проба паритета жанров `vo2-genre-parity.mjs` отказывает (код 2) с OW8: игла вырезания не находится — импортировать модуль
  напрямую; само заявление верно (126 вердиктов, 0 расхождений прямым импортом).
- ✅ (починено сессией 75) **A-F3** — слепок ядра голоса с UTF-8 BOM получает отказ контрольной точки с вводящим в заблуждение текстом — снимать BOM.
  Теперь ядро снимает метку перед сравнением; `s26` +1 (на сборке до починки красный ровно на нём).
- ✅ (починено сессией 75) **Заметка A** — строку «вход ≈ … токенов» машина печатает, но ни навык закрытия, ни руководство не велят передать её владельцу, как
  обещал принятый им сценарий (интервью №035, Q2). Теперь прощание `/end-chat-soft` несёт эту строку на языке владельца (оба слоя);
  пара сборки «entry cost ↔ /end-chat-soft» краснеет без предложения. Отчёт семи — `testcases/reports/2026-09-26_rl2-remaining-seven-findings.md`.
- ✅ (починено вторым шагом) Устаревшие маркеры `[TESTED]` (кластер D): `kaif-testrun-lint.mjs` (67/7), блок KAIF-WALK (прогон SC1), `templates-own-scan.mjs` (four
  answers), `up-mutants.mjs` (fourteen mutants).

## Лёгкий повторный судья RL2 (сессия 75, `ddca251..fd5dd01`)

Чистый экземпляр в своём клоне, 2026-09-26 10:13 → 10:35 +03:00: **VERIFIED WITH CAVEATS — 28 заявлений: 23 CONFIRMED · 5 WEAKENED ·
0 REFUTED**, блокеров выпуска нет. Оговорки и их судьба (отчёт — `testcases/reports/2026-09-26_rl2-ticket-107-and-rejudge-fixes.md`):
- ✅ **J-F1** — сторож документа при живой странице ОЧЕРЕДИ через минуту кончался кодом 2 с ложной строкой (регресс починки B-F1).
  Теперь замок `_queue` тоже живой контур для сторожа документа; самопроверка контура 4c, мутант ow6 на ней.
- ✅ **J-F2** — снятие «с 2.7» (C-F2) не доходило до поля по маршруту `update` ядром 2.7. Теперь свежее ядро на `checkpoint recheck`
  передаёт пункт `withdrawn-phrases`; `s21` E3 на настоящем маршруте с ядром v2.7.
- ✅ **J-F3** — B-F3 «шесть сообщений»: сообщений посреди хода было восемь, отказ — после шести; поправлено в страже, отчёте, реестре, плане.
- ✅ **J-F4** — стоячая ложь `STATUS.md` «находка B-F1 открыта» — снята с пометкой.
- **J-F5** — два заявления в сообщениях уже отправленных коммитов шире наблюдения (`c37d666` «красный ровно на этих трёх» — красных
  было пять, два из них нативный крах `bugs/109`; `56cf73e` «в задании каждого развёртывания» — только там, где в трёх файлах есть
  локальные правки); отчёты честны; поправку называет следующий коммит (история append-only).

## Лёгкий судья #107 и оговорок J (сессия 75, HEAD `365d163`)

Чистый экземпляр, 2026-09-26 11:15 → 11:27 +03:00: **REFUTED — 20 заявлений: 17 CONFIRMED · 2 WEAKENED · 1 REFUTED**. Выпуск остановлен
до починки (отчёт — `testcases/reports/2026-09-26_rl2-ticket-107-and-rejudge-fixes.md`, раздел «после судьи»):
- ✅ **K-F1** (опровержение, блокер: ложное «чисто» в поставке) — проверка голоса сверяла файл со снимком диска `shas`, который освежают
  update, update-verify и adopt-current: после любого обновления текст агента назывался «нетронутым шаблоном», код 0. Теперь — с
  `templateShas` (шаблон выпуска, нормализованные концы строк); `s26` +1 — правленный `GOAL.md` после `adopt-current` судится свидетелем.
- ✅ **K-F2** — `prerelease`, оставшийся в маркере не новее его версии, давал ложное происхождение на следующем обновлении. Теперь
  засчитывается только предрелиз новее версии маркера; `s04` S17 (б2); ответ #107 говорит, что поле дописывается руками только перед
  маршрутом загрузчика.
- ✅ **K-F3** — помощник меток времени заменил в правиле `STATUS.md` само слово-токен `{{T}}` — возвращено; помощник больше не трогает
  токен в обратных кавычках.
- **K-F4** — сторож документа считает живой и ЛЮБУЮ страницу очереди (ждёт, пока она жива, даже если его документ закрыт) — судья счёл
  приемлемым (терпение сторожа бесконечно по I9, он фоновый); названо здесь и в отчёте, кода не меняет.

## Судья релиз-кандидата 2.8 (сессия 75, HEAD `b276392`)

Чистый экземпляр, 2026-09-26 ≈ 11:45 → 11:57 +03:00: **VERIFIED WITH CAVEATS — 21 = 20 · 1 · 0**, блокеров нет; сценарий K-F1 повторён на
установке из `dist` (правленый `GOAL.md` после `adopt-current` — «written past the portrait», код 1); оговорки к нотам не относятся
(отчёт выпуска `testcases/reports/2026-09-26_release-2.8.md`, строка 10a). Судья закрытия (HEAD `54ddfd5`) — **VERIFIED WITH CAVEATS —
17 = 11 · 6 · 0**, шесть оговорок поправлены до отправки (записано 2026-09-26 12:16 +03:00).

## Отчёты судей

Полные отчёты — ответы субагентов этой сессии (таблицы строк, охоты, «второй угол», «не переисполнялось»); логи судей — в их временных
каталогах. Кластер B признал удаление по маске `%TEMP%\kaif-sbx-contour-shipped-*` в 05:52 — прогонов `s22` у оркестратора в ту минуту не
было.
