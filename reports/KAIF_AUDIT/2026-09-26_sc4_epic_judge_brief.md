# Бриф лёгкого судьи эпика SC 2.8 — «Сканеры видят проект глазами git»

> **Создан:** 2026-09-26 (сессия 74, ночной цикл; Claude Opus 5.5). **Родитель:** `plans/123` шаг SC4 · `plans/117` критерии 16–18.
> **Статус:** бриф для судьи чистым экземпляром. **Вовне:** —

## Что судить

Эпик SC — нулёвка и три шага, по коммиту на шаг (`git log --oneline -8`; сообщения несут блоки «ТЕСТ ИЗМЕНЁН»):

| Шаг | Коммит | Критерий `plans/117` | Отчёт прогона |
|---|---|---|---|
| SC0 | `3a04e9b` | — (нулёвка) | `testcases/reports/2026-09-26_sc0-inputs.md` |
| SC1 | `902e321` | 16 — обход не врёт «чисто» | `testcases/reports/2026-09-26_sc1-one-safe-walker.md` |
| SC2 | `193bff1` | 17 — заявление судится парой | `testcases/reports/2026-09-26_sc2-claim-is-a-pair.md` |
| SC3 | последний коммит эпика | 18 — шаблоны не краснят свой сканер | `testcases/reports/2026-09-26_sc3-templates-silent-under-own-scan.md` |

Между SC2 и SC3 — коммит починки `bugs/124` (личность git): суди его одной строкой реестра (п. 12). План — `plans/123`.

Сборка v2.7 для красных доказательств (`KAIF_DIST`):
`C:\Users\krinik\AppData\Local\Temp\claude\d--work-ai-sandbox-KAIF\fd24f464-9244-4fed-92b2-8a85edad28d2\scratchpad\ow1\v27\dist`.

## Правила тишины (обязательны)

- Ни одного окна у владельца: не запускай контур без `--no-open`, не поднимай страниц `--visible`, не открывай макеты и кадры.
- Ни одного звука: не зови `review.mjs --call` без `--dry-run`.
- Ни одного действия наружу: никаких `gh issue …`, `git push`, `kaif-core report` без `--dry-run`.
- Файлы репозитория не правишь; развёртывания поля (`D:\work\...` вне `KAIF`) — только чтение (копии — через `git archive`).
- **Никогда не пиши `git config` в этом репозитории и в его связанных worktree** (они делят `.git/config`; так случился `bugs/124`);
  личность для своих коммитов в песочнице — только переменными `GIT_AUTHOR_*` / `GIT_COMMITTER_*` или в своём `git init`.
- Полигон целиком (`npm run test:core`) не гоняй; отдельные своды и пробы — можно, по одному. Исполнитель параллельно правит ДРУГОЙ
  эпик в рабочем дереве — суди закоммиченное: экспорт `git show HEAD:dist/<файл>` в свою папку и `KAIF_DIST` на неё.

## Реестр заявлений (переисполнить, не перечитать)

1. **SC1 обход:** `s29` W1 (git, 20 worktrees, две битые ссылки) · W2 (без git) · W3 (ветка провала на блоке с подставной файловой
   системой) — зелёные; на v2.7 — красные поимённо. Семь копий блока `KAIF-WALK` равны (страж 5l; `node tools/sync-walker.mjs` — «in
   agreement»). Отмеченный в отчёте зазор: запрет чтения каталога на этой машине не воспроизводится — проверь сам, так ли это.
2. **SC1 восемь обходов:** скан заявлений, скан анонимности, шесть модулей — каждый на `kaifWalk`; модуль при провале обхода — код 1;
   пункт задания при провале — «could NOT see the whole tree», не «no lines found».
3. **SC1 функционально:** числа отчёта (дерево с 13 worktrees: 250 967 → 2 109 файлов, 184 973 в копиях → 0; `kaif-provenance` 313 → 26)
   переисполнимы пробой `tools/sandbox/probes/sc1-field-walk.mjs` (только чтение).
4. **SC2 пара:** `s29` C1–C7 зелёные; на v2.7 — C1–C4 красные; C5–C7 — стражи правил из функционального прогона.
5. **SC2 функционально:** скрипт `scratchpad/sc/sc2-field.mjs` на копиях четырёх полевых деревьев: 71 → 43 строки, 29 ушедших — все
   атрибуции или история? Прочитай выборку САМ; найди строку, которую новое правило потеряло, а она — настоящее заявление.
6. **SC2 пин:** словарь кода (`PIN_ID`) — не слишком узок? Пример из тикета #91 (`const EXPECTED_VERSION = '2.4';` в скрипте, который
   называет фреймворк) назван; придумай настоящий пин поля, который не назван.
7. **SC3 гард:** `node tools/check-framework.mjs --selftest` — строка 5m; `node tools/sandbox/probes/sc3-guard-mutants.mjs` — контроль 0,
   M1 3, M2 1; сборка на мутанте красная (отчёт, прогон 7).
8. **SC3 шаблоны:** под целью 2.8 → 2.9 шаблоны молчат; переписанные строки (оба слоя) не потеряли смысла.
9. **Команда `stale-claims`:** только чтение (ничего не пишет — проверь дерево до и после), код 1 только при провале обхода.
10. **Мутанты:** `node tools/sandbox/probes/sc-mutants.mjs` — «12 mutants red exactly on their named addressees».
11. **Отчёты SC0–SC3:** строки «Прогоны» с моментом и командой; числа совпадают с выводом команд; «РЕАЛЬНЫЙ МИР» не шире прогона.
12. **bugs/124:** преполёт 0 `tools/commit.mjs` — `--selftest` и отказ на тестовой личности; проба 1d больше не пишет `git config`.

## Охоты KAIF-блока (`/fable-judge`)

- Заявление шире наблюдения; ослабленный тест без обоснования; стоячая ложь в `plans/117` (16–18 и строка статуса), `plans/123`,
  `STATUS.md`, `MASTER_PLAN.md` §8.
- Развилки без строки `FORK:` (SC1 — где живёт обходчик; SC2 — пин) и решения агента в одежде слова владельца.
- Близнецы: другие обходы дерева в ядре или модулях, не перешедшие на `kaifWalk`; другие места, решающие «заявление» формой строки.
- Кириллица в теле поставки (`framework/` кроме `texts.mjs` и языковых пакетов) — новые вхождения; приватные имена в поставке.

## Что вернуть

Вердикт (VERIFIED / VERIFIED WITH CAVEATS / REFUTED) и реестр: по строке на заявление — CONFIRMED / WEAKENED / REFUTED с командой и её
выводом (первые строки). Находки — с адресом (файл:строка) и предлагаемой починкой. Не чини сам. Полный отчёт положи также в
`C:\Users\krinik\AppData\Local\Temp\claude\d--work-ai-sandbox-KAIF\fd24f464-9244-4fed-92b2-8a85edad28d2\scratchpad\judge-sc4\verdict.md`.
