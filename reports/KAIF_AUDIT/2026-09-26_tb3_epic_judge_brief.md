# Бриф лёгкого судьи эпика TB 2.8 — «Баг-репорт тестировщика и охота за шагами»

> **Создан:** 2026-09-26 (сессия 74, ночной цикл; Claude Opus 5.5). **Родитель:** `plans/124` шаг TB3 · `plans/117` критерии 27–28.
> **Статус:** бриф для судьи чистым экземпляром. **Вовне:** —

## Что судить

Эпик TB — нулёвка и один шаг на два критерия (`git log --oneline -6`; сообщения несут блоки «ТЕСТ ИЗМЕНЁН»):

| Шаг | Коммит | Критерий `plans/117` | Отчёт прогона |
|---|---|---|---|
| TB0 | `d0cec72` | — (нулёвка) | `testcases/reports/2026-09-26_tb0-inputs.md` |
| TB1–TB2 | `4b06b28` | 27 — отчёт тестировщика четырьмя разделами · 28 — охота за шагами | `testcases/reports/2026-09-26_tb1-tester-report-and-hunt.md` |

Тикет-источник — origin #105 (`gh issue view 105` — ЧТЕНИЕ разрешено; комментировать нельзя). План — `plans/124`. Тот же коммит
`4b06b28` несёт попутную правку статусов по суду эпика SC (критерии 16–17 сняты с ✅) — суди её одной строкой реестра (п. 10).

Сборка v2.7 для красных доказательств (`KAIF_DIST`):
`C:\Users\krinik\AppData\Local\Temp\claude\d--work-ai-sandbox-KAIF\fd24f464-9244-4fed-92b2-8a85edad28d2\scratchpad\ow1\v27\dist`.

## Правила тишины (обязательны)

- Ни одного окна у владельца: не запускай контур без `--no-open`, не поднимай страниц `--visible`, не открывай макеты и кадры.
- Ни одного звука: не зови `review.mjs --call` без `--dry-run`.
- Ни одного действия наружу: никаких `gh issue comment/close/edit`, `git push`, `kaif-core report` без `--dry-run`.
- Файлы репозитория не правишь; развёртывания поля (`D:\work\...` вне `KAIF`) — только чтение (копии — через `git archive`).
- **Никогда не пиши `git config` в этом репозитории и в его связанных worktree** (они делят `.git/config`; так случился `bugs/124`);
  личность для своих коммитов в песочнице — только переменными `GIT_AUTHOR_*` / `GIT_COMMITTER_*` или в своём `git init`.
- Полигон целиком (`npm run test:core`) не гоняй; отдельные своды и пробы — можно, по одному. Исполнитель параллельно правит ДРУГОЙ
  эпик в рабочем дереве — суди закоммиченное: свой клон на `4b06b28` (удали в нём remote) или экспорт `git show 4b06b28:<файл>`.

## Реестр заявлений (переисполнить, не перечитать)

1. **Канон обоих слоёв:** шаг 6 «Hunt the reproduction» (оси, каждая попытка записана, «не воспроизвелось» — не меньше трёх вариантов)
   и шаг 7 (четыре раздела, три строки, шаги — путь пользователя) — в `framework/TESTING_FRAMEWORK.md` и корневом `TESTING_FRAMEWORK.md`;
   смысл слоёв совпадает; потолок 270 соблюдён (гард 5j), слитые буллеты о стражах не потеряли обязательств
   (`node tools/canon-inventory.mjs --diff v2.7 --with framework/KAIF_REFERENCE.md --declared plans/118_epic117_CK_light_canon_budget_exits.md` → lost 0).
2. **Шаблон C** `framework/skills/report-bug/SKILL.md` — совпадает ли с формой тикета #105 (прочитай тикет); корневой навык ссылается.
3. **Линтер `bug`:** `node framework/tools/kaif-testrun-lint.mjs selftest` — 67 случаев OK; пять правил. Найди отчёт, который правило
   должно назвать, а оно молчит, или законный отчёт, который оно ложно краснит (строки таблицы охоты, «не воспроизвелось» в теле
   раздела, заголовки с номерами, английский и русский вперемешку, пустая строка значения).
4. **Свод `s25` раздел 5:** `node tools/sandbox/s25-testrun-lint.mjs` — «all 74 checks green»; на v2.7 — 21 красный, все 11 проверок TB
   поимённо.
5. **Мутанты:** `node tools/sandbox/probes/tb-mutants.mjs` — «7 mutants red exactly on their named addressees».
6. **Функциональный прогон на поле:** `scratchpad/tb/tb-field.mjs` на копиях четырёх полевых деревьев (пути деревьев — в задании
   судьи, не в брифе: имена полевых проектов приватны; оригиналы только читаются) —
   у C и D шаг в каноне и шаблон C в навыке, `bug` отвечает 0 · 1 · 1 · 0; у переведённых A и B — дельты в задании обновления.
   Пометка отчёта: проба ложно печатала «hunt step in file YES» для A и B — проверь, что вывод отчёта сделан по прочитанному.
7. **Буквальные «Проверки» критериев 27–28** — исполни их сами (отчёт: 04:07:44, `grep -cE` → 4; `grep -c "Hunt the reproduction"` → 1).
8. **Заметка 2.8 и строка политики** (`tools/build-framework.mjs`), записка §14 — говорят то же, что сделано; ни одной строки шире.
9. **Счётчики:** модулей 836 (шаблон C — новый модуль): `node tools/counters-guard.mjs` зелёный.
10. **Попутная правка статусов:** критерии 16–17 сняты с ✅ по суду SC4 в `plans/117`, `STATUS.md`, `MASTER_PLAN.md` §8, `plans/123` —
    счёт «23 из 29» верен (перечисли закрытые сам).

## Охоты KAIF-блока (`/fable-judge`)

- Заявление шире наблюдения («pass» отчёта TB1 — держится ли без отчёта разработчику из самого тикета?); ослабленный тест без
  обоснования (закреп строки самопроверки в `s25` сменён — обоснован?); стоячая ложь в `plans/117` (27–28 и строка статуса), `plans/124`,
  `STATUS.md`, `MASTER_PLAN.md` §8.
- Развилки без строки `FORK:` (носитель проверки — TB0) и решения агента в одежде слова владельца.
- Близнецы: другие места поставки, описывающие отчёт о дефекте старой формой строки (шаг 6 прежней редакции), — остались ли
  (`grep -rn "Steps to reproduce\|reproduction steps" framework/`).
- Кириллица в теле поставки (`framework/` кроме `texts.mjs`, языковых пакетов и разрешённого `kaif-testrun-lint.mjs`) — новые
  вхождения; приватные имена в поставке и в сообщении коммита.

## Что вернуть

Вердикт (VERIFIED / VERIFIED WITH CAVEATS / REFUTED) и реестр: по строке на заявление — CONFIRMED / WEAKENED / REFUTED с командой и её
выводом (первые строки). Находки — с адресом (файл:строка) и предлагаемой починкой. Не чини сам. Полный отчёт положи также в
`C:\Users\krinik\AppData\Local\Temp\claude\d--work-ai-sandbox-KAIF\fd24f464-9244-4fed-92b2-8a85edad28d2\scratchpad\judge-tb3\verdict.md`.
