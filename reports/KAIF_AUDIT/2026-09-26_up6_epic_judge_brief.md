# Бриф лёгкого судьи эпика UP 2.8 — «Обновление ничего не теряет молча»

> **Создан:** 2026-09-26 (сессия 74, ночной цикл; Claude Opus 5.5). **Родитель:** `plans/122` шаг UP6 · `plans/117` критерии 14–15.
> **Статус:** бриф для судьи чистым экземпляром. **Вовне:** —

## Что судить

Эпик UP закрыт одним коммитом шагов UP0–UP5 (последний коммит эпика — `git log --oneline -1` на момент суда; его сообщение несёт блок
«ТЕСТ ИЗМЕНЁН»). План — `plans/122`; сценарии критериев 14–15 — `plans/117` (строка «Проверка» каждого — первая команда суда).
Отчёты прогонов: `testcases/reports/2026-09-26_up0-inputs.md` · `testcases/reports/2026-09-26_up-update-loses-nothing.md`.

Сборка v2.7 для красных доказательств (`KAIF_DIST`):
`C:\Users\krinik\AppData\Local\Temp\claude\d--work-ai-sandbox-KAIF\fd24f464-9244-4fed-92b2-8a85edad28d2\scratchpad\ow1\v27\dist`
(копия артефактов тега v2.7; её можно только читать).

## Правила тишины (обязательны)

- Ни одного окна у владельца: не запускай контур без `--no-open`, не поднимай страниц `--visible`, не открывай макеты и кадры.
- Ни одного звука: не зови `review.mjs --call` без `--dry-run`.
- Ни одного действия наружу: никаких `gh issue …`, `git push`, `kaif-core report` без `--dry-run`.
- Файлы репозитория не правишь; развёртывания поля (`D:\work\...` вне `KAIF`) — только чтение.
- Полигон целиком (`npm run test:core`) не гоняй — его гоняет исполнитель в одиночку; отдельные своды и пробы — можно, по одному.

## Реестр заявлений (переисполнить, не перечитать)

1. `node tools/sandbox/s27-rename-map.mjs` — зелёный, случаи F1 · F2 («переименовано заранее»: нетронутое тело заменено новым модулем,
   правленое получает дельту в задании; лог НЕ пишет «the section arrives as new»); с `KAIF_DIST=<v2.7>` — красны ровно 4 новых случая.
2. `node tools/sandbox/s21-update-route.mjs` — зелёный; случаи B7 (слот со скобками), D1 (новый раздел доехал · не доехал — красный),
   D2 (повторное предложение по квитанции · контроль без неё), E (путь ядром 2.7: установка из `git show v2.7:dist/…`, `update` ядром 2.7,
   `update-verify` доставленным ядром — молчит на целом дереве, красный на недоехавшем); с `KAIF_DIST=<v2.7>` — красны ровно новые (6).
3. `node tools/sandbox/s18-update-symmetries.mjs` — зелёный; U3б (запись репетиции несёт отпечаток ядра) · U3в (чужая запись не
   привязана); с `KAIF_DIST=<v2.7>` — красны 2. Шов `KAIF_DIST` в `s18` не меняет поведение без переменной.
4. `node tools/sandbox/probes/up-mutants.mjs` — «5 mutants red exactly on their named addressees». Прочитай каждого мутанта: ломает ли он
   ИМЕННО заявленный предикат, или адресат краснеет по побочной причине.
5. Фикстура A2 `s21` теперь подписывает запись репетиции отпечатком ядра — суть случая («авто-запись потреблена») не ослаблена? Сверь с
   `git show HEAD~1:tools/sandbox/s21-update-route.mjs`.
6. Мета бандла `dist/KAIF-CORE-BUNDLE.md` несёт `sectionsNew` = `{ prev: "2.7", files: … }`. Каждый раздел в списке действительно новый
   против `git show v2.7:dist/kaif-module-map.json` (выборка ≥ 5 разделов, и хотя бы один — проверка, что НЕ новый раздел в список не попал).
7. Переведённое развёртывание: раздел, не найденный в переведённом файле, `update-verify` НАЗЫВАЕТ для ручной сверки (не красный); в
   непереведённом — красный. Строка «Результат» критерия 14 обещает ещё «агенту выдаётся развёрнутая форма файла под язык развёртывания
   для ручного слияния» — есть ли это в задании обновления на переведённом файле (прежнее поведение i18n или новое)? Если нет — находка.
8. Отпечаток ядра: явный `--rehearsal` с чужой подписью — отказ; авто-запись чужого ядра — игнор с названием причины; явная неподписанная
   — предупреждение. Найди код (`SELF_SHA`, `loadRehearsal`) и сверь три ветки с тем, что утверждают отчёт и `/kaif-update`.
9. `grep -n autocrlf framework/skills/kaif-update/SKILL.md .claude/skills/kaif-update/SKILL.md framework/KAIF_REFERENCE.md` — рецепт
   `git -c core.autocrlf=false archive` и условие о концах строк у «побайтно» в обоих слоях и в записке §10.8.
10. `wc -l framework/AGENT_GUIDE.md` ≤ 1080; `node tools/canon-inventory.mjs --diff v2.7 --with framework/KAIF_REFERENCE.md --declared
    plans/118_epic117_CK_light_canon_budget_exits.md` → `lost 0`.
11. Отчёты прогонов UP0 и UP: каждая строка «Прогоны» — с моментом и командой; числа совпадают с выводом команд, которые ты
    переисполнил; вердикт `partial` честен; строка «РЕАЛЬНЫЙ МИР» есть.
12. Заметка 2.8 для поля (сборка, `tools/build-framework.mjs`) говорит об UP то, что код делает, — не шире.

## Охоты KAIF-блока (`/fable-judge`)

- Заявление шире наблюдения («проверено на реальном» о проверенном на фикстуре; «путь поля» о пути, где ядро не 2.7).
- Ослабленный тест без обоснования в сообщении коммита (`git show HEAD --stat` + сообщение; фикстура A2).
- Стоячая ложь: «закрыто N из 29», числа проверок сводов, статус UP в `STATUS.md`, `plans/117`, `plans/122`, `MASTER_PLAN.md` §8 против
  переисполненного.
- Развилка без строки `FORK:` (UP2 — строка в `plans/122`); решение агента в одежде слова владельца.
- Кириллица в теле поставки (`framework/` кроме `texts.mjs` и языковых пакетов) — новые вхождения.
- Близнецы: захват слота `[^\n]+?` — есть ли другие места ядра с тем же исключением `<>`; `onDisk.has(o)` — другие привязки пар.

## Что вернуть

Вердикт (VERIFIED / VERIFIED WITH CAVEATS / REFUTED) и реестр: по строке на заявление — CONFIRMED / WEAKENED / REFUTED с командой и её
выводом (первые строки). Находки — с адресом (файл:строка) и предлагаемой починкой. Не чини сам.
