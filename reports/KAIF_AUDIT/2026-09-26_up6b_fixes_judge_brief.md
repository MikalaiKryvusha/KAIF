# Бриф повторного суда — починки суда эпика UP 2.8 (UP6)

> **Создан:** 2026-09-26 (сессия 74, ночной цикл; Claude Opus 5.5). **Родитель:** `plans/122` шаг UP6 · `plans/117` критерий 14 ·
> первый суд — бриф `reports/KAIF_AUDIT/2026-09-26_up6_epic_judge_brief.md` (вердикт REFUTED).
> **Статус:** бриф для судьи чистым экземпляром. **Вовне:** —

## Что судить

Первый суд эпика UP опроверг три пункта результата критерия 14 (R1 · R2 · R3) и назвал каверзы F4–F17. Починки — последний коммит
(`git log --oneline -1` на момент суда; сообщение несёт блок «ТЕСТ ИЗМЕНЁН»), отчёт — `testcases/reports/2026-09-26_up6-judge-fixes.md`,
перечень починок — `plans/122` → шаг UP6. Судишь ПОЧИНКИ: держится ли каждая, не сломано ли соседнее, честны ли своды и мутанты.

Сборка v2.7 для красных доказательств (`KAIF_DIST`):
`C:\Users\krinik\AppData\Local\Temp\claude\d--work-ai-sandbox-KAIF\fd24f464-9244-4fed-92b2-8a85edad28d2\scratchpad\ow1\v27\dist`.

## Правила тишины (обязательны)

- Ни одного окна у владельца: не запускай контур без `--no-open`, не поднимай страниц `--visible`, не открывай макеты и кадры.
- Ни одного звука: не зови `review.mjs --call` без `--dry-run`.
- Ни одного действия наружу: никаких `gh issue …`, `git push`, `kaif-core report` без `--dry-run`.
- Файлы репозитория не правишь; развёртывания поля (`D:\work\...` вне `KAIF`) — только чтение (копии — через `git archive`).
- Полигон целиком (`npm run test:core`) не гоняй; отдельные своды и пробы — можно, по одному. Исполнитель параллельно работает над
  другим эпиком в рабочем дереве (ядро меняется в ДРУГИХ функциях) — суди закоммиченное: `git show HEAD:<путь>`, а своды гоняй против
  `dist` из HEAD (`git stash` не делай; при сомнении — копия `dist` из `git show HEAD:dist/…` и `KAIF_DIST`).

## Реестр заявлений (переисполнить, не перечитать)

1. **R1** — `s27` случай G: переименование на переведённом файле — лог «upstream delta in the task (i18n: translated) — nothing replaced
   on disk», файл побайтно цел; на v2.7 — красный.
2. **R2** — `s21` N3: английский файл на переведённом развёртывании — невлитая строка апстрима названа `update-verify`; на v2.7 — красный.
   Проверь и обратное: переведённый файл (по квитанции) по-прежнему пропускается в сверке строк.
3. **R3** — `s21` N4: `diff --source <x> --render <файл>` побайтно равен тому, что пишет `install` (язык, алиасы, заполнения); отказ на
   файле вне выпуска. Функционально: рендер на копии полевого переведённого дерева несёт новый раздел выпуска.
4. **F4** — `s21` A2b: неподписанная авто-запись репетиции названа, отброшена и убрана с диска.
5. **F5** — `s21` A5: чужая подписанная запись на bootstrap — отказ ДО копии дерева, журнала обновления нет.
6. **F6** — `s21` N1 и E судят счёт провалов (+1); мутант J3 (без `missing++`) краснит их.
7. **F7** — `s21` E2: путь поля (квитанция ядра 2.7 без `translatedFiles`) на переведённом развёртывании — английский файл без нового
   раздела КРАСНЫЙ, не «проверь руками».
8. **F8** — `s21` B6b: H1 с именем проекта не «недоехавший раздел»; мутант M12 краснит.
9. **F13 · F16** — текст заметки 2.8 (`tools/build-framework.mjs`) и три исхода записи репетиции в `/kaif-update` обоих слоёв и §10.8 —
   равны поведению ядра.
10. **F14** — `sectionsNew` строится против старшего тега, который не указывает на этот коммит; свод E выводит тег так же.
11. **F15** — `s21` B8 и мутант M11.
12. **Проба** — `node tools/sandbox/probes/up-mutants.mjs` → «14 mutants red exactly on their named addressees»; прочитай J3 и M7: исправление
    их адресатов после первого прогона — законная поправка предсказания или подгонка?
13. **Отчёт** — числа отчёта починок совпадают с выводом команд; строка «РЕАЛЬНЫЙ МИР» не шире прогона; вердикт `partial` честен.

## Охоты KAIF-блока (`/fable-judge`)

- Ослабленный тест без обоснования (сообщение коммита: «ТЕСТ ИЗМЕНЁН»; усилены N1/E — проверь, что не ослаблены).
- Заявление шире наблюдения; стоячая ложь в `plans/117` (критерий 14 и строка статуса), `plans/122`, `STATUS.md`, `MASTER_PLAN.md` §8.
- Близнецы: другие места ядра, где флаг `i18n` развёртывания решает за файл; другие исходы лога, не смотрящие на прогон «без записи».
- Кириллица в теле поставки (`framework/` кроме `texts.mjs` и языковых пакетов) — новые вхождения.

## Что вернуть

Вердикт (VERIFIED / VERIFIED WITH CAVEATS / REFUTED) и реестр: по строке на заявление — CONFIRMED / WEAKENED / REFUTED с командой и её
выводом (первые строки). Находки — с адресом (файл:строка) и предлагаемой починкой. Не чини сам. Полный отчёт положи также в
`C:\Users\krinik\AppData\Local\Temp\claude\d--work-ai-sandbox-KAIF\fd24f464-9244-4fed-92b2-8a85edad28d2\scratchpad\judge-up6b\verdict.md`.
