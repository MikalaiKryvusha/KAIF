Thank you — both defects are fixed in [KAIF 2.7 — Audited KAIF](https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.7).

A question now has a fourth fact — **implemented**: when a decision has gone into the rules or the code, the agent marks it (`review.mjs --mark-implemented <doc> <Q> --where <commit or file>`), and the queue never raises that question a second time — instead of a page it prints "implemented, but open → close the status" and exits with code 2. The interview page now shows the live questions first, and everything answered plus the document text sits below in one collapsed archive (nothing is removed). The update to 2.7 brings the generator with both rules.
