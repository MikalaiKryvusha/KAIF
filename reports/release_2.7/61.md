Shipped in [KAIF 2.7 — Audited KAIF](https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.7), by the owner's statement of the expected behaviour: *the agent writes the text in the voice and by the rules the owner's stylometry prescribes; after writing, by that same stylometry, it runs an independent check of what it wrote, fixes it, and only then counts the text as written and brings it to the owner for approval.*

- `AGENT_GUIDE.md` (both layers) gains a FOURTH KAIF obligation at the fable loop's call point, three named steps: **write BY the portrait** (`AUTHOR_STYLOMETRY.md` — its rules, lexicon, anti-portrait and before/after pairs open BEFORE the first word) → **check INDEPENDENTLY by the same portrait** (the machine minute + the §7B pass by a clean instance — the writer's own glance is not one) → **fix, only then written, only then shown**; the same contract stands in "Showing is an action", checklist step 19 and the router row. Your proposal (b) — one line at step 5 and in the show ritual — is in, as the contract rather than a line.
- Your proposal (a): NEW optional tool module `.kaif/tools/kaif-voice-lint.mjs` (`check <files…> [--warn]` / `selftest`) — the machine half of step 2. It reads the §8 TABLE of the portrait — `pattern · class (stop / positive) · hint · legal exception` — and prints every hit as `file:line — «fragment» → hint`; `\|` inside the pattern cell is alternation, a bare pattern is case-sensitive, `/…/i` folds case, `\b`/`\w` are Unicode-aware (JavaScript's own are blind to Cyrillic); a row's `/regex/` exception silences a hit on its line, prose is printed beside it; fenced code, inline code and HTML comments are invisible; `SKIPPED=3` without a portrait, without a §8 table or with placeholder rows only — likeness is never judged, that verdict is the owner's. Your portrait's §8 already keeps its stop-constructions as a table; the linter wants the four columns above (the skeleton `.kaif/_owner-voice-template.md` shows the form) — a table with other columns is named in the `SKIPPED` line rather than half-read.
- `/owner-voice` carries the writing contract and gains mode `check`; the skeleton's §7 makes the checklist the INDEPENDENT check after writing by the portrait and §7B a clean-instance pass.
- Your proposal (c): the installer's `owner-voice` item now asks to wire the command into the build path of EVERY owner-facing text class the deployment has (a sheet generator, a string table, a README section) — a hit stops the build or is answered in the portrait's exception column.
- `/fable-judge` hunts **"owner text past the portrait"**: written without the portrait open, checked by no independent pass, or shown before the fixes — and a draft written natively and re-voiced later presented as "by the portrait".

- The owner's second word — write BY the stylometry WITH IT IN THE WORKING CACHE — is mechanized the way the context-refresh witness is: `node .kaif/tools/kaif-voice-lint.mjs load [--sections <regex>]` prints the portrait (or the modules the unit needs) INTO the agent's working context before the first word and leaves the witness `.kaif/voice-marker.json` (a history of load moments, the portrait's path and sha; session state, ignored by git); `check` refuses a text with no witness, with a witness for another portrait, last written before the first load, or written more than an hour after the last load ("written past the portrait" — the hour rule; never muted by `--warn`). A draft written natively and re-voiced later is the very class the contract closes.

Proof: `kaif-voice-lint selftest` — 48 cases in two languages (patterns, table parsing, the witness verdicts, `\b`/`\B` Unicode); sandbox suite s26 — 47 checks green on 2.7 (an EN and a RU project red on `sheet/steps.md:7 — «Помни, что» → …`, a file written before `load` → "written past the portrait", clean green with the load moment, prose §8 / no portrait SKIPPED for `check` and `load`, the deployed copy carries the module and the skeleton with both commands and the table form) and red on the 2.6 core (7 of 47 addressed to the absent module and skeleton form); the origin's own run is filed as `testcases/reports/2026-09-12_polygon-2.7-VC.md`. Your 2.5 → 2.7 update task carries the policy line. Thank you — the ticket named the class exactly, and the owner's correction of the statement (write BY the portrait first, with it in the cache, then check independently) is what shaped the contract.
