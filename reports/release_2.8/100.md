Shipped in [KAIF 2.8 — Noble KAIF](https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.8):

- **A queue file of another shape is read, not trusted.** The shipped contour checks the shape of `<decisionsDir>/queue.json` before it reads it: an array is its own queue; anything else — your `{ "items": [...] }`, or JSON that does not parse — is the project's own queue. `--queue --list` then prints one line and no stack trace: "the project keeps its own queue: interviews/decisions/queue.json is not this contour's shape — read as no items here and never written; its waiting documents are raised by the project's own contour (see HOUSE_RULES.md), the interviews are still scanned".
- **…and never written.** The twin your ticket did not hit but the code carried: `--enqueue` and the notice mark write the same file, and a naive "a foreign file reads as an empty queue" would have turned them into an overwrite of your queue. A write into a foreign-shape queue is refused — `--enqueue` exits 1 with "not written: … is the project's own queue (another shape) — this contour never overwrites it", and the file stays byte for byte.
- The documents in `interviews/` are still scanned by the shipped command.

What your side does: nothing, unless you want the shipped queue — then move your own file aside by your owner's word. The line points to `HOUSE_RULES.md` for your own contour's queue command, which the shipped contour cannot know; naming it there (the skeleton's section "Tools of this project") gives `/resume` a command it can find in your project.

Proof: sandbox suite `tools/sandbox/s22-contour-shipped.mjs` section B on a deployed copy — `{"items":[]}` in the decisions directory → `--queue --list` exits not 1, with the line and no `TypeError`; `--enqueue` into it → exit 1, the file byte for byte; both cases are red on the 2.7 core. The mutant probe `tools/sandbox/probes/ow3-contour-mutants.mjs` — "read without the shape check" (it reddens both cases: the write path read the object and crashed too) and "write over a foreign queue", each red only on its cases. Functional run on a read-only clone of another field deployment whose own, earlier contour keeps `queue.json` under the same name: before the fix the queue command crashed there with your `TypeError`; after it, exit 0, the line, no trace. Run report: `testcases/reports/2026-09-25_ow3-ow7-owner-debt-foreign-queue.md`.

Boundaries, said plainly: the optional half of your fix — reading `{ items: [...] }` by mapping `file → doc` — did not ship; the shipped contour lists none of your items, your own contour raises them. The example in `/resume` step 1b (`node tools/review.mjs --queue --list`) is unchanged — it stays an example of "the queue command of the project's interactive contour".

Reading a queue of another shape by mapping its items did not ship in 2.8; it is in the list of the next version.
