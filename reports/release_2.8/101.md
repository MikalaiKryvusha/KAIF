Shipped in [KAIF 2.8 — Noble KAIF](https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.8):

- **Every shipped module with exports imports silently.** The command runs only when the file IS the program — the direct-run guard the shipped contour already had, now in all seven lint modules with exports: `kaif-attribution-lint`, `kaif-experience-lint`, `kaif-guard-lint`, `kaif-ranking-lint`, `kaif-scenario-lint`, `kaif-testrun-lint`, `kaif-voice-lint`. An import prints nothing and never ends the importer's process; run from a terminal, each prints what it printed before.
- **Seven, not four.** Your twin check read only the last line of each import. Read whole, from an empty directory, all seven lint modules ran their command on import (exit 1–3, the import never resolved) — `experience`, `guard` and `scenario` too. The acceptance probe was written before the fix, to read the whole output.

What your side does: nothing; after the update the `process.argv` swap in `stylometry/eval/score.mjs` can be dropped — import `parsePortrait` and `lintText` directly.

Proof: the acceptance probe `tools/sandbox/probes/ow8-import-silence.mjs` — BAD 7 of 10 on the sources before the fix and on 2.7, then `GOOD — all 10 module(s) import silently` (re-run on the current sources on 2026-09-26: the same). Sandbox suite `tools/sandbox/s26-voice-lint.mjs`, section (6): ten imports from an empty directory print only `imported`, and a project tool that imports `parsePortrait` and `lintText` from the deployed `.kaif/tools/kaif-voice-lint.mjs` prints only its own line (`essay-check: N finding(s)`); on the 2.7 core that section has 8 red — the seven lint modules and the project tool. Mutant M18 of `tools/sandbox/probes/voice-mutants.mjs` — the voice module's guard removed — reddens exactly those cases. Run report: `testcases/reports/2026-09-25_ow8-modules-import-silent.md`.

Boundaries, said plainly: your own tool on macOS and Node v23 was not run here (it is not on this machine) — the deployed-copy test stands in for it, and your 2.8 update report is the confirmation. A module started through a symbolic link to its file may compare as an import and stay silent; the delivery path `node .kaif/tools/<module>` does not do that (named in the run report).
