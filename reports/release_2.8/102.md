Shipped in [KAIF 2.8 — Noble KAIF](https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.8):

- **A §8 row knows its genre.** A row whose hint OPENS with a genre label judges only its genres: `[work]`/`[работа]` — every genre but the essay; `[document]`/`[документ]` — documents only; `[prose]`/`[проза]` — the essay only. A row without a label judges every text. Labels are read in either case and several in a row (`[work][document]`); a bracketed word that is not one of them (a `[see §5]`) stays in the hint and labels nothing. The pattern column is untouched, and a portrait without labels lints exactly as before.
- **The genre is named at the call.** `node .kaif/tools/kaif-voice-lint.mjs check <file…> --genre <document|ticket|comment|message|reply|essay>` silences the rows of other genres and says how many (`genre essay: N rule(s) of other genres silent`). Without `--genre` every row judges every text, as before, and the run prints one line — `N rule(s) of <portrait> §8 carry a genre label — … name the text's genre` — while each hit prints its hint, label first.
- The genre names and the rule of applicability are the ones of `voice-check.mjs` in the owner's core storage, so one label means one thing in both tools.
- **Loading follows the genre.** A bare `load` now includes §1 (how to read the portrait) among the writing sections, and `load --genre essay` adds the prose register §3.

Nothing to merge: the module arrives with the update. Name the text's genre when you check it; an essay in the owner's name is `--genre essay`.

Boundaries, said plainly: the label map is fixed in the module, the same as the core storage's — there is no `voiceGenres` in `.kaif/kaif.json`, and the short label `[doc]` of your proposal is not a label (write `[document]` or `[документ]`, as the core does). `prose` is a label, not a genre: `--genre prose` is a usage error (exit 2); the essay is `--genre essay`. Without `--genre` the run names the COUNT of labelled rows, not each row.

Proof: the module's `selftest` — 62 cases, among them the labels in both languages, each label's applicability to all six genres, the filter of `check` and the essay load; sandbox suite `tools/sandbox/s26-voice-lint.mjs` section (4) on a deployed copy — 8 checks, all 8 red on the 2.7 core; `tools/sandbox/probes/voice-mutants.mjs` — the mutants "labels ignored" and "an unlabelled row falls silent under `--genre`" each red exactly on its named addressees; parity with the core storage's rule — 21 rows × 6 genres = 126 verdicts, 0 disagreements, while a copy with the labels switched off disagrees in 20. Functional run on the owner's own prose — seven excerpts, 937 words, only the counts printed: without a genre 4 stops, all four from `[работа]` rows; `--genre essay` — 0, green; `--genre ticket` — the same 4 named. That is your 214 of 217 on a smaller sample. Run reports: `testcases/reports/2026-09-25_vo2-genre-labels.md` and `testcases/reports/2026-09-25_vo4-epic-judge-fixes.md` (the epic's judge added the second mutant). The release court over this work returned VERIFIED WITH CAVEATS with no claim refuted.

Thank you — the ticket brought both the measurement and the smallest form of the fix.
