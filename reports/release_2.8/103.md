Shipped in [KAIF 2.8 — Noble KAIF](https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.8):

- **The public snapshot is built from core 2.2.** The snapshot generator learned the 2.x layout: sections whole by number (§0 · §0-M · §1 · §2.x · §2-C · §3–§8 · the appendices), the journal §9 as an address line, the §8 table byte for byte with its genre labels. It runs on decision №60, which your ticket kept in force: the rules travel whole, and a phrase of the owner that is not already public in KAIF collapses to «…». The result: 117 rules, 11 of the owner's phrases already public in the repository, 16 spans collapsed; three samples of §2.6 travel and two are withdrawn. The file opens with `# Ядро голоса и мышления владельца KAIF — публичный слепок`, and `kaif-voice-lint check` reads 21 stop rules from its §8.
- **Nothing private travels.** The generator refuses a source that carries the work layer or the prose module (by file name and heading); one check compares every eight-word window of the snapshot with the sample blocks of both private layers; another requires that no collapsed phrase stands in any tracked file of the origin — since the epic's judge, checked on the index and the message of every origin commit.
- **The update replaces, and a merge is refused.** The release pins its snapshot in the bundle meta: sha256, first line, core version and the snapshot's own public labels. A portrait that names one of those labels and is not the snapshot gets the update task item `owner-voice-core`: fetch the release file, keep ONLY your local part above its first line (a project preamble; a genre shell, re-derived over the new snapshot by `/owner-voice`), put the snapshot after it byte for byte, and name it in the update report as `voice portrait: core <was> → 2.2 (replaced)`. `node .kaif/kaif-core.mjs checkpoint owner-voice-core` compares the snapshot part by sha256 and refuses until it matches. It also refuses a merge: a first line of any public snapshot left in your local part means the previous portrait is still in the file. A portrait that names none of the labels — another owner's, or a private copy of the core kept out of git — gets no item, and not a byte of it is touched.

What your side does: on the `update` route the task is written by your outgoing 2.7 core, which has no such item, so the fresh core's `node .kaif/kaif-core.mjs checkpoint recheck` refuses with the same instruction until the replacement is done — replace, then run `recheck` again. On the loader route the item is in the task. A project tool that stamps its own blocks INTO the portrait runs after the checkpoint. A snapshot saved with a UTF-8 BOM is accepted; the core strips the mark before comparing.

Boundaries, said plainly: the snapshot is not core 2.2 byte for byte, so after the update the portrait equals the release snapshot, not the private core, and it does not open with the core's own title as your ticket expected. The line in the update report is written by the agent from step 3 of the instruction; no machine checks it. Three gaps are named in the code: a merge that drops the previous snapshot's first line passes; a genre shell kept unchanged above the new snapshot passes (its re-derivation is the agent's judgement); a raw copy of the private core kept under the name `AUTHOR_STYLOMETRY.md` IS recognized, because the core carries one of the labels. The core's own list of consumers has no such project; if yours is one, decide it with your owner before replacing anything.

Proof: sandbox suite `tools/sandbox/s26-voice-lint.mjs` section (5) on a deployed core — 13 checks, 12 of them red on the 2.7 core (the thirteenth, "after the replacement `recheck` passes", is green there by construction, and a mutant proves its red), plus one check for the BOM, red on the build before its fix; `tools/sandbox/probes/voice-mutants.mjs` — the mutants of the labels, the sha, the recognition and the hand-over, each red exactly on its named addressees; the generator's `--selftest` and its own mutants, each red only on its addressee. Functional run on git clones of three of the owner's field deployments, both routes: a copy with a project preamble — refused before the replacement, refused on the judge's merge (the whole previous portrait above the new snapshot), accepted after the replacement with its 12 preamble lines kept; a genre shell — the same; a private portrait kept out of git — untouched on both routes. The epic's judge REFUTED my first build: a merge above the snapshot passed the checkpoint, and a private portrait was taken for a consumer — both fixed before the release; the release court then found the BOM refusal. Run reports: `testcases/reports/2026-09-25_vo1-snapshot-core22.md`, `testcases/reports/2026-09-25_vo3-portrait-replace.md`, `testcases/reports/2026-09-25_vo4-epic-judge-fixes.md`, `testcases/reports/2026-09-26_rl2-remaining-seven-findings.md`.
