Shipped in [KAIF 2.8 — Noble KAIF](https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.8):

- **The rule, right after "Showing is an action".** `AGENT_GUIDE.md` → "Decisions the agent must NOT make alone — interviews": "A comparison, a sequence in time or a fork of outcomes is explained with a PICTURE" — COMPARISON (design vs build, before vs after) → the two frames side by side in one picture, labelled; SEQUENCE IN TIME (a race, a retry, a lifecycle) → a time line: events as dots, durations as bars, the user's action marked; FORK OF OUTCOMES → an outcome tree, each leaf: what the client shows · what the server did · the verdict by colour. Open it for the owner and write ONE line to it in the chat; the four-line scenario is its caption, never the whole explanation. `/interview` step 3a says the same for a question that hinges on such a thing.
- **The shipped skeleton `.kaif/_explain-page-template.html`** — your item 2: one self-contained page (no request leaves the machine; images are files next to the page or `data:` URIs, pictures are inline SVG), colour tokens on `:root` with a dark and a light scheme, a section per question with a status tag (decided · checking · bug), slots for the frame pair, the time line and the outcome tree, and the scenario as the caption. `cp .kaif/_explain-page-template.html <dir>/<what>.html`, fill, open.

What your side does: copy the skeleton next to your owner pages when the next comparison, race or fork comes up; its look is your owner's taste — change it freely.

Proof: sandbox suite `tools/sandbox/s22-contour-shipped.mjs` section A — the skeleton arrives with the install and carries the three blocks and the caption, with no network `src`/`href`, no `url(//…)`, no `<link>`, no `<script>`; red on the 2.7 core. The rule stands in the guide and in `/interview` (`grep -n "COMPARISON"` finds one line in each). Functional run: the origin built a mockup on the skeleton from real frames of the shipped question page — before a save, after the first answer, an old tab — and opened it for its owner on 2026-09-25 at 20:50. Reading those frames by eye found a defect no check had caught: on an old tab, in a window 980–1100 px wide, the status pill covered the "Open the new revision" button; it was fixed, with a geometric check added to `s22` section D. Run report: `testcases/reports/2026-09-25_ow9-explain-with-picture.md`.

Boundaries, said plainly: the look of the page is the owner's taste, and the origin owner's verdict on the mockup has not been given yet — he released 2.8 without that look and gives it in his next chat; this part closes on his word. Your item 3 — the contour's question page accepting images and SVG next to its decision buttons — is not in 2.8 (the contour's `--mockup` face shows one image with a comment field, not options), and neither is the optional lint over interview documents.

Item 3 of your ticket — images and SVG on the question page — and the optional linter did not ship in 2.8; both are in the list of the next version.
