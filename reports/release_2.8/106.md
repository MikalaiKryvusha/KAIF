Shipped in [KAIF 2.8 — Noble KAIF](https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.8):

- **The page at 1.7× the browser base, through `html { zoom }`** — the whole page, as Ctrl+Plus does, for your reason: raising `font-size` alone turns the radio circles into dots. `PAGE_SCALE = 1.7` in `.kaif/tools/contour/review.mjs`.
- **The Save button at 1.5×** — `SAVE_SCALE = 1.5`, its own zoom `1.5 / 1.7`, rendered as `.fab #save { zoom:0.882 }` (your `.88`).
- **The narrow-window breakpoint travels with the scale** — media queries do not see CSS zoom, so the 560-px rule is rendered at `560 × 1.7 = 952` px; your pair, `html{zoom:1.7}` with `@media (max-width:952px)`, is the same number. A comment in the code says the two constants move together.
- **One clause in `.kaif/INTERACTIVE_CONTOUR_SPEC.md` §4**, "Readable without the browser's zoom": 1.7× through `html { zoom }`, the Save button at 1.5×, every width breakpoint multiplied by the same scale — with a field owner's explicit word as its origin.

What your side does: nothing for the shipped contour. Your home contour (`tools/interview-page.mjs`) already carries the same pair; the update note says the same for any project's own contour page — the zoom and the breakpoint travel together.

Proof: `node .kaif/tools/contour/review.mjs --selftest` — three `#106` cases; a copy with `PAGE_SCALE = 1` reddens exactly those three. Sandbox suite `tools/sandbox/s22-contour-shipped.mjs` section D reads a REAL window — headless Edge, an app window of 1100×900 — and finds the page's computed zoom 1.7 and the button's 1.5; on the 2.7 core it reads `zoom {"h":1,"s":1}` and is red. Three frames of the page in that window were read by eye: text and radio circles large, the "Save decision" button top right and not covering the text. Run report: `testcases/reports/2026-09-25_ow11-page-scale-and-gate-once.md`.

Boundaries, said plainly: the numbers are constants of the shipped generator, not a setting in `.kaif/kaif.json` — your item 4's "the owner may change these" has no per-project switch in 2.8. Observed in Edge on Windows; other browsers were not checked. The size is your owner's word, taken as the default; the origin tested the numbers, not the taste.
