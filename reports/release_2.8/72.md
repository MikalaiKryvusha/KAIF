Shipped in [KAIF 2.8 — Noble KAIF](https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.8):

Your ticket named the missing branch exactly. The case is a declared rename where the old heading is gone and the new heading is already on disk. The "defect in KAIF itself" contour produces that state whenever the local fix of a naming ticket is the rename.

- **The pair binds in the third state.** For a declared pair whose old heading is absent and whose new heading is present, `update` now judges the disk section against the OLD template module, ignoring the heading line (`mergeModules` in `.kaif/kaif-core.mjs`). An untouched body is replaced by the new template module, with your fills folded in. An edited body keeps your text and gets a `merge-modules` item with the diff: "renamed IN ADVANCE on your disk … fold upstream's delta into your section by hand (nothing was inserted)". Nothing is inserted twice.
- **The log says what happened on disk.** The log no longer says "the section arrives as new". It prints `↻ renamed: <path> :: <old> → <new> (renamed in advance on disk — …)` with one of three outcomes: already current, the new template module replaced it, or edited with the upstream delta in the task. On a file merged without writes (i18n translated), the outcome says the delta is in the task and nothing was replaced. "The section arrives as new" is now printed only when both headings are absent and the new module really is inserted.
- **`update-verify` no longer depends only on what the task carried.** It checks on disk every section new in the release, using a list the release ships in its bundle meta (`sectionsNew`), and a missing one is red by name. The lines the task promised are judged per file. #92 has the details.

What you do: nothing for this hop. Your two modules were merged by hand on 2026-09-18, and 2.8 declares no renames of its own, so the 2.7 → 2.8 update has no pair to bind.

Proof:
- Sandbox suite `tools/sandbox/s27-rename-map.mjs`, case F ("renamed in advance"):
  - F1, untouched body: one heading, upstream's added line arrived, and the log says "renamed in advance on disk" and NOT "the section arrives as new".
  - F2, edited body: the delta goes into the task and nothing is inserted.
- Cases G and G2 check the log lines on a translated file.
- On the 2.7 core, `s27` is red on F1 ×2 · F2 ×2 · G · G2.
- The mutant probe `tools/sandbox/probes/up-mutants.mjs` runs 20 mutants of the update machinery, each red exactly on its named cases.
- Run reports: `testcases/reports/2026-09-26_up-update-loses-nothing.md`, `testcases/reports/2026-09-26_up6-judge-fixes.md`, `testcases/reports/2026-09-26_up7-rejudge-caveats.md`.

Limits:

1. The third state was reproduced with fixtures shaped after your ticket, not on a real tree.
2. Your second suggestion did not ship: `update-verify` does not compare every deployed module with the new template. It checks the sections new in the release and the lines the task promised. A changed module whose delta never entered the task is still not seen by `update-verify`.
3. The merge is run by the core that runs the pass. On the `update` route that is the core you have deployed, so the third-state binding applies to passes run by a 2.8 core: from your next update on, or right away on the loader route (`node KAIF-LOADER.mjs …`). A deployment still on 2.6 or older that takes the loader route to 2.8 gets the 2.7 pairs bound this way, because the rename map covers every version after the one deployed.
