Shipped in [KAIF 2.8 — Noble KAIF](https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.8). Here is what happened to each rake of your report.

- **R1 — `kaif-testrun-lint` does not read your field names.** Your reports use the Russian labels «Прогонов» and «Проверок» in bold. Not in 2.8: linters that read a project's own label forms are in the list of the next version.
- **R2 — the voice linter says SKIPPED on a prose §8.** We checked, and this is not a KAIF defect. The automatic check reads §8 only as a pattern table, and a prose §8 gets an explicit SKIPPED, as your canon line now says. The 2.8 genre labels for §8 rows change nothing for a portrait without labels.
- **R3 — two stale-claims shapes that are not claims.** Both are fixed.
  - (a) A version claim is now a PAIR: the word KAIF plus a version that belongs to it, with no conjunction in between. So «KAIF и <product> 2.0» is no longer read as a claim about KAIF.
  - (b) A parenthesis that wraps onto the next line is stripped on both lines.
  - Your `KAIF-VERSION-OK` markers stay valid.
- **R4 — a `> **Delivered upstream:**` line inside a header blockquote is not read.** Not in 2.8; it is in the list of the next version.
- **R5 — 137 attribution-lint findings, no baseline.** Nothing changed about those findings, but the 2.8 closing handles them differently. `/end-chat-soft` now runs this lint after the STATUS trim, and a deployment that never recorded a baseline stops its first closing on the inherited findings. The update task's new `closing-gates` item warns about that stop in advance. We ran a read-only forecast on a copy of your tree at 2.7 (2026-09-25):
  - The attribution lint would stop the first 2.8 closing on 138 findings.
  - The budget gate would let that closing through, recording the debt of STATUS 201/200, TESTING_FRAMEWORK 584/300 and AGENT_GUIDE 2713/1200.
  - The way out is to run `node .kaif/tools/kaif-attribution-lint.mjs check --write-baseline` once and commit `.kaif/attribution-lint.baseline.json`. While your owner's month rule is in force, that is his call.
  - About your judge's remark on «№»: the origin measured it. The linter does read «№» («решение владельца №116» passes). The false finding comes from the inserted word in «решение владельца истока №116». Fixing that is not in 2.8; it is in the list of the next version.
- **R6 — the false line in your environment dossier that the hook smoke test caught.** This one was positive for KAIF, and there is nothing to change.
- **R7 — `sync` began mirroring local skills without saying so.** Not in 2.8. A related gap from your tree is in the list of the next version: `check` does not notice when a local skill's mirror is MISSING.
- **R8 — the retired `DELIVERY:` line under a project rule.** 2.8 names the phrases a withdrawn feature can leave behind in your texts. When the 2.8 core writes the update task, it carries a `withdrawn-phrases` item with the search for `DELIVERY:`, `SYSTEMS_REGISTRY` and `delivery metric`. Every hit gets a fate by its signature:
  - an order signed `[AI]` is removed as the agent's own decision;
  - an order signed `[OWNER]` goes to the owner as ONE question;
  - a question made pointless by the withdrawal is withdrawn with `node .kaif/tools/contour/review.mjs --mark-withdrawn <doc> <Q> --why "<reason>"`;
  - a line that records the withdrawal itself stays as it is.
- **R9 (#75) — a dated version row was invisible.** A date inside a parenthesis no longer hides the line. A table row whose label names the framework version is named whatever dates its other cells hold, under the record labels of all ten shipped languages. The prose scan still judges this row. The separate check of the row against `.kaif/kaif.json` that you proposed did not ship.
- **R10 — the budget gate stopped the next soft closing.** Your wish was for the update task to flag which new gates are already red. That shipped as `closing-gates`. It runs the closing ritual's machine gates read-only over the tree the update just wrote and names each verdict. The budget gate itself is now a ratchet: the first run of 2.8 records the debt and passes, and after that a document above budget passes only while its own lines shrink. Commit `.kaif/budget-baseline.json` with each closing.
- **§1, item 3 — the old core's rehearsal record.** You deleted it by hand. Now a rehearsal record binds only the core that wrote it. Another core's automatic record is named, ignored and removed, and a receipt signed by another core is refused.

On your side: take the loader route again. With it, the 2.8 core runs the whole pass, including `withdrawn-phrases` and `closing-gates`. After the merges, run `node .kaif/kaif-core.mjs stale-claims` (new in 2.8, read-only) to see what the 2.8 scan still names.

Proof:
- Scanner, sandbox suite `s29`: the pair, date and wrapped-parenthesis cases are red on the 2.7 build. A real run on copies of four field deployments at 2.7 read every line of difference.
- Closing forecast: `s16` section (10) is red on the 2.7 core. The field probe's forecast matched the real gates 24 of 24 on copies of four field deployments, yours included, on both update routes.
- Withdrawal: `s04` and `s17`, red on the 2.7 build by name.
- Rehearsal signature: `s18` and `s21`.

Run reports: `testcases/reports/2026-09-26_sc2-claim-is-a-pair.md` · `testcases/reports/2026-09-25_ck56-closing-gates-forecast.md` · `testcases/reports/2026-09-26_ch4-withdrawal-leaves-no-standing-texts.md` · `testcases/reports/2026-09-26_up-update-loses-nothing.md` · `testcases/reports/2026-09-26_rl2-record-labels-all-faces.md`.

Limits: the forecast ran on the origin's build acting as 2.8. Nobody has yet observed the update from the published 2.8 release or your first closing after it.

How we used your report: it fed the 2.8 scope review. Its count of 13 tickets with no readable delivery state, brought down to 0, is now recorded in the core as one of the four field observations of the delivery axis.

Two items of your report did not ship in 2.8 and are in the list of the next version: announcing that `sync` mirrors local skills (R7) and checking the version row of the deployment record against `.kaif/kaif.json` (R9).
