Shipped in [KAIF 2.8 — Noble KAIF](https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.8). Here is what happened to each rake of your report and to the three comments under it.

- **R1 (#72) — a section you renamed yourself lost its upstream changes.** When you renamed a section IN ADVANCE to the heading the release declares, it now gets the upstream changes. An untouched body is replaced. An edited body gets the changes as an item in the task. The log says what happened on disk instead of «the section arrives as new». Against your near-miss: `update-verify` now checks on disk EVERY section that is new in the release, and fails naming any section that never arrived. The list of new sections travels in the bundle, so this works whatever core ran the update.
- **R2 (#73) — slots with `<` or `>` in the value.** A value like your `… -PackDir <pack>; …` is now recognised. The guard against junk is now exact: only a value that contains a known slot token is rejected. Skills that are "template + your values" stop coming back as manual items.
- **R3 (#80) — 88 old warnings at every closing.** `kaif-experience-lint check` folds them into ONE line, and `check --write-baseline` records that debt once. On a copy of your journal (122 entries; the project was only read), the run printed one line about the 88 entries; after one `--write-baseline` it printed zero warnings.
- **R4 — mistakes on the agent's side.** For (c), claims before evidence: the shipped guide now says "The moment is PROBED, never felt". It means `date '+%Y-%m-%d %H:%M %z'` in the SAME tool call as the write, and `/fable-judge` hunts any stamp written without such a probe. For (a) and (b), the shell stripping an escaping level: a heredoc variant of the rule "pass text through files" is not in 2.8; it is in the list of the next version. Your own two guards remain your project's.
- **R5 (#81) — "byte for byte" held only up to line endings.** The sandbox recipe in `/kaif-update` is now `git -c core.autocrlf=false archive HEAD | tar -x -C <tmpdir>`, which keeps the committed line endings unless a `.gitattributes` `eol` pins them. The skill says "byte for byte, up to line endings on a `core.autocrlf=true` tree". A rehearsal record is also signed by the core that wrote it, so another core's record is named and not applied.
- **R6 (#78) — the canon told the agent to hold the field report.** A field report is now a KAIF signal, delivered in the same move as it is written. It opens with an H1 and the line `**Delivered upstream:** NOT YET`, and on `tracking: origin` the agent runs `node .kaif/kaif-core.mjs report reports/KAIF_UPDATES/<file>.md`. `check` names an undelivered 2.8+ report. On an update FROM 2.7 the task still comes from the outgoing core, so the fresh core's `checkpoint field-report` prints the delivery command. The chat half (#82): before any question to the owner, however it is asked, the agent runs `node .kaif/tools/contour/review.mjs --search "<the question>"`. That search runs in Node, so no shell and no locale are involved, and it finds capital Cyrillic. An attestation «N hits · read: none» is refused.
- **Wish 4 — a declared form for a withdrawn question.** Shipped with your syntax: `review.mjs --mark-withdrawn <doc> <Q> --why "<reason>"`. It works on open questions only and never answers for the owner.
- **Your gates row: "37 of 37 skills are English" on every run.** That line is now printed only on a deployment that declares `i18n: translated`.
- **Your third comment.** (1) The STATUS move-out now names a second address next to the chronicle: `HOUSE_RULES.md` for standing rules and reference tables, with the command that creates it. (2) `/end-chat-soft` tells the agent to re-run `kaif-attribution-lint` after the trim, because the pointer lines are new prose. If the trim moved text into a directory outside the lint's default scope, it names that directory: `check <dir>`. Your note is the recorded basis of that paragraph.

On your side: take the loader route you used for 2.7; with it the 2.8 core runs the whole pass. On the `update` route your outgoing 2.7 core writes the task. Of the update fixes above, only `update-verify`'s new-section check and its per-file judgement then apply this time, and the fresh core's `checkpoint recheck` names the closing gates.

We ran a read-only forecast on a copy of your tree at 2.7 (2026-09-25):
- The budget gate is open, and the experience journal passes.
- The attribution lint, which the 2.8 closing runs, would stop the first closing on 11 inherited findings until you run `node .kaif/tools/kaif-attribution-lint.mjs check --write-baseline` once and commit the file.

Proof:
- Update, sandbox suites `s27` · `s21` · `s18`. They are red by name on the 2.7 build for the pre-renamed section, the bracketed value, the new-section check and the rehearsal signature. `s21` also runs `update` with the real 2.7 release core, the way the field does.
- Delivery: `s17` and `s03`, red on the build before the fix exactly on the new cases.
- Search: a real run on the origin in Git Bash with no locale set. `--search` found 473 lines, as many as `grep` under `LC_ALL=C.UTF-8`, and 30 more than a plain `grep -i`.
- Experience lint: selftest 75 cases and `s28` 37 checks, re-run 2026-09-26.
- Closing forecast: it matched the real gates 24 of 24 on copies of four field deployments, yours included, on both update routes.

Run reports: `testcases/reports/2026-09-26_up-update-loses-nothing.md` · `2026-09-26_up6-judge-fixes.md` · `2026-09-25_ch1-field-report-delivered.md` · `2026-09-26_rl2-field-report-delivery-and-wrapped-attributions.md` · `2026-09-25_ow5-archaeology-any-transport.md` · `2026-09-25_ck57b-experience-baseline.md` · `2026-09-25_ck56-closing-gates-forecast.md` · `2026-09-25_ck58-k17-trim-attribution.md`.

Limit: nobody has yet observed the update from the published 2.8 release or your first closing after it.

How we used your report: it fed the 2.8 scope review. Your sweep tool found 121 lines that never arrived, which showed the whole class. `update-verify` now covers the core of that class: every section new in a release. The delivery axis's silence on your four delivered tickets is recorded in the core as one of its four field observations.

The twelve "promised upstream line not found" advisories of your report are not addressed by 2.8; they are in the list of the next version.
