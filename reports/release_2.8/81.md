Shipped in [KAIF 2.8 — Noble KAIF](https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.8):

- **The recipe keeps the line endings.** `/kaif-update` step 2 and `KAIF_REFERENCE.md` §10.8 now export the sandbox copy with `git -c core.autocrlf=false archive HEAD | tar -x -C <tmpdir>`. The recipe says: "the flag keeps the committed line endings, unless a `.gitattributes` `eol` pins them; on a Windows tree with `core.autocrlf=true` a plain export rewrites them".
- **The claim says when it holds.** The sentence now reads: "in the field the live pass matched the sandbox byte for byte, up to line endings on a `core.autocrlf=true` tree (compare there with `git diff --ignore-cr-at-eol`)".
- **A rehearsal binds only the core that wrote it.** The same step now handles records from other cores:
  - The rehearsal record and the copy's receipt carry the fingerprint of the core that wrote them.
  - An automatic record written by another core is named, ignored and removed.
  - A `--rehearsal <receipt>` signed by another core is refused. On the loader route the loader refuses it before it writes the new core.
  - An unsigned receipt you name yourself (a pre-2.8 copy) is applied with a warning.

What you do: you can use the flag for your 2.8 rehearsal already, because the recipe is a command you run, not machinery. The fingerprint rule is enforced by the 2.8 core and loader: on the loader route now, and on the `update` route from your next update on.

Proof:
- The release's own functional runs exported copies of real field deployments with exactly this recipe (`testcases/reports/2026-09-26_up6-judge-fixes.md`, `testcases/reports/2026-09-26_sc2-claim-is-a-pair.md`).
- The release court examined the recipe sentence and found one case where the flag does not keep the endings: a `.gitattributes` `eol` setting. The recipe was narrowed to say so.
- The fingerprint rule:
  - `tools/sandbox/s18-update-symmetries.mjs` U3б/U3в: another core's record is not bound.
  - `tools/sandbox/s21-update-route.mjs` A2b · A5 · A5b · A6 · A6b:
    - an automatic record from another core is removed;
    - a named receipt is checked before anything is written, and on the loader route before the new core is written;
    - a receipt for another interval is refused.
  - `tools/sandbox/probes/up-mutants.mjs`, including a mutant of the loader, M17.
- Run reports: `testcases/reports/2026-09-26_up-update-loses-nothing.md` (UP4), `testcases/reports/2026-09-26_up7-rejudge-caveats.md`.

Limits:
- You offered the flag as a hypothesis for the origin's sandbox tests. No sandbox suite asserts that a copy made with the flag is byte-identical to the live pass. The recipe rests on the court's examination and on its use in the release's runs.
- The v1-manifest branch you read, a raw `fileSha` compared with `oldShas`, is unchanged in 2.8.
