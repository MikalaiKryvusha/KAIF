Shipped in [KAIF 2.8 — Noble KAIF](https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.8):

Your ticket proposed two mechanisms — a Stop-time check over the agent's last reply, and a checked line in the closing rituals. Neither shipped in that form. What shipped closes the same gap from the other side: the search no longer needs an `interviews/` document to run, and the canon now says a question in the chat passes it.

- **A pointed question in the chat passes the same search as an interview question.** `AGENT_GUIDE.md` → "Decisions the agent must NOT make alone — interviews": the one pointed question in the chat comes only after `node .kaif/tools/contour/review.mjs --search "<question>"` — "a question in ANY transport claims the matter is unsettled". `/interview` step 3d says the same for a question asked in the chat.
- **The search is a command, not a document form.** `--search` runs in Node over `interviews/ GOAL.md MASTER_PLAN.md plans/`, prints the hits by file and line and a ready attestation line — so a question typed into a reply can be checked in the same move it is written. (Its Git Bash half — a capital Cyrillic letter found without a locale — is origin issue #74.)
- **"Found, and nothing read" is refused** at the interview door: an attestation `N hits · read: none` with N > 0 is exit 3.
- **When the work waits on the owner's word, it is a call, not a chat line.** The same section: "When the work STOPS until the owner acts or answers … CALL the owner: `node .kaif/tools/contour/review.mjs --call "<what is needed>"` … a request left only in the chat is not delivered" (origin issues #95 · #98); `/fable-judge` hunts "Hands asked in the chat only".

What your side does: nothing to merge; the update note says "before a question to the owner in any form, run `--search`".

Proof: sandbox suite `tools/sandbox/s22-contour-shipped.mjs` section E on a deployed copy — `--search` with no document names a hit with a capital Cyrillic word by file and line and prints the attestation line; `read: none` beside hits is exit 3; both cases are red on the 2.7 core. The mutant probe `tools/sandbox/probes/ow5-archaeology-mutants.mjs` — two mutants, each red only on its own case. On the origin, in Git Bash without a locale, `--search` found 473 lines where a bare `grep -i` found 443. The new refusal was run read-only over the interviews of eight field deployments: 19 attestations, none refused. Run report: `testcases/reports/2026-09-25_ow5-archaeology-any-transport.md`.

Boundaries, said plainly. The chat rule is prose, and your own diagnosis stands: the reply never lands on disk and no repository tool sees it — so nothing in 2.8 notices, mechanically, a chat question that skipped `--search`. Not in 2.8: the Stop-time check that reads the last reply from the transcript — it is in the list of the next version, and it starts with a recon of the agent system's Stop-event contract (`transcript_path` in the event input) and a false-hit rate measured on the polygon, as your ticket asked before any door. The closing-ritual line "questions to the owner in this reply" is not in 2.8 either.

The cheaper half of your ticket — a line "questions to the owner in this reply" in the closing skills — did not ship in 2.8; it is in the list of the next version, next to the Stop-time check.
