Shipped in [KAIF 2.8 — Noble KAIF](https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.8). Here is what happened to each rake and each wish of your report.

- **R1 — an agent's rule signed as the owner's, around the retired `DELIVERY:` line.**
  - (b), the policy item never asked who ordered the line locally. 2.8 names the phrases a withdrawn feature can leave in your texts. The update task written by the 2.8 core carries a `withdrawn-phrases` item with the search for `DELIVERY:`, `SYSTEMS_REGISTRY` and `delivery metric`. Every hit gets a fate by its SIGNATURE: an `[AI]` order is removed as the agent's own decision, and an `[OWNER]` one goes to the owner as ONE question. That question, like any other, first passes the search for a prior answer (R10 below), and 2.8 refuses an attestation that found hits but read none. Reading the hits is what caught your case. Checking that an `[OWNER]` signature is true is not a separate step.
  - (a), the attribution lint accepts an interview address without reading what the interview says. Not in 2.8.
- **R2 — the raw bundle is not the deployed form of a file.** You asked for an oracle, and it is here. `node .kaif/kaif-core.mjs diff --source <x> --render <file>` prints a file exactly as `install` of `<x>` writes it, in YOUR language and with your filled-in values. Render the old source and the new one, then compare both with the disk.
- **R3 — the undelivered-signal axis on its real path.** Your three true positives are now recorded in the core, with your report as one of the four field observations behind the axis. Your fourth state shipped: `**Delivered upstream:** resolved in origin <version>` is silent in `check` when the version is given and is not newer than the deployment. Your tickets 07–12 can take that line, naming the release that carries their substance. The `DONE` tag in a file name is still not read; the delivery line is.
- **R4 — the release's own text trips its own stale-claims scan.** The build now runs the core's own scan over the shipped templates, with the next version as the target, and refuses on any line the scan names. The three template lines whose attribution wraps onto the next line were closed on one line, so even the outgoing 2.7 core's scan stays silent on the `update` route.
- **R5 — `KAIF-VERSION-OK` in a table.** The marker form for a table row is now described: the marker goes INSIDE the row, in any cell. The adjacency rule for prose is unchanged.
- **R6 — the misleading wording «arrives as new».** Fixed. For a tree that renamed a heading first, the log says what happened: an untouched body is replaced, an edited body gets the upstream changes as an item in the task.
- **R7 — `--rehearsal` works on the bootstrap route.** This was positive, and there is nothing to change.
- **R8 — 246 `no-class` warnings.** `kaif-experience-lint check` folds the pre-class entries into ONE line, and `check --write-baseline` records that debt once.
- **R9 — the sandbox recipe and a delete-deny rule.** We checked, and this is not a KAIF defect. The shipped recipe contains no deletion: it exports into a fresh directory. The `rm -rf` came from the session.
- **R10 (#74) — a Cyrillic grep printed a false «0 hits».** The search is now built into the tool: `node .kaif/tools/contour/review.mjs --search "<the question>"` runs in Node, so no shell or locale is involved, and it finds capital Cyrillic. The grep it prints carries `LC_ALL=C.UTF-8`. An attestation «N hits · read: none» is refused. A question typed straight into the chat passes the same search.
- **R11 — LF lines written into a CRLF `.gitignore`.** Not in 2.8; it is in the list of the next version. So is the team-constitution axis your judge found blind to §4 and §9.
- **Signal 8 — the judge in a clean context.** `/kaif-update` now requires two things: a public correction to a delivered ticket goes out only after the judge's finding behind it is re-measured by a command, and the update judge runs in a clean context.
- **Wish 4.4 — the smaller items.** "Arrives as new", `no-class`, the table-cell marker and "who ordered it" are covered above. The note in the attribution lint's header that an address is accepted unverified is not in 2.8.

**Your budget gate, which you did not run** (STATUS was about 2000 lines against 200): see your #84. The gate is now a ratchet, an owner's archive can be declared, and the update task has a `closing-gates` item that forecasts where the first closing stops.

We ran a read-only forecast on a copy of your tree at 2.7 (2026-09-25):
- The budget gate lets the first 2.8 closing through and records `GOAL.md` 2036/300 as debt.
- The attribution lint, which the 2.8 closing now runs after the STATUS trim, would stop that closing on 91 inherited findings. The way out is to run `node .kaif/tools/kaif-attribution-lint.mjs check --write-baseline` once and commit the file.
- You decided not to adopt that baseline, as an `[AI]` decision under your owner's moratorium. The first 2.8 closing will therefore put the choice in front of you again.

On your side: the loader route lets the 2.8 core run the whole pass, including `withdrawn-phrases` and `closing-gates`.

Proof:
- Template scan: build guard 5m. Its selftest gives four answers. A probe of the 2.7 core's scan rule went from red on 3 lines to green.
- Oracle: rendered output equals a fresh install byte for byte (`s21`).
- Withdrawal: `s04` and `s17`, red on the 2.7 build by name.
- Search: `--search` found 473 lines, the same as `grep` under `LC_ALL=C.UTF-8` and 30 more than a plain `grep -i`, all of them capitals.
- Closing forecast: it matched the real gates 24 of 24 on copies of four field deployments, yours included, on both update routes.

Run reports: `testcases/reports/2026-09-26_sc3-templates-silent-under-own-scan.md` · `2026-09-26_rl2-field-report-delivery-and-wrapped-attributions.md` · `2026-09-26_up6-judge-fixes.md` · `2026-09-26_ch4-withdrawal-leaves-no-standing-texts.md` · `2026-09-25_ow5-archaeology-any-transport.md` · `2026-09-25_ck56-closing-gates-forecast.md`.

Limit: nobody has yet observed the update from the published 2.8 release or your first closing after it.

How we used your report: it fed the 2.8 scope review. Your five judge passes refuted only the author's prose, never the update's mechanics, and two of your public corrections had to be corrected again. That record is behind the new `/kaif-update` rule: a clean-context judge, and a re-measure before any public correction.

Wish 4.4 of your report (R1 a) — the attribution linter saying in its header that an address is accepted without being checked — did not ship in 2.8; it is in the list of the next version.
