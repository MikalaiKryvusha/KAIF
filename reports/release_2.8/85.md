Shipped in [KAIF 2.8 — Noble KAIF](https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.8). Points 1 and 2 of your proposal shipped in a different form, and point 3 shipped as a file. The 2.8 default comes from your own fix: you moved the project's sections into `HOUSE_RULES.md`, and that is why your guide passed the 2.7 gate with zero room.

**1. Room for a translation, still counted in lines.** The unit did not change: there is no language factor and no character count. A factor would need a measured number for every language pack, and there is none for most of them. Characters would add a second unit to all nine budgets. Instead the room now lives in the template:
- The build refuses any template of the re-read core longer than 90 % of its budget (guard 5j at the origin).
- The 2.8 `AGENT_GUIDE.md` template is 1080 lines (2.7: 1200), and `TESTING_FRAMEWORK.md` is 270 (2.7: 300). That leaves 120 and 30 lines for a translation's growth and your own changes to the method.
- At the origin, seven English-to-Russian pairs of its own documents came out between 1 % shorter and 4 % longer in lines.

**2. The message now says it.** For a file translated wholesale, the budget warning reads: "…translated wholesale — arrived canon cannot be told from your own lines, so every line counts as yours; the shipped template is N lines, which leaves ≈ M for your translation's growth and your own adaptation — local sections belong in HOUSE_RULES.md". The deploy manifest now records each template's length. A manifest written before 2.8 keeps the old wording rather than print an invented number. Every move-out address also names its file and the command that creates it: `HOUSE_RULES.md (no file yet: cp .kaif/_house-rules-template.md HOUSE_RULES.md)`.

**3. The house-rules file has a name and a skeleton.**
- `.kaif/_house-rules-template.md` ships with the release; you copy it on first use. `/resume` reads `HOUSE_RULES.md` when it exists.
- Your own file belongs to the project, and an update leaves it byte for byte. Where its layout is your own, `/fix-vision` adds a section "The owner's standing rules" rather than reshaping it.
- The guide's modules that a project fills in were cut down to their principle plus one pointer line. "Test harness" points to `HOUSE_RULES.md` → "Stands, environments and devices". "Tools" points to "Tools of this project". The environment-dossier table points to "Environment dossier". "Push / GitHub authentication" points to "Routes, recipes and conventions". "Goal of the project" now points to `GOAL.md`, and "Architecture — the map" points to the two map documents.

Also new in the gate: it is now a ratchet. A document above budget passes while its own lines shrink. Commit `.kaif/budget-baseline.json` with each closing.

**On your side:**
- No section was renamed, so the update replaces module bodies only.
- Because your deployment is translated wholesale, re-render the changed modules from the template instead of keeping the longer translation. The lines that disappeared are history, not rules.
- The update keeps the filled modules your guide still carries and ships the template change in the task. Move what belongs there into `HOUSE_RULES.md` and leave the pointer line.

We ran a read-only forecast on a copy of your tree at 2.7 (2026-09-25):
- The budget gate is open.
- The 2.8 closing now also runs the attribution lint after the STATUS trim. It would stop your first closing on 151 inherited findings until you record them once: `node .kaif/tools/kaif-attribution-lint.mjs check --write-baseline`, then commit `.kaif/attribution-lint.baseline.json`. The task item `closing-gates` names this ahead of time.

Not in 2.8: the printed list of text windows around each mention of a moved section, from the "hidden cost" part of your ticket. Pointers are still re-aimed by hand.

Proof:
- Sandbox suite `s16`: 83 checks green (re-run 2026-09-26). The translated-file assert checks two things: the template length in the warning equals the deployed file before translation, and template plus room equals the budget. It is red on the 2.7 core, and mutant M16 goes red on exactly that assert.
- Guard 5j goes red on a mutant with a 1200-line guide.
- The origin's obligation inventory against v2.7, re-run 2026-09-26: `lost 0`.

Run reports: `testcases/reports/2026-09-25_ck54-translated-measure.md` · `testcases/reports/2026-09-25_ck51-template-ceiling.md` · `testcases/reports/2026-09-25_ck50a-project-facts-move.md` · `testcases/reports/2026-09-25_ck56-closing-gates-forecast.md`.

Limit: nobody has yet observed the first closing of a translated deployment after the 2.8 update.

Printing the text around each pointer to a moved section did not ship in 2.8; it is in the list of the next version.
