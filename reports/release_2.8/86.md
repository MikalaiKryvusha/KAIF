Shipped in [KAIF 2.8 — Noble KAIF](https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.8):

- **The queue names the owner's debt first, by name, with no date cut-off.** `node .kaif/tools/contour/review.mjs --queue --list` opens with `OWNER DECISIONS AWAIT APPLICATION — N: every question answered, the status not closed. Apply them FIRST, ahead of the plan (#86), then close the status.` — each document with "answered N d ago". It reads your form too: the words "awaiting application" · «ждут внесения» · «ждёт внесения» anywhere in the status block, not only a canonical status line. Queue documents older than the stale threshold (14 days by default, `KAIF_STALE_QUEUE_DAYS`) are named one by one — never folded into a counter.
- **Canon: `BUG_FIXING_FRAMEWORK.md` → "The owner's debt comes first — a bug he flagged, a decision he answered"** — your four steps: debt is job number one, ahead of the plan and the metric, and the task in flight goes to its nearest clean cut (a green commit); "apply it together with X" never parks the decision in X's queue; filing a bug is fixing it in the same move or writing `Fixing: <this session | after <commit or plan step>>`; and where the debt is seen.
- **`/what-next`** carries the line `Owner debt: <interview #NNN QN answered, awaiting application · bugs/NN the owner flagged — or "none">`; when it names any, row 1 closes one of them (`closes: interview #NNN QN · bugs/NN`). `node .kaif/tools/kaif-ranking-lint.mjs check <draft>` reddens a draft without the line (`no-owner-debt`) or whose row 1 closes none of the debt (`owner-debt-not-first`). The rule of origin issue #53 — a fresh remark is ranked by the metric — is untouched: a debt is not a fresh word.
- **`/resume` step 2** puts the owner's debt above everything else.

What your side does: run the queue list at your next entry and apply what it names first (the update note says exactly that).

Proof: sandbox suite `tools/sandbox/s22-contour-shipped.mjs` section B on a deployed copy — an answer 11 days old under an open status is the FIRST line, «РЕШЕНИЯ ВЛАДЕЛЬЦА ЖДУТ ВНЕСЕНИЯ — 1», with the document and «отвечено 11 дн. назад»; red on the 2.7 core. The mutant probe `tools/sandbox/probes/ow3-contour-mutants.mjs` — "the matcher finds nothing", "stale silent again", "the status-block words not read" — each red only on its own case. `tools/sandbox/s23-ranking-lint.mjs` — the two new rules, red on the 2.7 core. Functional run on a read-only git clone of your deployment (nothing was written to it). The first build of the view was blind to your form — «✅ ОТВЕЧЕНО» on the first line, «ЖДУТ ВНЕСЕНИЯ» on a continuation line, questions «А1/Б1» — and the fix (the words anywhere in the status block) was proven on your own history: at the state of your interviews just before the decision was applied, the view lists 7 decisions awaiting application, your #066 among them, "answered 17 d ago" (the age counts to the day of the run, 2026-09-25). On your state of 2026-09-25 it names 3: #068 and #097, whose own status blocks say they await application, and #064 — a false alarm, because its status block quotes the old «…ЖДУТ ВНЕСЕНИЯ» line as history; that class is named, not silenced, and costs one glance. The same clone also crashed the queue command with the `TypeError` of origin issue #100 — your own contour keeps `queue.json` under the same name; 2.8 reads it as "the project keeps its own queue" and never writes into it. Run report: `testcases/reports/2026-09-25_ow3-ow7-owner-debt-foreign-queue.md`.

Boundaries, said plainly: the shipped view sees what the documents say about themselves — a document whose status is closed and whose status block does not say it awaits application is not in that section; the `/interview` rule that the status closes last, after the application, is what protects it. The queue command reads interview documents; a bug the owner flagged reaches `/what-next` through the `Owner debt:` line the agent writes.
