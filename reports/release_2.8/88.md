Shipped in [KAIF 2.8 — Noble KAIF](https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.8):

- **A branch for the thrown TASK.** `AGENT_GUIDE.md` gains the section "The owner's word mid-turn — the system signs its author", right after "A leading skill word is an order". A message the agent system signs as the owner's is answered by its kind, as text, before the next tool call — and one of the kinds is yours: "switch to Y" → first a `PARKED:` line (where the task stands, how to resume) at the top of `STATUS.md` → "Where to continue" — the carrier that survives compaction — then Y. A note keeps the drive-by rule you described (file it, confirm in one line, return).
- **The way back is a command's first step.** `/kaif-go`, step 1, item 0: a `PARKED:` line is read FIRST — before the `/pause` note, the active plan and the rest of `STATUS.md` — "once the work that displaced it is done, it is next".
- **The loops stop the same way.** `/dayloop`, `/nightloop` and `/autoloop`: a message delivered mid-turn is the same word — "stop" ends the loop in this turn; "switch" writes a `PARKED:` line in `STATUS.md` first.
- **Your judge hunt, both halves.** `/fable-judge` → "Parked and dropped": an owner's "switch to Y" executed without a `PARKED:` line written BEFORE the first step of Y — or a parked task that no later step resumed or handed over.
- **A mechanical reminder (Claude Code).** The new optional hook `.kaif/hooks/pretool-owner-word.mjs` (event `PreToolUse`) refuses ONE tool call after an owner's mid-turn message that has no text answer yet; its reason quotes the owner's words and names the kinds — "switch to Y → a PARKED: line first, then Y". It never stops the work: one refusal per message.

What your side does: the rule arrives with the update of `AGENT_GUIDE.md` and the skills. The hook is part of the opt-in refresh-hooks module — merge the `PreToolUse` entry of `.kaif/hooks/settings-fragment.json` into `.claude/settings.json` by your owner's word.

Proof: the hooks — sandbox suite `tools/sandbox/s14-refresh-hooks.mjs` (red on the 2.7 core) and the mutant probe `tools/sandbox/probes/hooks-mutants.mjs`, including "the refusal repeated forever" — the stop an owner rejected. The gate was observed on the origin owner's real path on 2026-09-26, 07:53–09:25 +03:00: eight messages typed mid-turn; six met a gated call (one was not yet in the transcript, one was covered by a newer message), seven calls refused in six rounds (one round of two parallel calls), the next call passed 3.9–8.1 s after five rounds and 27.7 s after one, and no message was refused twice. The switch branch itself is checked as text: the guide, the three loops, `/kaif-go` and the judge carry it. Run reports: `testcases/reports/2026-09-25_ow2-owner-word-mid-turn.md`, `testcases/reports/2026-09-25_ow11-page-scale-and-gate-once.md`, `testcases/reports/2026-09-26_rl2-remaining-seven-findings.md`.

Boundaries, said plainly. A live "switch to …" typed by an owner mid-turn after the rule has NOT been observed yet — the origin's owner types one at the start of the release chat, and this part closes on his word. The `PARKED:` line asks for two things — where the task stands and how to resume; your four fields (next step · uncommitted · unverified) are not separate required fields. Nothing machine-checks that the line was written before the first step of Y — the hook checks only that the owner got a text answer; the judge hunt reads the record. The sentence your ticket quoted in the older drive-by paragraph of the guide is aligned too: it now reads "an explicit 'switch to this' → the `PARKED:` line first, then switch", so the rule holds for any owner's "switch", not only a mid-turn one.
