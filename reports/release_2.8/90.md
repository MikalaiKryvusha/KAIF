Shipped in [KAIF 2.8 — Noble KAIF](https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.8): the missing lookup step, next to the one for lessons, with the same standard of proof.

- **Checklist step 2** of `AGENT_GUIDE.md` is now "Recall experience & own work". Suppose a task falls on a surface the project already worked on: a device, a route, a stand, a recipe. Before acting, the agent searches its own work (the house-rules file, `researches/`, the project's tools). Then it cites what it found, or writes "no own work found". It is a citation, not a claim, as with the EXPERIENCE step (your remedy 1).
- **The context router** gains the row "A surface the project already touched (a device, a route, a stand, a recipe) → the house-rules file (`HOUSE_RULES.md`, if the project has one) and `researches/` first — cite your own work, or write 'no own work found'".
- **A place where the work shows up.** The shipped skeleton `.kaif/_house-rules-template.md` has a §5 "Routes, recipes and conventions — the index of own work". It lists step files, device routes, recorded recipes and style measurements of the project's own texts, so conventions count as well as routes (your remedy 4). A task on a surface listed there starts by opening the entry. `/resume` reads `HOUSE_RULES.md` at the start of every session when the file exists.
- **`/fable-judge` hunts "Re-derived own work".** The case: a session worked out again a route or recipe that was already in the house-rules file, `researches/` or the project's tools.

On your side: list your `android/paths/` directory, your measured tracker-text conventions and similar work in §5 of your `HOUSE_RULES.md`. Your update report (#93) says the project already keeps that file. An update leaves it byte for byte, so add the section by hand.

Two of your remedies are outside KAIF, and nothing about them shipped. Enforcement inside `android-qa` / `web-qa` (remedy 2) belongs to your project's own skills; KAIF ships no such skills. A path runner that announces recorded routes (remedy 3) would be a tool of your project. Both can point to checklist step 2 as the canon rule behind them.

Proof:
- The checklist step and the router row appear in both layers of the origin (`grep -n "own work"`).
- The hunt appears in both copies of `/fable-judge`, which are byte-equal.
- The origin's obligation inventory against v2.7 gives `lost 0`.
- The epic's judge re-ran these checks on its own.

Run report: `testcases/reports/2026-09-24_ck4-owner-rule-and-own-work.md`.

Limit: this is a canon step and a judge's hunt, not a machine gate. Nothing stops an agent that skips the step. A fresh field agent citing a recorded route before its first tap is your ticket's acceptance test, and nobody has observed it on a field deployment yet.
