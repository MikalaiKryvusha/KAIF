Shipped in [KAIF 2.8 — Noble KAIF](https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.8). Your §2 is why this release's canon got lighter, and each rake of the report was taken on its own. Here they are in your order.

**§2.4 — the guide, point by point.**
1. **Template under its budget, with room.** The `AGENT_GUIDE.md` template is now 1080 lines (v2.7: 1200), and `TESTING_FRAMEWORK.md` is 270 (v2.7: 300). The build refuses any template of the re-read core longer than 90 % of its budget. It is not the ≤ 600 lines you proposed. Halving the guide carried a higher risk of losing an obligation than the 4 % you measured, and the KAIF owner set no length target, only the direction: down. The cut follows your method. A rule stays as a step, a command or a checkbox, with one clause of "why". Its birth certificate moved to the second home you named: `.kaif/KAIF_REFERENCE.md` §17 "Why the canon says so", with one entry per canon section, keyed "`<file>` → <heading verbatim>". The router row "Changing or dropping a rule of the canon" points there.
2. **Tier 4 as a file.** `.kaif/_house-rules-template.md` ships, with the sections taken from the three house-rules files the field had already built, yours among them. It comes with a router row and a conditional `/resume` bullet. The project-specific parts of the guide moved into it: the test-harness table, tools, the environment dossier and the push recipe. There is no `check` presence axis, by design. The file is copied on first use, so a project that does not need it carries no empty skeleton, and `check` does not demand it.
3. **A water lint:** not in 2.8.
4. **A budget a translated deployment can reach.** You get the room from point 1, plus a warning that names it: "the shipped template is N lines, which leaves ≈ M for your translation's growth and your own adaptation — local sections belong in HOUSE_RULES.md". The gate is also a ratchet now: a document above budget passes while it shrinks.
5. **Three checks for a compression.** The origin cut its own templates under all three:
   - an obligation inventory keyed on the WHOLE clause, not the command inside it. That is a direct answer to your "the mechanical inventory was green while these were missing";
   - a meaning audit by a clean instance;
   - the colon meter against the previous version (your R6).

   On top of that, fresh agents ran three fixture tasks on the 2.8 guide and on the 2.7 guide, and no required step was lost. The inventory is an origin tool and does not ship to deployments.
6. **The release-note habit:** yes. The story goes to the notes and to §17, and the rule keeps one clause.

**On your side:**
- No section was renamed, so the update replaces module bodies only.
- Your deployment is translated wholesale: re-render the changed modules from the template instead of keeping the longer translation. The lines that vanished are history, not rules.
- Your `HOUSE_RULES.md` belongs to the project, and the update leaves it byte for byte. Where its layout is your own, `/fix-vision` adds a section "The owner's standing rules" rather than reshaping it.

**The rakes.**
- **R1 (#77): the stale-claims scan claimed a clean tree.** There is now one tree walker, shared by the core and the six tool modules that walk the tree. It covers the stale-claims scan and the anonymity walk. The file list is what `git ls-files --cached --others --exclude-standard` returns. A broken link is skipped and named (`walk: skipped N unreadable path(s) — …`), never a crash. A walk that could not read a directory fails loudly, and the stale-claims item then says the scan could NOT see the whole tree instead of «no lines found». `kaif-provenance check` no longer dies on the link. New read-only command: `node .kaif/kaif-core.mjs stale-claims`.
- **R2 (#91 · #75): the scan missed the version pin that mattered.** A script pin is now recognised in the code's own vocabulary: `const EXPECTED_VERSION = '2.4';`, `FRAMEWORK_VERSION`, `KAIF_REQUIRED`. A dated deployment-record row is named too. A date inside a parenthesis no longer hides the line, and a table row whose label names the framework version is named whatever dates its other cells hold. This works for the record labels of all ten shipped languages.
- **R3 (#92): an offered module became invisible.** A module that the previous update offered and nobody merged is offered again from its receipt, never read as your deletion. `update-verify` checks every section new in the release on disk; the list travels in the bundle, so this works whatever core ran the update. A file counts as «translated» by its own text, not by the deployment flag, so your two English files are checked like any other.
- **R4: the log reported writes that never happened.** On a file merged without a write, the rename outcome now reads "upstream delta in the task (i18n: translated) — nothing replaced on disk", not "(replaced)".
- **R5: the voice linter could not read your portrait.** `voicePortrait` in `.kaif/kaif.json` is read first; that was already the case in 2.7. A §8 written as measurement tables is SKIPPED by design, because the linter reads the four-column pattern table the skeleton shows. Naming which portrait was loaded on the OK line is not in 2.8; it is in the list of the next version.
- **R6: the compression turned into a telegraph.** The colon meter is now part of the origin's own inventory (point 5 above).
- **R7: the resume hook missed «выполни resume».** The leading-word hook now fires on an imperative before `resume` and stays silent on the Russian noun used as a heading.
- **R8: the stale `updated` field.** Not in 2.8. The core still records updates only in `history`.
- **R9: clock stamps ran ahead.** The shipped guide now has the rule "The moment is PROBED, never felt": `date '+%Y-%m-%d %H:%M %z'` (PowerShell: `Get-Date -Format 'yyyy-MM-dd HH:mm zzz'`) in the SAME tool call as the write, and a decision about a named hour reads that probe too. `/fable-judge` hunts any stamp written without a probe in the same call. A stamp-checking commit hook like yours is not in 2.8.
- **Your negative results.** The delivery axis's observation on its real path is now recorded in the core, with your report as one of the four field reports behind it.

Proof:
- The obligation inventory against v2.7, re-run 2026-09-26: `kept 310 · moved 0 · changed 37 · declared 63 · moved-out 0 · lost 0 · new 34`. The build's template guard goes red on a 1200-line guide.
- Scanners: sandbox suite `s29`, red on the 2.7 build, plus a real run on copies of four field deployments.
- Update: sandbox suites `s27`, `s21` and `s18`, all three red by name on the 2.7 build.
- Hook: `s14` +7, red on the 2.7 build.
- The release court on the canon epic returned VERIFIED WITH CAVEATS, with 0 refuted.

Run reports: `testcases/reports/2026-09-24_ck2-canon-inventory.md` · `2026-09-25_ck6-guide-functional-run.md` · `2026-09-25_ck50a-project-facts-move.md` · `2026-09-26_sc1-one-safe-walker.md` · `2026-09-26_sc2-claim-is-a-pair.md` · `2026-09-26_up-update-loses-nothing.md` · `2026-09-26_up6-judge-fixes.md` · `2026-09-25_ow2-owner-word-mid-turn.md` · `2026-09-26_ch3-time-from-the-clock.md`.

Limits: every run was on Windows 11 with Node v24. Your macOS path has not been observed.

How we used your report: it fed the 2.8 scope review. Its §2 became the first acceptance criterion of the version, and the plan quotes your owner's word about the cosmonaut's manual.

Two items of your report did not ship in 2.8 and are in the list of the next version: the "water" linter (§2.4, item 3) and the unmaintained `updated` field of the marker (R8).
