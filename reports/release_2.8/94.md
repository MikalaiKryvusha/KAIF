Not in KAIF 2.8 — Noble KAIF; in the list of the next version:

- **What was measured.** The 2.8 scope recon (2026-09-24, `researches/32_kaif_2.8_scope_recon.md`) reproduced the ticket on copies of the hooks with a forged event: a 23-minute-old marker and `cwd` = a subdirectory → "no refresh witness found this session"; the STATUS guard from the subdirectory → silent, from the root → `block` (`prompt-refresh-timer.mjs` lines 60/65/68, `stop-status-guard.mjs` lines 37/47–48). Beyond the ticket: the relative path `.kaif/refresh-marker.json` stands in the order text of three hooks — `prompt-refresh-timer.mjs`, `prompt-resume-word.mjs`, `session-start-refresh.mjs` — and the polygon suite s14 feeds the hooks only the root. Verdict: CONFIRMED. The origin had the same class on its own open list (hooks taking state from an unchecked input), and your fix — the root from the hook's own location through `import.meta.url` — was found right for the hooks of both layers.
- **Why it is not in 2.8.** The KAIF owner was offered two cuts of the version: five pieces of work, or the same five plus two small ones — this hook root and the red-from-the-apparatus class of #87 — about half a day later. He took the five (2026-09-24, `interviews/interview_034_kaif_2.8_scope.md`, Q1).
- **Where it lives now.** Item 1 of the next version's list, `ideas/31_kaif_2.9_scope.md`, "Hooks know the project root": the root instead of the event's `cwd`, the absolute path in the text of the three orders, the root through `import.meta.url` — together with the origin's own remaining hook findings, among them nine places that read external JSON without stripping a BOM. The recon sized it at about half a working session. The list is not worked until the owner opens that version, and the opening starts by re-measuring each item against the code of that day.

On 2.8 the two hooks still resolve their files from the event's `cwd` (`.kaif/hooks/prompt-refresh-timer.mjs`, `.kaif/hooks/stop-status-guard.mjs`), so the workaround your ticket describes stays needed after the update: keep the session's directory at the project root, and before obeying the timer's order check the age of `<root>/.kaif/refresh-marker.json`. This ticket stays open; the version that ships the fix answers it and closes it.
