Shipped in [KAIF 2.8 — Noble KAIF](https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.8):

Your ticket proposed three changes; the first two shipped, the third did not.

1. **The canon rule.** `AGENT_GUIDE.md` → "Decisions the agent must NOT make alone — interviews": "When the work STOPS until the owner acts or answers — a password, a cable, a device to unlock, a one-line answer — CALL the owner: `node .kaif/tools/contour/review.mjs --call "<what is needed>"` (sound → banner → voice, naming the calling session); a request left only in the chat is not delivered: the owner does not watch the chat while you work." Your update task also shows it as a policy change, in its section of decisions for the owner.
2. **The call without a page.** `--call "<what is needed>" [--dry-run]` is a door of the shipped contour and the same signal its page uses — the console line `CALL · <session>:`, the sound, the voice; quiet hours (I6) still hold, and `--dry-run` prints the phrase with no sound. `/owner-reviews` I28b: a request for the owner's hands or a quick answer outside a page goes by this call, never as a line left in the chat. I5 is unchanged — it governs the page's own call.
3. **A judge hunt, not a hook.** `/fable-judge` → "Hands asked in the chat only": the work stopped until the owner acted or answered, and the request stood only as a line in the chat — no call ran for it (`review.mjs --call`, or the project's own call). Evidence: the turn that ends waiting on the owner, and no call line in it. Not in 2.8: the Stop/turn-end hook that would flag such a turn mechanically — it is in the list of the next version, after a recon of the agent system's Stop-event contract and a false-hit rate on the polygon.

What your side does: when the work stops until your owner acts or answers, call. Your project's own `npm run signal` stays legal — the hunt names "the project's own call" — and `--call` is there for projects without one. With several workspaces the call also names the calling one (origin issue #98).

Proof: `node .kaif/tools/contour/review.mjs --selftest` covers `--call --dry-run`. Sandbox suite `tools/sandbox/s22-contour-shipped.mjs` section F calls with `--dry-run` from a deployed main copy and from a linked workspace (two different introductions) and from a single-workspace deployment (no session name, no sound) — all red on the 2.7 core, where `--call` is an unknown flag. The mutant probe `tools/sandbox/probes/ow4-call-mutants.mjs` — three mutants, each red only on its own case. Functional run on the origin: `CALL · main (dry run, no sound): Криник, это мейн. Нужен пароль от тестового телефона`. Run report: `testcases/reports/2026-09-25_ow4-call-names-session.md`.

Boundaries, said plainly. The run was dry — no live voice call sounded; how the phrase sounds is judged by an owner's ear at the first real call. Your platform was not observed: on macOS the contour's sound is the terminal bell and its voice is the system `say` with its default voice — that path picks no voice by language (a richer engine is reached through `KAIF_VOICE_TOOL`, `.kaif/INTERACTIVE_CONTOUR_SPEC.md`) — and none of it was run on the origin (Windows); your 2.8 update report is where it gets confirmed. The rule is scoped to what the work waits on ("when the work STOPS until the owner acts or answers"); it does not say that every request to the owner goes by a call, as your item 1 proposed.

On macOS the contour calls `say` with its default voice and does not pick a voice by language; that is not verified on a Mac and is in the list of the next version.
