Shipped in [KAIF 2.8 — Noble KAIF](https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.8):

- **The lines the closing rituals owe the owner are written in his language.** `/end-chat-soft` Step 5 now reads: "Answer it in the report, in the owner's language (`.kaif/kaif.json` → `language`; … the owner reads this line, no machine does); the English form is: `Standing falsehood: none` …". The English text is now a pattern, not a verbatim demand. `/end-chat-force` says the same of its standing-falsehood line and of its ceremonies-debt line.
- **The guide states the rule once.** In `AGENT_GUIDE.md`, the "Languages" table now covers every chat report to the owner, "with the lines a skill asks for by name in it": they are written in the owner's language. English stays only for "the keys a machine or the judge greps in a document": `FORK:` · `AUTH:` · `INTENT:` · `TWINS:` · `PENDING:` · `BOUNDARY:`.
- **The judge accepts both forms.** `/fable-judge`'s standing-falsehood hunt accepts the line "in the English form `Standing falsehood:` or in the owner's language, both legal (origin issue #97)".

What you do: on a translated deployment, the guide's new table row arrives as a module diff in the task, because nothing is applied to a translated file; merge it in your own words. The two closing skills come through the ordinary merge.

Proof:
- No code reads the line: `grep "Standing falsehood" framework --include=*.mjs` found 0 before the work began. So the check is the text itself.
- A fresh `--lang ru` install from the release build was read: `language: ru`, the rule in `/end-chat-soft` and in both lines of `/end-chat-force`, and the table row in the guide. The sandbox suite ran green.
- The epic's judge found that `BOUNDARY:`, the new opening line of a close on a named hour (origin issue #96), was missing from the list of machine keys. It was added.
- Run reports: `testcases/reports/2026-09-26_ch2-owner-language-lines.md`, `testcases/reports/2026-09-26_ch5-judge-fixes.md`.

Limits:

1. Your proposal 1 did not ship as pack data: the language pack does not carry the owner-facing form (ru: «Оставленная ложь: нет»). Instead the skill gives the English form as the pattern, and the agent writes it in the owner's language.
2. The keys that a machine or the judge greps stay English: `FORK:`, `BOUNDARY:` and the rest listed above. A closing report that a named hour started still opens with the English `BOUNDARY:` line.
3. Your proposal 3, a guard over the agent's own chat text, needs a hook over the agent's last answer (a `Stop` hook). It is on the list for the next version, after a look at the vendor's hook contract and a measure of the hook's false alarms on the origin's sandbox tests.
4. This has not been observed in the field yet. The first chat close on a Russian deployment after 2.8 will be that observation.
