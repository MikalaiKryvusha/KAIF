Shipped in [KAIF 2.8 — Noble KAIF](https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.8):

- **I28b, as you proposed it.** `/owner-reviews` → "I28b. The call names the caller": with more than one workspace of the project (a `/team-deployment` team, parallel worktrees) every call — the voice, the console banner `CALL · <session>:`, the page window's title — names the calling session right after the owner: «<owner>, this is <session>. …».
- **Derived, never typed per call** — in the order of your local `agentName()`: `KAIF_SESSION_NAME` wins (your `QA_AGENT_NAME`); a linked workspace (its `.git` is a file) is named by its directory, `<project>-team-<role>` → `<role>`, so your `…-team-dev2` → `dev2`; the main copy is `main` when its `.git/worktrees/` lists other workspaces; a project with one workspace calls with no name at all. The name is read from the file system — no git program is started.
- **Pronounceable** — the language pack speaks it: `dev2` → «dev two» in English, «дев два» in the Russian pack; the main copy — «мейн».
- **The door that carries it** — `node .kaif/tools/contour/review.mjs --call "<what is needed>" [--dry-run]` (origin issue #95); the page's own call uses the same introduction.
- **`/team-deployment`**, the workspace step: each workspace calls the owner under its seat's name, derived from the directory, `KAIF_SESSION_NAME` overriding it.

What your side does: nothing, if your seats follow `<project>-team-<role>` — the names come by themselves. Your `tools/signal.mjs` keeps working; the shipped call does the same for projects without one.

Proof — the check you wrote: sandbox suite `tools/sandbox/s22-contour-shipped.mjs` section F, on two deployed copies set up as a main copy and a linked workspace `callws-team-dev2`: `--call --dry-run` prints «…, это мейн.» with `CALL · main` from the first and «…, это дев два.» with `CALL · dev2` from the second; a single-workspace deployment calls with no name; all three cases are red on the 2.7 core. The mutant probe `tools/sandbox/probes/ow4-call-mutants.mjs` — "the name removed" (your threat), "the workspace not read", "the title without the session" — each red only on its own case. Functional run on the origin, a main copy with registered worktrees: `CALL · main (dry run, no sound): Криник, это мейн. Нужен пароль от тестового телефона`. Run report: `testcases/reports/2026-09-25_ow4-call-names-session.md`.

Boundaries, said plainly. The runs were dry — no live call sounded; how «дев два» sounds is your owner's ear, at the first real call. Nothing ran on macOS, where your seats live: the name is derived there by the same file read, and the voice is the system `say` — confirmation comes with your 2.8 update report. A linked workspace whose directory does not follow `<project>-team-<role>` is called by its whole directory name. Thank you — your local fix, already heard by your owner, and your I28b text were the design input.
