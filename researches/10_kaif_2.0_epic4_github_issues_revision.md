# Исследование 10 — KAIF 2.0, эпик №4: ревизия багов в GitHub

> **Родитель:** `ideas/18_kaif_2.0.md`, эпик №4 («почитать баги в GH — устаревшие закрыть,
> актуальные пофиксить»). **Создан:** 2026-07-27.
> **Статус:** справочный — потреблён 2.0 (зонтик `plans/20`). **Вовне:** —.
> **Метод:** `gh issue list --state all` + сверка каждого issue с текущим кодом машинерии.
> [TESTED: 2026-07-27 · gh CLI + чтение KAIF-CORE.mjs]

## Состояние трекера

Issues в `MikalaiKryvusha/KAIF` — **ровно один**, открытый; закрытых нет; трекер включён.

| # | Заголовок | Дата | Вердикт |
|---|---|---|---|
| 1 | core check: anonymous installs need explicit `--mode anonymous` while the install bundle exists | 2026-07-17 | **Актуален — чинить.** Воспроизводится в 1.6: `MODE`/`ANON` берутся только из CLI (`KAIF-CORE.mjs:41-42`), `cmdCheck` читает из маркера `agents`, но не `tracking` (:632-633); паттерн чтения tracking уже есть в `runFinalGates` (:496) — фикс ~3 строки. Независимо воспроизведён тремя полевыми отчётами (01-D1, 02-§4, 08-Д1). |

## Что делать в 2.0

Issue #1 — грань класса **bugs/11** (гейты проверяют не то свойство): чинится вместе с ним, в
ранней фазе фиксов. Закрытие issue — со ссылкой на коммит фикса и релиз; комментарий/закрытие в
чужом видимом трекере — после фикса, штатной репликой (внешняя коммуникация — по стоячей практике
релизов, ничего сверх не требуется). Устаревших issues, требующих закрытия «как неактуальные», нет.

Эпик №4 — самый дешёвый из шести: одна грань одного уже заведённого бага + гигиена трекера.

## Решения, принятые агентом без владельца

1. Issue не закрыт и не прокомментирован в этой сессии — сначала фикс, потом реплика в трекере
   (то же решение зафиксировано в researches/07).
