# Отчёт прогона — полигон истока на эпике VC (KAIF 2.7)

> Второй отчёт прогона истока по собственной поставке: шаблон — `framework/templates/_testrun-report-template.md`
> (в поле — `.kaif/_testrun-report-template.md`), форма судится `node framework/tools/kaif-testrun-lint.mjs check`
> (`TESTING_FRAMEWORK.md` → «Исполненный прогон рождает отчёт»). Каталог по дате: имя файла и есть индекс.

**Создан:** ≈ 2026-09-12 14:45 +03:00 · **Прогонял:** агент (Claude Fable 5.1), сессия 61 · **Версия/сборка:** KAIF 2.7 (открыта), сборка от HEAD `720717c` + рабочее дерево эпика VC

## 1. Работа

Эпик VC «Текст владельца — по его портрету, независимая проверка, потом владелец» (`plans/105`, критерий 14 `plans/100`;
тикет origin #61; постановка переписана словом владельца ≈ 14:25 +03:00, №112): tool-модуль `kaif-voice-lint` (машинная
половина независимой проверки), скелет портрета §7/§8 (таблица паттернов, чистый проход §7Б), `/owner-voice` — контракт
письма + режим `check` обоих слоёв, четвёртое обязательство fable-цикла тремя шагами и та же строка в «Показ — это
действие», чек-листе и роутере обоих слоёв, пункт `owner-voice` установки, охота судьи «Owner text past the portrait»,
свод `s26`. Базис — сценарий критерия 14 в `plans/100` (редакция №112), критерии 1–2 и «Верификация наблюдением» в
`plans/105`; набор кейсов — селфтест модуля (35 кейсов на первом прогоне, 48 на финальном — свидетель и `\B` добавлены
после слова владельца №112 и каверз судьи) и ассерты свода `s26` (27 → 42 → 47 на финальном) плюс 24 прежних свода полигона.

## 2. Контур

Поставка: `framework/tools/kaif-voice-lint.mjs`, `framework/templates/_owner-voice-template.md`,
`framework/skills/owner-voice/SKILL.md`, `framework/AGENT_GUIDE.md`, `framework/skills/fable-judge/SKILL.md`,
`framework/installer/KAIF-CORE.mjs` (текст пункта установки), `framework/KAIF_REFERENCE.md`, сборщик и валидатор
(`tools/build-framework.mjs` — записи 2.7, `tools/check-framework.mjs` — пары и носитель кириллицы), полигон
`tools/sandbox-suite.mjs`. Стенд — рабочее дерево истока на Windows 11 Pro 10.0.26200, Node v24.15.0, git-bash; своды
разворачивают копии в уникальных временных корнях (`tempRoot`). Продакшена нет — код истока, ничего в бою не тронуто;
для красного доказательства использован `dist/` релиза 2.6 (`git show v2.6:dist/KAIF-CORE.mjs` и
`…/KAIF-CORE-BUNDLE.md` в `C:/Users/krinik/AppData/Local/Temp/kaif_s61/dist26/`). Живой прогон на самом истоке — слепок
`AUTHOR_STYLOMETRY.md`, генерируемый из приватного ядра (таблицы §8 в форме поставки не несёт).

## 3. Прогоны

Моменты — по времени файлов логов в скретчпаде сессии (mtime); где лог не сохранялся — «≈».

| # | Момент | Команда | Код / исход |
|---|---|---|---|
| 1 | ≈ 2026-09-12 14:13 +03:00 | `node framework/tools/kaif-voice-lint.mjs selftest` | 0 — «voice-lint selftest OK — 35 cases, 2 languages» с первого прогона (EN «7:Remember that», RU «7:Помни, что»; фенс/код-спан/комментарий невидимы; `/regex/`-исключение гасит; `\b` Unicode — «ты» в «открыты» не попадание; §8 прозой → no-table; скелет незаполненный → no-rules) |
| 2 | 2026-09-12 14:15 +03:00 | `node tools/lang-packs-guard.mjs` → `node tools/lang-packs-guard.mjs --update-pins` → повтор | 1 → 0: «EN-описание навыка ПЕРЕПИСАНО: owner-voice» (ожидаемо — описание получило третий режим); ru-алиасы сверены глазами, добавлены «проверь голос», «сверь со стилометрией»; после `--update-pins` — «lang-packs-guard OK … пинов EN-описаний: 37» |
| 3 | 2026-09-12 14:15 +03:00 | `node tools/build-framework.mjs` | 0 — «KAIF-CORE-BUNDLE.md (180 file blocks) … kaif-module-map.json (789 modules)», «check-framework OK» (новые пары VC, носитель кириллицы `tools/kaif-voice-lint.mjs`) |
| 4 | ≈ 2026-09-12 14:16 +03:00 | `node framework/tools/kaif-voice-lint.mjs check STATUS.md` (живая проба истока) | 3 — «voice-lint SKIPPED — §8 of AUTHOR_STYLOMETRY.md carries no pattern table (its table lacks the pattern/hint columns)», два чужих заголовка таблиц §8 слепка названы поимённо (`База · Слов · Попаданий · На 10 000`, `Конструкция · Машина · **Автор**`) — граница, названная в плане |
| 5 | 2026-09-12 14:15 +03:00 | `node tools/sandbox/s26-voice-lint.mjs` | 1 — «s26: 2 of 27 check(s) failed»: ассерт итоговой строки ждал «8 line(s)», модуль считал завершающий перевод строки девятой строкой (находка 1; починена — прогон 8) |
| 6 | 2026-09-12 14:15 +03:00 | `KAIF_DIST=C:/Users/krinik/AppData/Local/Temp/kaif_s61/dist26 node tools/sandbox/s26-voice-lint.mjs` | 1 — контрольный кейс на ядре 2.6: «7 of 27 check(s) failed» (пять адресованы отсутствующей фиче + два той же находки 1); свод доходит до вердикта, исключений нет |
| 7 | 2026-09-12 14:18 +03:00 | `npm run test:core` | 1 — «sandbox suite: 1 of 25 suites FAILED — s26-voice-lint.mjs» (та же находка 1); остальные 24 свода зелёные, четыре преполёта зелёные («every machinery file carries a test-status marker (25 suites)») |
| 8 | 2026-09-12 14:19 +03:00 | после починки `visibleLines` (завершающий перевод строки — не строка): `node framework/tools/kaif-voice-lint.mjs selftest` · `node tools/build-framework.mjs` | 0 — «35 cases» · «180 file blocks … 789 modules», «check-framework OK» |
| 9 | 2026-09-12 14:20 +03:00 | `node tools/sandbox/s26-voice-lint.mjs` · `KAIF_DIST=C:/Users/krinik/AppData/Local/Temp/kaif_s61/dist26 node tools/sandbox/s26-voice-lint.mjs` | 0 — «✅ s26 voice-lint: all 27 checks green»; на ядре 2.6 — 1: «❌ s26: 5 of 27 check(s) failed» — все пять адресованы отсутствующей фиче (модуль не приехал · §7A скелета без команды · §8 скелета без таблицы · развёрнутого линтера нет ×2), 22 зелёных |
| 10 | 2026-09-12 14:22 +03:00 | `npm run test:core` | 0 — «✅ sandbox suite: all 25 suites green» (четыре преполёта, s26 в составе) |
| 11 | 2026-09-12 ≈ 14:19–14:23 +03:00 | стражи: `node tools/counters-guard.mjs` (после починки восьми зеркал 179/788 → 180/789 и 24 → 25 сводов) · `node tools/check-framework.mjs --selftest` · `node tools/doc-header-lint.mjs` · `node tools/questions-guard.mjs` · `node framework/tools/kaif-attribution-lint.mjs check` · `node framework/tools/kaif-scenario-lint.mjs check plans/105_epic100_VC_owner_voice_check.md plans/100_EPIC_kaif_2.7.md` · `node tools/experience-lint.mjs` · `node framework/tools/kaif-guard-lint.mjs check framework/installer/KAIF-CORE.mjs` · `node tools/verify-contour.mjs --etalon-only` · `node tools/private-names-guard.mjs` · `node tools/showcase-lint.mjs` · `node framework/tools/kaif-testrun-lint.mjs check` | counters «✅ counters OK — 50 зеркал сверены» (до починки — 8 расхождений, находка 2) · 5d «9 бывших слепых зон краснеют» · doc-header «findings 0» · questions «Новых нарушений: 0» (exit 0) · attribution «new 0 · debt 45» · scenario «13 scenario(s), 0 findings» · experience «новых нарушений 0» · guard-lint «3 declared block(s)» · etalon «66 зелёных, 0 красных» · private-names «приватных имён в поставке нет» · showcase «витрина чиста» · testrun «OK — 1 report(s)» (до этого отчёта) |
| 12 | 2026-09-12 ≈ 14:38–14:48 +03:00 | переделка по слову владельца (№112: три шага, затем «с ней в рабочем кеше» → `load` + свидетель): `node framework/tools/kaif-voice-lint.mjs selftest` · `node tools/build-framework.mjs` | 0 — «voice-lint selftest OK — 44 cases, 2 languages» (свидетель: пять чистых вердиктов, `--sections`); сборка «180 file blocks … 790 modules», «check-framework OK» (пары: токены трёх шагов обоих слоёв, `load`, новое имя охоты «Owner text past the portrait») |
| 13 | 2026-09-12 ≈ 14:49 +03:00 | `node tools/sandbox/s26-voice-lint.mjs` · `KAIF_DIST=C:/Users/krinik/AppData/Local/Temp/kaif_s61/dist26 node tools/sandbox/s26-voice-lint.mjs` · `npm run test:core` · `node tools/check-framework.mjs --selftest` | 0 — «✅ s26 voice-lint: all 42 checks green»; на ядре 2.6 — 1: «❌ s26: 7 of 42 check(s) failed» — все семь адресованы отсутствующей фиче (модуль · обе команды в скелете · таблица скелета · развёрнутые `load`/`check` ×4), 35 зелёных; полигон «✅ sandbox suite: all 25 suites green»; 5d «9 бывших слепых зон краснеют» |
| 14 | 2026-09-12 14:49 +03:00 | живые пробы истока: `node framework/tools/kaif-voice-lint.mjs load --sections "^8"` · `git status --short .kaif/voice-marker.json` · `node framework/tools/kaif-voice-lint.mjs check STATUS.md` · стражи (`doc-header-lint` · `questions-guard` · `kaif-attribution-lint check` · `kaif-scenario-lint check plans/105 plans/100` · `experience-lint` · `kaif-guard-lint` · `verify-contour --etalon-only` · `private-names-guard` · `showcase-lint` · `kaif-testrun-lint check` · `lang-packs-guard` · `diff` копий fable) | `load` — 0: «AUTHOR_STYLOMETRY.md (381 line(s), sections: ^8) is now in your working context; witness .kaif/voice-marker.json at 2026-09-12T14:49:30.795+03:00»; `git status` пуст — маркер в ignore; `check` — 3: SKIPPED, §8 слепка без таблицы паттернов; стражи: doc-header 0 · questions «Новых нарушений: 0» · attribution «new 0 · debt 45» · **scenario — 1 находка** (`plans/100:252 — one-action`: строка «Действие» критерия 14 после переделки несла несколько действий; находка 4, починена — прогон 15) · experience 0 · guard-lint 3 блока · etalon 66 · private-names чисто · showcase чисто · testrun «OK — 2 report(s)» · lang-packs OK · копии fable побайтно равны |
| 15 | 2026-09-12 ≈ 14:51–14:54 +03:00 | финал после починки критерия 14 и маркеров `[TESTED]`: `node tools/counters-guard.mjs` · `node tools/build-framework.mjs` · `node framework/tools/kaif-scenario-lint.mjs check plans/105_epic100_VC_owner_voice_check.md plans/100_EPIC_kaif_2.7.md` · `node framework/tools/kaif-testrun-lint.mjs check` · `node tools/doc-header-lint.mjs` · `node tools/sandbox/s26-voice-lint.mjs` · `npm run test:core` | counters «✅ counters OK — 50 зеркал» (после 789 → 790); сборка «180 file blocks … 790 modules», «check-framework OK»; scenario «13 scenario(s), 0 findings»; testrun «OK — 2 report(s)»; doc-header «findings 0»; «✅ s26 voice-lint: all 42 checks green»; «✅ sandbox suite: all 25 suites green» (14:54) |
| 16 | 2026-09-12 14:56–15:04 +03:00 | лёгкий судья эпика — субагент с чистым контекстом по `/fable-judge` (переисполнил заявления 1–9: selftest · s26 · `KAIF_DIST` красный · полигон · пересборка без дрейфа по хэшам · стражи · живые пробы · канон · дословность шести цитат) | **VERIFIED WITH CAVEATS** — все девять заявлений воспроизведены, фродов нет; 14 каверз: 1–5 протухшие числа/токены/цитата-сплайс, 6 дерево двигалось под судьёй, 7–14 границы свидетеля и модуля (E2: маркер трёхчасовой давности → exit 0; E3: `--sections "^zzz"` пишет полного свидетеля; E4/E9b/E10: скопированный маркер, `touch`, `--warn` гасит свидетеля; E6: `\W` в классе и `\B`; E7: смена портрета — предупреждение; E5: `--sections` без значения = all; E9a: смешанные зоны времени; E8: N одинаковых строк) |
| 17 | 2026-09-12 ≈ 15:08–15:17 +03:00 | починки каверз: `node framework/tools/kaif-voice-lint.mjs selftest` · `node tools/build-framework.mjs` · `node tools/sandbox/s26-voice-lint.mjs` · `KAIF_DIST=C:/Users/krinik/AppData/Local/Temp/kaif_s61/dist26 node tools/sandbox/s26-voice-lint.mjs` · `npm run test:core` · стражи (`counters-guard` · `lang-packs-guard` · `doc-header-lint` · `kaif-scenario-lint check plans/105 plans/100 plans/106` · `kaif-testrun-lint check` · `kaif-attribution-lint check` · `questions-guard` · `experience-lint` · `check-framework --selftest` · `diff` копий fable · `private-names-guard` · `showcase-lint` · `verify-contour --etalon-only`) | «voice-lint selftest OK — 48 cases, 2 languages»; сборка «180 file blocks … 790 modules», «check-framework OK»; «✅ s26 voice-lint: all 47 checks green»; на ядре 2.6 — «❌ s26: 7 of 47 check(s) failed», все семь — отсутствующая фича, 40 зелёных; «✅ sandbox suite: all 25 suites green» (15:17); counters «OK — 50 зеркал» · lang-packs OK · doc-header «findings 0» · scenario «15 scenario(s), 0 findings» · testrun «OK — 2 report(s)» · attribution «new 0 · debt 45» · questions «Новых нарушений: 0» · experience «новых нарушений 0» · 5d/5e OK · копии fable побайтно равны · private-names чисто · showcase чисто · etalon 66 |
| 18 | 2026-09-12 15:35–15:39 +03:00 | **переисполнение на ЗАКОММИЧЕННОМ дереве `93ec9ff` (сессия 62, закрытие долга форс-закрытия):** `node tools/build-framework.mjs` · `git status --short` · `node framework/tools/kaif-voice-lint.mjs selftest` · `node tools/sandbox/s26-voice-lint.mjs` · `npm run test:core` · стражи (`counters-guard` · `doc-header-lint` · `questions-guard` · `kaif-attribution-lint check` · `experience-lint` · `kaif-testrun-lint check` · `lang-packs-guard` · `private-names-guard` · `showcase-lint` · `verify-contour --etalon-only` · `kaif-scenario-lint check plans/100 plans/105 plans/106` · `kaif-guard-lint check` · `stylometry-snapshot --version-check`) · пары реестра (`diff` копий fable · `ls` референсов навыков · маркеры молитв · тройка версии) | сборка «180 file blocks … 790 modules», «check-framework OK», `git status` ПУСТ — дрейфа генератов после пересборки нет; «voice-lint selftest OK — 48 cases, 2 languages»; «✅ s26 voice-lint: all 47 checks green»; «✅ sandbox suite: all 25 suites green» (15:38); counters «OK — 50 зеркал» · doc-header «findings 0» + requirements «258 file(s), 0 findings» · questions «Новых нарушений: 0» · attribution «new 0 · debt 45» · experience «новых нарушений 0» · testrun «OK — 2 report(s)» · lang-packs OK (8 заморожено, 37 пинов) · private-names чисто · showcase чисто · etalon «66 зелёных, 0 красных» · scenario «3 file(s), 15 scenario(s), 0 findings» · guard-lint «3 declared block(s)» · слепок «совпадает с ядром (`5265de5`)»; пары: копии fable побайтно равны · референсы навыков совпали · маркеры `KAIF:CREED`/`KAIF:PRAYER` по 1 в обоих слоях · тройка версии 2.6 сходится |

## 4. Проверки

| Кейс | Статус | Наблюдение |
|---|---|---|
| Критерий 14 `plans/100` (редакция №112) — селфтест линтера красный на попадании и зелёный на чистом, оба языка | pass | прогоны 1 и 8: EN «7:Remember that», RU «7:Помни, что» с подсказкой и прозаическим исключением; чистый текст 0; фенс, код-спан, HTML-комментарий невидимы; `/regex/`-исключение гасит (silenced 1); голый паттерн регистрозависим, `/…/i` складывает; `\b` Unicode; отсутствующий позитив — предупреждение; §8 прозой → no-table; без §8 → no-section; чужие колонки → no-table с названием шапки; третий язык — по номеру; скелет поставки незаполненный → no-rules |
| Критерий 14 — свод полигона на установке 2.7: портрет с таблицей §8 → попадание названо строкой · паттерном · подсказкой; без портрета → `SKIPPED=3` | pass | прогон 9: «sheet/steps.md:7 — «Помни, что» → правило называется, читателю не напоминают» exit 1 (критерий 2 `plans/105` дословно) и EN-двойник; «no portrait at AUTHOR_STYLOMETRY.md» exit 3; §8 прозой — exit 3 «carries no pattern table … prose only»; `--warn` — exit 0 с попаданием; путь из `.kaif/kaif.json` → `voicePortrait`; usage — exit 2; развёрнутая копия — модуль и скелет (команда в §7A + шапка таблицы §8) приехали, незаполненная копия скелета → SKIPPED «placeholders are not rules», одна заполненная строка → «a.md:7 — «Remember that» → …» exit 1 |
| Критерий 14 — грепы трёх шагов и команды в обоих слоях `AGENT_GUIDE` | pass | проба 2026-09-12 ≈ 14:24 +03:00 (до переделки №112): `grep -c "kaif-voice-lint.mjs check" framework/AGENT_GUIDE.md` → 4, корневой → 4; скелет `grep -c kaif-voice-lint` → 2; навык payload 2 / wrapper 2; охота судьи payload 1 / wrapper 1; бандл несёт `FILE: .kaif/tools/kaif-voice-lint.mjs` → 1 и шапку таблицы §8 → 1. После переделки токены трёх шагов («Write BY the portrait.» · «Check INDEPENDENTLY by the same portrait.» · «Пиши ПО портрету.» · «Проверь НЕЗАВИСИМО по тому же портрету.») и новое имя охоты стережёт `check-framework` на финальной сборке — прогон 12 |
| Критерий 14 — на ядре 2.6 `✖` | pass | прогоны 6 и 9: «5 of 27 check(s) failed» на `dist26`, все пять — отсутствующая фича |
| Критерий 2 `plans/105` — скелет: §7A называет команду, §8 — таблица с двумя строками-примерами, которые линтер не читает | pass | `grep -c kaif-voice-lint framework/templates/_owner-voice-template.md` → 2; селфтест «the shipped skeleton, unfilled → no-rules» |
| Критерий 14 (№112) — свидетель загрузки: без маркера → находка, написано до первой загрузки → находка, после — чисто | pass | селфтест (прогон 12: пять вердиктов свидетеля) и свод `s26` (прогон 13: `check early.md` без свидетеля → «written past the portrait — no load witness»; `early.md`, написанный до `load`, → «the file was last written at …, the portrait was first loaded at …»; `clean.md` после `load` → «voice-lint OK … loaded 2026-…»); на копии — «changed since it was loaded» после правки строки §8 |
| Критерий 14 (№112) — `load` печатает портрет в рабочий контекст и оставляет свидетеля; `--sections` фильтрует; путь из маркера; без портрета — SKIPPED | pass | прогон 13 (свод: «load печатает портрет в контекст и оставляет свидетеля», `sections: all` / `^8`, `voice/PORTRAIT.md` из `.kaif/kaif.json`, «load SKIPPED (exit 3): загружать нечего») и прогон 14 (живая проба истока: 381 строка головы и §8 слепка, свидетель записан, git его не видит) |
| Критерий 14 (№112) — три шага и обе команды в обоих слоях `AGENT_GUIDE`, охота судьи переименована | pass | прогон 12: `check-framework OK` с парами «voice contract ↔ …» (токены «Write BY the portrait — with it in your working context.» · «Check INDEPENDENTLY by the same portrait.» · `kaif-voice-lint.mjs load` · `check <file…>` в payload; «Пиши ПО портрету — с ним в своём рабочем контексте.» · «Проверь НЕЗАВИСИМО по тому же портрету.» в корневом; «**Owner text past the portrait (KAIF 2.7).**» у судьи) |
| Критерий 14 (№112) — свидетель после суда: история загрузок, правило часа при письме, чужой портрет, `--warn` не гасит, `--sections` без совпадений — отказ | pass | прогон 17: селфтест («written 90 min after the last load → finding», «a re-load 10 min before the write → clean», «a marker for another portrait → finding», «\B is Unicode-aware»); свод («--warn НЕ гасит свидетеля (exit 1)», «load --sections без совпадений — usage (exit 2), свидетель не изменился», «портрет переключён после загрузки — свидетель чужого портрета: находка») |
| Судья эпика (лёгкий, критерии 1–2 `plans/105`, критерий 14 `plans/100`) | pass после починок | прогон 16: VERIFIED WITH CAVEATS, девять заявлений переисполнены, ослабленных проверок нет; 14 каверз закрыты до коммита (прогон 17): протухшие числа и токены — по выводу инструментов, цитата — дословно по (е), свидетель — история загрузок и правило часа, `--warn`/`--sections`/`\B`/зоны времени/одна строка на прогон — в модуле и селфтесте |
| Регрессия — 24 прежних свода на сборке с новым модулем | pass | прогоны 10, 13 и 17: «all 25 suites green» |
| Стражи закрытия | pass с двумя починками | прогон 11: зеркала счётчиков 179/788 → 180/789 и число сводов 24 → 25 по выводу сборки; страж языковых пакетов — по построению красный на переписанном описании, снят `--update-pins` после сверки |

Контрольный кейс (цепочка активностей, шаг 5 — «выключи фичу и наблюдай, как она НЕ работает»): прогоны 6 и 9 — тот же
свод на ядре 2.6 красный ровно там, где фичи нет.

## 5. Найдено

- Дефектов поставки на финальной сборке — ноль: ни один ассерт свода и ни один кейс селфтеста не покраснел.
- Находка 1 (дефект работы, S3): счёт строк модуля включал завершающий перевод строки — итоговая строка называла «9
  line(s)» на восьмистрочном листе; селфтест этого не ловил (он не судит счёт), поймал свод `s26` (ассерт итоговой
  строки); починено в `visibleLines` до коммита (прогон 8).
- Находка 2 (процесс): восемь зеркал счётчиков (179/788 → 180/789) и число сводов 24 → 25 отстали от сборки —
  `counters-guard`; починены по выводу сборки, «50 зеркал сверены».
- Находка 3 (постановка, S1 по доверию владельца): первая редакция обязательства — «проверка голоса командой до первого
  показа» — была половиной; владелец поправил постановку ≈ 14:25 +03:00 (№112): агент пишет ПО портрету → независимая
  проверка по нему → правит → только тогда написан → владельцу. Переделка шагом VC9 `plans/105` (канон обоих слоёв, навык,
  судья, скелет, записи 2.7, планы); модуль и свод кодом не менялись. Урок класса — `EXPERIENCE.md` на закрытии.
- Находка 4 (форма критерия, S3): после переделки строка «Действие» критерия 14 `plans/100` несла несколько действий —
  `kaif-scenario-lint` «one-action» (прогон 14); починено: одно действие (`check` перед показом), загрузка, письмо и
  чистый проход перенесены в «Ситуацию» как состояние (прогон 15).
- Второе слово владельца (≈ 14:35, дословно: «Это блять важно - писать ПО СТИЛОМЕТРИИ, С НЕЙ В СВОЕМ РАБОЧЕМ КЕШЕ»)
  механизировано командой `load` со свидетелем `.kaif/voice-marker.json` — по образцу маркера освежения; `check`
  отказывает тексту без свидетеля или написанному раньше загрузки (прогоны 12–14); после судьи (каверзы 7–14) —
  история загрузок, правило часа при письме, чужой портрет, `--sections` без совпадений, `--warn` не гасит свидетеля,
  `\B` Unicode, одна зона времени, одна строка на прогон (прогоны 16–17).
- Живой прогон истока: `check` — `SKIPPED=3`: слепок §8 несёт таблицы замеров, а не паттернов, и линтер назвал их поимённо;
  `load --sections "^8"` — 381 строка в контекст, свидетель записан и не виден git; перенос стоп-половины
  `tools/voice-lint.mjs` в таблицу слепка — только словом владельца (генератор — приёмка №62), кандидат в 2.8.

## 6. Следы

- Логи прогонов 2–11 — скретчпад сессии 61 (`C:/Users/krinik/AppData/Local/Temp/kaif_s61/`: `build1.log`, `build2.log`,
  `s26.log`, `s26-red.log`, `s26b.log`, `s26b-red.log`, `polygon1.log`, `polygon2.log`, `questions.log`; `dist26/` — dist
  релиза 2.6) — умирают с сессией; долговечные следы — этот отчёт, коммит эпика VC в `git log` и маркеры `[TESTED: …]` в
  шапках `framework/tools/kaif-voice-lint.mjs` и `tools/sandbox/s26-voice-lint.mjs` с теми же наблюдениями.
- Красный полигона, если случится, оставляет `run-fail-N.log` в каталоге прогона (форензика `bugs/109`) — в прогоне 7
  красный был свой (находка 1), а не флейк.
- Прогон 18 (сессия 62) логов в файлы не писал — вывод читался в чате по команде; долговечный след — строка 18 этой
  таблицы и сам факт: дерево на `93ec9ff` чистое, пересборка не дала дрейфа (`git status` пуст).

## 7. Вердикт

pass — критерий 14 `plans/100` (редакция №112, включая «с ней в рабочем кеше») и критерии 1–2 `plans/105` исполнены
наблюдением: линтер красный на попадании и зелёный на чистом в обоих языках, свидетель загрузки отказывает тексту без
загрузки, написанному до неё, позже часа после неё и под чужим портретом, свод `s26` зелёный на сборке 2.7 («all 47
checks green») и красный на ядре 2.6 («7 of 47 check(s) failed»), полигон целиком зелёный (прогоны 10, 13, 15, 17 и
переисполнение 18 на закоммиченном дереве `93ec9ff`); лёгкий судья эпика — VERIFIED WITH CAVEATS, 14 каверз закрыты до
коммита и переисполнены (прогон 17); четыре находки процесса названы и закрыты.

Прогон 18 добавлен сессией 62 при закрытии долга форс-закрытия: формальные ✅ эпика ставятся по СОБСТВЕННОМУ
наблюдению на уже закоммиченном дереве, а не по памяти закрывшейся сессии.
