# Отчёт прогона — ось «недоставленный сигнал» на реальных тикетах поля и полигон истока на эпике SD (KAIF 2.7)

> Шестой отчёт прогона истока по собственной поставке; шаблон — `framework/templates/_testrun-report-template.md`
> (в поле — `.kaif/_testrun-report-template.md`), форма судится `node framework/tools/kaif-testrun-lint.mjs check`
> (`TESTING_FRAMEWORK.md` → «Исполненный прогон рождает отчёт», «Что значит слово „тест“»). Каталог по дате: имя файла и есть индекс.

**Создан:** 2026-09-13 01:08 +03:00 · **Прогонял:** агент, сессия 63 — Claude Fable 5.1 (прогоны 1–5, до 2026-09-13 00:20 +03:00), затем Claude Opus 5 (прогоны 6–33; суд — субагент того же) · **Версия/сборка:** KAIF 2.7 (открыта), HEAD `3fc04a2` + рабочее дерево эпиков IW и SD, `bugs/116`, `bugs/117`

## 1. Работа

Эпик SD «Сигнал доставлен» (`plans/109`, критерий 17 `plans/100`; тикет origin #65 — рецидив #37). Три механизма: **(1)** ось
`check` ядра на `tracking: origin` называет каждый нумерованный тикет `bugs/KAIF/NN_*.md` без улики доставки — `NOT YET` с
готовой командой `report`, пропущенную или нечитаемую строку — «no readable delivery state» с обеими законными формами;
`report` и `check` читают строку одной функцией `deliveryState()`; **(2)** `/report-bug` обоих слоёв — заведение и доставка
один шаг, имя поля `**Delivered upstream:**` машинное и дословное; **(3)** исключение ворот `AUTH:` в их же строке
`AGENT_GUIDE` обоих слоёв. Базис — сценарии 1, 1б, 1в и 2 `plans/109`; набор кейсов — проба SD0 (скретчпад `sd0-probe.mjs`,
написана ДО кода), свод `s17` (+11 ассертов SD), мутанты блока (`sd-mutants.mjs`), функциональный прогон по реальным тикетам
поля (`sd-real-state.mjs`), пары `check-framework` (9), суд и повторный суд, полигон целиком.

## 2. Контур

Поставка: `framework/installer/KAIF-CORE.mjs` (`cmdCheck` — ось `undelivered-signal`; `deliveryState()`; `cmdReport`),
`framework/skills/report-bug/SKILL.md`, `framework/AGENT_GUIDE.md`, `framework/skills/fable-judge/SKILL.md`,
`framework/KAIF_REFERENCE.md`; обвязка: `AGENT_GUIDE.md`, `.claude/skills/report-bug/SKILL.md`,
`.claude/skills/fable-judge/SKILL.md`; машинерия: `tools/check-framework.mjs` (пары), `tools/build-framework.mjs` (записи 2.7),
`tools/sandbox/s17-report.mjs`. Стенд — рабочее дерево истока на Windows 11 Pro 10.0.26200, Node v24.15.0, git-bash; своды и
пробы разворачивают копии поставки из `dist` во временные корни; внешних действий нет — `gh` подменён на шве `KAIF_GH`.
Реальное состояние — 46 файлов `bugs/KAIF/` четырёх полевых развёртываний на диске владельца (обезличены: field-1…4),
скопированы в свежие установки; соседние деревья только читались. Живого полевого `check` после обновления 2.7 здесь нет — это
путь поля. Красные доказательства: `dist` релиза 2.6 (скретчпад `dist26/`) · первая сборка оси, судившая только `NOT YET`
(скретчпад `dist-blacklist/`, сохранена 00:54 до починки — время файла) · вторая редакция, где уликой был любой `#цифры`
(скретчпад `dist-hashany/`, сохранена 02:07 — время файла) · три мутанта блока.

## 3. Прогоны

Моменты сняты пробой `date` в тот же вызов, что и команда.

| # | Момент | Команда | Код / исход |
|---|---|---|---|
| 1 | 2026-09-13 00:04 +03:00 | `node <скретчпад>/sd0-probe.mjs` (ДО кода; копия из `dist` истока, тикет NOT YET на `tracking: origin`) | 1 — «--- check on tracking: origin with a NOT YET ticket → exit 0 · (no line about the ticket) · ❌ probe RED» |
| 2 | 2026-09-13 00:09 +03:00 | `node tools/build-framework.mjs` | 1 — «pair "signal ↔ AGENT_GUIDE carries the carve-out in the AUTH line (wrapper)" broken — AGENT_GUIDE.md does not carry "kaif-core.mjs report"» (команда была перенесена на две строки) |
| 3 | 2026-09-13 00:09 +03:00 | `node <скретчпад>/sd0-probe.mjs` (после SD1) | 0 — «⚠ undelivered KAIF signal: bugs/KAIF/15_upstream_signal_waits.md — … report bugs/KAIF/15_upstream_signal_waits.md · ✅ probe» |
| 4 | 2026-09-13 00:09 +03:00 | `node tools/sandbox/s17-report.mjs` · `KAIF_DIST=<скретчпад>/dist26 node tools/sandbox/s17-report.mjs` | 0 — «✅ s17 report: all green» (4 ассерта SD) · 1 — «❌ s17: 1 failure(s)»: красный только ассерт NOT YET; «после доставки молчит» и «anonymous молчит» зелёные на 2.6 по построению |
| 5 | 2026-09-13 00:10 +03:00 | `node tools/build-framework.mjs` | 0 — «check-framework OK … 792 modules / 150 md files» |
| 6 | 2026-09-13 00:31 +03:00 | `npm run test:core` | 1 — преполёт `bugs/59`: «fixed temp name: tools/lib/sandbox-run.mjs:88 — const QUIET_PATH = join(tmpdir(), 'kaif-quiet-no-programs-on-path')» (починка `bugs/116`) |
| 7 | 2026-09-13 00:32 → 00:35 +03:00 | `npm run test:core` | 0 — «✅ sandbox suite: all 26 suites green», преполёт «the contour generator never reaches the owner from a sandbox run» |
| 8 | 2026-09-13 00:47 +03:00 | `node <скретчпад>/sd-mutants.mjs` (первая редакция: два мутанта против первой сборки оси) | 0 — «M1-tracking … ❌ s17/SD: tracking: anonymous — … check молчит» · «M2-delivered … ❌ s17/SD: после доставки check о сигнале молчит» — оба ассерта «молчит» умеют краснеть |
| 9 | 2026-09-13 00:48 +03:00 | `node <скретчпад>/sd-real-state.mjs` (первая редакция; 46 реальных тикетов, первая сборка оси) | 0 — «field-1: 18 · warned 6», «field-2: 15 · warned 0», «field-3: 11 · warned 0», «field-4: 2 · warned 0»; строка-итог скрипта «check agrees with every real ticket paragraph» — оракул повторял регэксп ядра; находку дало чтение напечатанных абзацев (см. «Найдено») |
| 10 | 2026-09-13 00:58 +03:00 | `node tools/build-framework.mjs` (после починки `bugs/117`) | 0 — «check-framework OK — 59 embedded files … 792 modules / 150 md files» |
| 11 | 2026-09-13 00:58 +03:00 | `node tools/sandbox/s17-report.mjs` | 0 — «✅ s17 report: all green»: восемь ассертов SD (NOT YET · доставка · молчит после · форма #65 · обещание · README и доставленный молчат · `report` читает то же · anonymous) и 31 прежний |
| 12 | 2026-09-13 00:58 +03:00 | `KAIF_DIST=<скретчпад>/dist-blacklist node tools/sandbox/s17-report.mjs` | 1 — «❌ s17: 2 failure(s)»: ровно «форма #65» и «обещание вместо адреса» |
| 13 | 2026-09-13 00:59 +03:00 | `KAIF_DIST=<скретчпад>/dist26 node tools/sandbox/s17-report.mjs` | 1 — «❌ s17: 3 failure(s)»: NOT YET · форма #65 · обещание |
| 14 | 2026-09-13 00:59 +03:00 | `node <скретчпад>/sd-mutants.mjs` (три мутанта против починенного ядра) | 1 — M3 покраснил и «после доставки молчит»: в своде остаётся `bugs/KAIF/not_a_ticket.md` (кейс отказа «not a ticket»), и держит его вне оси только фильтр `NN_*.md` — ожидание скрипта было узким |
| 15 | 2026-09-13 00:59 → 01:00 +03:00 | `node <скретчпад>/sd-mutants.mjs` (ожидание M3 уточнено) | 0 — «✅ mutants: every silence assert proved red on its own broken condition» |
| 16 | 2026-09-13 01:00 +03:00 | `node <скретчпад>/sd-real-state.mjs` (вторая редакция: без оракула, вердикт рядом с текстом тикета) | 0 — «TOTAL: 46 files · NOT-YET named 6 · no readable delivery state 15 · silent 25» |
| 17 | 2026-09-13 01:01 → 01:03 +03:00 | `npm run test:core` | 0 — «✅ sandbox suite: all 26 suites green» |
| 18 | 2026-09-13 01:03 +03:00 | `SD_DIST=<скретчпад>/dist-blacklist node <скретчпад>/sd-real-state.mjs` | 0 — «TOTAL: 46 files · NOT-YET named 6 · no readable delivery state 0 · silent 40» |
| 19 | 2026-09-13 01:06 +03:00 | `node tools/counters-guard.mjs` | 0 — «✅ counters OK — 50 зеркал» |
| 20 | 2026-09-13 01:11 +03:00 | `node tools/lib/sandbox-run.mjs --selftest` · `node <скретчпад>/quiet-probe-mutants.mjs` (проба тишины `bugs/116` переписана безвредной) | 0 — «quietEnv: a child can neither find nor start cmd.exe · powershell.exe (cmd.exe=ENOENT powershell.exe=ENOENT)» · 0 — «Q1-real-path … (cmd.exe=FOUND powershell.exe=FOUND) ✅ probe RED» · «Q2-no-lookup … (cmd.exe=ran powershell.exe=ran) ✅ probe RED» |
| 21 | 2026-09-13 01:12 → 01:15 +03:00 | `npm run test:core` | 0 — «✅ sandbox suite: all 26 suites green» |
| 22 | 2026-09-13 ≈ 01:19 → 01:37 +03:00 | судья чистым контекстом, без единого окна (`/fable-judge`; его прогоны и `ds-eval.mjs` — скретчпад `judge/`) | **REFUTED** на SD-1 («молчит только на адресе issue» — любой URL был уликой: «NOT YET — a recurrence of https://…/issues/37» → delivered) и SD-5 (черновик ответа #65); двенадцать каверз — «Найдено» |
| 23 | 2026-09-13 01:41 +03:00 | `node tools/build-framework.mjs` (после починок по суду) | 0 — «check-framework OK — 59 embedded files … 792 modules» |
| 24 | 2026-09-13 01:41 +03:00 | `node tools/sandbox/s17-report.mjs` | 1 — «❌ s17/SD (форма #65) …»: регулярка ассерта сверяла прежний текст подсказки, которая стала точнее («write only …», «with no issue URL or #NN»); регулярка доведена до нового текста — проверка не ослаблена |
| 25 | 2026-09-13 01:41 +03:00 | `node tools/sandbox/s17-report.mjs` · `KAIF_DIST=<скретчпад>/dist-blacklist …` · `KAIF_DIST=<скретчпад>/dist26 …` | 0 — «✅ s17 report: all green» (девять ассертов SD, 31 прежний) · 1 — «❌ s17: 3 failure(s)»: форма #65 · обещание · одно чтение · 1 — «❌ s17: 4 failure(s)»: NOT YET · форма #65 · обещание · одно чтение |
| 26 | 2026-09-13 01:41 → 01:42 +03:00 | `node <скретчпад>/sd-mutants.mjs` | 0 — «✅ mutants: every silence assert proved red on its own broken condition» |
| 27 | 2026-09-13 01:42 +03:00 | `node <скретчпад>/sd-real-state.mjs` (ядро с неоднозначным состоянием и уликой «только issue») | 0 — «TOTAL: 46 files · NOT-YET named 6 · no readable delivery state 15 · silent 25»; строки вердиктов совпали с прогоном 16 построчно |
| 28 | 2026-09-13 01:49 +03:00 | `npm run test:core` | 1 — «✖ mute command (bugs/61): s17-report.mjs:225 — результат не доходит до ассерта: r = run('check');» — ассерт «одно чтение» судил `check` через переменную; разделён на два ассерта (`check` · `report`) |
| 29 | 2026-09-13 01:51 +03:00 | `node tools/sandbox-mute-guard.mjs` · `node tools/sandbox/s17-report.mjs` · `KAIF_DIST=<скретчпад>/dist-blacklist …` · `KAIF_DIST=<скретчпад>/dist26 …` · `node <скретчпад>/sd-mutants.mjs` | 0 — «немых команд нет — все 373 команд полигона либо судятся ассертом, либо обёрнуты must» · 0 — «✅ s17 report: all green» (десять ассертов SD, 31 прежний) · 1 — «4 failure(s)»: форма #65 · обещание · одно чтение, check · одно чтение, report · 1 — «5 failure(s)»: те же и NOT YET · 0 — «every silence assert proved red on its own broken condition» |
| 30 | 2026-09-13 01:51 → 01:54 +03:00 | `npm run test:core` | 0 — «✅ sandbox suite: all 26 suites green», преполёт «no mute command · … · the contour generator never reaches the owner from a sandbox run» |
| 31 | 2026-09-13 ≈ 01:57 → 02:03 +03:00 | повторный суд тем же судьёй, без единого окна (`judge/ds-eval2.mjs`, `judge/ds-eval3.mjs`) | **VERIFIED WITH CAVEATS**: оба опровержения и одиннадцать каверз из двенадцати закрыты; новые — (A) утренний показ `bugs/116` записан уроком EXP-0126 из первых рук, а строка звала его выводом; (B) улика `#NN` — любой `#цифры`: «⏳ sending this session — see step #2 of the skill» → delivered; (C) ассерт «одно чтение, check» не судил код возврата |
| 32 | 2026-09-13 02:07 → 02:08 +03:00 | `node tools/build-framework.mjs` · `node tools/sandbox-mute-guard.mjs` · `node tools/sandbox/s17-report.mjs` · `KAIF_DIST=<скретчпад>/dist-hashany …` · `…/dist-blacklist …` · `…/dist26 …` · `node <скретчпад>/sd-mutants.mjs` · `node <скретчпад>/sd-real-state.mjs` | 0 · 0 — «немых команд нет — все 374 команд» · 0 — «✅ s17 report: all green» (одиннадцать ассертов SD, 31 прежний) · 1 — ровно «#2 в тексте обещания» (вторая редакция с любым `#цифры`) · 1 — «5 failure(s)» · 1 — «6 failure(s)» · 0 — «every silence assert proved red» · 0 — «46 files · NOT-YET named 6 · no readable delivery state 15 · silent 25», строки вердиктов те же |
| 33 | 2026-09-13 02:10 → 02:13 +03:00 | `node tools/build-framework.mjs` · `npm run test:core` | 0 — «check-framework OK — 59 embedded files …» · 0 — «✅ sandbox suite: all 26 suites green» |

## 4. Проверки

Гигиена: свод `s17` зелёный (одиннадцать ассертов SD, 31 прежний) и красный на четырёх названных сломанных версиях (2.6 — шесть ассертов · первая сборка оси — пять · вторая редакция с любым `#цифры` — один · мутанты — каждый ассерт «молчит» на своём условии) · страж немой команды — ноль · пары `check-framework` зелёные (сборки 5, 10, 23) · `counters-guard` 50 зеркал · проба тишины `bugs/116` зелёная и красная на двух сломанных копиях · полигон «all 26 suites green» (прогоны 7, 17, 21)
Функциональный прогон: команда пользователя поставки `node .kaif/kaif-core.mjs check` на свежих установках из `dist` с РЕАЛЬНЫМИ тикетами четырёх полевых развёртываний (46 файлов, копии; стенд — временные корни, не живое развёртывание) — вывод ПРОЧИТАН построчно против текста каждого тикета (прогон 16, повтор на окончательном ядре — прогон 27, вердикты те же; судья перечитал все 46 строк независимо): 25 молчащих — у каждого адрес `…/issues/N` в строке `**Delivered upstream:**` и ни в одном нет «not yet»; 6 `NOT YET` — «awaiting the owner's word (outward action)» ×3 и «not yet — … collected by the origin for the 2.5 scope» ×3; 15 «no readable delivery state» — восемь с переведённым полем «Сигнал в исток:», «⏳ отправляется этой же сессией», «✅ this issue» без номера, `> **Delivery:** … folded into the 2.3 field update report`, четыре без строки; ни одного молчащего без улики. Плюс свод `s17` на развёрнутой копии: `check` и `report` запущены как пользователь, строки прочитаны

| Кейс | Статус | Наблюдение |
|---|---|---|
| Сценарий 1 — NOT YET на origin назван с командой; доставленный и anonymous молчат | pass | прогоны 3, 4, 11 |
| Сценарий 1 — красный на ядре 2.6 | pass | прогоны 4 и 13 |
| Ассерты «молчит» умеют краснеть (на 2.6 зелёные по построению) | pass | прогоны 8 и 15 |
| Сценарий 1б — форма #65 (поле переведено) и обещание названы; README и доставленный молчат; `report` читает то же | pass после починки | прогон 9 (первая сборка молчала) → `bugs/117` → прогоны 11, 12 |
| Функциональный прогон — реальные тикеты поля, каждый вердикт против текста тикета | pass | прогон 16; до починки — прогоны 9 и 18 |
| Сценарий 1в — «NOT YET + адрес issue»: `check` называет, `report` отказывает, `gh` не зван; красный на первой сборке оси и на 2.6 | pass после суда | прогон 22 (REFUTED: любой URL был уликой) → прогоны 23–29 |
| Сценарий 2 — пары «signal ↔ …» (AGENT_GUIDE оба слоя · `/report-bug` оба слоя · ядро · судья · дословное имя поля) | pass | прогоны 5, 10, 23 (прогон 2 — красный на переносе команды) |
| Полигон целиком на финальной поставке | pass | прогон 17 |

## 5. Найдено

- **S2 — `bugs/117`: первая сборка оси судила только слова `not yet` и молчала на всех 15 строках доставки, которые машина не читает** — переведённое имя поля (в этой форме накануне #65 были закоммичены ждущие тикеты: «Сигнал в исток: ждёт отправки», git того развёртывания `c68466b`; сегодня все восемь таких тикетов несут адрес issue), обещание или ссылка на себя вместо номера, строка пропущена. Пойман функциональным прогоном по реальному состоянию (прогон 9), починен до коммита: разрешающий список и одно чтение на `report` и `check` (прогоны 10–18)
- **суд чистым контекстом (прогон 22) — REFUTED на двух заявлениях, двенадцать каверз; всё починено до коммита:** (1) улика доставки была шире наблюдения — любой URL: «NOT YET — a recurrence of https://…/issues/37» молчал и не отправлялся → улика — только issue, «NOT YET + issue» неоднозначно и названо обеими командами (сценарий 1в); (2) черновик ответа #65 называл порядок предложений тикета и число тикетов второго развёртывания неверно → переписан по наблюдению; (3) висящий указатель «step 4» / «шаг доставки» в `AGENT_GUIDE` обоих слоёв и записях 2.7 → шаг заведения; (4) охота судьи «Signal filed, not delivered» знала одну строку предупреждения из двух → обе; (5) строка опроса замка в таблице запуска `/owner-reviews` могла висеть вечно → «исчез или pid не работает»; (6) серверная половина самопроверки окна IW не наблюдалась → два ассерта `s22` (отчёт IW, прогоны 14–15); (7) ассерт «report читает то же» был зелёным на старых сборках → переименован, «одно чтение» доказывает случай 1в; (8) в форензике `bugs/116` не хватало пятого показа страницы (утро 09:34, сессия 59) → строка и второй путь класса; (9) преполёт тишины снимает файл целиком одним `quietEnv(` и не видит импорт в процессе → названо в GAP; (10) «15 недоставленных» — завышено → «15 без машинно-читаемого состояния»; (11) вердикт отчёта IW называл снятую находку → переписан; (12) мелочи — сценарий 1б в `plans/100`, «два мутанта» → три, перечень охот 2.7 в шапке `/fable-judge`
- преполёт немой команды (`bugs/61`) поймал новый ассерт «одно чтение», судивший `check` через переменную (прогон 28) → разделён на два
- повторный суд (прогон 31) — VERIFIED WITH CAVEATS, три новые каверзы, все починены до коммита (прогон 32): (A) форензика `bugs/116` — утренний показ записан наблюдением EXP-0126, пятая причина «известный урок без механизма» (урок описал класс за тринадцать часов до ночи, механизм назвал и отложил в `ideas/30` п. 15; сессия 63 его не вспомнила), EXP-0126 и EXP-0132 связаны; (B) `#NN` — улика только самим значением строки или после слов origin/issue, ассерт «#2 в тексте обещания» красный на второй редакции; (C) ассерт «одно чтение, check» судит код 0
- оракул первой редакции `sd-real-state.mjs` повторял регэксп ядра — строка-итог «check agrees with every real ticket paragraph» была зелёной по построению и ничего не доказывала; дефект увиден только чтением напечатанных абзацев; вторая редакция печатает вердикт рядом с текстом тикета без оракула (прогон 16)
- ассерты «после доставки молчит» и «anonymous молчит» зелёные на ядре 2.6 по построению — красный на 2.6 доказывал только первый ассерт; закрыто мутантами (прогоны 8 и 15)
- пара «wrapper» не нашла `kaif-core.mjs report` — команда в `AGENT_GUIDE.md` была перенесена на две строки; перенос убран (прогон 2)
- преполёт `bugs/59` поймал фиксированное имя каталога в первой редакции `quietEnv()` (починка `bugs/116`, прогон 6) — каталог перенесён к каталогу node, полигон зелёный (прогон 7)
- ожидание мутанта M3 было узким — скрипт, не поставка (прогон 14)
- факты поля, не дефекты этой работы: в field-1 — шесть тикетов `NOT YET` (три — класс #37 с 2026-08-28, известны по №86) и три без читаемого состояния; в field-2 — двенадцать тикетов без машинной строки (восемь с переведённым полем, четыре старых); после обновления до 2.7 их `check` назовёт каждый

## 6. Следы

- логи — скретчпад сессии: `build9.log` (красный) · `build10.log` · `s17.log` · `s17-red.log` · `polygon5.log` (красный) · `polygon6.log` · `sd-mutants.log` · `sd-real-state.log` · `build11.log` · `s17-v2.log` · `s17-v2-blacklist.log` · `s17-v2-dist26.log` · `sd-mutants-v2.log` · `sd-mutants-v3.log` · `s17-M1-tracking.log` · `s17-M2-delivered.log` · `s17-M3-numbered.log` · `sd-real-state-v2.log` · `polygon7.log` · `sd-real-state-blacklist.log` · `counters-sd.log` · `sandbox-run-selftest.log` · `quiet-probe-mutants.log` · `polygon8.log` · `build12.log` · `s17-v3.log` · `s17-v3-blacklist.log` · `s17-v3-dist26.log` · `sd-mutants-v4.log` · `sd-real-state-v3.log` · `polygon9.log` (красный) · `mute-guard.log` · `s17-v5.log` · `s17-v5-blacklist.log` · `s17-v5-dist26.log` · `sd-mutants-v5.log` · `polygon10.log`; суд — каталог `judge/`
- проба и скрипты — скретчпад `sd0-probe.mjs`, `sd-mutants.mjs`, `sd-real-state.mjs`, `twins-fields.mjs`; красные версии — `dist26/`, `dist-blacklist/`, `dist-M1-tracking/`, `dist-M2-delivered/`, `dist-M3-numbered/` (тела доказательств перенесены в `s17`; скрипты в дереве не остаются — прецедент IW/CL)
- временные корни красных прогонов `s17` оставлены инструментом (`%TEMP%\kaif-sbx-report-*`, пути — в логах) и убираются после разбора

## 7. Вердикт

pass — сценарии 1, 1б, 1в и 2 `plans/109` закрыты наблюдением: проба до кода красная и после — зелёная; свод `s17` красный на четырёх названных сломанных версиях и на мутантах; функциональный прогон по 46 реальным тикетам поля прочитан построчно — у каждого молчащего адрес issue и ни одного «not yet»; суд REFUTED (прогон 22) → починки → повторный суд VERIFIED WITH CAVEATS (прогон 31) → три каверзы починены (прогон 32); полигон «all 26 suites green» (прогон 33). Находки S2 — о самой оси (`bugs/117`): поймана этим прогоном и судом, починена до коммита.
