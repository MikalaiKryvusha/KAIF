# Отчёт прогона — эпик CB «Бюджет канона», машинная половина (`plans/95`, критерии 1–4)

**Создан:** 2026-09-18 11:22 +03:00 · **Прогонял:** агент (Claude Opus 5) в отдельном git worktree
`.claude/worktrees/agent-a6d80db2fc349adbd`, ветка `worktree-agent-a6d80db2fc349adbd` от `3d57c09` ·
**Версия/сборка:** KAIF 2.6 в дереве 2.7-в-работе; `dist` пересобран (182 блока, 798 модулей).

## 1. Работа

Проверялась машинная половина эпика CB (`plans/95`): (1) бюджет размера документов ядра
перечитывания судится по СОБСТВЕННЫМ строкам проекта, а предупреждение несёт адрес выноса по
документу; (2) смесь языков в навыках судится по ДОЛЕ токенов чужой письменности, а не по единичному
вхождению письменности; (3, вторая половина) `install` печатает строку успеха РАНЬШЕ строки о
неполном языковом пакете, и та несёт указатель «что доложить»; (4) `check --gate-budgets` — дверь
ритуала закрытия: код 1 на документе выше бюджета по собственным строкам. Базис — четыре критерия
приёмки `plans/95` (сценариями) и критерий 10 `plans/100`; источники боли — origin issues #43/#45
(бюджет считает приехавший канон) и #71 (аудит владельца: «три документа ядра выше бюджета,
предупреждение печатается неделями и исполнено один раз»), разобранные в `researches/30` §2л.
Набора тест-кейсов отдельным документом у эпика нет — его роль играют сценарии критериев `plans/95`,
и каждый ассерт свода адресован критерию по номеру прямо в своём имени.

## 2. Контур

Часть системы — команда `check` и хвост команды `install` развёрнутого ядра
(`framework/installer/KAIF-CORE.mjs` → `dist/KAIF-CORE.mjs` → `.kaif/kaif-core.mjs` копии).
Стенд — РАЗВЁРНУТАЯ копия из `dist` во временном корне ОС (`tools/lib/temp-root.mjs`, корень уникален
по построению), развёртывание `install --lang ru`, Windows 11, Node из `process.execPath`.
Шов старого ядра — `KAIF_DIST` на распакованный ВНЕ репозитория `dist` тега `v2.6`
(`git show v2.6:dist/…`, пять файлов). Мутанты — на КОПИЯХ `dist`, никогда на рабочем дереве.

**РЕАЛЬНЫЙ МИР: накоплено · данные и техника · путь.** Продакшена у этой правки два: полевые
развёртывания KAIF на диске владельца и сам исток.
- *Полевые развёртывания* — **проверено с реальным состоянием**: файлы, названные манифестом двух
  развёртываний (псевдонимы `project B` — KAIF 2.5, `lang: ru`; `project E` — KAIF 2.5, `lang: ru`,
  `i18n: translated`), скопированы во временное дерево по 243 файла и прогнаны НОВЫМ ядром.
  Исходники только на чтение: sha256 всех 243 файлов каждого взят до и сверен после — **изменений 0**.
  Это НЕ путь владельца (`check` в его собственном дереве после обновления на 2.7) — это его
  состояние на моём стенде, и так оно и названо в блоке `@guard doc-budgets` (`ON-REAL-PATH: NOT YET`).
  Вторая граница того же прогона: сверка чисел РЕАЛИЗУЕТ ЗАНОВО тот же алгоритм (`splitModules` +
  пара «сигнатура, sha»), поэтому ловит ошибку монтажа в ядре и НЕ ловит ошибку самого алгоритма.
- *Исток* — **не проверено, и это не пробел, а свойство**: команда гейта у истока НЕИСПОЛНИМА по
  построению (здесь исходник, а не развёртывание), проба 11:07 ниже.

## 3. Прогоны

Двадцать девять прогонов 2026-09-18; метка каждого снята пробой `date '+%Y-%m-%d %H:%M %z'` в том же
вызове, что и прогон. Прогоны 1–23 — работа; 24 — судья; 25–29 — переисполнение после его каверз.

| # | Момент | Команда | Код / исход |
|---|---|---|---|
| 1 | 2026-09-18 10:51 +03:00 | `wc -l AGENT_GUIDE.md STATUS.md GOAL.md MASTER_PLAN.md PHILOSOPHY.md TESTING_FRAMEWORK.md BUG_FIXING_FRAMEWORK.md REQUIREMENTS_FRAMEWORK.md PROJECT_STRUCTURE_EXTERNAL_MAP.md` | 0 · нулёвка истока: 1314 / 300 / 36 / 404 / 260 / 304 / 212 / 223 / 162 |
| 2 | 2026-09-18 10:51 +03:00 | `node <scratch>/probe-mix.mjs .` | 0 · 37 навыков ru-слоя истока: 29 с кириллицей — доля чужих токенов **0,003 … 0,130**; 8 английских — **1,000**; один (`kaif-remove`) — **0,993**. Порог взят из этого разрыва |
| 3 | 2026-09-18 10:52 +03:00 | `node <scratch>/probe-field.mjs <шесть соседей>` | 0 · только чтение: `project B` `AGENT_GUIDE.md` **1661 / 1200** при 30 модулях в срезе; `project E`/`project F` — `i18n: translated` |
| 4 | 2026-09-18 10:55 +03:00 | `node <scratch>/probe-deploy.mjs .` | 0 · живая проба всех четырёх критериев на развёрнутой копии ДО свода (см. §4) |
| 5 | 2026-09-18 10:59 +03:00 | `node tools/sandbox/s16-doc-budgets.mjs` | 1 → 0 · 38 из 40, затем «all 40 checks green» (первый красный — пустой файл бандла вместо удаления, починено) |
| 6 | 2026-09-18 11:00 +03:00 | `KAIF_DIST=<dist v2.6> node tools/sandbox/s16-doc-budgets.mjs` | 1 · «23 of 40 check(s) failed» — и ОДИН ассерт критерия 1 оказался зелёным по чужой причине (см. §5) |
| 7 | 2026-09-18 11:00 +03:00 | `KAIF_DIST=<dist v2.6> node tools/sandbox/s16-doc-budgets.mjs` | 1 · «24 of 40» после починки того ассерта |
| 8 | 2026-09-18 11:01 +03:00 | `node <scratch>/cb-mutants.mjs .` | 0 · четыре мутанта предикатов на копиях `dist`: 2 / 5 / 2 / 4 адресных красных |
| 9 | 2026-09-18 11:01 +03:00 | `node <scratch>/cb-mutants.mjs .` | 0 · 3 / 5 / 2 / 4 после усиления ассерта об арифметике «свои + приехавшие» |
| 10 | 2026-09-18 11:02 +03:00 | `node <scratch>/cb-realstate.mjs . <project B> <project E>` | 0 · **функциональный прогон по копиям реального состояния**; напечатанные числа сошлись 9 из 9 в каждом; НАЙДЕНА ложь в собственной формулировке (§5) |
| 11 | 2026-09-18 11:03 +03:00 | `node tools/build-framework.mjs` | 0 · 182 блока, 798 модулей |
| 12 | 2026-09-18 11:03 +03:00 | `node tools/sandbox/s16-doc-budgets.mjs` | 0 · «all 43 checks green» (+3 ассерта ветки owner-seeded) |
| 13 | 2026-09-18 11:04 +03:00 | `node <scratch>/cb-realstate.mjs . <project B> <project E>` | 0 · переисполнение после починки: обе фразы верны на реальном состоянии |
| 14 | 2026-09-18 11:04 +03:00 | `node <scratch>/probe-mix.mjs <project B>` | 0 · три навыка циклов — английские тела с ДВУМЯ кириллическими токенами (0,997 … 0,998) |
| 15 | 2026-09-18 11:07 +03:00 | `node dist/KAIF-CORE.mjs check --gate-budgets` (из корня истока) | `✖ neither .kaif/install/KAIF-CORE-BUNDLE.md nor .kaif/deploy-manifest.json found — is KAIF deployed here?` |
| 16 | 2026-09-18 11:07 +03:00 | `node dist/KAIF-CORE.mjs check --bundle dist/KAIF-CORE-BUNDLE.md --gate-budgets` | `✖ MISSING or empty: .kaif/KAIF_REFERENCE.md` (и далее) — смерть задолго до блока бюджетов |
| 17 | 2026-09-18 11:09 +03:00 | `node tools/counters-guard.mjs` · `node tools/questions-guard.mjs` · `node tools/sandbox-mute-guard.mjs` | счётчики 0 (50 зеркал) · вопросы 0 · немых команд 2 → починены → 0 |
| 18 | 2026-09-18 11:13 +03:00 | `npm run test:core` | 1 · «1 of 27 suites FAILED — s10» (чужой свод цитировал СТАРУЮ формулировку предупреждения побайтно) |
| 19 | 2026-09-18 11:17 +03:00 | `node tools/sandbox/s10-l4-audit-noise.mjs` | 0 · «all green» после правки ассерта S5 (обоснование — в сообщении коммита) |
| 20 | 2026-09-18 11:17 +03:00 | `npm run test:core` | 0 · **«sandbox suite: all 27 suites green»** |
| 21 | 2026-09-18 11:20 +03:00 | `KAIF_DIST=<dist v2.6> node tools/sandbox/s16-doc-budgets.mjs` · `node <scratch>/cb-mutants.mjs .` | 1 · «25 of 43 check(s) failed» · пять мутантов → 3 / 5 / 2 / 4 / 2, невидимых нет |
| 22 | 2026-09-18 11:34 +03:00 | `node <scratch>/probe-gate-green.mjs .` | 0 · вторая сторона двери: `gate on a healthy deployment: exit 0`, строк о бюджете **0** |
| 23 | 2026-09-18 11:35 +03:00 | `node <scratch>/probe-strip.mjs` | 0 · границы очистки кода; **НАЙДЕНО:** незакрытый огороженный блок не снимается → доля 0,800 на фикстуре с одной русской фразой |
| 24 | 2026-09-18 11:54 +03:00 | `/fable-judge` субагентом с чистым контекстом (77 вызовов инструментов) | **VERIFIED WITH CAVEATS** · десять каверз, см. §5 |
| 25 | 2026-09-18 11:57 +03:00 | `node tools/build-framework.mjs` · `node tools/sandbox/s16-doc-budgets.mjs` | 0 · пересборка (182 блока, 798 модулей) · «**all 44 checks green**» после починок |
| 26 | 2026-09-18 11:57 +03:00 | `KAIF_DIST=<dist v2.6> node tools/sandbox/s16-doc-budgets.mjs` · `node <scratch>/cb-mutants.mjs .` | 1 · «**27 of 44** check(s) failed» · пять мутантов → **3 / 5 / 2 / 4 / 2**, невидимых нет |
| 27 | 2026-09-18 11:58 +03:00 | `npm run test:core` | 0 · **«sandbox suite: all 27 suites green»** на ПЕРЕСОБРАННОМ дереве (гейт свежести `dist` пройден) |
| 28 | 2026-09-18 12:00 +03:00 | `node framework/tools/kaif-guard-lint.mjs check …` · `counters-guard` · `sandbox-mute-guard` · `kaif-testrun-lint check` · `kaif-attribution-lint check` | 0 · 5 блоков/0 · 50 зеркал/0 · 422 команды/0 немых · 15 отчётов/0 · new 0 |
| 29 | 2026-09-18 12:01 +03:00 | `node <scratch>/cb-realstate.mjs . <project B> <project E>` | 0 · переисполнение после смены ключа пары: **сверено напечатанных чисел 3 и 4, обоюдно молчащих 6 и 5, расхождений 0**; sha 243 файлов каждого — изменений 0 |

`<scratch>` — `C:\Users\krinik\AppData\Local\Temp\claude\…\scratchpad`; `<project B>` и `<project E>` —
пути полевых развёртываний, называемые здесь псевдонимами (правило приватных имён).

## 4. Проверки

Hygiene: свод `s16` **44/44** зелёных · шов старого ядра `KAIF_DIST=<dist v2.6>` → **27 из 44** красных
адресно · **пять** мутантов предикатов на копиях `dist` → 3 / 5 / 2 / 4 / 2 красных, невидимых нет ·
полигон `npm run test:core` → **all 27 suites green** (на ПЕРЕСОБРАННОМ дереве) · `kaif-guard-lint
check` по ядру → 5 блоков, 0 находок · `counters-guard` 50 зеркал сверены · `sandbox-mute-guard`
немых команд нет (422) · `kaif-attribution-lint` new 0 · `kaif-testrun-lint` 0 · `questions-guard` 0 ·
`check-framework` OK · `build-framework` EXIT 0 · **судья-субагент с чистым контекстом — VERIFIED WITH
CAVEATS, десять каверз, все закрыты до коммита (§5)**.

Functional run: команда `node .kaif/kaif-core.mjs check` и `check --gate-budgets` пройдена ПУТЁМ
АГЕНТА ПРОЕКТА (так этой командой ходят: развернули, поработали, закрываешь чат) на копиях реального
состояния ДВУХ полевых развёртываний KAIF 2.5 — `project B` и `project E` — во временном дереве
(по 243 файла, названных их собственными манифестами). **Прочитано:** stdout+stderr обеих команд
построчно; **напечатанных чисел собственных строк сверено с посчитанными здесь же (из `wc -l` файла и
из `moduleShas` их СОБСТВЕННОГО манифеста) — 3 у `project B` и 4 у `project E`, расхождений 0**;
остальные шесть и пять документов молчали С ОБЕИХ сторон — это согласие двух молчаний, и уликой того
же веса оно не является (каверза судьи, счёт разделён). Код возврата `check` = 0 и
`check --gate-budgets` = 1 с перечнем документов; sha256 всех 243 исходных файлов каждого сверены до
и после — **изменений 0**. Плюс путь `install --lang ru` на свежей развёрнутой копии, прочитан
порядок строк вывода.
**Чего этот прогон НЕ делал:** он не ходил по дереву владельца — он ходил по КОПИИ; обновление
полевого проекта на 2.7 и его собственный `check` не исполнялись. И сверка чисел РЕАЛИЗУЕТ ЗАНОВО тот
же алгоритм, поэтому ловит ошибку монтажа в ядре, но не ошибку самого алгоритма.

| Кейс | Статус | Наблюдение |
|---|---|---|
| Критерий 1 · приехавший канон не в счёте | pass | развёрнутая копия: `AGENT_GUIDE.md` 1401 строка при бюджете 1200 → **ни одного предупреждения**; на ядре 2.6 та же фикстура предупреждает |
| Критерий 1 · собственные строки названы | pass | `⚠ AGENT_GUIDE.md: own lines 1332 of budget ~1200 (2520 lines on disk, 1188 of them arrived with KAIF and are not counted)`; 1332 + 1188 = 2520 = `wc -l` |
| Критерий 1 · адрес выноса по документу | pass | не-STATUS → `the chronicle PROJECT_HISTORY.md · researches/ · a house-rules file`; STATUS → `the chronicle PROJECT_HISTORY.md (move closed history VERBATIM — the /end-chat-soft bonsai trim)` |
| Критерий 1 · документ вне девятки | pass | `PROJECT_HISTORY.md` раздут на 1500 строк → тишина; ровно два предупреждения на два раздутых документа ядра |
| Риск (а) · файл переведён целиком | pass | `PHILOSOPHY.md` с переписанными заголовками → `own lines = wc -l` и фраза `translated wholesale — arrived canon cannot be told from your own lines` |
| Риск (а) · owner-seeded документ | pass | `MASTER_PLAN.md` той же формы → `all of them yours — this is an owner-seeded document whose shipped skeleton the project wrote over`; слова `translated wholesale` в его строке НЕТ |
| Запасная ветка · среза модулей нет | pass | после удаления бандла и `moduleShas` из манифеста → `no deployed module cut for this file — every line counts as yours`, число = `wc -l` |
| Критерий 2 · один английский токен | pass | RU-навык с одним `README` в прозе — в строке смеси НЕ назван |
| Критерий 2 · половина прозы английская | pass | `zz-mixed (50 % foreign)` в строке `⚠ language mix: … are a MIX — at or above 35 % …` |
| Критерий 2 · риск (б), код не проза | pass | RU-навык с командами, путями и английским код-блоком не назван; мутант «порог 0» его краснит |
| Критерий 2 · английское тело с кириллицей | pass | `zz-stray (99 % foreign)` назван; на ядре 2.6 такая форма невидима обеим строкам |
| Критерий 2 · прежняя строка жива | pass | `⚠ language mix: N of M skills are English (language: ru)` печатается отдельно и прежними словами |
| Критерий 3 · порядок строк install | pass | `deployed mechanically` на позиции 13 114, `INCOMPLETE BY DESIGN` — ниже; на ядре 2.6 наоборот (24 против 13 114) |
| Критерий 3 · указатель «что доложить» | pass | `↳ WHAT TO REPORT: … bugs/KAIF/NN_*.md … node .kaif/kaif-core.mjs report … github.com/…/issues` + граница «is not a defect — do not report it» |
| Критерий 4 · гейт НЕ закрывается на здоровом | pass | развёртывание внутри бюджетов → `check --gate-budgets` exit 0, строк о бюджете 0, `✅ manifest satisfied` (гейт, краснеющий всегда, — сломанный гейт) |
| Критерий 4 · гейт закрывается | pass | `check --gate-budgets` → exit 1 **и в выводе нет `unknown flag`** (то есть код от двери, а не от непонятого флага), `✖ AGENT_GUIDE.md: own lines 1332 of budget 1200 → <адрес>`, `✖ STATUS.md: …`, итог называет число документов |
| Критерий 4 · без флага прежнее поведение | pass | `check` → exit 0 при тех же предупреждениях |
| Критерий 4 · bugs/33 не сломан | pass | `check --no-such-flag` → exit 1, `✖ unknown flag for check: --no-such-flag` + `refusing to run check` |
| Критерий 4 · исток | **blocked** | команда неисполнима у истока по построению (прогоны 15–16); названа замена — `wc -l` девяти документов против девяти чисел `DOC_BUDGETS`, прогон 1 |
| Реальное состояние · `project B` | pass | `AGENT_GUIDE.md` 1661 строка, 596 приехали → собственных **1065 при бюджете 1200** → ядро 2.7 МОЛЧИТ там, где 2.6 предупреждало; гейт закрыт тремя owner-seeded документами (1944/200, 1995/300, 466/300) — три напечатанных числа сверены поимённо, шесть документов молчали с обеих сторон |
| Реальное состояние · `project E` | pass | `i18n: translated`: `AGENT_GUIDE.md` 2468 → все собственные, фраза «translated wholesale» верна; `TESTING_FRAMEWORK.md` 554, приехали 75 → собственных 479 при 300; гейт закрыт четырьмя документами — четыре напечатанных числа сверены поимённо, пять молчали с обеих сторон |
| Реальное состояние · смесь языков | pass | `project B`: три навыка циклов — английские тела с ДВУМЯ кириллическими токенами (0,997 … 0,998) → названы смесью «100 % foreign»; старый предикат не видел их вовсе |
| Первая половина критерия 3 (`@fork` на странице релиза) | **skipped** | не работа этой ветки — шаг RL; `reports/RELEASE_NOTES_2.7.md` не существует |

## 5. Найдено

- **Ложь в собственной новой формулировке, найдена ФУНКЦИОНАЛЬНЫМ прогоном и исправлена до коммита
  (не заведена багом — дефект не дожил до дерева main).** `STATUS.md` / `GOAL.md` / `MASTER_PLAN.md` /
  `PROJECT_STRUCTURE_EXTERNAL_MAP.md` двух реальных развёртываний получили строку «translated
  wholesale», хотя ничего не переводилось: это owner-seeded документы, которые проект пишет сам и
  которые теряют сигнатуры шаблона в КАЖДОМ здоровом развёртывании. Одна форма, две причины. Ветка
  разделена по `OWNER_SEEDED`, арифметика та же, фраза честная; +3 ассерта в свод, +1 мутант.
- **Ассерт, зеленевший по ЧУЖОЙ причине (класс EXP-0127), найден шом 2.6 и починен до коммита.**
  Центральный ассерт критерия 1 («предупреждения НЕТ») проверял отсутствие только НОВОЙ формулировки
  `own lines` — на ядре 2.6 он проходил, хотя там стоит прежняя строка `N lines against its budget`.
  Теперь судится любая форма; красных на 2.6 стало 24 вместо 23.
- **Чужой свод цитировал старую формулировку побайтно.** `s10` (`S5`) ждал `STATUS.md: 260 lines`;
  правка CB1 меняет слова. Ассерт переписан под новые слова с ТЕМ ЖЕ обещанием и усилен вторым
  числом (`260 lines on disk`). Обоснование правки теста — в сообщении коммита.
- **Две немые команды свода** (`bugs/61`): результаты `check --gate-budgets` и `check` присваивались в
  `r`, а судились через копию (`const gate = r`) — преполёт назвал обе поимённо, переписано на прямое
  присваивание.

**Судья прогона (`/fable-judge`, субагент с чистым контекстом, 11:54) — VERIFIED WITH CAVEATS.**
Он переисполнил каждое несущее число независимо (сам распаковал `v2.6:dist/*` в свой каталог и сверил
sha с моим, сам прогнал свод, мутантов и функциональный прогон по тем же соседям) и подтвердил их;
ослабленных тестов не нашёл. Десять каверз, **все закрыты ДО коммита**:

1. **БЛОКЕР: `dist` не пересобран** после правки блока стража в 11:21 — полигон отказывался стартовать
   гейтом свежести, а отгружаемый `dist/KAIF-CORE.mjs` нёс СТАРЫЕ числа стража (24/43, «four
   mutants»). То есть заявление «all 27 suites green» на переданном дереве было неисполнимо.
   → пересборка 11:57, полигон переисполнен 11:58 (прогоны 25, 27).
2. **Ложно-зелёный ассерт — ЦЕНТРАЛЬНЫЙ у критерия 4.** `ok(gate.code === 1, …)` зеленел на ядре 2.6,
   где `--gate-budgets` — незнакомый флаг и `check` отказывает ТЕМ ЖЕ кодом 1: ассерт «дверь
   закрывается» проходил там, где двери нет. → `&& !/unknown flag/.test(gate.out)`; красных на 2.6
   стало 27 из 44. Второй такой же (документ вне девятки судил только новую формулировку) — тоже.
3. **Шапка свода противоречила своду:** «тремя мутантами» при пяти, «ДВЕ запасные ветки» при трёх.
   → шапка переписана.
4. **«9 из 9 сошлись» — шире наблюдения.** Счётчик прибавлял согласие и там, где ОБЕ стороны молчали.
   Реально сверенных НАПЕЧАТАННЫХ чисел — 3 и 4. → счёт разделён в самой пробе и во всех текстах,
   включая `ON-REAL-PATH` стража и черновик ответа в issue #71; заодно снято слово «независимо»:
   сверка реализует ТОТ ЖЕ алгоритм.
5. **Два исполненных прогона не были в отчёте** (`probe-gate-green` 11:34, `probe-strip` 11:35), и
   второй нашёл незаписанную границу: незакрытый огороженный блок не снимается и текст под ним идёт
   в счёт смеси. → прогоны 22–23 в таблице, граница — комментарием у `stripCodeSpans` и в риске (б)
   плана; половина двери «здоровое развёртывание — код 0» получила СВОЙ ассерт (44-й).
6. **Обоснование правки чужого теста обещано «в сообщении коммита», а коммита не было.** → блок
   «ОБОСНОВАНИЕ ПРАВОК ТЕСТОВ» в сообщении этого коммита.
7. **Девять чисел бюджета продублированы в RU-навык** — вторая копия чисел в файле, который вслух
   говорит «одно место чисел» (класс `counter-stale-in-prose`). → числа убраны, вместо них команда
   `grep -n "budget:" framework/installer/KAIF-CORE.mjs`.
8. **Ярлык ассерта смеси утверждал то, чего на 2.6 не было** («старый предикат краснел бы» — на самом
   деле он такую форму не видел вовсе). → ярлык переписан; в «Боль» плана добавлена поправка с
   прежним текстом на месте.
9. **Два байта NUL в поставляемом исходнике** (разделитель ключа пары) — из-за них `grep` по ядру
   отвечал `Binary file matches` и молча терял содержимое. → ключ без разделителя: sha (64 знака
   фиксированной длины) впереди сигнатуры; поведение то же, файл снова текстовый.
10. Мелкая непоследовательность отрицательного ассерта — закрыта вместе с каверзой 2.
- **Исток сам выше бюджета на четырёх документах ядра** (прогон 1, 11:08): `AGENT_GUIDE.md` 1314/1200 ·
  `STATUS.md` 300/200 · `MASTER_PLAN.md` 404/300 · `TESTING_FRAMEWORK.md` 304/300. Это не дефект
  правки, а её предмет; стрижка — главной сессии (идёт параллельно), строки в передаче.
- Дефектов, уехавших в дерево, — **ноль**.

## 6. Следы

- Свод: `tools/sandbox/s16-doc-budgets.mjs` (44 ассерта, шапка несёт формулу шва и пяти мутантов).
- Блок стража: `framework/installer/KAIF-CORE.mjs` → `@guard doc-budgets` (четыре поля, числа этого прогона).
- План с шагами и метками: `plans/95_epic89_CB_canon_budget.md` (CB0–CB6, три `FORK:`, решения `[ИИ]` 2–11).
- Передача интегратору: `plans/handoff_CB.md`.
- Скретчпад прогона (вне репозитория): `…\scratchpad\probe-mix.mjs` · `probe-field.mjs` ·
  `probe-deploy.mjs` · `probe-gate-green.mjs` · `probe-strip.mjs` · `cb-mutants.mjs` ·
  `cb-realstate.mjs` · `dist26\` (распакованный `dist` тега v2.6; судья распаковал свою копию в
  `judge26\` и сверил sha — пять файлов побайтно совпали).
- Временные деревья функционального прогона с полным выводом обеих команд — `check.out` в корне
  каждого (`C:\Users\krinik\AppData\Local\Temp\cb-real-*`); красные прогоны свода оставляют свой
  корень и печатают путь.

## 7. Вердикт

**partial.** Все четыре критерия закрыты машиной и прочитаны наблюдением, включая функциональный
прогон по копиям реального состояния двух полевых развёртываний; судья с чистым контекстом
переисполнил каждое несущее число и вернул VERIFIED WITH CAVEATS, все десять каверз закрыты до
коммита. `partial`, а не `pass`, по трём названным границам: (1) путь владельца — обновление
полевого проекта на 2.7 и его СОБСТВЕННЫЙ `check` — не исполнялся, копия его не заменяет
(`ON-REAL-PATH: NOT YET` в блоке стража), и сверка чисел реализует тот же алгоритм, а не независимый;
(2) у истока команда гейта неисполнима по построению, и критерий 4 на истоке остаётся `blocked` до
обёртки, которой эта ветка не заводила; (3) первая половина критерия 3 (фраза `@fork` на странице
релиза) — работа шага RL и здесь не делалась.
