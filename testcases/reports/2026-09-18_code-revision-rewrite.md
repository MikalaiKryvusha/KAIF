# Отчёт прогона — переписанный навык `/code-revision` исполнен на зоне `framework/hooks/` (KAIF 2.7, эпик CR, шаг CR5)

> Одиннадцатый отчёт прогона истока по собственной поставке; шаблон — `framework/templates/_testrun-report-template.md`
> (в поле — `.kaif/_testrun-report-template.md`), форма судится `node framework/tools/kaif-testrun-lint.mjs check`
> (`TESTING_FRAMEWORK.md` → «Исполненный прогон рождает отчёт», «Что значит слово „тест“»). Каталог по дате: имя файла и есть индекс.

**Создан:** 2026-09-18 09:25 +03:00 · **Прогонял:** агент — Claude Opus 5 (1M) в отдельном worktree
`agent-a3e6a3faf66ceda0c` (ревизоры, скептики и судья — субагенты чистым контекстом) ·
**Версия/сборка:** KAIF 2.7 (открыта), HEAD `df8110b`, build 557

## 1. Работа

Шаг CR5 плана `plans/63`: **функциональный прогон ПЕРЕПИСАННОГО навыка `/code-revision`** — текста, который
исполнитель переписал сам в сессии 65 (шаг CR3; таблица находок CR2 —
`reports/KAIF_AUDIT/2026-09-18_code-revision-skill-by-its-executor.md`, 13 мест переписано, 6 оставлено).
Проверялись ДВЕ вещи одновременно: (1) **исполним ли текст** — исполнитель шёл по шагам 0–5 буквально и
записывал каждое место, где остановился или не понял, что взять (§ 6 ниже — это второй результат CR5);
(2) **даёт ли он заявленный результат** — реальные находки на реальной зоне по контракту выхода
(`references/audit-report-template.md` §§1–8: скелет SUMMARY, FAMILY-документы, карточка из восьми полей,
карта покрытия, бюджет шума, серия, брифы ревизора §7 и скептика §8).

Зона выбрана малая и полностью прочитываемая — опциональный модуль поставки `framework/hooks/` (10 файлов,
564 строки) плюс его детерминированный слой `tools/sandbox/s14-refresh-hooks.mjs` (329) как носитель
заявлений. База сравнения — ПЕРВЫЙ прогон навыка в прежней редакции
(`reports/KAIF_AUDIT/2026-08-08_guards-machinery-suites_SUMMARY.md`: 13 агентов, ≈ 1,18 М токенов, три
зоны, 9 → 5).

## 2. Контур

Исток на Windows 11 Pro 10.0.26200, Node v24.15.0, git 2.43.0.windows.1, git-bash + PowerShell 5.1 Desktop.
Рабочее дерево — отдельный git worktree `D:\work\ai_sandbox\KAIF\.claude\worktrees\agent-a3e6a3faf66ceda0c`,
ветка `worktree-agent-a3e6a3faf66ceda0c`, fast-forward с `803ef45` до HEAD `main` `df8110b` (иначе зона
читалась бы без флипа маркера `[TESTED]` хука RS — прогон 17 отчёта `2026-09-18_hook-resume-word.md`).
Предмет — ПОСТАВКА: `framework/hooks/{session-start-refresh,prompt-refresh-timer,stop-status-guard,prompt-resume-word}.mjs`,
`settings-fragment.json`, `README.md`, четыре `sample-*.json`; инструмент наблюдения — свод
`tools/sandbox/s14-refresh-hooks.mjs` и шов `KAIF_DIST` (мутанты на КОПИЯХ `dist/`, сам `dist/` не тронут).
Все пробы хуков — на РАЗВЁРНУТЫХ копиях модуля в скретчпаде (`EXP-0010`), ввод — буфером или файлом
события, никогда строкой в argv (`EXP-0121`, `EXP-0135`).
**Правило тишины (`bugs/116`):** полигон целиком (`npm run test:core`) НЕ запускался — среди 26 сводов есть
своды контура, поднимающие страницы; запускался только свод зоны, окон и звука не поднимающий. Ни одного
окна, звука и внешнего вызова за прогон.
**REAL WORLD:** живой агентской системы, кроме Claude Code, на машине нет; ни один хук не наблюдался
подключённым в Cursor/Codex/Copilot/Antigravity — это названный предел и прошлого прогона модуля.
**Внешние вызовы БЫЛИ** (поправлено по судье; прежняя редакция этого абзаца утверждала «ни одного
внешнего вызова» — заявление шире наблюдения): два разведчика доков внутри скептиков S2 и S3 сделали
**31 `WebFetch` и 9 `WebSearch`**, все на `code.claude.com` (`hooks-reference.md`, `worktrees.md`,
`settings.md`, `sessions.md`) — то есть контракт Claude Code в этом прогоне **переснят живьём
2026-09-18**, и именно на него опираются находка F4 (`cwd` следует worktree и `cd`,
`${CLAUDE_PROJECT_DIR}` — нет) и опровержение находки про ключ cooldown (`session_id` — общее поле
каждого события, валидный UUID). Доки Cursor/Codex/Copilot/Antigravity НЕ переснимались — это
объявленный датированный долг таблицы девяти систем (2026-08-07).

**Уроки `EXPERIENCE.md`, снятые грепом ДО работы** (дописано по судье: прежняя редакция называла только
идентификаторы в скобках, без строк — а канон требует либо строку урока, либо «релевантных уроков нет»):
`EXP-0136` — инструкция наблюдателя верна как описание и неполна как инструкция там, где наблюдатель видел
дело сделанным «само собой» · `EXP-0042` — «у системы есть хуки» непортируемо: решает ОДНО поле вывода, и
одинаковые имена полей ≠ одинаковый контракт · `EXP-0039` — предикат наблюдается ИЗГОТОВЛЕНИЕМ (состарить
mtime, подкрутить поле-метку), а не ожиданием · `EXP-0040` — ритуал, доставляющий улику, попутно её
уничтожает · `EXP-0135` — триггер это слово И его ПОЛОЖЕНИЕ; вход хука с переводом строки подаётся файлом
события · `EXP-0121` — Bash-инструмент снимает один уровень экранирования, текст идёт через файлы ·
`EXP-0133` — зелено на своей фикстуре, молчит на реальном состоянии; оракул, повторяющий проверяемое,
ничего не доказывает · `EXP-0059` — адресаты мутации называются ДО прогона, страж различает ДВА свойства ·
`EXP-0007` — на Windows любой JSON-парс входа только BOM-терпимым ридером · `EXP-0010` — у производной
поверхности проверяется то же свойство, что у оригинала · `EXP-0076` — ревизоров режут по кластерам
заявлений. Каждый лёг в брифы ревизоров и скептиков строкой, а не идентификатором.

**Развилки прогона (`FORK:`, дописано по судье — существо было в § 6, формы канона не было):**
- `FORK: options <A: базовой линией взять новейший по имени 2026-08-14_r2_triage_SUMMARY.md | B: взять
  предыдущий 2026-08-08_guards-machinery-suites_SUMMARY.md и назвать расхождение остановкой> · price of
  error <A: прогон стартует без карты покрытия, «Пределов» и «что сменить» — то есть без трёх входов,
  которые шаг 0 у базовой линии запрашивает; B: формальное отступление от буквы шага> · consulted <шаг 0
  п. 1 навыка; оба файла по телу; reports/README.md о жанре>` → **DECIDED [AI]: B** (и названо S1).
- `FORK: options <A: один ревизор на зону по букве шага 1 | B: два ревизора, нарезка по кластерам
  заявлений> · price of error <A: одна пара глаз на 564 строки двух разных жанров — код и документы; B:
  риск дублирующих карточек и двойного расхода> · consulted <§7 справочника о кластерах; EXP-0076>` →
  **DECIDED [AI]: B**; улика решения — дублирующих карточек ноль, а третью ось увидел только исполнитель.
- `FORK: options <A: ставить kaif-fp только в карточку аудита | B: ставить и в шапку баг-дока истока> ·
  price of error <A: карточка и баг-док расходятся ключом; B: в истоке появляется поле, которого нет ни в
  одном из 117 существующих баг-доков> · consulted </report-bug (ветка bugs/KAIF); греп kaif-fp по bugs/>`
  → **DECIDED [AI]: A** (и названо S5).
- `FORK: options <A: имя SUMMARY по §1 навыка — датой | B: по жанру каталога reports/README — номером> ·
  price of error <A: расхождение с общим правилом reports/; B: шесть существующих файлов KAIF_AUDIT
  названы датой, и базовая линия следующего прогона ищется по маске даты> · consulted <§1 справочника;
  reports/README.md:5; ls reports/KAIF_AUDIT/>` → **DECIDED [AI]: A** (и названо S6).

## 3. Прогоны

Моменты сняты пробой `date`; где стоит `≈` — по соседней пробе.

| # | Момент | Команда | Код / исход |
|---|---|---|---|
| 1 | 2026-09-18 08:31 +03:00 | `git merge --ff-only main` в worktree | 0 — `803ef45..df8110b`, 18 файлов; зона встала на HEAD `main` |
| 2 | 2026-09-18 ≈ 08:36 +03:00 | `node tools/sandbox/s14-refresh-hooks.mjs` (детерминированный слой, шаг 0 п. 6) | 0 — **68 ✅ / 0 ❌** |
| 3 | 2026-09-18 ≈ 08:36 +03:00 | `node framework/tools/kaif-guard-lint.mjs check framework/hooks` | **3** — `SKIPPED — no @guard / @forensic / @fork markers in 5 file(s); nothing was linted` |
| 4 | 2026-09-18 ≈ 08:37 +03:00 | `node tools/check-framework.mjs` | 0 — 59 встроенных файлов, 797 модулей, объявленные пары сходятся |
| 5 | 2026-09-18 ≈ 08:41 +03:00 | замер effective-FP прошлого прогона: `ls bugs/ \| grep -E "^5[5-9]_"` + `git log --oneline --grep="bugs/NN" -i \| wc -l` (NN = 55…58) | **0 из 5** — все четыре баг-дока `DONE`, коммитов 6 / 2 / 5 / 4; число дописано в отчёт прошлого прогона |
| 6 | 2026-09-18 08:38 → 08:50 +03:00 | два РЕВИЗОРА субагентами чистым контекстом по письменным брифам §7 (файлы `brief-reviewer-A.md`, `brief-reviewer-B.md`): A — поведение четырёх скриптов, B — заявления документов и образцов | A: 5 карточек + строка покрытия; B: 6 карточек + строка покрытия; обе — с оговорёнными осями «ничего не найдено» поимённо |
| 7 | 2026-09-18 ≈ 08:47 +03:00 | дымовая проба README дословно: PowerShell `node .kaif/hooks/prompt-refresh-timer.mjs < /dev/null`; `Get-Command printf` | 1 — `ParserError: The '<' operator is reserved for future use.`; `printf: NOT FOUND in PowerShell` |
| 8 | 2026-09-18 ≈ 08:48 +03:00 | та же строка в git-bash + строка 40 README, извлечённая скриптом и исполненная ИЗ ФАЙЛА (`bash smoke4.sh`) | 0 — приказ таймера; 0 — приказ `/resume`, 891 знак |
| 9 | 2026-09-18 ≈ 08:51 +03:00 | проба маркера: `at` = +1 год · `at` = 2030-01-01 · контроль `at` = −5 ч | тишина · тишина · приказ «last refresh 300 min ago» |
| 10 | 2026-09-18 ≈ 08:53 +03:00 | проба `cwd`: событие с `cwd` = корень и `cwd` = `<корень>/src/deep` при СВЕЖЕМ маркере; страж STATUS в подкаталоге | тишина · **приказ «no refresh witness found this session»** · **тишина** (обе половины неверны) |
| 11 | 2026-09-18 ≈ 08:55 +03:00 | проба стража без `session_id` (два проекта, повтор) | блок · тишина · тишина (ключ глобален) — позже ОПРОВЕРГНУТО как дефект |
| 12 | 2026-09-18 ≈ 09:02 +03:00 | проба BOM и домена `source` (`probe-bom-and-source.mjs`) | `clear` без BOM → `trigger "ritual:/clear"`; **с BOM → `"compaction"`**; `resume`-хук с BOM → **тишина**; `startup`/`resume` → «was just compacted» |
| 13 | 2026-09-18 ≈ 09:03 +03:00 | мутант M1 на копии бандла (обмен `prompt-resume-word` ↔ `stop-status-guard` событиями) → `KAIF_DIST=<копия> node tools/sandbox/s14-refresh-hooks.mjs` | **0 — 68 ✅ / 0 ❌** (дефект: свод не видит) |
| 14 | 2026-09-18 ≈ 09:05 +03:00 | проба стража на фикстуре «грязь и коммит старше сессии на 72 ч» | блок: «this session changed the tree, but STATUS.md was last touched ~72 h ago» |
| 15 | 2026-09-18 ≈ 09:06 +03:00 | три мутанта образцов (`--emit cursorX` · чужой скрипт в строке · удаление `--emit copilot`) через `KAIF_DIST` | **0 — 68 ✅** · 1 — 65 ✅ / 3 ❌ · 1 — 66 ✅ / 2 ❌ |
| 16 | 2026-09-18 ≈ 09:06 +03:00 | мутант ячейки таблицы README (`❌ → ✅`) через `KAIF_DIST` | **0 — 68 ✅ / 0 ❌** |
| 17 | 2026-09-18 ≈ 09:09 +03:00 | проба «битый маркер + старый mtime» и счёт фикстур свода | приказ «last refresh 120 min ago»; фикстур битого маркера — 1, `utimesSync` — только на `STATUS.md` |
| 18 | 2026-09-18 ≈ 08:55 +03:00 | механическая сверка ВСЕХ цитат карточек скриптом по адресу (`verify-quotes.mjs`, 42 адреса) | **41 hit / 1 miss** — промах: вспомогательная цитата F10 (`:13-14` вместо `:12-13`) |
| 19 | 2026-09-18 08:56 → 09:08 +03:00 | ШЕСТЬ СКЕПТИКОВ субагентами чистым контекстом по письменным брифам §8 (`brief-skeptic-S1…S6.md`), по вердикту на КАЖДУЮ находку | 11 CONFIRMED · **1 REFUTED целиком** · 3 половины REFUTED · 5 сужений формулировок |
| 20 | 2026-09-18 ≈ 09:10 → 09:25 +03:00 | запись выхода по контракту: SUMMARY + четыре FAMILY-документа + четыре баг-дока | файлы § 6 «Следы» |
| 21 | 2026-09-18 09:24 +03:00 (проба `date` в том же вызове) | `node framework/tools/kaif-testrun-lint.mjs check` | 1 → `verdict-not-named`; после заполнения § 7 в 09:24 — **0, «11 report(s), 0 findings»** (переисполнено судьёй: тот же вывод) |
| 22 | 2026-09-18 09:25 +03:00 (момент — `meta.json` субагента; прежняя редакция строки называла «≈ 09:35 → 09:50», то есть момент ИЗ БУДУЩЕГО — поправлено по судье, класс `bugs/78`) | СУДЬЯ `/fable-judge` субагентом чистым контекстом над этим отчётом и всем выходом прогона (правила в промпте: без окон и звука, полигон целиком не запускать, базовую линию свода снимать БЕЗ `KAIF_DIST`) | вернулся 09:44; **VERIFIED WITH CAVEATS** — § 7а |
| 23 | 2026-09-18 09:45 → 09:48 +03:00 (пробы `date` в начале и в конце) | починки по десяти пунктам судьи (этот отчёт, SUMMARY, FAMILY-документ 4, `plans/63`, передача) + повторный `node framework/tools/kaif-testrun-lint.mjs check` | 0 — «11 report(s), 0 findings»; перечень починок — § 7а |

## 4. Проверки

**Hygiene:** `s14` 68/68 на нетронутом `dist/` · шесть мутантов на копиях бандла через шов `KAIF_DIST`
(обмен пары — зелёный · опечатка формы — зелёный · ячейка README — зелёный · чужой скрипт — 3 ❌ · удаление
флага — 2 ❌ · константный ключ cooldown у скептика — зелёный) · `check-framework` 0 · `counters-guard` 0
(50 зеркал) · `lang-packs-guard` 0 · `kaif-guard-lint` по зоне — SKIPPED=3 (деклараций в зоне нет) ·
механическая сверка цитат **поля 1** всех 12 карточек скриптом (42 пары «адрес + фрагмент») — 41/42, промах
назван; **адреса в полях 2–8 скриптом НЕ сверялись** (их 48 сверх этих 42; судья пересчитал сам и нашёл
один неверный — `tools/kaif-canon-lint.mjs:66` без префикса `framework/` в поле 4 карточки F9, текст строки
верен; поправлено) · 36+ входов проб по четырём хукам (счёт и перечень —
наблюдение РЕВИЗОРА A своими пробами: пустой stdin, не-JSON, `[]`, `null`, `prompt` числом и массивом,
`--emit` пяти форм, флаги без значения; ни одного краша и ни одного ненулевого кода — его же измерение),
из них исполнитель повторил САМ: BOM у четырёх хуков, `source` четырёх значений, `cwd` корня и подкаталога,
маркер в шести состояниях · `doc-header-lint` — EXIT 1, 6 находок, **ни одна не указывает на файлы этого
прогона** (новые баг-доки и аудит-отчёты по шапкам чистые; метка `plans/63` без времени существовала уже
на `HEAD` — чужой долг; проверено судьёй независимо) · `attribution-lint` new 0, долг 45 ·
`scenario-lint` по четырём баг-докам 0.

**Functional run:** ИСПОЛНЕН — навык `/code-revision` в новой редакции пройден исполнителем на реальной
зоне истока, с реальными субагентами и реальным выходом: **шаги 0–4 буквально; шаг 5 — ЧАСТИЧНО**
(поправлено по судье: исполнены замер effective-FP и закрывающая запись «что сменить следующему прогону»,
а обязательные первым пунктом шага 5 урок `EXPERIENCE.md` с `Repro:` и точкой срабатывания и правки
крафт-рецептов сферы в этом прогоне НЕ исполнены — `EXPERIENCE.md` и рецепты worktree править не имел
права, урок передан черновиком в `plans/handoff_CR.md` § 3). Что прочитано и чем
кончилось: зона прочитана ЦЕЛИКОМ обоими ревизорами (10 файлов, 564 строки; плюс свод 329 строк) — карта
покрытия пофайлово в SUMMARY; поднято **12 карточек** (5 ревизор A + 6 ревизор B + 1 сам исполнитель — ось
`cwd` ниже корня, которую не назвал ни один ревизор); через шесть независимых скептиков (чистый контекст,
только карточка, обоснование ревизора не выдавалось) прошло **11**; каждую выжившую исполнитель
воспроизвёл САМ (прогоны 7–17), не полагаясь на неудачу опровержения; заведены четыре класс-документа
`bugs/118`–`bugs/121` (правок в зоне НЕ делалось — контракт навыка «verify, file, fix separately»);
effective-FP прошлого прогона замерен по процедуре §5 — **0 из 5**. Отдельно на РЕАЛЬНОЙ машине владельца
(не на фикстуре) исполнены командные строки дымовой пробы README и их вывод прочитан глазами:
ИСПОЛНИТЕЛЕМ — строка 36 в PowerShell 5.1 (`ParserError: The '<' operator is reserved for future use.`,
exit 1) и `Get-Command printf` (не найдено), обе строки в git-bash (приказ, exit 0; строка 40 подана из
файла); СКЕПТИКОМ независимо — та же строка 36 в `cmd` (`The system cannot find the path specified.`,
exit 1) и Windows-эквиваленты, которые работают (`< NUL` в `cmd`, `'' | node …` в PowerShell).

**Сравнение с базой (первый прогон навыка, 2026-08-08, прежняя редакция текста).**

| Ось | Прогон 1 (прежний текст) | Прогон 2 (переписанный текст) | Что дал переписанный текст |
|---|---|---|---|
| Базовая линия | «запись прошлой ревизии» без адреса; де-факто — этот же отчёт | шаг 0 п. 1 назвал адрес — новейший `*_SUMMARY.md` | **дал и промахнулся:** новейший по имени — не ревизия (§ 6, S1) |
| Брифы | 13 брифов писались руками, формы в навыке не было | §7 и §8 заполнены по скелету, ушли файлами; ревизоры и скептики не видели навык вовсе | **дал:** ни один субагент не запросил уточнений, ни один не съехал на стиль |
| Чтение ЦЕЛИКОМ | правило было прозой во вступлении | шаг 1: «читает зону ЦЕЛИКОМ, греп — не замена», строка покрытия пофайлово | **дал:** обе карты покрытия пришли пофайлово, с честным «не читали» |
| Скептик | 9 скептиков, по одному на находку | 6 скептиков по семействам, вердикт по каждой находке; «независимость по построению» | **дал:** 1 находка убита целиком, 3 половины, 5 сужений — и все сужения полезные |
| Воспроизведение | воспроизводил скептик | шаг 4: воспроизводит ИСПОЛНИТЕЛЬ, команда — поле 6 | **дал:** 11 из 11 выживших воспроизведены исполнителем отдельно от скептика |
| effective-FP | не замерялся (остался пунктом пула STATUS 41 день) | §5 дал процедуру: `DONE` баг-дока ИЛИ коммит называет семейство | **дал:** число получено за 4 минуты — 0 из 5 |
| Бюджет и расход | 13 агентов, ≈ 1,18 М токенов (число снято владельцем из интерфейса) | 11 агентов, ≈ 1 час стенного времени | **дал наполовину:** «token or time ceiling» исполнителю нечем измерить (§ 6, S3) |
| Норма отсева | 4 из 9 = 44 % | 1 из 12 = 8 % (+ 3 половины) | **вопрос, а не победа** — гипотеза и проверка записаны в SUMMARY «что сменить» |

| Кейс | Статус | Наблюдение |
|---|---|---|
| Шаг 0: базовая линия найдена по адресу навыка | **fail** (мягкий) | новейший `*_SUMMARY.md` — `2026-08-14_r2_triage_SUMMARY.md`, это НЕ ревизия: ни карты покрытия, ни «Пределов», ни «что сменить». Взят предыдущий — решением исполнителя |
| Шаг 0: детерминированный слой прогнан ПЕРВЫМ, его вывод — улика | pass | прогоны 2–4; выводы ушли в оба брифа дословно и в SUMMARY |
| Шаг 0: бюджет и правило остановки названы до старта | partial | ревизоры/скептики/потолок названы; токенный потолок измерить нечем — записан как время и агенты |
| Шаг 1: бриф §7 вместо навыка; зона читается целиком | pass | два брифа файлами; обе карты покрытия пофайлово |
| Шаг 2: нет цитаты — нет находки; отпечаток и аттестация | pass | 42 адреса сверены скриптом, 41 верен; `kaif-fp:` и `Dedup attestation:` в каждой карточке |
| Шаг 3: скептик независим, вердикт по умолчанию «не дефект» | pass | 6 субагентов чистым контекстом; 1 находка убита, 3 половины, 5 сужений |
| Шаг 4: воспроизводит исполнитель; `TWINS:` у каждой находки | pass | прогоны 7–17; строки `TWINS:` в четырёх баг-доках |
| Шаг 5: effective-FP по процедуре; запись для следующего прогона | **partial** (поправлено по судье) | 0 из 5; «что должен сменить следующий прогон» — шесть пунктов в SUMMARY; НО первый пункт шага 5 — урок `EXPERIENCE.md` с `Repro:` и точкой срабатывания — и правки крафт-рецептов не исполнены: оба файла worktree править не имел права, урок передан черновиком (`plans/handoff_CR.md` § 3) |
| Контракт выхода §§1–6 (SUMMARY + FAMILY + карточка 8 полей + карта + бюджет + серия) | pass | пять файлов `reports/KAIF_AUDIT/2026-09-18_hooks_*` |
| Правило тишины (окон и звука нет) | pass | полигон целиком не запускался; свод зоны окон не поднимает; ни одного окна за прогон |

## 5. Найдено

- `bugs/118` — стражи модуля судят КОПИЮ истины: фрагмент конфига вне оси имён событий (мутант обмена
  хуков событиями зелен 68/68) · форма вывода по подстроке · таблица девяти систем без стража. **S2.**
- `bugs/119` — состояние из непроверенного входа: `cwd` ниже корня (шум в каждый промпт у таймера и
  тишина у стража) · `at` в будущем глушит таймер · BOM обнуляет предикат (класс шире четырёх хуков —
  девять сайтов названы). **S2.**
- `bugs/120` — приказ утверждает ненаблюдённое: `source` ≠ `clear` объявляется сжатием (на двух
  безматчерных образцах самого модуля) · «this session changed the tree» без наблюдения сессии. **S2.**
- `bugs/121` — документ подключения обещает не то: дымовая проба неисполнима в PowerShell и `cmd` ·
  «каждый хук несёт cooldown» против своей же таблицы · `[TESTED]` шире свидетеля. **S2.**
- Опровергнуто (и это тоже результат): ключ cooldown стража STATUS — недостижимо ни на одной проводке,
  которой модуль обещает этот хук; выжившая дыра ПОКРЫТИЯ внесена строкой в `bugs/119`.

## 6. Следы

- `reports/KAIF_AUDIT/2026-09-18_hooks_SUMMARY.md` — сводка прогона (скелет §2 целиком).
- `reports/KAIF_AUDIT/2026-09-18_hooks_guard-judges-a-copy-of-truth.md` (F1–F3) ·
  `…_state-from-unchecked-input.md` (F4–F6) · `…_order-asserts-the-unobserved.md` (F7–F8) ·
  `…_opt-in-doc-promises-otherwise.md` (F9–F11) — карточки восьми полей.
- `bugs/118` · `bugs/119` · `bugs/120` · `bugs/121` — класс-документы.
- `reports/KAIF_AUDIT/2026-08-08_guards-machinery-suites_SUMMARY.md` — дописан раздел «Дописано
  2026-09-18 … effective-FP этого прогона замерен» (отчёт append-only; п. 4 его «что сменить» закрыт).
- Скретчпад сессии (мертвый, но названный): `brief-reviewer-A.md`, `brief-reviewer-B.md`,
  `brief-skeptic-S1…S6.md` (письменные брифы по §7/§8) · `verify-quotes.mjs` (сверка 42 адресов) ·
  `probe-*.mjs` (пробы предикатов) · `mutant-m1-v2.mjs`, `mutants-b3-executor.mjs`,
  `mutant-b4-executor.mjs` (мутанты на копиях) · `s14*.log`, `checkfw.log`, `guardlint.log`.
- `plans/handoff_CR.md` — тексты интегратору (CR6).

### Места навыка, где ИСПОЛНИТЕЛЬ остановился (второй результат CR5)

Семь мест. Это не «текст плох» — это места, где исполнитель класса Fable закрывает разрыв головой, а
модель послабее остановится или угадает.

1. **S1 — шаг 0 п. 1 находит НЕ ревизию.** Цитата: «**Find the baseline** *[mechanical]*: the newest
   `reports/KAIF_AUDIT/*_SUMMARY.md` IS the record of the last revision» (`SKILL.md:27-28`). Новейший по
   имени файл — `2026-08-14_r2_triage_SUMMARY.md`, сводная таблица триажа 104 вхождений фазы S: ни карты
   покрытия, ни «Пределов», ни «что сменить следующему прогону» — то есть ни одного из трёх входов,
   которые шаг у неё запрашивает. Шаг помечен *[mechanical]*, а механически даёт неверный ответ.
   **Правка:** назвать признак принадлежности (таблица методологии с осями и ревизорами / имя навыка в
   шапке) и что делать, когда новейший SUMMARY его не несёт.
2. **S2 — адреса детерминированного слоя названы для ПОЛЯ, не для истока.** Цитата: «`node
   .kaif/kaif-core.mjs check`, every `.kaif/tools/*` module (`check` and `selftest`)…» (`SKILL.md:47-49`).
   В истоке каталога `.kaif/` нет: поставка живёт в `framework/`, модули — `framework/tools/`, сводка —
   `tools/check-framework.mjs`. Плюс «every `.kaif/tools/*` module» — это 9 модулей × 2 вызова независимо
   от зоны, а правила «только те, чей охват пересекает зону» текст не даёт при собственном требовании
   бюджета. Взял три команды, пересекающие зону, и назвал это решением.
3. **S3 — потолок расхода, который исполнителю нечем измерить.** Цитата: «the token or time ceiling …
   The summary records what was actually spent (the origin's first run: 13 agents, about 1.2 M tokens…)»
   (`SKILL.md:36-39`). Счётчика токенов у агента нет — число первого прогона снято владельцем из
   интерфейса. Записал расход в агентах и стенном времени. **Правка:** назвать допустимую форму записи.
4. **S4 — «по ревизору на зону» против нарезки по кластерам.** Цитата: «One reviewer per zone, parallel
   where the harness allows» (`SKILL.md:57`) против §7: «a reviewer cut by claim clusters rather than by
   directory converged on the same defect from four sides — that convergence became evidence»
   (`references/audit-report-template.md:154-156`). Зона одна и малая — арифметика шага 1 даёт одного
   ревизора, а полевая улика хвалит кластеры (и EXP-0076 тоже). Взял двух на одну зону; текст такой формы
   не предусматривает. **Улика в пользу кластеров, снятая этим прогоном:** ревизор A и ревизор B НЕ дали
   ни одной дублирующей карточки, а третью ось (`cwd`) не увидел ни один — её поднял исполнитель.
5. **S5 — `kaif-fp` в истоке живёт по другому адресу.** Цитата: «with the feedback loop's own fingerprint,
   `kaif-fp: <surface> :: <symptom-class> :: v<major.minor>`… (`/report-bug`)» (`SKILL.md:84-86`). В
   `/report-bug` отпечаток и аттестация — принадлежность тикетов `bugs/KAIF/` ПОЛЕВЫХ развёртываний; в
   истоке баги ложатся прямо в `bugs/`, и ни один баг-док истока не несёт строки `kaif-fp:` как поле шапки
   (два упоминания прозой). Поставил отпечаток в карточку аудита и не стал добавлять в шапку баг-дока.
6. **S6 — два правила имени файла.** §1 справочника: `<date>_<scope>_SUMMARY.md`; `reports/README.md:5`:
   «По одному `NN_<имя>.md` на отчёт». Решил по прецеденту (все шесть файлов `KAIF_AUDIT/` названы датой).
   **Правка:** одна оговорка в §1 — «в `KAIF_AUDIT/` индекс — дата, а не номер».
7. **S7 — что значит `SKIPPED` детерминированного слоя.** `kaif-guard-lint check framework/hooks` → EXIT 3,
   «nothing was linted». Текст называет вывод слоя уликой, но не говорит, как читать третий код: «в зоне
   деклараций нет» — это улика ОТСУТСТВИЯ (кандидат на ось «обещание канона без механизма») или просто
   неприменимость инструмента? Отдал ревизорам и как улику, и как ось; ни один не завёл по ней карточку, и
   исполнитель тоже не стал — вопрос остался открытым и записан в «Пределы» SUMMARY.

Мест, где текст держал и никто не остановился, тоже назову — это результат: брифы §7/§8 (ни один из восьми
субагентов не запросил уточнений и ни один не съехал на стиль), контракт карточки из восьми полей (все 12
карточек пришли полными), правило «поля 6 и 7 читаются друг против друга» (ни одной карточки с
противоречием), процедура effective-FP (число получено за четыре минуты), пятый запрет про окна и звук
(исполнен буквально: полигон целиком не запускался).

## 7. Вердикт

**partial.** Навык исполним и даёт заявленный результат — прогон на реальной зоне дал 12 карточек, 11
выживших после независимых скептиков, четыре класс-документа и полный контракт выхода, — но `partial`, а не
`pass`, по трём названным причинам: (1) шаг 0 п. 1, помеченный *[mechanical]*, на реальном каталоге истока
даёт НЕ ревизию (кейс «базовая линия» — fail, § 6 S1), и правильный ответ исполнитель выбрал головой;
(2) заявленный контракт «token or time ceiling» исполнителю нечем измерить (кейс «бюджет» — partial,
§ 6 S3); (3) шаг 5 исполнен ЧАСТИЧНО — урок `EXPERIENCE.md` и правки крафт-рецептов остались за главной
сессией (worktree их править не имел права), и это поймал судья, а не исполнитель. Ни одна находка прогона
не наблюдалась в живой чужой агентской системе — это предел зоны, записанный в «Пределах» SUMMARY, а не
дефект навыка.

## 7а. Судья

**`/fable-judge`, субагент чистым контекстом, запущен 09:25 +03:00, вернулся 09:44 +03:00 (100 вызовов
инструментов). Вердикт — `VERIFIED WITH CAVEATS`.**

**Что судья переисполнил своей командой и получил ТО ЖЕ (14 из 14):** базовая линия свода без `KAIF_DIST`
(68/68, EXIT 0) · мутант обмена хуков событиями (68/68 зелёный) · мутант `--emit cursorX` (зелёный) ·
мутант ячейки README (зелёный) · КОНТРОЛЬ, что его собственный шов вообще краснеет — удаление
`--emit copilot` (EXIT 1, 2 ❌) · `at` в будущем (тишина · тишина · контроль-приказ) · `cwd` подкаталога
(обе половины) · BOM (`SessionStart:compact` вместо `clear`; четвёртый хук молчит) · `source=startup`,
`resume` и событие без поля · страж на 72-часовой грязи · битый маркер + старый mtime · мутант
`at = Date.now()` (68/68) · мутант константного ключа cooldown (68/68) · проба README в PowerShell, `cmd`
и git-bash · `kaif-guard-lint` SKIPPED=3 и `check-framework` 0 · effective-FP 0 из 5 с теми же числами
коммитов (6 / 2 / 5 / 4) · размеры зоны (564 и 329) · `TWINS:` в четырёх баг-доках с выборочной
перепроверкой их подмет. **Ни одна находка F1–F11 и ни одно «Измерено сегодня» не опровергнуты.**

**Что судья проверил по ЖУРНАЛАМ субагентов, а не по словам отчёта:** состав агентов (2 ревизора + 6
скептиков + 2 разведчика доков внутри S2/S3 + судья = 11 — строка «Расход» верна дословно); брифы —
письменные, со всеми девятью слотами §7 и всеми слотами §8, включая «ты видишь только карточку»; числа
карточек (5 + 6 + 1 = 12); и главное — что «скептик опроверг» не превратилось в «скептик согласился»
(дословные `REFUTED` в журналах S2, S1, S5). Отдельно отметил, что поправки скептиков к карточкам ВНЕСЕНЫ,
а не проглочены.

**Опровергнуто и сужено СЕМЬ заявлений о самом прогоне (бухгалтерия отчёта, не находки) — все починены:**
1. **REFUTED** «ни одного внешнего вызова»: их 31 `WebFetch` + 9 `WebSearch` на `code.claude.com` внутри
   двух разведчиков доков → § 2 переписан, и там же сказано, что контракт Claude Code переснят живьём.
2. **REFUTED** момент строки 22 «≈ 09:35 → 09:50» — момент ИЗ БУДУЩЕГО (класс `bugs/78`) → поставлен 09:25
   по `meta.json` субагента, источник назван; строка 21 получила момент пробы.
3. **REFUTED** две ссылки в пустоту («см. § 7» и «см. § 4 Судья», которого не было) → исход линта стоит
   прямо в клетке, а этот § 7а создан.
4. **СУЖЕНО** «шаги 0–5 исполнены БУКВАЛЬНО»: шаг 5 требует первым пунктом урок `EXPERIENCE.md` с `Repro:`
   и точкой срабатывания и правки крафт-рецептов — не исполнены → строка `Functional run:` и кейс шага 5
   (`pass` → `partial`) поправлены, причина названа.
5. **СУЖЕНО** «сверка ВСЕХ цитат»: сверялось поле 1 (42 пары); адреса полей 2–8 (их 48) скриптом не
   сверялись, и один из них был неверен → формулировка сужена, адрес починен (см. п. 7).
6. **СУЖЕНО** «рабочая копия чистая»: с 08:41 в дереве лежал дописанный замер effective-FP, и скептик S6
   вернул это замечанием, которое не доехало ни в один документ → строка состояния дерева в SUMMARY
   поправлена, замечание принято.
7. **СУЖЕНО** CR6 в `plans/63`: галочка стояла в 09:30, когда судья был только запущен → формулировка
   переписана, галочка — после этой записи.

**Починки судьи, внесённые в артефакты:** этот отчёт (§ 2 внешние вызовы и уроки/`FORK:`, строки 21–23
таблицы, `Functional run:`, кейс шага 5, строка сверки цитат, `doc-header-lint` в гигиене, § 7 и § 7а),
`reports/KAIF_AUDIT/2026-09-18_hooks_SUMMARY.md` (состояние дерева, строка сверки цитат, «Пределы» о
внешних вызовах), `reports/KAIF_AUDIT/2026-09-18_hooks_opt-in-doc-promises-otherwise.md` (адрес
`framework/tools/kaif-canon-lint.mjs:66`), `plans/63` (формулировка CR6), `plans/handoff_CR.md` (§ 7).
**Каветы, которые судья оставил как незакрытые:** счётчик «36+ входов проб» он не пересчитывал —
`UNVERIFIABLE` по числу, подтверждён по роду; и вердикт `partial` он назвал честным и не заниженным.
