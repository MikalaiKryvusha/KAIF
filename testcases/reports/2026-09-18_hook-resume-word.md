# Отчёт прогона — хук «слово resume в начале сообщения — приказ» и полигон истока на эпике RS (KAIF 2.7)

> Седьмой отчёт прогона истока по собственной поставке; шаблон — `framework/templates/_testrun-report-template.md`
> (в поле — `.kaif/_testrun-report-template.md`), форма судится `node framework/tools/kaif-testrun-lint.mjs check`
> (`TESTING_FRAMEWORK.md` → «Исполненный прогон рождает отчёт», «Что значит слово „тест“»). Каталог по дате: имя файла и есть индекс.

**Создан:** 2026-09-18 00:58 +03:00 · **Прогонял:** агент, сессия 65 — Claude Fable 5.1 (судья — субагент чистым контекстом) ·
**Версия/сборка:** KAIF 2.7 (открыта), HEAD `2b7e9e8` + рабочее дерево эпика RS (`plans/110`)

## 1. Работа

Эпик RS «Слово `resume` в начале сообщения — приказ исполнить `/resume` до работы» (`plans/110`, критерий 23 `plans/100`; слово
владельца №119). Четыре носителя: **(1)** хук `framework/hooks/prompt-resume-word.mjs` модуля refresh-hooks — первое слово промпта
`resume` / `/resume` / русское сокращение → приказ исполнить `/resume` целиком до остального сообщения, иначе тишина; **(2)** правило
«первое слово сообщения владельца — команда» в `AGENT_GUIDE` обоих слоёв рядом с «Формой обязательства», описание и тело навыка
`resume` обоих слоёв, ru-пакет, `/kaif-go` обоих слоёв; **(3)** строка в `CONTEXT_POINTER` установщика, `CLAUDE.md` истока, интро,
справочник, README, внешняя карта, запись 2.7 сборщика; **(4)** охота судьи «Resume word ignored (KAIF 2.7)». Базис — сценарии 1–3
`plans/110`; набор кейсов — проба ДО кода, свод `s14` (+6 ассертов), красные на ядре 2.6 и на мутанте бандла, девять пар
`check-framework`, стражи, полигон целиком.

## 2. Контур

Поставка: `framework/hooks/prompt-resume-word.mjs`, `settings-fragment.json`, `README.md` модуля, `framework/AGENT_GUIDE.md`,
`framework/skills/resume/SKILL.md`, `framework/skills/kaif-go/SKILL.md`, `framework/skills/fable-judge/SKILL.md`,
`framework/templates/languages/ru/skill-triggers.json`, `framework/installer/KAIF-CORE.mjs` (`CONTEXT_POINTER`), `framework/_intro.md`,
`framework/KAIF_REFERENCE.md`; обвязка: `AGENT_GUIDE.md`, `.claude/skills/resume/SKILL.md`, `.claude/skills/kaif-go/SKILL.md`,
`.claude/skills/fable-judge/SKILL.md`, `.claude/settings.json`, `CLAUDE.md`, `README.md`, `PROJECT_STRUCTURE_EXTERNAL_MAP.md`;
машинерия: `tools/sandbox/s14-refresh-hooks.mjs` (шов `KAIF_DIST`), `tools/check-framework.mjs` (пары), `tools/build-framework.mjs`
(запись 2.7), `tools/lang-packs.lock.json` (пины). Стенд — рабочее дерево истока на Windows 11 Pro 10.0.26200, Node v24.15.0,
git-bash; своды разворачивают копии поставки из `dist` во временные корни; ни одного окна, звука или внешнего действия. Красные
доказательства: `dist` релиза 2.6 (`git show v2.6:dist/…` → скретчпад `dist-2.6/`) · мутант бандла с предикатом `resumeXX`
(скретчпад `dist-mutant/`, 4 байта разницы). **Реального мира здесь нет:** живой приказ хука на пути владельца — первый промпт
следующего чата истока со словом `resume` (STATUS п. 0, порядок первого промпта — EXP-0040); до него — только стенд.

## 3. Прогоны

Моменты сняты пробой `date` в тот же вызов, что и команда (где `≈` — по соседней пробе).

| # | Момент | Команда | Код / исход |
|---|---|---|---|
| 1 | 2026-09-18 ≈ 00:35 +03:00 | `grep -c "prompt-resume-word" dist/KAIF-CORE-BUNDLE.md` (проба ДО кода) | 0 вхождений, в бандле три скрипта → ❌ RED (хука нет) |
| 2 | 2026-09-18 ≈ 00:41 +03:00 | `printf '{…"prompt":"resume\\nделаем эпик LP"}' \| node framework/hooks/prompt-resume-word.mjs` | тишина — ❌ ложный красный: Bash-инструмент снял уровень экранирования (EXP-0121), JSON пришёл битым; тот же промпт из файла события (прогон 3) — приказ |
| 3 | 2026-09-18 ≈ 00:43 +03:00 | `node framework/hooks/prompt-resume-word.mjs < <скретчпад>/event-resume-newline.json` | 0 — `event: UserPromptSubmit · len: 801`, текст начинается «KAIF: the owner's message OPENS with the word "resume" — that is an ORDER…» |
| 4 | 2026-09-18 ≈ 00:44 +03:00 | `node tools/build-framework.mjs` (первая сборка) | 1 — `check-framework FAILED`: «Cyrillic in an EN payload BODY» в шести файлах (регэксп и проза с `резюм…`) |
| 5 | 2026-09-18 ≈ 00:44 +03:00 | `node tools/lang-packs-guard.mjs --update-pins` → `node tools/lang-packs-guard.mjs` | 0 — «пины EN-описаний передвинуты (37)» → «lang-packs-guard OK — заморожено 8 пакетов … живые: ru» |
| 6 | 2026-09-18 ≈ 00:46 +03:00 | `node tools/build-framework.mjs` (после снятия кириллицы из прозы) | 1 — dist сгенерирован (181 блок, 793 модуля); `check-framework FAILED`: кириллица в строке 30 хука (мой `[TESTED]`-комментарий) |
| 7 | 2026-09-18 ≈ 00:46 +03:00 | `node tools/counters-guard.mjs` | 1 — 8 расхождений (README EN/RU, STATUS, AGENT_GUIDE: 180 → 181 блок, 792 → 793 модуля) → зеркала переписаны с вывода сборки |
| 8 | 2026-09-18 ≈ 00:47 +03:00 | `node tools/sandbox/s14-refresh-hooks.mjs` (новая генерация `dist`) | 0 — 68 ✅, среди них «доехал prompt-resume-word.mjs», «все четыре развёрнутых скрипта», шесть `s14 resume-word: …` |
| 9 | 2026-09-18 ≈ 00:47 +03:00 | `KAIF_DIST=<скретчпад>/dist-2.6 node tools/sandbox/s14-refresh-hooks.mjs` | 1 — 59 ✅ / 8 ❌: «доехал», «четыре скрипта во фрагменте», три «приказ», три «тишина» (HOOK-CRASH — файла нет) |
| 10 | 2026-09-18 ≈ 00:49 +03:00 | `KAIF_DIST=<скретчпад>/dist-mutant node tools/sandbox/s14-refresh-hooks.mjs` (бандл с `resumeXX` в предикате) | 1 — 64 ✅ / 3 ❌: ровно «resume» · «Резюм.» · «/resume» → приказ; три «тишина» ✅ |
| 11 | 2026-09-18 00:45:31 → 00:48:02 +03:00 | `node tools/build-framework.mjs; node tools/counters-guard.mjs; npm run test:core` (фон; сборка ещё красная строкой 30, `dist` сгенерирован) | counters 0; полигон 0 — «sandbox suite: all 26 suites green» |
| 12 | 2026-09-18 ≈ 00:50 +03:00 | `node <скретчпад>/fix-regex-escapes.mjs` → повтор прогона 3 и `printf '{"prompt":"Резюм. продолжаем"}' \| node …` | «строка 51 переписана; строк с кириллицей в файле: 0»; оба кириллических промпта → приказ, «дальше по плану» → тишина |
| 13 | 2026-09-18 ≈ 00:55 +03:00 | `node framework/tools/kaif-scenario-lint.mjs check plans/110_…md` · `kaif-attribution-lint.mjs check` · `node tools/doc-header-lint.mjs` | 0 · 0 · 0 — «3 scenario(s), 0 findings» · «new 0 · debt 45» · «requirements lint OK» |
| 14 | 2026-09-18 00:48:36 → 00:51:03 +03:00 | `node tools/build-framework.mjs && node tools/counters-guard.mjs && npm run test:core` (фон, окончательная поставка — регэксп экранирован) | 0 — «build OK» (check-framework зелёный, девять пар RS внутри); `counters exit=0`; «sandbox suite: all 26 suites green» |
| 15 | 2026-09-18 ≈ 00:56 → 01:09 +03:00 | судья `/fable-judge` субагентом чистым контекстом (Opus; 70 вызовов инструментов): переисполнил прогоны 3, 8–10, 13, 14, смоук README дословно, 14 кейсов хука, мутацию пары `check-framework` на копии репозитория, два своих мутанта («всегда приказ», «угадывает промпт») | **REFUTED** на двух заявлениях (см. § 5), остальное подтверждено; после починок — стоит как VERIFIED с живым наблюдением на следующем чате |
| 16 | 2026-09-18 ≈ 01:11 +03:00 | `cp framework/skills/fable-judge/SKILL.md .claude/skills/fable-judge/SKILL.md && cmp …` · `node tools/build-framework.mjs && node tools/counters-guard.mjs && node tools/sandbox/s14-refresh-hooks.mjs && npm run test:core` (фон, после починок по суду) | `cmp` — равны (01:10:50); сборка/счётчики/`s14`/полигон — <см. § 7> |
| 17 | 2026-09-18 08:12 +03:00 (сессия 66) | живой промпт владельца в новом чате истока: `resume` + перевод строки + `продолжаем` (не команда агента — путь владельца; проба `date` 08:12) | приказ хука `prompt-resume-word.mjs` стоял в контексте сессии рядом с приказом таймера освежения; процитирован в чате дословно ДО штампа маркера; `/resume` исполнен целиком; маркер `ritual:/resume` — `2026-09-18T08:13:04+03:00` |

## 4. Проверки

Hygiene: `s14` 68/68 на новой сборке · `check-framework` (девять новых пар) в сборке · `counters-guard` 0 · `lang-packs-guard` 0 · `scenario-lint` 0 · `attribution-lint` new 0 · `doc-header-lint`/`requirements-lint` 0 · красные: ядро 2.6 — 8 ❌ по имени, мутант — ровно 3 ❌ «приказ» при 3 ✅ «тишина» · ручной прогон хука со stdin из файла события (кейсы: перевод строки, кириллица с точкой, «/resume», без слова, слово не первым, без поля `prompt`)
Functional run: НАБЛЮДЁН 2026-09-18 08:12 +03:00 (сессия 66, Claude Fable 5.1, реальный путь владельца — новый чат истока, `.claude/settings.json` с четвёртой записью хука): первое сообщение владельца — `resume` первой строкой, `продолжаем` ниже; в контексте сессии стоял приказ хука, ПРОЧИТАН и процитирован в чате дословно ДО штампа маркера — «KAIF: the owner's message OPENS with the word "resume" — that is an ORDER, not a topic (AGENT_GUIDE.md → "A leading skill word is an order"; the owner's word: "if I write it, I REQUIRE the skill to run before the work"). BEFORE any work on the rest of this message run the /resume skill IN FULL: (1) read every canon document of its step 1 … (5) stamp .kaif/refresh-marker.json with trigger "ritual:/resume" …»; рядом стоял и приказ таймера освежения (424 мин) — два приказа не мешали друг другу; `/resume` исполнен целиком (14 документов, очередь — интервью №033 поднято страницей и отвечено Q1 = A в 08:14, символ веры и молитва, объявление), маркер `ritual:/resume` заштампован 08:13:04 +03:00 ПОСЛЕ цитаты (`.kaif/refresh-marker.json`); задача «продолжаем» взята только после объявления. Прежняя строка: `NONE — … наблюдение принадлежит первому промпту следующего чата (STATUS п. 0) — строка дописывается им` — дописана им

| Кейс | Статус | Наблюдение |
|---|---|---|
| RS1 «resume» первым словом, задача ниже → приказ | pass | `s14`: `✅ … — приказ исполнить /resume целиком (под капом)`; ручной прогон — 801 знак, имя события `UserPromptSubmit` |
| RS1 «Резюм.» первым словом (кириллица, регистр, точка) → приказ | pass | `s14` ✅; после Unicode-экранирования регэкспа — тот же приказ (прогон 12) |
| RS1 «/resume» → приказ | pass | `s14` ✅ |
| RS1 промпт без слова → тишина | pass | `s14` ✅; вывод пуст, код 0 |
| RS1 слово не первым («продолжай читать resume.log …») → тишина | pass | `s14` ✅ — граница пинка |
| RS1 событие без поля `prompt` → тишина | pass | `s14` ✅ |
| RS1 файл доезжает в `.kaif/hooks/`, фрагмент называет четыре скрипта | pass | `s14` ✅ ×2; образцы Codex/Cursor/Copilot/Antigravity НЕ обещают четвёртый хук — ✅ ×4 |
| RS1 красный на ядре 2.6 (файла нет) | pass | прогон 9: 8 ❌ ровно по новым ассертам |
| RS1 красный на мутанте (предикат сломан) | pass | прогон 10: ровно 3 ❌ «приказ», «тишины» зелёные — адресность названа до прогона |
| RS2 правило и алиасы в обоих слоях, указатель, README модуля, фрагмент, судья | pass | девять пар `check-framework` внутри сборки (прогон 14); грепы заголовков «### A leading skill word is an order» / «### Первое слово сообщения владельца — команда» |
| RS2 ru-пакет и пины | pass | прогон 5 |
| RS3 живое наблюдение на пути владельца | pass | сессия 66, 2026-09-18 08:12 +03:00: приказ хука в контексте первого промпта «resume\nпродолжаем», процитирован до штампа маркера, `/resume` исполнен целиком — строка `Functional run:` выше (прежний статус `skipped — первый промпт следующего чата`) |
| Инвариант §9.11: кириллица в теле EN-поставки | fail → fixed | прогоны 4 и 6 красные на моих же строках; проза — описанием, регэксп — `\uXXXX` скриптом из кодов (инструмент правки разворачивал экранирования в буквы); прогон 14 — зелёный |

## 5. Найдено

- Дефектов поставки — ноль (кроме предмета эпика: класс «первое слово владельца прочитано как проза», `researches/30` §2з).
- **Судья (прогон 15) — два опровергнутых заявления, починены до коммита (S3, без документа бага):** F1 — «копия судьи в
  обвязке побайтно равна» было ложно: `cp` 00:41 старее правки поставки 00:43 (снятие кириллицы) → `cp` + `cmp` 01:10;
  F2 — шапка хука несла `[TESTED: …]`, переключённый одной гигиеной и без адреса отчёта — класс «tested on hygiene alone»
  → `[NOT-TESTED: hygiene only … functional run pending → этот отчёт]`. Каверзы C1–C6 (граница ложных срабатываний не
  названа в правиле; правило шире хука — «продолжи» первым словом; охота у́же правила; кириллица в записи сборщика вне охвата
  §9.11; «37 пинов» читается как 37 сверок; «3 хука» в итоге свода) — починены тем же коммитом.
- Две грабли процесса без документа бага (S3 — строки урока EXP-0135): (а) Bash-инструмент снимает уровень экранирования — тест-вход
  хука с переводом строки подаётся файлом события; (б) инструмент правки разворачивает `\uXXXX` в буквы — экранирования в EN-поставке
  пишутся скриптом из кодов символов; инвариант §9.11 сборки поймал обе формы сам (страж сработал на реальном пути).

## 6. Следы

- Логи: `<скретчпад>/build-1.log`, `build-2.log`, `build-polygon.log` (первый полигон, 1262 строки), `build-polygon-final.log`,
  `s14-green.log`, `s14-red-2.6.log`, `s14-red-mutant.log`.
- Красные копии поставки: `<скретчпад>/dist-2.6/` (из тега `v2.6`), `<скретчпад>/dist-mutant/` (4 байта разницы — `resumeXX`).
- Файл события ручного прогона: `<скретчпад>/event-resume-newline.json`; скрипт экранирования: `<скретчпад>/fix-regex-escapes.mjs`.
- Временные корни сводов — `os.tmpdir()/kaif-sandbox-hooks-*` (уникальны по построению, `bugs/59`).
- Скретчпад сессии: `C:\Users\krinik\AppData\Local\Temp\claude\d--work-ai-sandbox-KAIF\43a0c95a-1e09-4ad5-8d90-2cc34afb13a6\scratchpad\`.

## 7. Вердикт

`pass` (2026-09-18 08:14 +03:00, сессия 66) — гигиена и красные доказательства зелёные/красные как заявлено, стражи чистые;
функциональный прогон на пути владельца наблюдён первым промптом этого чата (прогон 17: приказ хука в контексте, процитирован до
штампа маркера, `/resume` исполнен целиком) — критерий 23 `plans/100` закрыт ✅. Финальная цепочка (прогон 14): сборка OK, счётчики 0,
полигон 26 из 26 зелёный на окончательной поставке. Прежний вердикт — `partial` (сессия 65): «функциональный прогон на пути владельца
(живой приказ хука в новом чате) не выполнялся в этой сессии — строка `Functional run:` дописывается первым промптом следующего чата».
