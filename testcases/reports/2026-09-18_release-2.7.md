# Отчёт прогона — выпуск KAIF 2.7 (Audited KAIF): последний блокер суда D-F2 и контрольный прогон релиз-кандидата

> Шаблон — `framework/templates/_testrun-report-template.md`, форма судится `node framework/tools/kaif-testrun-lint.mjs check`
> (`TESTING_FRAMEWORK.md` → «Исполненный прогон рождает отчёт», «Что значит слово „тест“»). Каталог по дате: имя файла и есть индекс.

**Создан:** 2026-09-18 17:08 +03:00 · **Прогонял:** агент, сессия 69 — Claude Opus 5 (главное дерево истока) ·
**Версия/сборка:** релиз-кандидат KAIF 2.7 — `version.json` 2.7 · Audited KAIF · 2026-09-18, поверх HEAD `a05de51` + рабочее дерево
выпуска; сборка печатает 59 встроенных файлов · бандл 182 блока · карта 798 модулей

## 1. Работа

Выпуск версии 2.7 по плану `STATUS.md` п. 0 (слово владельца №122, №123). Проверялись: **(а)** последний блокер суда версии
D-F2 (реестр `reports/KAIF_AUDIT/2026-09-18_rl_court_2.7_registry.md`, строка D10: «вкладка на чужом профиле → сервер убит» —
страница обещала «ответ сохранён … агент заберёт», а ответ оставался в браузере, который агент не читает); **(б)** релиз-кандидат
целиком — шаги 1–4 навыка `/release`: сборка, полигон, полный прогон контура, стражи витрины и реестра пар, голос README и нот,
пересчёт числа суда. Базис (а) — критерий 19 `plans/100` и заметки к релизу, п. 5 («в окне контура ответ лежит в профиле окна и
его забирает следующий прогон очереди; во вкладке страница показывает текст ответа, чтобы его скопировать»); форма кейса вкладки —
проба судьи D. Набор кейсов (а): окно `--app` на профиле проекта (забор ответа) · вкладка на чужом профиле (кольцо спасения, без
обещания) · красный на сборке до починки. Базис (б) — команды шагов 1–4 `/release`.

**Уроки `EXPERIENCE.md`, снятые грепом ДО работы** (`#release #contour #human-facing`): **EXP-0149** (на «сколько осталось» —
часами) → в объявлении сессии назвал 60–75 минут; **EXP-0132 / EXP-0126** (генератор контура из песочницы — только в тихом
окружении, свод копии — после сборки) → все прогоны после `node tools/build-framework.mjs`, генераторы в `quietEnv()`, браузер
только безоконный; **EXP-0127** (красный — вердиктом, не исключением) → первый красный на сборке до починки был исключением
(срок вызова), ожидание страницы сокращено до 7 с, второй красный — вердикт; **EXP-0107** (число суда — скриптом по строкам) →
138 = 116 · 17 · 5 пересчитано грепом; **EXP-0144 / EXP-0121** (экранирование в параметрах инструментов) → правка QA7 и
сверка пар — скриптами-файлами.

## 2. Контур

Поставка: `framework/tools/contour/review.mjs` (JS страницы: `inApp` по `display-mode: standalone`; местная запись и обещание
забора — только в окне; во вкладке — путь I11), `framework/templates/_interactive-contour-spec.md` (120 строк, одна фраза),
`framework/skills/owner-reviews/SKILL.md` + RU-копия (I11, I47, QA7). Машинерия истока: `tools/lib/cdp-mini.mjs` (опция `app` у
`headlessPage`), `tools/sandbox/s22-contour-shipped.mjs` (раздел D), `tools/verify-contour.mjs` (QA7), проба
`tools/sandbox/probes/display-mode-headless.mjs`. Номер версии: `version.json`, `.kaif/kaif.json`, `KAIF_FRAMEWORK.md`. Стенд —
рабочее дерево истока, Windows 11 Pro 10.0.26200, Node v24.15.0, git-bash, Edge `C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe`
только headless; своды разворачивают поставку из `dist` во временные корни. Красный: `dist` HEAD `a05de51` (`git show HEAD:dist/…`
→ скретчпад `dist-head/`).
`РЕАЛЬНЫЙ МИР: накоплено — у владельца истока живых страниц контура нет (очередь пуста, проба --queue --list 16:49), профиль окна
создаётся первым запуском, починка меняет только поведение страницы при мёртвом сервере (проверено с реальным состоянием, снятым с
реального: настоящая страница поставки, настоящий Edge, профиль проекта во временном развёртывании); данные и техника — Edge на
Windows владельца, headless (проверено на реальном: тот же исполняемый файл); путь — окно --app и вкладка, оба безоконными
моделями; видимое окно для этой починки не поднималось — граница названа в вердикте.`

## 3. Прогоны

Моменты — пробами `date` соседних вызовов; `≈` — где момент снят по соседней пробе.

| # | Момент | Команда | Код / исход |
|---|---|---|---|
| 1 | 2026-09-18 16:49 +03:00 | `node tools/review.mjs --queue --list` | 0 · «Очередь владельца пуста» |
| 2 | 2026-09-18 ≈ 16:53 +03:00 | `node tools/sandbox/probes/display-mode-headless.mjs` | 0 · «app: standalone=true browser=false» · «tab: standalone=false browser=true» |
| 3 | 2026-09-18 ≈ 16:54 +03:00 | `node framework/tools/contour/review.mjs --selftest` · `node tools/build-framework.mjs` | 0 · «contour selftest green: 75 checks» · сборка зелёная (v2.6) |
| 4 | 2026-09-18 16:55 +03:00 | `node tools/sandbox/s22-contour-shipped.mjs` | 1 · исключение «Cannot set properties of undefined» — окно `--app` отдано на about:blank |
| 5 | 2026-09-18 ≈ 16:56 +03:00 | `node <скретчпад>/diag-app.mjs <корень прогона 4>/lp` | 0 · первый взгляд — about:blank, полей 0; через 0,5 с — страница, standalone=true |
| 6 | 2026-09-18 16:57 +03:00 | `node tools/sandbox/s22-contour-shipped.mjs` (помощник ждёт загрузки) | 0 · «all checks green», новый ассерт вкладки ✅ |
| 7 | 2026-09-18 ≈ 16:58 +03:00 | `KAIF_DIST=<скретчпад>/dist-head node tools/sandbox/s22-contour-shipped.mjs` | 1 · исключение «CDP timeout: Runtime.evaluate» — красный крэшем |
| 8 | 2026-09-18 ≈ 16:59 +03:00 | тот же шов после сокращения ожидания до 7 с; затем `node tools/sandbox/s22-contour-shipped.mjs` | 1 · «s22: 1 checks FAILED» (ассерт вкладки, в выводе — сам дефект) · затем 0 · «all checks green» |
| 9 | 2026-09-18 ≈ 17:00 +03:00 | `node tools/verify-contour.mjs` | 0 · «ИТОГ QA-прогона: 198 зелёных, 0 красных» |
| 10 | 2026-09-18 17:03 +03:00 | `node tools/build-framework.mjs` (после бампа 2.7) | 0 · «v2.7 (2026-09-18)» · «notes name KAIF 2.7 — Audited KAIF» |
| 11 | 2026-09-18 ≈ 17:04 +03:00 | `node tools/counters-guard.mjs` · `doc-header-lint` · `lang-packs-guard` · `showcase-lint` · `private-names-guard` · `questions-guard` · `estimates-guard` | все 0, кроме `doc-header-lint` 1 (2 находки «≈ между датой и временем») → поправлено → 0 · «findings 0» |
| 12 | 2026-09-18 ≈ 17:05 +03:00 | `node tools/voice-lint.mjs README.md reports/RELEASE_NOTES_2.7.md` · `node tools/stylometry-snapshot.mjs --version-check` · `node <скретчпад>/pairs-check.mjs` | 0 · «жёстких нарушений нет» · «слепок совпадает с ядром» · «PAIRS: all rows agree» |
| 13 | 2026-09-18 ≈ 17:05 +03:00 | `grep -cE '^\| \*{0,2}[A-Z][0-9]+\*{0,2} \|' reports/KAIF_AUDIT/2026-09-18_rl_court_2.7_registry.md` и по вердиктам | 138 = 116 CONFIRMED · 17 WEAKENED · 5 REFUTED |
| 14 | 2026-09-18 17:05 +03:00 | `npm run test:core` (в одиночку, 17:05:18–17:07:57) | 0 · «preflight: …» зелёный · «sandbox suite: all 27 suites green» |
| 15 | 2026-09-18 17:08 +03:00 | `node tools/readme-pdf.mjs` | 0 · «README.pdf generated» (3 499 279 байт); первая страница глазами не прочитана — на машине нет `pdftoppm` |
| 16 | 2026-09-18 17:24 +03:00 | `git tag v2.7` · `git push origin main` · `git push origin v2.7` · `gh release create v2.7 --title "KAIF 2.7 — Audited KAIF" --notes-file reports/RELEASE_NOTES_2.7.md <шесть файлов dist/>` | 0 · «a05de51..b41b3ac main -> main» · «[new tag] v2.7» · страница релиза |
| 17 | 2026-09-18 ≈ 17:25 +03:00 | `node tools/release-body-guard.mjs --tag v2.7` · `gh release view v2.7 --json …` · `gh release list --limit 2` | 0 · «опубликованное тело v2.7 совпадает … построчно» · «KAIF 2.7 — Audited KAIF · prerelease=false · draft=false», шесть ассетов · Latest |
| 18 | 2026-09-18 ≈ 17:25 +03:00 | `gh release download v2.7 --dir <скретчпад>` + `sha256sum` против `git show v2.7:dist/<файл>` | 6 из 6 EQUAL |
| 19 | 2026-09-18 ≈ 17:25 +03:00 | `node <скретчпад>/shot.mjs https://github.com/MikalaiKryvusha/KAIF/releases/tag/v2.7 <png> 1280 1800` (безоконный Edge) | 0 · снимок прочитан: заголовок и Latest, логотип с подписью, дата, абзацы целы, ссылки на тикеты — ссылками |
| 20 | 2026-09-18 17:26 +03:00 | `gh issue comment N --body-file reports/release_2.7/N.md` · `gh issue close N --reason completed` (N = 54…71) · `node <скретчпад>/verify-comments.mjs` · `gh issue list --state open` | 18 комментариев · 18 закрыты · «all 18: closed, replies equal their files» · открытых 0 |

## 4. Проверки

Гигиена: селфтест генератора 75/75 · сборка 59 · 182 · 798 · стражи шага 4 и витрины зелёные · реестр пар сходится · число суда
пересчитано скриптом · красный нового ассерта `s22 D` на сборке до починки — вердиктом (прогон 8), остальные ассерты `s22` на ней
зелёные.
Функциональный прогон: ПРОЙДЕН на настоящей странице поставки, развёрнутой из `dist` во временное развёртывание, настоящим Edge
в безоконном режиме, двумя путями владельца — окно `--app` на профиле проекта и вкладка на чужом профиле; результат прочитан
из DOM страницы и из записи решения. Видимое окно не поднималось.

| Кейс | Статус | Наблюдение |
|---|---|---|
| Окно `--app` на профиле проекта: ответ набран, сервер убит, «Записать» | pass | `s22 D`: «сохранён на этом компьютере», `__submitted` в профиле; `verify-contour` QA7 — пять эталонных true |
| Забор ответа из окна после жёсткого убийства браузера | pass | `s22 D`: «answer recovered … from indexedDB», `recovered: true`, текст побайтно равен набранному |
| Вкладка на чужом профиле: сервер убит, «Записать» | pass | `s22 D`: статус «ОШИБКА ЗАПИСИ: СЕРВЕР КОНТУРА НЕДОСТУПЕН — ответ НЕ уйдёт…», кольцо спасения с текстом ответа, кнопки живы, `__submitted` нет, жёлтая полоса вкладки |
| Тот же кейс вкладки на сборке ДО починки | fail (ожидаемо) | прогон 8: статус «Сервер недоступен — ответ сохранён на этом компьютере (в папке проекта); агент его заберёт. Окно можно закрыть.» — дефект D10 воспроизведён |
| Страница видит окно без видимого окна | pass | прогон 2: standalone=true для `--app`, false для вкладки |
| Релиз-кандидат: полигон целиком | pass | прогон 14: «sandbox suite: all 27 suites green», в одиночку |

## 5. Найдено

- Дефект D-F2 (реестр суда, D10) — воспроизведён на сборке до починки и починен (кейс вкладки).
- Свой дефект по ходу: помощник `headlessPage` в режиме `app` отдавал окно на about:blank (прогон 4) — починен ожиданием загрузки
  документа; найден прогоном, а не рассуждением.
- Свой дефект по ходу: первый красный на сборке до починки был исключением, а не вердиктом (прогон 7) — ожидание сокращено ниже
  срока вызова CDP (EXP-0127).
- Две метки в статусах `plans/63` и интервью №032 стояли в форме «дата ≈ время» — приведены к «≈ дата время».

## 6. Следы

Логи — скретчпад сессии 69 (`s22.log`, `s22-head.log`, `verify-contour.log`, `polygon.log`, `build.log`, `g-*.log`, `voice.log`);
копия сборки до починки — скретчпад `dist-head/`; проба замера — `tools/sandbox/probes/display-mode-headless.mjs`; свод —
`tools/sandbox/s22-contour-shipped.mjs` раздел D, случай (6).

## 7. Вердикт

**pass** (2026-09-18 17:09 +03:00) — D-F2 починен и наблюдён функциональным прогоном на настоящей странице поставки двумя
путями владельца (окно `--app` на профиле проекта — ответ забран побайтно; вкладка на чужом профиле — кольцо спасения с текстом,
без обещания забора); красный на сборке до починки — вердиктом, с текстом самого дефекта. Релиз-кандидат прошёл шаги 1–4
`/release` целиком: сборка, полигон 27 из 27 в одиночку, полный прогон контура 198/0, стражи, голос, реестр пар, пересчёт суда.
Границы, названные вслух: оба пути — безоконные модели в Edge на Windows; видимое окно для этой починки не поднималось;
Chrome, macOS и Linux не проверены; первая страница README.pdf глазами не прочитана (нет `pdftoppm`).

**Публикация (прогоны 16–20), 2026-09-18 17:24–17:26 +03:00:** релиз `KAIF 2.7 — Audited KAIF` опубликован с шестью ассетами,
равными `dist` тега; тело построчно равно файлу нот; первый экран страницы прочитан по безоконному снимку; 18 ответов опубликованы,
прочитаны обратно и тикеты закрыты — открытых 0.

**Лёгкий судья релиз-кандидата (шаг 4.5 `/release`), 2026-09-18 17:21 +03:00 — VERIFIED WITH CAVEATS.** Субагент с чистым
контекстом в отдельной рабочей копии на `32ed37e`, десять заявлений: версия (C1) · воспроизводимость сборки — `git status` пуст после
пересборки (C2) · D-F2 — `s22` на кандидате 81/0, на `dist` родителя `a05de51` ровно 1 красный, случай вкладки с текстом дефекта (C3) ·
число суда скриптом 138 = 116 · 17 · 5 (C4) · пять блокеров найдены в коде (C5, без переисполнения A-F1/E-F1/F-F1/D-F1) · п. 5 нот
сходится с кодом, спецификацией и навыком (C6) · стражи витрины и счётчиков (C8) · скоуп диффа (C9) · шесть ассетов и пины sha256 (C10) —
CONFIRMED; гейт переносов тела нот (C7) — WEAKENED: прозы с переносами нет, но буквальное правило ложно на структурных блоках (как и в
нотах 2.6). Оговорки: (1) C7 и (2) пины манифеста — строкой `ideas/30` п. 24; (3) стоячая ложь — `plans/116` и п. 0 `STATUS.md` называли
D-F2 открытым, реестр суда — исходы «в секции Вердикты»: исправлено до тега, коммитом после `32ed37e`; (4) граница «вкладка — только
безоконно» уже стоит в нотах (п. 5: проверено на Edge под Windows) и в этом вердикте.
