# Отчёт прогона — побочные прогоны сессии 67: граница списка `--docs` у штампа освежения и герметичный по сети полигон

> Отчёт прогона истока; форма судится `node framework/tools/kaif-testrun-lint.mjs check` (`TESTING_FRAMEWORK.md` → «Исполненный
> прогон рождает отчёт»). На этот файл уже ссылались метка `[TESTED]` в `tools/refresh-stamp.mjs` и `bugs/109` п. 4, а самого
> файла не было — сессия 67 закрылась до его записи. Написан сессией 68 по сохранённым логам и записям сессии 67.

**Создан:** 2026-09-18 ≈ 16:17 +03:00 · **Прогонял:** агент, сессия 67 — Claude Fable 5.1; отчёт записан сессией 68 — Claude Opus 5 ·
**Версия/сборка:** KAIF 2.7 (открыта), HEAD `f4915af` + рабочее дерево закрытия сессии 67

## 1. Работа

Две побочные правки сессии 67, обе вне эпиков версии. **(1)** `tools/refresh-stamp.mjs` складывал значение `--note` в список
«перечитано» (`docs`), хотя комментарий обещал «до следующего флага»; базис — строка «Использование» в шапке инструмента и
канон освежения (`AGENT_GUIDE.md` → «Освежение контекста»: маркер перечисляет перечитанные документы). **(2)** Полигон истока ходил
в сеть за артефактом прежнего релиза из каждого `update` / `install` / `diff` без своего `--baseline` — окно, в котором пять раз
умирал `node` (`bugs/109`); базис — `bugs/109` п. 3–4 (гипотеза H4: крах связан с сетевым вызовом) и инвариант полигона «стенд
детерминирован». Правка — общий раннер `tools/lib/sandbox-run.mjs` (`hermeticArgs`: пустой `--baseline` корня прогона, выключатель
`KAIF_SANDBOX_NETWORK=1`), `s01` переведён на тот же помощник; замер — проба-предзагрузка `tools/sandbox/probes/net-audit-preload.mjs`.

## 2. Контур

Машинерия истока: `tools/refresh-stamp.mjs`, `tools/lib/sandbox-run.mjs`, `tools/sandbox/s01-field-fixes.mjs`,
`tools/sandbox/probes/net-audit-preload.mjs`. Стенд — рабочее дерево истока на Windows 11 Pro 10.0.26200, Node v24.15.0, git-bash;
своды разворачивают копии поставки из `dist` во временные корни; окон, звука и внешних действий нет. Правка штампа проверялась на
копии маркера. **Реального мира здесь нет:** оба инструмента — инструменты самого истока, их пользователь — агент истока;
путь «владелец вызывает» у них отсутствует.

## 3. Прогоны

Моменты — из записи `[TESTED]` в `tools/refresh-stamp.mjs` (прогоны 1–2) и из первых строк и времени записи логов (прогоны 3–5).

| # | Момент | Команда | Код / исход |
|---|---|---|---|
| 1 | 2026-09-18 14:08 +03:00 | `node tools/refresh-stamp.mjs --trigger … --docs … --note "…"` (живой маркер сессии 67, до правки) | 0 — в `docs` три элемента, третий — текст заметки: ❌ дефект наблюдён |
| 2 | 2026-09-18 14:10 +03:00 | на копии: `--docs A B --note "n"` · `--note "n" --docs A B` · `--docs A --note "n" stray` · `--docs --note "n"` | `[A, B]` · `[A, B]` · `[A]` · отказ кодом 1 |
| 3 | 2026-09-18 15:30 +03:00 | `KAIF_NET_AUDIT_LOG=<скретчпад>/net-audit-smoke.log node --import <проба> <скретчпад>/net-audit-smoke.mjs` (`fetch` на `127.0.0.1:9`) | 0 — в логе одна строка `http://127.0.0.1:9/kaif-audit-smoke :: scratchpad/net-audit-smoke.mjs`: проба пишет строку на вызов и не ломает сам вызов |
| 4 | 2026-09-18 16:05:45 → 16:08 +03:00 | `KAIF_NET_AUDIT_LOG=<скретчпад>/net-audit-hermetic.log NODE_OPTIONS="--import …/net-audit-preload.mjs" npm run test:core` (герметичный, по умолчанию) | 0 — «dist is fresh against framework/ (27 suites)» · «sandbox suite: all 27 suites green»; в логе 4 строки |
| 5 | 2026-09-18 16:08:40 → 16:11 +03:00 | `KAIF_SANDBOX_NETWORK=1 KAIF_NET_AUDIT_LOG=<скретчпад>/net-audit-online.log NODE_OPTIONS="--import …" npm run test:core` (как было до правки) | 0 — «sandbox suite: all 27 suites green»; в логе 58 строк, из них 55 на github.com |

## 4. Проверки

Hygiene: селфтест раннера — 10 проверок (три команды получают флаг · свой флаг свода не трогается · прочие команды не трогаются ·
выключатель `KAIF_SANDBOX_NETWORK=1`); смоук пробы (прогон 3) — проба пишет строку и возвращает настоящий ответ; граница `--docs`
на копии маркера (прогон 2) — четыре порядка аргументов.
Functional run: полигон целиком дважды на одном дереве — герметичный (прогон 4) и прежний онлайн (прогон 5), каждый с пробой;
ПРОЧИТАНЫ оба лога. Герметичный: 4 вызова — один на github.com, намеренный (свод проверяет отказ на несуществующем
`kaif-polygon/absent-repo`), три на `127.0.0.1` (страница контура `s22`). Онлайн: 58 вызовов, 55 на github.com — каждый `update` /
`install` сводов тянул `releases/download/v2.6/KAIF-CORE-BUNDLE.md`. Оба прогона — «all 27 suites green». Для штампа функциональный
прогон — это прогон 2 на копии маркера плюс штамп этой сессии 68 в 16:15 (`--docs` двенадцать документов, `docs` — ровно двенадцать).

| Кейс | Статус | Наблюдение |
|---|---|---|
| `--note` после `--docs` не попадает в список | pass | прогон 2: `[A, B]` |
| пустой `--docs` отвергается | pass | прогон 2: код 1 |
| проба пишет строку на каждый `fetch` | pass | прогон 3: одна строка на один вызов |
| герметичный полигон не ходит на GitHub за артефактом прежнего релиза | pass | прогон 4: один вызов на github.com — намеренный отказной |
| прежнее поведение воспроизводится выключателем | pass | прогон 5: 55 вызовов на github.com |

## 5. Найдено

1. Дефект `tools/refresh-stamp.mjs`: значение `--note` уходило в список `docs` (прогон 1) — починено сессией 67 до коммита.
2. Находка чтением кода: `buildSyntheticBaseline` зовётся из КАЖДОГО `update` ядра, а не только на легаси-пути, и без флага идёт
   на github.com до трёх раз подряд (`bugs/109` п. 4) — это единственный сетевой вызов окна, в котором умирал `node`.
3. Причинность «сетевой вызов → нативный крах» НЕ наблюдалась: наблюдено совпадение окна в пяти вхождениях `bugs/109`. Герметичный
   полигон — митигация и опыт над гипотезой H4, а не доказанный фикс.

## 6. Следы

Логи сессии 67 (скретчпад `C:\Users\krinik\AppData\Local\Temp\claude\d--work-ai-sandbox-KAIF\b92df5b3-b628-4865-a16a-c3c5a6f16825\scratchpad\`):
`net-audit-smoke.log` · `net-audit-hermetic.log` (4 строки) · `net-audit-online.log` (58 строк) · `polygon-hermetic.log` ·
`polygon-online-audit.log`. Запись о штампе — метка `[TESTED]` в шапке `tools/refresh-stamp.mjs`. Документ бага — `bugs/109` п. 4.

## 7. Вердикт

`pass` (2026-09-18 ≈ 16:17 +03:00) — оба полигона зелёные и прочитаны; герметичный делает 4 сетевых вызова вместо 58, на GitHub —
один намеренный вместо 55; граница `--docs` держит все четыре порядка аргументов. Оговорка из § 5 п. 3 остаётся в силе: `bugs/109`
не закрыт этим прогоном, он ждёт следующего вхождения краха или его отсутствия на герметичном полигоне.
