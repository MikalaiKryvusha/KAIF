# Отчёт прогона — разведка скоупа 2.8: пробы заявлений 27 тикетов origin против HEAD `6af0dad`

**Создан:** 2026-09-24 19:20 +03:00 · **Прогон вёл:** Claude Opus 5.5 (1M context) — главная сессия и шесть субагентов-разведчиков

## 1. Работа

Сверка заявлений 27 тикетов origin #72–#98 (пришли после релиза 2.7) с поставкой на HEAD `6af0dad` — базис: тела и
комментарии тикетов (`gh issue view N --json title,body,comments`) против кода и канона `framework/`. Это разведка, а не
тест продукта: вопрос каждой пробы — «есть ли на HEAD то, что тикет утверждает». Итог — `researches/32` §2.

## 2. Контур

Исток KAIF, Windows 11, Git Bash (GNU grep 3.0, `LC_CTYPE=C.UTF-8` не наследуется по умолчанию) + Node; HEAD `6af0dad` =
тег `v2.7` по машинерии. Пробы, которым нужно писать файлы, — только во временных каталогах (`mktemp -d`) на КОПИЯХ ядра
и хуков; соседние развёртывания (KAGO · KUMM · ndim · unliminiumProject) — только чтение; полигон и контур не запускались
(бриф тишины). **РЕАЛЬНЫЙ МИР:** накоплено — деревья четырёх соседних развёртываний на диске (чтение: `wc -l`, `git log`,
`git ls-files --eol`); данные и техника — машина владельца, Windows; путь — тот же Git Bash, которым ходит полевой агент на
Windows. QA_Engineer (macOS) на этом диске нет — его числа взяты из текстов тикетов.

## 3. Прогоны

Шесть разведчиков параллельно, запуск ≈ 2026-09-24 18:27 +03:00; длительности из квитанций харнесса: хуки/время/зов 510 с ·
линтеры/скан/дверь 639 с · канон поведения 655 с · машинерия обновления 752 с · отчёты NDim/KUMM 957 с · отчёты KAGO/QA 1207 с.
Главная сессия: #98 — 18:44–18:50; поиск прошлого ответа двери — ≈ 18:55.

Ключевые команды (полный список — в отчётах разведчиков, пересказанных в `researches/32` §2а–§2д):
- `node probe-merge.mjs` — собственная `mergeModules` ядра на копии, пять состояний пары переименования (#72);
- `node probe-verify.mjs` — `update-verify` на двух временных развёртываниях, `i18n=translated` против отсутствия поля (#92);
- `node probe-budgets.mjs` — ворота бюджета на свежей ru-установке из `dist` (#84 #85);
- проба хуков с подложным событием на stdin, `cwd` = корень против подкаталога (#94) и `at` в будущем (#96);
- `printf 'Вопрос\n' | grep -ci 'вопрос'` с `LC_ALL=C.UTF-8` и без (#74);
- `grep -rniE "верси" interviews/ GOAL.md MASTER_PLAN.md plans/ | wc -l` с `LC_ALL=C.UTF-8` и без (главная сессия);
- `git show <tag>:framework/AGENT_GUIDE.md | wc -lw` по тегам v1.6 … v2.7 (#93 §2);
- `grep -n` по `framework/tools/contour/texts.mjs:131–137` (фраза зова, #98 — главная сессия).

## 4. Проверки

**Гигиена:** стражи истока по документам разведки и планирования — `node tools/questions-guard.mjs` (0 новых нарушений),
`node tools/doc-header-lint.mjs` (0 находок), `node framework/tools/kaif-attribution-lint.mjs check` (new 0 · debt 45),
дверь `node tools/review.mjs interviews/interview_034_kaif_2.8_scope.md --check` (аттестовано 2 из 2).

**Функциональный прогон:** NONE — разведка сверяла заявления тикетов с кодом, продукт путём пользователя не проходился;
каждое CONFIRMED — наблюдение пробы о HEAD, не тест починки.

## 5. Найдено

- Из 23 дефектов и улучшений опровергнут на HEAD ни один: все — CONFIRMED или PARTIAL (таблицы `researches/32` §2а–§2г); три
  внесены самой 2.7 (#72 — эпик HO, #84 и #85 — эпик CB).
- Из четырёх полевых отчётов — находки без своих тикетов (N1–N17, K5–K20, Q-§2, Q-R1′, Q-R4, K-R2a, K-R4 и др.), `researches/32` §2д.
- **Живое вхождение #74 на истоке:** напечатанная дверью команда поиска без `LC_ALL=C.UTF-8` — 662 строки против 696
  (заголовок Q1 №034) и 904 против 963 (Q2).
- Шаблоны 2.8 при переходе 2.7 → 2.8 назвали бы собственным сканом 11 строк в 7 файлах (K-R4, реплика построчных правил).

## 6. Следы

`researches/32_kaif_2.8_scope_recon.md` (таблицы заявлений с командами и строками кода) · `interviews/interview_034_kaif_2.8_scope.md`
(строки-аттестации поиска) · скрипты проб разведчиков — во временных каталогах и скретчпаде сессии (удалены или умирают
вместе с сессией; воспроизводимы по командам §3).

## 7. Вердикт

**partial** — разведка исполнена и прочитана, заявления сверены пробами; функционального прогона нет по природе работы
(это не тест продукта), и скрипты проб в репозиторий не легли.
