# Отчёт прогона — эпик CH 2.8, шаг CH1: полевой отчёт обновления уходит в KAIF сам

**Создан:** 2026-09-26 00:08 +03:00 · **Прогон вёл:** Claude Opus 5.5 (1M context), сессия 74 (ночной цикл) · **Версия/сборка:** HEAD `7a74fd8` + рабочее дерево

## 1. Работа

Критерий 10 `plans/117` (тикет origin #78; слово полевого владельца дословно — в тикете): README директории `reports/` поставки обещал,
что полевой отчёт «остаётся локальным, пока владелец не скажет иное», — против стоячей авторизации сигналов в KAIF (origin #15), и ни
одна ось не видела недоставленный отчёт. Сделано: README — отчёт есть сигнал KAIF, доставляется тем же движением, что написан; пункт
`field-report` задания обновления и установки — H1 и строка `**Delivered upstream:** NOT YET`, затем `node .kaif/kaif-core.mjs report
<отчёт>`; ось `check` «недоставленный сигнал» читает отчёты 2.8+ тем же чтением строки доставки, что тикеты (отчёты до 2.8 — локальные по
канону своего времени — молчат); `/kaif-update` шаг 5 обоих слоёв — отчёт доставляется, публичная поправка к тикету — только после
перемера находки судьи, судья обновления — в чистом контексте; исключение из ворот `AUTH:` в руководстве обоих слоёв называет полевой
отчёт; записка §10; заметка и запись изменения правил 2.8. Строка `FORK:` — `plans/121` CH1 (A: одна дверь `report`, одно чтение).

## 2. Контур

Исток KAIF, Windows 11, Git Bash, Node v24. Свод `s17` — развёрнутая копия с подменным `gh` (ни одного действия наружу); красный — `dist`
v2.7 через `KAIF_DIST`; мутанты — копии `dist`; реальный мир — КОПИИ 22 полевых отчётов пяти развёртываний (оригиналы только читаются) в
свежей установке с `tracking: origin`.

## 3. Прогоны

| # | Момент | Команда | Исход |
|---|---|---|---|
| 1 | 2026-09-25 23:51:00 | `node tools/build-framework.mjs` | 0; руководство поставки — 1080 строк (исключение `AUTH:` дополнено в тех же строках) |
| 2 | 2026-09-25 23:51:16 | `node tools/sandbox/s17-report.mjs` | 47 зелёных, 0 красных (+4 случая CH1) |
| 3 | 2026-09-25, после 23:51:16 | то же с `KAIF_DIST=<v2.7>` | 2 красных поимённо — «отчёт 2.8 с NOT YET назван» и «без строки доставки назван»; доставка `report` работает и на v2.7 (ей нужны лишь H1 и строка) — зелёные честно |
| 4 | 2026-09-25 23:52:09 | `node tools/sandbox/probes/ch1-report-mutants.mjs` | «3 mutants red exactly on their named addressees» — без ворот версии (2 · отчёт 2.7 назван), без ворот слежения (1 · anonymous), без ветки «доставлен» (1) |
| 5 | 2026-09-25 23:53:00 · 23:53:26 | `node scratchpad/ow9/ch1-realworld.mjs` (первые две редакции) | «0 строк» и на отчёте 2.8 — прогон НЕДЕЙСТВИТЕЛЕН: `check` в урезанном развёртывании обрывался раньше оси («is KAIF deployed here?»), а вторая редакция перезаписывала маркер урезанным; ноль прочитан как подозрительный, не как зелёный |
| 6 | 2026-09-25 23:53:37 | то же, полная установка и её маркер | 22 настоящих отчёта 2.1–2.7 пяти развёртываний — 0 строк; отчёт 2.8, написанный по словам пункта задания, — назван поимённо с командой `report`; `report --dry-run` — строка `gh issue create --repo MikalaiKryvusha/KAIF --title "FIELD — KAIF 2.8 update report"`, «nothing sent» |
| 7 | 2026-09-25 23:56:26 → 2026-09-26 00:00:50 | `npm run test:core` (в одиночку) | КРАСНЫЙ 1 из 27 — `s04` «S13-M3 анонимный профиль: пункт отчёта НЕ тянется к origin»: фраза доставки легла в задание установки КАЖДОГО развёртывания, а у анонимного профиля задание к origin не тянется (инвариант) |
| 8 | 2026-09-26 00:01:51 | фраза доставки — только на `tracking: origin` (`fieldReportDelivery()` читает маркер; нечитаемый — фразы нет); сборка; `s04`; `s17`; реальный мир | `s04` 0 красных; `s17` 48 (+1: задание установки на origin несёт строку доставки и команду); реальный мир — как в прогоне 6 |
| 9 | 2026-09-26, после 00:01:51 | `s17` с `KAIF_DIST=<v2.7>`; `node tools/sandbox/probes/ch1-report-mutants.mjs` | 3 красных поимённо; мутанты 3 из 3 на адресатах |
| 10 | 2026-09-26 00:03:54 → 00:08:19 | `npm run test:core` (в одиночку) | «sandbox suite: all 27 suites green» |

## 4. Проверки

Гигиена: `s17` 48/0 (+5), на v2.7 — 3 красных поимённо; `s04` — анонимное задание к origin не тянется; мутанты 3 из 3; `grep -c "stays LOCAL" framework/readmes/reports.md` → 0.
Функциональный прогон: проверка развёртывания на КОПИЯХ настоящих полевых отчётов (прогон 6) — ПРОЧИТАНО: старые отчёты молчат, новый
назван с готовой командой, сухая доставка печатает тот `gh`, который ушёл бы, и не зовёт его.
РЕАЛЬНЫЙ МИР: накоплено — 22 отчёта пяти развёртываний — проверено с реальным состоянием, снятым с реального (копии); данные и техника —
эта машина, настоящий `gh` не звался (сухой прогон) — проверено на реальном без действия наружу; путь — агент поля, исполняющий пункт
задания обновления 2.8, — после выпуска 2.8; первое обновление поля покажет.

## 5. Найдено

1. **Ноль, прочитанный как подозрение**: первые два прогона реального мира дали «0 строк» и на отчёте, который обязан быть назван, —
   урезанное развёртывание обрывало `check` раньше оси; прогон пересобран полной установкой.
2. **Фраза доставки в задании анонимного развёртывания** — поймал полигон (`s04` S13-M3, прогон 7): первая редакция писала её в задание
   каждого развёртывания; теперь — только на `tracking: origin`, и `s17` стережёт обратную сторону.
3. Ноль дефектов вне этого.

## 6. Следы

`framework/readmes/reports.md`, `framework/installer/KAIF-CORE.mjs` (пункты `field-report`, ось `check`), `framework/skills/kaif-update`
обоих слоёв, руководство обоих слоёв, `framework/KAIF_REFERENCE.md`, `tools/build-framework.mjs`, `tools/sandbox/s17-report.mjs`,
`tools/sandbox/probes/ch1-report-mutants.mjs`; логи — скретчпад сессии 74 (`ow9/s17-ch1*.log`, `ch1-realworld.log`).

## 7. Вердикт

**partial** — поведение проверено гигиеной и на копиях настоящих отчётов; путь агента поля, исполняющего задание 2.8, — после выпуска.
