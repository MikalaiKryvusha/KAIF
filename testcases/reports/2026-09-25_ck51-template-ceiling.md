# Отчёт прогона — эпик CK 2.8, шаг CK5.1: страж сборки «шаблон ядра ≤ бюджет × 90 %»

**Создан:** 2026-09-25 00:47 +03:00 · **Прогон вёл:** Claude Opus 5.5 (1M context), сессия 72 · **Версия/сборка:** HEAD `7b1fbf9` + рабочее дерево шага

## 1. Работа

Шаг CK5.1 `plans/118`: «Одна именованная константа с источником в комментарии, девять шаблонов; мутант „руководство в 1200 строк“
красный» — строка «Проверка» критерия 1 `plans/117` («гард сборки „шаблон ≤ бюджет − запас“ красный на мутанте в 1200 строк»). Запас —
10 % по решению CK5.0 (`researches/33` §7 (б), «Пересмотрено второй раз»).

Сделано: гард 5j в `tools/check-framework.mjs` — `TEMPLATE_RESERVE_PCT = 10` с источником в комментарии; бюджеты девяти документов ядра
берутся из `DOC_BUDGETS` ядра поставки через `readBudgets()` двери истока `tools/budget-gate.mjs` (копии чисел нет); потолок считается
целыми процентами; блок `@guard template-ceiling`; самодоказательство в `--selftest`.

## 2. Контур

Исток KAIF, Windows 11, Git Bash, Node v24; сборщик и валидатор поставки; копия дерева из `git archive HEAD` в скретчпаде для мутанта.
Функционального пути у гарда сборки нет — его путь — сборка истока.

## 3. Прогоны

| # | Момент | Команда | Исход |
|---|---|---|---|
| 1 | 2026-09-25, между 00:41:45 (коммит `7b1fbf9`) и 00:43:57 (старт полигона) +03:00 | `node tools/check-framework.mjs --selftest` | код 0; строка «гард 5j: шаблон в 1200 строк при бюджете 1200 назван с потолком; шаблон на потолке молчит; пропавший шаблон и неполная таблица названы» |
| 2 | там же | `node tools/build-framework.mjs` | код 0 — девять шаблонов под потолком |
| 3 | там же | копия `git archive HEAD` + новый `check-framework`, `framework/AGENT_GUIDE.md` дополнен до 1200 строк → `node tools/check-framework.mjs` | код 1; строка «template ceiling (CK 2.8): framework/AGENT_GUIDE.md is 1200 lines, above its ceiling 1080 (budget 1200 less the 10 % reserve) — move content out…» |
| 4 | там же | та же копия, руководство возвращено (`git show HEAD:framework/AGENT_GUIDE.md`) → `node tools/check-framework.mjs` | код 0; строк «template ceiling» — 0 |
| 5 | 2026-09-25 00:43:57 → 00:46:43 +03:00 | `npm run test:core` (в одиночку) | код 0, «all 27 suites green» |

## 4. Проверки

Гигиена: самодоказательство гарда — оба ответа (мутант формы v2.7 назван, шаблон на потолке молчит) плюс два отказа формы (пропавший
шаблон, таблица из восьми строк); мутант на копии дерева — красный с точной строкой, контроль на той же копии — зелёный; сборка;
полигон 27 из 27.
Функциональный прогон: NONE — гард сборки истока; его «реальный путь» — сборка, и она наблюдена (прогоны 2–4).

Потолки на сегодня (`wc -l` против `budget × 90 %`): `AGENT_GUIDE.md` 1080 / 1080 · `TESTING_FRAMEWORK.md` 269 / 270 ·
`PHILOSOPHY.md` 257 / 270 · `REQUIREMENTS_FRAMEWORK.md` 215 / 225 · `BUG_FIXING_FRAMEWORK.md` 210 / 270 · `STATUS.md` 82 / 180 ·
`GOAL.md` 42 / 270 · `MASTER_PLAN.md` 53 / 270 · `PROJECT_STRUCTURE_EXTERNAL_MAP.md` 44 / 270.

## 5. Найдено

- Руководство стоит ровно на потолке (1080 из 1080): любая следующая правка шаблона руководства в 2.8, которая добавляет строку, обязана
  вынести другую — гард не даст собрать иначе. Это и есть задуманное поведение, но цена у эпиков OW и CH — вынос рядом с каждой вставкой.
- Дефектов — ноль.

## 6. Следы

`tools/check-framework.mjs` (гард 5j, блок `@guard template-ceiling`, `selfProofTemplateCeiling`) · логи прогонов 3–5 — скретчпад сессии
(`mut5j.log`, `mut5j-ctl.log`, `polygon3.log`); строки вывода — дословно в таблице §3.

## 7. Вердикт

**partial** — гигиена гарда полная: красный на мутанте формы v2.7 на копии дерева и на фикстуре, зелёный на живом дереве; функциональный
прогон — NONE по природе работы (гард сборки).
