# Отчёт прогона — эпик CK 2.8, шаг CK5.2: храповик двери бюджета

**Создан:** 2026-09-25 01:02 +03:00 · **Прогон вёл:** Claude Opus 5.5 (1M context), сессия 72 · **Версия/сборка:** HEAD `5f08d30` + рабочее дерево шага

## 1. Работа

Шаг CK5.2 `plans/118` по развилкам (д) и (е) `researches/33` §7: база `.kaif/budget-baseline.json` (коммитится), проход при убывании,
стоп при росте и при стоянии выше бюджета (слово владельца №75 целиком), первое закрытие после обновления пишет базу и проходит.
Критерий 4 `plans/117`: «убывающий долг проходит (храповик)». Тикет истока #84.

Сделано: `framework/installer/KAIF-CORE.mjs` — константа `BUDGET_BASELINE` и чистая функция `budgetRatchet()` рядом с `DOC_BUDGETS`;
дверь `check --gate-budgets` читает и переписывает базу на КАЖДОМ прогоне (и зелёном — наличие файла делает следующее превышение новым);
решение по документу: первая дверь версии — долг записан, проход; убывание — проход и база затягивается; рост, стояние, новое
превышение — стоп; документ под бюджетом — из базы вычищен; нечитаемая база — стоп с подсказкой `git checkout`. Свод `s16` — раздел
(6) и ассерт «зелёный прогон записал пустую базу»; проба `budget-mutants` — якорь M2 под новую строку двери, M6 до +130 строк (шаблон
теперь на потолке 1080), шесть мутантов храповика M7–M12. `/end-chat-soft` обоих слоёв — абзац о храповике и коммите файла базы;
записка — строка `STATUS.md` таблицы ключевых документов; `POLICY_CHANGES_BY_VERSION['2.8']` — смена контракта двери для поля.

## 2. Контур

Исток KAIF, Windows 11, Git Bash, Node v24; развёрнутая копия из `dist` во временном корне свода; шов `KAIF_DIST` — `dist` тега v2.7,
извлечённый `git show` в скретчпад. Функциональный путь двери — закрытие чата полевого развёртывания после обновления до 2.8; до выпуска
2.8 он не наблюдаем.

## 3. Прогоны

| # | Момент | Команда | Исход |
|---|---|---|---|
| 1 | 2026-09-25, между 00:48 и 00:57 +03:00 (минуты не сняты) | `node tools/build-framework.mjs` → `node tools/sandbox/s16-doc-budgets.mjs` | первый — ОТКАЗ сборки гарда 5h: символ U+FEFF в теле `s16` (набран escape в параметре правки — `bugs/122`), починено `node tools/sandbox/probes/invisible-characters.mjs --fix …`; первый прогон свода — 1 красный: в сценарии «STATUS ушёл под бюджет» руководство стояло на месте и храповик законно остановил дверь — сценарий поправлен; далее 59 из 59 |
| 2 | там же | `KAIF_DIST=<скретчпад>/dist-v27 node tools/sandbox/s16-doc-budgets.mjs` | код 1, 13 из 59 красных — все ассерты храповика и только они |
| 3 | там же | `node tools/sandbox/probes/budget-mutants.mjs --list` (дважды — после правок сценария) | адресаты названы до судящего прогона; M6 со старыми +63 строками не краснел ничего — поднят до +130; M9 в первой формулировке ронял ядро — заменён двумя чистыми (M9, M12) |
| 4 | 2026-09-25 00:58:27 → 00:59:18 +03:00 | `node tools/sandbox/probes/budget-mutants.mjs` (финальный `dist`, в одиночку) | код 0: «12 mutants red exactly on their named addressees, and only on them — no invisible mutant» |
| 5 | 2026-09-25, между 00:59:18 и 00:59:32 +03:00 | `KAIF_DIST=<скретчпад>/dist-v27 node tools/sandbox/s16-doc-budgets.mjs` (свод после правок) | код 1, 13 из 59 — ассерты храповика |
| 6 | 2026-09-25 00:59:32 → 01:02:17 +03:00 | `npm run test:core` (в одиночку) | код 0, «all 27 suites green» |

## 4. Проверки

Гигиена: свод `s16` 59 из 59 на развёрнутой копии; красный на ядре 2.7 — 13 из 59, ровно раздел храповика; двенадцать адресных мутантов
предикатов двери и храповика — каждый красный на своих ассертах и только на них; сборка; стражи счётчиков; полигон 27 из 27.
Функциональный прогон: NONE — путь двери поля (первое закрытие после обновления до 2.8) до выпуска не наблюдаем; на сессии это — строка
`ON-REAL-PATH: NOT YET` стража `doc-budgets`.

Что ассерты раздела (6) судят (`tools/sandbox/s16-doc-budgets.mjs`): зелёный прогон двери — база есть и пуста · база стёрта → «debt
recorded», код 0, база = напечатанные числа · без изменений → «stood still», код 1 · убывание → «shrinking A → B», код 0, база
затянута · рост → «grew», код 1, база НЕ поднята, соседний документ, который убывает, назван проходящим · новый документ за бюджетом →
«crossed … a new overflow is never free», код 1, в базу не записан · STATUS под бюджетом → из базы вычищен, файл остаётся · версия
маркера сменилась → долг переписан, код 0 · нечитаемая база → «unreadable — restore it from git», код 1.

## 5. Найдено

- Грабля `bugs/122` сработала на мне, хотя стояла строкой «Действует» в STATUS: escape «обратная косая + u + четыре цифры» в параметре
  правки лёг в `s16` настоящим U+FEFF. Страж сборки 5h поймал его до коммита — класс механизирован и держит.
- Мутант M6 (рост поставленного шаблона за бюджет) после CK5.0а стал невидимым при старых +63 строках: шаблон на потолке 1080, а бюджет
  1200. Поднят до +130; первая линия против роста поставки теперь — гард сборки 5j, свод — вторая.
- Дефектов продукта — ноль.

## 6. Следы

Логи — скретчпад сессии (`s16d.log`, `s16-v27b.log`, `mut-judge2.log`, `polygon5.log`); итоговые строки — дословно в таблице §3.
`dist` тега v2.7 — `git show v2.7:dist/<файл>`, воспроизводим.

## 7. Вердикт

**partial** — храповик доказан на развёрнутой копии, красным на 2.7 и двенадцатью мутантами; функциональный прогон на пути поля — после
выпуска 2.8.
