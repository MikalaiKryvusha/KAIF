# Отчёт прогона — эпик CK 2.8, шаг CK5.3: объявленный архив владельца судится по дайджесту

**Создан:** 2026-09-25 01:16 +03:00 · **Прогон вёл:** Claude Opus 5.5 (1M context), сессия 72 · **Версия/сборка:** HEAD `6d49df7` + рабочее дерево шага

## 1. Работа

Шаг CK5.3 `plans/118` по развилке (г) `researches/33` §7: `.kaif/kaif.json` → `"archives": { "<документ>": "<дайджест>" }`
объявляет только владелец, как `canonArtifacts`; ворота судят дайджест архива, а не его длину. Критерий 4 `plans/117`: «Архив проходит по
дайджесту». Тикет истока #84 п. 1 (полевой владелец объявил `GOAL.md` дословным дописываемым архивом своих слов; единственное лекарство
двери — «вынеси» — ровно то, что его решение запрещает агенту).

Сделано: ядро — ветка архива в цикле бюджета `check` (архив — строка «ℹ … information, never a stop», бюджет несёт дайджест; дайджест
обязан называть свой архив по имени файла; пропавший или безымянный дайджест архива не прикрывает — архив судится как документ;
короткая форма без слова владельца называется вслух с готовой полной формой) и проверка формы поля в схеме маркера; свод `s16` —
раздел (7); проба мутантов — M13–M15 и новые адресаты M2/M8/M12; канон — ярус 1 таксономии обоих слоёв руководства (место в шаблоне —
снятием заглушки «задокументируй инструмент коммитов», факта проекта того же класса, что таблица инструментов), `/end-chat-soft` обоих
слоёв, записка §12.1, `POLICY_CHANGES_BY_VERSION['2.8']`.

## 2. Контур

Исток KAIF, Windows 11, Git Bash, Node v24; развёрнутая копия из `dist` во временном корне свода; шов `KAIF_DIST` — `dist` тега v2.7 в
скретчпаде. Функциональный путь — закрытие чата полевого развёртывания с объявленным архивом после обновления до 2.8; до выпуска не
наблюдаем.

## 3. Прогоны

| # | Момент | Команда | Исход |
|---|---|---|---|
| 1 | 2026-09-25, между 01:03 и 01:12 +03:00 (минуты не сняты) | `node tools/build-framework.mjs` → `node tools/sandbox/s16-doc-budgets.mjs` | код 0 и 0; «all 65 checks green» (раздел архива — 6 ассертов) |
| 2 | там же | `node tools/sandbox/probes/budget-mutants.mjs --list` → судящий прогон | адресаты названы до суда; «15 mutants red exactly on their named addressees, and only on them — no invisible mutant» |
| 3 | там же | `KAIF_DIST=<скретчпад>/dist-v27 node tools/sandbox/s16-doc-budgets.mjs` | код 1, 18 из 65: 13 храповика и 5 архива (шестой ассерт архива — «без объявления превышение названо» — прежнее поведение, на 2.7 тоже зелёный) |
| 4 | 2026-09-25 01:12:31 → 01:15:30 +03:00 | `npm run test:core` (в одиночку) | код 0, «all 27 suites green» |

## 4. Проверки

Гигиена: свод `s16` 65 из 65; красный на ядре 2.7 — ровно новые ассерты; пятнадцать адресных мутантов (M13 дайджесту необязательно
называть архив · M14 объявление игнорируется · M15 схема пропускает архив не из ядра) — каждый краснит свои ассерты и только их;
сборка; стражи счётчиков, авторства, языковых пакетов, шапок; полигон 27 из 27.
Функциональный прогон: NONE — путь поля (закрытие с объявленным архивом после обновления до 2.8) до выпуска не наблюдаем.

## 5. Найдено

- Решение «кто объявляет архив» машина не судит: ядро проверяет форму и напоминает назвать место слова владельца, а объявление без его
  слова — охота судьи (развилка (г)); в `/end-chat-soft` стоит «never declare one on your own».
- Дефектов продукта — ноль.

## 6. Следы

Логи — скретчпад сессии (`s16e.log`, `mut-judge3.log`, `s16-v27c.log`, `polygon7.log`); итоговые строки — дословно в таблице §3.

## 7. Вердикт

**partial** — архив доказан на развёрнутой копии, красным на 2.7 и мутантами; функциональный прогон на пути поля — после выпуска 2.8.
