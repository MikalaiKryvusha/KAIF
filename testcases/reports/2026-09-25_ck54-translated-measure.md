# Отчёт прогона — эпик CK 2.8, шаг CK5.4: мера файла, переведённого целиком

**Создан:** 2026-09-25 01:30 +03:00 · **Прогон вёл:** Claude Opus 5.5 (1M context), сессия 72 · **Версия/сборка:** HEAD `d0aab56` + рабочее дерево шага

## 1. Работа

Шаг CK5.4 `plans/118` по развилке (в) `researches/33` §7: мера файла, переведённого целиком, — те же строки; строка предупреждения
называет, сколько места оставил шаблон, и что местные разделы уезжают в `HOUSE_RULES.md` (тикет #85 п. 2). Критерий 4 `plans/117`:
«переведённый целиком файл судится мерой, у которой есть запас».

Сделано: ядро — поле `templateLines` манифеста развёртывания (длина развёрнутого шаблона каждого из девяти документов ядра) в обоих
писателях, установки и обновления; ветка «translated wholesale» предупреждения бюджета дописывает «the shipped template is N lines,
which leaves ≈ M for your translation's growth and your own adaptation — local sections belong in HOUSE_RULES.md»; манифест до 2.8 без
поля — прежняя фраза, числа не выдумываются. Длина считается вспомогательной функцией без обратной косой (перевод строки — по коду
символа), см. §5. Свод `s16` — ассерт в разделе (5); проба мутантов — M16.

## 2. Контур

Исток KAIF, Windows 11, Git Bash, Node v24; развёрнутая копия из `dist` во временном корне свода; шов `KAIF_DIST` — `dist` тега v2.7.
Функциональный путь — `check` полевого развёртывания, переведённого целиком, после обновления до 2.8; до выпуска не наблюдаем.

## 3. Прогоны

| # | Момент | Команда | Исход |
|---|---|---|---|
| 1 | 2026-09-25, между 01:16 и 01:24 +03:00 (минуты не сняты) | правка писателей манифеста скриптом `node <скретчпад>/tl.mjs` → `node --check framework/installer/KAIF-CORE.mjs` | ОТКАЗ разбора: heredoc Bash-инструмента съел уровень экранирования, `\n` в регулярном выражении лёг настоящим переводом строки (EXP-0121); починено правкой файла и функцией без обратной косой — «parses» |
| 2 | там же | `node tools/build-framework.mjs` → `node tools/sandbox/s16-doc-budgets.mjs` | код 0 и 0; «all 66 checks green» |
| 3 | 2026-09-25 01:24:48 → 01:26:20 +03:00 | `node tools/sandbox/probes/budget-mutants.mjs` | первый прогон — BAD M16: адресат был записан строкой вместе с кавычками внутри, ассерт тот же, сравнение не сошлось; поправлено; «16 mutants red exactly on their named addressees, and only on them» |
| 4 | 2026-09-25, после 01:26:20 | `KAIF_DIST=<скретчпад>/dist-v27 node tools/sandbox/s16-doc-budgets.mjs` | код 1, 19 из 66 — 13 храповика, 5 архива, 1 новый ассерт меры |
| 5 | 2026-09-25 01:30:54 → 01:33:48 +03:00 | `npm run test:core` (в одиночку) | код 0, «all 27 suites green» |

## 4. Проверки

Гигиена: свод `s16` 66 из 66; новый ассерт на ядре 2.7 красный; мутант M16 (фраза о месте шаблона пропала) красный ровно на нём;
сборка; стражи.
Функциональный прогон: NONE — путь поля до выпуска не наблюдаем.

Что судит ассерт: длина шаблона в строке предупреждения равна длине развёрнутого `PHILOSOPHY.md` до перевода, а сумма «шаблон + место»
равна бюджету 300 — число не выдумано, а снято с того, что развёрнуто.

## 5. Найдено

- Грабля EXP-0121 (Bash-инструмент снимает уровень экранирования) сработала при записи скрипта правки через heredoc: `\n` лёг
  настоящим переводом строки внутри регулярного выражения, ядро перестало разбираться. Поймано `node --check` до сборки. Ход на будущее:
  скрипт с обратной косой пишется инструментом Write, а код, где можно, обходится без обратной косой вовсе.
- Дефектов продукта — ноль.

## 6. Следы

Логи — скретчпад сессии (`s16f.log`, `mut-judge5.log`, `s16-v27d.log`); итоговые строки — в таблице §3.

## 7. Вердикт

**partial** — мера доказана на развёрнутой копии, красным на 2.7 и мутантом; функциональный прогон на пути поля — после выпуска 2.8.
