# Отчёт прогона — эпик CK 2.8, шаг CK5.5 и K14 шага CK5.8: адрес выноса называет файл; `bugs/117` закрыт; счёт английских навыков — только при `i18n`

**Создан:** 2026-09-25 02:01 +03:00 · **Прогон вёл:** Claude Opus 5.5 (1M context), сессия 72 · **Версия/сборка:** HEAD `40a9f1d` + рабочее дерево шага

## 1. Работа

Шаг CK5.5 `plans/118`: «`MOVE_OUT_ADDRESS` — `HOUSE_RULES.md` и команда копии скелета (лёгкий судья CK4.5, находка 4); второй адрес у
`STATUS.md` (K16). Первая правка ядра 2.8 — вместе с ней закрытие `bugs/117`». Базис: строка K16 `researches/32` («у STATUS адрес
выноса — только летопись») и критерий закрытия `bugs/117` («DONE — после первого полевого `check` на обновлённом до 2.7 развёртывании с
`tracking: origin`»).

Сделано: ядро — общий адрес выноса называет файл и команду («HOUSE_RULES.md (no file yet: cp .kaif/_house-rules-template.md
HOUSE_RULES.md) for local rules, routes and tools · the chronicle PROJECT_HISTORY.md · researches/»), у STATUS второй адрес — домашние
правила для стоячих правил и справочных таблиц; строки остались простыми строками в одинарных кавычках, потому что таблицу бюджетов
читают текстом дверь истока и гард 5j; блок `@guard undelivered-signal` — `ON-REAL-PATH` с четырьмя полевыми наблюдениями вместо
`NOT YET`; `bugs/117` → DONE (раздел закрытия с «Гигиена» и «Функциональный прогон»). Свод `s16` — два ассерта адреса обновлены, один
добавлен; проба — мутант M17.

K14 шага CK5.8 (тот же файл ядра, поэтому тем же коммитом): базис — строка K14 `researches/32` («`check` печатает „37 of 37 skills
are English“ на каждом прогоне развёртывания с языковым пакетом без `i18n`», CONFIRMED, косметика). Сделано: строка счёта английских
навыков печатается только у развёртывания, чей маркер объявляет `i18n: translated` (там английский навык выделяется; в остальных
английские навыки — политика поставки); строка о СМЕСИ осталась без условия. Свод `s16` — прежний ассерт «строка про английские навыки
жива и отдельна» заменён двумя: без `i18n` строки нет · с `i18n: translated` (маркер правится и возвращается) — есть рядом со строкой
смеси; проба — мутант M18 (флаг снова игнорируется), а M4 (строки смеси нет) теперь называет и новый ассерт — он судит строку РЯДОМ со
строкой смеси.

## 2. Контур

Исток KAIF, Windows 11, Git Bash, Node v24; развёрнутая копия из `dist` во временном корне свода; шов `KAIF_DIST` — `dist` тега v2.7.
Функциональный путь адреса — строка предупреждения и двери у полевого развёртывания после обновления до 2.8; до выпуска не наблюдаем.
Функциональный путь `bugs/117` наблюдён полем (§4).

## 3. Прогоны

| # | Момент | Команда | Исход |
|---|---|---|---|
| 1 | 2026-09-25, между 01:38 и 01:40 +03:00 (минуты не сняты) | `node -e "import('./tools/budget-gate.mjs')…readBudgets(…)"` | первая редакция адреса — шаблонной строкой с подстановкой; до сборки понял, что `readBudgets()` читает таблицу текстом и прочёл бы 8 строк из 9; переписано простыми строками — «9» |
| 2 | там же | `node tools/build-framework.mjs` → `node tools/sandbox/s16-doc-budgets.mjs` → `node tools/budget-gate.mjs` → `node tools/check-framework.mjs --selftest` | код 0 везде; «all 67 checks green»; «budget gate OK» |
| 3 | 2026-09-25 01:40:35 → 01:42:13 +03:00 | `node tools/sandbox/probes/budget-mutants.mjs` | «17 mutants red exactly on their named addressees, and only on them — no invisible mutant» |
| 4 | 2026-09-25, после 01:42:13 | `KAIF_DIST=<скретчпад>/dist-v27 node tools/sandbox/s16-doc-budgets.mjs` | код 1, 22 из 67 — все ассерты 2.8 (храповик 13, архив 5, мера 1, адрес 3) |
| 5 | 2026-09-25 01:42:29 → 01:45:40 +03:00 | `npm run test:core` (в одиночку; дерево CK5.5, до правки K14) | код 0, «all 27 suites green» |
| 6 | 2026-09-25, лог записан 01:46:07 +03:00 | `node tools/build-framework.mjs` → `node tools/sandbox/s16-doc-budgets.mjs` (правка K14) | код 0; «all 68 checks green» |
| 7 | 2026-09-25 01:46:20 → 01:48:06 +03:00 | `node tools/sandbox/probes/budget-mutants.mjs` (с M18) | код 1, «mutant proof FAILED: 1 of 18» — M4 красный на пяти ассертах при четырёх названных: пятый — новый ассерт K14 «жива и отдельна от строки о смеси» (он судит строку РЯДОМ со строкой смеси, а M4 её убирает); адресат дописан в M4 |
| 8 | 2026-09-25, лог записан 01:48:12 +03:00 | `KAIF_DIST=<скретчпад>/dist-v27 node tools/sandbox/s16-doc-budgets.mjs` | код 1, «23 of 68 check(s) failed» — ассерты 2.8 и ассерт K14 «без i18n строки нет» |
| 9 | 2026-09-25 01:50:54 → 01:52:40 +03:00 | `node tools/sandbox/probes/budget-mutants.mjs` | код 0, «18 mutants red exactly on their named addressees, and only on them — no invisible mutant» |
| 10 | 2026-09-25 01:52:57 → 01:55:48 +03:00 | `node tools/build-framework.mjs` → `npm run test:core` (в одиночку) | код 1, «1 of 27 suites FAILED — s18-update-symmetries.mjs»: красен ровно U6 — он ждал строку «N of M skills are English» у `install --lang ru` без `i18n`, то есть ровно то, что K14 снимает; близнец, не найденный до полигона |
| 11 | 2026-09-25 01:56:07 → 01:56:18 +03:00 | `node tools/sandbox/s18-update-symmetries.mjs` (U6 переписан под K14) | код 0, «s18: all green» |
| 12 | 2026-09-25 01:57:20 → 01:57:52 +03:00 | `node <скретчпад>/red18.mjs <исток> <копия> control`, затем без `control` — копия `tools/` и `dist/`, у мутанта ядро снова игнорирует флаг, sha ядра в манифесте копии перепинен | контроль: код 0, «s18: all green»; мутант: код 1, «s18: 1 red» — ровно U6 |
| 13 | 2026-09-25 01:57:58 → 02:00:53 +03:00 | `npm run test:core` (в одиночку) | код 0, «all 27 suites green» |

## 4. Проверки

Гигиена: свод `s16` 68 из 68; ассерты адреса и ассерт K14 «без i18n» на ядре 2.7 красные; мутант M17 (адрес снова без файла) красный
ровно на двух ассертах адреса, M18 (флаг `i18n` игнорируется) — ровно на ассерте K14; свод `s18` U6 (переписан под K14) — зелёный,
на копии с тем же мутантом красен ровно он; дверь истока читает таблицу девятью строками; сборка и самодоказательства; полигон.
Функциональный прогон: для адреса и для K14 — NONE (путь поля до выпуска не наблюдаем). Для `bugs/117` — поле: первый собственный `check` после
обновления до 2.7 на четырёх развёртываниях с `tracking: origin` (отчёты #76, #79, #83, #93; цитаты — `researches/32` §2д).

## 5. Найдено

- Своё утверждение в разделе закрытия `bugs/117` поймано до коммита: номер свода оси доставки был написан по памяти («`s23`»), grep
  нашёл `s17` — исправлено.
- Близнец K14, найденный полигоном, а не поиском до правки: свод `s18` U6 (эпик US 2.5, тикет #32) требовал строку счёта английских
  навыков у развёртывания без `i18n`. Поиск близнецов я запустил ПОСЛЕ правки ядра и свода `s16`, и он нашёл U6 одновременно с красным
  полигона. U6 переписан: новинку называет задание обновления (ассерт выше в том же U6), `check` счёт не повторяет. Урок класса — в
  журнал опыта на закрытии сессии.
- Дефектов продукта — ноль.

## 6. Следы

Логи — скретчпад сессии (`s16g.log`, `mut-judge6.log`, `s16-v27e.log`, `polygon9.log`; K14 — `s16h.log`, `mut-judge7.log`,
`mut-judge8.log`, `s16-v27f.log`, `poly10.log`, `s18a.log`, `red18c.log`, `red18m.log`, `poly11.log`; скрипт мутации на копии — `red18.mjs`).

## 7. Вердикт

**partial** — адрес и K14 доказаны на развёрнутой копии, красным на 2.7 и мутантами; `bugs/117` закрыт по полевому наблюдению;
функциональный прогон адреса и K14 — после выпуска 2.8.
