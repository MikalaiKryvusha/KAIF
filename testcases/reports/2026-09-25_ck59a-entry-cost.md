# Отчёт прогона — эпик CK 2.8, шаг CK5.9 (а): `check` печатает цену входа в чат в токенах

**Создан:** 2026-09-25 02:32 +03:00 · **Прогон вёл:** Claude Opus 5.5 (1M context), сессия 72 · **Версия/сборка:** HEAD `be71f73` + рабочее дерево шага

## 1. Работа

Шаг CK5.9 (а) `plans/118` — критерий 21 `plans/117`; слово владельца — интервью №035, Q2 = A (2026-09-25 01:32 +03:00): «На закрытии
чата вы видите строку „вход ≈ 230 тыс. токенов — 23 % памяти модели“ — справкой, закрытие она не останавливает»; тикет #99. Разведка
констант — коммит `be71f73` (проба `tools/sandbox/probes/ck59-token-calibration.mjs` на той версии файла поля, что мерил тикет).

Сделано: ядро поставки — `check` печатает `ℹ entry cost: /resume reads N re-read core document(s)[ + HOUSE_RULES.md] ~ Nk tokens —
X % of a 1M-token model window (reference, never a stop; ~ 2.5 ASCII / 1.9 other characters per token)`; две именованные ставки и окно —
константы с источником в комментарии; код возврата не меняется. `KAIF_REFERENCE.md` — оговорка «since 2.8» в описании осей `check`
(шесть изменений эпика: храповик, архив, мера переведённого, адрес выноса, счёт английских навыков, цена входа) — близнец, которого не
нашли поиски шагов CK5.2–CK5.8. Свод `s16` — раздел (9), два ассерта; проба мутантов — M19, M20. Половина (б) — портрет голоса
разделами — не начата.

## 2. Контур

Исток KAIF, Windows 11, Git Bash, Node v24; развёрнутая копия из `dist` во временном корне свода; шов `KAIF_DIST` — `dist` тега v2.7.
Функциональный прогон — временное развёртывание `install --lang ru` из `dist` в скретчпаде, поверх — копии девяти документов ядра
полевого развёртывания NDim (HEAD `8db7aab` 2026-09-25 02:15:36 +03:00; сам проект только читался; `HOUSE_RULES.md` у него нет).

## 3. Прогоны

| # | Момент | Команда | Исход |
|---|---|---|---|
| 1 | 2026-09-25 02:20:09 → 02:20:15 +03:00 | `node tools/build-framework.mjs` → `node tools/sandbox/s16-doc-budgets.mjs` | код 0 и 0; «all 72 checks green» |
| 2 | 2026-09-25 02:20:15 → 02:20:28 +03:00 | `KAIF_DIST=<скретчпад>/dist-v27 node tools/sandbox/s16-doc-budgets.mjs` | код 1, «27 of 72 check(s) failed» — оба ассерта (9) красные |
| 3 | 2026-09-25 ≈ 02:20:44 +03:00 | временное развёртывание: `node .kaif/kaif-core.mjs install --lang ru` → `check`; затем копии девяти документов NDim → `check` | свежее развёртывание: «~ 62k tokens — 6 % of a 1M-token model window»; с документами NDim (765 724 байта): код 0, «ℹ entry cost: /resume reads 9 re-read core document(s) ~ 225k tokens — 23 % of a 1M-token model window (reference, never a stop; ~ 2.5 ASCII / 1.9 other characters per token)» |
| 4 | 2026-09-25 02:21:07 → 02:23:12 +03:00 | `node tools/sandbox/probes/budget-mutants.mjs` (с M19, M20) | код 1, «mutant proof FAILED: 2 of 20» — число красных верно (1 и 2), но адресаты записаны частью имени ПОСЛЕ « — », а проба режет имя на первом « — »; адресаты переписаны |
| 5 | 2026-09-25 02:23:33 → 02:25:40 +03:00 | `node tools/sandbox/probes/budget-mutants.mjs` | код 0, «20 mutants red exactly on their named addressees, and only on them — no invisible mutant» |
| 6 | 2026-09-25 02:26:20 → 02:29:08 +03:00 | `node tools/build-framework.mjs` → `node tools/canon-inventory.mjs --diff v2.7 …` → `node tools/budget-gate.mjs` → `npm run test:core` (в одиночку) | сборка 0; «moved-out 0 · lost 0» (одна единица «changed» — абзац осей `check` справочника); «budget gate OK»; полигон — код 1, «1 of 27 suites FAILED — s25-testrun-lint.mjs»: ассерт CK4.5 «копия с HOUSE_RULES.md — check зелёный и про файл проекта молчит» увидел новую справочную строку, которая называет файл, потому что `/resume` его читает |
| 7 | 2026-09-25 02:29:34 → 02:29:39 +03:00 | `node tools/sandbox/s25-testrun-lint.mjs` (ассерт сужен: жалоб на файл нет, а строка цены его считает) | код 0, «all 63 checks green» |
| 8 | 2026-09-25: свод на 2.7 — завершён 02:29:51; полигон — 02:29:51 → 02:32:38 +03:00 | `KAIF_DIST=<скретчпад>/dist-v27 node tools/sandbox/s25-testrun-lint.mjs` → `npm run test:core` (в одиночку) | на 2.7: код 1, «10 of 63 check(s) failed», сужённый ассерт красный (строки цены нет); полигон: код 0, «all 27 suites green» |

## 4. Проверки

Гигиена: свод `s16` 72 из 72 — число тысяч токенов и доля окна равны НЕЗАВИСИМОМУ пересчёту по диску; файл домашних правил известного
веса (25 000 символов ASCII ≈ 10 тыс. токенов) добавляет «+ HOUSE_RULES.md» и свой вес; на ядре 2.7 оба красные; мутанты M19 (окно
200 тыс. при словах «1M») и M20 (строка не печатается) красные ровно на своих ассертах; инвентарь канона; сборка; полигон.
Функциональный прогон: копии реальных документов поля — путь пользователя «закрытие чата запускает `check`»: прочитана строка
«~ 225k tokens — 23 % of a 1M-token model window», сходится со сценарием владельца «≈ 230 тыс. токенов — 23 % памяти модели» (тикет
мерил 810 567 байт, с тех пор документы NDim похудели до 765 724).
РЕАЛЬНЫЙ МИР: накоплено — девять документов NDim как есть (проверено с реальным состоянием, снятым с реального: копии файлов, сам
проект не тронут); данные и техника — Windows 11, Node v24, та же машина, что у NDim (проверено на реальном); путь — ядро из `dist`
истока во временном развёртывании, а не `.kaif/kaif-core.mjs` NDim (у NDim ядро 2.7; путь через обновление — после выпуска 2.8).

## 5. Найдено

- Близнец в `KAIF_REFERENCE.md`: описание осей `check` не знало ни одного изменения эпика CK — поиски близнецов шагов CK5.2–CK5.8
  шли по коду и сводам, не по справочнику. Дописано одной оговоркой «since 2.8».
- Близнец в своде `s25` (CK4.5): ассерт «про файл проекта молчит» судил ЛЮБОЕ упоминание `HOUSE_RULES` в выводе `check` —
  нашёл полигон, не поиск. Ассерт сужен до жалоб, строка цены судится отдельно: она обязана файл сосчитать.
- Адресаты мутантов M19/M20 первой редакцией стояли после « — » в имени ассерта (проба режет имя на первом « — ») — поправлено до
  коммита.
- Дефектов продукта — ноль.

## 6. Следы

Скретчпад сессии: `build15.log`, `s16j.log`, `s16-v27h.log`, `fr-ndim/install.log`, `fr-ndim/check.log`, `mut-judge9.log`,
`mut-judge10.log`, `build16.log`, `poly14.log`.

## 7. Вердикт

**partial** — половина (а) доказана на развёрнутой копии, красным на 2.7, мутантами и функциональным прогоном на копиях документов
поля; половина (б) шага — портрет голоса разделами — не начата.
