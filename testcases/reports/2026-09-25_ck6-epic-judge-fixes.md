# Отчёт прогона — эпик CK 2.8, шаг CK6: починки по находкам лёгкого судьи эпика

**Создан:** 2026-09-25 12:12 +03:00 · **Прогон вёл:** Claude Opus 5.5 (1M context), сессия 74 · **Версия/сборка:** HEAD `69d5442` + рабочее дерево починок

## 1. Работа

Шаг CK6 `plans/118`: лёгкий судья эпика чистым экземпляром по брифу `reports/KAIF_AUDIT/2026-09-25_ck6_epic_judge_brief.md` (с
дополнением сессии 74) вернул **VERIFIED WITH CAVEATS** — ни одно несущее заявление не опровергнуто, семь находок. Этот отчёт — починки
находок 2–7; находка 1 (функциональный прогон сжатого руководства, критерий 1) — отдельный отчёт
`testcases/reports/2026-09-25_ck6-guide-functional-run.md`.

Находки и что сделано:
- **2 — у случая журнала критерия 4 нет свода и мутанта** (свёртка 88 записей в строку; мутанты CK5.7 жили в скретчпаде): свод `s28`
  — раздел развёрнутой копии, два ассерта (свёртка ОДНОЙ строкой с поимённой записью ПОСЛЕ первой классной; `--verbose` — все 89
  поимённо); новая проба `tools/sandbox/probes/fold-mutants.mjs` (три мутанта предикатов свёртки).
- **3 — линт сценариев, 5 находок «order» на `plans/117`**: дефект класса в модуле поставки `framework/tools/kaif-scenario-lint.mjs` —
  нумерованный пункт `N. **[…]**` не закрывал блок сценария, критерии без пустых строк сливались в один; нумерованный пункт теперь
  закрывает блок, случай селфтеста на обоих языках; четыре настоящие находки формы (критерии 10, 13 ×2, 17) переданы строкой в
  `plans/117` эпикам CH и SC.
- **4 — решения `[ИИ]` 10, 12, 14 без строки `FORK:`**: строки дописаны, и к решению 11 тоже; решение 14 объявлено отступлением от
  текста пункта.
- **5 — стоячая ложь в статусных строках**: `plans/118` шапка (пункт «утечка» был «следующим», уже ✅) · `plans/117` шапка
  («следующий — CK5») · `STATUS.md` заголовок п. 1 («следующий шаг — CK5.9 (б)») · `plans/118` отметка CK2 (769 без поправки на
  перезапись эталона 774 — поправка дописана на месте).
- **6 — имена полевых проектов в сообщениях коммитов**: не механизировано — зоны стража решает владелец; долг владельца в
  `STATUS.md` дописан числами судьи; решение `[ИИ]` 15 с `FORK:` (практика агента — имён в сообщениях не писать).
- **7 — у пробы `tools/sandbox/probes/ck59-token-calibration.mjs` нет отметки статуса**: переисполнена на той же версии файла поля,
  отметка `[TESTED]` поставлена.

## 2. Контур

Исток KAIF, Windows 11, Git Bash, Node v24. Своды — на свежем `dist` и на `dist` тега v2.7 швом `KAIF_DIST`; мутанты — на копии
`dist` в OS-temp; мутанты модулей истока — на копиях файлов в скретчпаде; проба калибровки — на версии `MASTER_PLAN.md` полевого
развёртывания `93a92e7` (`git show` в его репозитории, только чтение).

## 3. Прогоны

| # | Момент | Команда | Исход |
|---|---|---|---|
| 1 | 2026-09-25 11:41 +03:00 | `node tools/sandbox/probes/ck59-token-calibration.mjs <версия 93a92e7> 40880` | «bytes 140597 · chars 84449 · ascii 29203 · cyrillic 53830 · other 1416», «non-ascii chars/token 1.892» — числа замера |
| 2 | 2026-09-25 ≈ 11:42 +03:00 | `node framework/tools/kaif-scenario-lint.mjs selftest` → `… check plans/117_EPIC_kaif_2.8.md`; прежний модуль (`git show HEAD:…`) на том же плане | 35 случаев OK; «4 finding(s) in 21 scenario(s)» — строки 97 (vague-result) · 112 (implementation-leak, no-concrete-value) · 138 (no-command); прежний — «5 finding(s) in 6 scenario(s)», все «order» |
| 3 | 2026-09-25 ≈ 11:43 +03:00 | мутант на копии модуля без разрыва на нумерованном пункте → `selftest` копии | код 1, «2 of 35 case(s) FAILED» — ровно новый случай, en и ru |
| 4 | 2026-09-25 ≈ 11:44 +03:00 | `node tools/sandbox/s28-experience-lint.mjs` (первая редакция случая) → то же на `dist` v2.7 | «all 37 checks green»; на 2.7 — «5 of 37 check(s) failed» |
| 5 | 2026-09-25 11:44:32 → 11:44:37 +03:00 | `node tools/build-framework.mjs` → `node tools/sandbox/probes/fold-mutants.mjs` | код 1, «3 of 3» BAD: у каждого мутанта краснел и селфтест развёрнутого модуля (адресат не назван); F2 ассерт свёртки НЕ поймал — запись без класса над первой классной была ПОЗЖЕ по дате, её спасало условие по дате, а не место |
| 6 | 2026-09-25 11:45:11 → 11:45:17 +03:00 | `s28` (запись над первой классной — того же дня) → на `dist` v2.7 → `fold-mutants.mjs` (адресат селфтеста назван) | «all 37 checks green»; на 2.7 — 5 красных, ассерт свёртки среди них; мутанты — код 0, «3 mutants red exactly on their named addressees» (2 · 2 · 2) |
| 7 | 2026-09-25 11:46:27 → 11:49:21 +03:00 | `node tools/build-framework.mjs` → `npm run test:core` (в одиночку от других тяжёлых прогонов; параллельно работали два агента функционального прогона руководства в своих временных каталогах — лёгкие команды) | сборка 0; полигон код 0, «all 27 suites green» |

## 4. Проверки

Гигиена: селфтест линта сценариев 35 случаев, красный на мутанте ровно новым случаем; `s28` 37 из 37, на 2.7 ассерт свёртки красный;
три мутанта свёртки на адресатах; полигон — прогон 7.
Функциональный прогон: линт сценариев — на РЕАЛЬНОМ мета-плане версии (`plans/117`): прочитаны 21 сценарий и четыре находки против
прежних шести «сценариев» и пяти ложных «order»; проба калибровки — на реальной версии файла поля, числа прочитаны.
РЕАЛЬНЫЙ МИР: накоплено — мета-план 2.8 как есть и версия файла поля `93a92e7` (проверено на реальном: исток; полевой файл — снятый с
реального, только чтение); данные и техника — та же машина (проверено на реальном); путь — модуль линта истока, а не `.kaif/tools/`
полевых развёртываний (у них 2.7; путь через обновление — после выпуска 2.8).

## 5. Найдено

- Дефект модуля поставки `kaif-scenario-lint`: нумерованный пункт не закрывал сценарий — ложные «order» и спрятанные настоящие находки.
  Починен с селфтестом.
- Слабость нового ассерта `s28`, найденная пробой мутантов: фикстура с более поздней датой не отличала «место» от «даты». Фикстура строже.
- Четыре находки формы в критериях 10, 13, 17 `plans/117` — не этого эпика; переданы строкой CH и SC.
- Дефектов продукта сверх этого — ноль.

## 6. Следы

Скретчпад сессии, каталог `ck59b/`: `ndim-mp-93a92e7.md`, `scen-old.mjs`, `scen-mutant.mjs`, `scen-mut.mjs`, `s28-1.log`, `s28-red.log`,
`fold-mutants-1.log`, `s28-2.log`, `s28-red-2.log`, `fold-mutants-2.log`, `build-6.log`, `build-7.log`, `polygon-4.log`.

## 7. Вердикт

**pass** — находки 2–7 судьи CK6 закрыты: дефект класса в линте сценариев починен с селфтестом и доказан мутантом на реальном
мета-плане; случай журнала критерия 4 получил ассерты и три мутанта, первый прогон пробы укрепил фикстуру; стоячая ложь исправлена в
четырёх местах; строки `FORK:` дописаны; имена в сообщениях коммитов переданы долгом владельца; проба калибровки переисполнена.
