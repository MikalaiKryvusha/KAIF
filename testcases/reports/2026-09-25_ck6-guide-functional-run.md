# Отчёт прогона — эпик CK 2.8, шаг CK6: функциональный прогон сжатого руководства (критерий 1)

**Создан:** 2026-09-25 12:12 +03:00 · **Прогон вёл:** Claude Opus 5.5 (1M context), сессия 74; исполнители — два свежих экземпляра агента · **Версия/сборка:** поставка HEAD `69d5442` (2.8) против поставки тега v2.7

## 1. Работа

Критерий 1 `plans/117` («руководство худеет без потерь») и строка `plans/118` → «Верификация наблюдением»: «сжатие руководства —
функциональный прогон: свежий экземпляр агента получает только новый шаблон и исполняет по нему три задачи-фикстуры (баг · вопрос
владельцу · закрытие чата) — судья сверяет, что обязательные шаги прошли, как по шаблону 2.7». Находка 1 лёгкого судьи эпика CK6:
прогон не был сделан ни одним шагом эпика (отчёты CK3 и CK5.0а — «Функциональный прогон: NONE … на судье эпика»).

Базис сверки — чек-лист обязательных шагов, выведенный из канона 2.7 и ЗАПИСАННЫЙ ДО прогонов (скретчпад сессии,
`guide-run/checklist.md`): баг — B1 документ · B2 тяжесть · B3 воспроизведение · B4 `INTENT:` · B5 проверка наблюдением · B6 `TWINS:` ·
B7 проверка, доказанная красным · B8 маркер статуса · B9 отчёт прогона · B10 «решения без владельца» · B11 коммит; вопрос владельцу —
Q1 в `interviews/` · Q2 сценарии · Q3 язык заказчика · Q4 самодостаточность · Q5 поиск прошлого ответа · Q6 форма вариантов и
рекомендация · Q7 показ не подменён ссылкой, невозможность сказана · Q8 провенанс (где применимо); закрытие — C1 STATUS · C2 стрижка ·
C3 опыт · C4 строка о стоячей лжи · C5 момент пробой `date` · C6 судья · C7 ворота машинерией · C8 коммит.

## 2. Контур

Две СВЕЖИЕ установки во временных каталогах скретчпада: `install` из `dist` истока (2.8, руководство 1078 строк) и из `dist` тега v2.7
(контроль, 1200 строк), по 37 навыков; в обеих одинаковый проект-фикстура — CLI-список дел `todo.mjs` с настоящим багом (пустая задача) и
соседом-близнецом (`done <n>` падает без проверки), `GOAL.md` одной фразой, базовый коммит. Исполнители — два свежих экземпляра агента
(субагенты) с одинаковым заданием: работать только внутри каталога проекта, канон — его документы и навыки, шаг адаптации установки
пропустить, ни окон, ни звука, ни сети; вести `_walk.md` — каждый шаг с разделом канона, который его велел. Ограничения прогона названы
в «Найдено».

## 3. Прогоны

| # | Момент | Команда | Исход |
|---|---|---|---|
| 1 | 2026-09-25 11:39:44–11:39:45 +03:00 (время логов установки) | две установки: `node .kaif/kaif-core.mjs install` из `dist` истока и из `git show v2.7:dist/…` | код 0 и 0; руководство «1078 AGENT_GUIDE.md» и «1200 AGENT_GUIDE.md»; навыков 37 и 37 |
| 2 | 2026-09-25 11:40:13–11:40:14 +03:00 (базовые коммиты `f19e22e` · `de346ed`) | фикстура + базовый коммит; `node todo.mjs add ""` → `node todo.mjs list` в обеих | «added #1», «1. [ ] » — баг воспроизводится в обеих |
| 3 | 2026-09-25 11:40:14 → 12:01:47 +03:00 (последний коммит агента `b569599`) | свежий агент на установке 2.8: три сообщения владельца (задание в `_walk.md` и отчёте агента) | пять локальных коммитов; 38 · 17 · 16 шагов с адресом канона у каждого; «(не смог)» — только запрещённое заданием (дверь контура, показ, пуш) |
| 4 | 2026-09-25 11:40:14 → 12:09:24 +03:00 (последний коммит агента `9c07c5d`) | свежий агент на установке 2.7 (контроль), то же задание | четыре локальных коммита; 45 · 18 · 19 шагов; «(не смог)» — то же |
| 5 | 2026-09-25 12:10:31–12:10:32 +03:00 (время файлов улик) | `node guide-run/evidence.mjs <проект>` для обоих — улики по строкам чек-листа из артефактов и `_walk.md` | таблица ниже |
| 6 | 2026-09-25 между 12:10:32 и 12:12 +03:00 (после прогона 5, до записи отчёта) | `grep` обоих развёрнутых руководств на «clean instance» и правило пробы `date` | чистый экземпляр — только у прохода голоса §7Б в ОБЕИХ версиях; правила «момент — пробой `date`» нет в обеих поставках |

## 4. Проверки

Гигиена: NONE — прогон сам и есть проверка поведения; инвентарь канона (`lost 0`) — отдельный отчёт `2026-09-24_ck2-canon-inventory.md`.
Функциональный прогон: два свежих агента исполнили по развёрнутому канону одни и те же три задачи владельца; прочитаны оба `_walk.md`,
артефакты (документы багов, тест, отчёт прогона, интервью, STATUS, журнал опыта) и строки улик. Сверка по чек-листу:

| Строка | 2.8 | 2.7 (контроль) |
|---|---|---|
| B1–B3 документ, тяжесть S2, воспроизведение | есть | есть |
| B4 `INTENT:` · B6 `TWINS:` | есть (и `TWINS-MECH:`) | есть (и `TWINS-MECH:`) |
| B5 проверка наблюдением по пути владельца | есть (Git Bash и PowerShell) | есть |
| B7 проверка, доказанная красным | сьют 12 кейсов, красный на базовом коммите ровно на плохих аргументах | сьют 6 кейсов, красный ровно на C1–C4 |
| B8 маркеры · B9 отчёт прогона · B10 решения без владельца · B11 коммит | есть | есть |
| Q1 `interviews/` · Q2 сценарии · Q3 язык заказчика · Q4 самодостаточность | есть | есть |
| Q5 поиск прошлого ответа с аттестацией | есть (0 → 9 совпадений, прочитаны) | есть (9 совпадений, прочитаны) |
| Q6 варианты списком `- **A)**`, рекомендация | есть | есть |
| Q7 показ невозможен — сказано | есть (строка «NOT SHOWN» в STATUS) | есть (строка в STATUS и абзац) |
| C1 STATUS · C2 стрижка (переносить нечего — сказано) · C3 опыт · C4 стоячая ложь | есть | есть |
| C5 момент — пробой `date` | частично: две выдуманные минуты найдены и исправлены на месте | частично: `date` при старте, одна метка исправлена по находке судьи |
| C6 судья перед «готово» | есть, тем же экземпляром (названо) | есть, чистым экземпляром |
| C7 ворота машинерией | есть: дверь бюджета записала базу храповика (поведение 2.8) | есть: дверь бюджета код 0 |
| C8 коммит (пуша нет — сказано) | есть | есть |

Итог сверки: каждый обязательный шаг, который исполнил агент по руководству 2.7, исполнил и агент по руководству 2.8; потерь по
чек-листу — ноль. Две разницы — C5 и C6 — не потери сжатия: ни одно из двух руководств не требует чистого экземпляра для общего
прохода судьи, и ни в одной поставке нет правила «момент — пробой `date`» (прогон 6); это разброс исполнителей. Попутно наблюдены два
поведения 2.8 на пути агента: факты стенда ушли в файл домашних правил (агент 2.8 создал `HOUSE_RULES.md` из скелета, агент 2.7 дописал
строку в таблицу стенда руководства — перенос CK5.0а работает) и дверь бюджета записала базу храповика (CK5.2).
РЕАЛЬНЫЙ МИР: накоплено — свежие установки, а не накопленное полевое дерево (проверено с реальным состоянием, снятым с реального:
поставка истока и поставка тега v2.7, та же машина; путь полевого обновления — критерий эпика UP); данные и техника — Windows 11,
Git Bash, PowerShell 5.1, Node v24 (проверено на реальном); путь — агент читает развёрнутый канон и навыки своего проекта, как в поле
(проверено на реальном, с оговорками ниже).

## 5. Найдено

- Потерь обязательных шагов при сжатии руководства — ноль (сверка выше).
- Расхождение в поставке, найденное агентом 2.8: заготовка команды коммита в навыках `/end-chat-soft` и `/end-chat-force` —
  «Otherwise: git add -A && git commit … && git push» — спорит с гигиеной git «уходящий набор объявляется и печатается»
  (`framework/skills/end-chat-soft/SKILL.md:113`, `framework/skills/end-chat-force/SKILL.md:36`). Не этого эпика — строкой в
  реестр следующей версии.
- Замечания агента 2.7 о поставке 2.7 (проверить на 2.8 — в реестр): регулярка рекомендации контура не читает форму
  `**Рекомендация агента:** A`; `bugs/README.md` («even small ones») спорит с лестницей тяжести (S3 — без документа);
  `plans/README.md` требует файла плана для любой нетривиальной работы, а руководство разрешает план разделом в документе бага;
  `node --test test/` не работает на Node 24 (заметка стенда проекта, не канона).
- Ограничения прогона (GAP): шаг адаптации установки пропущен по заданию, поэтому в маркере остался язык по умолчанию `en` и
  плейсхолдеры (`<OWNER_LANGUAGE>`, `<BUILD_COMMAND>`); оба агента выбрали русский для документов владельца — расхождение, которое
  называли оба, — след фикстуры, а не канона; агенты — субагенты одной модели, контекст которых может нести файл контекста истока (им
  велено его не читать); по одному прогону на версию — разброс исполнителей не измерен.

## 6. Следы

Скретчпад сессии, каталог `guide-run/`: `checklist.md` (записан до прогонов), `evidence.mjs`, `evidence-v28.txt`, `evidence-v27.txt`,
`v28/todo-app/` и `v27/todo-app/` (деревья с коммитами агентов и `_walk.md`), `v28/install.log`, `v27/install.log`.

## 7. Вердикт

**pass** — критерий 1 в части «без потерь»: свежий агент по сжатому руководству 2.8 исполнил три задачи-фикстуры с тем же набором
обязательных шагов, что контрольный агент по руководству 2.7; ограничения — выше.
