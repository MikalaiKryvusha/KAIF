# Отчёт прогона — эпик CK 2.8, пункт CK6 «утечка»: преполёт 1c зовёт стража приватных имён на каждом коммите

**Создан:** 2026-09-25 11:15 +03:00 · **Прогон вёл:** Claude Opus 5.5 (1M context), сессия 74 · **Версия/сборка:** HEAD `6a65a4d` + рабочее дерево пункта

## 1. Работа

Пункт CK6 «утечка» `plans/118` (EXP-0159): бриф судьи CK6 ушёл в origin коммитом `290dbbb` с приватными именами двух проектов —
преполёт 1c `tools/commit.mjs` звал `private-names-guard` только при путях поставки (регулярка `DELIVERY_RE`), а зоны стража шире
(`reports/KAIF_AUDIT`, ноты релизов); молча прошёл и следующий коммит `a80df20` (один `HOUSE_RULES.md`). Базис — строка пункта: «звать
страж на каждом коммите (0,6 с), случай `--selftest` … красный на прежнем условии; строка в `HOUSE_RULES.md` §1».

Сделано: условие 1c вынесено в чистую функцию `privateNamesGateNeeded` — страж зовётся на каждом непустом коммите, вторая копия зон
из инструмента убрана; `--selftest` — три новых случая (форма `290dbbb`, форма `a80df20`, пустой набор); новая проба
`tools/sandbox/probes/commit-1c-leak-run.mjs` — функциональный прогон на пути пользователя; `HOUSE_RULES.md` §1 (две строки и проба),
механизация EXP-0159, строка «Действует» `STATUS.md`.

## 2. Контур

Исток KAIF, Windows 11, Git Bash, Node v24. Функциональный прогон — ВРЕМЕННЫЙ git-репозиторий в OS-temp: копии проверяемого
`tools/commit.mjs`, `revert-guard`, `private-names-guard` с `tools/lib/temp-root.mjs`, `version.json` и списка приватных имён истока
(в ignore и там, и там); одно имя из списка (не печатается) вписано в `reports/KAIF_AUDIT/brief.md`; коммит `--only … --no-push`.
Исток не тронут; в origin ничего не уходит.

## 3. Прогоны

| # | Момент | Команда | Исход |
|---|---|---|---|
| 1 | 2026-09-25 ≈ 11:12 +03:00 | `node tools/commit.mjs --selftest` | код 0, «преполёт 1c зовёт стража приватных имён на любом непустом коммите — и на форме утечки 290dbbb (EXP-0159)» |
| 2 | 2026-09-25 ≈ 11:12 +03:00 | мутант на КОПИИ инструмента во временном каталоге: прежнее условие `DELIVERY_RE` вместо нового → `--selftest` копии | код 1, «2 провалов»: «1c: коммит одного брифа в reports/KAIF_AUDIT зовёт стража приватных имён (форма утечки 290dbbb)» и «1c: коммит одного HOUSE_RULES.md зовёт стража (форма a80df20)» |
| 3 | 2026-09-25 11:13:10 → 11:13:11 +03:00 | одноразовый скрипт прогона (скретчпад) — текущий инструмент и инструмент `5dab517` | текущий: код 1, «HEAD did NOT move»; `5dab517`: код 0, «HEAD MOVED — the document was committed» |
| 4 | 2026-09-25 11:14:18 → 11:14:19 +03:00 | `node tools/sandbox/probes/commit-1c-leak-run.mjs` → то же с `<скретчпад>/commit-5dab517.mjs` (`git show 5dab517:tools/commit.mjs`) | текущий: проба код 0 — инструмент код 1, «❌ private-names-guard: 1 вхождений приватных имён…», «✋ коммит остановлен преполётом 1c», HEAD на месте; `5dab517`: проба код 1 — инструмент код 0, «committed locally, NOT pushed», HEAD сдвинулся |
## 4. Проверки

Гигиена: `--selftest` инструмента — три новых случая зелёные, на копии с прежним условием два из них красные.
Функциональный прогон: путь пользователя — коммит инструментом: документ с приватным именем в зоне стража, но вне путей поставки;
прочитано: текущий инструмент печатает красный вердикт стража и строку остановки 1c, HEAD временного репозитория не сдвигается;
инструмент `5dab517` тот же документ коммитит — ровно форма `290dbbb`. Коммит самого пункта на истоке (набор без единого пути
поставки) — наблюдение строки стража в его выводе записывает отчёт судьи CK6: вывод коммита не может лечь в тот же коммит.
РЕАЛЬНЫЙ МИР: накоплено — список приватных имён истока как есть, десять зон (проверено с реальным состоянием, снятым с реального:
копия списка, исток не тронут); данные и техника — та же машина, Git 2.43, Node v24 (проверено на реальном); путь — `tools/commit.mjs`
во временном репозитории (проверено с реальным состоянием); коммит на истоке — в отчёте судьи CK6.

## 5. Найдено

- Дефект, ради которого пункт: пара «зоны стража ↔ триггер гейта» разошлась — подтверждён прогонами 3–4 на инструменте `5dab517`.
  Починено: копия зон из инструмента убрана, страж зовётся на каждом непустом коммите.
- Заметка S3 без починки: отказ 1c наступает ПОСЛЕ бампа `version.json` и стейджинга (порядок инструмента: бамп → стейджинг → 1c), —
  после отказа в дереве остаются поднятый номер сборки и индекс; повторный коммит поднимает номер ещё раз. Так было и до пункта;
  данные не теряются.
- Дефектов продукта после починки — ноль.

## 6. Следы

Скретчпад сессии, каталог `ck59b/`: `commit-mutant.mjs`, `leak-run.mjs`, `leak-run-new.log`, `leak-run-old.log`,
`leak-probe-new.log`, `leak-probe-old.log`, `commit-5dab517.mjs`.

## 7. Вердикт

**pass** — утечка формы `290dbbb` останавливается на коммите: селфтест с мутантом прежнего условия и функциональный прогон на пути
пользователя (временный репозиторий, текущий инструмент против `5dab517`) прочитаны; наблюдение на коммите истока — отчёт судьи CK6.
