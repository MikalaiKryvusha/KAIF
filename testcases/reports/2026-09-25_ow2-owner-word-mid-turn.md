# Отчёт прогона — эпик OW 2.8, шаг OW2: слово владельца посреди хода — пять веток, парковка на «переключись», хук «стоп» и «выполни resume»

**Создан:** 2026-09-25 17:53 +03:00 · **Прогон вёл:** Claude Opus 5.5 (1M context), сессия 74 · **Версия/сборка:** HEAD `242b3c6` + рабочее дерево шага OW2

## 1. Работа

Критерии 5 и 6 `plans/117` (`bugs/123` S1 · #88 · находки D-F4 суда 2.7 и Q-R7 разведки 2.8): «стоп» посреди хода останавливает
агента в том же ходу; «переключись» сначала паркует задачу в носителе, который переживает сжатие, и `/kaif-go` находит её первой; хук
приказа `resume` срабатывает на «выполни resume» и молчит на «Резюме: …». Базис — разведдок `researches/34` (доставка по системам,
перепроверен 40 из 40), строки `FORK:` шага OW1 (`plans/119`: место — рядом с «Первое слово сообщения владельца — команда»; механическая
половина — правило-опора + ветка «стоп» хука усилителем), второе наблюдение класса в этой сессии (вопрос ждал 6 вызовов и 199 с).

Сделано: раздел канона обоих слоёв «Слово владельца посреди хода — автора подписывает система» (признак автора; пять веток ДО
следующего вызова инструмента; несимметричная цена; хук — усилитель); пункт 2 правила первого слова — повелительный глагол и заголовок;
место в шаблоне — вынос досье окружения в скелет домашних правил (`242b3c6`); стоп-условия `/dayloop` · `/nightloop` · `/autoloop`
обоих слоёв; `/kaif-go` обоих слоёв читает строку `PARKED:` первой; две охоты судьи обоих слоёв («owner's word mid-turn ignored»,
«parked and dropped»); хук `prompt-resume-word.mjs` — глагол перед словом, исключение заголовка, ветка «стоп»; близнецы — справочник
(строка хука, запись §17), README модуля хуков, заметка 2.8.

## 2. Контур

Исток KAIF, Windows 11, Git Bash, Node v24.15.0; свод `s14` — развёрнутая копия модуля хуков из свежего `dist`; красный — `dist` v2.7
через шов `KAIF_DIST`; функциональный прогон хука — команда проводки истока из `.claude/settings.json` (`node
${CLAUDE_PROJECT_DIR}/framework/hooks/prompt-resume-word.mjs`) на событиях формы Claude Code с настоящими сообщениями владельца сессии 74.

## 3. Прогоны

| # | Момент | Команда | Исход |
|---|---|---|---|
| 1 | 2026-09-25 17:32:43 → 17:36:00 | вынос досье; `node tools/build-framework.mjs`; `npm run test:core` в одиночку | руководство 1078 → 1060 строк; инвентарь канона `lost 0` (семь переездов объявлены); «all 27 suites green» — коммит `242b3c6` |
| 2 | 17:40:10 → 17:40:14 | правило обоих слоёв, навыки, хук; сборка; `node tools/sandbox/s14-refresh-hooks.mjs` | сборка OK (руководство 1079 из 1080); «песочница refresh-hooks зелёная», 95 проверок, семь новых на месте |
| 3 | 17:40:31 → 17:40:35 | `KAIF_DIST=<v2.7 dist> node tools/sandbox/s14-refresh-hooks.mjs` | «ПРОВАЛОВ: 5» — ровно пять новых поведений (глагол перед словом · заголовок · три формы «стоп»); «resume:» и «стопка» — прежнее поведение, зелёные по построению |
| 4 | 17:41:16 → 17:42:01 | `node tools/sandbox/probes/hooks-mutants.mjs` (M8–M10 добавлены) | «10 mutants red exactly on their named addressees, and only on them» |
| 5 | 17:42:39 | хук командой проводки истока на событиях формы Claude Code (скрипт в скретчпаде сессии) | четыре сообщения владельца посреди хода этой сессии — тишина (их судит правило, не хук); «resume» и «выполни resume» — приказ `/resume`; «Резюме: …» — тишина; «СТОП ЧАТА НЕМЕДЛЕННО! …» (форма `bugs/123`) и «стоп» — приказ остановиться |
| 6 | после 17:43 (момент прогона не снят пробой) | близнецы; сборка; инвентарь; `s14` | сборка OK; инвентарь `lost 1` — переписанный пункт 2 правила первого слова, объявлен строкой реестра → `lost 0`; `s14` зелёный |
| 7 | 2026-09-25 17:49:21 → 17:52:38 | `npm run test:core` в одиночку (после близнецов и `@guard leading-stop` в хуке) | «all 27 suites green» |
| 8 | 2026-09-25 17:53:10 | `node tools/sandbox/probes/ow1-midturn-scan.mjs <транскрипт сессии 74> --quote` | НАБЛЮДЕНИЕ на пути владельца: его вопрос «как продвигается?», пришедший посреди хода в 17:52:05, — «tool calls before the first reply: 0 · reply after 49 s» (тот же вопрос в 15:58 до правила — 6 вызовов и 199 с) |

## 4. Проверки

Гигиена: `s14` — 95 проверок, на v2.7 — 5 красных ровно на новых поведениях; мутанты хуков 10 из 10; инвентарь канона против v2.7 —
`lost 0` (восемь объявленных изменений); гард потолка шаблона — 1079 из 1080; строка критерия 6 — `grep -n "switch" framework/AGENT_GUIDE.md`
→ ветка «switch to Y → first a `PARKED:` line»; стоп-условия трёх циклов и две охоты судьи — в обоих слоях.
Функциональный прогон: хук — той командой, которой его зовёт настоящая проводка истока, на событиях формы Claude Code с НАСТОЯЩИМИ
сообщениями владельца этой сессии и формами поля (открытие «выполни resume» двух полевых сессий, «СТОП …» сессии 67): ПРОЧИТАНО —
вердикт на каждое сообщение (строки прогона 5). Поведение агента по правилу (ответ ДО следующего вызова инструмента, остановка на
«стоп», парковка до «переключись») наблюдается на следующем сообщении владельца посреди хода — мера `tools/sandbox/probes/ow1-midturn-scan.mjs`
по транскрипту (вызовов до первого ответа: 0) — ветка «вопрос» НАБЛЮДЕНА на пути владельца (прогон 8: вопрос посреди хода — 0 вызовов до ответа); ветки «стоп» и «переключись» — НЕ наблюдены: таких сообщений владельца после правила ещё не было.
РЕАЛЬНЫЙ МИР: накоплено — сессия истока с хуками, проведёнными в `.claude/settings.json`, и её настоящие сообщения владельца — проверено
с реальным состоянием, снятым с реального; данные и техника — эта машина, Node v24, Claude Code (VS Code) — проверено на реальном; путь —
событие `UserPromptSubmit` подано командой проводки, а не самим харнессом; сообщение владельца ПОСРЕДИ хода через живой харнесс — ждёт
его следующего сообщения (поведение агента по правилу — там же).

## 5. Найдено

1. **Вопросу не хватало ветки** — четыре ветки плана (стоп · переключись · заметка · долг) не покрывали вопрос владельца посреди хода;
   второе наблюдение класса (эта сессия: 6 вызовов и 199 с, «ответь мне») — пятая ветка «вопрос → ответ до следующего вызова инструмента».
2. **`/pause` паркует только в чат** — носитель парковки «переключись» не переживал бы сжатия; парковка — строка `PARKED:` первой в
   `STATUS.md` → «Где продолжить», `/kaif-go` читает её шагом 0.
3. **Потолок шаблона** — 1078 из 1080 под правило из ≈ 18 строк: вынос досье окружения (пара «руководство ↔ скелет», скелет отсылал назад).
4. **Граница хука «любое первое слово семейства»** опровергнута полем (Q-R7: «выполни resume» — тишина) и судом (D-F4: «Резюме:» — приказ):
   сужена и объявлена строкой реестра.
5. Ноль дефектов вне этих: своды, мутанты и сборка зелёные с первого прогона после правок.

## 6. Следы

Своды и пробы — `tools/sandbox/s14-refresh-hooks.mjs`, `tools/sandbox/probes/hooks-mutants.mjs`; скрипт прогона 5 и логи — скретчпад
сессии 74 (`ow1/live-hook-run.mjs`, `ow1/*.log`), переисполнимы командами выше.

## 7. Вердикт

**partial** — канон, навыки, охоты и хук отгружены и проверены (своды, мутанты, красный на v2.7, хук командой настоящей проводки на
настоящих сообщениях владельца); ветка «вопрос» наблюдена на живом пути (0 вызовов до ответа); «стоп» и «переключись» посреди хода
через живой харнесс не наблюдены — ждут сообщений владельца, мера — `ow1-midturn-scan.mjs` по транскрипту.
