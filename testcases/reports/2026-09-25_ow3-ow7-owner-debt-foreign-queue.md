# Отчёт прогона — эпик OW 2.8, шаги OW3 и OW7: долг перед владельцем раньше плана; очередь владельца на чужой форме файла

**Создан:** 2026-09-25 18:26 +03:00 · **Прогон вёл:** Claude Opus 5.5 (1M context), сессия 74 · **Версия/сборка:** HEAD `ae54c60` + рабочее дерево шагов OW3 и OW7

## 1. Работа

Критерий 7 `plans/117` (OW3, тикет #86 S1 — отвеченное решение владельца 11 дней ждало внесения за плановой работой; вид «отвечено, не
внесено» прятал старое за датой): первая строка ответа `/what-next` закрывает долг перед владельцем; вид «отвечено, не внесено» показывает
ответ без отсечки по возрасту; очередь без браузера поимённо печатает и протухшие документы. Критерий 23 (OW7, тикет #100): команда очереди
владельца не падает на файле очереди чужой формы — одна строка «очередь у проекта своя», без трассы; ни разу не показанный документ
по-прежнему останавливает вход. Базис — тексты тикетов #86 и #100, строка `FORK:` OW3 (`plans/119`: документ сам объявляет состояние —
все ответы заполнены, статус не закрыт; канон `/interview` — статус закрывается ПОСЛЕДНИМ, после разноса), близнец OW7 — запись в чужую
очередь обязана отказать.

Сделано: контур поставки — раздел «решения владельца ждут внесения» ПЕРВЫМ в `--queue --list` (по имени, с возрастом от ответа, без отсечки)
и поимённые строки протухших документов; чтение очереди с проверкой формы, запись в чужую форму — отказ (`ForeignQueueError`), `--enqueue` —
код 1 с одной строкой; `kaif-ranking-lint` — строки «Долг перед владельцем:» (правило `no-owner-debt`) и ось `owner-debt-not-first`; форма
`/what-next` обоих слоёв; раздел «Долг перед владельцем — первым» в `BUG_FIXING_FRAMEWORK.md` обоих слоёв; `/resume` шаг 2, пункт 0.

## 2. Контур

Исток KAIF, Windows 11, Git Bash, Node v24.15.0; селфтест контура — исходник `framework/tools/contour/`; своды `s22` и `s23` — развёрнутые
копии из свежего `dist`; красный — `dist` v2.7 через `KAIF_DIST`; мутанты — копии контура во временном каталоге; функциональный прогон —
git-клон полевого развёртывания тикета #86 (исходник только читался: HEAD и `git status` до и после совпали), команда очереди контура 2.8
истока запущена в клоне.

## 3. Прогоны

| # | Момент | Команда | Исход |
|---|---|---|---|
| 1 | 2026-09-25 ≈ 18:05 (момент не снят пробой; до пробы 18:06:46) | `node framework/tools/contour/review.mjs --selftest` (OW3) | «contour selftest green: 77 checks» — два новых случая OW3 |
| 2 | после прогона 1 | мутанты OW3 на копиях контура (скрипт скретчпада, затем `tools/sandbox/probes/ow3-contour-mutants.mjs`) | «matcher finds nothing» и «stale silent again» — каждый красный ровно на своём случае |
| 3 | 18:06:46 → 18:07:32 | сборка; `node tools/sandbox/s22-contour-shipped.mjs` | «all checks green»; случай ответа 11-дневной давности — первой строкой «РЕШЕНИЯ ВЛАДЕЛЬЦА ЖДУТ ВНЕСЕНИЯ» |
| 4 | 18:07:42 → 18:08:17 | `KAIF_DIST=<v2.7 dist> node tools/sandbox/s22-contour-shipped.mjs` | красный ровно новый случай |
| 5 | до 18:11 | `node framework/tools/kaif-ranking-lint.mjs selftest`; `s23`; `s23` на v2.7 | «23 cases, 9 rules × 2 languages»; первый прогон — честный красный: мутация `recency-first` роняла и новую ось (ось стала судить только строку 1, которая что-то несёт); `s23` зелёный; на v2.7 — три красных: изменённый счёт правил, находка `no-owner-debt` черновика #53, новая пара случаев долга |
| 6 | 18:11:55 | команда очереди контура 2.8 на клоне поля #86 | ПАДЕНИЕ: `TypeError: readQueue(...).filter is not a function` — ровно тикет #100 (OW7): OW3 на этом развёртывании не проверить без OW7 |
| 7 | между 18:14 и 18:15:44 (после подъёма страницы №038, до пробы `date`; момент не снят) | OW7; селфтест контура | «79 checks» — случаи OW7 (строка о своей очереди без трассы; запись отказана, файл проекта байт в байт) |
| 8 | 18:18:55 → 18:20:20 | сборка; `s22`; `s22` на v2.7 | зелёный с двумя случаями OW7 (`{"items":[]}` → код не 1, строка; `--enqueue` → отказ кодом 1, файл байт в байт); на v2.7 — красные ровно три новых случая (OW3 и два OW7) |
| 9 | 18:20:39 → 18:20:56 | `node tools/sandbox/probes/ow3-contour-mutants.mjs` | первый прогон — честный BAD: «чтение без проверки формы» роняет оба случая OW7 (путь записи тоже читает объект и падает `TypeError`) — адресат дописан; второй — четыре мутанта ровно на адресатах |
| 10 | 18:21:10 | команда очереди контура 2.8 на клоне поля #86 (после OW7) | код 0; очередь владельца — один документ; три протухших документа поимённо («протух в очереди (76 дн. > 14)…»); «ℹ очередь у проекта своя: interviews/decisions/queue.json …»; раздела «ждут внесения» НЕТ — см. «Найдено» п. 1 |
| 11 | 2026-09-25 18:22:13 → 18:25:33 | сборка; `npm run test:core` в одиночку (страница владельца закрыта) | «all 27 suites green» |

## 4. Проверки

Гигиена: селфтест контура 79 (+4), мутанты контура 4 из 4 на адресатах; `kaif-ranking-lint` 23 случая, 9 правил × 2 языка; `s22` и `s23` —
зелёные, на v2.7 красны ровно новые случаи; инвентарь канона против v2.7 — `lost 0`.
Функциональный прогон: команда очереди контура 2.8 на клоне полевого развёртывания тикета #86 — OW7: код 0, строка «очередь у проекта своя»,
без трассы (было: `TypeError`), протухшие документы названы поимённо — ПРОЧИТАНО (прогоны 6 и 10). OW3: раздел «ждут внесения» на этом поле
пуст — поле помечает «ответили, ещё не внесли» статусом с галочкой «✅ ОТВЕЧЕНО автором …» и дописывает «ВНЕСЕНО» после внесения, а вид 2.8
судит по канону KAIF (статус закрывается последним) — вид на форме этого поля долг не видит; на канонической форме (`s22`) видит.
РЕАЛЬНЫЙ МИР: накоплено — очередь чужой формы и 101 интервью полевого проекта как есть — проверено с реальным состоянием, снятым с реального
(клон); данные и техника — эта машина, Node v24 — проверено на реальном; путь — команда очереди при входе в чат, как её зовёт `/resume` шаг 1б —
проверено на клоне. OW3 на форме статусов поля — НЕ проверено положительно: зазор назван, следующий шаг — в плане.

## 5. Найдено

1. **Форма статусов поля ≠ канон** — «✅ ОТВЕЧЕНО» ставится до внесения, «ВНЕСЕНО» дописывается после; вид OW3 по канону считает такой
   документ закрытым. Следующий шаг OW3 (`plans/119`): строка `FORK:` о словаре статусов (канон · слова поля «ответили без внесения» · факт
   «внесено» по вопросу), источник — формы статусов этого поля, снятые с клона.
2. **OW3 упирался в OW7** на поле: без проверки формы очереди команда входа падала (прогон 6).
3. **Близнец OW7 подтверждён мутантом**: без проверки формы путь записи тоже падал бы, а не отказывал (прогон 9).
4. **Правила линта ортогональны**: «долг не первым» судит строку 1, только когда она что-то несёт — пустую строку 1 называет `recency-first`.

## 6. Следы

Своды и пробы — `tools/sandbox/s22-contour-shipped.mjs`, `tools/sandbox/s23-ranking-lint.mjs`, `tools/sandbox/probes/ow3-contour-mutants.mjs`;
логи и клон — скретчпад сессии 74 (`ow1/*.log`, `ow1/clone-u`), переисполнимы командами выше.

## 7. Вердикт

**partial** — OW7 **pass** (свод, мутанты, красный на v2.7, функциональный прогон на реальном клоне поля); OW3 — отгружен и проверен на
канонической форме, на форме статусов полевого развёртывания #86 долг не виден — зазор с планом следующего шага.
