# Отчёт прогона — эпик OW 2.8, шаг OW9: сравнение, порядок во времени и развилку исходов агент объясняет картинкой

**Создан:** 2026-09-25 20:56 +03:00 · **Прогон вёл:** Claude Opus 5.5 (1M context), сессия 74 · **Версия/сборка:** HEAD `3da457f` + рабочее дерево шага OW9

## 1. Работа

Критерий 22 `plans/117` (тикет истока #104; интервью №036, Q1 = A): агенту нужно объяснить владельцу поведение продукта — владелец
видит открытую агентом страницу: два кадра рядом с подписями, ленту времени с отметкой действия пользователя и дерево исходов с цветом
вердикта; в чате одна строка; сценарий — подписью. Базис — строка «Проверка» критерия 22 (`grep -n "COMPARISON"` руководства и навыка
вопросов поставки — по строке; скелет страницы объяснения приезжает установкой и не делает внешних запросов — свод полигона) и текст тикета
#104 (предложенные правило и скелет; пункт 3 тикета — картинки на странице вопросов контура — в критерий не входит и не делался).

Сделано: скелет `framework/templates/_explain-page-template.html` → `.kaif/_explain-page-template.html` (бандл, как другие скелеты;
самодостаточен — ни одного запроса с машины; блоки «сравнение · лента времени · дерево исходов» и сценарий подписью; тёмная и светлая
темы); правило в руководстве обоих слоёв рядом с «Показ — это действие» (в поставке — в потолке 1080: абзац «Showing is an action»
перенесён слово в слово) и в шаге 3а `/interview` обоих слоёв; валидатор сборки считает скелеты `.md|.html`; у помощника безголового
браузера (`tools/lib/cdp-mini.mjs`) — снятие кадра. Макет на нашем материале — `reports/explain_mockups/ow9_answers_one_by_one.html`
(настоящие кадры страницы вопросов поставки: «до записи / после записи Q1», старая вкладка) — открыт владельцу, домашка
`homeworks/07_explain_page_look.md`.

## 2. Контур

Исток KAIF, Windows 11, Git Bash, Node v24; кадры — генератор контура поставки на временном проекте с русским пакетом, безголовый Chromium
окном `--app` на временном профиле (ни окна, ни зова у владельца); проба пересечения — контур из последнего коммита (`git show HEAD:`) и
рабочего дерева на ширинах 800 · 980 · 1100; свод `s22` на свежей поставке и против `dist` v2.7; показ макета — системный браузер
владельца.

## 3. Прогоны

| # | Момент | Команда | Исход |
|---|---|---|---|
| 1 | 2026-09-25 20:39 | скрипт скретчпада `ow9/frames.mjs` (кадры страницы вопросов) | три кадра; ГЛАЗАМИ на кадре 3 (старая вкладка): полоса статуса у кнопки «Записать решение» легла поверх красной полосы и закрыла «Открыть новую редакцию», повторив её текст — дефект шага OW6, ни одна проверка его не видела |
| 2 | ≈ 20:40 | починка (кнопка первой в полосе; отступ полосы справа под плавающую кнопку, как у шапки; отказ старой вкладки снимает полосу статуса); кадры заново | на кадре 3 кнопка слева и свободна, текст читается целиком |
| 3 | 20:41:24 | `s22` с проверкой «центр кнопки — сама кнопка» на `dist` ДО починки | зелёный — проверка узкая, дефект не пойман |
| 4 | ≈ 20:42–20:43 | проба `ow9/btnfree-probe.mjs` на контуре до и после починки, ширины 800 · 980 · 1100 | до: при 980 кнопка закрыта и пересекает плавающий блок, при 1100 (окно владельца по умолчанию) — пересекает; после: пересечения нет ни на одной ширине |
| 5 | 20:44:49 | `s22` с проверкой «кнопка не пересекает плавающий блок», окно 1100×900, на `dist` ДО починки | КРАСНЫЙ ровно на случае старой вкладки |
| 6 | ≈ 20:45–20:47 | сборка; селфтест контура; `s22`; `node tools/verify-contour.mjs` | сборка 0; 98; 96 зелёных; «209 зелёных, 0 красных» (20:47:24) |
| 7 | 20:50 | макет на скелете (`reports/explain_mockups/`) — снят полным кадром и прочитан глазами; затем `cmd //c start "" <макет>` | блоки и кадры на месте; окно открыто владельцу 20:50:22 |
| 8 | ≈ 20:51–20:52 | сборка | отказ: счёт бандла 184 при ожидаемых 183 (валидатор считал скелеты только `.md`) → отбор `.md|.html`; затем отказ реестра пар: перенос абзаца разорвал стерегомую фразу «is CLOSED only by the command that checks» → склеено; затем счётчики прозы 183 → 184 |
| 9 | ≈ 20:52 | сборка; счётчики; `node tools/canon-inventory.mjs --diff v2.7 …`; страж вопросов; линт авторства | сборка 0; счётчики OK; «lost 0»; 0; new 0 |
| 10 | 20:53:41 | `node tools/sandbox/s22-contour-shipped.mjs` | «all checks green», 97 |
| 11 | 20:55:37 | то же против v2.7 | 16 красных поимённо (новый — скелета нет), трасс 0 |
| 12 | 20:58:29 → 21:02:35 | `npm run test:core` (в одиночку) | «sandbox suite: all 27 suites green» |

## 4. Проверки

Гигиена: `grep -n "COMPARISON" framework/AGENT_GUIDE.md framework/skills/interview/SKILL.md` — по строке в каждом; `s22` A — скелет
приехал, три блока и подпись, ни `src/href` на сеть, ни `url(//…)`, ни `<link>`, ни `<script>`; на v2.7 — красный; сборка, счётчики,
инвентарь канона (lost 0), стражи; руководство поставки 1080 строк (потолок 1080).
Функциональный прогон: настоящие кадры страницы вопросов поставки сняты и ПРОЧИТАНЫ глазами — так найден дефект кнопки (прогон 1),
починка прочитана на новом кадре; макет на скелете снят полным кадром и прочитан (блоки, подписи, цвета вердикта); показ владельцу —
окно системного браузера, 20:50:22.
РЕАЛЬНЫЙ МИР: накоплено — у поля скелета нет, установка 2.8 привозит его в `.kaif/` (проверено на развёрнутой копии, `s22` A); данные и
техника — эта машина, Windows, системный браузер владельца — показ сделан; путь — агент копирует скелет, наполняет, открывает — проверено
на макете истока; **вид страницы — класс «вкус»: вердикт владельца не получен (домашка 07) — поэтому критерий 22 🔧, а не ✅.**

## 5. Найдено

1. **Кнопка «Открыть новую редакцию» закрыта полосой статуса** (шаг OW6, найдено глазом на кадре; S2 — владелец не нашёл бы кнопку при
   окне 980–1100): починено; страж — геометрическая проверка `s22` D (7) в окне владельца 1100×900, красная на версии до починки.
   Проверка «центр кнопки — сама кнопка» дефект НЕ ловила (зависит от ширины) — заменена.
2. **Перенос строк ради потолка руководства разорвал стерегомую фразу** — реестр пар сборки поймал; стерегомые фразы при переносе
   склеиваются.
3. **Валидатор считал скелеты только `.md`** — отбор расширен.
4. Пункт 3 тикета #104 (картинки на странице вопросов контура) — не сделан, в критерий не входит.

## 6. Следы

`framework/templates/_explain-page-template.html`, `tools/build-framework.mjs`, `tools/check-framework.mjs`, `tools/lib/cdp-mini.mjs`,
`framework/tools/contour/review.mjs` (полоса старой вкладки), `tools/sandbox/s22-contour-shipped.mjs` (A — скелет; D (7) — пересечение),
`reports/explain_mockups/` (макет и три кадра), `homeworks/07_explain_page_look.md`; скрипты кадров и пробы — скретчпад сессии 74 (`ow9/`).

## 7. Вердикт

**partial** — механика отгружена и проверена (правило, скелет без запросов наружу, показ), дефект кнопки OW6 найден глазом и починен со
стражем; вид страницы — вердиктом владельца (домашка 07).
