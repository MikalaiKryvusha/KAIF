# Отчёт прогона — эпик VO 2.8, шаг VO0: ядро голоса 2.2 получено и измерено до правок

**Создан:** 2026-09-25 между 14:56 и 14:59 +03:00 (минута не снята) · **Прогон вёл:** Claude Opus 5.5 (1M context), сессия 74 · **Версия/сборка:** HEAD `dfbb248`; ядро — `krinik-stylometry` `origin/main` = `2c822d5`

## 1. Работа

Шаг VO0 `plans/120`: до любой правки снять, что такое ядро голоса 2.2 и чем его везти — версия и статус ядра, раскладка разделов,
таблица §8 и метки жанра, эталон меток в хранилище, и как поведёт себя на новом ядре генератор публичного слепка истока. Базис — тикеты
#102 и #103 (с комментарием владельца от 2026-09-25: ядро 2.2 принято, коммит `2c822d5`), решение №60 (слепок: правила едут, цитаты —
нет), слово владельца №129.

## 2. Контур

Исток KAIF, Windows 11, Git Bash, Node v24.15.0. Приватное хранилище ядра — клон `d:\work\krinik_voice`: только `git fetch` и
`git show origin/main:<файл>` — рабочее дерево клона не переключалось и не правилось (оно по-прежнему на 1.2, `5265de5`). Копия файла ядра —
в скретчпаде сессии. Генератор слепка — только с `--check` (не пишет слепок); слепок истока `AUTHOR_STYLOMETRY.md` — sha256 `05ee92a4c7c35d26…`
до и после прогона.

## 3. Прогоны

| # | Момент | Команда | Исход |
|---|---|---|---|
| 1 | 2026-09-25 между 14:51:51 и 14:56:20 +03:00 | `git -C d:/work/krinik_voice fetch origin` | `5265de5..2c822d5  main -> origin/main` |
| 2 | то же окно | `git show origin/main:version.json` (чтение полей) | `version 2.2 status accepted acceptedAt 2026-09-25T14:35:00+03:00`; потребители — KAIF · NDim Space · Unliminium · QA_Engineer; поле `propagation` — дословно в `plans/120` VO0 |
| 3 | то же окно | `git show origin/main:AUTHOR_STYLOMETRY.md > <скретчпад>/core22/AUTHOR_STYLOMETRY.md`; `grep -n "^## \|^### "`; `grep -c "^\| \`/"` | 615 строк, 82 754 байта; разделы 0 · 0-M · 1 · 2 (2.1–2.6) · 2-C · 3–9 · приложения А, Б; строк таблицы §8 — 21 |
| 4 | то же окно | `git show origin/main:tools/voice-check.mjs` (шапка и `applies`) | `GENRES = ['document', 'ticket', 'comment', 'message', 'reply', 'essay']`; «[работа] — все жанры, кроме essay; [документ] — только document; [проза] — только essay; без метки — везде» |
| 5 | 2026-09-25 14:56:20 +03:00 | `node tools/stylometry-snapshot.mjs --source <скретчпад>/core22/AUTHOR_STYLOMETRY.md --check --report <скретчпад>/core22/elided-report.txt` | код 1; «слепок: правил 1 · … · схлопнуто спанов 72 (уникальных 66)»; «РАСХОЖДЕНИЕ: слепок в репозитории отличается от пересборки из источника»; sha слепка истока до и после — `05ee92a4c7c35d26` |
| 6 | 2026-09-25 после 14:56:20 +03:00 | одноразовый скрипт скретчпада: на каждый из 66 спанов `git grep -n -F -e <спан> -- . ':!AUTHOR_STYLOMETRY.md'` | «spans: 66 · public in KAIF: 11 · not found (private or too short): 55» |
| 7 | 2026-09-25 между 14:56:20 и 14:59:44 +03:00 (момент не снят) | одноразовый скрипт скретчпада `labels.mjs <копия ядра>` — строки §8 по метке, открывающей подсказку | `rows 21 {"работа":15,"документ":1,"(без метки)":5}` |

## 4. Проверки

Гигиена: NONE — шаг ничего не строит; инструменты только читают.
Функциональный прогон: генератор публичного слепка истока — на новом ядре, путём, которым его зовёт исток (`--check`); ПРОЧИТАНО: раскладку
2.x генератор не знает («правил 1» — у ядра 2.2 правила живут в других разделах, чем у 1.x) и схлопывает 72 спана; разбор 66 уникальных по
публичности в KAIF — 11 публичны (например «ты пишешь английский, но русскими словами» — `EXPERIENCE.md:1304`, «Пока - нет.» — журнал решений
`MASTER_PLAN.md`), 55 нет; среди 55 — шаблоны оборотов, которые словами владельца не являются («Если …, то …», «Таким образом, …», «Настоящий
документ является руководством пользователя», «если у вас возникнут вопросы»), и непубличные фразы владельца из переписки и текстов его
проектов — здесь не приводятся (решение №60: публичный репозиторий их не несёт; первая редакция отчёта их цитировала — исправлено
2026-09-25 15:09 +03:00, в истории git коммита `19e19ff` они остались). Таблица §8 ядра — 21 строка, метка
жанра — первое слово подсказки: `[работа]` — 15 строк, `[документ]` — 1 (строка «то есть»), без метки — 5 (рамка ассистента, литоты, анонс
содержания и др.) — подсчёт одноразовым скриптом скретчпада, ячейки режутся по `|`, перед которым нет обратной косой (прогон 7).

## 5. Найдено

1. **Генератор слепка — раскладка 1.x** — на ядре 2.2 слепок собирался бы неверно («правил 1»); вход шага VO1.
2. **Ядро 2.2 несёт фразы владельца из переписки** внутри правил, 55 спанов непубличны в KAIF. Хранилище называет файл «public-safe core»
   (комментарий в #103) — это слово агента того проекта; слово владельца — решение №60 «правила едут целиком, цитаты — нет». Развилка VO1
   записана строкой `FORK:` в `plans/120` — вариант B (генератор под 2.x по №60); ядро байт в байт в публичный KAIF — только словом владельца.
3. **Эталон меток жанра уже существует** в хранилище (`tools/voice-check.mjs`, «[TESTED: … --selftest 12/12»); имена жанров `--genre`
   линтера поставки берутся оттуда (`plans/120`, решение 2).
4. **Реестр потребителей ядра** (`consumers` в `version.json`) называет проекты владельца поимённо — опора узнавания в VO3; чужой портрет не
   трогается.

## 6. Следы

`plans/120` (VO0 — цифры и цитаты); копия ядра, отчёт схлопнутых спанов и разбор публичности — скретчпад сессии (умирают с сессией; переисполнимы
командами прогонов 1–6 из этого отчёта); ядро — `krinik-stylometry` `2c822d5` (приватный).

## 7. Вердикт

**pass** — ядро измерено, вход шагов VO1–VO3 записан в `plans/120`; слепок истока и рабочее дерево клона ядра не тронуты.
