# Отчёт прогона — эпик VO 2.8, шаг VO1: генератор слепка знает раскладку ядра 2.x; слепок истока собран из ядра 2.2

**Создан:** 2026-09-25 15:18 +03:00 · **Прогон вёл:** Claude Opus 5.5 (1M context), сессия 74 · **Версия/сборка:** HEAD `37541b3` + рабочее дерево шага VO1; ядро — `krinik-stylometry` `2c822d5` (файл ядра — `c4bbf85`)

## 1. Работа

Шаг VO1 `plans/120`: генератор публичного слепка `tools/stylometry-snapshot.mjs` различает раскладку ядра 2.x и собирает слепок истока
`AUTHOR_STYLOMETRY.md` из ядра голоса 2.2 по решению владельца №60 («правила едут целиком, цитаты — нет»; строка `FORK:` VO1 — вариант B).
Базис — пункты VO1 плана: разделы целиком, §8 побайтно, непубличная фраза владельца схлопнута, шаблоны — белым списком с причиной,
публичное — по файлу, отказ на приватные слои, ось n-грамм против них; плюс механизация утечки, найденной по дороге (коммит `19e19ff`).

Сделано: профиль `v2` конфига (белый список 1.x наследуется; 51 своя запись с причиной; 11 публичных фраз и 3 публичных образца — их
проверяет код по файлу, запись без улики роняет сборку); узнавание раскладки по первой строке; образцы §2.6 блоком (непубличный —
строкой-объявлением); отказ на приватный слой по заголовку и имени; ось 4 — окна из восьми слов против блоков `<образец>` рабочего слоя и
модуля прозы; ось 5 — схлопнутая фраза (от трёх слов) не стоит ни в одном отслеживаемом файле (склеенный текст); шапка слепка 2.x
`tools/stylometry-snapshot-header-v2.md`; селфтест K16–K21; проба мутантов `tools/sandbox/probes/vo1-snapshot-mutants.mjs`; клон ядра
`d:\work\krinik_voice` переведён fast-forward `5265de5 → 2c822d5` (дерево было чистым); слепок пересобран.

## 2. Контур

Исток KAIF, Windows 11, Git Bash, Node v24.15.0. Ядро и приватные слои — сперва копии из `git show origin/main:<файл>` в скретчпаде, после
fast-forward — клон по умолчанию. Мутанты — копия генератора рядом с оригиналом, удаляется после прогона. Полигон шаг не трогает: своды
слепок истока не читают (у них свои фикстуры) — прогон полигона в VO4 вместе с линтером.

## 3. Прогоны

| # | Момент | Команда | Исход |
|---|---|---|---|
| 1 | 2026-09-25 15:07:56 +03:00 | `node tools/stylometry-snapshot.mjs --source <копия ядра 2.2> --check` | код 1: «правил 117 … схлопнуто спанов 30»; приёмка КРАСНАЯ — «фрагмент приватного слоя AUTHOR_STYLOMETRY_WORK.md в слепке: «слова и глифы образца в свой текст не…»» — ложный: правило §2.6 ядра, которое слой пересказывает |
| 2 | 15:08:30 | то же после сужения оси 4 до блоков `<образец>` слоёв | код 1 только «РАСХОЖДЕНИЕ» (слепок в репо — 1.x); приёмка зелёная; 30 схлопнутых — на ревизию |
| 3 | между 15:08:30 и 15:12:13 | одноразовый скрипт скретчпада: публичность 30 спанов (`git grep -F`) | «public in KAIF: 7»; две из семи «публичны» только потому, что стоят в МОЁМ отчёте VO0 — утечка `19e19ff`, исправлена коммитом `37541b3` |
| 4 | 15:12:13 | `--check` после расширения профиля | код 2: «публичность фразы не подтверждена файлом plans/68_…» — длинный вариант «Окно открывается…» стоит там с разметкой; перенесён в белый список с причиной |
| 5 | 15:12:34 | `--check` | приёмка зелёная, схлопнуто 17 |
| 6 | 15:13:45 | `--source <копия> --selftest` | код 0; K1–K21 и чистые копии — 24 строки ✅ |
| 7 | 15:14:16–15:14:17 | `node tools/sandbox/probes/vo1-snapshot-mutants.mjs <копия ядра>` | «GOOD — 6 of 6 mutants red on their addressee» (M1→K16 · M2→K17 · M3→K18 · M4→K19 · M5→K20 · M6→K21), немутированная копия зелёная |
| 8 | 15:14:35 | одноразовый скрипт: правило оси 5 построчным `git grep` против деревьев `19e19ff` и `37541b3` | `19e19ff` — 1 фраза найдена, `37541b3` — 0; вторая фраза утечки была разорвана переносом — ось переведена на склеенный текст |
| 9 | 15:15:11 | то же склеенным текстом | `19e19ff` — 2 фразы (оба файла утечки), `37541b3` — 0; `--check` на текущем дереве — ось 5 красная: фраза §6 «мог написать…» публична (`plans/68`, через перенос) — объявлена публичной |
| 10 | 15:15:33 | `--check`; `--selftest` | приёмка зелёная, схлопнуто 16; селфтест — 24 ✅, 0 ❌ |
| 11 | 15:15:46 → 15:16:04 | `--check` и `--selftest` на ядре 1.2 (клон до перевода) | первый `--check` красный осью 5 (публичное в планах, схлопнутое 1.x осторожно) — ось ограничена раскладкой 2.x; затем «слепок совпадает с пересборкой из источника» (побайтный эталон 1.x цел); селфтест 1.x — код 0 |
| 12 | между 15:16:04 и 15:16:19 | `git -C d:/work/krinik_voice merge --ff-only origin/main` | `2c822d5`, porcelain до — 0 строк |
| 13 | 15:16:19 | `node tools/stylometry-snapshot.mjs`; `--check` | записано; «источник … @ c4bbf85 (2026-09-25T14:38:08+03:00)»; «слепок совпадает с пересборкой из источника» |
| 14 | 15:16:34 | `node tools/private-names-guard.mjs`; `--version-check`; `node framework/tools/kaif-voice-lint.mjs load`; `… check README.md` | страж зелёный (слепок в его зонах); «слепок совпадает с ядром (`c4bbf85`)»; загрузка — «433 of 642 line(s) … ~17k of ~25k tokens»; `check` разбирает «21 stop rule(s)» нового слепка |
| 15 | 15:17:48 → 15:17:51 | `--selftest` (источник по умолчанию — ядро 2.2), `--check`, `--version-check`, проба мутантов | селфтест зелёный; совпадает; `c4bbf85`; 6 из 6 |

## 4. Проверки

Гигиена: селфтест генератора — K1–K15 (прежние) и K16–K21 (раскладка 2.x) зелёные, чистые копии молчат; мутанты — каждая новая защита
краснеет ровно на своём адресате; ось 5 доказана и против настоящей утечки (дерево `19e19ff` — красная, дерево исправления — молчит).
Функциональный прогон: генератор на НАСТОЯЩЕМ ядре владельца 2.2 путём, которым его зовёт исток (источник по умолчанию), и слепок в работе
агента, пишущего текст владельца; ПРОЧИТАНО: слепок открывается шапкой 2.x («Версия ядра | krinik-stylometry 2.2 … `accepted`», «правил —
117 · фраз владельца, уже публичных в этом репозитории, — 11 · схлопнуто спанов (уникальных) — 16 · образцы §2.6: едут — ОБ6 · ОБ2 · ОБ3,
сняты — ОБ4 · ОБ5»); на месте ОБ4 и ОБ5 — строки ««…» — непубличный текст владельца (решение №60)…»; пара П1 §6 — «ДО: «…» и «…»»;
`kaif-voice-lint load` грузит разделы для письма нового слепка, `check` разбирает его таблицу §8 (21 правило) и называет попадания с
метками жанра в подсказке; страж приватных имён и сверка версий — зелёные.

## 5. Найдено

1. **Генератор не знал раскладку 2.x** (VO0) — починено этим шагом.
2. **Утечка `19e19ff`** — мой отчёт VO0 и `plans/120` процитировали непубличные фразы владельца; исправлено `37541b3`, механизировано осью 5;
   история git — долг владельца (STATUS).
3. **Ложный красный оси 4 на всём тексте слоя** (правила, которые слой пересказывает) — ось сужена до блоков `<образец>` слоёв.
4. **Построчный поиск не видит фразу, разорванную переносом** — ось 5 и проверка публичности ищут по склеенному тексту (переносы, отступы,
   маркеры цитаты).
5. **Публичная фраза, размеченная непубличной** («мог написать…», `plans/68` через перенос) — объявлена публичной; нашла ось 5.
6. **Ось 5 на слепке 1.x** дала бы 32 находки публичного, осторожно схлопнутого 1.x, — ограничена раскладкой 2.x; GAP назван в коде.
7. **Вход VO2:** ядро 2.2 велит «держи разделы §0–§3 в рабочем контексте», а выбор разделов для письма линтера (0 · 2 · 5 · 6 · 7) не грузит
   §1 «Порядок работы и термины» и §3 «Проза» — развилка шага VO2.
8. **Шапка ядра 2.2 называет версию «черновик до слова автора»**, а `version.json` хранилища — `accepted`: расхождение хранилища, названо в
   шапке слепка; правится в источнике, не в KAIF.
9. README истока по новому слепку — 17 попаданий правил `[работа]` (документ — жанр, где `[работа]` действует): вход VO2 (`--genre`), не дефект
   слепка.

## 6. Следы

Слепок `AUTHOR_STYLOMETRY.md` (642 строки, 86 793 байта); конфиг `tools/stylometry-snapshot.config.json` → `v2`; шапка
`tools/stylometry-snapshot-header-v2.md`; проба `tools/sandbox/probes/vo1-snapshot-mutants.mjs`; отчёты схлопнутого и логи прогонов —
скретчпад сессии (переисполнимы командами выше).

## 7. Вердикт

**pass** — слепок истока собран из ядра 2.2 по решению №60, приёмка пятью осями зелёная и каждая новая ось доказана красной на своей
форме; путь 1.x побайтно цел.
