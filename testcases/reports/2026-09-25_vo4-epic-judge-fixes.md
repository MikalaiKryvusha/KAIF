# Отчёт прогона — эпик VO 2.8, шаг VO4: лёгкий судья эпика и починки его находок (критерии 25–26)

**Создан:** 2026-09-25 17:23 +03:00 · **Прогон вёл:** Claude Opus 5.5 (1M context), сессия 74 · **Версия/сборка:** HEAD `7bc3266` + рабочее дерево шага VO4

## 1. Работа

Шаг VO4 `plans/120`: лёгкий судья эпика чистым экземпляром по брифу `reports/KAIF_AUDIT/2026-09-25_vo4_epic_judge_brief.md` (`819b92f`,
заявления V1–V12) → починки → прогоны. Базис — критерии 25–26 `plans/117`, слово владельца о замене («не мержем, а заменой», #103),
данные самого ядра голоса (`version.json` → `consumers` и `propagation`: заменой через версию KAIF ядро получают KAIF · ndim · unliminium),
решение №60 (непубличные фразы владельца — вне публичного репозитория).

Вердикт судьи — **REFUTED** (V1 · V2 · V3 · V5 · V6 · V7 · V11 — CONFIRMED; V4 · V9 · V10 · V12 — WEAKENED; V8 — REFUTED частично):
два блокера, пять каверз, мелкое. Критерий 26 переоткрыт до починок (`7bc3266`, шестое обязательство).

## 2. Контур

Исток KAIF, Windows 11, Git Bash, Node v24.15.0; приватное хранилище ядра `d:\work\krinik_voice` @ `c4bbf85` (только чтение); поле —
git-клоны трёх развёртываний владельца (исходники только читались: HEAD и `git status` до и после), у развёртывания C — портрет,
исключённый из git, скопированный в клон режимом `--with-ignored`; преполёт 1d — во временном `git worktree` истока на HEAD.

## 3. Прогоны

| # | Момент | Команда | Исход |
|---|---|---|---|
| 1 | 2026-09-25 после 16:20:00 (проба `date` перед коммитом брифа) → ≈ 16:42 (длительность субагента 21 мин 17 с) | лёгкий судья (субагент, чистый экземпляр) по брифу | REFUTED — два блокера (слияние над слепком проходит контрольную точку; приватный портрет развёртывания C узнаётся потребителем, его `recheck` — exit 1), пять каверз, мелкое |
| 2 | между вердиктом и 16:45 (проба `date` записи переоткрытия; точные минуты не сняты) | чтение `consumers` / `propagation` ядра; счёт меток в четырёх портретах (только числа) | потребители называют метку `krinik-stylometry` (5 · 540 · 4), приватный портрет C — 0, путь приватного хранилища — 11 |
| 3 | 2026-09-25 16:49:39 → 16:49:53 | `node tools/build-framework.mjs`; `node tools/sandbox/s26-voice-lint.mjs` | сборка OK; пин — метки `krinik-stylometry` · `stylometry-snapshot.mjs`, заголовки двух раскладок; «all 74 checks green» |
| 4 | 16:50:03 | `KAIF_DIST=<v2.7 dist> node tools/sandbox/s26-voice-lint.mjs` | «26 of 74 check(s) failed»; раздел (5): 12 из 13 красны, «после замены recheck проходит» зелёный по построению (его красный — M11) |
| 5 | 16:50:56 → 16:54:09 | `node tools/sandbox/probes/voice-mutants.mjs` (M14–M16 добавлены) | «mutant proof FAILED: 3 of 16» — честные BAD: M9 и M12 краснят и новые ассерты (приватная копия; слияние на передаче), якорь M11 сдвинут правкой узнавания |
| 6 | 16:54:33 → 16:58:11 | то же после дописи адресатов и нового якоря M11 | «16 mutants red exactly on their named addressees, and only on them» |
| 7 | 16:59:03 → 16:59:52 | `node tools/sandbox/probes/vo3-field-portraits.mjs <A> --foreign` · `<B>` · `<C> --with-ignored` | A: оба маршрута, слияние судьи на настоящем портрете — отказ, замена (12 строк преамбулы) — приём, чужой — не тронут; B: оба маршрута, слияние оболочки без заголовка слепка проходит — названный GAP; C: bootstrap — не тронут, update — BAD пробы (строку лога печатает свежее ядро при `recheck`, проба искала её в выводе прежнего) |
| 8 | 17:00:09 → 17:00:14 | `vo3-field-portraits.mjs <C> --with-ignored` после починки пробы | оба маршрута: «no item, no refusal from recheck (exit 0), the file byte for byte, the fresh core's log names it»; исходник не тронут |
| 9 | 17:04:46 → 17:04:47 | `node tools/commit.mjs --selftest`; `node tools/stylometry-snapshot.mjs --leak-only` | селфтест зелёный (1d — четыре случая); «ось утечки: ни одна из 16 схлопнутых фраз не стоит в том, что едет коммитом (0 файл(ов))» |
| 10 | 17:06:33 → 17:07:04 | `node tools/sandbox/probes/commit-1d-leak-run.mjs` | A — фраза в новом файле: стоп 1d, HEAD на месте · B — фраза только в сообщении: стоп · C — чистый коммит: прошёл; текста фразы в выводе нет |
| 11 | 17:07:36 → 17:08:56 | `node tools/sandbox/probes/vo1-snapshot-mutants.mjs` (M7, M8; «красный только адресат») | «GOOD — 8 of 8 mutants red on their addressee» |
| 12 | после 17:08:56 (момент прогона не снят пробой) | `node tools/sandbox/s26-voice-lint.mjs` (+ строка без метки при `--genre`) | «all 75 checks green» |
| 13 | до 17:13:38 (конец прочитан ожиданием строки итога) | `voice-mutants.mjs` (M17 добавлен) | «mutant proof FAILED: 1 of 17» — M17 честный BAD: замолчавшая строка без метки меняет и счёт молчащих строк ассерта эссе (2 → 3) — адресат дописан |
| 14 | 17:13:55 → 17:17:33 | `node tools/sandbox/probes/voice-mutants.mjs` (в одиночку) | «17 mutants red exactly on their named addressees, and only on them» |
| 15 | 17:17:43 → 17:17:55 | `KAIF_DIST=<v2.7 dist> node tools/sandbox/s26-voice-lint.mjs` | «27 of 75 check(s) failed»: раздел (4) — 8 из 8, раздел (5) — 12 из 13 (передача «после замены проходит» на ядре без неё зелёная по построению, её красный — M11), 7 прежних — выбор разделов |
| 16 | 17:17:55 | `node tools/build-framework.mjs` | exit 1 — `SyntaxError`: апостроф во вставке в заметку 2.8 (строка в одинарных кавычках); первая починка скриптом через Bash не сработала — инструмент съел уровень экранирования (EXP-0121), вторая — скриптом-файлом |
| 17 | 17:18:46 → 17:19:00 | сборка; `s26` | сборка OK; «all 75 checks green» |
| 18 | 17:19:37; 17:19:45 → 17:22:53 | сборка (маркеры); `npm run test:core` в одиночку | «✅ sandbox suite: all 27 suites green» |

## 4. Проверки

Гигиена: `s26` — 75 проверок (раздел (5) — 13, раздел (4) — 8); на v2.7 — 27 из 75 (раздел (4) — 8 из 8, раздел (5) — 12 из 13, прежние 7); мутанты `voice-mutants.mjs` — 17 из 17 ровно на адресатах (первые прогоны — четыре честных BAD, адресаты названы); генератор —
`--selftest` K1–K21 и K20c зелёный, мутанты генератора 8 из 8, каждый красный только на своём адресате; `commit.mjs --selftest` — 1d.
Функциональный прогон: путь владельца полевого проекта — `update` 2.7 → 2.8 прежним ядром и bootstrap свежим на клонах трёх развёртываний;
ПРОЧИТАНО: у копии с преамбулой (A) — отказ до замены, отказ на слиянии судьи (прежний портрет целиком над слепком), приём замены; у
жанровой оболочки (B) — то же, слияние оболочки без заголовка слепка проходит (GAP: её пере-вывод — работа `/owner-voice`); у приватного
портрета C (исключён из git) — ни пункта, ни отказа, файл байт в байт на обоих маршрутах. Преполёт 1d — настоящим `tools/commit.mjs` во
временном worktree истока: фраза, схлопнутая слепком, в новом файле и в сообщении — стоп; чистый коммит — проходит.
РЕАЛЬНЫЙ МИР: накоплено — портреты трёх развёртываний как есть, включая приватный, исключённый из git (скопирован в клон, не выдуман) —
проверено с реальным состоянием, снятым с реального; данные и техника — та же машина, Node v24, приватное ядро владельца — проверено на
реальном; путь — `update` прежним ядром и bootstrap свежим, как ходит поле; коммит — настоящим инструментом истока — проверено на клонах и
временном worktree.

## 5. Найдено

1. **Блокер (судья): приватный портрет узнавался потребителем** — метка пина называла путь приватного хранилища; обновление развёртывания C
   остановилось бы. Починено: метки — только публичные метки слепка (данные ядра: все три потребителя их называют); лог «not derived from
   it (another owner's portrait or a private copy)». GAP: сырая копия приватного ядра под именем портрета узнаётся (метка ядра в нём есть) —
   такого потребителя в реестре ядра нет.
2. **Блокер (судья): слияние над слепком проходило** контрольную точку, узнавание и передачу. Починено: пин несёт первые строки всех
   раскладок слепка (`heads`, из шаблонов шапки генератора — не из истории git); `mergedAbove` — одна функция для узнавания, контрольной
   точки и передачи. GAP: слияние без первой строки прежнего слепка; оболочка жанра, оставленная как есть.
3. **Каверза (судья): числа** — на v2.7 было 23 из 71 (9 из 10 новых), не «22 / 8 из 10»: после переписки контрольного ассерта прогон на
   v2.7 не повторялся, число записано из прежнего прогона; «M9–M12» при существующем M13. Исправлено на месте.
4. **Каверза (судья): стоячая ложь** — строка FORK VO3 («манифест»), заметка CK нот 2.8 и строка справочника («§0 · §2 · §5 · §6 · §7» при
   «include §1»), строка пробы в `HOUSE_RULES.md`. Исправлено на месте.
5. **Каверза (судья): ось утечки** стояла только в прогоне генератора и печатала 40 символов фразы. Починено: находка — sha спана (K21,
   мутант M7 генератора); преполёт 1d `tools/commit.mjs` — индекс и сообщение коммита на каждом непустом коммите, без ядра — SKIPPED=3.
6. **Каверза (судья): проба** печатала «✅ both routes replaced» без портрета; портрет C назван «untracked» (он исключён из git). Исправлено;
   режим `--with-ignored`; итог называет, что проверено.
7. **Каверза (судья): «отчёт обновления называет прежнюю версию»** машиной не проверялось — строка «Результат» критерия 26 уточнена до
   проверенного (инструкция называет версии), зазор назван.
8. **Мелкое (судья)** — проба мутантов генератора требует «красный только адресат» (M8 добавлен); `samplePublic` сверяет строку образца
   целиком (K20c); итог генератора разводит две метрики; `s26` (4) — строка без метки при `--genre` (M17).
9. **Свои:** BAD пробы на маршруте update развёртывания C (прогон 7) — дефект пробы, не ядра; три честных BAD мутантов (прогон 5); якоря
   правок в `tools/commit.mjs` не совпадали — файл хранит CRLF в рабочей копии.

## 6. Следы

Своды и пробы — `tools/sandbox/s26-voice-lint.mjs`, `tools/sandbox/probes/voice-mutants.mjs`, `vo1-snapshot-mutants.mjs`,
`vo3-field-portraits.mjs`, `commit-1d-leak-run.mjs`; вердикт судьи — транскрипт сессии 74 (субагент); логи — скретчпад сессии 74
(`ow1/*.log`), переисполнимы командами выше.

## 7. Вердикт

**pass** — портрет-потребитель получает слепок релиза заменой на обоих маршрутах, слияние над слепком получает отказ и на настоящем
портрете поля, приватный портрет развёртывания не тронут; непубличная фраза владельца не проходит коммитом. Зазоры названы (§5 п. 1–2):
слияние без первой строки прежнего слепка, оболочка жанра как есть, сырая копия приватного ядра под именем портрета.
