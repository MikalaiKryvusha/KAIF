# Отчёт прогона — эпик CH 2.8, шаг CH2: обязательные строки владельцу — на его языке

**Создан:** 2026-09-26 00:16 +03:00 · **Прогон вёл:** Claude Opus 5.5 (1M context), сессия 74 (ночной цикл) · **Версия/сборка:** HEAD `093efc1` + рабочее дерево

## 1. Работа

Критерий 11 `plans/117` (тикет origin #97; слово полевого владельца дословно — в тикете): навыки закрытия поставки требовали строку о
стоячей лжи «verbatim» по-английски — и на русском развёртывании агент писал владельцу в чат английскую строку. Машинного читателя у
строки нет (`grep "Standing falsehood" framework --include=*.mjs` → 0, `researches/32` §2г). Сделано: `/end-chat-soft` и `/end-chat-force`
поставки — строки к владельцу «на языке владельца» (`.kaif/kaif.json` → `language`), английская форма остаётся образцом; руководство
поставки — строка таблицы «Languages»: отчёт владельцу вместе со строками, которые навык требует по имени, — на языке владельца, а
английский ключ — только там, где документ грепает машина или судья (`FORK:` · `AUTH:` · `INTENT:` · `TWINS:` · `PENDING:`); судья обоих
слоёв принимает обе формы. У истока навыки закрытия уже русские.

## 2. Контур

Исток KAIF, Windows 11, Git Bash, Node v24; свежая установка из `dist` с `--lang ru` во временном каталоге (развёрнутые навыки прочитаны).

## 3. Прогоны

| # | Момент | Команда | Исход |
|---|---|---|---|
| 1 | 2026-09-26 00:10:16 | правки (скрипт); `wc -l framework/AGENT_GUIDE.md`; `grep -n "owner's language"` по трём файлам критерия | 1080 строк (правило — в строке таблицы, число строк то же); строка правила в каждом из трёх |
| 2 | 2026-09-26, после 00:10:16 | `node tools/build-framework.mjs` | 0 |
| 3 | 2026-09-26, после сборки | свежая установка `install --lang ru`; грепы развёрнутых `.claude/skills/end-chat-*` и `AGENT_GUIDE.md` | `language: ru`; `/end-chat-soft` стр. 128 и `/end-chat-force` стр. 18, 21 — «in the owner's language»; строка таблицы в руководстве — на месте |
| 4 | 2026-09-26 00:12:04 → 00:16:18 | `npm run test:core` (в одиночку) | «sandbox suite: all 27 suites green» |

## 4. Проверки

Гигиена: строка «Проверка» критерия 11 — грепы по трём файлам; сборка; полигон.
Функциональный прогон: NONE в строгом смысле — правило исполняет агент поля в чате владельца; наблюдено одно звено пути: русская свежая
установка получает навыки закрытия с правилом (прогон 3).
РЕАЛЬНЫЙ МИР: накоплено — у развёртываний поля навыки закрытия приедут обновлением 2.8 (правка `framework/skills`) — после выпуска;
данные и техника — эта машина; путь — первое закрытие чата русского развёртывания после 2.8.

## 5. Найдено

Ноль.

## 6. Следы

`framework/skills/end-chat-soft/SKILL.md`, `framework/skills/end-chat-force/SKILL.md`, `framework/AGENT_GUIDE.md`, `/fable-judge` обоих
слоёв; вывод установки — скретчпад (временный каталог удалён).

## 7. Вердикт

**partial** — правило и его доставка до развёрнутых навыков проверены; исполнение агентом поля — после выпуска.
