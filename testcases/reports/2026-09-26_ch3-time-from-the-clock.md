# Отчёт прогона — эпик CH 2.8, шаг CH3: время и названный час — с часов

**Создан:** 2026-09-26 00:24 +03:00 · **Прогон вёл:** Claude Opus 5.5 (1M context), сессия 74 (ночной цикл) · **Версия/сборка:** HEAD `154066c` + рабочее дерево

## 1. Работа

Критерий 12 `plans/117` (тикет origin #96 · `bugs/119` №2, сторона записи): метки времени и решение «успел ли до названного часа» агент
поля брал из ощущения — метки опережали часы на 1–5 минут, «не успел до 12:00» сказано в 11:50; правила «метка — пробой часов в том же
вызове» в поставке не было вовсе (`grep -c "%H:%M %z" framework/AGENT_GUIDE.md` → 0 на CH0), исток починил класс себе и не отгрузил.
Сделано: руководство поставки — пункт «момент берётся пробой часов в ТОМ ЖЕ вызове» (`date '+%Y-%m-%d %H:%M %z'`, PowerShell `Get-Date`),
решение о названном часе — тоже по пробе; `at` маркера освежения — из пробы; строка границы `BOUNDARY: now <проба> · named <время владельца>
· pool <…>` — в `/end-chat-soft`, `/dayloop`, `/nightloop`, `/autoloop` обоих слоёв, как у `/guarded-loop`; охота судьи обоих слоёв — шире
`/guarded-loop`; записка §17 — почему (и предложение о границе стража метки, вынесенное из руководства — руководство те же 1080 строк).
Строка `FORK:` — в скрипте правки и в `plans/121` CH3: A (правило и шаг с командой в точке решения); B (хук строки часов на каждый промпт)
отклонён — хук промпта видит начало хода, семичасовой ход увидел бы одну строку; C — вторая копия сказанного владельцем.

## 2. Контур

Исток KAIF, Windows 11, Git Bash, Node v24; свежая установка из `dist` во временном каталоге. Живой путь правила — этот самый ночной цикл
(названный час 06:33): закрытие откроется строкой `BOUNDARY:` из пробы.

## 3. Прогоны

| # | Момент | Команда | Исход |
|---|---|---|---|
| 1 | 2026-09-26 00:17:45 | правки (скрипт); `wc -l`; `grep -c "%H:%M %z" framework/AGENT_GUIDE.md`; `grep -n BOUNDARY` по четырём навыкам | 1080 строк; 1; строка в каждом из четырёх |
| 2 | 2026-09-26, после 00:17:45 | `node tools/build-framework.mjs`; инвентарь канона | сборка 0; lost 1 — переписанный пункт о точности меток; объявлен в реестре `plans/118` «дополнен» → lost 0 |
| 3 | 2026-09-26, после сборки | правка: экранированный обратный апостроф внутри кода в строке `BOUNDARY` рендерился буквально — заменён словами (оба слоя) | сборка 0 |
| 4 | 2026-09-26 00:18:24 | свежая установка; грепы развёрнутых руководства, четырёх навыков и судьи | `%H:%M %z` — 1; `BOUNDARY` — по 1 в каждом из четырёх; охота судьи «Since KAIF 2.8 (origin issue #96)» — есть |
| 5 | 2026-09-26 00:19:33 → 00:23:56 | `npm run test:core` (в одиночку) | «sandbox suite: all 27 suites green» |

## 4. Проверки

Гигиена: строка «Проверка» критерия 12 — три греп-проверки; сборка; инвентарь `lost 0`; полигон.
Функциональный прогон: NONE в строгом смысле — правило исполняет агент в своей сессии; одно звено пути наблюдено: свежая установка несёт
правило, строку границы и охоту (прогон 4). Живое исполнение — закрытие этого ночного цикла в 06:33 (строка `BOUNDARY:` из пробы).
РЕАЛЬНЫЙ МИР: накоплено — навыки поля приедут обновлением 2.8; данные и техника — эта машина; путь — закрытие этого цикла и первое
закрытие по названному часу в поле после выпуска.

## 5. Найдено

1. **Экранированный обратный апостроф внутри встроенного кода** (строка `BOUNDARY` в `/end-chat-soft`): markdown показал бы его буквально —
   заменено словами до коммита.
2. Ноль дефектов вне этого.

## 6. Следы

`framework/AGENT_GUIDE.md`, `framework/KAIF_REFERENCE.md` §17, `framework/skills/{end-chat-soft,dayloop,nightloop,autoloop,fable-judge}`,
их копии у истока, `plans/118` (реестр); вывод установки — временный каталог удалён.

## 7. Вердикт

**partial** — правило, строка границы и охота доставлены до развёртывания; исполнение — закрытие этого цикла и поле после выпуска.
