# Отчёт прогона — контур вопросов: страница очереди без петли перезагрузки (bugs/125) и значки даты по местному дню

**Создан:** 2026-09-26 08:52 +03:00 · **Прогон вёл:** Claude Opus 5.5 (1M context), сессия 75 · **Версия/сборка:** HEAD `ddca251` (до
починки) → рабочее дерево с починкой `framework/tools/contour/review.mjs`, `dist` пересобран.

## 1. Работа

Две правки отгружаемого контура вопросов и их проверки:
1. `bugs/125` — страница-список очереди, открытая вкладкой, после ответа на первый документ ушла в бесконечную перезагрузку (слово
   владельца в чате ≈ 07:53: «на одно интервью ответил - страница выпбора интервью вошла в циклические перезагрузки, не могу открыть
   вторую»). Базис — «Fix accepted when» бага: без изменений очереди список не перезагружается сам; после ответа в другом окне — одно
   обновление, карточкой меньше; контур жив.
2. Значки «показан» и «внесено» печатали первые десять знаков момента по UTC: с 00:00 до 03:00 по местному — вчерашняя дата (пул
   STATUS, проба `contour-badge-date.mjs`). Базис — местный календарный день момента.

## 2. Контур

Исток KAIF, Windows 11, Node v24, Microsoft Edge headless (`tools/lib/cdp-mini.mjs`), зона +03:00. Контур — в процессе пробы (без окна и
голоса) и развёрнутой копией из `dist` в своде `s22` (генератор — `quietEnv()`, `--no-open --silent`). Окон владельцу не поднималось.

## 3. Прогоны

| # | Момент | Команда | Исход |
|---|---|---|---|
| 1 | 2026-09-26 08:32:37 | `node tools/sandbox/probes/contour-index-focus-loop.mjs` (код HEAD) | BAD: «loads in 3000 ms: 90»; после ответа и одного focus — «92 → 189» |
| 2 | 2026-09-26 08:35:12 | то же, после починки | GOOD: 1 загрузка; «1 → 2», одна карточка; контур жив |
| 3 | 2026-09-26 08:36:39 → 08:37:59 | `node tools/sandbox/s22-contour-shipped.mjs` (новый `dist`) | 100 из 100, (8) зелёный |
| 4 | 2026-09-26 08:38:06 → 08:39:19 | `KAIF_DIST=<dist HEAD ddca251> node tools/sandbox/s22-contour-shipped.mjs` | красный ровно на двух проверках (8): «loads 80, cards 3»; 98 зелёных |
| 5 | ≈ 2026-09-26 08:40 | `node tools/sandbox/probes/contour-badge-date.mjs` · `… <контур HEAD>` | GREEN (очередь и значок — 2026-09-26) · RED на HEAD (обе — 2026-09-25) |
| 6 | 2026-09-26 08:41:18 → 08:42:39 | `node tools/sandbox/s22-contour-shipped.mjs` (+ случай (9)) | 101 из 101 |
| 7 | 2026-09-26 08:42:39 → 08:43:52 | `KAIF_DIST=<dist HEAD> node tools/sandbox/s22-contour-shipped.mjs` | 5 красных: (8) ×2 и (9) — адресаты; и две строки «A: --selftest» — нативный крах `node` 3221226505 после последней проверки (`bugs/109`, шестое вхождение) |
| 8 | ≈ 2026-09-26 08:45 | `node tools/review.mjs --selftest` ×3 | «contour selftest green: 109 checks» трижды |
| 9 | 2026-09-26 08:47:13 → 08:48:22 | `node tools/verify-contour.mjs` | «213 зелёных, 0 красных» |
| 10 | 2026-09-26 08:48:35 → 08:54:24 | `npm run test:core` | «sandbox suite: all 28 suites green» (в одиночку) |

## 4. Проверки

Гигиена: самопроверка контура 109/109 трижды; свод `s22` на новой сборке зелёный, на сборке до починки красный ровно на новых адресатах.

Функциональный прогон: страница-список очереди в настоящем браузере (headless Edge, вкладкой, как у владельца) по поставочному генератору —
прочитаны число загрузок страницы, число карточек и живость сервера; полная приёмка контура в браузере (`verify-contour`, 213 проверок);
значки даты — прочитаны строка очереди и значок «внесено» на странице документа. Путь владельца (его Edge) — не пройден: DONE `bugs/125`
ждёт его следующей очереди из двух документов.

## 5. Найдено

1. `bugs/125` воспроизведён и починен: перезагрузка на КАЖДЫЙ фокус — петля во вкладке; теперь фокус спрашивает пульс, перезагрузка только
   при изменении отпечатка очереди.
2. Значки даты — починены помощником `localDay` (местный день момента), три места.
3. Шестое вхождение `bugs/109` — нативный крах `node` в самопроверке контура, без сети наружу (записано в баге).

## 6. Следы

Скретчпад сессии 75: `s22-new.log` · `s22-old.log` · `s22-new2.log` · `s22-old2.log` · `selftest-1..3.log` · `verify-contour.log` ·
`polygon-contour.log` · `olddist/` (dist HEAD) · `oldcontour/` (контур HEAD); лог краха — `%TEMP%\kaif-sbx-contour-shipped-RNVoEN\run-fail-1.log`.

## 7. Вердикт

**pass** — петля страницы-списка воспроизведена и снята (s22 (8) красный на сборке до починки ровно на новых адресатах), значки даты печатают местный день, приёмка контура 213/0 и полигон 28/28; путь владельца (его браузер) не пройден — DONE `bugs/125` ждёт его следующей очереди из двух документов.
