# Отчёт прогона — RL2, находка суда D-F2: строка записи о развёртывании узнаётся во всех десяти языках поставки

**Создан:** 2026-09-26 09:08 +03:00 · **Прогон вёл:** Claude Opus 5.5 (1M context), сессия 75 · **Версия/сборка:** HEAD `c37d666` (до
починки) → рабочее дерево с починкой `framework/installer/KAIF-CORE.mjs` и стражем 5n `tools/check-framework.mjs`, `dist` пересобран.

## 1. Работа

Находка суда версии D-F2 (реестр `reports/KAIF_AUDIT/2026-09-26_rl_court_2.8_registry.md`): сканер устаревших заявлений о версии считает
датированную строку записи о развёртывании («| **KAIF version** | 2.7 | 2026-09-18 |») самой записью, а не журналом, — но узнавал её
подпись только по-английски и по-русски; в восьми других языках поставки такая строка молчала. Базис — критерий 17 `plans/117`
(заявление о версии судится парой; строка записи судится при любой дате) и подписи строки записи в шаблонах `KAIF_FRAMEWORK.md` десяти
языковых пакетов.

## 2. Контур

Исток KAIF, Windows 11, Git Bash, Node v24. Сканер `stale-claims` ядра поставки: свод `s29` (развёртывание-фикстура), проба мутантов
`sc-mutants` (копии `dist`), функциональный прогон — git-клоны восьми реальных развёртываний на диске (оригиналы только читались, имена не
печатались — буквы A–I).

## 3. Прогоны

| # | Момент | Команда | Исход |
|---|---|---|---|
| 1 | ≈ 2026-09-26 08:58 | `node scratchpad/df2-edit.mjs` · `node tools/build-framework.mjs` · `node tools/check-framework.mjs --selftest` | подписи — из десяти шаблонов (10 разных); сборка OK; гард 5n: пакет вне списка, лишняя подпись и ядро без списка названы, равные молчат |
| 2 | 2026-09-26 08:58:41 → 08:58:59 | `node tools/sandbox/s29-scanners.mjs` | «✅ s29: all green», C14 — 8 из 8 строк названы |
| 3 | 2026-09-26 08:58:59 → 08:59:13 | `KAIF_DIST=<dist HEAD c37d666> node tools/sandbox/s29-scanners.mjs` | «❌ s29: 1 red» — ровно C14, «0 из 8» |
| 4 | 2026-09-26 08:59:59 → 09:04:39 | `node tools/sandbox/probes/sc-mutants.mjs` | «✅ 19 mutants red exactly on their named addressees, and only on them» (M19 новый; адресаты M6 · M14 дополнены C14) |
| 5 | 2026-09-26 09:05:34 → 09:06:17 | `node scratchpad/df2-field.mjs <ядро HEAD> <новое ядро> <девять развёртываний>` | восемь клонов (все — русские, версии 2.1 · 2.2 · 2.7): строк у старого и нового ядра поровну (13 · 13 · 8 · 6 · 1 · 8 · 8 · 19), добавлено 0, убрано 0; одно развёртывание — не git-репозиторий, пропущено |
| 6 | 2026-09-26 09:06:37 → 09:12:14 | `npm run test:core` | «sandbox suite: all 28 suites green» (в одиночку) |

## 4. Проверки

Гигиена: гард сборки 5n (самопроверка — четыре ответа; на настоящем ядре и настоящих пакетах молчит); свод `s29` C14 красный на сборке до
починки и зелёный после; 19 мутантов красные ровно на своих адресатах.

Функциональный прогон: сканер доставленного ядра на git-клонах восьми реальных развёртываний — прочитано число названных строк у старого и
нового ядра по каждому; разницы нет, потому что всё поле — русское, а правка трогает только восемь других языков (регресса на поле нет;
улучшение на языковом лице поля не наблюдалось — такого развёртывания на машине нет).

## 5. Найдено

1. Подписи строки записи в шаблонах — десять разных; ядро знало две. Теперь список — данными из шаблонов, страж 5n держит его равным.
2. Мутанты M6 и M14 теперь глушат и C14 — их адресаты дополнены (урок EXP-0169 в действии).
3. Регресса на поле — ноль.

## 6. Следы

Скретчпад сессии 75: `df2-edit.mjs` · `build4.log` · `s29-new.log` · `s29-old.log` · `sc-mutants.log` · `df2-field.mjs` · `df2-field.log` ·
`polygon-df2.log` · `olddist2/` (dist HEAD `c37d666`).

## 7. Вердикт

**pass** — строка записи о развёртывании под подписью любого из десяти языков поставки названа сканером (s29 C14 красный на сборке до починки ровно на себе), список подписей держит гард 5n, мутанты 19 из 19, поле без регресса; улучшение на нерусском развёртывании поля не наблюдалось — такого на машине нет.
