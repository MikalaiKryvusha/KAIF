# Отчёт прогона — RL2, семь оставшихся находок суда: D-F5 · D-F6 · B-F3 · E-F3 · A-F3 · заметка A · B-F4

**Создан:** 2026-09-26 10:08 +03:00 · **Прогон вёл:** Claude Opus 5.5 (1M context), сессия 75 · **Версия/сборка:** HEAD `83400e6` (до починок) →
рабочее дерево с починками `framework/tools/kaif-testrun-lint.mjs`, `framework/installer/KAIF-CORE.mjs`, `framework/hooks/pretool-owner-word.mjs`
(комментарий), `framework/skills/{report-bug,end-chat-soft}/SKILL.md`, `framework/KAIF_REFERENCE.md`, `tools/build-framework.mjs`,
`tools/check-framework.mjs`, своды `s22` · `s26`, проба `tb-mutants`; `dist` пересобран.

## 1. Работа

Семь последних находок суда версии (реестр `reports/KAIF_AUDIT/2026-09-26_rl_court_2.8_registry.md`, раздел «В RL2»):
- **D-F5** — вариант охоты за шагами, записанный пунктом списка (без исхода), засчитывался. Базис — шаблон C `/report-bug`: охота —
  таблица `| # | variant (axis: value) | outcome |`, не меньше трёх строк; `TESTING_FRAMEWORK.md` → «Охота за шагами» («записывай каждую попытку» с исходом).
- **D-F6** — заголовок `## **Description**` не узнавался разделом; значение метки на следующей строке («**Build:**» ⏎ «2.8.1») не читалось;
  шаблон C (английский) не называл принимаемых слов другого языка. Базис — тот же шаблон; отчёт пишется на языке владельца.
- **B-F3** — заметка версии 2.8 о страже слова владельца посреди хода: «It never stops the work: one refusal per message» — редакция
  «один отказ» живьём не наблюдалась (`ON-REAL-PATH … NOT YET observed live`).
- **E-F3** — записка §4: «five artifacts» при шести файлах выпуска; у карты модулей нет роли в манифесте.
- **A-F3** — слепок ядра голоса, сохранённый с меткой кодировки (BOM) в начале, получал отказ контрольной точки со словами «строки
  слепка нет», хотя она была.
- **Заметка A** — строку цены входа («entry cost ≈ N токенов») машина печатает, но навык закрытия не велит передать её владельцу, как
  обещал принятый им сценарий (интервью №035, Q2; тикет origin #99).
- **B-F4** — мигающий красный `s22 D` (`--close` закрывает страницу): фиксированная пауза 1,5 с, в тексте провала нет вывода генератора.

## 2. Контур

Исток KAIF, Windows 11, Git Bash, Node v24, Microsoft Edge headless (своды контура). Линтер отчёта тестировщика — самопроверка, проба
мутантов `tb-mutants` (копии модуля), сравнение HEAD ↔ рабочее дерево на трёх формах суда и на 7078 документах `bugs/` пятнадцати
развёртываний на диске (только чтение); ядро — свод `s26` на новой сборке и на `dist` HEAD `83400e6` (шов `KAIF_DIST`), проба
`voice-mutants`; реестр пар сборки (`tools/check-framework.mjs`); страж слова владельца — стенограмма ЭТОЙ сессии (настоящий путь:
хук подключён в `.claude/settings.json` истока, сообщения набирал владелец); полигон целиком.

## 3. Прогоны

| # | Момент | Команда | Исход |
|---|---|---|---|
| 1 | 2026-09-26 → 09:48:02 | `node tools/sandbox/probes/tb-mutants.mjs` | «❌ tb-mutants: 3 BAD» — M6 (адресаты изменились: hunt-2 · hunt-none · ru-form-1), M10 · M11 (якорь 0 раз) — проба отстала от правки D-F5 |
| 2 | 2026-09-26 → 09:49:10 | то же, адресаты и якоря обновлены, мутанты M12 · M13 · M14 добавлены | «✅ tb-mutants: control green, 14 mutants red exactly on their named addressees» |
| 3 | 2026-09-26 → 09:51:45 · → 09:52:02 | `node tools/sandbox/s26-voice-lint.mjs` · `KAIF_DIST=<dist HEAD 83400e6> node tools/sandbox/s26-voice-lint.mjs` | новая — «all 88 checks green»; HEAD — «1 of 88 check(s) failed», ровно A-F3: «…has no line «# Ядро голоса и мышления владельца KAIF — публичный слепок» — the release snapshot is not in it yet» |
| 4 | 2026-09-26 → 09:57:01 | `node tools/sandbox/probes/voice-mutants.mjs` | «✅ 18 mutants red exactly on their named addressees, and only on them» |
| 5 | 2026-09-26 → 09:58:32 | `node tools/check-framework.mjs` при удалённом предложении из `framework/skills/end-chat-soft/SKILL.md` (правка В ДЕРЕВЕ с копией-резервом, возвращена) | «❌ check-framework FAILED: … pair "entry cost ↔ /end-chat-soft farewell carries the line (payload)" broken» |
| 6 | 2026-09-26 → 09:58:35 | `node tools/build-framework.mjs` (предложение на месте) | сборка OK, check-framework OK |
| 7 | 2026-09-26 09:59:44 → 10:05:40 | `npm run test:core` | «sandbox suite: all 28 suites green» (в одиночку); `s22 D` — «--close закрывает: генератор завершился САМ кодом 2 … замок снят» ✅ |
| 8 | 2026-09-26 10:03:13 | `node <скретчпад>/oldlint/prove.mjs <линтер>` — HEAD (побайтно `git show HEAD:…`) и рабочее дерево | HEAD: список из трёх → `[]` · жирные заголовки → `[missing-section,hunt-too-short]` · значение на следующей строке → `[missing-line]`; новое: `[hunt-too-short]` · `[]` · `[]` |
| 9 | 2026-09-26 10:03:42 → 10:03:49 | `node <скретчпад>/field-bug-diff.mjs <HEAD> <новый> <15 развёртываний>` | «judged 7078 bug documents · with a hunt heading 0 · verdict changed 0» |
| 10 | 2026-09-26 ≈ 10:05 | `node <скретчпад>/gate-live2.mjs <стенограмма сессии 75>` | «genuine refusals 7 · owner messages 6» — строки ниже, §4 |
| 11 | 2026-09-26 → 10:06:06 | `node tools/build-framework.mjs` (после строки ON-REAL-PATH стража, B-F3) | сборка OK, check-framework OK |
| 12 | 2026-09-26 10:06:24 → 10:06:29 | `node tools/sandbox/s14-refresh-hooks.mjs` | «песочница refresh-hooks зелёная» — правка стража после полигона была только комментарием; свод его проверяет на новой сборке |
| 13 | 2026-09-26 ≈ 10:07 | `node framework/tools/kaif-testrun-lint.mjs selftest` · `… bug --keywords` | «selftest OK — 103 cases, 8 run-report rules and 5 bug-report rules × 2 languages»; ключевые слова en и ru напечатаны |
| 14 | 2026-09-26 ≈ 10:06 | `node <скретчпад>/ef3.mjs dist` · то же по `dist` HEAD | новая: «roles 6 … files without a role: none»; HEAD: «roles 5 … files without a role: kaif-module-map.json» |
| 15 | 2026-09-26 ≈ 10:21 | `node <скретчпад>/gate-live3.mjs <стенограмма сессии 75>` | «min 3.9 s · max 27.7 s over 6 messages»; 27,7 с — у сообщения 08:33:26, «refusals for other messages in between: 1» |

## 4. Проверки

Гигиена: самопроверка линтера 103 случая (новые: список без исходов → `hunt-too-short`; жирный заголовок — тот же раздел; значение на
следующей строке; три метки подряд без значений → ровно `missing-line`), `tb-mutants` 14 на адресатах (M12 · M13 · M14 — D-F5/D-F6);
`s26` красный на сборке до починки ровно на случае A-F3; `voice-mutants` 18 на адресатах; пара «цена входа ↔ прощание» краснеет без
предложения (прогон 5 — мутация сделана В ДЕРЕВЕ, а не на копии, с резервной копией и возвратом; нарушение EXP-0077 названо, файл
после возврата несёт предложение — сборка 6 зелёная); манифест — роль у каждого из шести файлов (на HEAD карта модулей без роли);
загрузчик закрепляет по sha256 ровно два файла (`framework/installer/KAIF-LOADER.mjs`: `const ARTIFACTS = ['KAIF-CORE.mjs', 'KAIF-CORE-BUNDLE.md']`) —
новая фраза записки §4 сверена с кодом.

Функциональный прогон: (а) B-F3 — страж на НАСТОЯЩЕМ пути: стенограмма сессии 75, где владелец восемь раз писал посреди хода
(2026-09-26 07:53–09:25 +03:00), и после шести из этих сообщений страж отказывал (одно ещё не легло в стенограмму — зазор GAP, одно
перекрыто более новым; исправлено 2026-09-26 11:08 +03:00 по лёгкому судье J-F3: стояло «шесть раз писал»): семь отказов в шести раундах (первый раунд — два параллельных вызова одного сообщения, как названо в GAP стража),
следующий вызов прошёл через 3,9–8,1 с после пяти раундов и через 27,7 с после одного — между ними был отказ для следующего сообщения
владельца (например, сообщение 07:53:17 → отказы 07:54:05 · 07:54:07 → вызов прошёл 07:54:11),
ни одно сообщение не получило второго отказа — работа не останавливалась; прочитано по строкам вывода прогонов 10 и 15. (Исправлено
2026-09-26 10:22 +03:00: стояло «через 2–8 с после каждого раунда» — число выведено на глаз из вывода прогона 10; прогон 15 считает
задержку скриптом.) Ответ текстом на
первое сообщение пришёл после прошедшего вызова (текст между вызовами мог лечь рассуждением — зазор GAP, не опровержение заметки).
(б) D-F5/D-F6 — линтер, который агент поля зовёт на свой отчёт, прочитан на трёх формах суда (прогон 8) и на 7078 реальных документах
багов поля: вердикт не изменился ни у одного (разделов охоты в поле пока нет — шаблон C пришёл с 2.8). (в) A-F3 — `checkpoint
owner-voice-core` настоящего ядра на развёртывании-фикстуре со слепком с BOM: «equals the release snapshot byte for byte» (прочитано);
на поле ни один из 11 портретов метки не несёт (`od` первых трёх байт) — починка поведения поля не меняет. (г) E-F3 — манифест
новой сборки прочитан (прогон 14). (д) Заметка A — у истока ядро поставки не исполнимо, строки цены входа нет; на развёртывании
строку печатает `check` (свод `s16`, полигон 7) — что агент поля передаст её владельцу, наблюдается только на его закрытии.
(е) B-F4 — `s22 D` зелёный в полигоне 7; мигание по требованию не воспроизводилось (охота не проводилась) — починка формой: ожидание
события (выход генератора и снятие замка, до 10 с) вместо фиксированной паузы, вывод генератора — в тексте провала (прочитано в коде,
провалом не наблюдалось).

## 5. Найдено

1. Семь находок починены (D-F5 · D-F6 · E-F3 · A-F3 · заметка A · B-F4 — правкой; B-F3 — наблюдением, строка ON-REAL-PATH стража
   дописана).
2. Проба `tb-mutants` отстала от правки D-F5 (3 BAD, прогон 1) — поймана тем же ходом по уроку EXP-0169, адресаты и якоря
   обновлены до коммита.
3. Мутация доказательства пары сделана в дереве, а не на копии (EXP-0077) — возвращена, последствий нет; названо здесь.
4. Попутно в журнале анализа: команда поиска `grep` по стенограмме сама напечатала текст отказа и ложно посчиталась отказом в
   первой редакции скрипта — вторая редакция считает только записи ошибки хука (`is_error` + «hook error»).
5. Стоячая ложь этого же отчёта: «следующий вызов прошёл через 2–8 с» — выведено на глаз из вывода прогона 10, стояло здесь, в реестре суда,
   в строке ON-REAL-PATH стража и в сообщении коммита `fd5dd01`; прогон 15 посчитал скриптом (3,9–8,1 с, один раз 27,7 с). Исправлено
   на местах; сообщение коммита не переписывается — поправку называет следующий коммит.

## 6. Следы

Скретчпад сессии 75: `tb-mutants.log` · `tb-mutants2.log` · `s26-new.log` · `s26-old.log` · `voice-mutants.log` · `pair-red.log` ·
`build9b.log` · `polygon-batch3.log` · `oldlint/` (линтер HEAD `83400e6` и `prove.mjs`) · `field-bug-diff.mjs` + `.log` ·
`gate-live2.mjs` + `gate-live.log` · `gate-live3.mjs` + `gate-live3.log` · `build10.log` · `s14-after-bf3.log` · `ef3.mjs` · `olddist5/` (dist HEAD `83400e6`).

## 7. Вердикт

**pass** — шесть находок починены и доказаны красным на сборке до починки (D-F5/D-F6 — прогон 8 и мутанты; A-F3 — `s26`; заметка A —
пара сборки; E-F3 — манифест), B-F3 закрыта наблюдением на настоящем пути владельца, полигон 28/28 в одиночку; B-F4 — починка формой
без воспроизведённого мигания (названо в §4 (е)).
