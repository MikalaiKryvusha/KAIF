# Отчёт прогона — тикет #107 (все четыре замечания, №137) и оговорки лёгкого повторного судьи RL2 (J-F1 · J-F2 · J-F3 · J-F4)

**Создан:** 2026-09-26 11:08 +03:00 · **Прогон вёл:** Claude Opus 5.5 (1M context), сессия 75 · **Версия/сборка:** HEAD `709` (коммит нот RL3) → рабочее
дерево с правками `tools/build-framework.mjs`, `framework/installer/KAIF-CORE.mjs`, `framework/tools/kaif-voice-lint.mjs`,
`framework/tools/contour/review.mjs`, `framework/hooks/pretool-owner-word.mjs` (комментарий), `framework/AGENT_GUIDE.md` и корневого
`AGENT_GUIDE.md` (одна фраза), своды `s04` · `s21` · `s26`; `dist` пересобран.

## 1. Работа

- **#107** (отчёт установки «почти готовой 2.8» из сборки `83400e6`; слово владельца — интервью №041, Q1 = A, решение №137):
  (1) сборка несёт удостоверение (`build.sourceTree`, `build.prerelease`), установка пишет его в маркер, обновление до этой версии
  называет происхождение; (2) пункт заполнения шаблонов установки говорит, что зеркала навыков синхронизирует `checkpoint placeholders`;
  (3) проверка голоса называет файл, побайтно равный записанному установкой, шаблоном; (4) пункт портрета — какую редакцию ставить в
  публичный репозиторий. Базис — тикет #107 и вариант A интервью №041.
- **J-F1** — сторож документа при живой странице очереди через минуту выходил кодом 2 с ложной строкой (починка B-F1 сузила «живой контур»
  до замка документа; сервер очереди держит замок `_queue`).
- **J-F2** — снятие «с 2.7» (C-F2) не доходило до поля по маршруту `update`: задание пишет ядро 2.7 без пункта `withdrawn-phrases`.
- **J-F3** — числа B-F3 шире наблюдения («шесть сообщений» при восьми, отказ — после шести); **J-F4** — стоячая ложь в `STATUS.md`
  («находка B-F1 открыта»). Попутно — дыра для #88, названная черновиком ответа: абзац набросов руководства (оба слоя) велел
  «явное "переключись" → переключайся» без строки `PARKED:`.

## 2. Контур

Исток KAIF, Windows 11, Git Bash, Node v24. Своды `s04` (установка и обновление фикстурами комплекта с изменённой метой), `s21`
(настоящий маршрут: установка из артефактов v2.7, `update` ядром 2.7, дальше свежее ядро), `s26` (развёрнутая копия с портретом);
самопроверки проверки голоса и контура; мутант контура на КОПИИ каталога; красные — швом `KAIF_DIST` на `dist` коммита `83400e6`
(`<скретчпад>/olddist5/dist`); полигон целиком.

## 3. Прогоны

| # | Момент | Команда | Исход |
|---|---|---|---|
| 1 | 2026-09-26 → 10:48:47 | `node tools/sandbox/s04-anon-legacy.mjs` (первая редакция) | «❌ ПРОВАЛОВ: 2» — S14c: пометка предрелиза задела заметки ВСЕХ версий до 2.8 (фикстура S14c ставит из текущего `dist`, а он теперь предрелиз 2.8); пометка сужена до одной версии |
| 2 | 2026-09-26 10:49:21 → 10:49:38 | то же после правки | «✅ все песочницы 5.5 зелёные» — S17 (а)–(г) шесть проверок |
| 3 | 2026-09-26 10:49:38 → ≈ 10:50 | `node tools/sandbox/s26-voice-lint.mjs` | «all 90 checks green» (+2: шаблон GOAL.md назван шаблоном; правленная копия со старым временем — «written past the portrait») |
| 4 | 2026-09-26 10:50:05 → 10:50:22 | `KAIF_DIST=<dist 83400e6> node tools/sandbox/s04-anon-legacy.mjs` | «❌ ПРОВАЛОВ: 5» — ровно S17 (а)–(в); контроль (г) зелёный и там |
| 5 | 2026-09-26 10:50:30 → 10:50:48 | `KAIF_DIST=<dist 83400e6> node tools/sandbox/s26-voice-lint.mjs` | «2 of 90 check(s) failed» — случай шаблона #107 и A-F3 (`83400e6` старше его починки); контроль #107 зелёный |
| 6 | 2026-09-26 ≈ 10:47 | `node framework/tools/kaif-voice-lint.mjs selftest` | «selftest OK — 64 cases» (+2: файл, равный sha развёртывания, — шаблон; правленный, чужой путь, нет манифеста — нет) |
| 7 | 2026-09-26 10:51:26 | `node tools/review.mjs --selftest` | «contour selftest green: 111 checks» (+1: 4c — сторож документа при живой странице очереди ждёт дольше окна и кончается кодом 2 с её концом) |
| 8 | 2026-09-26 ≈ 10:52 | копия `framework/tools/contour/` в скретчпаде, в ней `live()` → только замок документа; `node <копия>/review.mjs --selftest` | «SELFTEST RED: 1 of 111» — ровно 4c (J-F1) |
| 9 | 2026-09-26 10:54:16 → 10:54:53 | `node tools/sandbox/s21-update-route.mjs` | «s21: all green» (+2: E3 — задание ядра 2.7 без `withdrawn-phrases`; свежее ядро на `checkpoint recheck` называет снятие «с 2.7» с фразой, `(2.7 → 9.9)`) |
| 10 | 2026-09-26 10:55:01 → 10:55:36 | `KAIF_DIST=<dist 83400e6> node tools/sandbox/s21-update-route.mjs` | «❌ s21: 1 red» — ровно E3 (J-F2) |
| 11 | 2026-09-26 ≈ 10:57 | `node tools/canon-inventory.mjs --diff v2.7 --with framework/KAIF_REFERENCE.md --declared plans/118_…md` · `wc -l framework/AGENT_GUIDE.md` | «lost 0»; 1080 строк (потолок держится) |
| 12 | 2026-09-26 10:57:29 → 11:03:10 | `npm run test:core` | «sandbox suite: all 28 suites green» (в одиночку) |
| 13 | 2026-09-26 11:04:39 → 11:05:49 · затем ≈ 11:08 | `node tools/verify-contour.mjs` | «213 зелёных, 2 красных» — ровно намеренные G7 (новые живые документы №041 и №042 не в эталоне); `--write-etalon` — числа пересмотрены глазами (№041: 1 вопрос · 4 варианта; №042: 3 · 6), `--etalon-only` — «88 зелёных, 0 красных»; проба `node tools/sandbox/probes/ow6-contour-mutants.mjs` (11:05:49 → 11:07:20) — «9 mutants red exactly on their named cases» (новый — J-F1, красный ровно на 4c) |

## 4. Проверки

Гигиена: s04 S17 — шесть проверок, на `83400e6` красные ровно пять новых; s26 +2, на `83400e6` красный ровно случай шаблона; s21 E3,
на `83400e6` красный ровно он; самопроверки — проверка голоса 64, контур 111; мутант контура — красный ровно на 4c; инвентарь канона —
потерь 0; сборка и гарды зелёные.

Функциональный прогон: #107 — настоящее ядро и настоящий комплект по пути поля: `install` из комплекта-предрелиза печатает строку
«PRE-RELEASE of KAIF 9.8» и пишет маркер (прочитан JSON: `prerelease 9.8`, `build` двенадцать знаков, версия прежняя); `update --source`
до 9.8 пишет задание с пунктом `prerelease-origin` и помеченной заметкой (прочитано), маркер после — без `prerelease`; маршрут bootstrap —
то же. Проверка голоса — развёрнутая копия после `install`, настоящий портрет-скелет с одной заполненной строкой, `load`, затем `check
GOAL.md`: строка «byte-equal to what the deploy wrote» (прочитана) — эта первая редакция опровергнута лёгким судьёй (K-F1, ниже):
свежая установка — не весь путь поля. После починки — строка «equal to the template this release shipped» на нетронутом `GOAL.md` и
«written past the portrait» на правленом `GOAL.md` после `adopt-current` (прочитаны, `s26` новые случаи; исправлено 2026-09-26 11:36 +03:00). J-F2 — настоящий маршрут поля: артефакты релиза v2.7 из git, `update`
исполнило ядро 2.7, `checkpoint recheck` — свежее ядро; строка передачи с фразой прочитана. У поля (дерево проекта, поставленного из
`83400e6`) новый код не наблюдался: его маркер без `prerelease`, путь описан в ответе #107.

## 5. Найдено

1. Первая редакция пометки предрелиза задела заметки всех версий до 2.8 — поймана сводом `s04` S14c (прогон 1), сужена до одной версии.
2. При выносе пункта `withdrawn-phrases` в функцию первая правка вставила служебную метку и лишнюю скобку посреди `writeUpdateTask` —
   исправлено скриптом до сборки (`node --check` зелёный, вызов и функция на местах).
3. Правкой ввёл в два файла настоящий U+FEFF вместо escape (класс `bugs/122`) — сборка отказала, починено её готовой командой
   (`invisible-characters.mjs --fix`); урок в силе: escape в параметрах правки не набирать.
4. **Лёгкий судья этой пачки (HEAD `365d163`, 11:15 → 11:27): REFUTED — 20 = 17 · 2 · 1.** K-F1 (опровержение, блокер выпуска): проверка
   голоса сверяла файл со снимком диска `shas`, который освежают update, update-verify и adopt-current, — после любого обновления текст
   агента назывался «нетронутым шаблоном», код 0; мой контрольный случай правил ДРУГОЙ путь (`GOAL-edited.md`, его нет в манифесте) и
   ловить этого не мог. Починено: сверка с `templateShas` (шаблон выпуска, нормализованные концы строк); `s26` +1 — правленый `GOAL.md`
   после `adopt-current` судится свидетелем; на сборке `365d163` красный ровно этот случай («0 findings, 2 warning(s)») и изменившийся
   текст строки шаблона; самопроверка голоса 64 (+ CRLF). K-F2: `prerelease`, оставшийся не новее версии маркера, давал ложное
   происхождение — теперь засчитывается только предрелиз новее версии; `s04` S17 (б2), на `365d163` красный ровно он (первый прогон
   случая упал на фикстуре: ядро верно отказалось начинать обновление при незакрытом задании прошлого — фикстура удаляет его явно).
   K-F3: помощник меток времени заменил в правиле `STATUS.md` сам токен — возвращён, помощник больше не трогает токен в обратных
   кавычках (проверено на копии: 0 замен). K-F4: сторож документа ждёт, пока жива ЛЮБАЯ страница очереди, — судья счёл приемлемым,
   кода не меняет, названо в реестре.

## 6. Следы

Скретчпад сессии 75: `build12.log`–`build16.log` · `s04-107-new.log` · `s04-107-new2.log` · `s04-107-old.log` · `s26-107-new.log` ·
`s26-107-old.log` · `selftest-jf1.log` · `jf1-mut/` + `jf1-mut.log` · `s21-jf2-new.log` · `s21-jf2-old.log` · `polygon-107.log` ·
`olddist5/` (dist `83400e6`) · после судьи: `s04-kf-new4.log` · `s04-kf-old2.log` · `s26-kf-new.log` · `s26-kf-old.log` ·
`dist710/` (dist `365d163`) · `polygon-release2.log`.

## 7. Вердикт

**pass после починки опровержения** — первая редакция проверки голоса опровергнута судьёй (K-F1) и починена, K-F2 · K-F3 починены,
полигон на версии 2.8 после починок — в отчёте выпуска `testcases/reports/2026-09-26_release-2.8.md`. Все четыре замечания #107 внесены и доказаны красным на сборке `83400e6` (s04 S17 — пять новых проверок, s26 — случай шаблона); J-F1 — мутант контура красный ровно на 4c, J-F2 — s21 E3 красный на `83400e6` на настоящем маршруте с ядром v2.7; J-F3 · J-F4 — тексты поправлены с пометкой «исправлено»; полигон 28/28 в одиночку, приёмка контура зелёная. У поля новый код ещё не наблюдался — путь проекта, поставленного из `83400e6`, назван в ответе #107.
