# Отчёт прогона — RL2, находки суда B-F1 и C-F2: сторож без живого контура и снятие версии развёртывания

**Создан:** 2026-09-26 09:37 +03:00 · **Прогон вёл:** Claude Opus 5.5 (1M context), сессия 75 · **Версия/сборка:** HEAD `56cf73e` (до
починок) → рабочее дерево с починками `framework/tools/contour/review.mjs` и `framework/installer/KAIF-CORE.mjs`, `dist` пересобран.

## 1. Работа

Две находки суда версии (реестр `reports/KAIF_AUDIT/2026-09-26_rl_court_2.8_registry.md`):
- **B-F1** — сторож `--wait` ждал вечно, если контур кончился до его старта (канон обещает код 2). Базис — `/owner-reviews` I8 и таблица
  команд контура: «код 0 на каждой записи ответа, 2 — контур завершился без записи»; ритуал: сторож стартует ДО страницы.
- **C-F2** — снятие возможности «с 2.7» не искалось у развёртываний 2.7: сравнение требовало версию снятия строго новее версии
  развёртывания, а обновление 2.7 поиска фраз не несло (пункт пришёл с 2.8). Базис — критерий 13 `plans/117` (снятие не оставляет стоять
  тексты).

## 2. Контур

Исток KAIF, Windows 11, Git Bash, Node v24, Microsoft Edge headless. Самопроверка отгружаемого контура; проба мутантов контура (копии
`framework/tools/contour`); свод `s04` (развёртывание-фикстура: установка текущей версии → обновление с фикстурой снятий) на новой
сборке и на `dist` HEAD `56cf73e`; полная приёмка контура `verify-contour`.

## 3. Прогоны

| # | Момент | Команда | Исход |
|---|---|---|---|
| 1 | ≈ 2026-09-26 09:29 | `node tools/build-framework.mjs` · `node tools/review.mjs --selftest` | сборка OK; «contour selftest green: 110 checks» (новый случай «no live contour seen within its window → exit 2») |
| 2 | 2026-09-26 ≈ 09:29 → 09:30:46 | `node tools/sandbox/s04-anon-legacy.mjs` · `KAIF_DIST=<dist HEAD 56cf73e> node tools/sandbox/s04-anon-legacy.mjs` | новая — зелёный; HEAD — «❌ ПРОВАЛОВ: 1» ровно S14d/CH4 (C-F2) |
| 3 | 2026-09-26 09:30:55 → 09:32:01 | `node tools/sandbox/probes/ow6-contour-mutants.mjs` | «✖ 1 BAD»: мутант «страница не перечитывает себя после частичной записи» — якорь встретился дважды (второе вхождение добавила починка `bugs/125` утром; пробу тогда не прогоняли) |
| 4 | 2026-09-26 09:32:15 → 09:33:31 | то же, якорь уточнён | «✅ 8 mutants red exactly on their named cases» (новый — «сторож без живого контура ждёт вечно»: 1 красный, его случай) |
| 5 | 2026-09-26 09:33:40 → 09:35:24 | `node tools/sandbox/probes/ow3-contour-mutants.mjs` · `ow4-call-mutants.mjs` · `ow5-archaeology-mutants.mjs` | 5 · 3 · 3 мутанта красные ровно на своих случаях |
| 6 | 2026-09-26 09:35:32 → 09:36:42 | `node tools/verify-contour.mjs` | «213 зелёных, 0 красных» |
| 8 | 2026-09-26 09:42:40 → 09:43:40 | `node tools/review.mjs --wait interviews/interview_040_kaif_2.8_release_gate_and_name.md` (живой страницы нет) | выход 2, строка «No live contour … within 60 s» |
| 7 | 2026-09-26 09:36:50 → 09:42:31 | `npm run test:core` | «sandbox suite: all 28 suites green» (в одиночку) |

## 4. Проверки

Гигиена: самопроверка контура 110/110; мутанты контура — 8 · 5 · 3 · 3 на адресатах; свод `s04` красный на сборке до починки ровно на
новом случае.

Функциональный прогон: сторож — настоящая команда истока `node tools/review.mjs --wait <док>` без живой страницы: старт 2026-09-26 09:42:40, выход 09:43:40 кодом 2 со строкой «No live contour for interviews/interview_040_kaif_2.8_release_gate_and_name.md within 60 s — nothing to wait for …» (прочитано);
развёртывание-фикстура на настоящем ядре — `update`
с снятием «с версии развёртывания» (прочитано: задание называет фразу `FIXTURE-SAME-RELEASE-PHRASE` с командой поиска); полная приёмка
контура в браузере — 213 проверок. На пути владельца (его очередь, его вкладка) сторож наблюдался утром в прежней редакции; новая
ветка (окно без страницы) у владельца не наблюдалась.

## 5. Найдено

1. B-F1 и C-F2 починены (окно сторожа — минута; снятие — «не старше версии развёртывания»).
2. Проба мутантов контура сломалась от утренней починки `bugs/125` (якорь стал неоднозначным) и молчала до этого прогона — якорь уточнён;
   урок уже записан как класс (EXP-0169: новая проверка того же места — сначала адресаты и якоря проб).

## 6. Следы

Скретчпад сессии 75: `build7.log` · `s04-new.log` · `s04-old.log` · `ow6-mutants.log` · `ow6-mutants2.log` · `ow3-contour-mutants.log` ·
`ow4-call-mutants.log` · `ow5-archaeology-mutants.log` · `verify-contour2.log` · `polygon-bf1.log` · `olddist4/` (dist HEAD `56cf73e`).

## 7. Вердикт

**pass** — сторож без живого контура кончается кодом 2 через минуту (настоящая команда истока, строка прочитана), снятие «с версии развёртывания» названо заданием (s04 красный на сборке до починки ровно на нём), мутанты и приёмка контура зелёные.
