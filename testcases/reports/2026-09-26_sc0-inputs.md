# Отчёт прогона — эпик SC 2.8, шаг SC0: входы эпика сверены с HEAD, новых тикетов нет

**Создан:** 2026-09-26 01:28 +03:00 · **Прогон вёл:** Claude Opus 5.5 (1M context), сессия 74 (ночной цикл) · **Версия/сборка:** HEAD `fb982cb`

## 1. Работа

Шаг SC0 `plans/123`: каждый вход эпика — адреса разведки `researches/32` §2б и строк Q-R1′ · K-R4 · N3/N4, снятые на `6af0dad`, —
сверить с HEAD по подписи строки; перечень обходчиков дерева снять грепом; новые тикеты origin после #106; строка «Проверка» критерия 17.

## 2. Контур

Исток KAIF, Windows 11, Git Bash, Node v24, git 2.43, HEAD `fb982cb`. Проба `tools/sandbox/probes/sc0-inputs.mjs` только читает ядро,
шесть tool-модулей и сборщик; тикеты — `gh issue list` (чтение).

## 3. Прогоны

| # | Момент | Команда | Исход |
|---|---|---|---|
| 1 | 2026-09-26 01:26:14 | `node tools/sandbox/probes/sc0-inputs.mjs` | «GOOD — every input located (17)»: 4 совпали (`.claude/worktrees` в ядре — 0; `git ls-files` в ядре — 0; `kaif-canon-lint` стр. 60, `kaif-requirements-lint` стр. 116 — по 1), 9 сдвинулись (ядро выросло: глотающий `catch` обхода 1020 → 1172, ручной список каталогов 931 → 1083, `SKIP_FILES` по полному пути 969 → 1121, обход скана анонимности 2226 → 2488, пропуск датированной строки 1002 → 1154, окно соседства 16 знаков 960 → 1112, построчное снятие скобок 1010 → 1163; `kaif-attribution-lint` 190 → 210, `kaif-guard-lint` 103 → 108), 3 «present» (`kaif-provenance` стр. 209, `kaif-scenario-lint` стр. 156, `KAIF-VERSION-OK` только на строке или над ней — 1153), 1 счёт сдвинулся: гарда «шаблоны против своего скана» в сборщике нет — единственное совпадение `tools/build-framework.mjs:334` — текст заметки 2.5, не гард |
| 2 | 2026-09-26 01:27:07 | копия пробы с испорченной подписью входа 1, запуск, копия удалена | «ABSENT», «BAD — 1 input(s) ABSENT at HEAD», код 1 |
| 3 | 2026-09-26, после 01:26:14 | `gh issue list --state open --limit 10` | новейший открытый — #106 (2026-09-25 20:13 UTC); новых тикетов нет |
| 4 | 2026-09-26, после 01:27:07 | `node framework/tools/kaif-scenario-lint.mjs check plans/117_EPIC_kaif_2.8.md` | до правки — 1 находка `no-command` (критерий 17); после — «0 findings», 29 сценариев |

## 4. Проверки

Гигиена: проба покраснела на испорченной подписи; линт сценариев зелёный после правки. Функциональный прогон: NONE — шаг сверяет входы,
поведения не меняет. Каждое наблюдение разведки подтверждено на HEAD: обход скана заявлений глотает любую ошибку; каталоги пропускаются
ручным списком, вложенные копии не пропускаются нигде; `git ls-files` ядро не зовёт; датированная строка пропускается целиком; окно
соседства одно для прозы и кода; скобки снимаются построчно; маркер «версия в порядке» — только на строке или над ней.

## 5. Найдено

1. **Обходчиков шесть, а не пять:** к `kaif-provenance` · `kaif-canon-lint` · `kaif-requirements-lint` · `kaif-guard-lint` ·
   `kaif-attribution-lint` добавляется `kaif-scenario-lint` (голый `statSync` в `collect`), плюс два обхода ядра — скан заявлений и скан
   анонимности. SC1 берёт все восемь.
2. **`kaif-guard-lint` защищён, но молча:** битая ссылка пропускается без счётчика — тот же класс «ложно чисто» в мягкой форме.
3. **Строка «Проверка» критерия 17 без команды** — правлена (`plans/117`, пометка в строке): названы свод `s29-scanners` и проба
   `sc-mutants`; исходы и число случаев не тронуты (правило 7 формы — правка с обоснованием).
4. Ноль расхождений с планом, кроме адресов строк.

## 6. Следы

`tools/sandbox/probes/sc0-inputs.mjs`; вывод — в этом отчёте.

## 7. Вердикт

**partial** — шаг сверки выполнен; функционального прогона шаг не требует.
