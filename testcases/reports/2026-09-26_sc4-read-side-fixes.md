# Отчёт прогона — эпик SC 2.8, починки суда SC4 (часть A): сторона чтения обходчика и мелкие находки

**Создан:** 2026-09-26 04:35 +03:00 · **Прогон вёл:** Claude Opus 5.5 (1M context), сессия 74 (ночной цикл) · **Версия/сборка:** HEAD `4b06b28` + рабочее дерево

## 1. Работа

Суд эпика SC (SC4, ≈ 2026-09-26 03:51 +03:00; вердикт — `scratchpad/judge-sc4/verdict.md`) — REFUTED; критерий 16 `plans/117` снят с ✅.
Эта часть чинит находки стороны чтения и мелкие; точность скана (F3 · F4 · F5 · F6) и лица гарда 5m (F14) — часть B.

Базис — результат критерия 16: «падение обхода печатается как отказ, а не как „ничего не найдено“; один безопасный обходчик у ядра и у
tool-модулей». Сделано:
- **F1 (S2, регрессия SC1)** — нечитаемый файл ронял сканеры трассой стека, а `update` — после записи маркера. В блоке `KAIF-WALK` —
  `readWalked(tree, p)`: ошибка чтения уходит в `failed` (строка «walk FAILED at <файл> (EPERM)»), файл пропускается. Скан заявлений,
  скан анонимности (нечитаемый файл — отказ) и шесть модулей читают через неё. `update` оборачивает скан: исключение — названный пункт
  задания, не трасса стека.
- **F7** — `kaif-provenance report` на упавшем обходе печатал «✅ no AI text awaits acceptance», код 0; теперь отказ «INCOMPLETE», код 1.
- **F8** — `kaif-guard-lint` молча пропускал нечитаемый файл (`catch { continue }`); теперь называет его, код 1.
- **F9** — копия `.claude/worktrees` судилась по абсолютному пути: проект, который сам живёт под `.claude/worktrees/<агент>/`, при
  абсолютном корне терял все файлы («SKIPPED — nothing to scan»). Теперь путь судится ниже корня обхода.
- **F2** — ложная строка «запрет чтения не воспроизводится» исправлена ещё в `4b06b28` (стоячая ложь — сразу).
- **F10** — «11 красных» в отчёте SC2 → 10 (итог лога).
- **F11** — отказ преполёта 0 `tools/commit.mjs` при личности из окружения называл глобальный `.gitconfig` и советовал `--local
  --unset`; теперь называет переменные и `unset`, а для конфига — область по `--show-origin`.
- **F12** — проба `commit-1d` падала «nothing to commit» на чистом HEAD; коммит пробы — `--allow-empty`.
- **F13** — кириллица в примерах комментария ядра поставки → английские примеры (наборы слов — данные, остаются). (Исправлено 2026-09-26 05:53 +03:00 по
  суду RL1, E-F4: часть B того же утра внесла новый русский пример в комментарий ядра, и ещё две русские буквы-ссылки эпика CK стояли
  рядом — все три переписаны латиницей починкой суда RL1.)

## 2. Контур

Исток KAIF, Windows 11, Git Bash, Node v24. Запрет чтения из Git Bash не воспроизводится (привилегия резервного копирования), поэтому
отказ проверен двумя путями:
- **настоящий запрет ОС** — `icacls /deny` из PowerShell на временном проекте, настоящие процессы модулей и ядра;
- **в своде** — предзагрузка `node --import`, которая делает `readFileSync` / `readdirSync` названного пути EPERM в НАСТОЯЩЕМ процессе
  ядра и модулей поставки (`s29` W4).

Сборка до починки — `dist` коммита `4b06b28` (`git show`, шов `KAIF_DIST`).

## 3. Прогоны

| # | Момент | Команда | Исход |
|---|---|---|---|
| 1 | 2026-09-26 04:08:17 | `scratchpad/tb/f1f7-probe.ps1` (PowerShell, до починки) | `check` с нечитаемым файлом — трасса стека Node, код 1; `report` с нечитаемым каноном — «✅ no AI text awaits acceptance», код 0 (F1, F7 подтверждены) |
| 2 | 2026-09-26 04:16:48 | тот же скрипт, после починки | `check` — «walk FAILED at docs/secret.md (EPERM)», код 1; `stale-claims` ядра — читаемая строка названа и «walk FAILED …», код 1; `report` — «provenance report INCOMPLETE», код 1; запреты сняты, файлы читаются |
| 3 | 2026-09-26 04:20:40 | `node tools/sandbox/s29-scanners.mjs` | «✅ s29: all green» (26 проверок; первый прогон 04:19:58 — W4f красный по МОЕЙ фикстуре: строка вне раздела, который судит линтер требований; фикстура исправлена) |
| 4 | 2026-09-26 04:21:55 | `KAIF_DIST=<dist 4b06b28> node tools/sandbox/s29-scanners.mjs` | «❌ s29: 6 red» — W4a–W4f поимённо (первый прогон свод умирал на отсутствующем задании обновления — `staleItem` сделан устойчивым) |
| 5 | 2026-09-26 04:22:10 → 04:24:59 | `node tools/sandbox/probes/sc-mutants.mjs` | 12 OK, «BAD M5» — по МОЕМУ предсказанию: M5 (скан роняет строки обхода) законно краснит и W4a/W4b; адресаты дописаны |
| 6 | 2026-09-26 04:26:09 → 04:29:03 | `node tools/sandbox/probes/sc-mutants.mjs` | «✅ 13 mutants red exactly on their named addressees, and only on them» (M13 — сторона чтения снова бросает) |
| 7 | 2026-09-26 04:24:15 | `node scratchpad/sc/f11-probe.mjs` (одноразовый репозиторий) | отказ «источник: переменные окружения GIT_AUTHOR_NAME · …», совет «unset …», `.gitconfig` не назван; `version.json` не тронут, коммитов 1 |
| 8 | 2026-09-26 04:29:19 → 04:34:30 | `npm run test:core` (в одиночку) | «sandbox suite: all 28 suites green» |

## 4. Проверки

Гигиена:
- `s29` W4a–W4f зелёные, на `dist` до починки — 6 красных поимённо;
- мутант M13 красен на W4a/W4b; M2 перенесён на новую строку `nested`;
- `tools/commit.mjs --selftest` зелёный; семь копий блока `KAIF-WALK` равны (страж 5l — сборка зелёная).

Функциональный прогон: настоящий запрет ОС на настоящих процессах модулей и ядра (прогоны 1–2): до починки — трасса стека и ложный «✅»,
после — названный отказ. Прочитано: вывод каждой команды (выше).

РЕАЛЬНЫЙ МИР:
- **накоплено** — у полевых деревьев профили браузера и копии worktrees (форма #77);
- **данные и техника** — эта машина, Windows; настоящий запрет ОС;
- **путь** — `check` / `report` модулей и `stale-claims` ядра под запретом — проверено на реальном механизме ОС; `update` с нечитаемым
  файлом — проверено сводом через границу ФС настоящего процесса. Нечитаемых файлов в четырёх полевых деревьях сейчас нет — поле этот
  отказ не воспроизводит.

## 5. Найдено

1. Девять находок части A (F1 · F2 · F7 · F8 · F9 · F10 · F11 · F12 · F13) подтверждены и починены; F2 — ещё коммитом `4b06b28`.
   F12 проверен чтением правки (проба 1d требует приватного ядра голоса владельца — не гонялась).
2. **Моя фикстура W4f** (строка вне раздела требований) — исправлена до зачёта.
3. **Моё предсказание M5 устарело** от новых проверок W4 — уже известный класс урока «предсказания мутантов стареют от новых проверок».
4. **Свод умирал на отсутствующем задании** при прогоне против `dist` до починки — `staleItem` возвращает пустой пункт.
5. Ноль прочих.

## 6. Следы

`tools/sandbox/s29-scanners.mjs` (W4) · `tools/sandbox/probes/sc-mutants.mjs` (M13, M2, ожидания M5) · логи `scratchpad/sc/`
(`s29-sc4.log`, `s29-pre.log`, `mut-sc4.log`, `mut-sc4b.log`) · `scratchpad/tb/f1f7-probe.ps1` · `scratchpad/sc/f11-probe.mjs` · полигон
`scratchpad/ow9/polygon-close.log`.

## 7. Вердикт

**pass** — критерий 16 держится снова: нечитаемый каталог и нечитаемый файл — названный отказ у ядра и шести модулей, `update` доходит
до задания. Критерий 17 остаётся открытым до части B (F3).
