# Отчёт прогона — эпик UP 2.8, шаг UP0: входы эпика сверены с HEAD, новых тикетов нет

**Создан:** {{T}} · **Прогон вёл:** Claude Opus 5.5 (1M context), сессия 74 (ночной цикл) · **Версия/сборка:** HEAD `876caae`

## 1. Работа

Шаг UP0 `plans/122`: каждый вход эпика — адреса разведки `researches/32` §2а, снятые на `6af0dad`, — сверить с HEAD по подписи строки;
новые тикеты origin после #106; место в шаблонах и форма строк «Проверка» критериев 14–15.

## 2. Контур

Исток KAIF, Windows 11, Git Bash, Node v24, HEAD `876caae`. Проба `tools/sandbox/probes/up0-inputs.mjs` только читает
`framework/installer/KAIF-CORE.mjs`, `/kaif-update` поставки и свод `s27`.

## 3. Прогоны

| # | Момент | Команда | Исход |
|---|---|---|---|
| 1 | 2026-09-26 00:39:54 | `node tools/sandbox/probes/up0-inputs.mjs` | «GOOD — every input located (10)»: 3 совпали (`/kaif-update` «byte for byte» стр. 51; «autocrlf» в навыке — 0; случая «переименовано заранее» в `s27` — 0), 6 сдвинулись (ядро выросло: привязка пары 1431 → 1642, лог «arrives as new» 1757 → 1968, захват слота 584 → 724, пропуск отсутствующего модуля 1536 → 1748, квитанция `divergedModules` 1967 → 1976 · 2003, ворота i18n в `update-verify` 2404 → 2618), 1 «present» (запись репетиции) |
| 2 | 2026-09-26 00:40:01 | копия пробы с испорченной подписью входа 2, запуск, копия удалена | «ABSENT», «BAD — 1 input(s) ABSENT at HEAD», код 1 |
| 3 | 2026-09-26 00:40:01 | `gh issue list --state open --limit 100 --json number` | открытых 35, наибольший номер 106 — новых после #106 нет |

## 4. Проверки

Гигиена: проба покраснела на испорченной подписи. Функциональный прогон: NONE — шаг сверяет входы, поведения не меняет.
Каждое наблюдение разведки подтверждено на HEAD: пара переименования привязывается только при старом заголовке на диске; лог «the
section arrives as new» печатается для каждого ненайденного якоря; значение слота не может нести `<` и `>`; рецепт репетиции говорит
«byte for byte» без условия о концах строк; отсутствующий модуль без изменений апстрима пропускается; `update-verify` сверяет недовлитое
только на непереведённом развёртывании.

## 5. Найдено

Ноль расхождений с планом, кроме адресов строк (ядро выросло за эпики CK · OW · CH) — план адресов не нёс.

## 6. Следы

`tools/sandbox/probes/up0-inputs.mjs`; вывод — в этом отчёте.

## 7. Вердикт

**partial** — шаг сверки выполнен; функционального прогона шаг не требует.
