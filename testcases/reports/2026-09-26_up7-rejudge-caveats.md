# Отчёт прогона — эпик UP 2.8, шаг UP7: каверзы повторного суда починок

**Создан:** 2026-09-26 03:45 +03:00 · **Прогон вёл:** Claude Opus 5.5 (1M context), сессия 74 (ночной цикл) · **Версия/сборка:** HEAD `25d67fb` + рабочее дерево

## 1. Работа

Повторный суд починок UP6 (бриф `reports/KAIF_AUDIT/2026-09-26_up6b_fixes_judge_brief.md`, чистый экземпляр) — **VERIFIED WITH
CAVEATS**: 13 заявлений из 13 держатся (R1 · R2 · R3 и каверзы F4–F17; числа коммита и отчёта сходятся; 14 мутантов на адресатах;
прогон на копиях полевых деревьев воспроизводится), десять каверз C1–C10 и одна находка вне предмета (`bugs/124` — личность git,
починена отдельным коммитом `a6910bd`). Каверзы починены:

- **C1** — два соседних с R1 сообщения лога («rename anchor not found … arrives as new», «may arrive DOUBLED») на файле, который
  сливается без записи, теперь говорят, что ничего не вставлено.
- **C2** — половина «переведённый файл только НАЗВАН» стережётся: случаи N5 (список квитанции) и N6 (путь поля — файл судится сам).
- **C3** — ЗАГРУЗЧИК судит названный `--rehearsal` (есть, JSON, тот же интервал, подпись = скачанному ядру) ДО записи ядра: отказ больше
  не оставляет новое ядро под старым маркером (тонкий `KAIF.md` — +21 строка загрузчика).
- **C4** — `--render` вписывает ручные заполнения развёртывания так же, как обновление.
- **C5** — заметка 2.8 называет маршрут: на `update` проход ведёт уходящее ядро 2.7, на пути загрузчика — ядро 2.8 целиком.
- **C6** — нечитаемая авто-запись репетиции убирается; названная запись другого интервала — отказ.
- **C7** — `--render` без `--source` — отказ с подсказкой; анонимному развёртыванию не рендерится навык, которого установка там не пишет.
- **C8** — два пункта плана UP6 описывали починки, которые построены иначе, — исправлены на месте с пометкой.
- **C9** — E2 судит и счёт провалов (+1).
- **C10** — «краснеет» в плане: невлитая строка изменённого модуля — НАЗВАНА (предупреждение, замысел bug 17), красный — только новый
  раздел; исправлено на месте.

## 2. Контур

Исток KAIF, Windows 11, Git Bash, Node v24; своды `s27` · `s21` на развёртываниях из `dist`; загрузчик — из `framework/installer` (шов
`KAIF_LOADER` — мутант загрузчика); мутанты — копии `dist`.

## 3. Прогоны

| # | Момент | Команда | Исход |
|---|---|---|---|
| 1 | 2026-09-26, до 03:17:32 | правки ядра и загрузчика; `node tools/build-framework.mjs` | код 0; тонкий `KAIF.md` 248 строк (было 227) |
| 2 | 2026-09-26 03:17:32 → 03:18:15 | `s27` · `s21` | с первого прогона зелёные: `s27` 26 проверок (+G2), `s21` 71 (+N4b ×2 · N4c · N5 ×2 · N6 ×2 · A5b · A6 ×2; E2 судит счёт) |
| 3 | 2026-09-26 03:19:02 → 03:28:14 | `node tools/sandbox/probes/up-mutants.mjs` (20: прежние 14 и M14–M19; M17 — мутант загрузчика) | «mutant proof FAILED: 3 of 20» — снова предсказания, не код: J3 теперь краснит и E2 (E2 судит счёт с C9), M8 (без рендера) краснит и N4b (рендер с заполнениями), M18 каскадом краснил вторую половину A6 в той же фикстуре; адресаты J3 и M8 дополнены, A6 разнесён на две фикстуры (A6 · A6b) |
| 3a | 2026-09-26 03:29:28 → 03:38:45 | `node tools/sandbox/probes/up-mutants.mjs` | «20 mutants red exactly on their named addressees, and only on them» |
| 4 | 2026-09-26 03:39:05 | `s27` · `s21` с `KAIF_DIST=<v2.7>` | красны поимённо: `s27` 6 (F1 ×2 · F2 ×2 · G · G2), `s21` 18 (прежние и N4b · N4c · N5 · N6 · A6 · A6b); A5b на v2.7 зелёный по построению — он судит НОВЫЙ загрузчик (своды берут его из `framework/installer`), его красный — мутант M17 |
| 5 | 2026-09-26 03:40:03 → 03:45:25 | `npm run test:core` (в одиночку) | «sandbox suite: all 28 suites green» |

## 4. Проверки

Гигиена: по случаю свода на каверзу, по мутанту на починку. Функциональный прогон: NONE — шаг чинит каверзы суда, путь поля тот же, что
у UP6 (прочитан там на копиях двух полевых деревьев); новые ветви (отказ загрузчика, рендер с заполнениями, отказ `--render` без
источника, записи репетиции) исполнены сводом на развёрнутых копиях.
РЕАЛЬНЫЙ МИР: накоплено — у поля записи репетиции прошлых ядер, ручные заполнения, переведённые файлы — проверено с реальным
состоянием, снятым с реального (формы полевых записей и заполнений из тикетов #42/#48 и копий деревьев UP6); данные и техника — эта
машина; путь — первое обновление поля 2.7 → 2.8 после выпуска.

## 5. Найдено

1. Десять каверз повторного суда — починены (C1–C10).
2. Три ошибки предсказания адресатов (прогон 3) — исправлены с причинами до зачёта (прогон 3a).
3. Тонкий `KAIF.md` вырос на 21 строку (227 → 248) — проверка загрузчика; решение №8 держит точку входа тонкой (без поставки внутри), рост — сама проверка.
4. Ноль дефектов вне этого.

## 6. Следы

`framework/installer/KAIF-CORE.mjs` · `framework/installer/KAIF-LOADER.mjs` · `tools/build-framework.mjs` (заметка) · записка §10.7 и
§10.8 · `plans/122` · своды `s27` · `s21` · проба `up-mutants` (20 мутантов, `loader: true`); логи — `scratchpad/sc/up7-*.log`,
`scratchpad/sc/upmut3.log`; вердикт повторного суда — `scratchpad/judge-up6b/verdict.md`.

## 7. Вердикт

**partial** — каверзы починены и доказаны сводом и мутантами; первое обновление поля 2.7 → 2.8 — после выпуска.
