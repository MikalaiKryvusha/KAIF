# Ядро голоса и мышления владельца KAIF — публичный слепок

> ⚠️ **ЭТОТ ФАЙЛ СГЕНЕРИРОВАН — руками не правь.** Источник — приватное ядро голоса владельца; сборка —
> `node tools/stylometry-snapshot.mjs`. Правка мимо генератора будет затёрта следующей пересборкой, а расхождение
> поймает `--check`. Правило то же, что у `KAIF.md`: правь источник, пересобирай.

## Что это и откуда

| Поле | Значение |
|---|---|
| **Что это** | Слепок ядра голоса и мышления Николая Кривуши (Кот Криник) — автора KAIF: инструкция агенту, который пишет текст за владельца или для него. Правила ядра едут целиком; что не едет — раздел «Граница» ниже. |
| **Источник** | Приватное хранилище `MikalaiKryvusha/krinik-stylometry` (локально `d:\work\krinik_voice`), файл `AUTHOR_STYLOMETRY.md`. |
| **Момент слепка** | Коммит источника `{{SOURCE_SHA}}` от `{{SOURCE_DATE}}`. |
| **Версия ядра** | {{CORE_VERSION}} |
| **Состояние** | Ядро 2.2 принято 2026-09-25 по мандату владельца — его слова в тикете истока #103: «сделай стилометрическое ядро, как считаешь нужным», «без моей вычитки». Шапка самого ядра ниже ещё называет версию «черновик до слова автора», а файл версии хранилища — `accepted`: расхождение хранилища, названное здесь, а не исправленное (правится только в источнике). Слепое сравнение хранилища: текст по ядру 2.2 ближе к владельцу, чем без ядра, в 22 парах из 24 и ближе, чем по ядру 1.2, в 15 из 24 — замер хранилища, в KAIF не переисполнялся. |
| **Счётчики слепка** | правил — **{{RULES}}** · фраз владельца, уже публичных в этом репозитории, — **{{PUBLIC_SPANS}}** · схлопнуто спанов (уникальных) — **{{ELIDED}}** · образцы §2.6: едут — **{{SAMPLES_KEPT}}**, сняты — **{{SAMPLES_DROPPED}}**. Числа печатает генератор. |

## Граница — что сюда не едет и почему

Решение владельца **№60** (интервью №014, Q1 = B) в силе: **правила едут целиком, непубличные фразы владельца — нет.** KAIF —
публичный репозиторий под MIT, а ядро приводит внутри правил живые фразы владельца из переписки и текстов его проектов.
Генератор режет по двум осям, и ни одна из них не является суждением модели в момент сборки:

1. **Фраза в кавычках едет, только если она не слово владельца** — шаблон формы («Если …, то …»), оборот словника, пример машинного
   текста; каждая такая запись стоит в конфиге генератора с причиной, — **или уже публична в этом репозитории**: файл, который её
   содержит, проверяет код. Всё прочее схлопнуто в `«…»`.
2. **Образец руки автора (§2.6) — целый текст владельца**: едет, только если публичен в этом репозитории (каждую его строку находит
   код); иначе на его месте стоит строка «непубличный текст владельца» с адресом образца в приватном ядре.

**Приватные слои хранилища сюда не едут никогда** — рабочий слой `AUTHOR_STYLOMETRY_WORK.md` (формы и образцы рабочих жанров) и
модуль прозы `AUTHOR_STYLOMETRY_PROSE.md` (дословная проза автора); ядро ссылается на них для приватных проектов автора. Приёмка
сборки сверяет каждое окно из восьми слов слепка с обоими слоями и отказывает при совпадении.

Что это значит для агента: у правила, чей пример схлопнут, пример открывается в приватном ядре, а не додумывается.

## Как этим пользоваться

- **Пишешь текст, который владелец подписывает или читает как свой** — до первого слова `node framework/tools/kaif-voice-lint.mjs load`
  (разделы для письма в рабочий контекст; для эссе — `load --genre essay`), после письма — `node framework/tools/kaif-voice-lint.mjs check
  <файл…> --genre <жанр>` (строки §8 с меткой `[работа]` или `[документ]` молчат на чужом жанре) и проход §7
  чистым экземпляром (четвёртое обязательство KAIF, `AGENT_GUIDE.md`).
- **Проекты владельца получают этот слепок при обновлении KAIF на 2.8 заменой, а не слиянием** — слово владельца (интервью 039, Q4
  полевого проекта; тикет истока #103).
- **Расхождение версий констатируется, ничего не требуя** (решение №62): `/end-chat-soft` и `/release` печатают строку, если слепок
  отстал от ядра. Этот файл — зеркало, а не истина: правило меняется в приватном ядре и приезжает сюда пересборкой.

---
